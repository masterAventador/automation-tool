(() => {"use strict";const number=(value,fallback)=>{const parsed=Number.parseFloat(value);return Number.isFinite(parsed)?parsed:fallback};const style=(opacity=0.72,blur=18)=>Object.freeze({background:`rgba(255,255,255,${Math.max(0,Math.min(1,opacity))})`,backdropFilter:`blur(${Math.max(0,Math.min(48,blur))}px)`,border:"1px solid rgba(255,255,255,.28)",boxShadow:"0 18px 48px rgba(15,23,42,.18)"});class Canvas{constructor(canvas){if(!canvas||typeof canvas.getContext!=="function")throw new TypeError("canvas required");this.canvas=canvas;this.isReady=true}waitForInit(){return Promise.resolve(this.isReady)}renderGlassElements(){if(!this.isReady)return;const context=this.canvas.getContext("2d");if(!context)return;const canvasRect=this.canvas.getBoundingClientRect();for(const element of this.canvas.querySelectorAll(".liquid-glass")){const computed=getComputedStyle(element);const rect=element.getBoundingClientRect();const width=rect.width||number(computed.width,0);const height=rect.height||number(computed.height,0);if(width<=0||height<=0)continue;const left=rect.width?rect.left-canvasRect.left:number(computed.left,0);const top=rect.height?rect.top-canvasRect.top:number(computed.top,0);const radius=Math.max(0,number(computed.borderRadius,24));context.save();context.globalAlpha=Math.max(0,Math.min(1,number(computed.opacity,1)));context.beginPath();if(typeof context.roundRect==='function')context.roundRect(left,top,width,height,radius);else context.rect(left,top,width,height);const fill=context.createLinearGradient(left,top,left+width,top+height);fill.addColorStop(0,"rgba(255,255,255,.34)");fill.addColorStop(1,"rgba(174,216,255,.12)");context.fillStyle=fill;context.shadowColor='rgba(15,23,42,.24)';context.shadowBlur=24;context.shadowOffsetY=12;context.fill();context.shadowColor='transparent';context.lineWidth=1.5;context.strokeStyle='rgba(255,255,255,.55)';context.stroke();context.restore()}}destroy(){this.isReady=false}}const api=Object.freeze({Canvas,style});Object.defineProperty(globalThis,"NeutralGlass",{value:api,writable:false});})();
