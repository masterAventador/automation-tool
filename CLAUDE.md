# 自动化运营工具项目规则

## 1. 必读基线

每个新会话首次进入本项目时，必须完整阅读以下文档：

- `docs/dt-ai-helper-competitive-analysis.md`：竞品事实、证据等级和风险；
- `docs/product-plan.md`：产品范围、优先级、MVP 和验收标准；
- `docs/project-structure.md`：目录、文件归属和依赖方向；
- `docs/frontend-architecture.md`：Tauri、React、Rust、网络桥和本地执行器桥接；
- `docs/backend-architecture.md`：Control Plane、Local Executor、RPA、协议、数据和部署；
- `docs/development-roadmap.md`：里程碑、依赖、失败矩阵、完成定义、任务状态和当前下一步；
- `docs/development/<任务ID>.md`：单个任务的 RED、GREEN、真实边界、失败矩阵、清理、文档和遗留证据。会话恢复时只读当前任务文件，禁止批量加载历史任务记录。

如果代码、临时方案或其他文档与这些基线冲突，必须先说明冲突及影响，再决定是否修改基线。不能在未更新文档的情况下悄悄引入第二套架构。

## 2. 产品与阶段边界

当前优先级固定为：

```text
RPA 运营 > 内容生产与分发 > AI 员工与工作流
```

P9 以前的第一期是本地单设备 MVP：

- 暂不做产品注册、登录、用户、组织、租户、RBAC、套餐和账号中心；
- App 打开后直接进入 RPA 运营工作台；
- 单机、单用户、单任务执行器，不依赖云端账号即可运行首条闭环；
- 社交平台本身的登录态不等于本产品账号体系。抖音、小红书等仍需平台授权或扫码登录；
- 先完成一个真实平台、一个真实任务、一个完整恢复链路，再扩平台和扩功能；
- 不为了未来 SaaS、多租户或 AI 中台需求提前建设无当前用户价值的底座；
- 任何客户 Demo 交付前必须完成路线图 U9-01～U9-06 的产品账号登录、Session、恢复、停用和账号设备归属；登录后设备自动绑定当前账号，不使用匿名设备申请、配对码、轮询或后台逐设备审批。

范围、顺序或完成状态变化时，必须在同一任务中同步更新产品规划和开发台账。

### 2.1 开发台账同步（强制）

`docs/development-roadmap.md` 是任务定义、依赖、状态和当前下一步的唯一事实源；`docs/development/<任务ID>.md` 是对应任务执行证据的唯一事实源：

专项 Roadmap 例外：`docs/embedded-browser-video-studio-roadmap.md` 是其中 87 个内置浏览器、Browser Use、视频制作、视频剪辑和首期发布任务的唯一状态台账，只保留任务、依赖和当前状态；这些任务不得向 `docs/development-roadmap.md` 双写状态，也不得把完成记录不断追加到专项 Roadmap。每个专项任务必须单独使用 `docs/development/<任务ID>.md` 记录日期、提交、RED、GREEN、失败矩阵、正常用户路径验收或不适用原因、真实边界、清理和文档证据，并与代码及状态变更在同一提交完成。专项以外的任务仍遵守上一段规则。

**台账的唯一用途是跟踪进度，所以它的状态必须随时是对的。任务验收通过后，第一件要做的事就是更新台账——排在写总结、向用户汇报和开始下一个任务之前。**

- 顺序不可颠倒：验收通过 → 立刻把台账状态改对 → 才轮到总结、汇报、下一个任务。对外汇报的内容必须与台账一致，不允许「嘴上说完成、台账还挂着未开始」；
- **说某一项的状态之前，先找到它此刻的证据在哪。** 台账是证据的索引，不是证据本身。已经出过事故：三个任务实际早已完成（服务端数据、磁盘文件、真实出片都在），台账仍写着「等用户做」，于是被逐字复述给用户当成事实；
- 因此：**找不到证据的「未完成」和找不到证据的「已完成」一样不可信。** 复述台账不算核对状态；
- 发现台账与现实不符时，当场查清现实并立即改正台账，不推迟、不批量补；
- 已完成的任务不得继续占着「未收口」区——那会让剩余量虚高，直接违背跟踪进度这个目的；
- 台账里同一个数字不许存在两处（小节标题的计数、正文里的合计都算），计数由 `scripts/check_demo_roadmap_counts.py` 守，改完台账必须跑一次；
- 开始任何小任务前，先检查前置依赖，并把对应行更新为 `🧪 RED` 或 `🚧 实现中`；
- 同一时间最多一个小任务处于 `🧪 RED` 或 `🚧 实现中`；
- 每个小任务开始时创建或更新独立的 `docs/development/<任务ID>.md`，记录 RED；完成后立即把路线图状态更新为 `✅ 已完成`，并在该独立文件中写入日期、提交、GREEN、真实边界、失败矩阵、清理和文档证据；
- 禁止把任务完成记录、测试命令明细或历史证据追加回 `docs/development-roadmap.md`；
- 代码、测试、迁移、配置和台账更新必须属于同一个小任务和同一个提交，禁止代码先合并、台账以后补；
- 禁止把多个已完成任务攒在一起批量更新状态；
- 任务只通过 Mock 或仍缺真实平台、桌面、安装包或部署门禁时，最多标为 `🔍 待验收`，不得标记完成；
- 真实进度与台账不一致时，任务视为未闭环；先核对代码、测试和实际环境，再立即修正台账；
- 依赖、范围或完成定义变化时保留历史任务，写清原因和替代关系，不能删除未完成项或降低门禁来制造进度；
- 每次会话恢复、上下文压缩或中断后，先检查 Git、运行进程、本路线图“当前下一步”和当前任务的独立记录，再继续写代码；不得为了恢复上下文读取全部历史任务文件。

## 3. 工程结构基线

项目使用单一 Git 仓库，业务代码按以下边界组织：

```text
automation-tool/
├── frontend/                 # React + TypeScript + Vite，共享 UI 与 Tauri
│   ├── src/
│   ├── e2e/                  # Playwright 测试专用 UI Harness E2E
│   ├── e2e-tauri/            # WebdriverIO 真实 Tauri E2E
│   └── src-tauri/            # Rust 原生桥接、权限、Sidecar 生命周期和打包
├── backend/                  # Python 可部署控制面、本地执行器和测试
├── contracts/                # OpenAPI、事件 Schema、跨语言样例
├── docs/                     # 产品、架构、路线图与 ADR
├── scripts/                  # 跨工程生成、检查和打包脚本
└── .local/                   # 本地运行和测试数据，禁止提交 Git
```

强制规则：

- 根目录不混放前端或后端业务代码；
- 前后端各自维护依赖、测试和构建配置；
- Python/Pydantic 是控制面 REST、任务事件和本地执行协议的唯一来源，生成的跨端契约禁止手工维护第二份；
- 运行数据、浏览器 Profile、Cookie、截图、聊天、素材、数据库和日志不得提交 Git；
- 依赖版本必须通过清单和锁文件固定，禁止同时维护多个包管理器锁文件。

## 4. 技术架构基线

### 4.1 App

- Tauri v2 是正式桌面容器；
- React + TypeScript + Vite 构建业务 UI；
- Ant Design 作为基础组件体系；
- TanStack Query 管理服务端状态，Zustand 只管理必要的纯客户端状态，Zod 校验运行时边界；
- 业务页面不得直接导入 `@tauri-apps/*`，不得访问 `window.__TAURI__`，所有原生差异经过 `PlatformAdapter`；
- 产品只有 Tauri 桌面客户端，不建设、不部署、不交付 Web 产品；
- P9 本地 Profile 仍直接进入工作台；客户 Demo Profile 在 U9 完成后必须先通过产品账号登录和账号所属 Installation 双门禁，未登录不得挂载业务工作台；
- Vite 浏览器运行模式只作为本机开发与 Playwright 使用的测试专用 UI Harness，禁止演变成用户入口或生产部署单元。

### 4.2 Python Control Plane

- Python + FastAPI + Pydantic 实现独立业务后端，负责产品 API、任务、事件、配置和以后内容/AI/工作流；
- Control Plane 从第一天就是独立进程和独立部署单元，不随 Tauri 安装包分发；
- Control Plane 不得直接依赖 Tauri、Playwright、微信窗口、用户本机路径或平台 Cookie；
- 开发时在本机启动，App 的 `baseUrl` 指向 loopback；客户 Demo 使用同一服务和迁移部署到云端，App 只切换受控 `baseUrl` Profile；
- 数据库从第一天使用 PostgreSQL，开发使用本机 Docker，Demo 使用服务器 PostgreSQL；
- 云端客户 Demo 必须使用产品账号认证，并把 Installation/设备凭据绑定到已登录账号；账号或设备任一失效都拒绝业务访问，禁止裸露匿名写接口；
- local/demo 只允许配置、凭据和基础设施不同，禁止维护两套业务实现。

### 4.3 Python Local Executor

- Local Executor 永远运行在用户电脑，负责 Playwright、浏览器 Profile、微信、OCR、本地文件和真实外部副作用；
- Executor 以 Tauri Sidecar 形式随 App 打包，不要求用户另装 Python；
- Tauri 负责 Executor 启动、握手、健康检查、崩溃恢复、停止和安装包生命周期；
- Executor 通过版本化执行协议与 Control Plane 协作，不直接承载产品页面、用户或工作流 API；
- 开发阶段连接本机 Control Plane；客户 Demo 通过出站受认证连接连接远程 Control Plane；
- 每次本地握手使用新的高熵会话令牌，令牌不得出现在命令行、环境变量、日志或前端持久存储中。

### 4.4 任务与事件

- 任务快照是刷新和重启恢复的权威来源，实时事件只构建前端投影；
- 状态转换必须显式、可测试，非法转换一律拒绝；
- 所有事件携带任务 ID、单调序号、时间、事件版本和脱敏安全消息；
- 外部副作用必须有幂等键；如果无法确认平台动作是否已成功，不得自动重放，必须进入结果不确定或人工接管；
- 暂停、取消和紧急停止是协作式控制，只有执行器确认终态后才能宣称副作用已经停止。

## 5. 浏览器与登录态规则

浏览器主方案固定为：

```text
App 安装包内与 Playwright 锁定版本严格匹配的内置 Chromium
        +
App 管理的独立运营 Profile
        +
可见、可人工接管的独立运营窗口
```

强制规则：

- 用户电脑无需预装 Chrome 或 Edge；生产运行时只接受 Tauri Resources 中经 Manifest、版本、平台、架构和逐文件摘要验证的内置 Chromium，不发现、选择、下载或回退到系统浏览器；
- 不在 Tauri WebView 内嵌抖音、小红书等运营网页；
- 不直接自动化用户日常 Chrome/Edge 的默认 Profile；
- 不读取、复制或上传用户默认浏览器 Cookie；
- App 从未发布，首期直接创建全新 App 私有运营 Profile，不迁移开发期 Profile、系统浏览器选择或 Cookie；
- 每个平台首次扫码或登录一次，后续复用 App 独立 Profile 中的平台登录态；
- 浏览器必须默认有界面，用户能看见实际操作并随时人工接管；
- 验证码、滑块、风险提示、异常登录和平台安全校验只能暂停并转人工，禁止实现绕过；
- MVP 不做 stealth、指纹伪装、验证码识别或隐藏自动化；
- 登录状态、Profile 路径和执行任务必须隔离，注销时按明确流程停止任务并清理对应平台会话；
- 将来若增加浏览器扩展接管当前标签页，只能作为用户主动启用的可选模式，不能替代独立 Profile 的稳定主链路。

## 6. 模块和第三方边界

- 平台适配器以稳定接口隔离抖音、小红书、快手、视频号和微信，不在业务服务中散落选择器；
- 页面定位、动作编排、频控策略和结果解析分层，页面改版不能迫使重写任务引擎；
- 内容渲染通过 `VideoRenderProvider` 隔离云端或本地实现；
- AI 模型通过供应商无关 Adapter 接入；第一期 RPA 闭环不得依赖 AI 调用才能运行；
- 禁止修改 Playwright、Tauri、FastAPI、LangGraph、模型 SDK 等第三方源码，禁止 Monkey Patch、私有 Fork 或复制内部实现；
- `vendor/moneyprinterturbo` 与 `vendor/hyperframes` 只允许按 `contracts/quality/third-party-sources.v1.json` 的正式 tag/完整 commit 作为只读 Submodule 存在；禁止在 Submodule 内修改、跟随 branch/latest、运行时更新或绕过 `scripts/check_third_party_sources.py`。升级必须使用独立任务并同步 Gitlink、许可证摘要、资产权利清单和 SBOM；
- 用户界面中的两种视频制作方式只允许显示“智能素材成片”和“品牌动效成片”；两个上游项目名只能出现在代码、内部诊断、SBOM 和独立第三方软件声明页，禁止进入菜单、卡片、按钮、帮助、错误、日志、任务、无障碍名称、文件名或导出。用户文案使用 `contracts/quality/user-facing-terminology.v1.json` 的通俗映射，禁止出现未解释的 `B-roll`、`PoC` 等术语，并运行 `scripts/check_user_facing_branding.py`；
- 内置浏览器、Browser Use、视频制作和剪辑必须遵守 `contracts/security/embedded-browser-video-threat-model.v1.json`：随机 loopback Worker、生成 HTML、素材下载、密钥、路径和名称泄漏六类边界均默认拒绝并做失败矩阵；
- 遇到上游问题依次使用公开扩展点、外围适配、锁定可用版本和提交上游 Issue/PR。

## 7. 安全与隐私

- 平台会话和原始证据默认本地存储；Control Plane 只采集业务闭环必需的最少数据并设置最短保留；
- Cookie、Token、平台消息、联系人、页面原文和本机私有路径不得进入普通日志或错误响应；
- 设备私钥、长期设备凭据及产品账号 access/refresh secret 只允许进入 Tauri `app_data_dir` 下由 Rust 管理的固定 App 私有文件；不调用系统钥匙串，不向 React 或 Tauri Command 返回，不进入 `localStorage`、普通配置、日志或错误响应；
- 截图和 Trace 只在用户开启诊断或任务失败时按需保存，必须有数量、大小和保留期限上限；
- 平台动作必须有任务级和每日硬上限、最小间隔、黑名单、目标去重、连续失败熔断和一键紧急停止；
- 默认使用测试账号和自有/授权目标验收，不得用真实第三方用户做骚扰式测试；
- 模型输出、网页内容、文件名和外部输入一律视为不可信数据，不能直接拼接为 Shell、路径、SQL 或浏览器执行指令；
- Tauri Capability、CSP、Sidecar 权限和文件访问遵循最小权限。

## 8. 开发和测试规则

所有代码变更遵循测试驱动开发：

1. 先写能证明目标行为且当前会失败的测试；
2. 运行并确认失败原因正确；
3. 写满足行为的最小实现；
4. 运行相关测试进入 GREEN；
5. 重构后运行受影响回归。

全局采用“生产同路径验收”原则：每个对外接口和用户功能只有通过生产中原本的调用入口、正式适配层与真实依赖完成验收，才算通过。直接调用下层函数、绕过正式入口的脚本、Mock、Fake、Test Harness、进程内客户端或日志断言只能作为分层测试，不能替代该接口或功能的完成验收；若真实调用链尚未实现，相关跨端或用户功能任务最多标记 `🔍 待验收`，并把补验收依赖写入台账。测试仍须保留更低层单元、契约和故障注入覆盖，不能用端到端测试替代失败定位能力。

桌面端采用四层测试门禁：

| 层级 | 工具 | 负责范围 |
| --- | --- | --- |
| 单元与组件 | pytest / Vitest / Testing Library | 领域逻辑、状态机、Schema、组件和适配器契约 |
| UI Harness E2E | Playwright | 测试专用 React 页面中的点击、输入、提交和业务投影；原生能力使用受控测试 Adapter |
| Tauri 原生逻辑 | `cargo test` | Rust Command、权限、Sidecar 生命周期、错误转换 |
| Tauri 桌面 E2E | WebdriverIO + Tauri Service | 真实 App、WebView、IPC、窗口和代表性原生流程 |

- 凡正式 Tauri App 会调用的 Control Plane 接口，Mock/Test Harness 只能证明单元和 UI 投影，不能作为跨端完成依据；必须由真实测试版 Tauri App 经正式 Rust 网络桥发出请求，连接真实本地或隔离后端，验证认证注入、请求、响应解析、错误映射和界面结果；
- 后端端点可在对应服务端任务完成真实 PostgreSQL/FastAPI 门禁，但最迟必须在网络桥或消费该端点的桌面任务补齐上述真实 App 纵向验收；缺少该证据时，跨端任务不得标记完成；

此外：

- 启动前端、后端、RAGFlow、Docker Compose、测试服务器或任何其他本地监听服务前，必须先检查计划使用的每一个目标端口是否已被占用；发现占用时不得终止、复用或接管来源不明及其他项目的进程，必须改用本项目专属空闲端口并同步调用方配置；
- 本项目所有本地运行和测试资源必须显式使用 `automation-tool` 专属且可追溯的 Compose project name、容器名、网络名、Volume 名、SQLite/状态文件路径和端口段，禁止依赖 Compose 默认项目名、复用其他项目资源或写入通用临时路径；并行测试实例还必须追加本次运行的唯一隔离标识；
- 本地服务启动后必须核对实际监听端口和资源归属，测试结束只清理本次 `automation-tool` 隔离实例创建的进程、容器、网络、Volume 和数据，禁止清理其他项目资源；
- RPA 完成必须有受控平台账号的真实最终状态验证；DOM 可定位、按钮被点击、日志写“成功”或 Mock 返回成功都不能单独算完成；
- 真实平台账号、扫码或人工安全校验暂时不可用时，不停下来等待用户：先用自建测试网页、隔离平台 Adapter 和无外部副作用的自动化把其余实现与失败矩阵完成；仅把必须验证平台最终状态的验收项标为 `🔍 待真实账号` 并登记补验收证据，然后自动继续下一个前置已满足且不依赖账号的任务；测试页或 Fake 证据不得冒充真实平台验收；
- UI Harness 通过只能证明 React 业务交互，不能证明浏览器 RPA、微信、IPC、系统权限或 Sidecar 生命周期可用；
- macOS 和 Windows 的正式包、权限和内置 Chromium 链路分别验收；平台专属功能不能用另一平台的单元测试代替；
- 正式安装包必须确认不含 WebDriver、调试端口、测试凭据和测试命令；
- 测试数据与本地演示数据隔离，测试结束清理临时服务、浏览器和 Profile；
- 已建立的 E2E 用例长期保留，产品行为变化时同步更新。
- 自动化测试启动 Tauri App 时必须使用测试专用隐藏窗口配置在后台运行，不得抢占前台、弹出窗口或干扰用户当前电脑操作；正式产品构建仍保持正常可见，隐藏测试配置不得进入正式包。
- 本机自动化测试启动浏览器时必须默认使用无头模式；只有真实扫码、人工安全校验或专门验证可见产品窗口且无法由无头模式替代时，才允许显式启用有头浏览器，并须提前告知用户。CI 的独立桌面 Runner 可以执行明确标记的可见浏览器验收，但常规本机全量门禁不得隐式触发它。
- 用户可操作功能只有从正式 App 的正常用户入口按真实页面路径执行，并核对真实进程、文件或外部平台最终状态，才算验收通过；Mock、fixture、单元测试、UI Harness、直接内部 API/函数/Command 或测试专用入口只能作为分层证据。缺少真实账号、密钥或正式环境时最多标 `🔍 待验收`，但继续推进不依赖该证据的任务；
- 每个浏览器测试必须由确定的资源所有者在成功、失败、超时和取消路径关闭 Page、Context、Playwright driver 及其完整浏览器进程树；测试结束后复查本次项目隔离 Profile/进程无残留，只能清理本次 `automation-tool` 实例创建的资源，禁止积累后台浏览器或误杀用户及其他项目浏览器。

### 8.1 Worktree 一律用 `scripts/new_worktree.py` 创建

**创建 worktree 必须走 `python3 scripts/new_worktree.py <名称> [提交]`，禁止手工 `git worktree add` 之后自行拼装 vendor、venv 与 node_modules。**

`git worktree add` 留下的 `vendor/` 是空目录，而手工补齐它有三条路，其中两条会造成事故——这些都是 2026-07-26 实测出来的：

- **软链**：`motion_style_freezer.py:105` 显式要求 vendor 根是真实非软链目录并直接拒绝。这是有意的供应链控制：vendor 的可信度建立在「这个目录**就是**那个锁定 commit 的检出」上，而软链可以指向任何地方；
- **硬链（`cp -al`）**：共享 inode，一棵树里的写入会**穿透到另一棵树**。实测后果是主树 submodule 被检出成错误 commit 并留下 155 条暂存删除，而当时被误判成「共享 `.git/modules` 竞争」——核查后发现 git 早就给每棵 worktree 独立的 module 目录，污染源是硬链本身；
- **`git submodule update --init`**：正确但要重新落地 879 MB，其中 581 MB 过 git-lfs smudge，实测超过十分钟，且必须串行。

脚本走的是第四条：APFS `clonefile`（`cp -c -p -R`）。实测 **879 MB 用 0.9 秒、占 2 MB**，产出真实目录、独立 inode，写入互不影响；不支持 clone 的文件系统自动退回普通拷贝，只慢不错。

两个必须由脚本负责、手工极易漏掉的细节：

1. **每棵 worktree 的 submodule 元数据目录要各自一份**，并把 `core.worktree` 改写成绝对路径。拷贝过来的 `config` 仍带源树的相对 `core.worktree`，此时 `git config`（无论 `cwd=` 还是 `--git-dir=`）都会先 chdir 过去而失败 —— 只有 `git config --file` 不打开仓库，能改掉这一项；
2. **`core.checkStat` 必须设为 `minimal`**。clone 换了 inode 会让 index 的 stat 缓存失效，git 于是逐个重读文件并跑 clean 过滤器；对于 blob 以内容而非指针形式存储的 LFS 路径，这个来回不还原原 blob，**68 个没动过的文件会报成 modified**，足以让 `check_third_party_sources.py` 判整棵树为脏而拒绝。`minimal` 比较 mtime 与大小、忽略 inode。

判据是**门禁而不是形态**：树建完必须 `scripts/check_third_party_sources.py` 退出 0。上面那条 stat 缓存问题在结构检查（不是软链、不共享 inode、gitlink 正确）全绿的情况下依然存在，只有真跑门禁才暴露。

不需要 vendor 的工作线（例如只看前端界面的）用 `--no-vendor`，那是最快也最省的做法。

**但「需不需要 vendor」要按构建去判，不是按你打算跑什么去判。** 2026-07-29 用
`--no-vendor` 建树跑一批桌面验收，理由是「这批 spec 不读 vendor」——读的不是 spec，
是**执行器包的构建**：它的 PyInstaller spec 把 `vendor/hyperframes` 下的文件当资源
打进去，于是十六条驱动全部死在 `the Executor spec package resource is missing`。

#### 建完树先跑一条冒烟，再铺开

同一天同一件事上连错三处，全部是「凭印象判断依赖」而不是去看脚本怎么做的：

| 假设 | 实际 |
| --- | --- |
| 内置浏览器在 `~/Library/Caches`，跨树共享 | 机器缓存里是**锁定归档**；解包后的分发落在每棵树自己的 `.local/desktop-e2e/`，新树是空的 |
| 这批 spec 不读 vendor，可以 `--no-vendor` | 见上，读它的是执行器包的构建 |
| （压根没想过）新树是当前分支 | **`new_worktree.py` 默认基于 `origin/main`**，要当前代码必须显式传提交：`new_worktree.py <名称> HEAD` |

第三条最要命：环境就算是好的，**验的也是没有本次改动的旧代码**——全绿说明不了任何事，
全红还会把人引去查不存在的问题。

所以：**建完树先单跑一条最便宜的验收，确认它真能通，再放其余的。** 一次押上十六条
的代价是十六次启动全部空转，而冒烟只要一次。判据不是「树建好了」，是「这棵树上有
一条真跑通了」。

缺的东西用 APFS clone 从主树搬（`cp -c -p -R`，`.local/desktop-e2e` 1.5 GB 实测 0.65 秒）。
可信度来自使用时的摘要校验，不来自「谁下载的」——与 PC-16 处理离线字体目录同一个理由。

### 8.2 派子代理并行开发前，先给它建一棵 worktree

**只要子代理会写文件，就必须先 `python3 scripts/new_worktree.py <名称>`，再把那棵树的路径作为它的工作目录派下去。** 不建树就派，等于让两条工作线在同一棵树上同时写。

**不要用 Agent 工具自带的 worktree 隔离参数**：它做的是朴素的 `git worktree add`，留下空的 `vendor/`——而任何要读 `vendor/hyperframes` 或 `vendor/moneyprinterturbo` 的任务在那棵树里直接跑不起来，见 §8.1。需要 vendor 就用脚本建，不需要就 `--no-vendor`。

**这条规则来自 2026-07-27 的实测代价。** 当天把中文字体任务派给子代理时没有隔离，两条线在同一棵树上并行，具体后果是：

- **全量测试的结果不可信。** 主线跑 `pytest backend/tests/unit` 报出一条红，需要人工判断它属于谁——实际是子代理把 `pyproject.toml` 的 `default-groups` 从 `["dev", "executor"]` 改成三项后撞上了一条既有断言。**这类归属判断每次都要花时间，而且判错一次就会去"修"别人正在做的事**；
- **提交必须逐个文件列出来。** 两边都不能用 `git add -A`，否则会把对方未完成的改动一起带走。这不是纪律问题，是结构问题——纪律只要有一次没执行就出事；
- **中途搬迁比一开始就隔离贵得多。** 发现时子代理已有 11 个文件在途，把它搬进 worktree 意味着让它停下、暂存、迁移、重新校准路径，风险高于继续并行，所以当天选择了不搬。**代价已经付了，只能不再重复。**

判据不是"有没有隔离"而是**主线能不能独立跑出可信的全量结果**。做不到就说明隔离没做对。

### 8.3 判定「跑通了」之前，先确认那条命令真的会失败

两个当天实测的坑，都不是「答错了」，是「答了一个没人问的问题」：

- **`cmd > log 2>&1; echo "exit=$?"`** —— 退出码来自 `echo`，永远是 0。被测命令挂了，
  harness 照样报成功。2026-07-29 一次桌面验收因此被当成通过汇报出去，而它在构建阶段
  就失败了，日志末尾是完整 traceback。**命令后面不要拼东西**，退出码就由被测命令决定；
- **`tsc --noEmit -p frontend/tsconfig.json`** —— 那是 solution 文件（`"files": []` +
  references），`-p` 它等于编译一个空文件列表：退出 0、零输出，和「整个前端类型干净」
  完全同形。三个真实类型错误因此躲了一整天。正式入口是 `tsc -b`，仓库里每个 `build:*`
  脚本用的都是它。已由 `scripts/test_typecheck_entrypoint.py` 守着。

判据是**这条命令在东西坏掉时会不会红**。拿不准就先弄坏一次看它红不红——本项目已经用
这招抓出过多条不可能失败的断言（PC-19 的时间窗、PC-21 的登录闸）。

### 8.4 批量改测试文件必须逐个看 diff，形状匹配会改变语义

2026-07-29 一天内同一个错误犯了两次：

- 按 `assert.doesNotMatch(await bodyText(), /` 这个**形状**插入新断言，而原文只有三处
  regex 真的提到工作台，另两处在「登录成功之后」的分支——**断言方向正好反了**；
- 给八处等待自动补导航，脚本把 `openAutomationRuns()` 插到了「点击『查看运行详情』
  之后」——那次点击本身已经导航过去，再插一次是走回列表。

两次都是 tsc、eslint、门禁**全绿**，只有肉眼看 diff 才发现。跨越几十个文件的机械替换
可以用脚本**生成**，但必须 `git diff` 逐文件过一遍再提交；改动落在断言语义上时，
脚本只能用来定位，不能用来下笔。

## 9. 失败矩阵与完成定义

每个跨进程、跨层或有外部副作用的任务，开发前必须覆盖适用的失败矩阵：

- 非法状态转换、重复请求和多实例竞争；
- Sidecar 未启动、启动超时、崩溃、挂起和版本不匹配；
- App 退出、电脑休眠、断网、平台超时和页面改版；
- 登录失效、二维码过期、验证码、风控和人工接管；
- 暂停、取消、紧停与正在发生的外部动作竞争；
- 幂等、结果不确定、重试上限和失败补偿；
- 磁盘满、权限拒绝、文件过大、路径替换和敏感信息泄漏；
- App 重启后的任务、事件和平台登录态恢复。

功能只有在以下条件全部满足后才能标记完成：

- 产品规划中的完成定义全部满足；
- 相关单元、集成、契约、Playwright、Rust 和 Tauri E2E 通过；
- 需要真实平台的能力完成受控账号验收并验证最终状态；
- 安全失败、资源清理、安装包内容和目标平台得到验证；
- 同一任务内更新开发台账、验证命令和证据。

### 9.1 验收证据必须落在可外部核对的终态（门禁：`scripts/check_acceptance_evidence_depth.py`）

每份 `docs/development/<任务ID>.md` 开头必须声明 `用户可操作：是` 或 `用户可操作：否`；声明「否」的还要写 `证据类型：决策 / 查证 / 分层实现 / 文档`。

**声明「是」的，证据里必须至少出现一个读者能自己去核对的具体物**：带尺寸或摘要的产物文件、`ffprobe`/`stat` 读数、数据库里的行、平台返回的作品 ID、具名日志里的退出码。判据不是措辞而是**这东西没真跑过就写不出来**——没出片就贴不出 `h264 1280×720 / 90 帧`，没发布就贴不出平台给的 ID。

**「元素可见 / 按钮可点击 / 接口返回 200 / 测试 passed」都不算终态**，它们只证明验到了「能点」。这条规则来自一次真实事故：T108 把「智能素材成片窗口整页点不动」修好并在真实 Tauri 窗口验收（`driverActive: false`、`pointerEvents: auto`），验收就停在这里——「到底能不能生成一条视频」从未验证，于是台账写着完成而能力是未知的，最后由用户打开正式包才发现。**验收深度不够是「AI 自测全绿、用户一用全不行」的三个来源之一**（另两个是构建路径分叉与 Mock 冒充真实）。

**豁免清单的纪律（重要）**：`contracts/quality/acceptance-evidence-depth.v1.json` 冻结了规则生效前的历史证据文件。

- **冻结是为了让信号可读，不是放过。** 一次性把几百份判红，门禁第一天就会被当噪音关掉，那比没有门禁更糟；
- **重新编辑一份被豁免的证据、要往里加新结论时，必须顺手补齐声明与终态，并把它从豁免清单里删掉**——豁免只覆盖「当时那份」，不覆盖后来加进去的主张；
- **清单里不许出现已经不存在的文件**，由 `test_acceptance_evidence_depth.py` 守着。否则清单会慢慢腐烂成一张万能通行证：往里加个名字就能绕过。这是所有豁免机制的通病，`single_build_path.rs` 的 `REVIEWED_` 清单同理。

### 9.2 `excludes=[]` 意味着 import 图就是打包边界

`backend/automation-tool-executor.spec` 声明 `excludes=[]`：**没有任何东西按名字被挡在冻结包外**，什么会进客户的安装包，完全由 import 图决定。因此包的 `__init__.py` 处在打包边界的根上——它重新导出什么，就等于允许什么被顺带打包。

- **测试替身（`Fake*` / `Mock*` / `Stub*` / `Dummy*`）不得出现在 `automation_tool/executor/__init__.py` 的导入与 `__all__` 中**，由 `backend/tests/unit/executor/test_shipped_package_boundary.py` 守着（读 AST，按标记子串匹配，改名躲不掉）；
- 需要它们的测试**按模块路径显式导入**（`from automation_tool.executor.fake import ...`），能力不受影响；
- 判据不是"产物里搜不到就没事"：2026-07-27 实测产物确实没有假执行器，但那是入口恰好只 import 子模块的**运气**，一句 `import automation_tool.executor` 就会改变结论。**结构上断掉比事后扫描可靠。**

### 9.3 本机共享基础设施不适用「用完就关」

「本地联调后停掉服务」针对的是**本次为自己启动的**进程。**全局共享入口不在此列**：colima 的 `/var/run/docker.sock` 由最后启动的 profile 接管，停掉自己的 profile 会把别人挂在那里的链接一并摘掉——容器本身不受影响，但其他项目通过默认 context 的访问会断。

2026-07-27 实测踩过：为跑一次验收启动 `colima --profile default`，用完停掉，结果把另一个项目 profile 提供的 `/var/run/docker.sock` 摘没了。**启动前先看 `colima list` 与 `docker context ls`；若默认入口已被别的 profile 占用，用 `DOCKER_CONTEXT=` 指定实例，不要另起 profile 争抢那个路径。**

**换实例前先确认它有镜像缓存。** 同一天接着踩的第二个：改用 `DOCKER_CONTEXT=` 指向另一个实例后，`docker compose up --wait postgres-test` 卡死二十分钟，当时顺手归因为「跟那 11 个容器抢资源」——**而 CPU 是 0%，那正是「不在算、在等」的反证，就摆在眼前没去看**。真实原因是那个实例的镜像缓存是空的，`postgres:18.4-bookworm` 要现从 Docker Hub 拉，国内直连拉不动。**卡住时先看 CPU：非零看资源，为零看网络与依赖拉取，不要凭印象归因。**

## 10. 工作方式

- 所有多步骤任务使用带 `[Tn]` 的可见计划，并保持同一时间最多一个 `in_progress`；
- 先做可运行的纵向闭环，再扩展平台、并发或抽象；
- 禁止无证据的大范围重构和为假设性需求过早抽象；
- 当前任务发现相关高风险缺陷时，先补复现测试，再做最小修复；
- 每个完成的任务一律提交并推送，不等用户下令；用户未明确要求时，不部署到远程环境、不采购云资源、不对外发布；
- 依赖或工具缺失时可以按用户授权使用 Homebrew 等标准方式全局安装，但必须先确认不会留下冲突的重复版本；
- 用户已授权开发、测试、构建、启动 App/服务、Docker、浏览器和桌面 E2E 等常规操作在沙盒外执行；凡沙盒权限可能改变运行行为时，默认使用沙盒外真实环境，不把沙盒限制导致的失败判为产品失败；
- 沙盒外操作优先申请按工具和用途限定的可复用授权规则；若平台强制显示一次确认，直接发起授权请求并继续等待，不再另行停下来询问；破坏性删除、生产部署、付费资源和对外发布不包含在这项常规授权内；
- 当前仓库已初始化 Git；每个完成的 `[Tn]` 应形成独立、可回滚提交，不夹带无关改动；
- 不使用子代理，除非用户明确要求委派或并行代理工作。
