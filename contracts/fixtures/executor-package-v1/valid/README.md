# Executor package v1 valid fixture

This directory is a deterministic cross-language fixture for the Executor package manifest.
The signing seed `00..1f` is test-only material and must never be used for a release package.
The package payload is intentionally inert and is not an executable.
