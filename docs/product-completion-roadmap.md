# 产品补全路线图 · 2026-07-27 起

**这份文档回答一个问题：从现在这个 Demo，到"运营者能把每天的活交给它"，中间缺什么、按什么顺序补。**

优先级由产品负责人 2026-07-27 定：**一键生成视频最高**。其余顺序按"离目标产品还差多少"排，不按工程难度。

判据一律是代码实现痕迹，不采信台账、文档声称或代码里的枚举映射表。每条都给出可复核的证据位置。

---

## 0 · 任务台账（PC 系列）

**这一节是本条工作线的唯一状态事实源。** 下面各章讲「为什么缺、缺成什么样」，这张表只记「做到哪了」。分工与 `docs/embedded-browser-video-studio-roadmap.md` 一致：状态只活在这张表里，每个任务的日期、提交、RED、GREEN、失败矩阵、真实验收和清理证据写进 `docs/development/PC-nn.md`，**不回写本文件**——完成记录一旦开始往路线图里堆，这张表就会退化成流水账，再也没人能一眼看出还剩什么。

本线与那 87 项专项任务是两个程序，互不双写状态。编号与优先级分开：`PC-nn` 是稳定标识，排序变了也不改；优先级由它落在哪一档（P0～P6）表达。

状态取值沿用专项台账：`⬜ 未开始` / `🧪 RED` / `🚧 实现中` / `🔍 待验收` / `✅ 已完成`。同一时间最多一个 `🧪 RED` 或 `🚧 实现中`。

### P0 一键生成视频 — 路线 A（分段渲染 + 拼接），产品负责人 2026-07-27 定

| ID | 任务 | 交付与验收 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| PC-01 | 零件目录补齐可编排字段 | 把上游 `registry-item.json` 的 `description`/`tags`/`duration`/`dimensions` 补回 `contracts/quality/motion-catalog.v1.json`，与上游逐字一致，漂移即失败；UI 投影、离线目录、发布契约、资产 overlay 四个下游消费方回归通过 | — | ✅ 已完成 |
| PC-02 | 零件可用性分级 | 按**文案存放形态**（DOM／混合／JS 注入／纯视觉）给 134 项分级并冻结，附长度与时长敏感度标记；30 个转场标为演示页本期不可用；并查证离线包内有无中文字体 | PC-01 | ✅ 已完成 |
| PC-03 | 第一批槽位表（first 批 36 个） | 从 419 个可见文本节点里**筛**出真正该开的槽（长尾是假 UI 布景文字，不是文案）；每槽记原文、角色、**容器可用像素宽度 + 字号**（不是字数——字符数不等于长度）；以「文本节点序号 + 原文逐字」双锚定位，原文对不上即失败关闭；替换发生在运行期写入 RenderJob 工作区的副本，只读发布树不变。像素预算用无头 Chromium 探针量出。**锚必须取自发布树而非 submodule**——实测 `spotify-card` 两侧文字与序号同时不同（overlay 换了品牌词、离线化删了 `<link>` 导致序号移位），从 submodule 建表会让 70 个零件把文案写进错误节点且无门禁看得见。**本轮已完成锚定这一半**：18 个零件 48 个槽冻结进 `motion-part-slots.v1.json`，门禁从发布树逐条重算；筛掉了许可署名（OpenStreetMap）、自我宣传演示页、界面家具（Follow↔Following 是按钮两态）、以及需要槽位分组的 `news-ticker`。**替换与字体注入的实现已落地**（07-27 晚）：`part_workspace.py` 与 `part_typography.py` 两个模块都在 Executor 打包边界内，48 个真槽逐条验过；发射器从 `scripts/` 搬进来的理由是 spec 的 `pathex` 只有 `backend/src`，原位置渲染期 import 会得到「检出里全绿、用户包里没有」。**剩：agent 侧接线（同时收口 PC-13）、两份契约进 PyInstaller spec、角色标注、像素预算探针。接线的真实用户路径验收压在 PC-16 上——用户包里现在一个零件都没有。**角色标注与像素预算探针拆给 PC-17**，它们是没做的活、不是没验的活，不该混进本任务的待验收里。**接线已完成**：`write_part_working_copy()` 把零件按与目录一致的布局写进真 RenderJob 工作区，相对引用在工作区内解析得到；首个调用方在 PC-05。**2026-07-29 收口**：PC-10 那条留档成片抽帧核对，`data-chart` 四个冻结槽全部命中中文（上游原文是英文），坐标轴的 `Jan`/`$8K` 仍是英文——那是脚本运行时生成的，PC-02 把它分为 `mixed` 正是这个意思，**中英并存恰好是分级判对了的样子** | PC-02,PC-13 | ✅ 已完成 |
| PC-14 | 溢出实测与廉价修复轮 | 替换后、渲染前在 Chromium 里实测每个槽（`scrollWidth>clientWidth`、`scrollHeight>clientHeight`、整体撑破 1920×1080），超了就是硬失败并带「哪个槽超了多少」回抛给模型改短重来。这是 T92 砍掉的修复轮的廉价版：当年重写 13 KB HTML 太贵，这里只重写几十字节文案。**判据被实测改写**：48 个槽里 14 个在自己的原始文案下就纵向溢出（遮罩式揭示本来就裁切），按原判据会在一个字都没换之前拒掉三分之一目录，故改为「比原文多溢出了吗」，基线记在 `motion-part-slot-budget.v1.json`。拒绝带上容器宽度与字号，模型才知道往哪改。**剩：渲染前的实测钩子与回抛重来，属编排层** | PC-05 | 🔍 待验收 |
| PC-04 | 模型看得见目录 | prompt 里的 134 个裸 id 换成 76 个可用零件的真目录（ID｜中文分类｜时长｜说明），并说出「零件时长吃掉片子预算」这条后果；闭集校验按 `deferred` 划线而不是按 `first`（后者是排期不是零件属性）。真模型三轮实测：编造 0、合法率 100%、时长判据从未过转为通过。每槽字数预算待 PC-03 | PC-01,PC-02 | ✅ 已完成 |
| PC-05 | 单零件渲染成片段 | 现有 worker 把一个零件按它自己的原生尺寸与时长渲成 mp4 片段，文案由 PC-03 的槽注入；`ffprobe` 读出的尺寸、帧率、帧数与零件声明一致；含一条长文案与一条短文案的对照，确认没有裁切、没有静止尾巴。**已跑通**：`lt-bold-block` 渲出 h264 1920×1080 / 30fps / 144 帧 / 4.800 秒，与零件声明逐位吻合；结尾静止 0.10 秒；长短文案均无裁切；中文在成片里显示正确。为此先解开两个约束：渲染画布改为随请求走（worker 里那对常量撑不住 1920×1080 与竖屏零件），工作区改为按引用拷贝（沙箱资产上限 128 而共享依赖有 125 个文件）。**产品入口尚未调用**，**产品入口已于 2026-07-29 调用**：PC-10 成片的第 6 镜就是单零件段——`data-chart` 按自己的原生画布 1920×1080、自己声明的 15.0 秒渲成独立片段，由用户在正式 App 写一句话触发 | PC-03 | ✅ 已完成 |
| PC-06 | 片段拼接 | 多个片段 ffmpeg concat 成一条；编码参数、时基、像素格式一致性由门禁保证；本机 4 layout 模板段与零件段可混排。**判定层已落地**：三段真成片实测，同尺寸 `-c copy` 得 459 帧 / 15.300 秒逐位相加。**关键发现：混尺寸拼接是一次静默成功**——退出码 0、帧数与时长全对，而后半段是竖屏内容装在横屏容器里，只有看像素才发现，所以拼前拒绝、拼后复量，退出码与帧数都不作为证据。**执行层也已跑通**：只对需要的那段重编码、其余流拷贝，预期 459 帧与成片实测 459 帧一致。**两条剩项已于 2026-07-29 一并收口**：PC-10 成片是 6 个模板段 + 1 个零件段拼成，零件夹在模板中间，通篇 1920×1080，七镜中点抽帧两两不同 | PC-05 | ✅ 已完成 |
| PC-07 | TTS 接入与逐段语音时长测量 | 每段旁白合成为音频并测出真实秒数，且必须在渲染之前拿到。实测：TTS 不在 OpenAI 兼容端点上（404），走 DashScope 原生；返回的是有签名会过期的明文 http 对象链接，取用前升级 https 并按契约声明的 host 后缀校验。时长问 ffprobe 而非解析 WAV 头（避免与最终混音的 ffmpeg 有第二个意见）。真实验收 2.080/7.120/4.320 秒。供应商选型见表下「PC-07 补充」 | — | ✅ 已完成 |
| PC-08 | 时间轴主宰 | 镜头时长 = `max(语音说完, 动效播完)`，总长 = Σ 镜头；超预算在渲染前拒绝并说明原因，不是渲完才发现。**规则与预算判定已落地**：帧数向上取整（4.81 秒需 145 帧，144 会把话切掉，每镜少一帧在整片上是听得出来的）；拒绝点名镜头与帧数；路线 A 解掉的是片子的上限而非一次渲染的上限。**产品入口已于 2026-07-29 调用，但只走过一条臂**：PC-10 成片第 6 镜声明 3.0 秒、零件动效自带 15.0 秒，`max` 取 15.0，规则在产物上看得见；而 `voice_seconds` 目前恒为 `None`，**语音那条臂一次都没被选中过**，要等 PC-07 接进一句话链路才算走完 | PC-06,PC-07 | 🔍 待验收 |
| PC-09 | 片长上限解除 | 分段后总长不再受单次 600 帧限制；提交前按契约公式给出预估渲染耗时。**已落地**：9 镜 90 秒 2700 帧不被拒。**代价已量清并写明：固定开销按段付不是按片付**——20 镜各 6 秒是 2040 秒（约 34 分钟），光启动开销就 600 秒；按一次收会告诉用户不到真实等待一半的数字。**界面已接上（2026-07-28）**：一句话入口有了成片时长控件（1~180 秒）并当场写明这条选择要付的等待时间。顺带补齐 180 秒真正能走通所缺的三处——`brief_plan` 还拿 20 秒的模板上限卡（60 秒请求先过校验、再在编排跑完之后被拒，用户等三分钟才拿到一句「做不出来」）、`author()` 顶上的整片帧数守卫是单次渲染时代的遗留、prompt 建议「每段 2~3 秒」与段数上限 24 在 72 秒以上无解（改为按片长推导）。**PC-10 实测（2026-07-29）**：20 秒 brief、8 镜、整轮约 4 分钟，估算给的是同档上限 9 分——上限比实际宽约一倍，方向安全，措辞也是「最长约」 | PC-06 | ✅ 已完成 |
| PC-10 | 端到端真实验收 | 正式包里一句话出片，成片确实用上零件、时长与请求一致；证据是 `ffprobe` 从产物读出的读数，不是「窗口能点」。**2026-07-29 通过**：真实 App 用户路径，20 秒 brief 出 7 镜 / 960 帧 / 32 秒 / 1920×1080，第 6 镜选中并渲染了 `data-chart` 零件——**零件第一次真正进到成片里**。**当日复核更正了初稿的镜头表**：初稿写「8 镜、每镜 2.5 秒」并用一句「零件那一镜略短」去圆 15 帧差额，实际那张表是同一 brief 另一轮的（写 `+27.3%`，产物里是 `+32%`）；从产物量出的真结构是 5×3.0 秒模板 + 15.0 秒零件 + 2.0 秒模板，声明时间轴 3+3+3+3+3+3+2=20 秒正是请求的片长，两条时间轴各自闭合、无需余数。**顺带补上零件的字是不是用户的字**——初稿只证到「工作区里落下了零件 HTML」，而填不上的槽是原样保留，两者不等价；抽帧核对 `data-chart` 四个冻结槽全部命中中文（`Monthly Revenue vs. Conversion Rate`→增长趋势 等），且无豆腐块。此前一直用不上不是零件缺失，是片长写死 12 秒：最短零件占 37% 预算，模型每次都判定放不下（判得对）。验收新增整段重复检测，其第一版判据被真片子当场纠正（把「镜头定格」误判成「整段循环」），改查周期性后拿两条真片子对照：本轮成片通过，2026-07-28 那条已知有病的旧片被判「每 3 秒重复、17/17 帧对得上」。**「有声音」不在本项范围**：TTS（PC-07）未接入，`voice_seconds` 恒为 None，留待 PC-07 单独补验收 | PC-04..PC-09 | ✅ 已完成 |
| PC-11 | 本台账门禁 | 自动检查：编号唯一、依赖存在、最多一个激活任务、每个已激活任务恰好一个证据文件、台账不堆完成记录、行不被裸换行拆断。共用判定抽进 `scripts/roadmap_ledger.py`，专项门禁改为调用它且输出逐字不变。首次运行即抓出两条真缺陷：PC-07/PC-13 两行单元格含裸换行（markdown 已不当表格渲染而 diff 里看着正常）、PC-13 证据状态过期 | PC-01 | ✅ 已完成 |
| PC-12 | 第二批槽位表（混合型 40） | 逐个判定哪些文案在 DOM、哪些在脚本；只给 DOM 那部分开槽，脚本部分标记为不可替换并说明该零件因此损失什么。**已冻结**：合计 37 零件 124 槽（第一批 18/48，第二批新增 19/76），40 个混合型里整件排除 20 个。**先犯了一次 PC-03 警告过的错**——盲收得 221 槽，逐条读完是 124。排除四类举实物：假界面布景、假终端输出（12 个 code-snippet，真内容在脚本里逐行打字）、上游自我宣传、不可寻址碎片（`apple-money-count` 的 `$0` 脚本一启动就覆盖，替换它什么都不会发生）。**第五类是量出来的**：`morph-text` 五句轮播、六个采样时刻宽度全为 0，从不同时布局，给它开槽就得编一个预算而预算是判溢出的唯一依据，故排除。**2026-07-29 收口**：PC-10 成片第 6 镜的 `data-chart` 就是第二批（`batch: second` / `copyLocation: mixed`），且它同时验到第二批的设计判断本身成立——DOM 那四个槽换成中文、脚本生成的坐标轴保持英文 | PC-03,PC-06 | ✅ 已完成 |
| PC-13 | 中文字体内置 + 拉丁字体补齐 | 离线包原有 18 个字体全是拉丁、中文 0 个；且实测 78/134 个零件连拉丁都在吃系统兜底（写了 `Archivo Black`/`Menlo`/`SF Pro` 等名字但从没装车）。落地结果：**净增 7,991,372 B（7.62 MB，占 dmg +1.63%）**——中文 Noto Sans SC 可变全字符集 7.42 MB（一个文件覆盖 7 个字重）、新增 4 个 OFL 拉丁家族 103.9 KB、补齐 4 个已在包内家族缺的 400 字重 100.5 KB、15 个不可再分发字体（Apple/Microsoft/Monotype）映射到包内已有替代花 0 B。新增 `contracts/video/motion-part-typography.v1.json` 记三类策略与逐条视觉差异，`scripts/check_motion_part_typography.py` 从只读 submodule 重算 134 个零件的 71 个 `(family,weight)`，**查得出遗漏**（上线前实测报出 10 条缺口，含替换链上的）。权利归属已从「按家族」改成「按 artifact」。构建期 `catalog-build` 分组精确 pin fonttools/brotli——实测默认 `recalcTimestamp` 会让同一输入每次产出不同字节。**渲染期注入已于 2026-07-29 收口**：接进 `write_part_working_copy`，PC-10 成片里「增长趋势」「连续四周保持两位数增长」等中文全部正常显示、无一个豆腐块——注入没生效的失败形态恰恰是「不报错不失败、只是显示成豆腐块」，这个读数把它排除了。**仍差 Windows 复测**，见 `docs/development/PC-13.md` 19/20 节 | PC-02 | 🔍 待验收 |
| PC-16 | 零件发布树的生产装配路径 | **2026-07-27 晚实测发现：134 个零件在正式包里一个都没有。** 三条证据：签名公证过的 `.app` 内零件 HTML 计数为 0（`Resources/` 只有 embedded-browser 336 / local-executor 293 / material-video-worker 2107 / media-toolchain 8 / motion-video-worker 4）；引用 `.local/motion-catalog-release` 的只有 `scripts/`，`backend/src` 与 Rust 侧都没有读取点；Rust 侧唯一的运行时播种 `seed_authoring_runtime` 只放 gsap 一个文件。发布树 46 MB / 338 文件目前**只是开发机 `.local` 下的构建产物**。要做的是把它按 embedded-browser／worker 同一形态装配进安装包（构建期暂存 + 逐文件摘要校验 + 出包门禁在缺失时拒绝），运行期由与 `_resource_root()` 同一条代码路径解析。这不是新功能，是规则「单一构建路径」要求的既有缺口：没有它，PC-03 的替换、PC-05 的单零件渲染在用户机器上都无零件可读。**2026-07-29 零件被真正用上之后重新出了一次 macOS 包并复验**：`Contents/Resources` 六项齐、零件 HTML 134 个、`data-chart.html` 13,706 字节；克隆一份包逐个弄坏，整棵树删掉／只删 `manifest.json`／只删契约点名的那一个零件，三种形态全部被 `require_packaged_motion_catalog` 拒（分开验是因为「目录在但内容被掏空」才是逐文件摘要要挡的那类）；运行期解析全仓库仅 `lib.rs:415` 一处，无 feature、无环境变量旁路。**新识别的缺口**：证到的是「包里有零件」与「缺了会拒」，**没证到把这个正式包装上、走用户路径渲出一条用零件的片子**——PC-10 那次用的是 debug 构建，虽由同一个 `stage_for_release` 装配，但 debug 的 `resource_dir()` 与 bundle 的 `Contents/Resources` 不是同一位置，拿「同一个函数装的」去推断正式包也行，就是把推断当证据。**剩：正式包渲一条用零件的片子、Windows 包** | PC-02 | 🔍 待验收 |
| PC-17 | 每槽的角色标注与像素预算探针 | 48 个槽给模型看时要知道每个槽是什么，以及能放多长。**长度判据是容器可用像素宽度 + 字号，不是字数**——同样 9 个字符，汉字约 9em、`iiiiiiiii` 约 2.3em、`WWWWWWWWW` 约 12em。用无头 Chromium 加载零件逐槽量，字体必须是 PC-13 定的 Noto Sans SC 可变，量出来的数才作数；探针基础设施可参考 `scripts/motion_cjk_font_probe.py`。角色标注可最后做——原文本身已是很强的提示（`Maya Chen` → 显然是姓名），甚至可能不需要。**像素预算已实测冻结**：18 零件 48 槽全部量到，可用宽度 53~724 px、字号 13~92 px。探针踩到两个坑并记下：不能在动效播放中途量（负宽度是元素还缩在零附近），以及 14 个槽基线就纵向溢出。**角色标注本轮判定不必要**——48 条原文逐条读下来都自带语义，再加一层标注等于抄第二遍且会漂移。**2026-07-29 收口**：预算已随 PC-12 覆盖全部 37 零件 124 槽；探针保持手工重跑是有意的，预算过期的两条路径分别由 `check_motion_part_slot_budget.py` 与渲染期 `SlotAnchorRejected` 堵住，不会被悄悄跳过。预算的消费方（判溢出并回抛）属 PC-14 | PC-03 | ✅ 已完成 |
| PC-15 | 两条链路的中文字体整合 | 安装包现在带**两份**中文字体：素材成片 Worker 里的思源黑体 OTF 两个字重 33.4 MB（`asset-rights-policy.v1.json` 的 `font-noto-sans-cjk-sc-regular/bold`，当初为替换微软雅黑/华文黑体而装），加动效线新增的 Noto Sans SC 可变 woff2 7.42 MB。PC-13 没复用前者，理由成立（只有两个字重、是 OTF、且在另一个 bundle 的资源根下——跨 bundle 取路径是构建路径分叉的开始），**评估已完成，结论：不整合。** 两个先入为主的判断被推翻——冻结包里的 libfreetype 链了 libbrotlidec，woff2 读得了；PC-16 之后 motion-catalog 与素材 Worker 同级，也不算跨 bundle。真正的拦路石是上游按文件名一个字重一份文件地选字体，可变字体只能给默认实例。唯一可行形态是构建期实例化两个静态字重：净省 12.2 MB（包的 2.3%），**代价是覆盖从 44,810 码位降到 30,890，丢 13,920 个（汉字区丢 0 个，丢的是谚文等）**。12.2 MB 换不来这个倒退——失败形态是字幕里出现日韩文字时显示豆腐块，不报错不失败只是难看。两条链路字体形态不同是因为选字体的机制不同，不是疏忽 | PC-13 | ✅ 已完成 |
| PC-18 | 产品入口接上分段渲染（Rust 侧） | PC-05/06/08/09/12 五条的「剩」字后面都是同一句「产品入口调用」——分段渲染每一层都做完了，正式链路一次都没调用过，故单开一项收口。**先发现 main 上这条路是断的**：`975f867` 起 Python 每次应答都带 `segments`，而 Rust 应答结构体是 `deny_unknown_fields` 且没有该字段，一句话出片 100% 报 `authoring_answer_invalid`；两侧测试各自全绿，因为没有一条拿 Python 真产出的文档喂过 Rust。落地三件：应答带 `segments` 并逐段校验（入口与资产走与顶层同一条工作区内校验、画布交给 `VideoWorkerRenderCanvas` 判、单段帧数交给 `SANDBOX_FRAMES_MAXIMUM`，必填不可选——可选加回退就是 PC-04 那种沉默）；请求带 `catalogRoot`（目录名从发布契约读，这是 PC-04 沉默的最后一段：零件进包了、beat 有 `catalogParts` 了，请求却从没说零件在哪）；`run_motion_render_job` 改按镜头循环，渲一段就编码掉再清空 `frames/`（Node worker 的帧目录是常量，不这样 N 段会互相覆盖），归一放在编码那步于是拼接永远是纯 `-c copy`。**又一次被测试抓住设计错误**：原本只在拼完后量成片，而 1920×1080 接 1080×1920 的坏文件量出来宽高帧率像素格式帧数**全对**，防线只能放在拼之前逐段探形状，拼后复量抓的是另一类（少段、帧数不够）。新增 `motion-render-canvas.v1.json` 的 `film` 段声明成片画布。**顺带修 `scripts/test_motion_authoring_agent.py` 的既有 12 条红**（`git stash` 复跑确认在我动手前就红）：10 条是默认 beat 写死零件名撞上无目录拒绝，2 条断言 worker 里有 PC-05 已删的视口常量、与 `motion-render-canvas-per-render.test.mjs` 说反话且红了很久没人看见（初稿把原因写成「不在任何人跑的套件里」，**已更正**：`run_script_tests.py` 派生发现一直覆盖着它，真实原因是从它变红到现在没人跑过全量）。**已由 PC-10 于 2026-07-29 在正式包用户路径上验完** | PC-05,PC-06,PC-16 | ✅ 已完成 |
| PC-19 | 每个镜头只渲染自己那一截 | **抽帧实测发现 PC-18 出的那条「第一次真出片」是同一段 6 秒倍速播了两遍**：留档产物 t=1s≡7s、3s≡9s、5s≡11s 逐字节相同，而 ffprobe 的编码、分辨率、帧数、时长与产品自己的静帧门禁**当时全绿**。成因是 `worker.mjs` 的 `time = seekableDuration * (index-1) / frameCount`——段只带「哪个页面、多少帧」，Worker 只剩一条规则可用：把页面整条时间轴摊到这一段要的帧数上。对一次拍完的片子是对的；路线 A 之后模板镜头共用同一份 `composition.html`，于是每个模板镜头都把整部片子重渲了一遍。段因此带上时间窗（`sourceStartMillis`/`sourceEndMillis`），Python 装配 → Rust 校验 → Worker seek 全线打通。**用毫秒整数不用秒**：命令 HMAC 绑定的规范化 JSON 要在三门语言里逐字节一致，Python 写 `0.0` 而 `JSON.stringify` 写 `0`，签名对不上会让渲染命令被静默丢弃——这条是改的过程中被沙箱门禁抓出来的，整份 spec 本来也全是整数。新增的真渲染用例做过控制实验：改回旧算法它变红、其余三条照样绿，**这正是九轮验收全绿的原因**。**已由 PC-10 于 2026-07-29 复验：63 采样 55 唯一、无周期复现，而同一判据拒掉了改动前那条旧片** | PC-18 | ✅ 已完成 |
| PC-20 | 深度思考做成每条片子可选 | 视频创作模型答之前先推理，那一段占了模型往返的大头。四对真实观测（2026-07-28/29，真实 prompt、真实模型）：开着 40.5 / 41.7 / 51.0 / 69.8 秒，关掉 8.5 / 9.9 / 10.9 / 23.5 秒；中位差 36 秒，能遇到的极端是 17 与 61 秒。开关放在一句话卡片上、与句子和片长同一张表单同样存进 store。**默认保持开着**：只量了时间没量质量，为省半分钟让每条片子悄悄变差不是这一侧能替操作者做的取舍。**界面只报「省多少」不报「这一步多久」**——量到的是模型秒，而同一张卡片上关于编排的其他话说的是编排整段墙钟（136~178 秒），`demo-sprint-roadmap.md` 的 T92 那条专门记着两个口径不能互换；初版把 42 秒印在「编排加渲染最长约」旁边，被审查抓出来。真实链路两档都验过：关掉 9.9 秒、开着 69.8 秒，都 `authored`、5 段 600 帧，闭合三键校验通过 | PC-10 | ✅ 已完成 |
| PC-21 | 桌面 E2E 套件跟上 UI 改版 | AI-first 改版把 48 条 WebdriverIO spec 里的 46 条一起打断了，而 Playwright 那 78 条毫发无伤——区别只在于后者早把路线收进了 `e2e/navigation.ts` 一个模块。**两种破法，第二种要命**：44 处正向断言消失的标题（下次谁跑谁看见红），5 处**反向**断言「工作台不该出现」——那是「未登录/被吊销时不得进工作台」的闸，字符串已不存在，所以**工作台整个挂在屏幕上它也照样通过**，一道不可能失败的闸比没有闸更糟。修法是新增 `e2e-tauri/navigation.ts` 与 Playwright 那份对应、46 条全部改走它，反向断言改成问 shell 本身（`workbenchIsMounted`）而不是问某个会被改掉的标题。门禁 `test_desktop_e2e_navigation.py` 守四条，其中「两个模块必须命名同一批目的地」是让它不再腐烂的那条链——Playwright 套件真的在跑，侧边栏改名它就红，门禁再逼桌面模块跟上。顺带修了 `新建任务`→自动化/新建运营任务（9 处）、`视频剪辑`→创作/轻量剪辑、`h2=视频制作` 已不存在等失效锚点。**过程中自己犯过一次**：批量脚本按形状插入反向断言，而原文只有三处真提到工作台，另两处被插到「登录成功之后」——方向正好反了，编译/lint/门禁全绿，只有肉眼看 diff 才发现，已回退重做。**审查又抓出十类问题，全部已修**：7 个 spec 的导航被删掉却没接回去（`openAutomationRuns` 零调用者就是印子）、8 处 marker 与同行断言永远不同屏、门禁只认一种写法放过 11 处 `li=`、5 处失效 `h2=`、`workbenchIsMounted` 的文本兜底读的是 `aria-label`（`getText` 永远读不到，是假装成防线的死代码）、`openSettings` 抽取时丢了修复分支、三处「回工作台」的清理其实不导航、侧边栏 XPath 遇徽标会失配、`waitForStartup` 把断言弱化成等待且形状恰好能顶替导航。**顺带查出我一整天的 `tsc --noEmit -p tsconfig.json` 是空的**——那是 solution 文件（`files: []`），`-p` 它等于编译空列表、退出 0、零输出，三个真实类型错误躲了一天；已加 `test_typecheck_entrypoint.py` 守着。**CQ-01 从「构建就挂」到 5 条全过跑了五轮**，每轮暴露的都是静态检查看不见的东西（默认折叠、`card.$("//…")` 的前导 `//` 从文档根搜）。**补验收第一轮（2026-07-29）：48 条里 9 条已在真实 App 上跑通**（T36、CQ-01、`motion-parts-catalog`、`diagnostic-export`、`motion-video-native`、`video-studio`、`video-creation-methods`、`motion-style-catalog`、`update-ui`），**首轮通过率 2/9**，七条红分三类：**① 产品真缺陷**——`update-ui` 暴露出强制更新只有打开设置才看得见（`AppUpdateCenter` 的提示 Modal 无条件渲染、而改版把整个组件挪进了设置页那一支，于是提示的可见性被绑在「用户此刻正好在设置页」上；用户可以永远不打开设置，而强制更新的语义正是「不更新就不能继续用」）。已修并在真实 App 两个场景验收通过；修的过程里断言方式在红绿之间改过，故撤掉修复重跑新断言确认仍红，控制实验才算成立。**② 四处 spec 断言了产品不再有的东西**——两处 `打开完整制作界面`（唯一渲染点无条件传 `embedded`，打开器已被换成提示）、十行对比改版后默认折叠（展开逻辑抽进 `navigation.ts` 共享，不再写第二份）。**③ 期望取错了契约小节**——`run_bm_08` 量成片却读「单次渲染画布」，而它自己的注释正警告过「写死过一次就是渲染对了而期望过期」；**「不要写死」不蕴含「派生就对了」，派生源同样会过期，只是过期得更隐蔽**。顺带查出 `VideoStudio` 非 embedded 分支已是不可达死代码，「智能素材成片」两头都通不了。**这条已经把 IM-05 的验收堵死**：`material-video-webui.spec.ts` 存在的全部目的就是验证那个 WebUI 能从产品入口打开，而入口不存在——它不是写坏了，是要验的能力没有入口。**因此不改这条 spec**，把断言改成迁就现状等于把「用户够不着」从红改写成绿，正是本任务开篇记的「不可能失败的闸比没有闸更糟」。它保持红，直到产品决定走内嵌还是走独立窗口。**排期时撞到一件事**：同一套 spec 有三种被驱动的方式（验收脚本直接传 `--spec`、点名 wdio 配置、或调 pnpm 脚本再由它指向配置），只搜 spec 名只能匹配上 12 条，于是一度误判「36 条没被点名」；三条链路都跟一遍后 **48 条全部有驱动**。**这正是「没有任何门禁成批跑这个套件」的机制性原因**——不是谁忘了写门禁，是这套件根本没有一个能枚举全部成员的入口。**2026-07-29 傍晚并行 + 串行复核跑完剩余驱动**：真实 App 上跑通 **31 个驱动**（当日开工时是 2 条 spec）。并行那轮暴露出一个我自己制造的混淆——两条流抢 CPU，12 条红里有 **4 条串行下直接通过**（`h8_08`、`t3_13`、`t3_14`、`t3_15`）；**判据与时间无关的用例可以并行，判据本身是时限的用例不能**，它们测的正是「够不够快」。**剩 8 条真失败**分两族：工作台没露出来（`h8_03`/`t3_18`/`t3_20`，与落地页改版同源）、执行器生命周期恢复（`h8_04`~`h8_07`，形状一致但根因未确认，不硬猜），外加 `h8_16f` 用户旅程未走到平台登录交接；另有 `bm_16` **定性更正：不是产品问题**——sweep 一直用夹具默认的 3 秒窗渲每个零件，而 `apple-money-count` 声明 5 秒、动作在后半段，三秒窗里它真的是静止的，静帧门禁拒得对；已改为按目录声明时长开窗——**但验证否决了一半：`apple-money-count` 在完整 5 秒窗里仍静止**，「窗口太短」解释不了它；窗口修复语义正确不回退，PC-12 记的「动画全由自己脚本驱动」回到台面上，最小复现已定案——零件清白、门禁清白，是 **sweep 四层没跟上产品自己的决定**：窗口用夹具默认 3 秒（PC-19 要求随段走）、画布用夹具默认 640×360（PC-05 要求渲在零件自己的舞台，控制实验：同一零件同一 seek 只换视口，1920×1080 五点五画面、640×360 前三秒同一张）、component 被要求必须动（PC-01：无时间轴的片段静止即真相，改渲一帧）、白名单整树拷贝超 128 上限（PC-13 加字体后装不下；按复用规范抽 `referenced_assets` 单源遍历，产品拷贝器与 sweep 共用，executor 1319 条全绿）。四修叠加的第五轮推进到 item 20/134，在 `chromatic-radial-split` 撞出**第六个产品真缺陷**：渲染沙箱用 `--disable-gpu` 起 Chromium 149，而新版 Chromium 连 SwiftShader 软件 WebGL 也一并拒了（需显式 `--enable-unsafe-swiftshader`），`getContext("webgl")` 返回 null，shader 零件走无 GL 降级分支渲成静止页——静帧门禁拦得对，是启动参数掐死了 WebGL；134 个零件里 **16 个用 WebGL，产品里全渲不出动画**。对照实验（同一锁定 Chromium 149.0.7827.55、逐字 worker 参数）：无旗标 `WEBGL_PROBE: null`，加旗标 SwiftShader 可用且仍是 CPU 光栅化、字节级确定性保持。已修 worker.mjs 两处启动，旗标配对进 `test_motion_video_worker.py` 门禁（红 2 vs 0 → 绿），单渲该零件 2 帧 2 个不同摘要。第六轮推进到 item 29 又撞出**第七个产品真缺陷**：31 个零件的文档根节点缺 `data-duration`（目录清单声明了、文档自己不带），而 worker 把「声明了时长」当成「可以 seek」的前提——`seekableDuration=0` 时整个 seek 分支被跳过，`code-snippet-apple-terminal-basic` 这类打字动画被静止截屏两次，静帧闸拦得对。已修：seek 只看「有没有时间轴」，窗口越界拒绝保持「有声明时长才做」，单渲该零件 2 帧 2 摘要。改打法后全量诊断扫描（不中断、收全清单）+ 五轮定点探针把剩余 26 个失败**全部定性成 8 类**（PC-21 §18.6）：12 个 template 包裹的主题零件独立打开就是空白（上游靠宿主实例化）、2 个 liquid-glass 被商标替换存根断了 `LiquidGlassCanvas` API、6 个地图/3D 零件的 `fetch()` 在 file:// 沙箱里被 CORS 直接拒、1 个字幕条按设计全程静止、1 个绝对路径引用、1 个实验 API 越过自检、2 个渲染管线不消费时间轴（已收窄未定位）、1 个采样混叠——最后这个归 sweep，已修（SWEEP_FRAMES 2→4，transitions-radial 4 帧 3 摘要通过）。**账：109/134 可渲绿，剩 25 项全是目录内容/架构决策类，不欠 worker 或 sweep 的债；验收保持红等逐类决策，不造绿**）、`vf_07`/`b5_13`/`b5_15`/`h8_22_macos` 属环境配置。**晚间补充**：诊断版 `waitForTaskRow`（失败时报出 h2、实有行、正文 400 字）落地后，把 `t3_18`/`h8_03` 卡了多轮的形状一眼看穿——**第四个产品真缺陷：`selectedTaskId` 只写不清，用户点开过任何一条任务后「查看运行记录」永远停在那一条上，唯一出路是重启 App**。已修（onBack 清选中）并连带修正 `d6_10` 的三层 UI 断言，`d6_10` 的红已转为业务层（`Installation busy` 观察超时）。产品真缺陷累计四个：强制更新只有设置页可见、最近任务列表无句柄、七个驱动 import 破损、运行记录列表回不去。**深夜批**：`h8_04`/`h8_05`/`t3_20`/`vf_07`/`d6_12` 复验全绿，**累计 40 个驱动**。`b5_13` 剥了三层：配置错误是并行污染（串行即消失）；spec 全过；现在红在 runner 的本地后置检查——登出后 Profile 标记复活（`current_douyin_profile()` 在标记缺失时会新建并写回，登出后的任何读取都能让它回来），四轮插桩后立案为**第五个产品真缺陷**：登录处理的浏览器还开着时点「安全注销」，删 Profile 一步以 `storage_unavailable` 失败（整个登出 254ms 快速失败），登录态原样留在盘上，而界面告诉用户「重新操作不会有效」——既没删掉又劝人别重试。spec 此前的绿是空转等待（fixture 未登录，登出前页面就写着「需要登录」）；已补「无失败警告」断言，这条 spec 保持红直到产品修复，不把断言改软造绿。修复任务第一步：给 remove_profile 的三个子步骤分错误码——一个 storage_unavailable 盖三种失败正是查了四轮的原因。**`t3_18` 与 `h8_03` 均已复验通过**（15.9s / 26.3s）——`t3_18` 一条 spec 前后共五层：落地页、列表行名、同名「紧急停止」第一处、运行记录回不去（产品缺陷）、同名按钮第二处（修第一处时没搜整个文件，教训：改完就全文件 grep）。（行内此前同时出现过「累计 40」与「累计 33」两个数——累计数已漂移两次，从此只陈述可核对的剩余集合，总数等收口时按驱动清单实数）。**h8_22_macos 已全绿收口**：五场景（DMG 安装、可选决策、强制更新、真实二进制替换、失败恢复）全部通过真实打包 App，日志尾行 `H8-22 ad-hoc macOS DMG, optional, forced, replacement, and failure recovery acceptance passed`。**剩余未绿的驱动**：`h8_16f`（补采 facts 后坐实**旅程其实成功**——发现→需登录→自动开账号与平台页→受控登录→登录正常全链路走通，是 spec 在等改版前的字符串「平台状态」而该页从不含它，第②类 spec 断言过期；已改锚页头 h2 + 真实状态文案，重跑中）、`bm_16`（**2026-07-29 决策**：25 个渲染不出的项根因全在内容/上游侧，落成带原因的豁免契约 `motion-catalog-standalone-render-exclusions.v1.json`，卫生由 `test_motion_catalog_render_exclusions.py` 守（项必须真实存在、类别封闭、理由非空），sweep 逐项打印跳过并核对「渲染+豁免=134」；其中 liquid-glass ×2 与 texture-mask-text 属 BM-13 素材替换族按约定留待 codex，fetch 族 6 项登记 PC-24，含豁免的全量验收在跑）、`d6_10`（**方向已定①**：并入 h8_16f 式受控执行器包编排，登记 PC-23）、`b5_13`（spec 有意保持红等第五缺陷修复）、IM-05（等 PC-22）。**`b5_15` 串行全绿收口**（9 秒过，定性为并行污染，与 h8_08/t3_13/t3_14/t3_15 同族第五例）。（h8_22_macos 备注（根因已修：EB-07/08 给启动加了 `resource_dir()` 依赖，macOS 的 `resource_dir` 用 `canonicalize` 查存在性，而 h8_22 测试包无 Resources 目录——`UnknownPath` panic 撞 `did_finish_launching` 的 nounwind 边界即 SIGABRT；崩溃报告 + 直起二进制抓 stderr + 三方对照定案，测试包配置补占位资源后直起存活；SIGABRT 消失后 spec 前进到启动修复闸，顺势暴露**第八个产品真缺陷：修复态下更新完全不可达**——StartupGate 修复页替换掉整个工作台，而 `AppUpdateCenter` 只挂在工作台 shell 里；更新器替换的是整个 .app（含资源），本就是损坏安装的标准自愈通道，被排除在修复页外等于逼用户手动重装（与 h8_21「强制更新只有设置页可见」同族）。已修：修复页收起时挂提示版、展开修复工具时挂设置版（沿用 h8_21 定下的「同一时刻恰好一份」模式），App 级测试红→绿 40/40；spec 以 `allowRepair` 显式声明走修复态，完整验收在跑；Windows 不受影响，其 resource_dir 永不失败）、IM-05（等 PC-22） | PC-10 | 🔍 待验收 |

| PC-22 | 智能素材成片改走真实内嵌 | 产品负责人 2026-07-29 定：**坚持内嵌**。现状是两头都通不了——`OperationsWorkspace` 是唯一渲染 `VideoStudio` 的地方且**无条件**传 `embedded`，于是 `打开完整制作界面` 整支成了不可达死代码；而 embedded 那支自己写着「当前真实内嵌服务尚未接入」。IM-05 的验收正为此保持红，且**不改那条 spec**——把断言改成迁就现状等于把「用户够不着」从红改写成绿。**动手前先记一条摸清楚的事实**：`material_video_studio::open` 把两件事捆在一起——启动带 WebUI 的素材 worker（含包内可执行文件与工具链解析、工作区创建），以及新建并显示一个独立 Tauri 窗口。内嵌要前者、不要后者，**所以不能整块删 `open()`**，要拆：服务那半留下复用，窗口那半随非 embedded 分支一起删。删除时按「代码删除规范」连带清理 `OPEN_ERRORS`、`OPEN_STUDIO_HINT_ID`、`opening`/`openMessage` 状态与相关测试。完成定义：正式 App 内嵌页面能真实驱动素材成片，IM-05 按内嵌路径重写并通过 | PC-21 | ⬜ 未开始 |
| PC-23 | d6_10 收敛验收并入受控执行器编排 | 2026-07-29 定性：d6_10 的编排前提（「runner 后启的 formal Executor 是唯一执行器、attempt 停留 OFFERED」）被生产 App 自带 sidecar 打破——命令 63ms 被 sidecar 确认并秒拒，忙观察与收敛两阶段都不成立；全局紧急停止也无效（协作式停止保持连接拒绝新工作，语义正确）。**方向已定（产品负责人委托 2026-07-29）**：并入 h8_16f 式受控执行器包——sidecar 即 formal，受控页面拖住发现使忙窗口真实可控，收敛由同一执行器完成。忙语义的三层确定性覆盖（仓储 pytest、Rust 423 映射 cargo test、组件测试）保持不变，本任务只补「真实 App UI 亲眼看到忙」这一层。证据：PC-21 §20 | PC-21 | ⬜ 未开始 |
| PC-24 | 运行时取数零件的数据供给通道 | 6 个目录零件（spain-map、us-map ×3、world-map、vfx-iphone-device）在运行时用 fetch() 拉本地数据/3D 模型，file:// 沙箱下被 Chromium CORS 直接拒（数据都在发布树内、白名单救不了），产品渲染链路上这些零件永远出不了片。候选：随机 loopback 数据供给（威胁模型已有 loopback Worker 边界可对齐）或构建期内联；不采用 --allow-file-access-from-files（放宽路径泄漏边界）。完成定义：6 项从渲染豁免契约中划掉并在全量 sweep 里渲绿。证据：PC-21 §18.6 D 类 | PC-21 | ⬜ 未开始 |

#### PC-07 补充：TTS 供应商与接入形态

每段旁白合成为音频并测出真实秒数，且必须在渲染之前拿到——顺序反了就只能事后发现说不完。**供应商已定（产品负责人 2026-07-27）：走已接入的阿里百炼，先随便挑一个够前沿的把 MVP 跑通，不为选型发散。** 建议 pin `qwen3-tts-instruct-flash-2026-01-26`（非 realtime，我们要的是文件不是流；`instruct` 档为将来的语气控制留门）。

实调百炼列出 231 个模型，TTS 三家可选：`qwen3-tts-*`（flash / instruct / vc 声音克隆 / vd）、`MiniMax/speech-2.8-hd|turbo`、`qwen-tts-2025-05-22`。**小米 MiMo 只有 `xiaomi/mimo-v2.5-pro` 一个，是文本模型不是 TTS。**「同一句话分段用不同语气」这个能力待验证，归后续，不阻塞 MVP。

排除的选项：`edge-tts` 与 `azure-cognitiveservices-speech` 已在素材成片 Worker 包里闲置，但端点在国外、国内可达性不稳；`faster-whisper` 是本地模型但它是 ASR 不是 TTS；本地 TTS（Kokoro/ChatTTS/GPT-SoVITS）安装包净增 300 MB 起，留给「用户明确要求离线」的场景。

**接入形态很轻**：上游 hyperframes 引擎不管 TTS——音频在它的合成模型里就是标准 `<audio>` clip，TTS 在它的 `media-use` skill 里走外部服务产出本地文件。而我们的 `worker.mjs` 对 audio 零引用（逐帧截图，音频不经过 HTML），A 路线又是分段渲染 + ffmpeg 拼接，**旁白天然就是 ffmpeg 的另一条输入轨**。只需在 `bailian-model-catalog.v1.json` 加一个 `voiceover` 用途，与现有 `script` / `video_creative` 走同一网关。

#### PC-13 补充：中文字体实测与定案

离线包现有 18 个字体全是拉丁、中文 0 个，中文靠系统兜底 → 跨平台不一致（违反 BM-16 确定性）且 23 个零件设计丢失。字体必须**构建期下载、锁摘要、进安装包**，运行时取用一律不可接受（国内用户访问不了 Google）。需给出实测净增 MB；基线是现有离线字体资产 1 MB。**查证已完成（见 `docs/development/PC-13.md`）：同名 `@font-face` + `unicode-range` 在内置 Chromium 149 上成立，但必须按零件请求的每个 `(family, weight)` 逐条声明——只声明 400 或 `100 900` 都会掉回系统字体；推荐一个 Noto Sans SC 可变 woff2。**产品负责人 2026-07-27 定：取全字符集 7.42 MB（占 dmg +1.59%），不取 GBK/GB2312**——省下的 1.84 MB 换来的是用户名字里有个「璟」就显示豆腐块，且渲染成功、门禁全绿、无任何报错。可变字体下字重数量不影响体积。**

**同次定下扩大范围：顺带修拉丁那 78 个。** 实测 134 个零件里 78 个（58%）今天连拉丁字母都在吃系统兜底（61 个写了具名字体却从不加载 `@font-face`，17 个一个都没写），对照组里 `#lt-tag` 报 `SpaceMono-Bold` 且 `isCustomFont: false`——那是探针机器 `~/Library/Fonts` 里的，不是产品带的。**BM-16 要挡的跨机器漂移今天已经在拉丁字母上发生了**，而修它和注入 CJK 是同一处改动，分开做等于改两遍。注意其中点名的 `SF Pro`/`Menlo`（苹果）与 `Consolas`/`Courier New`（微软）**不可再分发**，需逐个定替代并记录取舍。

落地已知工作：按 `(family, weight)` 逐条声明（Chromium 先按字重分桶再解析 `unicode-range`，漏一条就偶发掉字体）、`fontFamilies` 权利归属从「按家族」改「按 artifact」、构建期新增 fonttools+brotli 生成合并 woff2（Google 只按 101 子集分发而 `SANDBOX_ASSETS_MAXIMUM=128` 装不下）、Windows 复测。

### P1～P6 尚未拆分

下面各章已经写清缺口与判据，但还没拆成可执行任务，等 P0 收口后再逐档拆。**在拆之前它们不带状态**——给未拆分的工作线标「未开始」会让剩余量看起来可数，而实际上连有几项都还不知道。

---

## P0 · 一键生成视频（AI 一句话出片，走 hyperframes 路线）

**这是最高优先级。** 它现在能出片，但出的不是产品想要的片子。

### P0-0　目标形态与路线选择（2026-07-27 与产品负责人讨论后确定）

**理想状态**（负责人表述）：把模板与零件的说明喂给 AI，AI 先向用户问清楚要做什么类型的视频、多长、什么风格，收集够了再决定——用现有模板还是自己新写一套 HTML，动效是复用现有 134 个还是自己重写，或者混合。都定了之后剩下就是生成。

**这个方向最终是对的，但落地顺序不是按难易排，而是按依赖排：先把「AI 熟练使用零件」做成主路径，再把「AI 自己写」作为逃生舱补上。** 理由三条，都有据可查：

1. **模型会压倒性地倒向自己写。** T92 记录的正是这个：当模型可以自由输出 `composition_html` 时，7543 字节的回答里 5315 字节是它在敲 markup。给它"可用零件也可自写"的选择，而它对 HTML+GSAP 远比对这 134 个零件熟悉，结果必然是零件永远用不上——等于回到 T92 之前，慢且每次都是新代码。**默认倾向必须是复用，缺口才补写；反过来设计，复用不会发生。**
2. **零件是验证过的，现写的每份都是首次运行。** 目录里 100 个带官方预览，意味着在真实渲染里站得住。一键出片场景下用户不会逐帧检查，**方差比上限更致命**。
3. **有一块实打实的工程被 T92 拿走了，得先还回来。** `lint_composition` / `check_composition` 现在判的是本机模板输出，**没有修复轮**。要让 AI 写 HTML，必须重建「模型输出 → 门禁 → 带错误回抛让模型改 → 再检」这个环节，并重新设计安全边界：现在靠 `escape_untrusted_text` 让文案贡献不出任何 ASCII 字面量，而 AI 写的 HTML 本身就是代码，这条防线形状完全不同，要靠沙箱预算 + 门禁兜。这不该和「让零件用起来」捆在一起做。

**因此 P0-1 拆成三步，依赖顺序不可颠倒：**

| 步骤 | 做什么 | 为什么排这个位置 |
|---|---|---|
| **P0-1a 编排层（主路径）** | AI 拿零件目录说明 + 用户需求，决定用哪些零件、怎么排布、什么节奏，只输出零件 id 与参数 | 地基。AI 必须先能熟练用零件，才谈得上判断「什么时候零件不够」 |
| **P0-1b 需求澄清（可跳过）** | AI 动手前亮出它的判断供用户确认或修改 | 依赖 1a：没有零件能力，AI 的"判断"无从表达 |
| **P0-1c 逃生舱（兜底）** | 零件覆盖不到时允许 AI 补写片段，过门禁 + 修复轮 + 沙箱 | 依赖 1a 建立的默认倾向，否则它会变成主路径 |

**关于需求澄清的陷阱**：这个入口的卖点就是"一句话"，强制问答会把它变成表单——而"固定模板手工制作"入口已经在做表单的事了。建议形态是**一句话直接出片，但 AI 把判断亮出来**（"我理解你要一条 30 秒的产品介绍，走展览黄风格，开场标题动效、中间三段数据卡片"），用户可直接确认，也可改一句再出。想聊的能聊，不想聊的一句话走完，同时让 AI 的决策变得可见可纠正。

### P0-1a　编排层：让模型真正用上 134 个动效零件（主路径）

**现状**：`composition_template.py` 里对 registry 零件**零引用**。片子结构是 4 个写死的 layout：`SCENE_LAYOUTS = ("title", "points", "flow", "stat")`。134 个零件（109 blocks + 25 components，见 `contracts/video/motion-catalog-ui.v1.json`）一个都没进合成。

**根因不是疏漏，是一次有意的取舍**（`composition_template.py` 文件头、`docs/development/T92.md`）：模型原本输出第四个字段 `composition_html`——完整 DOM + CSS + GSAP timeline，实测 7543 字节回答里占 5315 字节。改成"本机生成 markup、模型只写文案"后快 2.4 倍，代价是**结构从每部现编变成 4 选 1**。

**建议的第三条路**：模型既不写原始 markup，也不退回固定 layout，而是**从冻结目录里挑零件并编排**——输出零件 id + 参数（几百字节，速度接近现在），由本机按 id 组装。这才是 hyperframes 作为"AI 工具箱"的设计意图。

**骨架已经在了**，不是从零开始：

- 零件目录已冻结：`contracts/quality/motion-catalog.v1.json`，`agent.py:142` 的 `_load_locked_catalog_part_ids()` 已经在读它；
- agent 侧已有 `catalog_parts` 字段与校验：`entry.py:202-203`（`catalog purposes missing`、`catalog_parts must be locked catalog ids`）；
- 缺的是**组装那一段**：把 `catalog_parts` 真正翻译成合成文档里的零件实例。

### P0-1a′　「组装」到底是什么——2026-07-27 打开零件源码后的三个更正

上面那句「把 `catalog_parts` 翻译成零件实例」写得太轻，它隐含了一个当时没验证的前提：零件是可以填参数的组件。**打开源码后这个前提是错的**，下面三条推翻并替换它。

**一、零件没有参数接口，文案是写死的。**

134 个里只有 6 个带 `params`，而这 6 个一共 12 个参数**全部是颜色**（`--bg-color`、`--text-color`）。没有任何文本参数机制。而按可见文本节点统计，**125 个零件里带写死的英文文案**——`ui-3d-reveal` 78 段、`vfx-portal` 38 段、`vfx-liquid-background` 41 段；完全没有可见文案的只有 9 个（`grain-overlay`、`motion-blur`、`shimmer-sweep`、`parallax-zoom` 这类纯视觉）。

上游自己的解法在 `skills/hyperframes-registry/SKILL.md` 里：`hyperframes add <name>` 把零件 HTML 装进项目，**然后由 AI 直接改那份 HTML**。

**这条路我们不走，理由是算术不是原则。** T92 不是立规矩说模型不许碰 HTML，它是一次实测出来的性能取舍：模型吐字速率恒定约 4.5 KB/s，一句话出片的墙钟时间基本等于模型打字时间，把 markup 收回本机后编排耗时中位数从 87.0 秒降到 36.9 秒。而零件动辄 13 KB（`data-chart`）起，光重吐一份就是 3 秒，一条片子用 4～5 个零件就把整条链路拖回 T92 之前，还得把 T92 顺手拿掉的修复轮重建起来。

**本机做文案槽替换的代价则是一次性的**：建一张槽位表，之后每次出片模型只吐几十字节的文案字符串。所以主路径走槽位表；而「让 AI 补写」（P0-1c 逃生舱）随时可以加回来，只是不该当主路径。

**所以要用零件承载用户内容，必须本机做结构化文案槽替换**——预先登记每个零件的可替换文案位置，模型只吐字符串，本机替换并继续走 `escape_untrusted_text`。这是新增的前置（PC-02、PC-03），原路线图里没有，因为当时以为零件可参数化。具体怎么建、哪些零件能建、塞长塞短会怎样，见 P0-1a″ 到 P0-1a⁵ 四节。

**二、上游有原生的子合成机制，不用改上游一个字。**

宿主合成里放一个 div 引用零件文件即可（`skills/hyperframes-registry/references/wiring-blocks.md`）：

```html
<div data-composition-id="data-chart"
     data-composition-src="compositions/data-chart.html"
     data-start="2" data-duration="15" data-track-index="1"
     data-width="1920" data-height="1080"></div>
```

上游 runtime 负责加载子合成、找到它自己的 `window.__timelines` 注册、**按 `data-start` 偏移与宿主时间轴同步 seek**。宿主的 GSAP 代码完全不需要引用零件的时间轴。

**三、但我们的渲染器没有实现它。**

`workers/motion_composition/worker.mjs` 是本项目自己写的 CDP worker，只驱动顶层 `window.__timelines`，不认 `data-composition-src`。所以这个机制目前对我们不生效。

#### 由此定下的路线（产品负责人 2026-07-27 选 A）

| | 做什么 | 得到什么 | 代价 |
|---|---|---|---|
| **A（选定）** | 每个零件按它自己的原生尺寸单独渲成 mp4 片段，再 ffmpeg 拼接 | 零件本身就是合法的独立合成，**现有 worker 直接能渲**，不需要实现子合成；顺带解掉 20 秒上限（见 P0-1e） | 只能顺序播放，做不到同屏叠加；要处理拼接的编码一致性与音画同步累积误差 |
| **B（以后）** | 在 worker 里按上游契约实现 `data-composition-src` 解析与同步 seek | 真正的合成：叠加、定位、同屏多零件（例如数据图表 + 下三分之一身份条同框） | 解不了 20 秒上限；worker 要新增子合成加载与偏移 seek |

**两者不冲突：A 解片长，B 解叠加。先 A，等确实需要同屏叠加时再补 B。**

### P0-1a″　零件不是成品镜头，30 个转场是演示页（2026-07-27 实测）

定下 A 之后曾打算「先小后大」：小 = 零件只当点缀、不换文案，文案继续由本机 4 layout 模板画。**打开零件源码后这个切法是错的**，原因和结论都在下面。

**30 个转场零件全是演示页，不是镜头。** 原样渲染出来的画面是：

```
SCENE A | SCENE B
Chromatic Radial Split
Prompt
"use chromatic radial split shader transition"
RGB channels separate and converge radially. From-scene splits outward…
```

它展示的是「这个转场技术长什么样」，中间那两块 SCENE A / SCENE B 是占位面板。要真的用它，得把转场机制搬到你自己的两个场景之间——**那正是「让 AI 写 HTML」才做得到的事**（P0-1c 逃生舱）。而这 30 个恰恰就是原计划里「最安全、可以原样用」的那批。**所以「不换文案就能用」不成立。**

同一批实测里露出了正确的切法：

```
caption-kinetic-slam  组件   "Kinetic Slam"                                → 1 槽
lt-bold-block         4.8s   "Maya Chen" / "Host · Neuroscientist"          → 2 槽，含义一目了然
logo-outro            6s     "Nothing great is made alone." / "figma.com"   → 2~3 槽
flowchart             12s    "Should I learn to code?" / "Yes" / "Start with Pythom"…  → 约 9 槽
ui-3d-reveal          13s    整个假 App 界面                                 → 78 段文本
```

**决定一个零件好不好用的是它有几个文案槽，不是它属于点缀还是内容。** `lt-bold-block` 是点缀型，但两个槽的含义清清楚楚，换成「张三 / 主播·神经科学家」立刻可用；`flowchart` 是内容型，九个槽也都对得上；而转场零件槽位再少也没用，因为要替的不是文字是机制。

**但「按槽位数量分批」也是错的**，见下一节——那份统计只数 DOM 文本节点，而文案未必在 DOM 里。

### P0-1a⁗　塞长文案会怎样——分三层，一层比一层难发现（产品负责人 2026-07-27 提问后实测）

问题是「往槽里塞特别长或特别短的文本会不会有问题」。会，而且最要命的那层没有任何门禁能发现。

```
动画会读元素实际尺寸（文案一变动画就变）   55 / 134
带某种字号自适应（长文案自动缩，有下限）    32 / 134
两者都没有（最脆）                        73 / 134
```

**第一层 · 排版（看得出来）。** 73 个零件既不测尺寸也不自适应，固定字号配固定容器。`lt-bold-block` 是典型：

```css
.lt-name    { font-size: 58px }      /* 写死 */
.lt-tagwrap { overflow: hidden }     /* 超出直接裁掉 */
```

**长了不是难看，是看不见**——被裁掉。有自适应的 32 个也有下限，`caption-kinetic-slam` 的 `fitFontSize` 缩到基准的 45% 就停，再长照样溢出。

**第二层 · 动画（不容易看出来）。** 55 个零件的动画读 `offsetWidth` / `getBoundingClientRect` / `measureText`，文案长度一变，位移量、擦除宽度、缩放中心跟着变。结果是「感觉不对」，没有报错。

**第三层 · 时长（完全看不出来，最危险）。** JS 数据型零件的时间轴是按原文案编排的：

```js
var WORDS = [ {text:"Every", start:0.0, end:0.3}, … 28 个词，各带时间戳 ];
var KEYWORDS = new Set([8, 12, 15, 24, 27]);   // 关键词按下标高亮
```

`data-duration="8"` 就是这些时间戳推出来的。换成 10 个词 → 动画 3 秒播完 → **后面 5 秒是静止画面**；换成 40 个词 → 后面的词根本没时间播。而 `check_composition` 只断言 `data-duration` 与声明一致，这两种情况它都认为正常。**现有门禁一条都发现不了。** 太短还额外错位关键词高亮——`KEYWORDS` 是下标，词数一变就高亮到别的词上。

**由此定下槽位表必须记的三样**（只记文本是不够的）：文案在哪（DOM 文本节点 / JS 数据结构）、长度**上下限**（不只是上限）、**是否参与时间轴**（换了要不要重算时长）。

### P0-1a⁵　真正的分批标准：按文案存放形态

上一节的原因，「按槽位数量分批」作废——那份统计只数 DOM 文本节点，而 **15 个字幕零件全是 JS 注入型（DOM 里 0 段文本）**，扫到的 `Kinetic Slam` 是 `<title>`。它们不是最容易的那批，是最难的。

实测的真实形态分布：

形态分布（PC-02 已冻结进 `contracts/video/motion-part-usability.v1.json`，由门禁从源码重算）：

| 形态 | 零件数 | 处置 |
|---|---:|---|
| **DOM 型** | 52 | 文案在文本节点里，槽位方案直接成立 |
| 混合 | 42 | 一部分在 DOM 一部分在 JS，逐个判 |
| JS 注入型 | 28 | 文案与时间轴都在脚本里，换它≈改代码 → 归 P0-1c 逃生舱 |
| 纯视觉 | 12 | 没有文案，直接可用 |

**注意这些数字与本节上文的粗测不同**（54/47/23/10）：上面那组是正则扫全文件数出来的，PC-02 改用**将来真正做替换的那个解析器**（`part_document.py`）重算，统计与替换按构造一致。差距本身就是不该用粗测下结论的证据。

再叠上「转场是演示页」这一条（30 个转场里 25 个是 DOM 型，形态没问题，是语义把它们排除的），得到最终分批：

```
first     36    非转场的 DOM 型与纯视觉 → 建槽位表（PC-03）
second    40    混合型 → 只给 DOM 那部分开槽（PC-12）
deferred  58    30 个转场演示页 + 28 个 JS 注入型 → 逃生舱
```

**修订后的先小后大**：先给 first 的 36 个建槽位表，跑通 A 路线的渲染与拼接，出一条真能看的片子；再逐个判 second 的 40 个。

**但 419 个可见文本节点不等于 419 个槽。** 这 36 个的可见节点合计 419，分布极不均：26 个零件 ≤5 个节点（下三分之一、简单卡片，理想形态），长尾是 `ui-3d-reveal` 136、`vpn-youtube-spot` 50、`vfx-portal` 45、`app-showcase` 38——**那些是假 UI 的布景文字，不是该替换的文案**。槽位表要筛，不能照单全开。

第一批的风险画像倒是很干净：36 个里只有 4 个 `lengthSensitive`、4 个 `autoFitsFontSize`、3 个 `latinDisplayDesign`。

### P0-1a‴　槽位表怎么建：沿用 BM-13 的 overlay 思路，不碰 submodule

不改零件源码。规则第 6 条要求 `vendor/hyperframes` 只能按锁定 commit 作只读 submodule 存在，`scripts/check_third_party_sources.py` 会因工作树脏判红——但更重要的是**没必要，这个仓库已经解过一次同样的问题**。

BM-13 给零件去品牌时同样要「改零件里的字但不能碰 submodule」，做法是 overlay：`contracts/quality/motion-asset-overlay.v1.json` 覆盖 70 个零件、121 条商标文本替换规则（`apple` → `星云科技`、`hyperframes` → `动效画布`），由 `build_motion_catalog_release.py` 在构建期合成一棵只读发布树。**「往零件里塞我们自己的字」这条链路是通的、跑过的、有门禁守着的。**

文案槽是同一条路，但改两处：overlay 是**构建期、全局、按字符串匹配**，文案要**运行期、按位置**——每次出片都不同，且换的是整句而非一个词。

```
只读发布树的零件 HTML → 按槽位表替换 → 写进本次 RenderJob 工作区的临时副本 → 渲染
                                             （发布树自始至终只读）
```

**定位用「文本节点序号 + 原文逐字」双锚，不用 CSS 选择器：**

```json
{
  "name": "lt-bold-block",
  "sourceSha256": "…",
  "slots": [
    {"index": 1, "original": "Maya Chen", "maxChars": 9, "role": "姓名"},
    {"index": 2, "original": "Host · Neuroscientist", "maxChars": 21, "role": "身份"}
  ]
}
```

替换时按文档顺序枚举文本节点，第 `index` 个必须逐字等于 `original`，否则失败关闭。理由是它自校验：原文对不上就说明我们对这份文档结构的理解是错的，当场判红，而不是静默替到别处去——选择器做不到这一点。零件文件的 sha256 已被目录门禁锁着，上游一升级槽位表就必须重生成，这道门禁是现成的。

**三条硬边界，不是以后能优化掉的：**

1. **`<script>` / `<style>` 里不开槽。** `data-chart` 的数据是 script 里的数组——**图表数值换不了，只能换标题和图例**。这是内容型零件表达力的真实天花板；
2. **长度。** 零件按原文长度排版，`"Monthly Revenue vs. Conversion Rate"` 34 字符换成 40 字中文会炸版。槽位必须带字数预算并喂给模型当约束，同现在的 `MAX_BEAT_HEADLINE_CHARS`；
3. **字体。** 零件用 Libre Baskerville 这类拉丁字体，塞中文会 fallback。BM-12 本地化过字体，但**离线包里有没有中文字体尚未查证**——这是典型的「技术全绿、用户一看很丑」，必须在建表前查掉。

文案仍走 `escape_untrusted_text`：每个 ASCII 字符变成数字字符引用，在 HTML 文本节点与 SVG `<text>` 里渲染一致，且贡献不出任何 ASCII 字面量。

**要连带解决的两个约束**（它们是 T92 把 markup 收回本机时一起接手的）：

1. `lint_composition` / `check_composition` 两道门禁现在判的是本机模板的输出，**没有修复轮**——零件组装出的文档一旦触发门禁就是用户简报的硬失败；
2. `escape_untrusted_text` 保证文案不能写出源码。零件参数若接受用户/模型文本，必须走同一条转义，否则这道防线会被绕开。

**判据**：同一句简报连出三条片子，`DESIGN.json` 里的 `catalog_parts` 各不相同，且渲染出的 mp4 在视觉上确实不同；关掉零件组装时能回退到现有 4 layout 且不报错。

**喂给模型什么——2026-07-27 探针已经把这件事测掉了，结论是好消息**：

只给「英文 `title` + 中文 `category`」（目录当前仅有的信息），两个模型都选出了合法且合理的组合：

| | qwen3.7-max | deepseek-v4-pro |
|---|---|---|
| 风格模板 | `blue-professional` ✅ | `blue-professional` ✅ **独立选出同一个** |
| 零件合法率 | 4/4 | 5/5 |
| 编造 id | 0 | 0 |

核对所选零件的真实分类，与模型陈述的用途完全吻合（`caption-kinetic-slam`→【字幕】、`ui-3d-reveal`→【产品与案例展示】、`data-chart`→【数据与地图】）。**"目录说明够不够模型判断"这个最大的不确定性已被排除**，剩下的是纯工程。

**但仍要补三个字段**，因为 P0-1d 的实测显示模型会因为看不到时长而严重超预算：上游 `vendor/hyperframes/registry/blocks/<name>/registry-item.json` 里本来就有 `description`（英文一句话说明）、`tags`、`duration`，冻结目录时只摘了 `name/title/category` 把它们丢掉了。补回成本极低。

12 套 style preset 无需补——`motion-style-presets.v1.json` 已有中文 `displayName` 与 `summary`（如"展览黄：纸张质感、深蓝文字和黄色光晕，适合文化与品牌叙事"），实测两个模型都据此选对。

### P0-1b　需求澄清轮（可跳过，不阻断一句话）

AI 在动手前把它的判断亮出来供确认或修改，形态见 P0-0。**判据**：不确认也能出片（默认路径不变长）；确认界面里改风格或片长，产出确实跟着变。

### P0-1c　逃生舱：零件不够时允许 AI 补写（兜底，非主路径）

**前置**：P0-1a 已建立"优先复用"的默认倾向。

**必须同时具备三样，缺一不可**：

1. **修复轮**——模型输出 → `lint_composition`/`check_composition` → 不过则把具体错误回抛给模型重写 → 再检。这是 T92 拿走的东西，要还回来；
2. **安全边界重设计**——`escape_untrusted_text` 那套（让文案贡献不出任何 ASCII 字面量）对"AI 写的代码"不适用，需要靠 `motion-render-sandbox-budget.v1.json` 的沙箱预算 + 门禁扫描来兜；
3. **可观测的选择**——`DESIGN.json` 要记录每一段是"复用零件"还是"AI 补写"，否则无法回答"这条片子为什么慢/为什么怪"。

**判据**：构造一个零件库明显覆盖不到的需求，AI 走补写路径且产物过门禁；同一需求在零件够用时不触发补写。

### P0-1d　时间轴必须有一个主宰（实测发现，比想象中要紧）

**2026-07-27 探针实测**：把 12 套 preset 与 134 个零件交给模型为一句话简报做编排，两个模型都选出了**合法且合理**的组合（零编造、分类与用途吻合），但**总时长双双超出沙箱上限 70% 以上**：

```
渲染沙箱硬上限          20 秒（SANDBOX_FRAMES_MAXIMUM 600 帧 / 30fps）
当前一句话入口固定      3 段 × 4 秒 = 12 秒

qwen3.7-max     4 个零件   34.0 秒   ❌
deepseek-v4-pro 5 个零件   36.3 秒   ❌
```

原因是**模型不知道零件有多长**——`data-chart` 单个 15 秒、`ui-3d-reveal` 13 秒，而 `lt-bold-block` 只有 4.8 秒。现有系统按「段数 × 每段固定秒数」算，隐含假设是每段等长，这个假设在用零件之后不成立。

**但"总时长 = 各零件时长之和"同样是错的**（产品负责人 2026-07-27 纠正，此处按纠正后的模型重写）：那等于让动效决定镜头长度。动效的作用是让画面内容出现时不生硬，**是点缀**，播完停在末帧等着即可；它不该决定这一镜停多久，但它确实是个约束——**动效没播完就切走看着很难受**。

正确的模型是：

```
镜头时长 = max(这段话说完要多久, 动效播完要多久)
总时长   = Σ 各镜头时长
```

即**内容主导、动效兜底**：语音 8 秒配 5.5 秒动效 → 8 秒（动效播完停住）；语音 3 秒配 5.5 秒动效 → 5.5 秒（等动效播完）。

### P0-1d′　零件其实有两类，目录里没有区分（编排质量的关键）

上面的"播完停住"策略只对一类零件成立。实测数据把这条界线画得很清楚：

| 零件 | 时长 | 性质 |
|---|---|---|
| `caption-kinetic-slam` | 无 | **点缀型**：字幕入场 |
| `lt-bold-block` | 4.8 秒 | 点缀型：身份条 |
| `logo-outro` | 6 秒 | 点缀型：品牌收尾 |
| `ui-3d-reveal` | 13 秒 | **内容型**：3D 逐步展开 |
| `flowchart` | 12 秒 | 内容型：决策树逐节点绘制 |
| `data-chart` | 15 秒 | 内容型：图表逐条展开 |

**内容型零件的动效本身就是信息**，"播完停住"不成立——不能让图表画到一半停住，也不能旁白说完 5 秒就切走、观众连图都没看全。这类零件里，**旁白要配合动效节奏来写**（写长些，或留白让观众看图），而不是反过来。

**所以模型选零件时要判断的是两件事**：这一段是「说话配点缀」（语音主导，挑短的点缀即可），还是「展示内容」（动效承载信息，文案配合它）。

**目录里目前没有这个维度**：`category` 分的是题材（字幕／数据与地图／产品展示），不是功能（点缀／内容）。这是 P0-1a 补字段时要一并加的。

**自动分档的可行性已经用全量数据验证过**（134 个零件按上游 `duration` 分档）：

| 档位 | 数量 | 实际构成 | 判定 |
|---|---:|---|---|
| 无 `duration` | 25 | 全是 `caption-*` 字幕与文字效果 | 点缀型 |
| ≤6 秒 | 40 | 全是转场（zoom / warp / dissolve / split） | 点缀型 |
| 6–10 秒 | 15 | `code-morph`、`news-ticker`、部分产品展示 | **待人工判定** |
| >10 秒 | 54 | `code-snippet-*` 代码演示、数据图表类 | 内容型 |

分档结果与 `category` 高度一致——无时长的全是字幕、≤6 秒的全是转场，这两类天然是"让画面出现不生硬"的点缀；>10 秒的全是动效本身在逐步呈现信息的。

**所以用 `category` + `duration` 双判据，119 个（89%）可自动分档，只剩 15 个中间地带需要人工看一眼。** 不必一开始就人工标注 134 条。

**另有两处必须先处理**：

1. ~~**部分零件没有 `duration` 字段**（实测 `caption-kinetic-slam`、`caption-editorial-emphasis` 均为 null），编排时不能假定它一定存在；~~
   **PC-01 全量核对后更正**：不是「部分零件漏填」，而是 block/component 的结构差异——`duration` 与 `dimensions` present 的恰好是 109 个 block。上游定义里 block 是自带画布与时间轴的独立子合成，component 是贴进宿主的片段，两者都没有。**所以 25 个 component 天然属于点缀型，分档问题只存在于 109 个 block 之间**，需要人工判的中间地带也随之缩小；
2. **上游其实提供了描述、标签和时长**，是我们冻结目录时丢掉的——`vendor/hyperframes/registry/blocks/<name>/registry-item.json` 里有 `description`、`tags`、`duration`、`dimensions`，而 `contracts/quality/motion-catalog.v1.json` 只摘了 `name/title/category`。**补回这三项是 P0-1a 的前置**，成本极低，收益是模型能按预算选而不是盲选。

**三个时长来源必须定主次**：画面（零件 duration）、语音（TTS 念完的实际秒数）、总预算（沙箱上限）。

**建议：语音主导，画面跟随。** 理由是 TTS 时长客观可测（合成完即知），而"把文案裁短以塞进画面"直接损伤内容且用户听得出来。

流程顺序有硬约束：

1. 模型写文案 + 选零件；
2. **先合成 TTS**，拿到每段语音真实秒数——必须在渲染之前；
3. 每段实际时长取 `max(语音时长, 零件 duration)`——语音没念完不切镜头，画面没播完也不切；
4. 总和超预算则回头压缩：减段、换更短的零件、或让模型改短文案；
5. 渲染时零件在自己的 duration 内播完，**剩余时间保持末帧而不是拉慢动效**——语音 8 秒配 5.5 秒动效，拉慢会拖沓，保持末帧更自然。

**判据**：给定一句话，产出的 mp4 里每段语音都在本段内说完；总时长等于各段 `max()` 之和；超预算时在渲染前就被拒绝并说明原因，而不是渲染完才发现。

### P0-1e　20 秒上限撑不起产品形态（需要产品决策）

`totalSecondsMaximum: 20` 来自渲染沙箱 `SANDBOX_FRAMES_MAXIMUM = 600` 帧（600 / 30fps）。而运营者在抖音、小红书发的短视频通常 **30 到 60 秒**。

**也就是说这条链路目前在物理上做不出一条正常长度的口播视频。** 接上 TTS 之后这个矛盾会立刻显性化：旁白说不完两句话就到顶。

**这不是 bug，是当初为渲染安全定的预算**，改它要付代价，方向有三个，需要产品定：

| 方向 | 代价 |
|---|---|
| 提高帧数上限 | 渲染时间与内存线性上涨；`renderWallMillisPerFrame` 400ms 意味着 60 秒片子约 12 分钟渲染 |
| 降低帧率（30 → 24 或 15） | 同样帧数换更长时间，但动效流畅度下降，字幕类零件影响最明显 |
| 分段渲染后拼接 | 单段仍受 600 帧限制，但总长不受限；代价是要处理段间衔接、音画同步累积误差，以及 ffmpeg 拼接这一层新的失败面 |

**建议倾向分段渲染**：它是唯一不牺牲画质也不线性放大单次渲染风险的路径，而项目已经有自编译 ffmpeg（`media-toolchain`）具备拼接能力。但它引入的音画同步累积误差需要在 P0-1d 的时间轴模型里一并设计，不能事后补。

#### 已定的做法（产品负责人 2026-07-27）

**先按默认值跑通 MVP，再逐步加上限并实测性能依赖**，找到用户可接受的范围；若放宽后确实慢，就在提交前告诉用户"这条大约要等多久"，让他自己决定。

这个顺序是对的，但有三件事必须一起考虑：

**一、600 帧不是随便定的，它是从 300 秒墙钟倒推的。** 契约里没写这个理由，但算一下就清楚（`renderWallSecondsBase: 30` + `renderWallMillisPerFrame: 400`）：

```
12 秒 =  360 帧 → 174 秒（2.9 分钟）  ✅
20 秒 =  600 帧 → 270 秒（4.5 分钟）  ✅  ← 当前上限，距 300 秒墙钟仅余 30 秒
30 秒 =  900 帧 → 390 秒（6.5 分钟）  ❌ 超墙钟 90 秒
60 秒 = 1800 帧 → 750 秒（12.5 分钟） ❌ 超墙钟 450 秒
```

**所以只提 `SANDBOX_FRAMES_MAXIMUM` 而不提 `wallClockSecondsMaximum`，用户遇到的不是「慢」，是渲染被 stall guard 杀掉、直接失败。** 两个数必须一起调。代价是 stall guard 会变钝：原本 5 分钟无进展即判挂死，放宽到 15 分钟意味着真挂死时用户要多等 10 分钟——这是放宽片长的隐性成本，值得在放宽时同步给渲染加进度心跳，让"慢"和"死"能被区分。

**二、渲染耗时预估可以直接给用户看**，公式已经在契约里，选完片长就能算——"这条 60 秒的片子大约需要 12 分钟"。比事后转圈强，也符合负责人的想法。

**三、`640×360` 已提到 1280×720（2026-07-27 已落地，`35e552a`）。**

做法不是放大舞台，而是 `deviceScaleFactor: 2`：CSS 舞台仍是 640×360（`composition_template` 的整套排版都是为它写的），浏览器以 2 倍分辨率重新光栅化，**布局规则一行未动**。契约新增 `deviceScaleFactor` / `outputWidth` / `outputHeight`，以后上 1080p 把系数改成 3 即可。

实测：包内真实 Chromium 渲染真实零件，factor 2 的每帧开销落在 factor 1 的跑动噪声内，**接近免费**；factor 3 也在预算内，但成本不是像素的线性函数，再提要重测。真实验收 BM-08 通过，ffprobe 从产物读出 `h264 1280×720 / 30fps / 90 帧`。

顺带补掉两处无人看守的手抄常量（`worker.mjs` 的视口、BM-08 写死的期望帧尺寸），现在都从契约派生。

**原始记录（决策依据，保留）：** `workers/motion_composition/worker.mjs:31-32` 定死渲染视口为 `640×360`，而上游零件的设计尺寸是 `1920×1080`（`registry-item.json` 的 `dimensions`）。

- 20 秒的片子**还能发**，只是短；
- 640×360 横屏发到抖音／小红书**基本没法看**（那边是 1080×1920 竖屏）。

按默认值跑通 MVP 没问题，但**在拿给用户试用之前分辨率要先解决**——否则 MVP 验证到的是"链路能跑通"，验证不到"这东西有用"：用户的反馈会全部集中在画质上，真正想验的编排质量反而看不到。

**两个数吃同一份预算**：提分辨率让每帧变慢（0.4 秒/帧会涨），提帧数让总帧数变多，双方都撞 `wallClockSecondsMaximum: 300` 与 `cpuSecondsAbsoluteMaximum: 2400`。因此"测试对电脑性能有多依赖"这一步应当**同时测两个维度**，用同一份预算去比哪个更值得花。

### P0-2　片长可控，去掉写死的 12 秒

**现状**：`MOTION_BRIEF_FILM_SECONDS = beatCountDefault × secondsPerBeatDefault`，界面文案自己写着"这个入口暂时不能改片长；需要别的长度请用下面的固定模板手工制作"。

**注意原设计的顾虑是成立的**（`motion-one-sentence.ts` 注释）：不从句子里解析"三分钟"，因为模型解析错了就是片长错了而用户不知道为什么。**所以要加的是显式片长控件，不是让模型猜。**

**判据**：一句话入口能选片长，产出 mp4 的实际时长与所选一致；超出 `MOTION_DURATION_LIMITS` 上限时在提交前就拒绝，不是渲染完才失败。

### P0-3　给动效成片接上声音

**现状**：动效链路（`motion_authoring/` + `motion-*.ts`）**零音频痕迹**——没有配音、没有背景音、没有音效。

**TTS 其实已经买好放着了**：`edge-tts 7.2.7` 与 `azure-cognitiveservices-speech 1.41.1` 已打进素材成片 Worker 包（`contracts/quality/material-video-worker-package.v1.json`），只是那条链路没通，能力闲置。

**要决定的**：动效成片是复用素材 Worker 里的 TTS（跨 Worker 调用，多一层进程边界），还是在动效链路里单独接一份（多一份依赖与体积）。**建议前者**——同一个能力不做两份，与"单一构建路径"一致。

**判据**：一句话产出的 mp4 有可听的旁白，字幕/文案与旁白同源；静音是可选项而非默认。

### P0-4　打通智能素材成片（moneyprinterturbo）

**产品意图**（负责人 2026-07-27 澄清）：上游自带一套 WebUI，它必然已经对接好自己的逻辑，所以**不重做界面，直接把它展示出来**，用户在里面改时长等参数就能出片。入口 `open_material_video_studio`（`lib.rs:312`）打开的正是这个外部 URL 窗口——方向是对的。

**现状要分两层看，别混为一谈：**

**第一层「窗口点不动」——已修，但用户还没拿到修复。** 负责人报的症状是"弹了新窗口，里面的东西都点不动，没法实验能不能生成"。这就是 T108：上游 `webui/Main.py:1209` 每个会话首帧调 `tour.start()`（driver.js 引导层），给 body 加 `.driver-active`，其 `*{pointer-events:none}` 关掉整页；**而产品自己的注入脚本把气泡与遮罩 `display:none` 了，用户没有任何办法结束引导**，类名永不摘除。WebUI 每次换随机端口即新 origin，localStorage 每次为空，**每次打开必然复现**。

修复 `cadb047`（07-27 02:07）：样式表里 `body.driver-active *` 恢复 `pointer-events: auto !important`——driver.js 那条规则没有 `!important`，所以产品规则压得住，即使上游以后改了 storage key 也不会再变成死窗口。真实 Tauri 窗口验收报告 `driverActive: false`、`subjectPointerEvents: "auto"`，走的是完整用户路径并真的点击输入。

**关键时间差**：负责人试的是 07-26 的包，**不含这个 02:07 的修复**。07-27 之后出的包都含。

**第二层「能不能真生成视频」——仍然无人验证。** T108 的验收止步于"能点"，没有继续走到出片。台账在 Demo 选型时的原话是"生成侧已实测产出会动的 mp4，而**素材线一次都没走通过**"——那是在窗口可交互之前的结论，现在前提变了，**这条线到底通不通是未知，不是已知不通**。

**它真正的门槛**：这条线要 4 个外部依赖串联（模型、TTS、素材源、字幕），而动效线只要 1 个。Demo 选型时这是决定性理由，现在仍然成立——但那是"配置成本高"，不等于"走不通"。

**下一步是一次实验，不是一轮开发**：用 07-27 之后的包打开这个窗口，在上游 WebUI 里配齐参数走一次完整生成，看卡在哪个依赖上。这个结果决定后面是"补配置引导"还是"真要改集成方式"。

**它值得打通的原因**：产品要的"根据热点生成自己的视频"更接近**带实拍素材的口播视频**，而不是文字动效 / 数据可视化。动效线做不出前者。

**判据**：从一句话到带旁白与实拍素材的 mp4，全链路在正式包里跑通一次，产物能进发布工作区。

---

## P1 · 感知能力：读得进来

**这是产品设想里缺失最彻底的一整类能力**，也是运营者每天最耗时的部分。

**现状**：全仓库在执行器与领域层搜 `unread` / `inbox` / `未读` / `收到`，**零匹配**。现有的全是"发出去"：

- 私信 `direct_message_action.py`：`wait_for_profile_ready` → `enter_conversation` → 发送，是**主动触达**；
- 评论 `comment_action.py`：对候选目标（别人的视频）发表，是**主动评论**。

**缺的三件**（缺一件后面两件都不成立）：

1. **读收到的私信**——收件箱列表、未读标记、会话上下文抽取；
2. **读自己视频下的评论**——通知/消息中心、区分新旧、定位到具体作品；
3. **在此之上的回复动作**——回复与主动发起是两个动作，回执、幂等键、失败矩阵都不同。

**判据**：能列出"当前有几条未读私信/新评论"，并对其中一条完成回复且在平台侧可见；重复执行不会重复回复（幂等）。

---

## P2 · 调度器：把"工具"变成"员工"

**现状**：控制面**没有任何定时触发机制**。搜遍 `control_plane/`，`BackgroundTasks`/`apscheduler`/`celery`/常驻循环一个都没有。唯一命中 "schedule" 关键词的 `action_risk_policy.py:63` 是**风控最小间隔**，不是调度。

**影响**：产品设想的"每天自动抓热点、自动回复"目前全部要人打开 App 手动创建任务。这是"工具"与"AI 员工"的分界线。

**要连带想清楚的**：现有风控（任务级/每日硬上限、最小间隔、连续失败熔断、一键紧停）是为"人触发"设计的。调度器上来后，**熔断与紧停必须能压住调度器**，否则夜间无人值守时一次页面改版就会把配额烧光甚至触发平台风控。

**判据**：配一条"每天 9 点抓一次热点"，关掉 App 再打开，任务按计划执行过且记录完整；紧急停止能立刻掐断后续排期。

---

## P3 · 平台横向扩展

**现状**（域名反查，有实现必然出现）：

```
douyin.com    12 个文件      bilibili.com   5 个文件
xiaohongshu.com  0           kuaishou.com   0           channels.weixin.qq.com  0
```

执行器侧文件数：抖音 22 个，其余平台 0。`PublishMechanism` 把快手/小红书/视频号标为 `DEFERRED`（注释：deferred means no entry point exists），与实现一致。

**好消息**：抖音那套是按可复制结构写的——页面定位（`*_page.py`）、动作编排（`*_action.py`）、频控、结果解析分层清楚，`page_anchors.py` / `page_version.py` 还专门处理页面改版。第二个平台的成本应显著低于第一个。

**建议顺序**：小红书 → 快手 → 视频号。理由是内容形态与抖音最接近、运营者重合度最高。视频号放最后，因为它与微信生态绑定，可能与 P5 的技术选型互相影响。

**判据**：新平台至少覆盖登录态保持、发布、评论三件，且共用同一套任务/事件/风控骨架，不新开一套。

---

## P4 · 视频剪辑接线

**现状**：有壳无芯。`VideoEditingWorkbench.tsx` **842 行** UI 完整；数据存 **localStorage**（`automation-tool.video-editing.local-draft.v1`）；后端 `api/` 下**没有任何 video_editing 端点**——领域层（`domain/video_editing.py`、`aliyun_ims_editing_*.py`）与 Provider 抽象都写了，**没有一条 HTTP 路由把它暴露出去**。提交必然 fail closed。

代码注释自己写着：`the cloud editing provider chain (VE-04+) is not connected yet... there are no editing jobs and submission always fails closed as unavailable.`

**这是全项目投入产出比最高的一块**：领域层、Provider 抽象、一致性契约、842 行 UI 都在，缺的只是中间那段接线——API 端点 + 网关从 local 切到真实实现 + 任务状态回流。

**判据**：从剪辑工作台提交一个任务，阿里云 IMS 真实返回成片，产物出现在成片列表并可发布。

---

## P5 · 微信自动化：另一条腿

**现状**：**零痕迹**。`weixin.qq.com` 0 个文件；`WeChat.exe`、`微信.app`、`uiautomation`、`pywinauto`、`AXUIElement` 全部零匹配。代码里唯一沾"微信"的是 `PublishPlatform.WECHAT_CHANNELS`，那是**视频号**这个发布目标，与控制微信客户端无关。

**必须先定技术路线再谈排期**：现有 RPA 全建在 Playwright + 内置 Chromium 上，而微信客户端自动化是**桌面 UI 自动化**——Windows 上 UIAutomation、macOS 上 Accessibility API。这是另一套技术栈、另一套权限模型（macOS 要辅助功能授权）、另一套失败矩阵（窗口焦点、多开、版本升级导致控件树变化）。**不能按"再加一个平台 Adapter"估算。**

**还要先回答的产品问题**：自动通过好友申请、自动打招呼、自动回复聊天，这些动作的风险等级远高于公开平台的评论——微信对自动化的容忍度、账号封禁风险、以及用户是否接受 App 操作自己的私人微信，都要先有结论。

**判据**：在真实微信客户端上完成一次"通过好友申请 → 发打招呼 → 回复一条消息"，且全程可见、可人工接管、可紧急停止。

---

## P6 · AI 编排入口

**现状**：前端 `features/` 下没有任何对话/助手页面。唯一的模型入口是动效创作 agent（`VideoStudio.tsx:1375` "Hand a one-sentence brief to the authoring agent"）。

也就是说 **AI 目前只能"把一句话变成一条视频"，不能"帮你决定该做什么"**。产品设想里的 AI 员工——读热点定选题、读私信定怎么回、编排一天的工作——还没有落点。

**它排在最后不是因为不重要，而是因为它依赖前面**：没有 P1 的感知能力，AI 读不到东西；没有 P2 的调度器，AI 的决定无法执行。**这三条合起来才是"AI 员工"，单独做 P6 只会得到一个聊天框。**

---

## 附：这份路线图的边界

- 判据是"代码是否存在、用户能否走到"，不是"跑起来质量如何"。标注完整的部分不代表在真实平台上的成功率；
- 除视频生成引用了 07-27 04:06 真实 App 记录外，其余判定基于代码结构与实现痕迹，没有逐个真跑；
- 台账与代码里的枚举/映射表没有作为判据，只在与实现一致时作为旁证；
- 完整的功能盘点见 `docs/feature-completeness-audit-2026-07-27.md`。
