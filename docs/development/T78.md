# T78 修复视频桌面验收的完整启动链

> 状态：🔍 七条本地真实桌面驱动已通过；VE-04 缺云凭据，8/8 完整真实验收未闭环
>
> 日期：2026-07-26
>
> 原提交：`0e41d59b73ef56c9b8912b746675072089d96995`、
> `93252198d21530a891b99b3c8dd45f318bfe2dee`
>
> 合并后回归修复：`036c267`
>
> 证据补记：第三批事后补录，未与原代码同提交

## 结论

视频线不是“7 个脚本少调一次 prerequisite”，而是 8 个独立构建驱动、9 个唯一 spec
都缺完整启动环境。共享 `video_studio_startup_harness` 现在负责：

- 隔离环境变量与 AppData；
- 准备嵌入式浏览器、签名 Executor 和生产视频运行时；
- 用唯一 Compose project 启动隔离 PostgreSQL；
- 跑正式 Alembic 链；
- 在固定 8765 启动真实 Control Plane，并拒绝占用未知端口；
- 把 App build 和随后 WDIO 执行放在同一生命周期；
- 成功、失败和部分启动时只清理本轮拥有的资源。

后续 `9325219` 又移除 BM-08/IM-05 已被生产代码忽略的路径注入，改为将生产资源实际安装到
Tauri debug Resources，并修复资源根若是软链时会写穿其他 worktree 的问题。

## RED

第二批留下的实际 RED 链：

1. 只补 7 个 `prepare_startup_gate` 时旧门禁假绿，显示 `executed checks: 7`；
2. 动态门禁随后发现 8 个驱动均未导入完整 harness；
3. 生命周期测试报 `no attribute 'video_studio_startup_harness'`；
4. 后续 RED 分别锁住 WDIO 越出 harness、Control Plane 健康失败泄漏、Compose 部分启动失败
   不清理、环境污染；
5. 生产运行时复核又发现资源根是软链时会写穿目标，新增测试先证明外部 sentinel 被影响风险，
   再修为 unlink 并建立独立目录；
6. BM-08/IM-05 旧环境变量实际被生产单路径忽略，静态门禁先失败后改为真实 Resources staging。

原始完整终端 transcript 未随提交入仓，本文件不补造每轮用时。

## GREEN

### 分层门禁

- 动态驱动范围固定为 8 个构建驱动/9 个唯一 spec；
- 启动 harness 结构、生命周期、资源隔离与失败清理 11/11；
- `test_prepare_video_runtime.py` 4/4；
- `test_video_studio_runtime_staging.py` 8/8；
- 生产入口、类型检查、lint、Python Ruff/format 相关门禁通过。

### 真实本机桌面验收

root 串行实跑并记录全绿：

- VF-06；
- BM-06；
- BM-08；
- BM-15；
- CQ-01；
- IM-05；
- VE-03。

其中 VF-06/BM-06 的真实 App、真实 Rust IPC、真实 PostgreSQL/Control Plane 和实际视频
运行时均进入链路；VF-06 四个桌面 spec 全部通过。VE-04 在任何云提交前按设计因本机未配置
阿里云凭据响亮停止，未读取、复制主工作树密钥，也未触发付费动作。

### 合并后回归

`0e41d59` 重写 `run_bm_08_acceptance.py` 时删掉模块级
`CHROMIUM_CONTRACT`、`DEFAULT_ARCHIVES`、`_first_existing`，两个导入方没有同步，导致
backend integration 和 scripts 收集失败。主线 `036c267` 把共享常量/双布局解析搬到
`build_embedded_chromium_staging.py` 单一归属并修复三层红。这个回归属于 T78 的真实欠账，
不能从证据里省略。

## 失败矩阵

| 场景 | 结果 |
| --- | --- |
| 8765 已被未知进程占用 | 拒绝启动，不终止未知进程 |
| PostgreSQL/迁移/CP 健康失败 | 响亮失败并回收本轮资源 |
| Compose 只启动一部分 | finally 定向清理唯一 project |
| build 成功但 WDIO 越出 harness | AST 门禁拒绝 |
| 驱动通过同名假函数/死分支绕过 | 导入、shadow、调用图和可达性门禁拒绝 |
| debug Resources 根是软链 | unlink 后建立独立真实目录，外部目标不受触碰 |
| 视频运行时缺失/损坏 | staging/manifest/启动门禁拒绝 |
| 云凭据缺失 | VE-04 在外部副作用前停止 |
| 测试进程被中断 | runner 负责 App、driver、服务、Compose、AppData 定向回收 |

## 真实边界

七条本地驱动是真实隐藏 Tauri App 链路，不是只读源码或 UI Harness。VE-04 只验证到
“缺凭据时安全停止”，没有完成真实云渲染最终状态，因此整体证据保持 `🔍`，不把 7/8 写成
8/8。Windows 视频桌面链也未由这些 macOS 结果替代。

## 清理

每个驱动使用专属 Compose project、AppData、端口和临时目录；结束后回收 App、WebDriver、
浏览器子进程、Control Plane、PostgreSQL 和 staging。资源软链修复后不再写穿主工作树。

## 文档与遗留

- VE-04 需要在受控、明确授权且可计费的阿里云环境补真实验收；
- `036c267` 的共享常量修复应由 C4 跨模块 import 门禁继续守住；
- T79 的“124 个验收驱动聚合执行”没有被本任务的 8 个视频驱动门禁覆盖。
