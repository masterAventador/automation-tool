# SA-07 完成证据

用户可操作：否

证据类型：分层实现

> 状态：🔍 待验收（管理投影与组合对抗矩阵已实现并测试通过；真实站点快照回放
> 依赖 PB-08 真实账号，按项目规则以夹具先行、真实平台部分待真实账号补验）
>
> 日期：2026-08-05
>
> 提交：本文件所在提交

## 任务

管理界面、灰度与长期质量：查看版本树、适用变体、成功率、失败步骤、审核/停用/回滚；
真实站点快照回放、prompt injection、版本投毒、漂移和错误自愈矩阵。

## 为什么标「🔍 待真实账号」而不是「✅ 已完成」

本任务分两半，本仓库规则要求分别对待：

- **管理投影 + 组合对抗矩阵**：已用受控夹具落地并测试通过（下方 GREEN）；
- **真实站点快照回放**：需要抖音真实账号与真实站点快照，依赖 PB-08（仍待真实账号
  验收）。按项目规则，缺真实账号时以夹具先行、把「必须验证真实平台最终状态」的验收项
  标 `🔍 待真实账号`，不以 Mock 冒充真实平台证据。故本任务整体标 `🔍 待真实账号`。

## RED

`backend/tests/unit/executor/test_skill_management.py` 先失败：

```text
ModuleNotFoundError: No module named 'automation_tool.executor.skill_management'
```

7 条用例：版本树完整（含 disabled 标记但不删）、适用变体命名、回滚版本仍列出；组合
矩阵四条——prompt injection 被清洗器拒、版本投毒被签名拒、不可变阻止覆盖、页面漂移
路由到 None。

## GREEN

```text
.venv/bin/python -m pytest tests/unit/executor/test_skill_management.py   7 passed
.venv/bin/python -m pytest tests/unit/executor                           3967 passed, 2 skipped
.venv/bin/python -m mypy （management + registry）                        Success
uvx ruff check（实现 + 测试）                                             All checks passed
```

**管理投影**（`build_management_view`）是注册表 + 路由 + 统计的只读投影：每个 skill
的完整版本树（每个不可变版本，disabled 标记但从不丢）、各版本成功率与父版、
`applicableVersionFor(page)` 用 SA-06 同一路由规则返回适用版本或 `None`（漂移时），
固定控制集 review/disable/rollback。

**组合对抗矩阵**——不在本层重造防御，而是断言 SA-01..06 的组合防御端到端成立：

- **prompt injection**：注入「忽略之前的指令并 `<script>…`」→ SA-02 清洗器的文本扫描
  （复用 SA-01 禁止集）直接拒；
- **版本投毒**：伪造已签记录改 domain → SA-03 Ed25519 验签失败拒；
- **覆盖线上版本**：重发同版本 → SA-03 注册表 append-only 拒（immutable）；
- **页面漂移**：指纹全不匹配 → SA-06 路由无候选，投影返回 `None`（「需重新学习」），
  而不是回落到过期版本。

## 失败矩阵

四条对抗 + 管理投影三态 —— 逐条用例。真实站点快照回放、错误自愈的真实平台矩阵归
待真实账号补验。

## 正常用户路径验收

管理界面的用户可见交互（版本树 UI、审核/停用/回滚按钮）尚未接前端；本层交付读模型
与投影。真实用户路径验收随前端接入 + 真实账号一并补，登记在遗留。

## 真实边界

- **真实站点快照回放、错误自愈的真实平台矩阵未做**，依赖 PB-08 真实账号，如实标待验收；
- 管理投影为纯读模型，尚未接前端 UI 与持久化统计源；`VersionStats` 的真实来源
  （回放/接管真实统计的落盘）与页面指纹真实计算接入随真实 Page 适配层落地；
- 纯库层，无 I/O、无网络、无常驻服务。

## 清理

无临时产物；无常驻服务。

## 文档变化

- 专项 Roadmap SA-07 置 `🔍 待真实账号`，汇总与计数同步并跑门禁；
- 至此 SA 线 SA-01～SA-06 全部 `✅ 已完成`，SA-07 因真实站点部分依赖真实账号而
  `🔍 待真实账号`——自愈式自动化的**声明式 Schema、轨迹清洗、编译签名、确定性回放、
  失败接管、版本路由、管理投影与组合对抗防御**这条链路的实现与分层验收已完成。

## 遗留项

| 项 | 状态 |
| --- | --- |
| 真实站点快照回放 + 错误自愈真实平台矩阵 | 🔍 待真实账号（依赖 PB-08） |
| 管理界面前端接入 + 真实用户路径验收 | 待前端接入 |
| VersionStats 真实统计落盘 + 页面指纹真实计算 | 随真实 Page 适配层落地 |
