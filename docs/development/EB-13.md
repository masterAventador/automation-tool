# EB-13 完成证据

> 状态：🔍 待验收（内置 Chromium 迁移与既有矩阵已通过；真实平台最终状态与
> Windows 证据见遗留项）
>
> 日期：2026-07-25
>
> 提交：本文件所在的 EB-13 独立实现提交

## 任务

- 评论链路原样迁移：本期不接 Browser Use；ActionGate、内容哈希、
  prepare/dispatch/verify、单次发送与 outcome_uncertain 语义零重写，只把两个
  集成测试的启动来源换成摘要校验过的内置 Chromium。

## RED

迁移型任务：仅删系统 Chrome 常量与 skip 守卫、注入 `staged_embedded_chromium`
夹具（diff 4 insertions / 15 deletions，全部为启动来源替换）。首轮迁移后直接
3/3 GREEN，无先失败步骤，如实记录不构造伪 RED。

## GREEN

```text
uv run pytest tests/integration/test_douyin_comment_page_browser.py \
  tests/integration/test_douyin_comment_action_browser.py     3 passed（真实内置 Chromium）
uv run pytest tests/unit/executor                             822 passed, 1 skipped
uvx ruff check --config pyproject.toml tests/integration/     All checks passed
python3 scripts/check_embedded_browser_video_roadmap.py       通过
```

覆盖：评论页对象（夹具语料、无外呼、进程关闭）与评论动作生产链
（ActionGate + 内容哈希 + 单次发送 + ledger 一次性记录）。

## 失败矩阵

原样保留（未降级、未修改）：ActionGate 拒绝、重复发送拒止、outcome_uncertain、
页面遮罩/漂移；相应矩阵在 822 项执行器单测中回归。

## 正常用户路径验收

正式入口（App→Rust→Executor 评论命令）由 P 系列既有桌面 E2E 承载；本任务不改
产品代码。真实抖音评论的受控账号最终状态验证：`🔍 待真实账号`。

## 真实边界

- 同套页面对象在系统 Chrome（历史证据）与内置 Chromium（本任务）下分别通过；
- Windows 同链路证据由并行 Windows 会话补齐。

## 清理

- 测试经 `runtime.running` 关闭浏览器与进程树，staged Chromium 随 pytest 临时
  目录清理，无残留。

## 文档变化

- 激活专项 Roadmap 的 EB-13 并更新为 `🔍 待验收`、同步计数；
- Windows 待补清单新增 EB-13 待补节。

## 遗留项

- Windows 平台同链路证据已于 2026-07-25 补齐（Mac 经 SSH 远程发起、Windows 真机执行，见 `docs/development/windows-evidence-checklist.md` 对应节）。

- 真实抖音评论链路受控账号最终状态验收：`🔍 待真实账号`；
- Windows 平台同链路证据随并行 Windows 会话补齐；
- 上述闭合前保持 `🔍 待验收`。
