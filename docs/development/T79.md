# T79 124 个验收驱动缺少聚合执行者

> 状态：⬜ 未完成
>
> 核实日期：2026-07-26
>
> 第二批提交：无
>
> 证据性质：第三批只读核实，不是实现提交

## 结论

第二批没有完成 T79。`scripts/test_video_studio_startup_gate_drivers.py` 的 749 行改动属于 T78，
它只动态发现并检查 8 个调用 `build:tauri:video-studio-test` 的视频桌面构建驱动，不能替代
124 个验收驱动的聚合执行器。

## 核实证据

在当前 `codex/batch-3`、包含第二批与主线回归修复的 HEAD 上只读核实：

- `scripts/run_*_acceptance.py`：恰好 124 个；
- 全仓唯一对 `run_*_acceptance.py` 做 glob 的代码是
  `scripts/test_video_studio_startup_gate_drivers.py`；
- 该文件的 `video_studio_build_drivers()` 只选择含视频 App build token 的 8 个驱动；
- `scripts/run_script_tests.py` 明确只发现 `scripts/test_*.py` 与
  `deploy/**/test_*.py`，不执行 `run_*_acceptance.py`；
- `run_vf_01_acceptance.py` 在排除文档和 worktree 后的代码/配置中仍为零引用；
- 没有发现 workflow、package script、commit gate 或另一个 runner 聚合执行全部 124 个驱动。

因此，“视频启动链的 8 个驱动有结构门禁和七条真实实跑”与“T79 已完成”是两个不同命题。
前者成立，后者不成立。

## RED

T79 没有进入实现，所以不存在合格的任务级 RED。第三批核实只证明执行者缺失的现状，不能把
`rg` 零命中包装成已经完成 TDD。

真正开工时应先写能失败的聚合器测试，至少证明：

1. 新增一个没有执行者的 `run_*_acceptance.py` 会让门禁失败；
2. 只被 `read_text/readFile` 的驱动仍算“未执行”；
3. 子进程实际启动但零场景/零检查不能算成功；
4. 聚合器自身有确定执行者；
5. 慢、需凭据、需实体机或会产生费用的驱动必须有显式分类，而不是静默跳过。

## GREEN

无。T78 的 11/11 结构门禁、7 条真实视频驱动和 T72 的脚本测试聚合均不能作为 T79 GREEN。

## 失败矩阵

| 场景 | 当前结果 |
| --- | --- |
| 驱动被 npm/subprocess 真执行 | 部分存在，未统一统计 |
| 驱动只被测试读取源码 | 仍可能假绿，T79 未防住 |
| 驱动全仓零执行者 | 仍存在，`run_vf_01_acceptance.py` 是明确样本 |
| 新增第 125 个驱动 | 没有全局门禁要求为它指定执行者 |
| 驱动需云凭据/实体机/付费资源 | 没有统一显式分类与汇总 |
| runner 返回 0 但没有执行场景 | T72 只守脚本测试 checks，不覆盖验收驱动 |
| 124 条串行成本过高 | 尚无分层/分片/缓存策略，不能以成本为由假装已执行 |

## 真实边界

本文件是只读审计结论，没有启动 124 条验收、没有访问云凭据、没有产生费用，也没有声称
48 条“只读源码”的原统计已在当前 HEAD 重新逐条分类。主线记录的历史分类
“37 被 npm 点名、4 被真 subprocess 执行、48 只读源码、35 零执行者”仍需要 T79 实现时用
机器可执行规则重新生成和验证。

## 清理

没有启动服务、浏览器、容器或 App；没有生成临时文件。

## 文档与下一步

T79 应作为独立开发任务执行 TDD，并与 C5 区分：

- C5 只核实 deploy 的 48 条断言是否进入脚本测试聚合；
- T79 负责 124 个真实验收驱动的“引用不等于执行”问题。

在聚合执行者、可数真实输出和显式环境分类完成前，T79 必须保持未完成。
