# U9-06 账号与设备管理验收

- 状态：✅ 已完成
- 日期：2026-07-23
- 提交：本任务独立提交
- 范围：补齐独立认证的账号运维入口、当前用户设备查看/吊销，以及真实 Tauri App 的账号与设备纵向验收；不开放匿名注册、公开恢复申请或运维 HTTP 路由
- RED：先新增账号设备应用边界与 HTTP 契约测试；首跑在收集阶段 2 个模块均准确失败于 `application.account_devices` 不存在，证明当前代码尚无“从 access Session 解析当前用户后列出/吊销其设备”的能力
- 运维 RED：新增独立账号运维 CLI 的 capability、stdin 秘密边界与固定失败测试；首跑在收集阶段准确失败于 `bootstrap.account_operations_cli` 不存在，现有底层服务尚未形成可认证、可审计且不经 argv 传递密码的运维入口
- App RED：扩展原生账号 gateway 契约覆盖设备列表与吊销；167 项既有 Node 契约保持全绿，聚焦 Vitest 准确失败于 `gateway.listDevices is not a function`，证明设备管理尚未越过 Rust 私有 access Token 边界进入安全投影
- GREEN：新增当前账号 Installation 列表与原子吊销 API；身份只从 Rust 私有 access Session 解析，响应不含设备凭据、公钥或账号秘密。吊销同时失效 Installation、当前设备凭据与存活设备 Session，并写入 append-only `device.revoked` 审计。
- 运维入口：新增独立 `automation-tool-account-operations` CLI，通过 `atoc1` capability 的服务端摘要认证；密码与 capability 只从有界 JSON stdin 接收，不进入 argv、HTTP、日志或返回体。支持创建、停用、恢复和签发一次性恢复能力，全部复用 U9 账号领域、事务与审计。
- App：React 提供当前账号设备列表和吊销交互；正式调用经 Tauri Rust 网络桥完成，access Token 不返回 React。401 会清理原生账号 vault，吊销当前 Installation 后下次启动 fail closed。
- 真实纵向验收：隐藏真实 Tauri App 共 8 次启动，连接隔离 PostgreSQL/FastAPI，完成登录与自动绑定、重启恢复、断网、Session 失效、重新登录、账号停用、账号恢复、设备吊销及再次启动拒绝。最终数据库确认账号 revision `3`、credential version `2`、Installation revision `2/revoked`，审计含账号创建/停用/恢复、三次设备绑定和一次设备吊销。
- 失败矩阵：覆盖缺失/错误认证、跨账号访问、非法 revision/request ID、重复或并发吊销、失效账号、缺失/篡改/过期/重复绑定证明、数据库异常、运维 capability/配置异常、断网和重启恢复；错误统一脱敏且 fail closed。
- 门禁：后端 `2342 passed, 5 skipped`，语句 `15935/15935`、分支 `3670/3670`，总覆盖率 `100%`；Ruff、格式、Mypy、OpenAPI 生成检查、生产边界检查均通过。前端 Node `169`、Vitest `253`、Playwright `5` 全绿；Rust 全目标测试、Clippy `-D warnings`、fmt、production build 及真实 Tauri U9-06 验收全部通过。
- 清理与边界：真实验收仅使用本项目隔离数据库、端口、App 数据目录与隐藏窗口，完成后清理；正式包不包含 WebDriver、测试命令或测试凭据。未部署云环境，Wave 10 的域名、TLS 与云资源仍从 `C10-01` 开始。
