# PC-10 — 端到端真实验收：正式包里一句话出片

> 状态：**已完成** ✅
> 日期：2026-07-29
> 分支：`feature-audit`
> 提交：`e369181`

用户可操作：是

---

## 1. 这一项要证明什么

台账写的是：「正式包里一句话出片，成片确实用上零件、有声音、时长与请求一致；
证据是 `ffprobe` 从产物读出的读数，不是「窗口能点」」。

三件事分开看：

| 要证的 | 本轮结果 |
| --- | --- |
| 正式包里走用户路径出片 | ✅ 真实 Tauri App，WebdriverIO 从设置模型 → 填一句话 → 点按钮 → 等成片 |
| **成片确实用上零件** | ✅ 第一次做到，见 §3 |
| 时长与请求一致 | ✅ 按路线 A 的定义：片长 = Σ 镜头，20 秒 brief 出 32 秒片子 |
| 有声音 | ❌ **本项不覆盖**，旁白（PC-07）还没接，见 §6 |

## 2. 怎么跑的

```
backend/.venv/bin/python scripts/run_t36_acceptance.py
```

真实链路，无 Mock：签名过的 Executor、内置 Chromium、真实百炼模型、隔离的
PostgreSQL 与 Control Plane、WebdriverIO 驱动真实 Tauri 窗口。退出 0。

```
[webkit] T36 一句话自动制作的真实 App 用户路径
   ✓ configures the model, refuses an empty brief, then authors, renders and plays
1 passing (4m 3.9s)
```

## 3. 成片读数

```
.local/kept-evidence/2026-07-29-pc10-用上零件且不重复.mp4
h264 / 1920×1080 / yuv420p / 30fps / 960 帧 / 32.000 秒 / 968,120 字节
```

分镜——**从留档产物本身量出来的，2026-07-29 复核时更正**：

```
镜1   0.0s +3.0s   模板   本周销售增长报告 / 三大核心驱动力助推业务突破
镜2   3.0s +3.0s   模板   +32% / 本周销售额环比增长率
镜3   6.0s +3.0s   模板   新客户拓展 / 精准获客渠道带来高质量线索
镜4   9.0s +3.0s   模板   产品优化升级 / 核心功能迭代提升用户体验
镜5  12.0s +3.0s   模板   营销策略创新 / 多渠道整合营销提升转化效率
镜6  15.0s +15.0s  零件   data-chart ← 声明 3 秒，动效自带 15 秒，取 max
镜7  30.0s +2.0s   模板   持续增长 未来可期 / 三大驱动力构筑竞争壁垒
```

两条时间轴各自闭合，不需要任何余数去圆：

```
声明时间轴  3+3+3+3+3+3+2 = 20.0 秒   ← 正是 brief 请求的片长
成片        3+3+3+3+3+15+2 = 32.0 秒 = 960 帧 @30fps   ← ffprobe 读数
```

片长 = Σ 镜头，且 `max(语音, 动效)` 只在镜6 上真正做了选择（声明 3 秒 vs 动效
15 秒）——**PC-08 的时间轴主宰规则在产物里看得见**，不是只在单测里成立。

工作区里落下了 `catalog/items/data-chart/data-chart.html`——**零件被真正写进渲染
计划并渲染了，这是这条线上第一次**。

### 3.1 初稿这张表是错的，错法值得记下来

初稿写的是「8 镜、每镜 2.5 秒」，还用一句「本轮 960 帧对应零件那一镜略短」去圆
`7×75 + 450 = 975` 与实测 960 的 15 帧差额。

**那张表根本不是这条片子的**，是同一 brief 另一轮的 `storyboard.json`。逐帧比对
当场戳破：台账写镜2 是 `+27.3%`，产物里是 `+32%`；写镜3 是「新客转化提升」，
产物里是「新客户拓展」。

真结构用亮度采样量出来（零件是浅底、模板是深蓝，切点一目了然）：

```
ffmpeg -i <片子> -vf "fps=10,signalstats,metadata=print:key=lavfi.signalstats.YAVG" -f null -
→ 切点恰好落在 3.0 / 6.0 / 9.0 / 12.0 / 15.0 / 30.0 秒，无一例外
```

15 帧的差额不是「略短」，是**镜数从一开始就数错了**。这是本条线反复出现的同一个
形状：**先有一个对不上的数，然后编一个算术解释盖住它，而不是去量一遍。**
（PC-19 是同一个形状的更贵版本：所有机械读数全绿，而片子是同一段播两遍。）

面向未来的防线见 §4.1——分镜表不再靠人誊抄，由验收脚本从产物里重算并比对。

### 为什么此前一直用不上零件

不是零件缺失，是**片长写死 12 秒**：2026-07-28 对真实模型量过，12 秒预算下最短的
零件也要 4.5 秒、占掉 37%，模型每次都判定放不下——而且判得对，提示词本来就告诉它
零件时长从整片预算里扣。134 个零件在原理上够得着、实际一个都用不上。
片长控件（PC-09 / PC-19）落地之后这一项才可能通过。

## 4. 不重复：这一轮新加的门禁

**上一条被当作里程碑的成片是错的。** PC-19 记着那次抽帧比对：12 秒的片子里
t=1s≡7s、3s≡9s、5s≡11s 逐字节相同——同一段 6 秒倍速播了两遍，而编码、分辨率、
帧数、时长、静帧门禁**当时全绿**。

所以验收脚本加了 `require_no_repeated_stretch`：按 0.5 秒抽帧算指纹，查**整段
周期性复现**。

**第一版判据是错的，被真片子当场纠正。** 它把「任何两帧相同」判成重复，第一次
遇到真实成片就误判：`data-chart` 动十秒、定格五秒，64 个采样 55 个唯一，重复全在
那段定格里——而定格恰恰是动效该有的行为（`film_timeline.py` 的原话：动效是配菜，
让内容不突兀地出现，然后停在最后一帧）。改成查周期性之后拿两条真片子做对照：

| 片子 | 新判据 |
| --- | --- |
| 本轮成片（7 镜、含零件、960 帧） | 通过：63 采样 55 唯一，无周期复现 |
| 2026-07-28 那条已知有病的旧片 | **拒绝**：每 3.0 秒重复一次，17/17 帧对得上 |

两头都夹住了：既要放过好片子，也要拦住已知的坏片子。

## 4.1 镜头结构也不再靠人誊抄（2026-07-29 补）

上面 §3.1 那次错记暴露的是另一类缺口：**重复检测查的是「有没有播两遍」，
没人查「镜头是不是按分镜切的」。** 一份分镜表在证据文件里对不上产物，
当时没有任何机械手段看得见。

判据已经写好并有测试：`scripts/motion_shot_structure.py`。

1. **`describe_shots`** 打印镜头表——序号、起点、时长、合计。证据文件应当引用这段
   输出，而不是由人对着屏幕誊抄。这次错记就是誊抄错的。
2. **`refuse_duplicate_shots`** 按各段时长取每镜中点抽帧，要求任意两镜的中点画面
   不相同。这是精确判据、不需要调阈值，且**正面命中 PC-19 那类缺陷**：那条坏片的
   每个模板镜头都把整部合成重渲了一遍，各镜中点必然雷同。它与周期性检测互补——
   后者查「整段 footage 播了两遍」，这条查「两个镜头画的是同一样东西」。

**中点而不是起点**：实测这部片子的六个模板镜头**全都从同一帧开始**（3.0/6.0/9.0/
12.0 秒处亮度都是 47.26，那是入场动画尚未开始的纯背景）。按起点抽帧会把一条健康的
片子判成重复。

### 4.2 它还没接进验收脚本，原因记在这里

接上去需要每一镜的时长，而**分段文件在渲染结束时被产品主动删掉**
（`lib.rs` 的 `remove_dir_all(outputs.join(MOTION_SEGMENT_DIRECTORY))`，
它们只是中间产物，删得对）。`segments` 也没有到前端——Rust 侧内部消费掉了，
所以 WebdriverIO 的 spec 同样读不到。

三条路都试过判据，两条不成立：

| 路 | 结论 |
| --- | --- |
| 为验收保留分段文件 | **不做**。为了测试改产品的清理行为，正是「单一构建路径」要挡的那类 |
| 从画面推断切点 | **不成立**。场景检测只找到 6 个切点里的 2 个（模板卡共用蓝底）；亮度落差 2.61（真切点）与 2.37（镜内动画）重叠，无阈值可分 |
| 让镜头结构成为产品数据 | **可行，另开任务**。帧数在 Rust 里本来就有（`segment.frame_count()` 已累加进 `total_frames`），写进渲染作业输出目录即可——那个目录本来就保留。它同时是产品该有的能力：PC-09 需要向用户解释「20 秒的请求为什么出 32 秒的片子」，而答案正是这张镜头表 |

在第三条落地之前，这个模块只有单测在跑。**这一节不声称它已经在守着任何东西。**

先试过两条更直觉但不成立的做法，记下来省得再试：

- **场景检测**（`select='gt(scene,0.3)'`）在这部片子上只找到 2 个切点（15.0 / 30.0
  秒，即零件的进出）。六个模板镜头共用同一张蓝底，只有中间文字变，场景分不够。
  调低阈值就变成对这套模板过拟合；
- **亮度突变**分不开。真实切点的落差是 2.61，而镜头内部入场动画的相邻步长是 2.37
  ——两者重叠，任何阈值都会同时误判两边。

而中点比对不依赖任何阈值：两帧要么逐字节相同，要么不同。

## 5. 失败矩阵

| 情形 | 期望 | 结果 |
| --- | --- | --- |
| 空句子提交 | 当场拒绝，不建任务 | 绿 |
| 模型密钥经真实设置表单保存 | 保存后输入框清空，页面任何地方都搜不到密钥 | 绿 |
| 成片画布 | 与 `motion-render-canvas.v1.json` 声明的 16:9 成片画布一致 | 绿 |
| 单帧成片 | 拒绝 | 绿（`nb_read_frames ≥ 2`） |
| 整段重复 | 拒绝 | 绿（见 §4 对照） |
| 上一轮失败 | 保留 App 数据目录供诊断 | 绿（上一轮确实保留了，本轮成功后清理） |

## 6. 真实边界

- **旁白没有覆盖。** 台账原文写了「有声音」，而 TTS（PC-07）尚未接入，
  `voice_seconds` 目前恒为 `None`。本项不声称这一条，等 PC-07 落地后单独补验收。
- **只在 macOS 验过。** Windows 的正式包、权限与内置 Chromium 链路另行验收。
- 本轮用的是受控测试目标与自有账号，无对外副作用。

## 7. 清理

- 隔离的 PostgreSQL 容器与 Control Plane 由脚本自行停止；
- 上一轮失败保留的 App 数据目录已删除；
- 抽帧用的临时 PNG 在临时目录中随进程退出清理；
- 两条成片留档在 `.local/kept-evidence/`（未进 Git）。

## 8. 文档

- `docs/product-completion-roadmap.md`：PC-10 置为 ✅；PC-09、PC-18、PC-19 的
  「剩：压在 PC-10」随之收口。
- `scripts/run_t36_acceptance.py`：新增 `require_no_repeated_stretch`，判据与两条
  对照片子的结论写在函数注释里。
