# LE-08 自适应抽帧

用户可操作：否
证据类型：分层实现

## 日期与状态

- 开工日期：2026-07-30
- 当前状态：T1～T4 已完成，T5 首轮 Review 两项 P2 已修复，等待修复提交复审
- 前置：LE-07 `🔍 待验收`，公开 `PackagedMediaTools` 与随包 ffmpeg/ffprobe 路径边界已交付

## 范围

本任务在 Local Executor 侧完成自适应抽帧：先按场景切点取帧，再对超过 8 秒的长镜头按
时间补抽，按素材时长分档封顶并在超限时优先保留切点，最终产出最长边 768px 的 JPEG。
本任务不调用多模态模型、不写回 Material、不装配 Tauri Worker，也不建立用户界面。

## 开工前【先定】

### LE-07 交接与有界运行器

`PackagedMediaTools` 是公开边界，直接使用并在每次执行前 `revalidate()`。LE-07 私有
`_run_bounded` 不跨模块导入；本模块照 `_require_tool` 的先例重述运行器，并保留四条硬性质：

1. ffmpeg 输出落临时文件，不用 stdout/stderr 管道承载图片；
2. 子进程运行时持续统计输出文件总字节数；
3. 超过字节上限立即杀死并有界回收子进程；
4. 所有拒绝理由由返回值携带，不在临时目录清理路径中抛出业务拒绝。

运行器丢弃 ffmpeg stdout/stderr，避免源路径和媒体 metadata 进入异常或日志。临时目录创建、
打开、枚举、读取失败统一为 `WORKSPACE_UNUSABLE`；正退出但非零为 `UNDECODABLE`，信号退出
为 `TOOL_FAILED`，超时与输出超限各有封闭理由。清理失败必须被吞掉，不能改写已经确定的
业务结论。

### 场景、补抽与分档

- 场景表达式固定为 `select='eq(n,0)+gt(scene,0.1)'`；不改用已实测漏切点的 `scdet`；
- 首帧总是候选切点；相邻场景候选间超过 8 秒时按 8 秒间隔补候选；
- 时长与帧数上限固定如下，边界值归较短一档：

| 素材时长 | 帧数上限 |
| --- | --- |
| ≤15 秒 | 6 |
| ≤60 秒 | 12 |
| ≤300 秒 | 24 |
| ≤1200 秒 | 40 |
| >1200 秒 | 60 |

超上限时先保留场景切点；切点自身已超过上限时按时间均匀抽取切点，首帧必留。剩余额度再从
时间补帧中均匀选择。不能用简单“每 N 个取一帧”让尾部或密集切点系统性消失。

### 输出

- JPEG 最长边固定 768px，不放大小图；保持原画幅，不裁切、不加边；
- 最终文件使用稳定序号命名，结果只携带受控文件名、时间戳、是否场景切点和字节数，不携带
  源路径；
- 工作区不可写沿用 LE-07 的 `WORKSPACE_UNUSABLE` 语义：问题在本机暂存空间，不把它误报
  成用户素材损坏。

## Task 进度

| Task | 内容 | 状态 |
| --- | --- | --- |
| T1 | 本模块有界 FFmpeg 运行器 | 已完成；`75b29e7` 复审干净 |
| T2 | 场景检测抽帧 | 已完成；`b5069f1` Review 干净 |
| T3 | 长镜头按时间补抽 | 已完成；`b6e4d51` Review 干净 |
| T4 | 分档封顶与保切点降采样 | 已完成；`9b49906` Review 干净 |
| T5 | 768px JPEG 产出、失败矩阵与模块收口 | 首轮 Review 两项 P2 已修复；待复审 |

## T1 RED 目标

1. 子进程输出总量恰好等于上限时成功并返回文件；多 1 字节时拒绝；
2. 子进程在运行中超过上限时立即被杀，不能执行尾部完成标记；
3. 超时子进程被杀并回收，不能留下完成标记或后台进程；
4. stdout/stderr 均为 `DEVNULL`，图片只从临时目录文件读取；
5. 非零退出已经确定为 `UNDECODABLE` 后，即使临时目录清理失败也不能改判或抛出清理异常。

首次运行 `pytest tests/unit/executor/test_adaptive_frame_extraction.py -q` 在收集期按预期
失败：`adaptive_frame_extraction` 模块尚不存在；没有生产实现参与本轮。

## T1 GREEN 与捕获力

新模块只实现有界子进程和输出收集，不含场景表达式、时间补帧或降采样算法。真实 Python
子进程代替 ffmpeg 写入临时文件，验证：

- 恰好 64 字节成功，多 1 字节返回 `OUTPUT_LIMIT_EXCEEDED`；
- 写出 65 字节后继续休眠的进程在完成标记前被杀；
- 0.1 秒 deadline 杀死并回收仍准备休眠 5 秒的进程；
- Popen 的 stdin/stdout/stderr 全部为 `DEVNULL`，结果只从文件读取；
- 子进程 exit 1 已返回 `UNDECODABLE` 后，清理函数先真实删除目录再抛错，业务结果保持不变。

门禁结果：T1 文件 `5 passed`；两个改动 Python 文件 Ruff format/check 通过，严格 mypy
通过。捕获力抽查临时把退出后的总字节比较从 `>` 改成 `>=`，精确上限用例立即变为
`1 failed, 4 deselected`；还原后 `5 passed`，工作树未保留变异。

## T1 首轮 Codex Review 修复

提交 `8f7b6c1` 的逐任务 Review 找到两项 P2，均先补 RED 再修：

1. 监控逻辑抛出异常时，原实现先进入 `Popen.__exit__` 的无界等待，之后才 kill。故障注入让
   子进程休眠 1 秒，RED 看到自然退出码 `0`；修复把 kill/reap 移进 `with process` 内层异常
   路径，GREEN 断言子进程已由信号终止并回收。
2. ffmpeg 因工作区不可写而以正退出码失败时，原实现直接返回 `UNDECODABLE`。RED 注入受控
   工作区写失败，实际得到错误的素材判定；修复在判素材损坏前以受控探针文件执行
   create/write/flush/fsync/unlink，失败返回 `WORKSPACE_UNUSABLE`，探针内容与文件名均不含
   源路径或媒体信息。

修复后本文件 `7 passed`；Ruff format/check 与严格 mypy 通过。清理失败仍由原用例守住，
不能改写已经确定的业务结论。

修复提交 `75b29e7` 已推送；逐提交 Codex 复审重新运行定向 pytest、Ruff 与严格 mypy，
结论为未发现需要上报的新增缺陷。T1 至此完成。

## T2 RED 目标

1. 生产入口直接消费公开 `PackagedMediaTools`，每次抽帧前重新校验随包 ffmpeg；
2. ffmpeg 过滤表达式精确使用
   `settb=1/1000,select='eq(n,0)+gt(scene,0.1)'`，配合 filter timebase 与
   `frame_pts` 让受控输出文件名携带毫秒时间戳；
3. 随包 ffmpeg 真实生成并读取一条 3 秒、3 个硬切场景的视频，抽帧时间必须精确为
   `0/1000/2000 ms`，每帧为真实 JPEG；
4. 同样长度的单场景视频只能抽到 `0 ms` 首帧；
5. 抽帧前后用 LE-07 公开文件稳定性操作闭合使用窗口，返回值与异常消息均不携带源路径。

首次只运行 T2 真实媒体文件时，收集期按预期因生产模块尚无 `extract_scene_frames` 失败；
随后才写生产实现。

## T2 GREEN 与捕获力

生产入口直接接收 `PackagedMediaTools`、源文件和 LE-07 公开操作交出的已批准 stat。调用顺序
固定为工具重校验 → 源文件稳定性前检 → 有界 ffmpeg → 源文件稳定性后检；工具失效返回
`TOOL_FAILED`，源文件已变化返回 `SOURCE_UNAVAILABLE`，二者均不抛带路径的业务异常。

本阶段固定 15 分钟场景扫描 deadline 和 64 MiB 中间 JPEG 总量上限；这只是 T2 扫描保护，
不代替 T4 的最终帧数封顶。输出使用 12 位毫秒 PTS 受控文件名，解析后只保留时间戳、场景
切点标志和 JPEG bytes，两个承载 bytes 的 dataclass 都从 `repr` 隐去内容。

真实随包 ffmpeg 构造的 3 秒硬切视频精确产出 `0/1000/2000 ms` 三张 JPEG；3 秒单场景只
产出 `0 ms` 首帧。测试同时钉住 filter 字符串、`enc_time_base=filter`、`frame_pts=1`、
特殊字符路径、每次工具重校验和前后两次源文件稳定性检查。T1+T2 专项共 `11 passed`；
三个改动 Python 文件 Ruff format/check 与严格 mypy 通过。

捕获力抽查临时把场景阈值从 `0.1` 改成 `1.0`，三硬切测试实际从三帧变为仅首帧并失败；
还原后该用例重新通过。另分别先写 RED 证明真实 JPEG bytes 会进入两个 dataclass 的默认
`repr`，再以 `field(repr=False)` 收口；工作树未保留任何变异。

提交 `b5069f1` 已推送。逐提交 Codex Review 复跑 `11 passed`、Ruff 和严格 mypy，并额外
用非零起始 PTS、截断媒体、metadata 媒体和多视频流核对时间戳、拒绝码与隐私边界；结论为
未发现该提交新引入的明确缺陷。T2 至此完成。

## T3 RED 目标

1. 镜头跨度恰好 8 秒不补帧，超过 8 秒时从镜头首帧起每 8 秒补一帧；
2. 每个场景切点重新开始 8 秒间隔，补帧不得与切点重复；
3. 随包 ffmpeg 真实生成一条 60 秒渐亮、无硬切的单场景视频，最终候选时间必须精确为
   `0/8000/16000/24000/32000/40000/48000/56000 ms`；
4. 真实补帧 JPEG 的中心像素必须随时间严格变亮，证明实现实际 seek 到对应画面，而不是只
   给同一张图改时间戳；
5. 每次补帧仍走 T1 有界文件输出，并在每次工具使用前重校验公开 `PackagedMediaTools`。

首次运行 T3 文件在收集期按预期因生产模块尚无 `extract_adaptive_frame_candidates` 失败；
随后才写生产实现。

## T3 GREEN 与捕获力

补帧计划按每个场景的 `[切点, 下一切点或素材末尾)` 独立计算；跨度恰好 8 秒不补，超过
8 秒才加入 `切点 + 8000 ms`，之后继续按 8 秒递增。实际取图对每个计划点单独启动随包
ffmpeg，使用输入 seek、`copyts`、1/1000 filter timebase 和 `frame_pts`，因此候选保存的是
相对素材起点的实际解码帧 PTS，而非调用方计划值；`start_at_zero` 负责消除容器非零起始
PTS。每次进程都重新校验工具和源文件前后身份，并共享一个 15 分钟补帧 deadline 与 T2
剩余的 64 MiB 总量预算。

真实 60 秒渐亮单场景精确得到 8 个候选：
`0/8000/16000/24000/32000/40000/48000/56000 ms`；逐张解码 JPEG 后中心亮度严格递增。
贴边/切点重置表覆盖 8 秒、8.001 秒和多场景。另以故障注入让两个 seek 目标落到同一实际
PTS，RED 得到重复 `(0, 20000, 20000)`；修复后统一按时间戳去重，插入顺序保证场景切点
优先于补帧。另用 `output_ts_offset=1.6` 制造 12 秒真实非零起始 PTS 素材，修复前补帧被
记成 `9600 ms`，加 `start_at_zero` 后恢复为素材相对时间 `8000 ms`。

T1～T3 专项共 `24 passed`；三个改动 Python 文件 Ruff format/check 与严格 mypy 通过。
捕获力抽查临时把间隔从 8 秒改成 9 秒，贴边矩阵 3 项和 60 秒真实媒体 1 项同时失败；还原
后 5 项重新通过；另临时删除 `start_at_zero`，非零起始 PTS 用例从 `8000 ms` 退化为
`9600 ms` 并按预期失败。两项变异均已还原，工作树未保留变异。

首轮 Codex Review 复现 P1：240 帧、`30000/1001 fps` 的真实 MP4 容器时长为 `8008 ms`，
实际尾帧却早于 `8000 ms`；按容器时长计划的尾部 seek 不产帧，旧实现把整段已完整扫描
成功的素材误判为 `undecodable`。新增真实 NTSC 边界用例先稳定 RED；修复后，仅当最后一个
补抽目标没有可解码帧时停止补抽，前面已取得的场景帧仍正常返回，非尾部抽帧失败仍保持拒绝。
修复提交复审又找到两项 P2：展平补抽列表的末项可能仍属于更早镜头，且成功进程也可能返回
空文件集合。两项均先加故障注入 RED；最终只有“最后补点且该点位于最终镜头”可容忍无帧，
更早镜头的末项、非末补点的 `undecodable` 与空输出全部保持拒绝。

第三轮复审继续找到一项 P2：素材可能只有 1 秒视频流，却有 20 秒音频流与容器时长；此时
第一个 `8000 ms` 补点已位于最终镜头但并非整个计划的末项，旧实现仍误判素材损坏。新增
真实随包 ffmpeg 音视频异长用例先稳定 RED；修复后，场景扫描已成功解码完整视频流的前提下，
最终镜头内首次无帧即视为视频 EOF 并停止后续补抽，而更早镜头内的失败与空输出仍保持拒绝。
第四轮复审指出最终镜头里的普通 `undecodable` 也会被旧分支静默吞掉。故障注入先证明
ffmpeg 非零退出错误地返回已有帧；修复为让补抽命令以 `strict=unofficial` 在正常 EOF 时
成功退出并交出可辨识的空文件集合，只在最终镜头容忍该空结果，任何非零退出仍保持拒绝。
第五轮复审实测上述合规放宽会把补抽 JPEG 从全范围 `yuvj420p/pc` 改成有限范围
`yuv420p/tv`。真实 12 秒恒定黑色视频先看到切点帧与补抽帧解码为 `(0,0,0)` 与
`(16,16,16)`；改为显式 `pix_fmt=yuvj420p` 后两帧颜色一致，同时正常 EOF 仍成功交出空结果。

修复提交 `b6e4d51` 已推送。最终逐任务 Codex Review 复跑真实随包 FFmpeg 专项
`24 passed`、Ruff 与严格 mypy，并额外核对正常 EOF、非零起始 PTS、异长音视频及奇数尺寸
像素格式转换；结论为未发现该提交新引入的可操作缺陷。T3 至此完成。

T3 入口完整证明“所有长场景候选都能真实取到”。T4 的最终编排必须先按时长档位选出保留的
候选时间戳，再只 seek 入选补帧；禁止先为 4 小时单场景生成 1799 张 JPEG、事后再丢到 60
张。T2 场景检测本身仍受 64 MiB 运行中上限保护，切点超档的选择归 T4。

## T4 RED 目标

1. `15/60/300/1200 秒` 四个边界值都归较短一档，多 1 ms 才进入下一档；四小时上限为
   60 帧；
2. 切点不超档时全部保留，只从时间补帧中均匀填满剩余额度，入选数大于 1 时同时覆盖首尾；
3. 切点自身超档时只均匀选择切点，首帧和尾部切点都保留，不再分配时间补帧；
4. 四小时单镜头虽然能规划出 1799 个补点，但场景首帧占用一个名额后，只允许实际执行
   59 次补抽，最终返回 60 帧，证明封顶发生在 seek 之前。

首次运行 T4 文件按预期失败：生产模块还没有时长分档和候选选择函数；四小时编排用例则会
暴露旧实现先执行全部补抽再返回 1800 帧的行为。没有为通过测试提前修改生产实现。

## T4 GREEN 与捕获力

时长上限按 `≤15s/≤60s/≤300s/≤1200s/>1200s` 固定为 `6/12/24/40/60`，边界多
1 ms 才换档。候选选择先处理切点：切点达到或超过上限时，以首尾时间戳划分等距时间目标，
为每个目标选择最近且保持单调的切点，不再补帧；切点未超档时全部保留，再以同样方式从补点
中填满剩余额度。补点只剩一个名额时选择最接近真实时间中点的候选，避免固定偏向开头。

选择发生在补抽循环之前。四小时单镜头虽规划出 1799 个补点，实际只调用 59 次 seek，加上
场景首帧精确返回 60 帧；首个补点 `8000 ms` 和末个补点 `14392000 ms` 都被保留。场景扫描
仍先在 T2 的 64 MiB 有界进程内完成；若切点本身超档，只在内存中选择已生成的切点 JPEG，
不会再启动补抽进程。

首轮 GREEN 时 T1～T4 专项共 `38 passed`；四个改动 Python 文件 Ruff format/check 与
严格 mypy 通过。捕获力抽查临时把等距采样末索引从 `len - 1` 改成 `len - 2`，补帧尾点、
切点尾点和四小时末补点三项断言立即失败为 `3 failed, 11 passed`；还原后 `38 passed`。

首轮 Codex Review 找到一项 P1：原实现等距取候选元组下标，不是等距取时间轴目标；切点
密集分布在素材开头时，会把大多数名额挤在首秒，正是【先定】禁止的“每 N 个取一帧”。另
一个补帧名额也取序号中点，不一定最接近真实时间中点。新增不均匀切点与不均匀补点用例先
稳定 RED；修复必须按等距时间目标选择最近候选，并继续固定首尾。

修复后不均匀切点从错误的 `(0,300,600,900,6000,14999)` 改为覆盖整段素材的
`(0,3000,6000,9000,12000,14999)`；不均匀补点只剩一个名额时，选择最接近时间中点的
`31663 ms`，不再错误选择序号中点 `39663 ms`。专项增加到 `39 passed`，Ruff 与严格 mypy
通过。捕获力抽查临时把“按时间戳计算目标距离”破坏为“按候选序号计算”，两项时间选择断言
立即失败为 `2 failed, 13 passed`；还原后 `39 passed`，工作树未保留变异。

修复提交 `2b614a0` 的 Codex Review 又找到一项 P1：逐个时间目标贪心取最近候选仍会挤占
后续目标。有效切点 `(0,232,5721,8892,11752,14845,14999)` 原本会错误选择
`(0,5721,8892,11752,14845,14999)`，丢掉早期切点并在尾部留下仅相隔 154 ms 的两帧。
新增该序列先稳定 RED；修复改为在固定首尾的前提下，以动态规划对全部中间时间目标和单调
候选做全局匹配，最小化总时间偏差，得到
`(0,232,5721,8892,11752,14999)`。T1～T4 专项现为 `40 passed`，Ruff 与严格 mypy
通过。捕获力抽查把动态规划的最小前缀代价故意改为最大，既有均匀选择用例立即失败为
`2 failed, 14 passed`；还原后选择文件 `16 passed`，工作树未保留变异。修复等待逐提交
Codex Review。

修复提交 `a1e3928` 的 Codex Review 穷举 `33748` 组小规模输入，确认全局匹配结果均达到
最小总时间偏差；同时找到一项 P2：最多 58 行完整 Python 父指针列表会随切点数线性放大，
10 万候选实测令进程峰值内存升至约 200 MB。新增 `tracemalloc` 门禁先用 2.5 万候选稳定
RED，原实现仅采样阶段峰值约 `35.5 MB`；修复将代价表改为有符号 64 位紧凑数组、父指针改为
有符号 32 位紧凑数组。日常门禁用 1 万候选限制采样阶段峰值小于 `5 MiB`，GREEN 实测通过，
T1～T4 专项现为 `41 passed`，Ruff 与严格 mypy 通过。捕获力抽查把紧凑父指针临时退回
Python 列表，门禁测得约 `13.3 MB` 并立即失败；还原后通过，工作树未保留变异。修复等待
逐提交 Codex Review。

修复提交 `9b49906` 已推送。最终逐任务 Codex Review 复跑 T1～T4 专项 `41 passed`、
Ruff 与严格 mypy，并以 `120310` 组输入逐项比较紧凑数组实现和原 Python 对象实现的选择
结果；同时核对 4 小时时长与 12 位时间戳上界，结论为行为等价、无可达溢出且未发现需要
修复的问题。T4 至此完成。

## T5 开工前【先定】

1. 最终入口接收调用方已经建立、仍由调用方持有路径的单次任务输出目录；返回值只给
   `filename/timestamp_ms/is_scene_cut/byte_size`，不回传源路径、输出目录绝对路径或 JPEG
   bytes。这样 LE-13 以后可用自己已知的目录拼受控文件名，日志和业务结果仍不会泄露路径。
2. 候选仍按时间戳升序，最终文件固定命名为 `frame-000001.jpg` 起的六位连续序号。入口不
   覆盖同名文件；目录不是普通目录、目录身份在写入期间变化、独占创建/完整写入/文件
   `fsync` 失败或同名冲突，一律返回 `WORKSPACE_UNUSABLE`。
3. 文件写入沿用本仓库 `LocalArtifactStore` 已证明的边界形状：受控名字、独占创建、
   `0600`、循环写完整 payload、`fsync` 后核对普通文件与大小。任何失败只回滚本次调用
   已创建的受控文件，绝不删除调用前已经存在的文件。
4. 768px 变换在实际抽帧的同一次随包 ffmpeg 命令内完成，使用 `scale` 保持原画幅；横图、
   竖图最长边都不超过 768，小图不放大，不裁切、不加边。场景帧和时间补帧使用同一变换，
   且都显式输出全范围 `yuvj420p` JPEG。
5. 坏素材由真实随包 ffmpeg 非零退出返回 `UNDECODABLE`；最终输出目录不可用返回
   `WORKSPACE_UNUSABLE`。T1～T4 已锁住超时、输出上限、工具失效和源文件变化，本阶段不把
   同一故障重新映射成新的理由。
6. `ffmpeg-toolchain.v1.json` 的 `select/scdet/xfade` 能力声明按交接明确归 LE-10 T5
   一次性收口；LE-08 不提前拆改该全局契约。

## T5 RED 目标

1. 新公开入口必须返回只含四个受控字段的不可变产物元数据，并把真实候选写成
   `frame-000001.jpg` 起的稳定连续文件；
2. 真实随包 ffmpeg 分别处理横向大图、纵向大图和小图视频，断言 JPEG 文件存在、帧数
   正确、画幅保持，最长边分别收敛到 768 或保持原小图尺寸；
3. 使用时长故意不落在整秒或 100 ms 栅格上的真实硬切素材，断言最终元数据时间顺序、
   场景切点标志与文件数量一致；
4. 真实坏素材返回 `UNDECODABLE`，不创建最终文件；
5. 输出目录不是可写普通目录时返回 `WORKSPACE_UNUSABLE`；中途写失败时仅清理本次已经创建
   的受控文件，预先存在的陌生文件保持不变。

首次只运行 T5 文件应在收集期因生产模块尚无最终产物类型和公开入口失败；随后才写生产实现。

## T5 GREEN、失败矩阵与捕获力

首次运行新文件在收集期按预期因缺少 `AdaptiveFrameArtifact` 失败。生产入口随后接入既有
候选编排，把同一条 768px `scale` filter 同时放进场景扫描和补抽命令；横图、竖图分别真实
得到 `768×432` 与 `432×768`，`320×180` 小图保持原尺寸。两段时长不落整秒或 100 ms
栅格的真实硬切素材得到 `0/1233 ms` 两个切点，最终稳定命名为
`frame-000001.jpg/frame-000002.jpg`。

落盘入口先验证输出目录为普通目录，再按 `LocalArtifactStore` 的已验证形状逐帧独占创建、
循环写完整 payload、设为 `0600`、`fsync` 并核对文件类型/链接数/大小。返回的不可变元数据
只有 `filename/timestamp_ms/is_scene_cut/byte_size`。真实坏素材返回 `UNDECODABLE` 且
不产最终文件；非目录工作区、已有稳定文件名和第二帧写入时注入的磁盘失败均返回
`WORKSPACE_UNUSABLE`。中途失败会删除第一帧，但保留预先存在的陌生文件与同名文件。

T1～T5 全模块专项 `49 passed`；五个相关 Python 文件 Ruff format/check 与严格 mypy
通过。捕获力抽查临时把最长边从 768 改成 769，横图实际变成 `769×433`、竖图变成
`433×769`，尺寸矩阵稳定得到 `2 failed, 1 passed`（小图不放大所以继续通过）；还原后
全模块 `49 passed`。工作树未保留变异，等待本任务提交后的 Codex Review。

## T5 首轮 Codex Review 修复

提交 `e0347b5` 已推送。Codex Review 复跑全模块 `49 passed`，同时用最小探针找到两个 P2：

1. 第一帧写完后输出目录若被换位，下一轮身份检查虽会返回 `WORKSPACE_UNUSABLE`，旧实现却
   因原路径已不是旧 inode 而跳过回滚，被移走的原目录残留第一帧；
2. 文件完成写入、`fsync` 和核验后，若 `os.close()` 仍报告错误，异常发生在外层登记文件名
   之前，入口虽返回 `WORKSPACE_UNUSABLE`，刚创建的帧仍留在目录里。

两项均先通过最终公开入口补 RED，稳定得到 `2 failed` 且看到精确残留。修复后整个输出事务
从候选提取前就持有原目录：POSIX 用 `O_DIRECTORY|O_NOFOLLOW` 打开目录并把 `dir_fd` 用于
独占创建、无跟随核验、目录 `fsync` 和回滚，因此路径换位后仍能删除旧目录里的本次文件；
Windows 用不共享删除权限、拒绝 reparse point 的目录 HANDLE 固定事务目录，并从 HANDLE
解析当前路径。文件名在独占创建成功的瞬间加入回滚清单，故晚到的关闭错误也能清理。

修复后新增两项 `2 passed`，T5 文件 `10 passed`，T1～T5 全模块 `51 passed`；五个相关
Python 文件 Ruff format/check、默认严格 mypy 与生产模块 `mypy --platform win32` 均通过。
等待修复提交的逐提交 Codex Review。
