# T65 启动时清理过期视频工作区

> 状态：✅ 真实文件系统保留策略测试通过；🔍 尚无正式 App 启动清理的用户路径验收
>
> 日期：2026-07-26
>
> 原提交：`f8cc1c14247e94de86202d8a02b74e2d4ad16eba`
>
> 证据补记：第三批事后补录，未与原代码同提交

## 结论

30 天保留策略原来只有测试调用者，生产初始化从未执行，视频工作区会单调增长。原提交在启动
自愈完成后按当前 UTC 时间执行过期清理，同时保留活跃工作区。

## RED

启动清理断言在实现前失败：

`expired workspace removed during startup`，实际仍得到 `Ok`

这里的 `Ok` 表示过期目录仍能被打开，准确证明启动没有调用保留清理。

## GREEN

- `startup_removes_expired_retained_workspaces_without_manual_cleanup`
  构造过期与未过期的真实目录并重新初始化；
- 过期保留工作区被删除，活跃与未到期工作区保留；
- 原有 `retention_cleanup_preserves_active_jobs_and_initialization_recovers_partial_imports`
  继续覆盖手工清理和半成品恢复；
- 第二批最终 Rust 门禁记录为 420 项通过、12 项忽略。

## 失败矩阵

| 场景 | 结果 |
| --- | --- |
| 已终态且超过 30 天 | 启动时删除 |
| 活跃任务 | 不删除 |
| 未到保留期限 | 不删除 |
| 清理与 import 半成品同时存在 | 先完成安全自愈，再按策略清理 |
| 文件时间异常或路径被替换 | 沿既有严格元数据/路径门禁处理 |
| 删除失败 | 初始化响亮失败，不报告已清理 |

## 真实边界

证据使用真实磁盘目录和时间戳，但不是从正式 Tauri App UI 启动。因此保留策略调用关系和磁盘
结果已证实，演示机/客户机首次启动时的可见体验未单独验收。

## 清理

临时工作区由测试夹具回收，没有删除用户 `.local` 或 AppData。

## 文档与遗留

本任务与 T61/T66b 共用 `video_job_workspace` 启动路径，但分别保留独立 RED 和失败矩阵。
