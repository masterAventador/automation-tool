# C11：删除过时演示前清单并核清文档替代关系

- 日期：2026-07-27
- 状态：✅ 文档清理完成
- 范围：仅 `docs/`；未修改产品代码、测试、配置或 `docs/demo-sprint-roadmap.md`
- TDD：不适用。本任务只删除过时文档、修正文档引用与陈述，没有生产代码变更；没有伪造 RED

## 1. 删除对象与正式替代

删除对象是 `docs/development/DEMO-preflight-checklist.md`。删除前已确认：

- 文件标题为「客户演示前一天检查清单」；
- 第 20 行仍声称安全注销是「已知缺陷」并要求演示脚本绕开；
- 功能链路仍是空占位；
- 正式清单 `docs/demo-preflight-checklist.md` 已明确声明自己是替代文件，并记录旧过程稿应尽快删除；
- T70 与 T103 的历史证据都明确指出两份清单并存会让演示者随机拿到错误结论。

正式替代只有 `docs/demo-preflight-checklist.md`。删除后，该文件开头改为明确的唯一正式清单声明，不再继续发布一个指向已删除过程稿的操作性引用。

## 2. 删除前引用核查与清理

删除前使用文件名、标题及关键旧说法做全仓搜索。引用按用途分成三类：

| 类型 | 删除前命中 | 处理 |
| --- | --- | --- |
| 正式操作文档 | 正式清单自身两处引用旧稿；`docs/demo-runbook.md` 仍把 T50 写成「仍未修」 | 移除旧稿依赖；把 T50 改为「代码与契约已修、待真实账号复验」，复验前仍不演示 |
| 当前人工待办 | T48 与 T31 的 Gatekeeper 首次打开待办指向旧稿 §4.3 | 改指 `docs/demo-preflight-checklist.md` B3 |
| 历史证据 | T36、T70、T103 与本批交接单记载旧稿当时存在、未被触碰或建议删除 | T36 的非必要路径引用改为「当时尚存、后来由 C11 删除的过程稿」；T70、T103 和交接单保留为历史审计证据，不把历史记录改写成当时已经删除 |

`docs/demo-sprint-roadmap.md` 的 T50 行已经准确写明原缺陷、修复、自动化证据和真实账号复验缺口；本任务按硬约束未修改该文件。

## 3. 额外过时文档审计

只读扫描了 `docs/` 中包含「取代、替代、作废、过时、obsolete、deprecated、superseded、不再维护、请删除」等标记的 Markdown，并复核文件开头的状态声明。

| 候选 | 结论 | 处理 |
| --- | --- | --- |
| `docs/dt-ai-helper-competitive-analysis.md` | 只有第 10 节部分旧项目建议被三份当前基线取代；竞品事实、证据等级和风险结论仍有效，而且该文件是 `CLAUDE.md` 强制必读基线 | 保留 |
| `docs/adr/0001-embedded-chromium-runtime.md` 与产品规划中的历史 ADR-P002 | ADR-0001 是当前正式决策；产品规划保留被替代决策是决策历史，不是两份互相竞争的操作文档 | 保留 |
| `docs/development/B5-02.md` | 文件明确是已被 EB-10 替代的历史任务证据；路线图要求历史任务和证据不得删除 | 保留 |
| `docs/development/T46-upstream-branding.md` | 只有文件内一条旧结论标为「已过时，本条作废」，整份任务证据并未被宣告废弃 | 保留 |
| `docs/development/PLAN-*`、`FIX-*` | 多数已有后续实现记录，但没有统一声明整份文档作废；其中多份仍被后续证据文件引用 | 不做推断性删除；若要归档，应另立有引用清单和替代关系的任务 |

本轮没有发现第二份同时满足「整份文档明确过时、替代文件明确、引用已核清」的删除候选，因此没有扩大删除范围。

## 4. 验证

执行：

```text
test ! -e docs/development/DEMO-preflight-checklist.md
rg -n 'docs/development/DEMO-preflight-checklist\.md|DEMO-preflight-checklist\.md|客户演示前一天检查清单|安全注销.*(仍未修|已知缺陷)|5 次复现 4 次|演示脚本必须绕开' docs
rg -n 'docs/demo-preflight-checklist\.md|demo-preflight-checklist\.md' docs
git diff --check
```

结果：

- 旧文件不存在；
- 正式清单与 runbook 不再依赖旧稿，也不再把 T50 描述为仍未修；
- T31/T48 的人工待办都指向正式清单 B3；
- 旧路径剩余命中只属于本批交接单、T70/T103 历史证据和本文件的删除审计记录；
- `git diff --check` 通过。

## 5. 边界与风险

- T50 的代码与契约修复不能替代真实抖音账号复验；因此只纠正「仍未修」的假话，没有把安全注销直接加入演示动作。
- 早期过程稿关于「领导的 M4 Air / macOS 26 / 24G、演示者不懂技术」的交付形态没有第二份来源。正式清单继续把它列为待项目负责人确认，不能因删除过程稿而把未确认信息变成事实。
- 历史任务证据中的过时陈述按当时语境保留；当前操作必须以正式清单和 runbook 的最新段落为准。
