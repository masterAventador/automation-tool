# T50 修复抖音安全注销投影过早超时

> 状态：✅ 预算缺陷和契约门禁已修；🔍 修改后尚无真实抖音账号注销最终状态验收记录
>
> 日期：2026-07-26
>
> 原提交：`6197eba47fc200a1da37377be1d9e541488b2807`
>
> 证据补记：第三批事后补录，未与原代码同提交

## 结论

外层安全注销预算为 60 秒，内层权威状态轮询却约 5 秒就放弃，导致注销已经成功而页面仍报
失败。原提交把内层权威投影预算对齐到完整外层预算，没有改变注销控制流或放宽终态要求。

## RED

Node 契约在修复前实际失败：

`the authoritative projection must receive the full outer command budget`

该断言直接针对预算错配，不是依赖缺失造成的假红。

## GREEN

- `frontend/tests/platform-session-logout-acceptance.test.mjs` 锁定内外层预算一致；
- B5-13/B5-14 已有安全注销链路仍是产品真实入口的验收载体；
- 第二批前端回归记录为 523 项通过、1 项预期失败，契约 241/241，typecheck/lint 通过。

上述 GREEN 证明代码和契约收口；它没有证明修改后在真实抖音账号上再次完成注销。

## 失败矩阵

| 场景 | 结果 |
| --- | --- |
| 注销实际完成但权威态传播超过 5 秒 | 继续在完整预算内轮询，不提前报失败 |
| 60 秒内仍无终态 | 保持超时失败，不伪造成功 |
| gateway 调用拒绝 | 显示读取/操作失败，不投影为已注销 |
| Executor 或 Control Plane 不可用 | 既有安全注销错误路径保持不变 |
| 平台网页注销与服务端权威态不一致 | 必须以权威态收敛；未做真实账号复验 |

## 真实边界

第二批没有留下修改后真实抖音测试账号“点击注销→平台会话清除→权威态 missing→页面成功”的
完整记录。因此本文件把真实平台链路标为待验收，不能仅凭 Node 契约宣称演示现场问题已经实测
消失。

## 清理

本提交只改预算与测试，没有启动浏览器、读取 Cookie 或清理真实 Profile。

## 文档与遗留

- 原复现是 5 次中 4 次误报；
- T69 后续增加固定、脱敏的 Control Plane/Sidecar 事件日志，能提升同类故障可诊断性；
- 补验收需使用受控账号，并核对浏览器 Profile、服务端权威态及页面终态。
