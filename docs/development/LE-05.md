# LE-05 数据库迁移与仓储

用户可操作：否
证据类型：分层实现

## 日期

2026-07-29

## 提交列表

按计划，LE-05 分 T1～T5 五个提交，每个 task 一提交。提交号在 T5 收口时统一回填
（一个提交装不下自己的 hash）。本文件随每个 task 追加，不等到最后补写。

**实际是 13 个提交，不是 5 个**：每个 task 的质量审查与复审各自成一提交，T1 走了四轮、
T2 三轮、T3 两轮、T4 三轮。计划那句写的是 task 数，不是提交数，T5 就地写清以免后面
有人按「五个提交」去找。

- T1 `editing_projects` 表 + 迁移 0036 + 仓储：`3abc3c7`；
- T1 修复轮（1 Critical + 4 Important + 5 条 spec 意见）：`2998c6e`；
- T1 复审轮（MRO 四处更正、失败词汇结构守卫、列宽三份副本补钉）：`56f4d7b`；
- T1 收口轮（五处文字：遗留第 3 条收窄、门禁数字回填、两处 docstring 更正、
  词汇守卫加类型过滤）：`84a3523`；
- T2 `materials` 表 + 迁移 0037 + 仓储 + AST 边界测试：`c3e5a7a`；
- T2 修复轮（质量审查 2 Important：描述保护的读后写竞态、AST 别名构造；
  spec 补记 4 条）：`52ab6a3`；
- T2 收口轮（复审 3 Important：「同事务」论证被真实 PG 推翻、AST 三种一行绕过、
  LE-13 约束搬进异常 docstring；2 条 Nit）：`0a5e65f`；
- T3 `timelines` 表 + 迁移 0038 + 仓储：`58d9bbc`（**原记 `58bd8dc`，该对象不存在——
  两位十六进制转置，T5 逐条 `git log -1 <hash>` 核对时 `fatal: unknown revision` 报出来的，
  已改正**。这也是「提交号必须逐条解析一次」的理由：转置后的 hash 看上去完全正常）；
- T3 修复轮（质量审查 5 Important：五条「拒绝理由不对」各自补上归属）：`5c2e5b2`；
- T4 `editing_jobs` 表 + 迁移 0039 + 仓储 + 三条不变式：`e7d9199`；
- T4 修复轮（1 Critical：双谓词换成真 CAS；1 Important：部分索引那句错话；
  spec 2 条；阈下 2 条）：`7c089a8`；
- T4 确认复审轮（2 Important：CAS 丢了时钟倒退守卫、三处过期陈述；
  顺手补克隆侧 sha 比对）：`9688a93`。T4 至此收口；
- T5 收口（final gate 整目录连跑、台账判 `🔍 待验收`、过强文字清扫五处、
  本收口节）：`62a3882`，由本行所在的回填提交补上（照 LE-09 `afb1e66` 的先例——
  一个提交装不下自己的 hash，所以最后一个 task 需要一次纯回填提交）。

上面 13 条**已全部逐条 `git log -1 <hash>` 解析过**，命中且 subject 与描述相符
（T5 实测；T3 那条是这么查出来的）。

> **顺带记一条方法论。** 回填前这里写的是「要确切 hash 的人取
> `git log --grep="T5 收口" --format=%h`」，**而那条命令交回两个 hash**——本线的
> `62a3882` 和 LE-03 的 `ba923fc`（`docs(le-03): T5 收口清单补入……`）。每条线都有 T5，
> 按「T5 收口」grep 必然撞车。写完命令要真跑一次看它交回什么，这是 CLAUDE.md §8.3
> 那条规则在文档上的同一个形状：**拿不准就先弄坏一次看它红不红**。

## T1 `editing_projects` 表 + 迁移 0036 + 仓储

### 交付物

| 文件 | 内容 |
| --- | --- |
| `backend/src/.../infrastructure/database/schema.py` | `editing_projects` 表，两个值对象拍平成列 |
| `backend/migrations/versions/20260729_0036_editing_projects.py` | 建表 + 可真跑的 `downgrade()` |
| `backend/src/.../application/editing_projects.py` | 四个失败类 + 私有基类 |
| `backend/src/.../infrastructure/database/editing_project_repository.py` | `SqlAlchemyEditingProjectRepository` + `_hydrate` + `_timestamp` |
| `backend/tests/integration/test_editing_project_repository.py` | 真实 PostgreSQL 的 8 条用例 |
| `backend/tests/unit/control_plane/test_editing_project_repository_validation.py` | 失败关闭分支的 13 条用例 |
| `backend/tests/unit/control_plane/test_application_failure_vocabulary.py` | application 层失败词汇的结构守卫，动态发现，T2～T4 自动纳入 |

`alembic_head.py` **不需要改**：它不钉版本号，而是用 `ScriptDirectory.get_heads()`
从已提交的迁移脚本里解析，并在头数不等于 1 时报错。派工单第 5 条要求「对齐
`20260729_0036`」，与文件现状不符；实测 `resolve_head_revision()` 加完迁移后直接返回
`20260729_0036`，无需改动。计划 T5 里的同一句已就地删除。

### RED

首轮：两个测试文件先写、先跑，都在收集阶段报
`ModuleNotFoundError: No module named 'automation_tool.control_plane.application.editing_projects'`
（`1 error in 0.48s`，两次）。

修复轮：先把六条新用例写完再跑，收集阶段报
`ImportError: cannot import name 'EditingProjectDataRejected' / 'EditingProjectAlreadyRegistered'`
（两个文件各一次）。实现后当轮为集成 7 passed、单元 13 passed。

复审轮与收口轮各自的红：基类「不收参数」的守卫是先造变异体验红的（把基类改成收
`*detail` 透传，新守卫 5 failed 而原单元文件 13 全绿）；列宽三处副本同理，先实测
`schema.py` 改 32 时**整套仍全绿**，再补钉子让它红。**T1 收口后的最终数是集成 8、
单元 13 + 51**，与上面交付物表一致；本节保留的是各轮当时的数，不是最终态。

| RED 项 | 落点 |
| --- | --- |
| 逐列 `SELECT` 断言（含列类型与 tzinfo） | 集成，整行 dict 相等 |
| `get` 水合出的对象与存入的域相等 | 集成 |
| 缺行 → `EditingProjectNotFound` | 集成 + 单元（stub session 返回空） |
| 重复 `save` → `EditingProjectAlreadyRegistered`，且原行未被覆写 | 集成 |
| 由 PK 冲突触发（违例注入，裸 INSERT） | 集成，断言 SQLSTATE `23505` |
| 四种「表接受、域拒绝」的行 → `InvalidEditingProjectModel` | 集成 |
| 存的是非 v4 UUID → 水合拒绝 | 集成（真行）+ 单元（`_hydrate` 直测） |
| 非 `EditingProjectId` 入参（裸 UUID、`TaskId`） → `EditingProjectDataRejected` | 单元 |
| 库不可达 → `Unavailable`，渲染不含连接串 | 单元 |
| 错密码连接 → `Unavailable`，渲染不含角色名 | 集成（真实 PG）+ 单元（真异常类注入） |
| 水合把带偏移的时间戳归一到 UTC | 单元 |
| 水合拒绝 naive / `None` / 文本 / 数字时间戳 | 单元（参数化 4 例） |
| 迁移建出声明的形状（HEAD + 10 列的类型/可空/长度 + 约束）并可回滚 | 集成 |

### 两个「主动查」点的实测结论

用一次性探针（跑完即删）对真实 PostgreSQL 18.4 + asyncpg 0.31.0 + SQLAlchemy 2.0.51 实测：

1. **唯一冲突抛什么**：`sqlalchemy.exc.IntegrityError`（`SQLAlchemyError` 的子类），
   `.orig` 是 asyncpg 适配后的 `IntegrityError`，底层 `asyncpg.exceptions.UniqueViolationError`，
   `orig.sqlstate == "23505"`。集成测试里把 23505 钉住了。
2. **TIMESTAMPTZ 读回的 tzinfo**：`datetime.timezone.utc`，且 `tzinfo is UTC` 为真，
   微秒完整保留（`123456` 原样回来）。**但零偏移不等于 UTC**——域层
   `_validate_timestamp` 只检查 `utcoffset() == 0`，任何零偏移时区都能过，所以
   「水合后 tzinfo 是 UTC」不能靠域层保证，由 `_timestamp()` 显式归一并有用例钉住。

### 与计划不符的三处，依据都是实测

**一、决策 8 的 `except SQLAlchemyError` 挡不住「库不可达」。** 连接被拒时冒出来的是
`ConnectionRefusedError`（`OSError`），来自 asyncio 的 connect 调用；asyncpg 方言只包装
asyncpg 自己的异常，所以它原样穿过那个 except，把 host 和 port 带给调用方。
`session.py:53` 早就捕 `(OSError, SQLAlchemyError, TimeoutError)`（第三项冗余，
`TimeoutError` 自 3.10 起是 `OSError` 子类，已实测 MRO）；house 另有五处
`(OSError, SQLAlchemyError)` 先例。

**二、握手之后还有一整类异常在漏——这是首轮漏掉的，由审查实测补上。** 上面查到
`ConnectionRefusedError` 就停了，那只是**握手前**。认证阶段的失败是
`asyncpg.exceptions.InvalidPasswordError`，消息里带角色名与库名。asyncpg 0.31.0 实测
MRO：

```
InvalidPasswordError       → InvalidAuthorizationSpecificationError → PostgresError → PostgresMessage → Exception
InsufficientPrivilegeError → SyntaxOrAccessError                    → PostgresError → …
InvalidCatalogNameError    → PostgresError → …
TooManyConnectionsError    → InsufficientResourcesError             → PostgresError → …
```

四个类里**只有 `InvalidCatalogNameError` 直接继承 `PostgresError`**，其余三个各自隔了
一层中间类——所以按任何单个具名基类去接都会漏掉一部分，共同点只有 `PostgresError`
这条脊和「`OSError`、`SQLAlchemyError` 都不在 MRO 上」。其中
**`TooManyConnectionsError`——连接池打满是生产常态，不是配置错误**。按
`bilibili_publish_repository.py:158-163` 补 catch-all 尾巴。

这段 MRO 首轮在四处文档里写成了「只有 `PostgresError → Exception`」，漏掉中间两层，
由复审实测更正。**教训在这里出现了两次**：第一次是「查过一次第三方异常面」不等于
「查全了」（握手前后是两个阶段）；第二次是记录这条教训的那段文字本身又把第三方异常面
写错了一遍。结论没受影响（catch-all 接得住全部四个），错的只是解释。

**三、计划指定的 house 范例选错了。** `workbench_metrics_repository.py` 是只读投影，
没有重复键路径，一个 `Rejected` 够用；带 `save` 的仓储照抄它，会把「已存在」
「不存在」「库挂了」压成同一个异常。按 `customer_account_repository.py:162-165`
的两分法拆成四类，命名对齐 `customer_accounts.py`。这不是可选的整洁度问题：
T4 的三条不变式全部经 `IntegrityError` 浮出，LE-06 紧接着要分 409/404/503。

以上三处已在计划 `docs/superpowers/plans/2026-07-29-le-05-persistence.md` 决策 8 就地更正。

### 水合的顺序：先验后归一

`.astimezone(UTC)` 放在构造器**之前**是个洗白守卫的写法，首轮就是这么写的：对 naive
时间戳它不报错，而是按宿主机时区重新解释、把瞬间平移一个时区偏移，然后交出一个
完全合法的 aware 对象——域层本来会拒绝 naive，被这一步洗过去了。`None` 与文本更糟，
直接从仓储内部抛裸 `AttributeError`。而 asyncpg 对 TIMESTAMPTZ 本来就还 UTC，
**这句对真实列全是 no-op，唯一净效果就是削弱守卫**。T2 的 `described_at` 可空，
照抄即活路径。现在 `_timestamp()` 只对「已经 aware」的值做归一，其余原样交给构造器
拒绝，四种形状各有一条用例。

### 表为什么没有 CHECK 约束

值域上下界留在 `editing_project.py`，表只承担类型、主键与 NOT NULL。完整理由写在迁移
docstring 里，含**与 0032/0034 相反做法的登记**：那两个迁移给云剪辑表堆满 check，是因为
它们背后没有域对象（行来自供应商 API，约束就是全部校验）；`editing_projects` 反过来，
每一行进出都过 `EditingProject.__post_init__`。

### 列宽的三份副本，以及现在谁在守

`caption_font_key` 的上限**写在三处**（首轮登记成「两处」，漏了一处）：迁移
`varchar(64)`、`schema.py` 的 `String(length=64)`、域侧 `_FONT_KEY_PATTERN` 的 `{0,63}`。
`title` 好一点但也有两处：`schema.py` 引用域常量（真引用，不是副本），迁移硬编码 200
——迁移是冻结历史，不能 import 会变的常量，所以这份重复是结构性的、消不掉的。

首轮以为「迁移 ↔ schema.py 已被新的迁移测试兜住」。**实测是没有**：那个测试读
`information_schema`（迁移建出来的）跟测试自己的常量比，根本没读 `schema.py`；把
`schema.py` 改成 `String(length=32)` 或把 `title` 改成字面量 100，整套仍然全绿。所以
本轮补了两处真的钉子：

1. 迁移测试里加一句，拿 `editing_projects.columns`（即 `schema.py` 的声明）跟同一份
   期望比对——这把 `schema.py` ↔ 迁移这对接上了；
2. 新增 `test_the_longest_values_the_domain_accepts_still_fit_the_columns`：域允许的
   最长值（200 个汉字标题、64 字符 font key）必须真跑一趟往返存得下，**且再多一个字符
   必须被域拒绝**。两条断言合起来说的是「域的上限和列宽是同一个数」，不需要第四份副本。
   标题用汉字，于是 200 字符 = 600 字节，顺带钉住 `varchar` 按字符计而不是按字节。

四个漂移方向现在各有一个变异体被杀（见上表）。这三处字面量仍然是三处，但任何一处单独
改动都会红。

顺带：域上限收窄（比如 `{0,63}` 改成 `{0,31}`）不会被上面这对断言抓住——那是域自己
的边界测试的职责，LE-04 已经覆盖。这里守的是「域比列宽」这个方向，那才是会变成
`StringDataRightTruncation` 的那一侧。

### 变异自证

首轮 6 个、修复轮 8 个（含 1 个必须存活的 canary）、复审轮 5 个、收口轮 1 个，
全部符合预期。表里两个 SURVIVED 是**有意的**：一个是 canary，一个是已登记的遗留缺口。

> **注记（T2 加）：下面第二行「`_CONNECTION_FAILURES` 改成只有 `SQLAlchemyError` → KILLED」
> 只在它跑的那一刻成立，对现在已提交的代码不成立。** 那次实跑在首轮，catch-all 尾巴是
> 修复轮才加的；尾巴加上之后同一个变异语义等价（`OSError` 也是 `Exception`），T2 在
> 同形状的仓储上实测 SURVIVED。留着原行是因为它是当时的真实读数，但**只读本节会得出
> 一个对现有代码错误的结论**，所以在这里指路：完整结论与「谁在守」的归属见下面 T2 节的
> 「变异自证」。

| 变异 | 结果 |
| --- | --- |
| 去掉 `_hydrate` 的 UTC 归一 | KILLED |
| `_CONNECTION_FAILURES` 改成只有 `SQLAlchemyError` | KILLED（**见上方注记：已被 T2 实测推翻**） |
| `get` 的 `raise ... from None` 去掉 `from None` | KILLED |
| `_hydrate` 绕过构造器（`object.__new__` + `__setattr__`） | KILLED |
| 迁移里主键整条删掉 / 放宽成 `(project_id, title)` | KILLED（各一次） |
| **两处 catch-all 尾巴同时停用** | KILLED |
| **归一挪到校验之前** | KILLED |
| **不再翻译 `InvalidResourceId`** | KILLED |
| **`AlreadyRegistered` / `NotFound` 分别塌回 `Unavailable`** | KILLED（各一次） |
| **迁移里 `created_at` 改成 `nullable=True`** | KILLED |
| **只绕过 `EditingProject.__post_init__`（保留两个值对象）** | KILLED |
| **基类改成收 `*detail` 并透传给 `super().__init__`** | KILLED（5 failed） |
| **`schema.py` 的 `varchar(64)` 改 32 / `title` 改成字面量 100** | KILLED（各一次） |
| **域侧 `{0,63}` 放宽成 `{0,127}` / `MAX_PROJECT_TITLE_CHARACTERS` 改 400** | KILLED（各一次） |
| CANARY：`(OSError, SQLAlchemyError)` 调换顺序 | SURVIVED（预期，语义等价） |
| **`schema.py` 里 `created_at` 改成 `nullable=True`** | SURVIVED（21 passed）——已登记为遗留第 3 条，不是漏测而是已知边界 |

canary 那次的输出是 `18 passed`，核对如下：它跑的是单元 + 集成两个文件，而**跑的时刻
在补覆盖率用例之前**——当时单元 11 条、集成 7 条，11 + 7 = 18。之后单元加了 2 条
（`IntegrityError` 翻译、`InvalidResourceId` 翻译），集成加了 1 条（宽度端点），同一条
命令现在是 13 + 8 = 21。数字没错，但它对应的是一个已经不存在的快照，故在此写明。

两个曾经存活、已修掉的：

- **主键换成 `UNIQUE(created_at)` 存活**：两个夹具用同一个 `created_at`，换过去的约束
  照样拒绝重复行，测试分不出是哪条约束在拦。按 Global Constraints 的「跨维夹具必须
  两维有区分度」改夹具（第二个项目 title 与 created_at 都不同）后被杀。
- **只绕过 `EditingProject.__post_init__` 存活**：首轮唯一的非法行用例是
  `caption_font_px = 0`，它在 `CaptionStyle.__post_init__` 就被拒，到不了聚合根——
  「水合过域构造器」钉在了错误的层。补三条只有聚合根能拒的行（150px 字幕配 130px 画面、
  未 trim 的标题、带控制字符的标题）后被杀。这三种形状恰恰就是迁移 docstring 论证
  「SQL 表达不了所以只能靠水合」时点名的形状。

### 门禁

数字取自 T1 收口时的最后一次实跑，不是中间轮次的旧值。

| 命令 | exit |
| --- | --- |
| `pytest tests/integration/test_editing_project_repository.py -q` | 0（8 passed，13.90s） |
| `pytest tests/unit -q` | 0（3156 passed，2 skipped，44.50s） |
| `ruff format --check` / `ruff check`（T1 累计改动的 8 个 Python 文件） | 均 0 |
| `mypy`（全量，house 配置） | 1 —— 17 errors / 8 files，与 LE-04 记录的既有基线逐字一致，T1 改动 0 新增 |
| `scripts/check_local_editing_roadmap_counts.py` | 0 |
| `scripts/check_demo_roadmap_counts.py` | 0（但见下方说明：它守的不是本次改的那份台账） |
| `scripts/check_embedded_browser_video_roadmap.py` / `scripts/test_c10_derived_facts.py` | 均 0（这两个才真的守 `development-roadmap.md`） |
| 迁移相关集成回归 | 0（42 passed）——**信号强度有限，见下一节** |

单元总数 3156 里，比修复轮的 3105 多出的 51 条正是新增的
`test_application_failure_vocabulary.py`（4 个基类 + 21 个子类，每个两条，加 1 条发现性
自检）。T1 累计改动的 8 个 Python 文件：`schema.py`、迁移 0036、
`application/editing_projects.py`、`editing_project_repository.py`、`database/__init__.py`、
两个测试文件、词汇守卫文件。

两个新模块的分支覆盖率：**只跑单元测试**即达 100%（Stmts 66 / Miss 0 / Branch 10 /
BrPart 0）。首轮是 89%，靠并入集成测试才凑到 100%，而 Global Constraints 明写
「集成测试不计覆盖率」——两条分支（`IntegrityError` 翻译、`InvalidResourceId` 翻译）
当时在单元层没有任何证据。现已用 `FailingSessions` 与 stub session 补齐。

### 集成回归的信号强度，以及给 T5 的要求

上表那条集成回归是**挑出来的子集**，所以它证明的东西比看上去少。三件事分开说，
证据等级不同：

1. **我实跑过的**：本轮结束后，含新用例的 12 文件子集 **42 passed / 58.11s**；另一个
   由账号与任务生命周期组成的 5 文件子集 **22 passed / 31.01s**。
2. **审查者报告的**：另一个子集 8 failed，且判定为既有的子集互扰、与本次改动无关。
   **这一条我没能复现**——上面两个子集都是全绿。所以本文件不把它当已证事实转述，
   只如实记录来源与我的复现结果。
3. **结构上确实成立的**（直接读 `tests/integration/conftest.py` 得到，不靠推测）：
   `postgresql_url` 是 `scope="session"` 的单实例，而大量用例在开头 `delete()` 共享表
   （`installations`、`tasks` 等）。同一个库、跨文件互删，子集组合不同则前置数据不同。
   **这足以说明子集全绿不构成回归证据**——不需要先复现出某个具体的 8 failed。

**T5 的 final gate 必须跑整个 `tests/integration` 目录，不许挑子集**（已写进计划 T5）。
这条要求由第 3 点独立支撑，不依赖第 2 点是否成立。

### 失败矩阵（本 task 适用行）

| 场景 | 落点 |
| --- | --- |
| 重复请求 | 重复 `save` → `AlreadyRegistered`，且不是 upsert；裸 INSERT 违例注入证明拦截在数据库 |
| 多实例竞争 | 仓储不做「先查后插」，两个并发调用都由主键拒绝其一 |
| 断连 / 库不可达 | 真实拒绝连接 → `Unavailable`，traceback 不含连接串 |
| 认证失败 / 权限不足 / 连接数上限 | 真实错密码 → `Unavailable`，traceback 不含角色名 |
| 敏感信息泄漏 | 四个异常消息全部固定、构造器不收参数；`from None` 抑制上下文，由 traceback 渲染断言钉住 |
| 非法数据持久化面 | 四种非法行 + 非 v4 UUID 在水合处被拒，不返回对象 |

### 遗留（不在本 task 修，已各自登记）

1. **`(OSError, SQLAlchemyError)` 缺口在 6 个仓储 17 处 try 仍存在。** 已立成
   `docs/development-roadmap.md` 的 `H8-23`，依赖 H8-11。
   **数字是本 task 用 AST 自查的，不是转述**：遍历 `backend/src` 全部 `try`，
   凡捕了 `SQLAlchemyError` 而没捕 `OSError`/`Exception`/裸 except 的算一处，得
   17 处 / 6 文件（`account_device`、`account_session`、`customer_account`、
   `task_discovery`、`task_target_result`、`workbench_metrics`）。审查意见里的
   「11 文件 22 处」与实测不符——按 `grep -c "except SQLAlchemyError:"` 数会得到
   更大的数，因为那样数不掉那些**同一个 try 里已经有 catch-all 尾巴**的（bilibili、
   platform_session_health、task_event_convergence、task_event_stream、
   action_execution_orchestration、action_risk_authorization 六个仓储与 `session.py`
   都已正确收口）。
2. **409/503 的可分辨性**：本轮 I4 已解，四类异常就位，LE-06 直接映射即可。
3. **`schema.py` 与迁移的一致性只补上了一半。** 上面「列宽的三份副本」那节已经把
   **列名与 varchar 宽度**接上了（`declared` 断言比对 `Table.columns`，加宽度端点用例），
   四个漂移方向各有变异体被杀。**但 `schema.py` 的列类型与 nullable 仍然没有门禁**：
   实测把 `schema.py` 里 `created_at` 改成 `nullable=True`，集成 8 + 单元 13 共
   21 passed，一条不红——因为 `declared` 只取了 `column.name` 与 `column.type.length`。
   迁移那一侧的 nullable 是有门禁的（`EXPECTED_COLUMNS` 的 `is_nullable`，变异实测被杀），
   漏的只是 `schema.py` 这一侧。全仓库其他表同此，非本 task 引入；要补的话是把
   `declared` 扩成也比对类型与 nullable，属独立任务。
4. **`session.py:53` 的 `TimeoutError` 冗余**（`OSError` 子类），未改，属 H8-23 范围。

### 给 T2 的交接：哪些形状照抄，哪些别抄，哪一条必须自己重判

T1 的形状会被 T2～T4 照抄，所以这里把「可抄」和「不可抄」分开写。

**照抄这些（它们已经被变异体钉住，抄过去直接生效）**

1. **四类异常 + 私有 `_XxxFailure` 基类**：`message` 写成类属性、`__init__` 不收参数。
   基类的「不收参数」由 `test_application_failure_vocabulary.py` 自动守——它动态发现
   application 包里所有 `_...Failure`，T2 的模块一落地就自动纳入，不需要另写测试。
2. **异常映射四段式，顺序不能换**：`except IntegrityError` → `AlreadyRegistered`；
   `except (OSError, SQLAlchemyError)` → `Unavailable`；`except Exception` 兜底 →
   `Unavailable`；三条都 `from None`。
3. **`try` 只包 DB I/O，`_hydrate` 必须在 `try` 外面调用。** 挪进去的话，catch-all 会把
   域异常一起吞成 `Unavailable`，「库里的行不是信任边界」这条就废了。
4. **水合过域构造器**，域异常向上传播不翻译；`InvalidResourceId` 翻译成本模块的域异常。
5. **`_timestamp()` 先验后归一**：只对已经 aware 的值 `astimezone(UTC)`，其余原样交给
   构造器拒。
6. **迁移测试断言形状而不只是存在性**：HEAD 版本 + 每列的 `(data_type, is_nullable,
   character_maximum_length)` + 约束名 + 拿 `schema.py` 的 `Table.columns` 再比一次。
7. **列宽端点测试**：域允许的最长值往返一次，再多一个字符必须被域拒绝。

**别抄这些（照抄会得到错的东西）**

1. `EXPECTED_COLUMNS` 的内容与 `PREVIOUS_REVISION = "20260728_0035"`——T2 的表和前驱
   都不同（T2 的前驱是 `20260729_0036`）。
2. `forged_identifier()` 的 nil-UUID 手法：它成立的前提是「`uuid` 列接受任意版本而
   `EditingProjectId` 只接受 v4」。T2 的 `content_digest` 是 `CHAR(64)` + UNIQUE，
   越界形状完全不同，要另找「列接受、域拒绝」的值。
3. `row_values()` 的固定夹具值——尤其是所有行共用一个 `created_at`。T1 因此让一个变异体
   活了下来（换成 `UNIQUE(created_at)` 照样拒绝重复行，测试分不出是哪条约束在拦）。
   T2 的表有 UNIQUE(`content_digest`)，夹具**必须在两维以上有区分度**，否则同样的坑。
4. 「四种非法行」那份清单：T1 挑的是能穿过两个值对象、只有聚合根能拒的形状。T2 的
   `Material` 结构不同，要按它自己的层级重挑，别照搬字段名。

**这一条 T2 必须自己重判，逐字照抄会得到方向反了的断言**

`test_hydration_refuses_a_timestamp_it_cannot_trust` 的参数化里有 `None`，期望是
**被拒绝**——因为 `editing_projects.created_at` 是 `NOT NULL`，`None` 只可能来自坏数据。
**T2 的 `described_at` 是可空列**，`None` 是它的合法值，水合后应当得到 `described_at=None`
而不是异常。逐字抄过去，这条用例会要求 T2 拒绝一个完全正常的行；更糟的是它会**通过**
（如果 T2 的实现也照抄了拒绝逻辑），于是两边一起错、测试还是绿的。

T2 要做的是给可空时间戳单独定一套期望：`None` 合法且原样水合，naive / 文本 / 数字仍然
拒绝。`_timestamp()` 本身不用改——它对 `None` 已经是「原样返回、交给构造器判」，
改的是**构造器那侧的期望**和这条参数化用例的方向。

### 清理

一次性探针 `tests/integration/test_zzprobe_le05_facts.py` 用完即删，未进提交。
`managed_test_postgres` 负责本次 compose 实例的起停；收尾 `docker ps` 里没有本次
pid 的 `automation-tool-pytest-<pid>-postgres-test-1`。列表里另有同前缀容器属其他
工作线，本会话开始前就在运行，未动。变异脚本写在会话 scratchpad，不在仓库内。

## T2 `materials` 表 + 迁移 0037 + 仓储 + AST 边界测试

### 交付物

| 文件 | 内容 |
| --- | --- |
| `backend/src/.../infrastructure/database/schema.py` | `materials` 表，16 列，本仓库第一张带 JSONB 的表 |
| `backend/migrations/versions/20260729_0037_materials.py` | 建表 + 可真跑的 `downgrade()` |
| `backend/src/.../application/materials.py` | **五个**失败类 + 私有基类；`MaterialDescriptionProtected` 的 docstring 承载给 LE-13 的两条接口约束 |
| `backend/src/.../infrastructure/database/hydration.py` | **新增**：`normalise_timestamp`，T1 的 `_timestamp` 上移到此，两个仓储共用 |
| `backend/src/.../infrastructure/database/material_repository.py` | `SqlAlchemyMaterialRepository` + `_hydrate` + 两个 JSON 转换器 |
| `backend/tests/integration/test_material_repository.py` | 真实 PostgreSQL 的 38 条用例（首轮 36 + 修复轮 2） |
| `backend/tests/unit/control_plane/test_material_repository_validation.py` | 失败关闭分支的 36 条用例（首轮 34 + 修复轮 2） |
| `backend/tests/unit/control_plane/test_material_construction_boundary.py` | Material 描述保护的 AST 结构守卫，21 条（首轮 11 + 修复轮 7 + 收口轮 3） |

`editing_project_repository.py` 也改了：`_timestamp` 删掉，改从 `hydration.py` 引入。
理由是复用规则——T3 的 `created_at`、T4 的 `created_at`/`updated_at` 还要再用两次，
四份副本里最先出事的一定是那条「先验后归一」的顺序：它看起来像可以简化成
`value.astimezone(UTC)` 的多余判断，而简化掉之后针对另一份副本写的测试全都照样绿。
纯搬移，无行为改动，T1 的 13 条单元 + 8 条集成用例逐条仍绿。

### RED

三个文件先写、先跑，各自的红互不相同：

| 文件 | 首跑输出 |
| --- | --- |
| `test_material_repository_validation.py` | `ModuleNotFoundError: No module named 'automation_tool.control_plane.application.materials'`（收集阶段，`1 error in 0.70s`） |
| `test_material_repository.py`（集成） | 同上（`1 error in 0.60s`） |
| `test_material_construction_boundary.py` | `2 failed, 9 passed in 0.43s` —— `FileNotFoundError: .../material_repository.py`。**另外 9 条当场就绿，而且这正是想要的**：那 9 条里有 5 条是喂合成违例源码、3 条是喂合法源码、1 条是全库扫描，它们在被守护对象还不存在时就已经证明了检查函数会红也会绿 |

第四条红是**变异先行**造出来的，见下面「变异逼出来的两条用例」。
修复轮另有两条红，见「T2 修复轮」：Q1 的红是真实 PG 上复现的竞态链
（先 `ImportError: cannot import name 'MaterialDescriptionProtected'`，单元与集成各一次），
Q2 的红是四种别名构造形态 `4 failed, 12 passed`。

### 三个「主动查」点的实测结论

一次性探针（`tests/integration/test_zzprobe_le05_t2_facts.py`，跑完即删，未进提交）
对真实 PostgreSQL 18.4 + asyncpg 0.31.0 + SQLAlchemy 2.0.51 实测：

1. **asyncpg 对 JSONB 列返回什么类型。** 派工单问的是「str？dict？取决于 codec 注册」。
   实测：**SQLAlchemy 的 asyncpg 方言已经注册了 JSON codec，取回来的是解析好的
   Python 对象，永远不是文本**——JSON 数组是 `list`，JSON 对象是 `dict`。
   具体读数：

   ```
   segments type=<class 'list'> value=[[0, 1000], [2000, 3000]]
     inner type=<class 'list'>
   tags     type=<class 'list'> value=['户外', '露营']
   empty tuple -> []      (<class 'list'>)
   object-not-array -> {'not': 'an array'} (<class 'dict'>)
   ```

   写入方向：Python 的 `tuple` 和嵌套 `tuple` 都能直接序列化成 JSON 数组，
   不需要先转 `list`（仓储仍显式转 `list`，因为那是列里真实存的形状，
   让 `_column_values` 的读者不必先去查编码器接不接受 tuple）。
   **所以水合必须把 `list of list` 转回 `tuple of tuple`**，否则域构造器直接拒
   （`not isinstance(segment, tuple)`）。这也是本表是全库第一张 JSONB 表的直接后果：
   `schema.py` 此前没有任何 JSON 列，没有先例可抄。

2. **`CHAR(64)` 是 `bpchar`，会补空格。** 实测存 `'short'` 取回来是
   `'short' + 59 个空格`（`len=64`），而且 `= 'short'` 仍然匹配到它
   （原生 SQL 与 SQLAlchemy Core 各测一次，都匹配 1 行）——空白填充比较会忽略尾部空格。
   对经仓储写入的摘要两者都不生效（域侧只接受 64 位小写十六进制，没有可填充的余量，
   也不会有尾部空格），但它给了一条免费的「表接受、域拒绝」注入形状：补白后的值不是
   十六进制，水合当场拒绝。已有独立用例钉住这个填充事实，换成 `varchar` 会红。

3. **可空 `TIMESTAMPTZ` 取回来是 `None`，带偏移的时间戳被 asyncpg 归一成 UTC。**
   `'...+08'::timestamptz` 存进去取出来是 `tzinfo=datetime.timezone.utc` 的等值时刻。
   这与 T1 对 `created_at` 的结论一致，区别只在 `None` 这一侧，见下一节。

### `[null]` 方向重判：T1 的交接说的是对的

T1 那条参数化用例里的 `None` 期望「被拒绝」，因为 `editing_projects.created_at`
是 NOT NULL。`materials.described_at` 可空，`None` 是它的合法值。逐字照抄会得到一条
方向反了的断言，而且**照抄实现之后它还会通过**——两边一起错、测试仍绿。

本 task 的做法：

- `normalise_timestamp()` 一行没改。它本来就对「不是 aware datetime 的东西」原样透传，
  对 `None` 就是交给构造器判。改的是构造器那侧的期望；
- 参数化用例拆成两组：`None` 单独一组，**断言水合出 `described_at=None`**，
  并且给了两种都合法的行形状（从没被描述过：`ai_description=None` + 源 `ai`；
  用户自己写的：源 `user` + 有描述无时间）——只写一种的话，「USER 源必须没有
  described_at」这条域规则就没被这条用例碰到；
- naive / 文本 / 数字仍然拒绝，`ids=["naive", "text", "number"]`，`null` 从这一组移出。

集成层也各有一条真行用例（`NULL_DESCRIBED_AT_ROWS`），不是只在单元层用 stub 证。

### UNIQUE 的夹具区分度：两条约束各自被单独钉住

T1 因为所有夹具共用一个 `created_at`，让「主键换成 `UNIQUE(created_at)`」的变异体活了下来。
本表有两条唯一约束（`pk_materials` 与 `uq_materials_content_digest`），所以两个夹具在
**两维上各自独立变化**：`DIGEST_ONE`/`DIGEST_TWO` 无共同前缀，`material_id` 各自新生成。

违例注入用三条裸 INSERT，一条只动一维：

| 注入行 | 只可能被谁拒 |
| --- | --- |
| 同 `material_id`、不同 digest | 只有主键 |
| 不同 `material_id`、同 digest | 只有唯一索引 |
| 两维都不同 | 谁都不该拒——这一条防「约束过宽，靠拒绝一切通过前两条」 |

前两条同时断言 `sqlstate == "23505"` 与**约束名出现在驱动消息里**，因为交换两条约束的
列会让 sqlstate 一模一样。变异实测：删唯一索引 → 3 failed；把 UNIQUE 挪到 `material_id`
→ 3 failed；删主键 → 3 failed。三条各自可分辨。

### AST 边界测试：为什么选显式豁免，而不是把水合搬进 `material.py`

计划决策 9 给了两条路，要求「按侵入性小的定，写明理由」。**选了显式豁免**，
而且豁免的粒度是 `(文件, 函数名)` 而不是整个文件：`material_repository.py` 的
`_hydrate` 里可以构造 `Material`，同一个文件的其他任何地方都不行。

否掉「搬进 `material.py`」的理由是写出来之后才看清的：那样等于在**这条规则要保护的那个
模块里**放一个公开的第二构造器。任何人都能从任何地方调它，直达规则想拦的那组字段组合，
而守卫连可匹配的名字都没有了——豁免一个函数留下的口子，比它替代的那个小。

顺带的正当性：水合确实是唯一「从零件装配」正确的地方——库里的行不是「一个被描述过的
素材正在被重新描述」，而是一个正在被重新构成的素材，而且它必须过构造器，因为行是输入不是真理。

> **以下三条与盲区那段是首轮形状，已被修复轮与收口轮扩过两次，处置见「T2 修复轮 Q2」，
> 2026-07-30 T5 补标。** 判据不是措辞而是这段还描不描述现在的代码，逐项对：
> 第 1 条的「精确匹配 `Material` 这个名字」**不成立**——现在匹配的是「模块实际绑定到该类的
> 全部本地名」，含 `as X`、`Y = X` 的不动点链、`Y: type = X` 与 `Y = module.Material`；
> 「不是子串」与那条防误报用例仍成立。**盲区是三条不是一条**（运行期取名、解包/容器绑定、
> 无导入且变量名无关的 `replace`），且「没有任何静态手段能抓到」只对第一、三条成立——
> 第二条是**可补但不值得**，理由写在代码里。自证语料也不再是那 8 段／9 条，
> 该文件现为 **21 passed**（实测，2026-07-30）。**本段以下保留原文作为过程记录。**

**守卫查三件事**，能力边界写在测试文件的模块 docstring 里，没有含糊过去：

1. 调用 `Material(...)`（`Name` 与 `Attribute` 两种形态）——**精确匹配名字，不是子串**。
   包里真有 `BilibiliPublishMaterial` 和 `OpaqueBearerMaterial`，`endswith` 会把它们
   永远报成违例，专门有一条用例钉住这两个不被误报；
2. 导入了 `Material` 的模块里出现的任何 `replace(...)` 调用；
3. 第一个实参名字里带 `material` 的 `replace(...)`，无论有没有导入——这一条覆盖
   `replace(material, ...)` 这个不需要导入的现实形状。

**明确记录的盲区**：一个模块拿到 `Material`、从不写这个类名、又用别的变量名传给
`replace`，静态检查抓不到。没有任何静态手段能抓到；把盲区写出来是比假装它不存在更诚实的选择。

**自证**：5 段合成违例源码（直接构造、限定名构造、导入后 replace、不导入 replace、
`dataclasses.replace`）各断言检查函数返回非空；3 段合法源码断言返回空。这 9 条在
实现还不存在时就已经跑绿——「只对现状绿」的守卫和「什么都不报」的守卫从外面看一模一样。
另有两条：豁免的目标函数必须真实存在（否则豁免会变成永久免票），
以及**去掉豁免时那个文件必须变成违例**（否则豁免可以被删掉而无人察觉，说明扫描根本没到那个文件）。

扫描范围只有 `backend/src`。测试可以自由构造 `Material`——领域对象本来就是这么测的，
范围放宽会让 LE-02 自己那套用例全部违规。

### 变异逼出来的两条用例

两条都是先造变异体看它活下来、再补断言、再看它死。

**一、`update_description` 的「其余列不动」没有任何证据。** 把 UPDATE 的
`.values(**_description_values(material))` 换成 `**_column_values(material)`
（即整行 16 列全写），原有的
`test_update_description_rewrites_four_columns_and_no_others` **照样绿**——因为它传进去的
对象其余字段本来就和库里那行一致，写全列产出的是同一行。补了
`test_update_description_ignores_the_probing_facts_it_is_handed`：同一个 `material_id`，
但 kind、时长、宽高、digest、语音事实全不同，断言库里那行只有四个描述列变了。
在变异体上实跑 `1 failed, 1 passed`（新用例红、旧用例绿，正是它存在的理由），
还原后 36 passed。

**二、AST 守卫自己的脚手架有个洞。** 把全库扫描那一步改成「遇到被豁免的文件直接
`continue`」（即把「豁免一个函数」偷偷放大成「豁免整个文件」），**11 条全绿**——
包括那条专门检查「豁免是承重的」的用例，因为它直接调检查函数、不走扫描循环。
补了一句 `assert exempt_path in scanned`（扫描过的路径集合里必须真的有它），
再跑同一个变异体：`1 failed`。

### 变异自证

**修复轮把整套重跑了，下面是本轮的读数，不是首轮的。** 41 个变异体、42 次实跑
（Q2b 重跑一次，原因见表下），`unexpected=0`——每一条的结果都与事先写下的预期相符。
KILLED 38、SURVIVED 3（2 个 canary + 1 个已解释）。

批量脚本 `le05_t2_mutations_r2.py`
sha256 `89bb80ac7d5cc3ce57e1636450329d11a840641c6529a354d78a4b58dc9b3793`；
Q2b 重跑脚本 `le05_t2_mutations_r2b.py`
sha256 `5948de377c9fad57ced0bd55f7729a210ca662dc3560be5eedaeb29810e9501e`。
三个脚本都写在会话 scratchpad，不进仓库；记 sha256 是为了让上面这些读数能被追溯到
产生它们的那一版脚本。

**收口轮另有 8 条，跑在克隆树里**，脚本 `le05_t2_mutations_r3.py`
sha256 `f606c9186504a96ef5489a06ed2381063b23d78aff6d47e783288b6d5306fd50`，
见下面「收口轮：AST 新分支的变异，跑在隔离克隆里」。

| 变异 | 结果 |
| --- | --- |
| `_segment_pairs` 从不转换 | KILLED（6 failed） |
| `_segment_pairs` 无条件转换（不先判形状） | KILLED（2 failed） |
| `_sequence` 从不转换 | KILLED（6 failed） |
| `_hydrate` 不再翻译解析失败（`except ValueError` 收窄） | KILLED（6 failed） |
| **`normalise_timestamp` 不再归一（共用模块）** | KILLED（2 failed，材料与项目两个文件各一条） |
| **`normalise_timestamp` 归一挪到校验之前（共用模块）** | KILLED（2 failed，同上） |
| `AlreadyRegistered` 去掉 `from None` | KILLED |
| 三处 catch-all 尾巴同时停用 | KILLED |
| `update_description` 不再检查 `rowcount` | KILLED |
| `get` 把 `NotFound` 塌回 `Unavailable` | KILLED |
| `save` 不再翻译 `IntegrityError` | KILLED |
| **`_hydrate` 挪进 `_row` 的 try 里** | KILLED（24 failed）——交接第 3 条 |
| `find_by_digest` 忽略入参 | KILLED |
| 迁移删掉 digest 唯一约束 / 把 UNIQUE 挪到 `material_id` / 删掉主键 | KILLED（各 3 failed） |
| 迁移把 `described_at` 改 NOT NULL | KILLED（5 failed） |
| 迁移把 `speech_segments_ms` 改可空 | KILLED |
| 迁移把 `content_digest` 改 `varchar(64)` | KILLED（2 failed） |
| 迁移把 `ai_description` 收窄到 200 | KILLED（2 failed） |
| **`schema.py` 把 `described_at` 改 NOT NULL** | KILLED —— T1 遗留第 3 条的那个方向，本表已补 |
| **`schema.py` 把 `content_digest` 改 `String` / `ai_tags` 改 `String`** | KILLED（各一次） |
| **`schema.py` 把 `speech_transcript` 改成字面量 1000** | KILLED |
| 违例注入：某模块用 `replace()` 重建 Material / 直接构造 Material / **用别名构造** | KILLED（各一次） |
| 扫描跳过被豁免的整个文件 | 先 SURVIVED（11 passed），补断言后 KILLED |
| `update_description` 写全部 16 列 | 先 SURVIVED（旧用例），补新用例后 KILLED |
| **Q1a 去掉描述保护谓词** | KILLED |
| **Q1b 谓词对用户写入也生效（过度修复）** | KILLED |
| **Q1c `Protected` 塌回 `NotFound`** | KILLED |
| **Q1d 消歧永远报「行不存在」** | KILLED |
| **Q1e 谓词比错枚举值（`!= 'ai'`）** | KILLED（3 failed） |
| **Q2a 不收集 import 别名** | KILLED（3 failed） |
| **Q2b′ 不跟赋值别名** | KILLED（2 failed，正好是两条赋值别名用例） |
| **Q2c 赋值别名只跟一层** | KILLED |
| **Q2d 构造改回只比字面 `Material`** | KILLED（4 failed） |
| **W1 `MAX_DESCRIPTION_CHARACTERS` 放宽 2000→4000** | KILLED（2 failed） |
| **W2 `MAX_TRANSCRIPT_CHARACTERS` 放宽 100000→200000** | KILLED（2 failed） |
| **W3 `MAX_TAG_CHARACTERS` 放宽 32→64** | **SURVIVED（38 passed）——预期，见下** |
| CANARY-1：`(OSError, SQLAlchemyError)` 调换顺序 | SURVIVED（预期，语义等价） |
| **CANARY-2：`_CONNECTION_FAILURES` 缩成只有 `SQLAlchemyError`** | **SURVIVED（36 passed）——见下** |

**Q2b 为什么重跑。** 第一版把 `while True:` 改成 `while False:`，函数于是从末尾掉出去
返回 `None`，随后 `x in None` 抛 `TypeError`——`14 failed` 是**崩掉**而不是「漏报违例」，
属于因错误理由而 KILLED，不能算数。重跑版改成在收集完 import 别名后直接 `return names`，
是一个语义合法、只是不跟赋值别名的检查器，结果 `2 failed`，红的正好是
`assignment-alias` 与 `chained-assignment-alias` 两条。**这才是这条钉子的证据。**

**W3 存活是预期的，而且它正好说明 W1/W2 在测什么。** `MAX_TAG_CHARACTERS` 背后没有列
——标签存在 JSONB 里，没有宽度可漂移，所以放宽它在集成层没有任何东西该红。
`ai_description` 与 `speech_transcript` 背后各有一个 `varchar`，放宽域常量而迁移不动，
`schema.py` 引用的常量与迁移里写死的数字当场分家，两条都 KILLED。
（域自身的边界收窄方向由 LE-02 的领域用例守，不在本表范围。）

**CANARY-2 这个存活是预期的，而且它顺带纠正了 T1 记录里的一条。** `OSError` 从具名子句里
去掉之后行为完全不变，因为后面那条 catch-all 尾巴照样接得住它（`OSError` 也是
`Exception`），转出来的仍是同一个 `Unavailable`、同样 `from None`。所以这条变异**语义等价**，
是第二个 canary 而不是漏测。

覆盖没有丢：把**两者一起**去掉（具名元组缩成 `(SQLAlchemyError,)` **且**三处尾巴全停用）
实测 `2 failed` —— OSError 这条路是被钉住的，只是钉子不在具名子句上。

**T1 的变异表把同一个变异记成 KILLED**，那在当时是真的：T1 首轮还没有 catch-all 尾巴，
尾巴是修复轮才加的，加完之后这条变异在 T1 的仓储里同样会存活。T1 已经自己记过一次
「数字对应的是一个已经不存在的快照」（canary 那条 18 passed），这是同类的第二处。
两处都不影响结论，错的只是「谁在守」这个归属。

那么具名元组还留着做什么：它是**意图的声明**，写明这两族是已知会来的；尾巴是兜底。
删掉尾巴（KILLED）证明尾巴承重，删掉两者（KILLED）证明这条路承重。

### 门禁

**数字取自修复轮的最后一次实跑**，不是首轮或任何中间轮次的旧值。

| 命令 | exit |
| --- | --- |
| `pytest tests/integration/test_material_repository.py -q` | 0（38 passed，29.18s） |
| `pytest tests/integration/test_material_repository.py tests/integration/test_editing_project_repository.py -q` | 0（46 passed，40.28s）——T1 的 8 条在 `hydration.py` 上移之后逐条仍绿 |
| `pytest tests/unit -q` | 0（3225 passed，2 skipped，44.50s） |
| 单元层单跑三个新模块的分支覆盖 | 100%（Stmts 113 / Miss 0 / Branch 24 / BrPart 0），未借集成 |
| `ruff format --check`（T2 累计改动的 11 个 Python 文件） | 0（11 files already formatted） |
| `ruff check`（同 11 个文件） | 0（All checks passed） |
| `mypy`（全量，house 配置） | 1 —— 17 errors / 8 files，与 T1 记录的既有基线逐字一致，T2 改动 0 新增 |
| `scripts/check_local_editing_roadmap_counts.py` | 0 |
| `scripts/check_demo_roadmap_counts.py` | 0 |
| `scripts/check_embedded_browser_video_roadmap.py` / `scripts/test_c10_derived_facts.py` | 均 0 |
| `scripts/check_third_party_sources.py` | 0 |

**11 个文件**：`schema.py`、迁移 0037、`application/materials.py`、`hydration.py`、
`material_repository.py`、`database/__init__.py`、`editing_project_repository.py`、
三个新测试文件、以及 `test_editing_project_repository_validation.py`
（`_timestamp` 上移后 docstring 里那句指路要跟着改）。首轮门禁表写「10 个」漏了最后这个，
spec 审查补记第 4 条已改正。

单元总数 3225 里，比 T1 的 3156 多出的 69 条 = 材料仓储 36 + AST 守卫 21 +
失败词汇守卫自动纳入的 12。**最后这 12 条是自动来的**：`test_application_failure_vocabulary.py`
动态发现 application 包里的 `_...Failure`，`materials.py` 一落地就被扫进去
（1 个基类 + **5** 个子类 = 6 个类 × 2 条参数化用例），该文件从 51 条变成 **63** 条，
无需改它一个字。修复轮新增的 `MaterialDescriptionProtected` 同样是自动纳入的
——从 61 到 63 那 2 条就是它。T1 交接第 1 条说的自动纳入，两轮各实测一次，都成立。

### 与计划/派工单不同的五处，依据都是实测或既有先例

**一、列类型按 house 风格落成 `varchar`，不是计划字面写的 `TEXT`。** 计划 T2 节的表结构
是速记：`kind TEXT`、`ai_description TEXT`。实现用 `varchar(16)`（枚举列，对齐
`schema.py` 里 `String(length=16)`/`String(length=32)` 的五处先例）、
`varchar(2000)`/`varchar(100000)`（绑定域常量）。T1 有同样的先例——计划写
`title TEXT`，实现是 `varchar(200)`。好处是有宽度可钉：域允许的最长值往返一次、
再多一个字符必须被域拒绝，这对断言说的是「域的上限和列宽是同一个数」。

**二、`_timestamp` 上移到新的 `hydration.py`，动了 T1 的文件。** 理由在「交付物」一节。

**三、`find_by_digest` 只做 `isinstance(str)` 守卫，不校验摘要格式。** 格式是域的事，
而且一个格式正确但没人存过的摘要、和一个格式错误的摘要，正确答案都是 `None`。
类型守卫是必须的：非字符串会被拿去和 `bpchar` 比较，比出什么取决于驱动，
而调用方会把 `None` 读成「可以导入」，于是同一个文件存两遍。

**四、`update_description` 没有 `IntegrityError` 子句，`save` 有。** 那四个描述列上
没有任何 UPDATE 能违反的约束，写一条永远走不到的分支既是死代码也过不了 100% 覆盖。
真要变成可能了，`SQLAlchemyError` 那条照样接得住（`IntegrityError` 是它的子类），
只是转成 `Unavailable`——所以这一条写进了方法的 docstring，不是靠记性。

**五、决策 7 的「抛模块异常（带 digest 不带路径）」被推翻成「两样都不带」。**
这一条是**对已锁死决策的推翻**，所以单列而不是混进上面四条。计划决策 7 原文要求
重复登记时抛的异常带上 digest；实现没带。两条依据：

1. **与决策 8 结构性冲突。** 决策 8 定死四类异常「`message` 写成类属性、`__init__`
   不收参数」，为的是让 `raise X("detail")` 在调用点就是 `TypeError`。要带 digest 就得
   给构造器开口子，那个口子一开，`raise X(row)` 也随之合法——决策 8 想挡的正是这个，
   而且 `test_application_failure_vocabulary.py` 会当场判红；
2. **`content_digest` 是用户本机文件的摘要**，CLAUDE.md §7 要求这类值不进普通日志与
   错误响应。带上它等于让每个 `except` 处的 traceback 都拿到它。

调用方要知道是哪个 digest 冲突，手上本来就有那个值——它是自己刚传进 `save` 的入参。
异常只需要说「重复了」。集成层有一条断言钉住 digest 不出现在渲染出的 traceback 里。

### 失败矩阵（本 task 适用行）

| 场景 | 落点 |
| --- | --- |
| 重复请求 | 重复 `material_id` → `AlreadyRegistered` 且不是 upsert；重复 `content_digest` → 同一异常且消息不含摘要 |
| 多实例竞争 | 仓储不做「先查后插」，两个并发导入同一文件都由唯一索引拒绝其一；裸 INSERT 违例注入证明拦截在数据库，且两条约束各自可分辨 |
| 断连 / 库不可达 | 真实拒绝连接 → `Unavailable`，traceback 不含连接串（四个方法各一条） |
| 认证失败 / 权限不足 / 连接数上限 | 真实错密码 → `Unavailable`，traceback 不含角色名（四个方法各一条） |
| 敏感信息泄漏 | 四个异常消息固定、构造器不收参数；`from None` 由 traceback 渲染断言钉住；`content_digest` 来自用户本机文件，重复导入的拒绝路径专门断言它不出现 |
| 非法数据持久化面 | 参数化的 22 种「表接受、域拒绝」的行在水合处被拒（其中 12 种是 JSONB 畸形形状，另有非法枚举串、大写与非十六进制摘要、跨字段矛盾）；非 v4 UUID 与 `CHAR(64)` 补白后的摘要各另有一条独立用例。一条都不返回对象 |
| 结果不确定 / 静默失败 | `update_description` 命中 0 行 → 后续 SELECT **尽力**消歧成 `NotFound` 或 `MaterialDescriptionProtected`，不静默返回；保护决策由 UPDATE 那一条语句单独做完，这次读**可被并发删除抢跑**，错的只会是标签（原写「同事务内消歧」，把被收口轮推翻的那句话的形状留在了这里，2026-07-30 T5 改准——同事务不让两种答案穷尽，源码 docstring 已写明） |
| 多实例竞争（读后写） | 陈旧快照 + 正规方法能把 AI 描述盖到用户描述上；拦截做成 UPDATE 谓词而不是先读后判，真实 PG 复现链有用例（修复轮 Q1） |

### 清理

一次性探针 `tests/integration/test_zzprobe_le05_t2_facts.py` 用完即删，未进提交。
三个变异脚本（首轮两个、修复轮两个，其中一个是首轮脚本的替代）写在会话 scratchpad，
不在仓库内；修复轮两个脚本的 sha256 记在「变异自证」一节，读数可追溯到具体那一版。
开工前按审查交接检查过工作树：`git status --porcelain --untracked-files=all` 为空，
另清掉了质量审查者临时探针 `test_zzreview_probe.py` 留下的一个 `.pyc`
（源文件已由审查者删除，只剩 `__pycache__` 里的字节码，未跟踪）。`managed_test_postgres` 负责本次 compose
实例的起停；收尾 `docker ps` 里没有本次 pid 的
`automation-tool-pytest-<pid>-postgres-test-1`。列表里另有同前缀容器属其他工作线，
本会话开始前就在运行，未动。

### 遗留（不在本 task 修，已各自登记）

1. **T1 遗留第 3 条（`schema.py` 的类型与 nullable 没有门禁）在 `materials` 上已补，
   其余表仍旧。** 本表的迁移测试把 `Table.columns` 的**类型类名与 nullable** 也比了一遍
   （`EXPECTED_TABLE_TYPES`），三个方向各有变异体被杀。`editing_projects` 与全库其他表
   仍只比列名与宽度——扩这件事属独立任务，本 task 没有顺手改 T1 的断言。
2. **AST 守卫的三个静态盲区**（运行期取名如 `getattr`；解包/容器绑定如
   `Rebuild, = (Material,)`；模块不写 `Material` 这个名字、变量名也不带 material 时的
   `replace`）已写进测试文件的模块 docstring，**并各有一条用例把它钉成事实**——见修复轮 Q2。
   第一、三条无静态手段可闭合；**第二条可补但不值得**（要按位置对齐元组/列表的目标与值，
   那是开始求值这个模块而不是读它，且下一种容器形态立刻把口子开回来），理由写在代码里。

   > **原文写「两个盲区」，与代码不符，2026-07-30 T5 改正。** 收口轮复审又找出三种一行
   > 绕过，其中解包那条移进了盲区清单（修复轮 Q2 的表里就是这么记的），本行漏了跟着改。
   > 现在测试文件的模块 docstring 是三条，`None of the three is worth closing`。
3. **`(OSError, SQLAlchemyError)` 缺口在 6 个仓储 17 处 try 仍存在**，仍是 `H8-23`，
   与 T1 记录一致，本 task 未扩大也未缩小。

## T2 修复轮：质量审查 2 Important + spec 补记 4 条

### Q1 陈旧快照能把 AI 描述盖到用户头上——结构性缺口，真实 PG 已复现

**审查者是对的，首轮那句 docstring 是错的。** 原文写「Whether the rewrite is allowed at
all was decided before this point, by `Material.with_ai_description`」——`with_ai_description`
看的是**调用方手里的快照**，不是库里那一行。完整链路，每一步都走正规方法：

1. 某个 describe 流程 `get` 出素材，此时 `description_source` 是 `AI`；
2. 用户在这期间写了自己的描述，`with_user_description` → `update_description`，库里变 `user`；
3. describe 流程拿着**第 1 步那个快照**调 `with_ai_description`——快照说 AI，
   所以域方法不返回 `self`，老老实实交出一个改好的对象；
4. `update_description` 无条件写入。用户的字没了，`description_source` 翻回 `ai`。

没有人构造 `Material`，没有人调 `replace`，所以 AST 守卫无话可说；域守卫被满足了，
因为它看的那个对象**确实**说自己是 AI。**AST 挡构造、域挡快照，没人挡 update。**

**拦截做成 UPDATE 里的谓词，而不是 Python 里的判断**，理由和 `save` 不做「先查后插」
是同一条：读一行再决定，在低一层有一模一样的缺陷——两个 describe 流程都读到 `ai`，
都往下走。只有数据库一次只看一条语句。

语义定死如下：

| 传入 material 的 source | UPDATE 附加谓词 | 理由 |
| --- | --- | --- |
| `AI` | `description_source != 'user'` | 模型不得盖用户 |
| `USER` | 不附加 | 用户改自己的描述永远允许 |

`rowcount == 0` 于是有两个含义，**用同一个事务里的一次后续 SELECT 消歧**：

- 查不到行 → `MaterialNotFound`；
- 查得到行 → `MaterialDescriptionProtected`（**新异常，进四类词汇体系变五类**）。

**「同一事务所以只有这两种」这句话是错的，已被真实 PG 推翻。** 首轮把它当成修复形状的
中心理由写进了源码注释和用例 docstring——后人最会引用的两个位置。实测：连接跑在
`read committed` 下，**每条语句各取一次新快照**；UPDATE 之后、SELECT 之前另一个连接
DELETE 并提交，同事务的 SELECT 读出 `ROW_VISIBLE=False`。同事务只省了一次连接池取用，
没有让两种情况穷尽；换 session 会误报的场景，同 session 一样误报。

诚实的版本（源码与用例 docstring 两处都已改成这个）：**保护决策由 UPDATE 谓词一条语句
单独做完**，后续 SELECT 只是尽力消歧，可被并发抢跑。那个窗口里两种答案都安全——行真被
删了答 `NotFound` 是实话，行还在且归用户答 `Protected` 是实话，会错的只是「并发删除」
这一种情形的标签，而两个标签对调用方是同一个意思：停。**塌成一个才是不安全的**。

**不能沿用 `NotFound`**：LE-06 要对这两种分别答 409 与 404，LE-13 要区分「素材没了，
别重试」和「有人接管了这个字段，别再写」。用例里专门断言
`not isinstance(captured.value, MaterialNotFound)`——**它挡的是「把 `Protected` 写成
`NotFound` 的子类」这种塌回**（那样写 `pytest.raises(Protected)` 照旧绿，而调用方
`except MaterialNotFound` 会把它一并吞掉）。首轮这里写的理由是「两者同基类所以必要」，
**那条理由不成立**：兄弟类不会让 `raises(Protected)` 失效。复审实测：这个变异只被这条
断言和失败词汇守卫抓到。

**RED**：新增
`test_a_stale_snapshot_cannot_walk_an_ai_description_over_the_users`，真实 PG 上逐步复现
上面四步，断言拿到 `Protected`、用户描述保留、`description_source` 仍是 `user`。
修前它红在「没有 `MaterialDescriptionProtected` 这个名字」（`ImportError`，单元与集成各一次）。

**顺带修掉一条一直在演示该 bug 的旧用例。**
`test_update_description_rewrites_four_columns_and_no_others` 原来的顺序是
「先写用户描述、再写 AI 描述」，加上谓词之后当场变红——**它一直在跑的正是这个缺陷本身**，
而且一直是绿的。改成按真实顺序（先模型、后用户），并在 docstring 里写明反序不是疏忽、
是那条 bug。

**新增反向用例** `test_the_user_may_keep_rewriting_their_own_description`：用户连写两次
必须都成功。这是防「过度修复」——把谓词无条件加到所有 update 上，正是最容易顺手做出来的错。

**给 LE-13 的接口约束不写在这里**，写在 `MaterialDescriptionProtected` 自己的 docstring
里——见下面「约束搬家」。

### Q2 AST 守卫漏别名构造：能补的补掉，补不动的登记

审查者实测七种绕过，四种 MISSED：`as M`、`as _Mat`、`Rebuild = Material`，以及链式
`A = Material; B = A`。**而首轮我自己在 `_imports_protected_class` 的注释里写过
「别名导入是那种用过才暴露的洞」——`replace` 那一侧照着做了，构造这一侧没有。**

修法：构造匹配从「比对字面 `Material`」改成「比对**这个模块实际绑定到该类的所有本地名**」：

- `ImportFrom` 里 `alias.name == "Material"` 的取 `asname or name`；
- 简单赋值 `Y = X` **跟到不动点**而不是一层——停在一层，两行就能绕过，读完规则的人立刻想得到；
- `Attribute` 形态（`material_module.Material(...)`）仍按属性名匹配，因为它拼的就是类名本身。

RED：把四种别名形态加进合成违例语料，跑出 `4 failed, 12 passed`，失败的正好是这四条。

**复审又找出三种一行绕过，我自己实测复现了这三条**（合成语料一次跑出
`3 failed, 18 passed`，红的正好是它们）：

| 形态 | 为什么漏 | 处置 |
| --- | --- | --- |
| `Rebuild: type = Material` | 只跟了 `ast.Assign`，`AnnAssign` 整条没看 | **补掉**（加 `AnnAssign` 分支） |
| `Rebuild = m.Material` 再 `Rebuild(...)` | 只跟 `Name` 值；直接 `m.Material(...)` 反而抓得到 | **补掉**（赋值右侧也认 `Attribute`） |
| `Rebuild, = (Material,)` | 解包不走上面任何一种节点形态 | **登记进盲区**，配用例 |

第二条尤其难看：**先给属性绑个名字反而躲掉，而直接调它会被抓**——正是我自陈要堵的
「改个名字就绕过去」那一类。补完两条后重跑，只剩解包那条红，随即移进盲区清单。

解包不补的理由写在代码里：要补就得把元组/列表的目标与值按位置对起来，那是**开始解释
这个模块而不是阅读它**的第一步，而且下一种容器形态立刻又把口子开回来。

**三条盲区如实登记，各有一条用例把它钉成事实**（首轮只登记了 `replace` 那一侧，
与实际不符；复审后补上解包这条）：

1. **运行期取名**——`getattr(module, "Material")(...)`、查表、工厂返回类。静态无解；
2. **解包/容器绑定**——`Rebuild, = (Material,)`。可补但不值得，见上；
3. **模块不导入类名、变量名也不带 material 时的 `replace`**。静态无解。

三条各有一条 `test_a_name_this_technique_cannot_resolve_is_a_recorded_blind_spot`
参数化用例断言「当前不报」，docstring 写明：**这条用例哪天红了，说明检查变强了，
去把模块 docstring 的盲区清单同步删掉那一条**。断言一个洞存在听着奇怪，但它把边界变成
可核对的事实，而不是没人验证过的假设——「限制没写下来的守卫会被当成密不透风」。

**「构造这一侧可以静态闭合」这个说法已经改掉**（首轮 Q2 小标题与模块 docstring 都这么写）。
三轮下来的实际形状是：每补一个分支，下一种绕法都只差一行；`Material` 的
`getattr`、解包、以及 `replace` 那侧的盲区各自都还开着。所以模块 docstring 现在写的是
**两条路都不是精确判定**，并加了一段说明这个守卫到底是干什么的——它让正规路径成为省事的
那条、把无意的绕过变成一条红测试；**它不是拦住存心绕过的人的屏障**，那三条各只要一行。
它守的是一条设计规则，而设计规则靠知道它为什么存在的人来遵守；这个文件的职责是让人知道。

复审已确认 `backend/src` 现在没有这三种形态中的任何一种，所以这轮补的是 future-proofing，
不是在修一个正在漏的洞。

### spec 审查的四条补记

1. **T1 变异表已就地加注记**（见上方 T1「变异自证」小节的引用块），指向 T2 节。
   同一文件相邻两张表对同一变异给出相反结论，单读 T1 节会得出对已提交代码不成立的结论；
   §2.1 要求当场改正而不是推迟。
2. **决策 7 的推翻已登记**，「与计划不同」从四处改成五处，见上一节第五条。
3. **域常量「放宽」方向的变异已补跑**，见下面变异表的 W1～W3。首轮只跑了列收窄侧，
   放宽侧只有机制推理，不符合 Global Constraints 的「两个方向都要实跑」。
4. **门禁表的文件数已改对**：10 → 11（首轮漏数了 `test_editing_project_repository_validation.py`，
   它因为 `_timestamp` 上移改了 docstring）。修复轮又新增改动，最终数见门禁表。

### 收口轮：AST 新分支的变异，跑在隔离克隆里

新加的两个绑定分支要有证据，而变异 AST 守卫意味着**改的就是那个负责扫描 `backend/src`
的测试文件**——跑挂了就可能把工作树留在半变异状态。所以这一轮换了做法：

1. `cp -c -p -R backend <scratch>/mutclone/backend` —— APFS clonefile，**356 MB 用 1.5 秒**
   （同 CLAUDE.md §8.1 给 vendor 用的那招），产出真实目录、独立 inode；
2. 把克隆里 `site-packages/automation_tool.pth` 的内容改写成克隆自己的 `src` 路径
   （原文是工作树的绝对路径，不改的话克隆里的测试会去 import 工作树的代码）；
3. **动手前先验证隔离**：脚本第一步跑
   `python -c "import automation_tool; print(automation_tool.__file__)"`，
   路径不在克隆里就直接拒绝执行。实测输出确实是克隆里的 `__init__.py`；
4. 再验证克隆在变异前是绿的（`21 passed`），否则也拒绝——不然「杀死」可能只是克隆本来就坏。

结果 8 条全部符合预期（`unexpected=0`），事后 `git status` 只有本轮真正编辑的 5 个文件，
克隆里那份测试文件与工作树逐字节相同。

| 变异 | 结果 |
| --- | --- |
| N1 删掉 `AnnAssign` 分支 | KILLED（正好红 `annotated-assignment-alias`） |
| N2 赋值右侧不再认 `Attribute` | KILLED（正好红 `attribute-bound-to-a-name`） |
| N3 赋值右侧永远不绑定 | KILLED（4 条别名用例同时红） |
| **N4 反过来让它也跟解包** | **KILLED——红的是盲区用例 `alias-bound-by-unpacking`** |
| N5 不收集 import 别名（重构后重跑） | KILLED（3 failed） |
| N6 绑定只跟一层（重构后重跑） | KILLED（正好红链式那条） |
| N7 构造改回只比字面 `Material` | KILLED（6 failed） |
| CANARY-3 `while True` 写成 `while 1` | SURVIVED（预期，语义等价） |

**N4 是这一轮最该留下的一条。** 盲区用例断言的是「当前抓不到」，这种断言天然有个问题
——它对一个**什么都不做**的检查函数也成立。N4 把解包支持真的加进去，那条盲区用例当场
变红，说明它确实盯着「解包到底有没有被跟」这件事，而不是白写一条永远绿的断言。
盲区清单要能当事实用，就得有这条证据。

### 给 T3 的交接

**照抄**：失败类 + 私有基类（词汇守卫会自动纳入，不用另写测试）；异常映射四段式；
`_hydrate` 必须在 try 外；水合过域构造器、`InvalidResourceId` 折进域异常；
`normalise_timestamp` 从 `hydration.py` 引入，**不要再抄一份**；迁移测试比形状而不是存在性，
并且**把类型与 nullable 也比上**（本 task 新加的那一半）。

**别抄**：`EXPECTED_COLUMNS`/`EXPECTED_TABLE_TYPES` 的内容与 `PREVIOUS_REVISION`
（T3 的前驱是 `20260729_0037`）；`REFUSED_ROWS` 那份清单是按 `Material` 的字段挑的。

**T3 要自己重判的两条**：

1. **`tracks` 的 JSONB 不是同构标量序列，是嵌套 dict 树。** 本 task 的 `_segment_pairs`
   和 `_sequence` 只处理「数组的数组」和「扁平数组」，`tracks` 里每个 clip 是一个对象，
   asyncpg 交回来是 `dict`（实测过：JSON 对象 → `dict`）。转换器要重写，但**那条
   「只转已经对形状的东西、其余原样交给构造器」的规矩必须照抄**——它是本 task
   12 条 JSONB 畸形用例全绿的唯一原因。
2. **`timelines` 的主键是复合的 `(timeline_id, revision)`**，所以夹具的区分度要求比
   本 task 更强：两条约束（PK 与那条给 T4 当靶子的超键 UNIQUE）覆盖的列高度重叠，
   靠「只动一维」区分不开。建议直接断言约束名，本 task 的两条注入已经证明约束名
   出现在驱动消息里、可断言。

**T3/T4 都要过一遍的一条通用检查（Q1 的推广）**：凡是「先 `get` 出聚合根、在内存里
判一下、再写回」的方法，那个判断用的都是**快照**，不是库里此刻那一行。`Timeline`
修订不可覆写由主键挡住，`EditingJob.update` 的 `updated_at` 必须前进则是同一类问题——
**写成 UPDATE 谓词（`WHERE updated_at < :new`），不要写成先读后比**，否则两个并发调用
都读到旧值、都判定「可以前进」。判据不是有没有加检查，是**那个检查是不是数据库一次
只看一条语句时做的**。

> **括号里那个具体谓词由 T4 修复轮实测推翻，2026-07-30 就地标注。** 本段的**判据**
> （检查必须发生在数据库一次只看一条语句时）成立，并且正是 T4 最终做法的依据；
> 但 `WHERE updated_at < :new` 本身不是乐观并发令牌——活调用方的新时间戳是 `now()`，
> 永远晚于库里的值，对每个调用方都放行。正确形态是比对**版本**而不是方向：
> `WHERE status = 快照.status AND updated_at = 快照.updated_at`。详见「T4 修复轮 F1」。

### 约束搬家：给 LE-13 的接口约束写在异常自己的 docstring 里

**首轮把它写在了本文件里，而且写了两处。** 那是错的位置，理由不是重复，是**读不到**：
CLAUDE.md §2.1 明令「会话恢复时只读当前任务文件，禁止批量加载历史任务记录」——
LE-13 的会话不会打开 `LE-05.md`。约束写在没人会打开的文件里，等于没写；
两处副本还会各自漂移。

现在它在 `application/materials.py` 的 `MaterialDescriptionProtected` docstring 里，
**调用方 `import` 这个名字时必然看到**。本文件只留这一处指路，不再复述内容。

搬过去的时候补了复审发现的**第二条约束**（首轮完全没有）：语义不对称。

- 快照**陈旧**、库里已归用户 → `with_ai_description` 交出一个 AI 对象 →
  `update_description` 抛 `Protected`；
- 快照**新鲜**、已经是用户的 → `with_ai_description` 直接返回 `self`（仍是 USER）→
  `update_description` 不挂谓词、UPDATE 命中 → **静默成功**。

同一个「用户已接管」的事实，两条路一条抛异常、一条正常返回。**LE-13 若按「没抛异常
就是写进去了」判定，会把丢弃当成写入。** 正确做法是比对写回后的内容或看
`description_source`，不要把「没异常」读成写入成功的证据。

时间窗方面的措辞也改准了：拿 `Material` 与写回之间隔着一次模型调用，所以这是**真实的
交错**而不是理论上的；但本期是单机单用户，要触发它得是用户恰好在那次模型调用期间编辑
了那条素材。首轮写「压测或并发场景下会稳定出现」，与本期形态不符，已删。

## T3 `timelines` 表 + 迁移 0038 + 仓储

### 交付物

| 文件 | 内容 |
| --- | --- |
| `backend/src/.../infrastructure/database/schema.py` | `timelines` 表，6 列，三条约束（复合主键、外键、超键唯一） |
| `backend/migrations/versions/20260729_0038_timelines.py` | 建表 + 可真跑的 `downgrade()` |
| `backend/src/.../application/timelines.py` | **五个**失败类 + 私有基类 |
| `backend/src/.../infrastructure/database/timeline_repository.py` | `SqlAlchemyTimelineRepository` + `_hydrate` + 三层文档转换器 |
| `backend/src/.../infrastructure/database/__init__.py` | 转出 `timelines` 表与仓储 |
| `backend/tests/integration/test_timeline_repository.py` | 真实 PostgreSQL 的 30 条用例 |
| `backend/tests/unit/control_plane/test_timeline_repository_validation.py` | 失败关闭分支的 90 条用例（首轮 72 + 修复轮 18） |

`hydration.py` 与 `editing_project_repository.py` 一个字没改——T2 把 `normalise_timestamp`
上移的收益在这里第一次兑现：`created_at` 直接引入，没有产生第三份「先验后归一」的副本。

### RED

两个测试文件先写、先跑，红都来自「模块不存在」，与 T1/T2 同形：

| 文件 | 首跑输出 |
| --- | --- |
| `test_timeline_repository_validation.py` | `ModuleNotFoundError: No module named 'automation_tool.control_plane.application.timelines'`（收集阶段，`1 error in 0.43s`） |
| `test_timeline_repository.py`（集成） | 同上（`1 error in 0.49s`） |

第三条红是**变异先行**逼出来的（M9，见下面「变异逼出来的一条用例」），第四条是
`domain/__init__.py` 不转出两个长度常量导致的
`ImportError: cannot import name 'MAX_TIMELINE_DURATION_MS'`——处置见「与计划不同」第三条。

### 六个「主动查」点的实测结论

一次性探针（`tests/integration/test_zzprobe_le05_t3_facts.py`，跑完即删，未进提交）
对真实 PostgreSQL 18.4 + asyncpg 0.31.0 + SQLAlchemy 2.0.51 实测，两轮：

1. **外键违例的异常面。** `IntegrityError`，`.orig.sqlstate == '23503'`，驱动消息带约束名，
   **DETAIL 行逐字引用找不到的键值**（`Key (a, b, c)=(...) is not present in table ...`）。
   所以 `from None` 在这条路径上不是形式——那串值就是调用方刚传进来的 `project_id`。
   复合主键违例是 `'23505'`，同样带约束名。两者都只能靠 SQLSTATE 区分，这是仓储把一个
   `IntegrityError` 分成三种答案的唯一依据。

2. **`IntegrityError.orig` 可以是 `None`。** `IntegrityError("stmt", None, None).orig is None`
   实测成立，`getattr(None, "sqlstate", None)` 是 `None`。所以取 SQLSTATE 必须走 `getattr`，
   直接 `error.orig.sqlstate` 会从仓储内部抛 `AttributeError`。有独立用例钉住。

3. **超键唯一约束永远不会是唯一的拒绝者。** 造一行同时违反 PK 与超键 UNIQUE 的数据，
   PostgreSQL 报的是 **`pk_le05_probe_parent`**（主键）。这是结构决定的：超键的列是主键列的
   超集，任何违反超键的行必然先违反主键。**所以违例注入无法证明这条约束存在**，
   派工单要求的「存在性断言」是唯一可行的形式——本 task 把它做得比名字更强，见下一条。

4. **三列 UNIQUE 确实能当复合外键的靶子。** 直接建了一张带
   `foreign key (a, b, c) references parent (a, b, c)` 的表，PostgreSQL 接受。这验证的是
   决策 3/5 的可行性本身。集成用例照这个做法写：**真的建一张探针表把复合外键声明上去**，
   建完立刻 drop。靶子缺失或列不对时 PostgreSQL 会报
   `there is no unique constraint matching given keys`——正是 T4 否则要现场撞上的那个失败。
   断言名字只能证明「有个叫这个名的东西」，建外键证明的是**T4 需要的那个性质**。

5. **JSONB 交回来是解析好的对象树，四层都是。** `list` → `dict` → `list` → `dict`
   （tracks → track → clips → clip），transition 也是 `dict`，JSON `null` 是 `None`。
   与 T2 对扁平数组的结论一致，只是深了两层。**另外 jsonb 会重排键**（实测
   `{"z":1,"a":2,"bb":3}` 存回来是 `{'a': 2, 'z': 1, 'bb': 3}`，按长度再按字节序），
   所以不能拿它跟带顺序的表示比——Python 的 dict 相等本来就不看顺序，本 task 的断言不受影响，
   但值得记一笔：**存进去的字节和取出来的不是同一串**。

6. **JSON 把 `-6` 和 `-6.0` 当两种东西，而域只接受 `float`。** 实测 `-6` 取回是 `int`，
   `-6.0` 取回是 `float`；`TimelineClip._validate_gain` 写的是 `type(self.gain_db) is not float`。
   **这条是本 task 最容易埋雷的地方**：一个「顺手把整数音量归一成 `-6`」的序列化器会写出
   一条谁都读不回来的行，而它在只用非整数音量（`-3.5`）的夹具下永远不会暴露。所以主夹具里
   专门放了一个 `-6.0`，集成层断言取回来的 `type(...) is float`，变异 M16 钉住它。

### `revision` 的上界：域没有，列有，本 task 只钉住现状不假装修好

域对 `revision` 只要求 `>= 1`，没有上界；列是 `integer`，上界 2³¹−1。实测越界一格
（`2**31`）的行为：**asyncpg 在语句发出前就拒绝参数**，抛自己的 `DataError`
（`value out of int32 range`），SQLAlchemy 包成普通 `DBAPIError`——是 `SQLAlchemyError`
但**不是** `IntegrityError`，于是落进 `(OSError, SQLAlchemyError)` 那条，答成
`TimelinePersistenceUnavailable`。

**这个答案是错方向的**：它让调用方去重试一件永远不会成功的事。没有在本 task 修，理由是
修法要么给域加上界（改 LE-03 的域对象，超出 T3 范围），要么在仓储里按 SQLSTATE `22000`
分流（`22000` 是「data exception」的总类，拿它当判据比问题本身更危险）。而触发它需要同一条
时间轴累积二十一亿个修订，靠自增达不到。

处置：**两端都写成用例**——`2**31 - 1` 正常往返，`2**31` 断言当前答成 `Unavailable`，
用例 docstring 写明「这条断言哪天红了，说明上界被收口了，该删的是这份遗留说明而不是这条用例」。
登记在下面「遗留」第 1 条。

### 「先 get、内存判、再写回」这条通用检查：T3 没有这种方法，但理由不是「用不上」

T2 交接要求 T3/T4 各过一遍。逐个方法核对的结果：

| 方法 | 有没有读后写 | 保护由谁做 |
| --- | --- | --- |
| `save` | 没有。不查重复也不查项目是否存在，直接 INSERT | 复合主键与外键，一条语句 |
| `get` | 只读 | —— |
| `latest_revision` | 只读；**取最大值交给 `ORDER BY ... DESC LIMIT 1`**，不是读全部再在 Python 里挑 | —— |

`latest_revision` 那一条值得单列：把所有修订读回来再 `max()` 今天也能跑对，但它是同一个形状的
错误——**判断发生在应用层拿到的快照上**，而且代价随修订数增长。

**措辞按 T2 收口轮的更正来写，不复制被推翻的那句。** 保护是「数据库一次只看一条语句」这件事
带来的，与「同一个事务」无关：连接跑在 `read committed` 下，同事务里每条语句各取一次新快照。
本 task 没有需要事后消歧的路径（`save` 的三种 SQLSTATE 已经互斥），所以这句话在这里只以
「为什么不写成先查后插」的形式出现。

### 键集必须逐字相等，而不是「取到的字段够用就行」

三层文档都要求 `document.keys()` 与域 dataclass 的字段名**完全相等**，不多不少，
键集从 `dataclasses.fields()` 读而不是手写一份。多出来的键直接拒绝。

理由不是洁癖，是 `.get()` 的静默面：**`TimelineClip` 九个字段里有四个的合法值就是 `None`**。
一个把 `gain_db` 拼错成 `gan_db` 的行，用 `.get()` 读出来是「没有音量」——在画面轨上完全合法，
于是水合出一个域接受、但和存进去的东西不是一回事的对象。这正是本仓库反复记的那类失败：
不是报错，是安静地返回一个看起来对的值。

反过来，缺键这一侧其实不靠键集也能拦住（构造器会拒 `None`），**键集唯一独有的战果是「多余的键」**
——变异 M3/M4/M5 三条各自只被对应的 `extra-key` 用例杀掉，这一点在用例 docstring 里写明了，
免得后人以为这个检查在挡缺键。

### 拒绝夹具必须「去掉那一处毛病就是合法的」

本 task 的拒绝用例全部按这条构造，因为反例太容易写：包装本身非法的话，用例不管被测代码做什么
都是绿的。两处具体的坑，都是写的时候当场发现并改掉的：

1. **转场用例最初放在画面轨的第一个片段上。** 第一个片段没有前一段可叠，
   `_validate_layout` 里 `overlap >= previous_tail`（`previous_tail` 初值 0）会拒掉**任何**转场，
   与文档形状无关。九条转场用例会全绿，而 `_transition` 整个可以是空实现。改成放在第二个片段，
   并单独加一条控制用例断言「同一个包装装上合法转场必须水合成功」；
2. **片段用例的行 `duration_ms` 最初固定 13200，而单轨只到 6000。** 聚合根会因为
   「声明长度 ≠ 画面轨终点」拒掉一切，同样掩盖 `_clip`。改成让包装的 `duration_ms` 跟着轨道走。

集成层的 `REFUSED_ROWS` 同理，每一条的 `duration_ms` 都按那条自己的轨道算，并按
**「只有哪一层看得见」**分了三组：只有轨道能看见的（画面轨有洞、转场吃穿前一段、
第二次转场重复吃同一段、音频轨乱序、轨内 `clip_id` 重复、字幕片段挂素材、旁白片段没音量）、
只有聚合根能看见的（声明长度对不上画面轨、根本没有画面轨、两条轨同种、两条轨同 id、
某片段越过时间轴末尾）、以及任何 CHECK 都写不出来的（空轨列表、长度两端越界一格）。

其中**「第二次转场重复吃同一段」是最该留下的一条**：它只被
`previous_tail`（剩余尾巴）这种写法拒绝。若把规则写成拿前一段的原始 `duration_ms` 比，
这一行会被接受，而中间那段只剩 1000ms 的片段会被两个转场吃干净。域对象的注释专门讲了这件事，
现在有一行数据钉住它。

### 变异逼出来的一条用例：M9 首轮 SURVIVED

`_material_id` 把 `InvalidResourceId` 吞掉、改答 `None`——**四条畸形素材 id 用例全部照绿**。

原因是那四条用的片段都没有 `text`：素材一变成 `None`，片段就变成「既没素材也没字幕」，
被 `TimelineClip` 的异或规则拒掉。**拒绝是真的，理由已经不是那个 id 了。**
这是「测试红 ≠ 断言有捕获力」的教科书形态：断言没有区分「解析不了」和「本来就没有」。

能把两者分开的只有一种形状：**字幕轨上一个既有 `text`、`source_material_id` 又畸形的片段**。
当前实现拒绝它（解析失败）；吞掉异常的版本会把它水合成一个完全正常的字幕片段——
**一条存进去的素材引用被安静地水合没了**。补了参数化用例
`test_a_material_identifier_that_cannot_be_parsed_is_not_read_as_absent`（四种畸形形态），
M9 转为 KILLED。

### 顺带修掉一条会误报的断言，以及它对 T2 的含义

`test_a_database_error_is_refused_without_leaking_its_message` 照 T2 的形状写成
「把消息设成 `"private database failure"`，断言渲染出的 traceback 里没有 `"private"`」。
**在克隆里跑它当场变红，而什么都没泄漏**：`traceback.format_exception` 会给每一帧渲染
`File "..."`，而 macOS 的临时目录是 `/private/tmp/...`——哨兵词命中的是**路径**。

这不是把断言改松，是把它改成本文件开头自己写的规则（「哨兵必须不可能巧合命中」）：
换成 `le05_leaked_database_failure`，与同文件 `LEAKED_TOKENS` 的命名一致。

**对 T2 的含义**：`test_material_repository_validation.py` 里有一条一模一样的断言，
同样会在路径含 `private` 的树里误报。没有在本 task 改它（那是另一个 task 的文件），
登记在「遗留」第 2 条——**谁下次对 T2 做变异或克隆验证，会先撞上它**。

### 变异自证

跑在隔离克隆里，做法同 T2 收口轮：`cp -c -p -R backend` + `compose.yaml` + `scripts/`
（APFS clonefile，**389 MB 用 1.6 秒**），改写克隆里 `automation_tool.pth` 指向克隆自己的
`src`，动手前先断言 `automation_tool.__file__` 落在克隆内（实测输出确认），再验证克隆在变异前
是绿的（`98 passed`），两项任一不成立就拒绝执行。脚本每条变异跑完立刻还原，收尾比对
三个源文件与工作树的 sha256。

脚本在会话 scratchpad。首轮 sha256 `28ff47a5e40daa2a9a59dcec332c9c2d0edc2992e07ba16f92b4053c39d3ccc1`；
**修复轮改写后（精确 M8、新增 S1～S3 与 M22、加入字节码清理）sha256
`fdfe86118968cdb35f8e271086c5313a909f4f77a9f6dca8d494b4b5d99eef76`**——下表是修复轮那一版的读数。

| 变异 | 结果 |
| --- | --- |
| M1 外键违例改答「修订已存在」 | KILLED |
| M2 未知完整性违例改答 `Unavailable`（可重试） | KILLED |
| M3 轨道不再校验键集 | KILLED（正好红 `extra-key`） |
| M4 片段不再校验键集，改用 `.get()` | KILLED（正好红 `extra-key`） |
| M5 转场不再校验键集，改用 `.get()` | KILLED（正好红 `extra-key`） |
| M6 `_all_documents` 只判 `list`，不判元素 | KILLED |
| M7 `_tracks` 不先判形状直接转换 | KILLED |
| M8 未知枚举值原样返回（保留循环与正常路径） | **首轮用整函数替换，只证明了「转换发生了」；修复轮换精确形态后一度 SURVIVED → 补函数级用例后 KILLED**（见修复轮 Q1） |
| **M9 素材 id 解析失败改答 `None`** | **首轮 SURVIVED → 补用例后 KILLED**（见上一节） |
| M10 时间戳不归一到 UTC | KILLED |
| M11 `_hydrate` 折进失败翻译里（T2 交接第 3 条） | KILLED |
| M12 `revision` 守卫从 `type(...) is int` 放宽成 `isinstance` | KILLED（`True` 那条） |
| M13 `get` 忽略入参的 revision 那一半 | KILLED |
| M14 `latest_revision` 改成升序 | KILLED |
| M15 `latest_revision` 丢掉 timeline 过滤 | KILLED |
| M16 整数音量归一成 JSON 整数 | KILLED |
| M17 `save` 不再翻译 `IntegrityError` | KILLED |
| M18 超键唯一约束当冗余删掉（schema + 迁移） | KILLED |
| M19 外键删掉（schema + 迁移） | KILLED |
| M20 主键收窄成单列 `timeline_id`（schema + 迁移） | KILLED |
| M21 `tracks` 在 `schema.py` 单侧改成可空 | KILLED |
| CANARY-1 SQLSTATE 常量写成字符串拼接 | SURVIVED（预期，语义等价） |
| CANARY-2 `_all_documents` 的循环变量改名 | SURVIVED（预期，语义等价） |

**M18 值得单看**：超键唯一约束无法用违例注入证明（第 3 条实测），但删掉它照样有测试变红——
红的是那条**真的去建一次复合外键**的用例。这说明「存在性断言」没有退化成「断言一个名字存在」。

### 与计划/派工单不同的四处，依据都是实测或既有先例

**一、失败词汇是五个类，不是四个。** 多出来的是 `TimelineProjectMissing`。派工单要求
「想清楚归哪类，写明理由」：它是**调用方错误**而不是数据损坏——时间轴本身格式完好，
`EditingProjectId` 也没法知道那个项目存没存过（领域对象不持有彼此引用，这正是 LE-04 记的那件事），
所以只有仓储这一层能发现。不能并进 `TimelineRevisionAlreadyStored`，因为那句话是假的
（这个修订并不存在）；也不能并进 `Unavailable`，因为重试永远不会成功。LE-06 要拿它答 404，
而 404 指的是**另一个资源**。

**二、未知 SQLSTATE 落到 `TimelineDataRejected` 而不是 `Unavailable`。** 三个候选都试过反面：
落 `AlreadyStored` 会告诉调用方「存过了」而其实没存（最不安全的方向）；落 `Unavailable` 会让它
重试一件不可能成功的事。`DataRejected` 说的是「这行被约束拒了，改数据别重试」，与它在
isinstance 守卫处的含义（「你给的数据不对」）是同一个调用方动作。M2 钉住这个方向。

**三、派工单把「转场重叠超限」标成「聚合根专属行」，实际是轨道级。**
`previous_tail` 那条规则在 `TimelineTrack._validate_layout`，聚合根 `Timeline` 看不到它。
钉的层是对的（那条数据确实只有整棵轨道能拒），但当时没顶回这个措辞——spec 审查提出后补记。
本文件「拒绝夹具」一节把三组分层写清楚了：轨道级七条、聚合根级五条、任何 CHECK 都写不出来的五条。
真正的聚合根专属行是「声明长度对不上画面轨」「根本没有画面轨」「两条轨同种/同 id」
「某片段越过时间轴末尾」——这些才需要跨轨道或拿轨道跟时间轴自己的声明比。

**四、两个长度常量从 `domain.timeline` 子模块引入，没有加进 `domain/__init__.py`。**
LE-03 有意没把 `MIN/MAX_TIMELINE_DURATION_MS` 放进域包的对外面（`TransitionKind` 等类型都放了），
而本 task 之外没有第二个使用者——`schema.py` 不需要它们（本表没有 CHECK、整数列也没有宽度要写）。
为一个测试的方便去改 LE-03 选定的公开面，是把别人的 task 顺手改了。等 LE-06 真的要用再加。

### 门禁

| 命令 | exit |
| --- | --- |
| `pytest tests/integration/test_timeline_repository.py -q` | 0（30 passed，24.74s） |
| `pytest` T1 + T2 + T3 三个集成文件 | 0（76 passed，52.91s）——T1 的 8 条与 T2 的 38 条逐条仍绿 |
| `pytest tests/unit -q` | 0（3638 passed，2 skipped，46.22s） |
| 单元层单跑两个新模块的分支覆盖 | 100%（Stmts 128 / Miss 0 / Branch 30 / BrPart 0），未借集成 |
| `ruff format --check`（本 task 改动的 7 个 Python 文件） | 0（7 files already formatted） |
| `ruff check`（同 7 个文件） | 0（All checks passed） |
| `mypy`（全量，house 配置） | 1 —— 17 errors / 8 files，与 T1/T2 记录的既有基线逐字一致，T3 新增 0 |
| `scripts/check_local_editing_roadmap_counts.py` | 0 |
| `scripts/check_demo_roadmap_counts.py` | 0 |

**7 个文件**：`schema.py`、迁移 0038、`application/timelines.py`、`timeline_repository.py`、
`database/__init__.py`、两个新测试文件。

单元总数比 T3 开工前多出的 102 条 = 时间轴仓储 90 + 失败词汇守卫自动纳入的 12。
最后这 12 条同样是自动来的：`test_application_failure_vocabulary.py` 动态发现 application 包里的
`_...Failure`，`timelines.py` 一落地就被扫进去（1 个基类 + **5** 个子类 = 6 个类 × 2 条参数化用例），
该文件从 T2 收口时的 63 条变成 **75** 条，没改它一个字。T1 交接第 1 条说的自动纳入，三轮各实测一次。

`mypy` 有一处是写的时候撞上、当场改掉的：`_enumeration_member` 原本写成 `members(stored)`，
经 `type[MemberT]` 调用会解析到 `StrEnum.__new__`（签名收 `str`），而 JSON 文档给的是 `object`。
**没有 `cast(str, ...)` 糊过去**——那个 cast 恰好是在对最可能不是字符串的那个值撒谎。改成遍历成员比值，
这本来就是按值查找时枚举自己做的事，对 `object` 诚实。

### 失败矩阵（本 task 适用行）

| 场景 | 落点 |
| --- | --- |
| 重复请求 | 同 `(timeline_id, revision)` 第二次 `save` → `TimelineRevisionAlreadyStored`，且不是 upsert（事后比对整行）；不同 revision、不同 timeline 各一条必须被接受 |
| 多实例竞争 | 仓储不做「先查后插」也不做「先查项目」，两条约束在数据库一侧拒绝；裸 INSERT 违例注入证明拦截在数据库 |
| 跨聚合根引用不存在 | `project_id` 指向未存项目 → 外键拒绝，翻成 `TimelineProjectMissing`；存好项目后同一条时间轴落库成功（证明拒的是引用本身） |
| 断连 / 库不可达 | 真实拒绝连接 → `Unavailable`，traceback 不含连接串（三个方法各一条） |
| 认证失败 / 权限不足 / 连接数上限 | 真实错密码 → `Unavailable`，traceback 不含角色名（三个方法各一条） |
| 敏感信息泄漏 | 五个异常消息固定、构造器不收参数；`from None` 由 traceback 渲染断言钉住；外键违例的 DETAIL 行逐字带 `project_id`，专门断言它不出现在渲染结果里 |
| 非法数据持久化面 | 单元层 56 条 + 集成层 17 条「表接受、域拒绝」的行在水合处被拒，覆盖三层文档形状、非法枚举串、非 v4 UUID、畸形素材 id、修订与长度两端越界。一条都不返回对象 |
| 结果不确定 / 静默失败 | `latest_revision` 空表答 `None` 而不是抛错（否则与「库挂了」不可分）；`get` 缺行答 `NotFound` |
| 越界数据到达驱动 | `revision` 超 int32 → 当前答 `Unavailable`（方向不理想，已登记遗留 1），两端各一条用例 |

### 清理

一次性探针 `tests/integration/test_zzprobe_le05_t3_facts.py` 两轮用完即删，未进提交。
变异脚本与克隆树都在会话 scratchpad，不在仓库内；克隆（389 MB）跑完已 `rm -rf`，
脚本 sha256 记在「变异自证」一节。开工前工作树 `git status --porcelain` 为空，HEAD 为
`260b1b5`（le-09-captions 合入后的合并节点），与派工单一致。

`managed_test_postgres` 负责本次 compose 实例的起停；收尾 `docker ps` 里没有本会话任何
pid 的 `automation-tool-pytest-<pid>-postgres-test-1`。列表里另有两个同前缀容器
（pid 32564 / 18424，已分别运行 12 小时与 23 小时，owning 进程均已退出），
属其他工作线，本会话开始前就在运行，**未动**——按 CLAUDE.md §8 只清理本次实例创建的资源。

### 遗留（不在本 task 修，已各自登记）

1. **`revision` 的域上界缺失，与 `integer` 列不匹配。** 越界一格答成 `Unavailable`
   （告诉调用方重试一件不可能的事）。修法要动 LE-03 的域对象或按 `22000` 分流，两者都超出 T3 范围；
   触发需要二十一亿个修订。两端已各有用例，越界那条的 docstring 写明「它红了就该删这条遗留」。
2. **`(OSError, SQLAlchemyError)` 缺口在 6 个仓储 17 处 try 仍存在**，仍是 `H8-23`，
   与 T1/T2 记录一致，本 task 未扩大也未缩小。**修复轮发现了它方向相反的另一半**，
   见修复轮 Q5——两者是 house 层同一件事的两端，宜合并成一个任务处置。
3. **`schema.py` 的类型与 nullable 门禁现覆盖 `materials` 与 `timelines` 两张表**
   （T3 沿用了 T2 新加的 `EXPECTED_TABLE_TYPES` 做法，M21 钉住），`editing_projects`
   与全库其他表仍只比列名与宽度。扩这件事属独立任务。**约束层的门禁只有 `timelines` 有**
   （修复轮 Q2 新加），其余表的 `schema.py` 约束与库中实际约束仍无人比对。

### 给 T4 的交接

**照抄**：五类失败 + 私有基类（词汇守卫自动纳入，不用另写测试）；`_hydrate` 必须在 `try` 外；
水合过域构造器、`InvalidResourceId` 折进域异常；`normalise_timestamp` 从 `hydration.py` 引入；
迁移测试比形状（列类型 + nullable + 约束名）而不是存在性；拒绝夹具「去掉那一处毛病就是合法的」。

**T4 直接可用的两件事**：

1. **复合外键的靶子已经就位并被钉住。** `uq_timelines_revision_project`，列序
   `(timeline_id, revision, project_id)`，T3 已用一张真建出来的探针表证明它能承载复合外键。
   T4 的迁移照这个列序写 `editing_jobs (timeline_id, timeline_revision, project_id)
   REFERENCES timelines (timeline_id, revision, project_id)`；列序在 T3 的用例里断言过，
   改了会红在 T3 而不是 T4。
2. **`_refuse_integrity_violation` 的按 SQLSTATE 分流可以照抄**，但 T4 要多一种：
   决策 5 的③是**部分唯一索引**，违例同样是 `23505`，与「重复 job_id」撞在同一个 SQLSTATE 上。
   **靠 SQLSTATE 分不开，必须看约束名**——而约束名出现在驱动消息里（T2/T3 各实测过、可断言），
   代价是那条消息同时带着 DETAIL 的键值，翻译时只能取名字、不能把消息带出去。

   > **本条后半句由 T4 实测推翻，2026-07-30 就地更正**（格式照决策 8 的先例）。
   > 「约束名只在驱动消息里、代价是带着 DETAIL 键值」不成立：约束名是
   > `error.orig.__cause__`（asyncpg 异常）上的**结构化字段** `constraint_name`，
   > 与 `detail` 分开，取它根本不碰消息。`error.orig` 本身（SQLAlchemy 的
   > `AsyncAdapt_asyncpg_dbapi.IntegrityError`）属性面只有 `args`/`pgcode`/`sqlstate`，
   > 没有 `constraint_name`——所以照本条原文去 `error.orig` 上找会永远取到 `None`。
   > 前半句（`23505` 分不开、必须看约束名）成立。详见 T4「五个主动查点」第 1 条。

**T4 要自己重判的两条**：

1. **`EditingJob.update` 的 `updated_at` 必须前进，写成 UPDATE 谓词（`WHERE updated_at < :new`），
   不要先读后比。** 这条是 T2 Q1 的推广，T3 因为没有可变行而用不上，T4 是第一个真正需要它的。
   **论证按 T2 收口轮的更正写**：保护由谓词那一条语句单独完成，与「同一个事务」无关
   （`read committed` 下每条语句各取新快照，实测过）；事后读只是尽力消歧。
   计划已定死「相等的 `updated_at` 算拒绝」，幂等重放靠状态机不靠仓储。

   > **本条的谓词形态由 T4 修复轮实测推翻，2026-07-30 就地标注。**「不要先读后比」
   > 与「保护由一条语句单独完成」都成立；`WHERE updated_at < :new` 不成立——它对每个活
   > 调用方都放行（新时间戳是 `now()`），只拒重放与时钟倒退。现在是
   > `WHERE status = 快照.status AND updated_at = 快照.updated_at` 的真 CAS，签名双参；
   > 「相等的 `updated_at` 算拒绝」也随之改了含义：**相等现在是承重的接受条件**
   > （域允许相邻两状态共享一瞬间），拒绝的是「比读到的版本早或晚」。
   > 详见「T4 修复轮 F1」。
2. **部分唯一索引的 `WHERE status = 'queued'` 是承重的，两侧都要钉。** 第二个 QUEUED 必须被拒，
   而一个 QUEUED + 一个 SUCCEEDED 必须共存——只钉前者的话，一条把 WHERE 去掉的变异照样活着。

**顺手改掉的一行（T3 发现，留给 T4，因为 T4 本来就要读那个文件）**：
`test_material_repository_validation.py` 的
`test_a_database_error_is_refused_without_leaking_its_message` 用 `"private"` 当哨兵词，
而 `traceback.format_exception` 会渲染每一帧的 `File "..."`——**macOS 的临时目录是
`/private/tmp`，任何 scratchpad 克隆都会让它误报**，什么都没泄漏也判红。T3 的同名用例已改成
`le05_leaked_*` 形状（与该文件自己 `LEAKED_TOKENS` 的命名一致），T2 那份照改即可。
**这不是把断言改松**——原写法碰巧还能命中路径泄漏，但那是意外而不是设计，
而它误报的时机恰好是「正在依赖这个信号做变异验证」的时候。

**首轮那条通用检查怎么用，再提醒一次**：T3 的 M9 与修复轮的 Q1/Q3/Q4 是同一件事的四次复发——
断言存在、拒绝也真的发生，但拒绝的理由不是被测的那段代码。**每写完一条拒绝用例问一句：
把被测的那处换成最笨的错误实现，这条用例会红吗？** 红不了，它钉的就是别的东西。

## T3 修复轮：质量审查 5 Important + spec 记录 3 条

五条全是同一族——**断言存在、拒绝也真的发生，但拒绝的理由已经不是被测的那段代码**。
这正是首轮 M9 自己写下的形态，说明那一条当时只被当成个案处理，没有推广去复查同类断言。
每条都先在隔离克隆里复现，再动手。

### Q1 `_enumeration_member` 的拒绝没被钉住，首轮 M8 的 KILLED 不成立

**审查者是对的，我复现了。** 精确变异（保留 `for` 循环与正常路径，只把落空分支改成
`return cast("MemberT", stored)`）在单元与集成两层**全绿**。

首轮 M8 用的是整函数替换，把正常路径一起打断，于是被 6 条 happy-path 用例杀掉——
**它证明的是「枚举转换发生了」，不是「未知成员被它拒了」**。台账那一行已就地改成它实际钉住的东西。

根因与 M9 同型：`TimelineTrack.__post_init__` 与 `TimelineTransition.__post_init__` 都有
`isinstance` 检查，裸字符串到不了对象上。**区别在于 M9 有救而这里没有**——M9 那边存在
「丢掉值反而水合成功」的形状（字幕轨片段），所以能用一行数据钉住；枚举这边每个消费者都
isinstance 一遍，**不存在任何一行数据能区分「拒了因为解析」和「拒了因为不是成员」**。

所以只能直接钉归属，加在函数层：`_enumeration_member(TimelineTrackKind, "lower-third")`
必须抛 `InvalidTimelineModel`，两个枚举各六种形态（未知、大小写不符、空串、null、数字、布尔）。
另加一条正向用例，用 **`is`** 而不是 `==` 断言——`StrEnum` 成员与自己的字符串相等，
`== "visual"` 对这条用例要拒的那个裸串照样成立。

> **函数已不在本模块，2026-07-30 T5 补标。** T4 出现第二个使用者，按 `hydration.py`
> 自己的规矩上移成公开的 `hydration.enumeration_member`，多一个 `refusal:
> type[Exception]` 参数；`timeline_repository.py` 删掉本地副本改为引入，本节这两条用例
> 改指 `hydration.enumeration_member(..., InvalidTimelineModel)`，**断言一字未改**。
> 上面写的 `_enumeration_member`（带下划线的私有名）在今天的代码里不存在——本节论证的
> 「为什么必须钉在函数层」不受影响，那正是它上移后仍被钉住的原因。详见「`enumeration_member`
> 上移」一节。上面 `mypy` 那段提到的同名函数同理。

### Q2 `schema.py` 的三条约束零钉，autogenerate 漂移是实的威胁

**三条独立变异（只删 `schema.py` 侧的超键 / 外键 / 主键收窄）全部 SURVIVED，我复现了。**

危险不在运行期——库里的约束由迁移建，删 `schema.py` 侧不影响任何行为。危险在
`migrations/env.py:19` 的 `target_metadata = metadata` 就是这份 metadata：**库里有而
`schema.py` 没有的约束，就是下一次 `alembic revision --autogenerate` 会提议 DROP 的东西**。
而下一个跑 autogenerate 的正是 T4，它依赖的正是那条超键。

修法：迁移测试的 `declared` 从只遍历 `.columns` 扩成同时比对 `Table.constraints` 的
**约束名 + 类型 + 列序**，外键另比 `target_fullname`。列序进断言是因为 T4 的复合外键必须按
这个顺序引用。

两个实现细节：`Table.constraints` 的静态类型是 `Constraint`，基类没有 `.columns`，
**用 `isinstance` 过滤会把一个没有 `.columns` 的约束静默跳过**（那正好是又一条「拒绝理由不对」），
所以写成循环里 `assert isinstance(...)` 让它露出来；`ColumnCollectionConstraint` 从
`sqlalchemy.sql.schema` 引入（不在 `sqlalchemy` 顶层，实测）。

顺带把那条「没有别的地方比对它们」的注释改准——它对列成立，读起来却像覆盖了整张表。

### Q3 水合拒绝整数 gain_db 零用例

派工单说「`-6` vs `-6.0` 已有用例」——**只在写入侧**（落库文档断言 `type is float`，M16 钉住）。
读取侧一行都没有，而 `_clip_document` 的 docstring 把「`TimelineClip` 拒绝整数音量」当成
承重论据，水合的整个前提又正是「行也会从 fixture、迁移和手跑语句进来」。

补读取侧时踩到派工单预告的那个坑：现有片段参数化跑在**画面轨**上，而画面轨**任何**音量都非法
（自己不带声音），整数音量放那儿会因为完全无关的理由被拒。所以新加了 narration 轨包装。
拒绝侧六种（整数 `-6`/`0`、**两个边界值 `-60` 与 `12` 的整数形态**、文本、布尔），
接受侧四种（含两端边界的 float）。两个边界值是特意的：只查范围不查类型的守卫会放它们过去，
只有类型检查能拒。

### Q4 `save()` 把序列化缺陷报成「数据库不可用」

`_column_values(timeline)` 原本在 `try` **里**，尾部 catch-all 会把它的任何失败翻成
`TimelinePersistenceUnavailable`——**让调用方重试一件永远不会成功的事**。这正是 `_row` 的
docstring 自己点名、并为此把 `_hydrate` 留在 try 外的那个危险，`save` 没有沿用同一条纪律。

今天触发不了（产出全是 JSON 原生类型，时间轴也已校验过），但这是「守卫便宜」的论据，
不是「把失败模式接成危险那一侧」的论据——**给域加一个字段，「今天触发不了」就不再成立**。

修法一行：`values = _column_values(timeline)` 提到 `try` 之上。用例注入一个会抛的序列化器，
断言异常**原样传播**而不是被翻译——坏掉的序列化器不属于调用方能处置的那五种结局，
把它伪装成其中一种正是它会被藏住的方式。这条是本轮唯一的真 RED（改之前实跑红在
`TimelinePersistenceUnavailable`）。变异 M22 把它挪回去，KILLED。

### Q5 `except _CONNECTION_FAILURES` 在本文件是行为冗余——裁定：保留，改说法

**审查者的观察成立**：本文件每个 `try` 都以 `except Exception` 收尾，删掉这一子句零行为差。
**但不删**——这是横跨七个仓储的 house 形状，其中几个没有尾巴、确实靠它，
单方面在 T3 里动它是越权。

要修的是**过度声称**：`_CONNECTION_FAILURES` 处的注释与三条用例 docstring 都把
OSError/SQLAlchemyError 的区分说成本文件的承重依据。对没有尾巴的仓储成立，对本文件不成立。
三处已改成「记录第三方事实、此处为文档性冗余、保留为 house 统一形状」，
并把每条用例真正钉住的东西写明（结局是 `Unavailable`、渲染出的 traceback 不含主机/角色/口令；
集成那条真正承重的是**三个方法都过一遍**，因为尾巴是每个 `try` 各写一次的）。

**house 层处置登记**：这与遗留 2 的 `H8-23` 是同一件事的两端，方向相反——那 17 处**缺**尾巴
（真的会漏），这里**有**尾巴（于是子句冗余）。两者宜合并成一个 house 任务：要么统一加尾巴、
把 `_CONNECTION_FAILURES` 降级为纯文档，要么统一去尾巴、让这个元组真正承重。**不要分两次改**，
否则中间态是「有的仓储靠它、有的不靠」，比现在更难读。

### spec 审查的三条记录

1. **M9 与 Q3 的夹具补了常驻断言。** `caption_with_material_row(None)` 与 `narration_row(-6.0)`
   各加一行进「包装本身合法」那条控制用例。没有它，字幕轨或音量规则一变，这两组拒绝用例
   会静默空转——**与 M9 本身完全同型的隐患，只是发生在夹具而不是断言上**；
2. **派工单错标「聚合根专属」已顶回**，见上面「与计划/派工单不同的四处」第三条；
3. **T2 的 `private` 哨兵不在本 task 改**，移进「给 T4 的交接」——T4 本来就要读那个文件。

### 方法论：克隆会把 `__pycache__` 带过来

`cp -c -pR` 连 `__pycache__` 一起复制（实测 24 个目录），mtime 原样保留于是 Python 认为
缓存有效，而 `.pyc` 里存的是**源树的 `co_filename`**——克隆里跑出来的 traceback 指向工作树，
**看着像隔离失败**。首轮就是这样被误导过一次（当时归因到 `.pth`，实际 `sys.path` 是对的）。

修复轮的克隆流程因此加了两步：建完立刻 `find -name __pycache__ -prune -exec rm -rf`，
跑测试用 `python -B` + `PYTHONDONTWRITEBYTECODE=1` + `-p no:cacheprovider`。
判据仍是「先断言 `automation_tool.__file__` 落在克隆内、再验证克隆本身是绿的」两道闸。

这与 CLAUDE.md §8.1 的 `core.checkStat` 同类：**结构看着全对，真跑一次才暴露**。

### 修复轮的变异

在重建的隔离克隆里跑整套（不是只跑新增的几条），首轮 21 条 + 新增 4 条 = 25 条全部 KILLED，
2 条 canary 全部 SURVIVED，`unexpected=0`，收尾三个源文件与工作树 sha256 一致。

| 新增变异 | 结果 |
| --- | --- |
| M8（精确形态，只改落空分支） | 补用例前 SURVIVED → 补后 KILLED |
| S1 只从 `schema.py` 删超键 | 修 Q2 前 SURVIVED → 修后 KILLED |
| S2 只从 `schema.py` 删外键 | 修 Q2 前 SURVIVED → 修后 KILLED |
| S3 只在 `schema.py` 收窄主键 | 修 Q2 前 SURVIVED → 修后 KILLED |
| M22 建行语句挪回 `try` 内 | KILLED |

**Q2 的三条改完断言后又重跑了一次**：因为 `isinstance` 那处从字典推导改写成显式循环，
断言形状变了，不能拿改写前的 KILLED 当数——重跑 S1/S2/S3 + M18～M21 + canary，仍全部符合预期。

### 修复轮门禁

数字已就地更新进上面 T3 的门禁表（同一个数字不在两处出现）。本轮相对首轮的变化：
单元层新增 18 条（90），全量单元 3638，覆盖 Stmts 128（`save` 多了一行），
集成仍 30 条、三文件回归仍 76 条，`ruff` 与 `mypy` 均维持 0 新增
（`mypy` 一度因 `Constraint` 无 `.columns` 报 18，改成显式 `isinstance` 断言后回到 17 基线）。

## T4 `editing_jobs` 表 + 迁移 0039 + 仓储 + 三条不变式

### 交付物

| 文件 | 内容 |
| --- | --- |
| `backend/src/.../infrastructure/database/schema.py` | `editing_jobs` 表，9 列，两条约束 + 一条部分唯一索引 |
| `backend/migrations/versions/20260729_0039_editing_jobs.py` | 建表 + 建索引 + 可真跑的 `downgrade()` |
| `backend/src/.../application/editing_jobs.py` | **七个**失败类 + 私有基类 |
| `backend/src/.../infrastructure/database/editing_job_repository.py` | `SqlAlchemyEditingJobRepository` + `_hydrate` + 两条 UPDATE 谓词 |
| `backend/src/.../infrastructure/database/hydration.py` | `enumeration_member` 上移（第二个使用者出现） |
| `backend/src/.../infrastructure/database/timeline_repository.py` | 改用上移后的 `enumeration_member`，删掉本地副本 |
| `backend/src/.../infrastructure/database/__init__.py` | 转出 `editing_jobs` 表与仓储 |
| `backend/tests/integration/test_editing_job_repository.py` | 真实 PostgreSQL 的 39 条用例 |
| `backend/tests/unit/control_plane/test_editing_job_repository_validation.py` | 失败关闭分支的 78 条用例 |

另外两处是派工单指定的顺手修（T2/T3 留下的）：`test_material_repository_validation.py` 的
`private` 哨兵改成 `le05_leaked_*`；`test_timeline_repository.py` 里那段
`assert isinstance` 的注释改成据实表述。

### RED

两个测试文件先写、先跑，红都来自「模块不存在」，与 T1～T3 同形：

| 文件 | 首跑输出 |
| --- | --- |
| `test_editing_job_repository_validation.py` | `ModuleNotFoundError: No module named 'automation_tool.control_plane.application.editing_jobs'`（收集阶段，`1 error in 0.56s`） |
| `test_editing_job_repository.py`（集成） | 同上（`1 error in 0.56s`） |

第三条红是**变异先行**逼出来的（M18，见下面「变异逼出来的两条用例」）。

### 五个「主动查」点的实测结论

一次性探针（`tests/integration/test_zzprobe_le05_t4_facts.py`，跑完即删，未进提交）
对真实 PostgreSQL 18.4 + asyncpg 0.31.0 + SQLAlchemy 2.0.51 实测，两轮：

1. **约束名不在 `error.orig` 上，在它的 `__cause__` 上。** 这一条推翻了 T3 交接里
   「只能从驱动消息里取名字」的做法。实测 `error.orig` 是 SQLAlchemy 的
   `AsyncAdapt_asyncpg_dbapi.IntegrityError`，属性面只有 `args`、`pgcode`、`sqlstate`
   ——`constraint_name` 在它 `__cause__` 指向的 asyncpg `UniqueViolationError` 上，
   且与 `detail` 是**两个独立字段**：

   ```text
   orig dir            ['add_note', 'args', 'pgcode', 'sqlstate', 'with_traceback']
   cause type          <class 'asyncpg.exceptions.UniqueViolationError'>
   cause constraint_name  'uq_le05_probe_queued'
   cause detail           'Key (tl)=(2222...-2222) already exists.'
   ```

   所以取名字**根本不用碰消息**，键值那一串不会被顺手带出来。交接说的「代价是那条消息
   同时带着 DETAIL 的键值」在这条路径上不成立。降级方向是安全的：某天 SQLAlchemy 不再
   串 `__cause__`，取到 `None`，落到 `EditingJobDataRejected`——说得更少，而不是说错。
   M3 钉住这一跳（把 `getattr` 从 `__cause__` 改回 `error.orig`，KILLED）。
2. **部分唯一索引与主键撞同一个 `23505`**，实测两者 SQLSTATE 逐字相同，只有约束名不同
   （`pk_le05_probe` / `uq_le05_probe_queued`）。交接对这一点的判断成立。
3. **CHECK 是 `23514`，复合外键是 `23503`**，且 `23503` 的 DETAIL 一次引用三个键值
   （`Key (timeline_id, timeline_revision, project_id)=(...) is not present in table "timelines"`）
   ——这是本表泄漏面最大的一条，集成用例专门断言这三个值都不出现在渲染结果里。
4. **`Index` 的谓词读法。** `index.dialect_options["postgresql"]["where"]` 交回
   `BinaryExpression`；`compile(compile_kwargs={"literal_binds": True})` 渲染成
   `editing_jobs.status = 'queued'`。**不带 `literal_binds` 渲染成 `status = :status_1`**
   ——字面量藏进绑定参数，换成任何别的状态都比出来相等，门禁等于没写。
5. **空 `IN` 列表是合法且恒假的**：`in_([])` 渲染成 `status IN (NULL) AND (1 != 1)`。
   这条当时决定了 `QUEUED` 的空前驱集不需要特判。**第三方事实仍然成立，但这个用途已经
   没有了**——修复轮把前驱集整个移出 SQL（F1），现在没有任何 `in_()` 谓词在这个文件里。
   保留它是因为它是实测过的 SQLAlchemy 行为，下一个想在谓词里放空集合的人会需要。

### `update` 的第二条谓词：`updated_at` 单独挡不住，实测

> **本节的结论只对了一半，由质量复审实测推翻，处置见下面「T4 修复轮 F1」。**
> 「`updated_at` 单独挡不住」成立；「加一条前驱集谓词就挡住了」不成立——它只关了
> 目标状态前驱集唯一的那些边。**本节以下保留原文作为过程记录，不要照它实现**：
> 现在的 `update` 是真 CAS（`WHERE status = 快照.status AND updated_at = 快照.updated_at`），
> 转换合法性移到 Python 守卫并答 `DataRejected`。

计划与交接都只要求 `WHERE updated_at < :new`。**只有这一条时有一条真实的数据丢失路径，
本 task 实测复现，并加了第二条谓词堵上。**

`EditingJob` 自己的 docstring 就写了这个场景（LE-12 的对账遍历）。三步全部走正规方法：

1. worker 拿到一个 CANCELLING 的作业——取消是协作式的，仍可能跑完——成功，写
   `SUCCEEDED` + artifact，时间 `t2`；
2. 对账遍历手里还是第 1 步之前那个 **CANCELLING 快照**，判定作业被遗弃，调
   `fail(WORKER_LOST)`，时间 `t3 > t2`；
3. `CANCELLING -> FAILED` 是合法边，域对象照单全收；`t3 > t2`，**时间谓词放行**。

结果是一条已经出片的记录被标成失败、产物成孤儿。这与 T2 Q1 是同一族：
**状态机判的是调用方手里的快照，不是库里那一行**，而 T2 收口时定的判据正是
「那个检查是不是数据库一次只看一条语句时做的」。

修法：谓词加一条 `status IN (<该目标状态的合法前驱>)`，前驱集**从
`EditingJobStateMachine.can_transition` 推导**而不是手写第二份图——手写的那份会在
边一变时静默漂移，且漂移方向危险（多留一个前驱 = 放一条非法转换进库）。
`QUEUED` 的前驱集是空元组，于是任何把作业写回队列的 update 匹配不到行，这与域里
「没有回到 QUEUED 的边」是同一句话；空 `IN` 恒假由第 5 条实测保证。

**答案仍是 `EditingJobStale`，不新增第八个类**：两条谓词说的是同一件事——你手里的
快照不再是库里那一行，重新读一次再决定。合并成一个不是偷懒，是它们对调用方是同一个动作。

变异 M9（删掉状态谓词）由 `test_a_stale_snapshot_cannot_undo_a_render_that_already_finished`
杀掉；M10/M11（前驱集恒真 / 反向读图）由推导表的用例杀掉。

**这是对计划 T4 节的一处扩写而不是推翻**：计划写的「`updated_at` 必须前进」原样保留，
「幂等重放靠状态机不靠仓储」也保留——相等的时间戳仍然拒绝。加的是库侧那一半，
因为域侧那一半按定义看不见库里的行。

### `update` 为什么写全列，以及它没有解决的那个问题

> **已被修复轮取代：`update` 现在只写 `_MUTABLE_COLUMNS`。** 本节末尾说「没解决」的
> 改指问题已经关掉，而其中「部分索引拦得住占用槽位」那句话本身是错的（F2）。
> 见「T4 修复轮 F2」与「第三条路」。以下保留原文作为过程记录。

`_column_values` 一个函数同时供 `save` 和 `update`，理由是 T2 在 `_description_values`
上写过的那条：两份列集会在域加字段时漂移，且漂移方向是「插入时存了、之后每次更新都
静默丢掉」。把身份列写成它们自己的值不花任何代价，而复合外键在 UPDATE 上一样会重查，
所以它们改不成不存在的东西。

**没解决的**：一个自己构造 `EditingJob`（不经 `get`）的调用方，可以把作业改指到**另一条
同样存在**的时间轴修订上。外键仍然拦得住不存在的目标，部分索引仍然拦得住占用中的
QUEUED 槽位，所以不是安全洞，但它确实是「全列更新」买单的那一项。**没有在本 task 收窄**：
正规路径（`get` → 转换方法 → `update`）永远不改这四列，所以不存在 T2 Q1 那种「不需要
误用就发生」的形态；而把身份列挪进 `WHERE` 会给 `rowcount == 0` 加上第三种含义，
把消歧弄得更糊。登记在下面「遗留」第 1 条。

### 「行不存在 vs 被谓词拒」：区分，理由与 T2 的 Protected 同型

`rowcount == 0` 两种含义，用一次后续 SELECT 消歧：查不到行 → `EditingJobNotFound`；
查得到 → `EditingJobStale`。

**照 T2 收口轮的更正表述写，不复制被推翻的那句**：保护由 UPDATE 那一条语句单独做完，
后续 SELECT 只是尽力消歧、可被并发抢跑（`read committed` 下每条语句各取新快照，
同事务也一样）。那个窗口里两种答案都安全，都是「停下来重读」。

**值得分**：LE-06 要对这两种分别答 404 与 409。塌成 `NotFound` 会告诉调用方一个正在跑的
作业不存在——这正是不安全的那个方向。M13/M14（两边各塌一次）都被杀。

### 三条不变式的违例注入证据

三条全部手工 `INSERT` 违例行，断言 PostgreSQL 自己拒绝，并断言 SQLSTATE 与约束名。
**注入行都构造成「去掉那一处毛病就是合法的」**，并且每条都配一条对照：把那一处改对，
同一条数据必须落库成功。

| 不变式 | 注入的行 | SQLSTATE | 约束名 |
| --- | --- | --- | --- |
| ① job.project_id ≠ timeline 所属 project | 时间轴在、修订在、**另一个项目也真的存过**，只有三者不属于同一组 | `23503` | `fk_editing_jobs_timeline_revision` |
| ② 指向不存在的 revision | 时间轴在、项目对，只有 `revision + 1` 从没写过 | `23503` | 同上 |
| ③ 同 `(timeline_id, revision)` 第二个 QUEUED | 第一条经仓储存好，第二条裸 INSERT | `23505` | `uq_editing_jobs_queued_timeline_revision` |

①的夹具有一处是特意的：**第二个项目必须真的存在**。若用一个从没存过的项目，
这行会被一条弱得多的规则拒掉，把复合外键换成单列外键测试照样绿——那就退化成
「门禁绿 ≠ 门禁在守」。S5（复合外键收窄成单列 `project_id` → `editing_projects`）
正是钉这一点的变异，KILLED。

③**两侧都钉**（交接重判②）：除了「第二个 QUEUED 被拒」，另有一条
`test_finished_renders_of_one_revision_coexist_with_a_queued_one`——同一修订上
SUCCEEDED / FAILED / CANCELLED / RUNNING / CANCELLING 五行 + 再来一条 SUCCEEDED +
最后压一条 QUEUED，全部必须接受。缺这一条，S1/S2（去掉 `WHERE`）活着。
还有第三条 `test_leaving_the_queue_frees_the_slot_for_the_next_render`：作业一
`start()`，槽位必须立刻空出来——这是 LE-12 重试的前提，而「索引在 UPDATE 上重算」
和「索引在 INSERT 上生效」是两件事。

### 部分唯一索引不在 `Table.constraints` 里，门禁另比 `Table.indexes`

派工单第 3 条。T3 新加的约束门禁遍历 `Table.constraints`，而 `Index` 根本不在里面——
第三条不变式在 `schema.py` 侧会是零钉，与 T3 修 Q2 之前的超键同形，autogenerate
漂移威胁一模一样（`migrations/env.py` 的 `target_metadata` 就是这份 metadata）。

本 task 的门禁因此比对三样，**分别落在两个不同的事实源上**：

1. **迁移建出来的**：`pg_indexes.indexdef` 逐字相等，含
   `WHERE ((status)::text = 'queued'::text)`；外键读 `pg_get_constraintdef`，
   含三列的顺序；
2. **`schema.py` 声明的**：`Table.constraints` 比名字 + 类型 + 列序（照抄 T3），
   **另加 `Table.indexes` 比列序 + `unique` + `postgresql_where`**，谓词用
   `literal_binds` 渲染后比字面量；
3. `downgrade` 之后另断言索引在 `pg_indexes` 里也没了（drop table 连带 drop index，
   但那是 PostgreSQL 的行为而不是本迁移写下来的事，值一条断言）。

S2（只从 `schema.py` 删 `postgresql_where`）与 S3（只从 `schema.py` 删整条索引）
都由第 2 条杀掉；S1（两边都删）由不变式③的共存用例杀掉。

### `enumeration_member` 上移，以及 T3 那两条用例跟着搬

第二个使用者出现，按 `hydration.py` 自己 docstring 写的规矩上移
（`normalise_timestamp` 在 T2 走的是同一条路）。留在两处的代价那段 docstring 写得很清楚：
「straightforward 版本会通过为另一份写的每一条测试」——这里的 straightforward 版本
就是 `members(stored)` 或直接 `cast`。

签名多一个 `refusal: type[Exception]`，因为两个模块各自答自己的域异常；以类的形式
`raise`，于是两个域异常都必须继续保持「构造器不收参数」——这正是防止把违例值附上去的那条。

`timeline_repository.py` 删掉本地副本改为引入，T3 的两条函数级用例改指新位置
（`hydration.enumeration_member(..., InvalidTimelineModel)`），断言一字未改。

### 变异逼出来的两条用例：M18 首轮 SURVIVED

`_optional_artifact_id` 把 `InvalidResourceId` 吞掉、改答 `None`——**整套 78 + 37 条全绿**。

首轮那条用例是「**succeeded** 行 + 畸形 artifact id」，还专门在 docstring 里写了
「跑在 succeeded 行上，因为 queued 行的合法值就是 `None`，解析器丢掉值会看起来正确」。
**这个理由反了。** 丢掉之后那行变成「succeeded 却没有 artifact」，被
`_validate_facts_match_status` 拒掉——**拒绝是真的，理由已经不是那个解析器。**
与 T3 的 M9 逐字同型。

能区分的只有 **queued 行**：丢掉之后是一个完全正常的排队作业，吞异常的版本水合成功，
当前实现拒绝。补了参数化用例
`test_a_value_that_cannot_be_parsed_is_not_read_as_the_absence_of_one`
（artifact 三种畸形 + failure_code 三种畸形，全部挂在 queued 行上），
集成层 `REFUSED_ROWS` 同步加两行。M18 转 KILLED。

**顺带发现首轮 M17 是个假 KILLED。** M17 把 `_optional_failure_code` 整个改成
`return None`，于是往返用例（断言 `failure_code is WORKER_LOST`）杀掉它——
**证明的是「转换发生了」，不是「读不懂的值被拒了」**，与 T3 复审揪出的 M8 同一个毛病。
补了精确形态 M24（只吞落空分支、正常值照旧解析），新用例把它也杀掉。

### 变异自证

跑在隔离克隆里，做法同 T3 收口轮：`cp -c -p -R backend` + `scripts/` + `compose.yaml`
（APFS clonefile，**357 MB 用 ~2 秒**），改写克隆里 `automation_tool.pth` 指向克隆自己的
`src`，**建完立刻清掉 157 个 `__pycache__` 目录**，跑测试用 `python -B` +
`PYTHONDONTWRITEBYTECODE=1` + `-p no:cacheprovider`；动手前先断言
`automation_tool.__file__` 落在克隆内（实测输出确认），再验证克隆在变异前是绿的
（`204 passed` / `39 passed`），两项任一不成立就拒绝执行。每条变异跑完立刻还原，
收尾比对四个源文件与工作树的 sha256（一致）。

脚本另有一道**锚点唯一性预检**：替换目标在文件里出现次数不等于 1 就整条拒绝并还原。
实测拦下两次——`Column("status", String(length=16), nullable=False),` 在 `schema.py` 里
有两处（另一处是别的表），`failure_code` 那行的锚点有三处。**没有这道闸，这两条会静默
改到别的表上，然后以「KILLED」记账。**

脚本在会话 scratchpad，sha256
`b99d035c114b18a66c10b3e565aed4bca9dc70ab0d6e18dc9ac618d3d80e9019`；克隆跑完已 `rm -rf`。

| 变异 | 结果 |
| --- | --- |
| M1 外键违例改答「已登记」 | KILLED |
| M2 未知唯一约束猜「已登记」 | KILLED |
| **M3 约束名从 `error.orig` 取（不走 `__cause__`）** | **KILLED**——钉住主动查第 1 条那一跳 |
| M4 未知完整性违例改答 `Unavailable`（可重试） | KILLED |
| M5 `save` 不再翻译 `IntegrityError` | KILLED |
| M6 `update` 不再翻译 `IntegrityError` | KILLED |
| **M7 CAS 退回首轮的 `updated_at <`（F1 缺陷本身）** | **KILLED**（修复轮） |
| M8 CAS 时间戳放宽成 `<=` | KILLED（修复轮） |
| **M25 CAS 去掉 status 那一半** | **KILLED**——红的是同一瞬间那条（修复轮） |
| M26 CAS 去掉 updated_at 那一半 | KILLED——红的是等值边界（修复轮） |
| M27 Python 转换守卫删掉 | KILLED（修复轮） |
| M28 守卫不再要求两参同一个作业 | KILLED（修复轮） |
| **M29 `update` 又写回全列** | **首轮设计下不存在；修复轮首跑 SURVIVED → 补身份列用例后 KILLED** |
| M30 `status` 划进身份列（update 不再写它） | KILLED（修复轮） |
| **M31 时间戳倒退守卫删掉** | **KILLED**（确认复审轮） |
| M32 倒退守卫放宽成 `<=`（把相等也拒了） | KILLED——红的是同一瞬间那条（确认复审轮） |
| M33 倒退守卫比较方向写反 | KILLED（确认复审轮） |
| **M12 `update` 丢掉 `job_id` 过滤** | **首轮 KILLED；改 CAS 后 SURVIVED → 换成同版本邻居后 KILLED** |
| ~~M9 删掉状态谓词 / M10 前驱集恒真 / M11 反向读图~~ | **已退役**——`_SOURCE_STATUSES` 被 F1 移除，这三条无处可施 |
| M13 `rowcount 0` 一律答 `NotFound` | KILLED |
| M14 `rowcount 0` 一律答 `Stale` | KILLED |
| M15 `matched` 恒真 | KILLED |
| M16 未知枚举值原样返回（精确，只改落空分支） | KILLED |
| M17 失败码整个不解析（**粗糙形态，见上文**） | KILLED（只证明了「转换发生了」） |
| **M18 artifact id 解析失败改答 `None`** | **首轮 SURVIVED → 补用例后 KILLED** |
| M24 失败码读不懂改答 `None`（精确，只吞落空分支） | KILLED |
| M19 `updated_at` 不归一到 UTC | KILLED |
| M20 时间戳先转换后校验（naive 被洗白） | KILLED |
| M21 `_hydrate` 折进读取的 try 里 | KILLED |
| M22 `save` 建行语句挪进 try 内 | KILLED |
| M23 `update` 建行语句挪进 try 内 | KILLED |
| S1 部分索引两边都去掉 `WHERE` | KILLED（红的是共存那条） |
| S2 只从 `schema.py` 去掉 `WHERE` | KILLED（红的是 `Table.indexes` 门禁） |
| S3 只从 `schema.py` 删整条索引 | KILLED（同上） |
| S4 部分索引两边都改成 `status = 'running'` | KILLED |
| S5 复合外键两边都收窄成单列 `project_id` | KILLED（红的是不变式①） |
| S6 只从 `schema.py` 删外键 | KILLED |
| S7 `status` 列宽收到 8（`schema.py` 侧） | KILLED |
| S8 `failure_code` 在 `schema.py` 单侧改成非空 | KILLED |
| CANARY-1 SQLSTATE 常量写成字符串拼接 | SURVIVED（预期，语义等价） |
| CANARY-2 可变列过滤的推导变量改名 | SURVIVED（预期，语义等价） |

**38 条变异全部 KILLED，2 条 canary 全部 SURVIVED，`unexpected=0`**（含修复轮新增 8 条、
确认复审轮新增 3 条、退役 3 条）。脚本最终 sha256
`e872ff9e4729e728ea9166fcd0e72896debda0a64fb8260c0aa91964b45794c4`。
**每轮都是整套重跑**，不是只跑新增的那几条。

### 与计划/派工单不同的四处，依据都是实测或既有先例

**一、失败词汇是七个类，不是四个。** 三个「写被拒了」（`AlreadyRegistered`、
`RevisionAlreadyQueued`、`TimelineRevisionMissing`）+ 两个「更新没落」（`NotFound`、
`Stale`）+ 两个通用（`DataRejected`、`Unavailable`）。多出来的都是本表**可变**带来的：
前四张表没有 `update`，也就没有 `Stale`，更没有「同一个 `23505` 要分两种」。
**①和②合成一个 `TimelineRevisionMissing`**，因为一条复合外键拒的时候 PostgreSQL 不说
是哪一半——分成两个就得靠猜，而「那条时间轴、那个项目、那个编号的修订不存在」这句话
两种情况下都是真的，且指向同一个修法。

**二、约束名走 asyncpg 的结构化字段，不走驱动消息。** T3 交接说「必须看约束名，而约束名
在驱动消息里」，实测那是 `error.orig.__cause__` 上的一个独立属性，与 `detail` 分开——
见主动查第 1 条。这是把交接的做法改好，不是绕开它。

**三、`status`/`failure_code` 用 `String(16)`/`String(32)` 而不是计划字面写的 `TEXT`。**
沿用 T2 的既有形状（`materials.kind`、`description_source` 都是 `String(16)`），
并补一条单元用例断言两个枚举各自最长成员放得下——两侧都从各自的定义读，不产生第三份副本。
S7 钉这个方向。

**四、`update` 的并发控制与计划不同，而且改过两轮。** 计划只要求
「`updated_at` 必须前进」。首轮加了一条前驱集谓词（`status IN (<合法前驱>)`），依据是实测
复现的数据丢失；**修复轮证明那还不够**，换成真 CAS
（`WHERE status = 快照.status AND updated_at = 快照.updated_at`，签名 `update(previous,
changed)` 双参），前驱集移进 Python 守卫答 `DataRejected`，并另加一条「时间戳不许倒退」
的守卫。**这一条是本清单里唯一改过两轮的**，完整依据与逐边表见「T4 修复轮 F1」——
查「实现与计划差在哪」的人看到这里就够，不必回读被推翻的那两节。

### 门禁

| 命令 | exit |
| --- | --- |
| `pytest tests/integration/test_editing_job_repository.py -q` | 0（48 passed，34.35s） |
| `pytest` T1 + T2 + T3 + T4 四个集成文件 | 0（124 passed，82.74s）——前三个 task 的 76 条逐条仍绿 |
| `pytest tests/unit -q` | 0（3741 passed，2 skipped，46.45s） |
| 单元层单跑两个新模块的分支覆盖 | 100%（Stmts 125 / Miss 0 / Branch 26 / BrPart 0），未借集成 |
| `ruff format --check`（本 task 改动的 12 个 Python 文件） | 0 |
| `ruff check`（同 12 个文件） | 0 |
| `mypy`（全量，house 配置） | 1 —— 17 errors / 8 files，与 T1～T3 记录逐字一致，T4 新增 0 |
| `scripts/check_local_editing_roadmap_counts.py` | 0 |
| `scripts/check_demo_roadmap_counts.py` | 0 |
| `scripts/check_third_party_sources.py` | 0 |

单元总数比 T4 开工前多出的 103 条 = 作业仓储 87 + 失败词汇守卫自动纳入的 16。
后者同样是自动来的：`test_application_failure_vocabulary.py` 动态发现 application 包里的
`_...Failure`，`editing_jobs.py` 一落地就被扫进去（1 个基类 + **7** 个子类 = 8 个类 ×
2 条参数化用例），该文件从 T3 收口时的 75 条变成 **91** 条，没改它一个字。
T1 交接第 1 条说的自动纳入，四轮各实测一次。

`mypy` 有三处是写的时候撞上、当场改掉的，**都没有用 `type: ignore` 糊过去**：
`compiled_predicate` 原本 `cast` 一个未标注的 dialect 对象（`postgresql.dialect` 与
`PGDialect` 都是未标注构造器），改成不传 dialect——实测默认编译器与 `PGDialect` 对这条
谓词渲染逐字相同，而**真实 PostgreSQL 那一侧本来就由 `pg_indexes.indexdef` 单独断言**，
所以这里要的只是「`schema.py` 声明的字面量有没有变」；参数化夹具改成带类型的元组，
去掉一处 `cast` + `type: ignore[arg-type]`；artifact 那处先 `assert is not None` 收窄，
去掉 `type: ignore[union-attr]`。

### 失败矩阵（本 task 适用行）

| 场景 | 落点 |
| --- | --- |
| 重复请求 | 同 `job_id` 第二次 `save` → `AlreadyRegistered`，且不是 upsert（事后比对整行）；同修订第二个 QUEUED → `RevisionAlreadyQueued` |
| 多实例竞争 | 仓储不做任何「先查后插/先读后判」；主键、复合外键、部分唯一索引、两条 UPDATE 谓词全在数据库一侧；三条不变式各有裸 INSERT 违例注入证明拦截在数据库 |
| 非法状态转换的持久化面 | 非法 status/failure_code 串在水合处被拒；**陈旧快照在 CAS 处被拒**——比对的是「行还是不是读到的那个版本」（`status` + `updated_at` 成对），而不是「这一步合法吗」；不合法的 `(previous, changed)` 对由 Python 守卫答 `DataRejected`。逐边证据见修复轮 F1 的表 |
| 跨聚合根引用不存在 | 复合外键拒 →`TimelineRevisionMissing`；补齐引用后同一条数据落库成功（证明拒的是引用本身） |
| 幂等 / 重放 | CAS 等值比对（贴边界：命中版本本身接受，前后各一微秒拒绝）；重放在第一次写入后自然不再匹配 |
| 结果不确定 / 静默失败 | `rowcount == 0` 不塌成一种答案，后续 SELECT 消歧并写明可被并发抢跑；坏掉的序列化器原样传播而不是伪装成 `Unavailable`（`save`/`update` 各一条） |
| 断连 / 库不可达 | 真实拒绝连接 → `Unavailable`，traceback 不含连接串（三个方法各一条） |
| 认证失败 / 权限不足 / 连接数上限 | 真实错密码 → `Unavailable`，traceback 不含角色名（三个方法各一条） |
| 敏感信息泄漏 | 七个异常消息固定、构造器不收参数；`from None` 由 traceback 渲染断言钉住；复合外键的 DETAIL 一次带三个键值，专门断言项目与时间轴 id 都不出现 |
| 非法数据持久化面 | 单元层 + 集成层共 26 条「表接受、域拒绝」的行在水合处被拒，覆盖两个枚举的畸形串、状态与事实的八种错配、两个时间戳的四种畸形、`updated_at < created_at`、非 v4 UUID、以及**读不懂的值被当成没有值**这一类 |
| App 重启后的恢复 | `get` 经域构造器水合，六状态 × 事实组合各一条往返 |

### 清理

一次性探针 `tests/integration/test_zzprobe_le05_t4_facts.py` 两轮用完即删，未进提交。
变异脚本与克隆树都在会话 scratchpad，不在仓库内；克隆（357 MB）跑完已 `rm -rf`，
脚本 sha256 记在「变异自证」一节。开工前工作树 `git status --porcelain --untracked-files=all`
为空，HEAD 为 `5c2e5b2`，与派工单一致。

`managed_test_postgres` 负责本次 compose 实例的起停；收尾 `docker ps` 里没有本会话任何
pid 的 `automation-tool-pytest-<pid>-postgres-test-1`。列表里另有两个同前缀容器
（pid 32564 / 18424），属其他工作线，本会话开始前就在运行，**未动**——按 CLAUDE.md §8
只清理本次实例创建的资源。colima `default` profile 本会话开始前已在运行，未启停
（§9.3：全局共享入口不适用「用完就关」）。

### 遗留（不在本 task 修，已各自登记）

1. ~~**`update` 写全列，能把作业改指到另一条存在的修订**~~ —— **修复轮已关闭**：
   `update` 改为只写 `_MUTABLE_COLUMNS`，身份列与 `created_at` 结构上写一次，
   由 `test_an_update_cannot_move_a_job_to_another_timeline_revision` 钉住（M29 杀）。
   连带更正原文里一句错话：「外键与部分索引仍然拦得住……占用中的槽位」——**部分索引
   那半句是错的**（F2），`update` 写不出 queued 行，索引根本不参与这件事；真正覆盖
   这几列的只有复合外键，而它分不出「另一条合法的修订」。
2. **`(OSError, SQLAlchemyError)` 缺口在 6 个仓储 17 处 try 仍存在**，仍是 `H8-23`，
   与 T1～T3 记录一致，本 task 未扩大也未缩小；本文件自己那两处 `try` 都有
   `except Exception` 尾巴，属 T3 修复轮 Q5 记的「文档性冗余」那一端。
3. **`scripts/check_user_facing_branding.py` 在本分支 exit=1**，报的是
   `frontend/src/app/WorkbenchShell.tsx` 少了「独立于视频制作」「视频剪辑」两句文案。
   **与 T4 无关**：本 task 的改动全部在 `backend/` 下，`git diff HEAD -- frontend/` 为空，
   该失败在 HEAD 上就存在。留给前端工作线。
4. **T3 的 `assert constraints >= EXPECTED_CONSTRAINTS` 是单向的**——能抓「本文件期望而
   库里没有」，抓不到「库里有而 `schema.py` 没听说过」，而后者正是 autogenerate 漂移。
   T4 自己那条已改成按 `contype in ('p','f','u','c')` 过滤后**等值**比对；`timelines`
   那条留给拥有该表的任务改，已在该行加注说明，不在本轮跨任务动它。
5. **`scripts/check_acceptance_evidence_depth.py` 在本分支不存在**，与
   `test_shipped_package_boundary.py` 同一情况（台账 LE-05 行已登记它由未合并分支引入）。
   本文件开头的 `用户可操作：否 / 证据类型：分层实现` 声明按该门禁的格式写好，
   等那条分支合并后可直接被扫到。

## T4 修复轮：质量审查 1 Critical + 1 Important + spec 2 条 + 阈下 2 条

首轮的 spec 审查通过（三处裁决全背书、数据丢失路径被独立重演、M18 的区分度被复现）；
质量审查报了 1 Critical + 1 Important，**两条我都在真实 PG 上复现了，复审者是对的**。

### F1（Critical）双谓词只关了终态那一半，「合法边 + 陈旧快照」仍能穿过

**复现（改之前，真实 PostgreSQL，一次性 RED 文件跑完即删）：**

```text
FAILED tests/integration/.../test_a_stale_queued_snapshot_can_fail_a_job_that_is_already_running
E   Failed: DID NOT RAISE EditingJobStale
```

链路，每一步都走正规方法：行是 `QUEUED`，调度侧读到它；另一实例把它推到 `RUNNING`；
调度侧**仍持 QUEUED 快照**，调 `fail(INVALID_TIMELINE)`——`QUEUED → FAILED` 是合法边，
域对象照单全收；`RUNNING ∈ pred(FAILED)`，前驱谓词放行；时间戳更晚，时间谓词放行。
**落库。** 随后真正在渲的 worker 调 `succeed(artifact)` 被判 Stale。
终态：mp4 已出、行 `failed`、`output_artifact_id` 为 NULL 成孤儿——
**正是首轮 docstring 声称防住的那个结果。**

**根因**（复审者的诊断，我核实成立）：`updated_at <` 不是乐观并发令牌。活调用方的新时间戳
是 `now()`，永远晚于库里的值，**所以它对每个调用方都放行**，只拒重放与时钟倒退。
而前驱集谓词问的是「从行现在的状态出发这一步合法吗」，**「陈旧但合法」正是它回答「是」
的那一类**。逐边枚举，前驱集 ≥ 2 的目标全部开着口：

| 快照 | 行实际状态 | 目标 | 危害 |
| --- | --- | --- | --- |
| QUEUED | RUNNING | FAILED | **数据丢失**：已出片被标失败、产物成孤儿（复审者复现的那条） |
| QUEUED | CANCELLING | FAILED | 同型，稍轻 |
| RUNNING | CANCELLING | FAILED | 良性（失败一个正在取消的作业本就合法） |
| QUEUED | RUNNING | CANCELLING | 良性（取消一个在跑的作业就是调用方的本意） |
| RUNNING | CANCELLING | SUCCEEDED | 良性（协作式取消，跑完就是成功） |

pred 只有一个元素的目标（`RUNNING`←{QUEUED}、`CANCELLED`←{CANCELLING}）与终态没有口子。

**裁定照复审者的要求做真 CAS，不走登记。** 现在的谓词是
`WHERE job_id = ? AND status = 快照.status AND updated_at = 快照.updated_at`。

**签名取 `update(previous, changed)`（双参）。** 理由：CAS 需要「读到的那个版本」，
而域对象不带装载时间戳。另两条路都更差——让 `EditingJob` 携带装载时间戳是把持久化关注点
塞进 LE-04 的域对象（还得改另一个 task 的文件）；让 `get` 返回版本令牌是更大的 API 改动。
双参把「比对基线」摆在每个调用点上，这正是要的：**拿不出自己读到的版本的调用方，
本来就不该写。**

**版本是 `(status, updated_at)` 成对，不是只有时间戳。** 这一条是我自己推的、并补了用例：
域允许 `changed.updated_at == previous.updated_at`（`_moved_to` 只拒**倒退**），
所以一个作业的两个相邻状态可以共享同一瞬间；那时时间戳单独已经标识不了版本——行动了，
被比对的那一列没动。`test_the_version_is_the_status_and_the_timestamp_together` 把它钉住，
M25 杀。**这不是微秒级竞态**：一个为一批转换算一个时刻的调用方每次都产生这个形状。

**前驱集谓词：既不留在 SQL 也不删掉，移进 Python 守卫。** 复审者给的是「留或删二选一」，
我选了第三种，依据是**两种答案对调用方后果不同**：一个不构成合法边的 `(previous, changed)`
对，意味着调用方把两者从不同东西上算出来了，**重读永远修不好**——而 `Stale` 的全部含义
就是「重读再决定」，会把调用方送进死循环。所以它答 `EditingJobDataRejected`。
移出 SQL 不丢任何保护，因为 CAS 已经把行的 status 钉在 `previous` 上了。
连带好处：「把作业推回队列」从 `Stale` 变成 `DataRejected`，也是更准的答案。
M27 杀守卫本身，`test_a_pair_that_is_not_a_legal_transition_is_a_caller_error` 七条覆盖
（含 `a-state-to-itself`——图里没有自环，重放不是一次转换）。

**三处过强文字已就地改准**：`update` 的 docstring（现在写明首轮那条比较为什么不是并发
令牌，并把复现链路留在里面）、`EditingJobStale` 的 docstring（现在写明它**不**表示
「转换非法」，也**不**承诺行现在是什么）、台账失败矩阵两行。首轮那两节
（「第二条谓词」「为什么写全列」）加了推翻标记并保留原文作为过程记录。

### F2（Important）「部分索引拦得住 update 改指占用槽位」是错的

复审者说得对，我复现了：`_SOURCE_STATUSES[QUEUED] = ()` 恒假 ⇒ update 永远写不出
queued 行 ⇒ **它绝不参与槽位竞争**。作业改指到被占修订照样落库，占位者无损。
**真实安全网只有复合外键一张，而它分不出「另一条合法的修订」。**
两处文字（遗留 1 与 `_column_values` 的 docstring）已改准。

### 第三条路（spec 2）：身份列/可变列具名划分，一并做了

spec 说「不是二选一，存在第三条路」，且提醒 `created_at` 同样在全列里而登记只提了改指。
**做了**：`_IDENTITY_COLUMNS`（含 `created_at`）与 `_MUTABLE_COLUMNS` 具名，
`update` 只写后者，于是身份列与 `created_at` **结构上写一次**。首轮选全列的唯一理由
（防「插入存了、更新静默丢」）由一条断言保住：两个集合必须**同时**划分
`_column_values` 的键集**和表自己的列名**——新增字段不分类就红，比「全写一遍」更强。
遗留 1 因此关闭而不是改写。

### 两条阈下

1. `constraints >= EXPECTED_CONSTRAINTS` 是单向的（漏「库有而 schema 没有」，正是
   autogenerate 漂移的方向）。T4 自己那条改成按 `contype in ('p','f','u','c')` 过滤后
   **等值**比对——加过滤是为了不让 PostgreSQL 新版本的内部 not-null 条目（`contype = 'n'`）
   把断言变成随服务器版本浮动。T3 同型那条加注登记，见遗留 4；
2. 原 `test_the_statuses_...` 头两条断言是实现复写——该测试随 `_SOURCE_STATUSES` 一起
   移除，问题自然消失，替代它的是七条行为用例。

### 修复轮的两条 SURVIVED，都是「声明写在 docstring 里而没有用例」

新设计的整套变异首跑有两条存活，处置与首轮 M18 同型：

1. **M29（update 又写回全列）SURVIVED。** 我的划分断言只看 `_mutable_values` 本身，
   而 M29 改的是 `update` 里的**调用点**；行为上也无差别——每个 `changed` 都是从匹配该行的
   `previous` 派生的，身份列写回去是同值。补
   `test_an_update_cannot_move_a_job_to_another_timeline_revision`：`previous` 是真版本
   （CAS 必须匹配、写必须真发生），`changed` 手工构造成指向另一条**同样合法**的修订并带
   不同的 `created_at`，断言行的身份列与 `created_at` 一个都没动。转 KILLED；
2. **M12（update 丢掉 `job_id` 过滤）首轮 KILLED，改 CAS 后 SURVIVED。** 首轮杀它的是
   「邻居作业」，而 CAS 之后 WHERE 的另两列就是版本，**邻居必须与被测行同版本才可能撞上**。
   原邻居只是 `RUNNING`（状态和时间戳都不同），于是永不匹配。改成同 `status`、同
   `updated_at`、挂在另一条修订上的邻居后转 KILLED。**这条值得记：换了并发模型，
   原来有效的夹具会静默失效，而测试全绿。**

### 修复轮的方法论收获：变异脚本自己有个还原 bug

`apply_edits` 对同一文件做两处替换时，`saved` 会记两条（第二条是**第一处改完之后**的内容），
还原时按顺序写回，**最后一次写的是中间态——克隆留在半变异状态**，之后每条变异都在污染的
副本上跑。已修成「每个路径只记第一次的内容」。

**据实说明它的触发状态：这个 bug 从未被触发过，是读代码时发现的前瞻修复。**
交付的脚本里没有任何一条变异对同一文件做多处替换——两处替换的 S1/S4/S5 都跨
`schema.py` 与迁移两个文件，M23 是单条替换。（本文件先前写成「本轮 M23 需要同文件多处时
才暴露出来」，与交付脚本不符：当时确实先写了一个双 edit 版本的 M23、因此看出这个缺陷，
但最终交付的是单 edit 版本。按 §2.1 就地改准，不推迟。）

**为什么它躲过了检查：工作树 sha256 比的是工作树，不是克隆。** 半还原的副本不会让那条
断言变红。按复审建议已在 `main()` 末尾补上克隆侧源文件的 sha 比对与 `DIRTY IN CLONE`
提示——这类「检查对象选错了」的缺陷只有把被污染的那个对象纳入比对才发现得了。

### 修复轮的变异

在重建的隔离克隆里跑整套（不是只跑新增的），两道闸照旧（`automation_tool.__file__` 落在
克隆内；克隆变异前先绿：`212 passed` / `47 passed`）。结果已就地合并进上面 T4 的变异表。
锚点唯一性预检本轮又拦下两次（M23 与 CANARY-2 的锚点随重写失效，报 `matched 0 times`
而不是静默不改），另加 S7/S8 首轮已记的两次。

### 修复轮门禁

数字已就地更新进上面 T4 的门禁表（同一个数字不在两处出现）。本轮相对首轮的变化：
集成 39 → **47** 条（新增陈旧快照逐边 5 条、CAS 版本对 1 条、复审链路 1 条、身份列写一次
1 条，边界那条改了形状不计入新增）；单元 78 → **86** 条（转换守卫 7 条参数化 + 两参同作业
1 条 + 划分断言 1 条，减去随 `_SOURCE_STATUSES` 移除的 1 条）；四文件集成 115 → **123**；
全量单元 3732 → **3740**；覆盖 Stmts 124 → **125**；`ruff` 与 `mypy` 均维持 0 新增
（`mypy` 仍是 17 errors / 8 files 基线）。

### 给 LE-12 / LE-06 的交接（修复轮更新）

- **`update` 是双参 CAS。** 调度器、worker、对账遍历都必须把 `get` 出来的那个对象留着当
  `previous`，不能只留转换后的结果。拿不到 `previous` 就说明那条路径没有真正读过库；
- **三种答案对应三个动作**：`Stale` = 重读再决定（HTTP 409）；`NotFound` = 作业不在了
  （404）；`DataRejected` = 服务端仓储调用缺陷，重试无用（500；HTTP 客户端坏输入应在
  进仓储前由请求/域校验答 422）。**`Stale` 不可与 `NotFound`
  合并**，理由写在 `EditingJobStale` 的 docstring 里（调用方 `import` 那个名字时必然看到，
  按 T2 收口轮「约束搬家」的规矩，不复述在本文件）；
- **`CANCELLING` 卡住不会自愈。** `EditingJob` 的 docstring 指定由对账遍历把被遗弃的
  `CANCELLING` 收到 `FAILED` + `WORKER_LOST`；现在它必须带 `previous` 来做，
  而如果 worker 已经写了终态，它会拿到 `Stale`——**这就是正确行为**，F1 的整条链路就是
  它在没有 CAS 时会造成的损失；
- **重放不需要仓储帮忙。** 第一次写入之后 CAS 自然不再匹配，所以 LE-12 不必自己记
  「这条更新发过了」；
- **身份列与 `created_at` 改不动。** 要把一条时间轴修订换成另一条，只能新建作业
  （`save`），这也符合「re-running it is a new job」。

## T4 确认复审轮：2 Important + 1 条顺手，T4 就此收口

确认复审把 CAS 全部坐实——**复审者用 BFS 从域图独立推导了全部 stale-but-legal 三元组，
与 `STALE_BUT_LEGAL` 逐条相等（5 条不多不少）**；守卫答 `DataRejected` 的隔离实测、列集
划分的两向区分度、M12 的重设计、canary 论据未被还原 bug 污染，均复核通过。剩两条 ❌。

### C1（Important）CAS 把「时钟不许倒退」丢了，而 docstring 说丢的东西无价值

**复审者是对的，两层各复现一次。** 首轮 docstring 写旧谓词
「rejected replays and clocks running backwards, **and nothing else**」——那句话把丢掉的
东西说成不值钱的。逐项核对：

- **重放**被 CAS 重新盖住了（复审者核过，我也核过）：版本对在第一次写入后就不复存在，
  而图里无环，同一个 `(status, updated_at)` 不会再出现；
- **时钟倒退现在没人管**。CAS 只把库里的值与 `previous` 比**相等**，
  **对正在写入的那个值一句话都没说**。

RED（改之前）：

```text
# 单元层
E   Failed: DID NOT RAISE EditingJobDataRejected
# 集成层，真实 PG
FAILED ...::test_an_update_cannot_walk_a_rows_timestamp_backwards
```

手工构造一个 `updated_at` 早 80 秒的 `changed`（仍晚于 `created_at`，所以域照样造得出来），
**写入落库、行的时间戳往回走**。

可达性只在手工构造——正规路径 `_moved_to` 拒倒退。**但这不是留着它的理由**：本轮刚用
身份列划分从结构上关掉同一类手工构造误用（改指），同时静默开着另一个说不通；而且
LE-06/LE-12 会按 `updated_at` 排序与分页，**一行走回头路不会在任何地方报错，只会安静地
排到错的位置**——首轮交接对此一字未提。

修法：往已有守卫加一句 `or changed.updated_at < previous.updated_at`。
**必须 `<` 不能 `<=`**：相等是承重的接受条件（域允许相邻两状态共享一瞬间，
`test_the_version_is_the_status_and_the_timestamp_together` 正靠它）。M32 把它放宽成 `<=`
当场被那条用例杀掉，M33 把方向写反也被杀。`and nothing else` 已改准，现在两项各自
交代去处。

复审者说已逐条查过现有 47 + 86 条全兼容（每条 `changed` 都是域方法派生的）；
**我自己实测了没照抄**——加守卫后两层各跑一次，无一条既有用例变红。

### C2（Important）三处「读起来是当前状态」的过期陈述

复审点名三处，并建议全文 grep 复核。**grep 了 `前驱集`／`两条谓词`／`写全列`／
`updated_at <`／`第二条谓词`／`状态谓词` 六个词，逐条判过**，结果：

| 位置 | 判定 |
| --- | --- |
| `:1701`「与计划不同的四处」第四条 | **改**——这份清单正是后来人查「实现与计划差在哪」的入口，首轮却毫无标记。已重写成「并发控制改过两轮」，写明它是清单里唯一改过两轮的一条，并指向修复轮 F1 |
| `:1469`「空前驱集不需要特判」 | **注明**——第三方事实（`in_([])` 恒假）仍成立且值得留，但这个用途没有了：现在文件里没有任何 `in_()` 谓词 |
| `:1255` T3 交接第 1 条 | **加横幅**——同块紧邻的第 2 条首轮反倒加了横幅，这条漏了。已写明「不要先读后比」成立、`WHERE updated_at < :new` 不成立，且「相等算拒绝」的含义反转成「相等是承重的接受条件」 |
| `:895` T2 交接给 T3/T4 的通用检查 | **加横幅**（复审未点名，grep 查出）——判据（检查须发生在数据库一次只看一条语句时）成立且正是最终做法的依据，但括号里的具体谓词不成立 |
| `:505` T2 自己的 `写全列` | **不改，本来就对**——那是 T2 对**自己**代码的一条变异描述。**而且它是我该早点读到的先例**：T2 当时就记过「写全列照样绿，因为传进去的对象其余字段本来就和库里那行一致」，并用一条「探针事实」用例解决。修复轮的 M29 存活是同一件事的复发，先例就在同一份文件里，我没去用 |
| `:1471`／`:1510` 两节、`:1645` 起变异表、`:1763` 遗留 1、`:1806` 起修复轮 | 已在横幅或已就地改准，无需再动 |

### 顺手一条：变异脚本只比工作树，不比克隆

复审建议在 `main()` 末尾加克隆侧源文件 sha 比对。**已加**（含 `DIRTY IN CLONE` 提示），
本轮四批全部输出 `clone matches worktree: True`。理由值得记：还原 bug 当初正是这样躲过
检查的——**被污染的是克隆，而断言比的是工作树**。这类缺陷不是「断言写少了」，
是**检查对象选错了**，只有把可能变脏的那个对象纳入比对才发现得了。

同时按复审要求把「M23 需同文件多处才暴露」改成据实（那个 bug 从未被触发，是读代码发现的
前瞻修复；交付脚本里没有任何一条变异对同一文件多处替换）——见上面「方法论」一节。

### 本轮门禁

数字已就地更新进上面 T4 的门禁表。相对修复轮：集成 47 → **48**、单元 86 → **87**、
四文件集成 123 → **124**、全量单元 3740 → **3741**；**覆盖率实测未变**——
Stmts 125 / Branch 26 / Miss 0 / BrPart 0，仍 100%，因为新守卫是加在既有 `if` 的布尔表达式
里，不产生新语句也不产生新分支点（这一条是实测，不是推算）。`ruff` 0、`mypy` 仍 17 errors /
8 files 基线、计数脚本 0。变异 38 条全 KILLED、2 canary SURVIVED，脚本最终 sha256 见
「变异自证」。

**T4 至此收口。** T5（收口任务）由协调方另派，本 task 不开。

## T5 收口：本线到此为止

T5 不写生产代码。它做四件事：跑一次不挑子集的 final gate、按 §2.1 判台账状态、把
T1～T4 散在各处的遗留与交接收成一处、以及把「读起来像当前状态而其实已经过期」的文字
清掉。

**数字纪律说明**：下面那张表里的每个读数都是**本轮自己跑出来的**，出处就是 T5。
其中两条（全量单元的条数、四文件同批的条数）与 T4 门禁表的读数相同——那是两次独立
测量得到同一个结果，记下来正是为了显示它们一致，不是把同一个数字抄了两遍；**T1～T4
各自的历史读数仍然只住在它们自己那张门禁表里，本节一条都不搬过来**。

### final gate（T5 实测，2026-07-30）

| 命令 | exit | 读数 |
| --- | --- | --- |
| `pytest tests/integration -q`（**整目录，72 个测试模块**） | 0 | **445 passed，5 skipped，441.67s** |
| `pytest` T1～T4 四个集成文件同批 | 0 | 124 passed，80.51s |
| `pytest tests/unit -q` | 0 | 3741 passed，2 skipped，46.66s |
| 五模块单元层分支覆盖（四仓储 + `hydration.py`） | — | **100%：Stmts 363 / Miss 0 / Branch 88 / BrPart 0** |
| `ruff format --check`（LE-05 全线改动的 25 个 Python 文件） | 0 | 25 files already formatted |
| `ruff check`（同 25 个文件） | 0 | All checks passed |
| `mypy`（全量，house 配置） | 1 | 既有基线，见下 |
| `scripts/check_local_editing_roadmap_counts.py` | 0 | 改台账后重跑 |
| `scripts/check_demo_roadmap_counts.py` | 0 | 9 张状态表，135 个唯一任务号 |
| `scripts/check_embedded_browser_video_roadmap.py` | 0 | |
| `scripts/check_third_party_sources.py` | 0 | |
| `scripts/check_user_facing_branding.py` | **1** | 既有前端失败，见遗留 5 |

**整目录才是信号，这一条是 T1 就登记的结构性理由，不是某一次的红**：
`tests/integration/conftest.py` 的 `postgresql_url` 是 `scope="session"` 的单个实例，
而大量用例在开头 `delete()` 共享表，同一个库、跨文件互删——换一个子集组合就换一套前置
数据，所以子集全绿不构成回归证据。**这是 LE-05 全线第一次真的跑完整个目录**：T1～T4
的门禁表里最宽的一条也只到「四个集成文件同批」。5 条 skip 全部是显式的可见浏览器与
真实账号验收（`test_douyin_qr_login_browser` 两条、`test_douyin_session_browser`、
`test_embedded_browser_lifecycle`、`test_packaged_browser_probe`），按 CLAUDE.md §8
「常规本机全量门禁不得隐式触发可见浏览器」本就该跳过，不是本线的缺口。

**不跑全 App**（用户限定）。全 App final gate 属于合并回 `main` 时的动作，本 task 不做，
合并时机也不由本 task 决定。

**与 T4 读数的一致性**：全量单元的条数逐字相同（耗时 46.66s vs 46.45s）；四文件同批的
条数逐字相同（80.51s vs 82.74s）。两次都是重新跑出来的，不是抄的。

`mypy` 仍是那条既有基线，**但这轮加了一项之前四轮都没做的核对**——把报错的 8 个文件与
LE-05 改动的 25 个文件**取交集，为空**（8 个全在 `executor/` 下与 `tests/integration/
conftest.py`、`tests/unit/test_integration_alembic_command.py`）。此前「LE-05 新增 0」
靠的是「总数没变」，那只是**必要条件**：本线加一个错、别处恰好少一个错，总数一样。
交集为空是直接证据。

五模块覆盖是**新的量法**，与 T1～T4 那四条不可比：那四条各自量的是「本 task 的两三个新
模块」，含各自的 `application/*.py` 失败词汇模块，且 `hydration.py` 被 T2 那条算过一次。
本轮量的是四个仓储 + `hydration.py` 这五个文件，逐文件都是 100%：
`editing_job_repository` 105/26、`timeline_repository` 106/26、`material_repository`
90/22、`editing_project_repository` 49/8、`hydration` 13/6（Stmts/Branch）。
**只跑 `tests/unit`，没借集成**。

### alembic head 核对（T5 实测，无改动）

复审代核过，本轮自己跑了一遍确认：`ScriptDirectory.get_heads()` 交回**单头
`20260729_0039`**；全图 39 个 revision、单一 base `20260718_0001`、**没有任何分叉点
（子数 > 1）也没有 merge revision（`down_revision` 为 tuple）**，即整条链线性。

计划原文要求「`alembic_head.py` 对齐 `0039`」，**那句已由 T1 实测推翻并从计划里删除**：
该文件不写死版本号，它从已提交的迁移脚本解析并在头数 ≠ 1 时报错。所以 T5 这里没有任何
文件要改，只有读数入档。

### 四表四仓储的最终形状

| 表 | 迁移 | 列 | 数据库侧结构 |
| --- | --- | --- | --- |
| `editing_projects` | `0036` | 10 | `pk_editing_projects (project_id)` |
| `materials` | `0037` | 16 | `pk_materials (material_id)`；`uq_materials_content_digest (content_digest)` |
| `timelines` | `0038` | 6 | `pk_timelines (timeline_id, revision)`——修订不可覆写由主键挡住；超键 `uq_timelines_revision_project (timeline_id, revision, project_id)`，它存在的唯一目的是给 `editing_jobs` 的复合外键当靶子；`fk_timelines_project` |
| `editing_jobs` | `0039` | 9 | `pk_editing_jobs (job_id)`；复合外键 `fk_editing_jobs_timeline_revision (timeline_id, timeline_revision, project_id)` → `timelines (timeline_id, revision, project_id)`；**部分**唯一索引 `uq_editing_jobs_queued_timeline_revision (timeline_id, timeline_revision) WHERE status = 'queued'` |

四个仓储的公开方法：`EditingProject` 是 `save`/`get`；`Material` 是
`save`/`get`/`find_by_digest`/`update_description`；`Timeline` 是
`save`/`get`/`latest_revision`；`EditingJob` 是 `save`/`update`/`get`。
**只有两张表可变**（`materials` 的描述、`editing_jobs` 的状态），也只有这两个仓储有写回
方法，而两者的写回都做成了 UPDATE 谓词而不是「先读后判」。

LE-04 交下来的三条跨聚合根不变式**全部由库结构挡住，没有一条靠应用层检查**：
① 项目与时间轴归属的三角 → 复合外键；② 修订必须真实存在 → 同一条复合外键；
③ 同修订不得有两个 QUEUED → 部分唯一索引。三条各有裸 INSERT 违例注入证明拦截点在
数据库一侧，证据在 T4「三条不变式的违例注入证据」。

### 遗留汇总（逐条给 owner）

1. **`(OSError, SQLAlchemyError)` 缺口，17 处 try / 6 个仓储** —— owner
   **`docs/development-roadmap.md` 的 `H8-23`，依赖 H8-11**。LE-05 四轮均未扩大也未缩小。
   T5 用 AST 重新自查了一次（不是转述 T1）：`account_device` 2、`account_session` 7、
   `customer_account` 4、`task_discovery` 2、`task_target_result` 1、`workbench_metrics` 1，
   与 T1 及 roadmap 该行记录的范围逐条一致。
   **建议与另两件并成一个任务**：T1 遗留 4（`session.py:53` 的 `TimeoutError` 是 `OSError`
   子类，冗余）与 T3 修复轮 Q5（本线两处 `try` 有 `except Exception` 尾巴，是**方向相反**的
   同一件事——那 17 处**缺**尾巴，这两处**多**一层文档性冗余）。三者分开做会各自碰同一批文件。
2. **`schema.py` 声明面的门禁覆盖不齐** —— owner **独立任务**（非本线引入，全库同此）。
   T5 实测当前状态：**列类型 + nullable**（`EXPECTED_TABLE_TYPES`）覆盖 `materials`、
   `timelines`、`editing_jobs` 三张，`editing_projects` 仍只比列名与 varchar 宽度；
   **约束层做等值比对的只有 `editing_jobs` 一张**（按 `contype in ('p','f','u','c')`
   过滤后 `==`），其余三张是 `>= EXPECTED_CONSTRAINTS` 的**单向**断言——能抓「本文件期望
   而库里没有」，抓不到「库里有而 `schema.py` 没听说过」，**而后者正是 autogenerate 漂移**。
   `timelines` 那条已在该行就地加注说明，归拥有该表的任务收紧；LE-05 没有跨任务去动它。
3. **`Timeline.revision` 的域上界缺失，与 `integer` 列不匹配** —— owner **需动 LE-03 的域
   对象，或在仓储按 SQLSTATE `22000` 分流**，两者都超出 LE-05 范围。现状是越界一格答成
   `Unavailable`，即告诉调用方去重试一件不可能成功的事；触发需要二十一亿个修订。
   两端各有用例，越界那条的 docstring 写明「它红了就该删这条遗留」。
4. **`Material` AST 守卫的三条静态盲区** —— **没有 owner，这是设计边界不是待办**。
   三条（运行期取名、解包/容器绑定、无导入且变量名无关的 `replace`）各有一条用例断言
   「当前不报」，哪条红了就说明检查变强了、去同步删盲区清单。第一、三条静态无解；
   第二条可补但不值得。守卫的定位写在测试文件的模块 docstring 里：**它让正规路径成为
   省事的那条、把无意的绕过变成一条红测试，它不是拦住存心绕过的人的屏障**。
5. **`scripts/check_user_facing_branding.py` 在本分支 exit=1** —— owner **前端工作线**
   （台账 §7 已登记）。报的是 `frontend/src/app/WorkbenchShell.tsx` 少了「独立于视频制作」
   与「视频剪辑」两句文案。**与 LE-05 无关，T5 已复核**：本线 12 个提交的改动文件里
   `frontend/` 下一个都没有，该失败在分出点上就存在。
6. **`scripts/check_acceptance_evidence_depth.py` 与
   `backend/tests/unit/executor/test_shipped_package_boundary.py` 在本分支不存在** ——
   owner **等 `feature-audit` / `pc21-b` 合并**（台账 LE-05 行已登记它们由未合并分支引入）。
   本文件开头的 `用户可操作：否 / 证据类型：分层实现` 已按该门禁的格式写好，那条分支合并后
   可直接被扫到，不需要再补。

T4 遗留 1（`update` 写全列能把作业改指到另一条修订）**已在 T4 修复轮关闭**，不进本汇总。

### 给 LE-06 的交接（REST 面）

**失败到 HTTP 的映射。** 每个模块一个私有基类 `_XxxPersistenceFailure` + 若干子类，
**子类数逐表不同：`editing_projects` 4、`materials` 5、`timelines` 5、`editing_jobs` 7**
（T5 实测数的类定义；每个模块的四轮门禁表里记的「基类 + 子类」拆法与此一致）。
**别按前缀猜名字**——`timelines` 那两个专有失败既不叫 `AlreadyRegistered` 也不叫
`RevisionMissing`：

| 应用层失败 | HTTP | 语义 |
| --- | --- | --- |
| `EditingProjectAlreadyRegistered` / `MaterialAlreadyRegistered` / `EditingJobAlreadyRegistered` | **409** | 主键或唯一列被已有的行占了，**不是 upsert**（事后比对整行证明过） |
| `TimelineRevisionAlreadyStored` | **409** | 有意不叫 "already registered"：**被占的是一个修订，不是这条时间轴**。存修订 4 而修订 3 已在是正常情况，不是冲突 |
| `EditingJobRevisionAlreadyQueued` | **409** | 同 `(timeline_id, revision)` 已有一个 QUEUED；由**部分**唯一索引拒，与「重复 job_id」撞同一个 SQLSTATE `23505`，靠 asyncpg 的 `constraint_name` 结构化字段分开 |
| `TimelineProjectMissing` | **409**（不是 404） | 时间轴本身合法，但它指的项目没落库。**重试同一条永远不成功，先存项目就成功** |
| `EditingJobTimelineRevisionMissing` | **409**（不是 404） | 同上一类：拒的是**引用**。①②两条不变式合成这一个，因为复合外键被拒时 PostgreSQL 不说是哪一半；补齐引用后同一条数据落库成功，T4 有用例证明拒的是引用本身 |
| `...NotFound`（四个模块各一） | **404** | 要读/要写的那一行不在了 |
| `EditingJobStale` | **409** | 你手里的版本不是库里那一行，**重读再决定** |
| `MaterialDescriptionProtected` | **409** | 用户已经写过这条描述，AI 不许盖掉 |
| `...DataRejected`（四个模块各一） | **500** | 仓储调用方写错类型/状态或撞上未识别约束，是服务端缺陷；客户端请求/域校验应在进仓储前答 422 |
| `InvalidEditingProjectModel` / `InvalidMaterialModel` / `InvalidTimelineModel` / `InvalidEditingJobModel` | **500** | 坏的既有数据库行在水合时被域拒绝；这些异常有意不翻译成 `DataRejected`，公开面只给固定内部错误信封，不反射存量值 |
| `...PersistenceUnavailable`（四个模块各一） | **503** | 库不可达 / 认证失败 / 权限不足 / 连接数上限 |

这里的「调用方」是**仓储在服务端内部的调用方**，不是 HTTP 客户端。一个通过 Pydantic 与
域构造的合法请求仍然撞到 `DataRejected`，说明 REST 装配、仓储调用或数据库约束版本至少
有一处不一致；答 422 会把服务端缺陷错推给用户。反过来，非法请求根本不该走到仓储，
由请求/域校验直接答 422。这一口径由 LE-06 T1 的两组 RED 分开守。

**`Stale` 与 `NotFound` 不许合并**，理由写在 `EditingJobStale` 的 docstring 里
（按 T2 收口轮「约束搬家」的规矩，约束写在调用方 `import` 时必然看到的地方，本文件不复述）。
塌成 `NotFound` 会告诉调用方一个**正在跑的作业不存在**，这正是不安全的那个方向。

**只有 `EditingJob.update` 是双参 CAS，签名 `update(previous, changed)`。** Timeline
修订是 write-once，只有 `save/get/latest_revision`，没有 `update` 或 `Stale`。LE-06 负责
把全部失败词汇映射完整，但首期任务 API 只提交与查询，不新增任意状态 PATCH；真正消费 CAS
的是 LE-12 的 Worker 状态写回。任何消费路径都必须把 `get` 出来的那个对象留着当
`previous`；拿不出 `previous` 就说明这条路径没有真正读过库。完整依据与逐边表在「T4
修复轮 F1」，不在这里重述。**另外两件上层会直接碰到的**：

- **`update` 不写身份列。** 要把作业换到另一条时间轴修订，只能 `save` 一个新作业——
  这也符合「重跑就是一个新作业」。所以 REST 不要提供「改 job 的 timeline」这种端点；
- **`update_description` 的「没抛异常」不等于「写进去了」。** 快照新鲜且描述已归用户时，
  `with_ai_description` 直接返回 `self`，UPDATE 不挂谓词、命中，**静默成功而内容没变**。
  要判断是否真的写入，比对写回后的内容或看 `description_source`。这条同样是给 LE-13 的。

**分页与排序的坑（LE-06 会第一个碰到）**：`updated_at` 现在有守卫挡住倒退
（T4 确认复审 C1 加的），所以可以拿它排序；但它**不唯一**——域允许相邻两状态共享同一
瞬间，而「相等」是 CAS 承重的接受条件。按 `updated_at` 排序必须再带一个 tiebreaker，
否则同一瞬间的两行在分页边界上会重复或漏掉。

### 给 LE-12 / LE-13 的交接

**LE-12（Worker 生命周期）**：交接内容已写在「给 LE-12 / LE-06 的交接（修复轮更新）」
一节，五条，不在这里重复。其中最容易做错的一条再点一次：**`CANCELLING` 卡住不会自愈**，
对账遍历要把被遗弃的 `CANCELLING` 收成 `FAILED` + `WORKER_LOST`，而如果 worker 已经写了
终态，它会拿到 `Stale`——**那就是正确行为**，不要把它当错误重试。

**LE-13（素材理解）**：接口约束**不在本文件**，在
`application/materials.py` 的 `MaterialDescriptionProtected` docstring 里，两条
（陈旧快照会抛 `Protected`；新鲜快照下用户已接管则静默返回 `self`）。放那里是有意的：
CLAUDE.md §2.1 明令会话恢复只读当前任务文件，**LE-13 的会话不会打开 `LE-05.md`**，
约束写在没人会打开的文件里等于没写。

### 过强文字清扫（判据：这句描述的还是不是现在的代码）

T4 确认复审轮点名三处并建议全文 grep，那一轮已处置五处。**T5 逐条复核了那五处，全部
到位**：「与计划不同的四处」第四条已重写为「改过两轮」并指向 F1；「空前驱集不需要特判」
已注明用途消失；T3 交接第 1 条与 T2 交接的通用检查各有横幅；T2 自己那处 `写全列` 本来
就对（它描述的是 T2 自己的代码）。两处被取代的整节（「第二条谓词」「为什么写全列」）
横幅也在。

`in_()` 与 `_column_values` 两条断言 T5 实测复核：五个模块里**确实没有任何 `in_()`
谓词**；`_column_values` 确实还在，`_mutable_values` 由它派生并按 `_MUTABLE_COLUMNS` 过滤。

本轮**另扫出并改掉五处**，都不在已有横幅覆盖范围内：

| 位置 | 原文问题 | 处置 |
| --- | --- | --- |
| T2 失败矩阵「结果不确定/静默失败」行 | 写「**同事务内**消歧成 NotFound 或 Protected」——把收口轮**已推翻**的那句话的形状留在了这里。源码 docstring 明写「共享事务不让两种答案穷尽」 | **改准**：后续 SELECT 只是**尽力**消歧、可被并发删除抢跑，错的只会是标签 |
| T2「AST 边界测试」整节 | 「守卫查三件事」第 1 条说**精确匹配 `Material` 这个名字**；盲区写**一条**且「没有任何静态手段能抓到」；自证语料 8 段 | **加横幅**并逐项对：现在匹配的是模块**实际绑定到该类的全部本地名**；盲区**三条**，其中第二条是「可补但不值得」而非无解；该文件现为 21 passed（实测） |
| T2 遗留第 2 条 | 写「AST 守卫的**两个**静态盲区」 | **改正为三个**，补上解包/容器绑定那条，并写明它与另两条的区别 |
| T3 修复轮 Q1 与 T3 `mypy` 段 | 都写 `_enumeration_member`（带下划线的私有名），**今天的代码里不存在这个名字** | **加横幅**：T4 出现第二个使用者后上移为公开的 `hydration.enumeration_member`，多一个 `refusal` 参数，两条用例改指新位置、**断言一字未改**；本节「为什么必须钉在函数层」的论证不受影响 |
| 顶部提交列表 T3 那一行 | 记的 `58bd8dc` **这个对象不存在**（两位十六进制转置） | **改为 `58d9bbc`**。发现方式是逐条 `git log -1 <hash>`，报 `fatal: unknown revision` |

最后那条值得单独说一句：**转置后的 hash 看上去完全正常**，只有真去解析才会报错。
所以提交清单不能只写不核——12 条 T5 全部逐条解析过，其余 11 条命中且 subject 与描述相符。

### 变异脚本的克隆侧 sha 比对（T5 核对，无改动）

T4 确认复审轮顺手加的那条（在 `main()` 末尾比对克隆侧源文件 sha）**T5 实地核对了，不是
读文档转述**：脚本还在本会话 scratchpad，`main()` 里确有 `clone_after = digests(CLONE)`
与 `clone matches worktree:` 输出，以及逐文件的 `DIRTY IN CLONE` 提示；脚本 sha256 与
T4「变异自证」记录的值**逐字相同**。所以这条已完成，无需再动。

它防的那类缺陷值得记住形状：还原 bug 当初躲过检查，**不是因为断言写少了，是因为检查
对象选错了**——被污染的是克隆，而断言比的是工作树。

### 台账判定：`🔍 待验收`，不是 `✅ 已完成`

按 CLAUDE.md §2.1 与 §8：LE-05 拿到的全部是**真实 PostgreSQL 的分层证据**，没有任何
用户可操作路径。**四个仓储今天没有任何生产调用方，T5 实测**：`backend/src` 下除了它们
自己的模块，只有 `infrastructure/database/__init__.py` 提到这四个类名，而那是转出口不是
调用。REST 面在 LE-06，正式 App 的用户路径在 LE-17。§8 允许「后端端点在服务端任务完成真实 PostgreSQL 门禁」，但同一段紧接着要求
「最迟必须在消费该端点的桌面任务补齐真实 App 纵向验收」，而 §2.1 明写只通过分层证据的
任务**最多标 `🔍 待验收`**。

所以台账 LE-05 行改为 `🔍 待验收`，补验收依赖 **LE-06 + LE-17** 写进行内；§4 计数同步
（🔍 待验收 +1、在途 −1），`scripts/check_local_editing_roadmap_counts.py` 重跑 0。
§5「当前下一步」改为 LE-06，并在那里点明三件按错会出事的交接（异常映射、双参 CAS、
三个答案各自的动作）。

**这不是把门禁抬高来显得谨慎**：本文件开头的声明就是 `用户可操作：否 / 证据类型：分层
实现`，两处一致。要让 LE-05 变 `✅`，需要的是 LE-17 之后从正式 App 的正常入口跑一遍、
并核对可外部核对的终态，那笔证据按 §9.1 归那个任务。

### 清理

T5 未产生任何一次性探针或临时脚本入库，也没有改动任何生产代码或测试——
本提交只动两份文档（本文件 + 台账）。覆盖率跑留下的 `backend/.coverage` 仍在磁盘上，
由仓库既有 `.gitignore:51` 排除、可随时重新生成，不进提交。提交前
`git status --porcelain --untracked-files=all` 只有那两份文档。

`managed_test_postgres` 负责本次两轮集成（整目录 + 四文件同批）的 compose 实例起停，
每轮各自按 pid 取项目名与空闲端口；收尾 `docker ps` 里没有本会话任何 pid 的
`automation-tool-pytest-<pid>-postgres-test-1`。列表里另有两个同前缀容器
（pid 32564 / 18424，分别已运行 21 与 31 小时），**属其他工作线，本会话开始前就在运行，
未动**——按 CLAUDE.md §8 只清理本次实例创建的资源。

colima `default` profile 本会话开始前已在运行，**未启停**：按 CLAUDE.md §9.3，
`/var/run/docker.sock` 是全局共享入口，停掉自己用过的 profile 会把别的项目挂在那里的
链接一并摘掉。开工前核过 `colima list` 与 `docker context ls`，当前 context 是
`colima`，未另起 profile 争抢那个路径。

**LE-05 至此收口，本线到此为止。** 合并回 `main` 的时机与全 App final gate 由主线与
用户对齐后决定，不由本 task 决定。

## LE-17 补验收（2026-08-01）

上文 `🔍 待验收` 是 LE-05 自身收口时的历史判定。LE-17 已在同一最终提交 `904f2075` 上，
分别从 macOS 与 Windows 正式 App 的正常视频剪辑入口穿过真实 REST 和本仓储：创建 Project、
保存 Timeline、提交 Job，经真实 PostgreSQL 收敛到 `succeeded` 并落下 Artifact；两端 ffprobe
均核对 H.264 `720×1280@20`、20 帧、1 秒。完整 backend integration 同期为
`460 passed, 17 skipped`。该用户可操作纵向证据见 `docs/development/LE-17.md` T6，已满足
本节原登记的完成条件；LE-05 顶格状态同步为 `✅ 已完成`。
