# C10-02 Control Plane Docker

- 状态：✅ 已完成
- 日期：2026-07-23
- 提交：本任务独立提交
- 范围：构建只运行单 worker Control Plane 的锁定 Linux 镜像，固定非 root 身份、只读运行兼容、容器健康检查、30 秒优雅停止和 OCI 版本/源码标签；不包含数据库、TLS 反代、Secret 值或部署动作
- 前置依赖：F1-14 的生产依赖锁与 C10-01 的单实例部署契约已完成
- RED：先新增镜像静态契约和真实容器验收，要求 base image 使用不可变 digest、仅安装 lock 中生产依赖、无测试/浏览器/Secret、非 root、固定 `8000`、`/api/v1/health`、30 秒 stop、只读 rootfs/临时 `/tmp` 可运行，并核对 OCI version/revision 与应用 `/version`；当前 `backend/Dockerfile`、容器入口和专用验收均不存在，测试应准确失败
- GREEN：新增 `backend/Dockerfile`、`.dockerignore` 与固定 `automation-tool-control-plane-container` 入口；Python `3.12.13-slim-bookworm` 和官方 uv 构建器均以 digest 锁定，只按 `uv.lock` 安装非 dev 依赖
- 运行边界：镜像固定 UID/GID `65532:65532`、端口 `8000`、1 worker、关闭 access/server header、30 秒 graceful shutdown、SIGTERM 和 `/api/v1/health` 容器探针；OCI version/revision/source 由构建参数写入
- 真实验收：`scripts/run_c10_02_acceptance.py` 构建真实镜像，在无宿主端口、只读 rootfs、临时 `/tmp`、`no-new-privileges` 和 `cap-drop=ALL` 下连接隔离 PostgreSQL，核对真实 `/health`、`/version`、UID、无 pytest/浏览器下载、写根目录失败、OCI 标签、日志无 DB Secret，并验证 `docker stop --time 30` 退出码为 0
- 失败矩阵：覆盖缺失镜像/入口、构建器下载边界、虚拟环境绝对 shebang 跨阶段失效、健康前退出、运行探针权限、可写 rootfs、意外宿主端口、测试依赖/浏览器资产、标签漂移、日志泄密和优雅停止超时
- 门禁：后端 `2344 passed, 5 skipped`，语句 `15942/15942`、分支 `3670/3670`，总覆盖率 `100%`；Node 契约 `174/174`，Ruff、格式、Mypy、ESLint 与 `uv lock --check` 全绿；真实 Docker 验收通过
- 清理与边界：验收只创建带 `automation-tool-c10-02-*` 唯一名称的临时镜像、网络和容器，不发布端口，结束后精确清理且复查零残留；没有部署云环境、TLS、真实域名或持久数据库，C10-03 继续落地数据库边界
