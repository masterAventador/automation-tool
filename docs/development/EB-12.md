# EB-12 完成证据

> 状态：🔍 待验收（内置 Chromium 上的迁移与既有失败矩阵已通过；真实抖音平台
> 最终状态与 Windows 证据见遗留项）
>
> 日期：2026-07-25
>
> 提交：本文件所在的 EB-12 独立实现提交

## 任务

- 搜索、浏览、候选提取、有界滚动与页面漂移诊断五条链路原样迁移：固定 Playwright
  页面对象和语义零重写，只把启动来源从系统 Chrome 换成摘要校验过的内置 Chromium；
- 现有页面变化与遮罩失败矩阵不降级。

## RED

迁移型任务：五个集成文件仅删除系统 Chrome 常量与 skip 守卫、注入
`staged_embedded_chromium` 夹具（EB-11 落地的生产 staging 管线共享夹具），页面
对象、断言与失败矩阵一行未改。首轮迁移后直接 6/6 GREEN，无先失败步骤——如实
记录，不构造伪 RED；风险由下列既有矩阵回归与逐文件 diff（10 insertions /
40 deletions，全部为启动来源替换）背书。

## GREEN

```text
uv run pytest tests/integration/test_douyin_search_execution_browser.py \
  tests/integration/test_douyin_browse_browser.py \
  tests/integration/test_douyin_candidate_extraction_browser.py \
  tests/integration/test_douyin_bounded_scroll_browser.py \
  tests/integration/test_page_drift_artifact_browser.py        6 passed（真实内置 Chromium）
uv run pytest tests/unit/executor                              822 passed, 1 skipped
uvx ruff check --config pyproject.toml tests/integration/      All checks passed
python3 scripts/check_embedded_browser_video_roadmap.py        通过
```

覆盖链路（全部真实内置 Chromium + 0o700 私有 Profile + 本地夹具页语料，无外呼）：
搜索执行与结果页路由、假 Profile 浏览（不发送）、搜索→候选提取隐私边界、
搜索→有界滚动、正式 discover 命令的页面漂移捕获与人工接管（含遮罩矩阵）。

## 失败矩阵

原样保留（未降级、未修改）：页面改版/漂移捕获与 handoff、遮罩出现、候选提取
隐私边界、浏览不发送边界、滚动上界；相应单测矩阵在 822 项执行器单测中回归。

## 正常用户路径验收

这些链路的正式入口验收（App→Rust→Executor discover/search 命令）由 P 系列既有
桌面 E2E 承载；本任务变更仅测试启动来源，不改产品代码，无新增用户路径。真实
抖音平台的最终状态验证仍需受控账号：`🔍 待真实账号`。

## 真实边界

- 内置 Chromium 与系统 Chrome 行为一致性由同套页面对象在两代浏览器下的测试分别
  证明；
- 真实抖音页面结构漂移由 page-drift 诊断链路与待真实账号项覆盖；
- Windows 同链路证据由并行 Windows 会话补齐。

## 清理

- 全部测试经 `runtime.running` 在成功/失败路径关闭浏览器与进程树；staged
  Chromium 位于 pytest 会话临时目录随测试清理，无残留。

## 文档变化

- 激活专项 Roadmap 的 EB-12 并更新为 `🔍 待验收`、同步计数；
- `docs/development/windows-evidence-checklist.md` 新增 EB-12 待补节。

## 遗留项

- Windows 平台同链路证据已于 2026-07-25 补齐（Mac 经 SSH 远程发起、Windows 真机执行，见 `docs/development/windows-evidence-checklist.md` 对应节）。

- 真实抖音搜索/浏览/候选链路的受控账号最终状态验收：`🔍 待真实账号`；
- Windows 平台同链路证据随并行 Windows 会话补齐；
- 上述闭合前保持 `🔍 待验收`。
