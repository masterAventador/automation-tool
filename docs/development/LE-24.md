# LE-24 深度思考开关与耗时告知

状态：已完成

用户可操作：是

## 边界与拆分

本任务只交付 Roadmap 指定的一个智能剪辑偏好：默认关闭、统一控制素材理解/文案分句/
语义匹配、跨 App 会话持久化，并在开关旁展示由固定素材集实测得出的额外耗时。它不改变
模型选择、生成流程、进度阶段或失败语义。

1. T1：同一个开关值真实到达三处模型请求，已完成；
2. T2：默认关闭、用户偏好持久化和高级选项界面，已完成；
3. T3：固定素材集开/关实测、计算规则与用户可见耗时文案，已完成；
4. T4：正式入口功能验收、文案门禁与 LE 收口，已完成。

## T1 三处模型请求统一透传

状态：已完成

### TDD

RED：先让生成管线的 `prepare` 阶段必须接收与分句、匹配阶段相同的
`enable_thinking`，并让素材理解适配器断言收到开启态。首个专项测试固定失败为
`SmartEditGenerationRejected`：编排器没有向 `prepare` 传该参数。

Green：`SmartEditGenerationPipeline.prepare` 和生产
`LocalSmartEditGenerationPipeline.prepare` 接收严格布尔值；编排器把一次请求的同一个值
依次交给素材理解、文案分句和语义匹配，素材理解构造
`MaterialUnderstandingOptions(enable_thinking=...)`，不再固定使用默认关闭。

### Code Review 与功能验收

首轮 Review 发现新增用例的插入位置把原测试末尾两条结果断言移到了新测试，已恢复原用例
的独立断言后复审，无待修 finding。

- 智能剪辑 generation/pipeline/worker 专项：`36 passed`；
- 三个真实请求体边界分别覆盖开/关：`6 passed`，均断言 JSON 中
  `enable_thinking` 与同一次用户选择完全一致；
- Ruff、格式与 `git diff --check` 全部通过，上述命令退出码均为 0。

## T2 默认关闭与偏好持久化

状态：已完成

### TDD

RED：组件测试先执行“首次进入默认关闭 → 开启 → 卸载并重新挂载工作台仍开启 → 再关闭
→ 再次挂载仍关闭”。修正测试环境缺失 Storage 的夹具问题后，固定失败为找不到
“高级选项”，证明失败来自产品尚未实现而非测试环境。

Green：现有总开关移入可见的“高级选项”；初值只接受本机 `localStorage` 中精确的
`enabled`，缺失、未知值或读取异常全部回到关闭。每次切换保存 `enabled/disabled`；写入
失败不阻断当前会话，本次生成仍使用 React 内存中的用户选择。

### Code Review 与功能验收

Review 核对默认关闭、严格存储值、存储异常和请求时当前值，未发现待修 finding。智能剪辑
组件完整文件 `21 passed`，改动文件 ESLint 与全前端 TypeScript build 均通过，退出码为 0。

## T3 固定素材计时

状态：已完成

### 当前检查点

正式隐藏 App 旅程通过成对环境参数选择深度思考开/关和 `1`～`3` 条固定素材。runner
使用随包 ffmpeg 生成三张确定的测试图，逐条从正式“导入本机素材”按钮进入素材库；每次导入都
以新增素材编号为完成条件，避免复用上一条成功提示。旅程从点击“生成草稿”开始，以正式页面
出现第 1 版 Timeline 为结束，用单调时钟记录毫秒值，并只输出一个无路径、无密钥的
`LE24_MEASUREMENT` JSON 记录。Python 边界严格校验参数配对、素材数、开关值、字段集合、
唯一记录与耗时范围；未提供计时参数时仍走原 LE-19 正常/失败旅程。

### TDD、Code Review 与专项验收

RED 先后固定了缺失计时环境入口、缺失独立协议模块、runner 未接协议，以及多素材导入可能被
旧成功提示提前放行；最后一项失败明确表现为正式 App spec 没有按导入前后素材编号增长等待。
Green 后协议单测 `4 passed`、正式 App 结构验收 `1 passed`，前端 TypeScript、改动面 ESLint、
Python Ruff/格式与 `git diff --check` 均以退出码 0 完成。

逐文件 Review 发现并修复上述多素材计数不可信问题；复审覆盖计时边界、开关断言、参数隔离、
单条机器可读记录、敏感信息检查、受控源文件不变与清理路径，无剩余 actionable finding。
获得用户明确授权后，固定提示词和程序生成的受控测试图被发送给百炼，完成了 18 次真实正式 App 观测。

### 成对采样与判型器检查点

成对 runner 对 `1/2/3` 条素材各做三轮开/关观测，共 18 次正式 App
生成；每一对交替先开或先关，减少固定执行顺序造成的偏差。报告绑定平台和完整 Git SHA，
只记录九对毫秒值、差值、每个素材数的中位差值、判型结果及由结果机械生成的候选文案，
不记录提示词、密钥或本机路径。

判型规则已经测试锁定：每个素材数必须恰好三对有效观测且中位额外耗时为正；三组样本稳定、
相邻增量均为正且相差不超过 50%、线性残差在固定阈值内、截距非负时，展示“固定耗时 +
每条素材耗时”；否则展示三个中位差值覆盖的实测区间。显示到秒时，上界和按条数耗时向上
取整，区间下界向下取整。首轮 Review 发现负截距被归零后会让展示公式脱离拟合结果，已先以
失败用例复现，再改为这种数据直接走区间；复审无剩余 finding。

纯本地专项为 `12 passed`，Ruff、格式与 `git diff --check` 全绿。

### 路线图收口补漏

完整 backend integration 首轮准确捕获一处 LE-24 签名变更后的旧调用：真实 H.264 用例没有
传必填的 `enable_thinking`。现用原失败作 RED，按默认关闭语义显式传 `False`；专项
`1 passed`、同类调用复审无剩余 finding，完整复跑 `472 passed, 17 skipped`，关闭本地回归门禁。

### 真实成对观测结果

报告为 `.local/local-video-editing/le24-measurements/macos-375ca017acd7.json`，绑定完整
Git SHA `375ca017acd7527fb9ab303cc29b15a833e3b57a`，文件大小 `1813 bytes`，SHA-256 为
`2919e4cab6c66218e752985fb529d31f84605ea6e0f3d549af320aab926064a2`。观测只计算点击
“生成草稿”到页面出现 Timeline 的时间，构建、导入和端口 `TIME_WAIT` 等准备时间不计入。
固定提示词仅要求一句旁白，使 1～3 条素材共享同一个业务语义，避免模型自行扩展开场/收尾导致
本来可用的受控素材被低分拒绝。

| 素材数 | 轮次 | 关闭（ms） | 开启（ms） | 额外耗时（ms） |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 1 | 10009 | 53015 | 43006 |
| 1 | 2 | 11008 | 56009 | 45001 |
| 1 | 3 | 11006 | 59008 | 48002 |
| 2 | 1 | 18013 | 75015 | 57002 |
| 2 | 2 | 16008 | 80007 | 63999 |
| 2 | 3 | 14007 | 83010 | 69003 |
| 3 | 1 | 22009 | 76010 | 54001 |
| 3 | 2 | 21009 | 102953 | 81944 |
| 3 | 3 | 21010 | 82008 | 60998 |

1/2/3 条素材的额外耗时中位数分别为 `45001` / `63999` / `60998 ms`。第三组中位数低于
第二组，而且同组样本有明显波动，因此不冒充线性增长，按预定规则判为 `range`。显示下界向下、
上界向上取整后，用户文案确定为：**“开启后预计约多花 45～64 秒。”**

## T4 正式入口与 LE 收口

状态：已完成

### TDD 与修复

实测 runner 先后用 RED 复现并修复了失败时诊断被吞、计时前未重新打开项目、持久化开关被
无条件点击成相反状态、固定提示词产生多句旁白，以及 macOS 端口内核回收竞态。最终按实际
`aria-checked` 只在必要时切换，计时前断言项目已打开，并轮询等待产品端口真正可用。

先写精确文案断言后，UI 在“深度思考”开关旁始终展示实测文案，并独立显示“已开启/
默认关闭”当前状态。组件完整文件 `21 passed`，正式 App 结构验收 `1 passed`，TypeScript build、
改动面 ESLint 与用户文案门禁均通过。

### Code Review 与功能验收

Review 逐项核对了实测报告的 SHA 绑定、九对数据、判型输出、无敏感输出、开关的可访问性、
持久化和窄屏纵向布局，无 actionable finding。最终 UI 提交 `55022dad` 的正式隐藏
Tauri/WebKit App 旅程为 `1 passing (20.1s)`，单素材默认关闭观测为 `10008 ms`，退出码为 0；
旅程后 App 数据、PostgreSQL 容器、Worker 和产品端口均已清理。LE 结束完整 backend integration
最终为 `472 passed, 17 skipped`（`326.19s`），退出码为 0。
