# T77 复核登录态投影与权威状态一致性

> 状态：✅ 否定性复核完成；没有生产代码改动
>
> 日期：2026-07-26
>
> 原提交：`006078cc54117d9a809d2dafce85bacfd78787f6`
>
> 证据补记：第三批事后补录；原任务本身只有复核结论

## 结论

扫描线怀疑权威态 `state: "missing"` 被 UI 错投影成“暂时无法读取”。逐层复核后前提不成立：
`missing` 已稳定映射为“需要登录”；只有 gateway 调用拒绝才显示“暂时无法读取”。用户观察到的
误报与 T50 注销投影预算过短一致，因此本任务不制造重复修复。

## RED

没有新增 RED，也没有生产代码。组件/gateway 6 项与 B5-13/B5-14 契约 3 项在现状下均通过。
这是调查任务的有效否定结论；事后伪造一个失败测试会歪曲事实。

## GREEN

- 组件和 gateway 的 6 项既有测试通过；
- B5-13/B5-14 登录态/安全注销契约 3 项通过；
- 搜索投影逻辑确认：
  - `missing` → 需要登录；
  - gateway 拒绝/不可读 → 暂时无法读取；
  - healthy/risk/expired 等状态保持封闭映射；
- commit 只更新第二批调查结论，没有改 `frontend/src`。

## 失败矩阵

| 场景 | 现有投影 |
| --- | --- |
| 权威态 missing | 需要登录 |
| gateway 查询拒绝 | 暂时无法读取 |
| 登录态失效/二维码过期 | 对应人工处理提示 |
| 风控挑战 | 等待人工处理，不自动绕过 |
| 注销已完成但轮询提前放弃 | 属 T50 预算缺陷，不在此处复制修复 |

## 真实边界

本任务证明现有源码和分层测试自洽，没有重新用真实抖音账号走一次注销。因此它可以关闭
“投影映射本身写错”的调查，不可以替 T50 提供真实平台完成证据。

## 清理

没有启动浏览器、读取 Cookie、修改 Profile 或生产文件。

## 文档与遗留

这是“前提不成立”的交付。后续若真实账号再次出现 `missing` 却显示读取失败，必须先保存权威
响应和 gateway 错误分类，再开启新 RED，不能直接改文案。
