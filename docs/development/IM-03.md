# IM-03 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-24
>
> 提交：本文件所在的 IM-03 Windows 补证独立提交

## 交付范围

- App 启动器只向独立 Worker 写入一次性 256-bit 会话令牌、协议版本和经校验的素材根目录，
  Worker 只监听 `127.0.0.1` 的系统随机端口。
- 网关只开放健康、能力查询和受控素材检查三个固定路由；Bearer 认证、精确来源白名单、CORS
  预检、64 KiB 请求上限、10 秒超时和固定错误体统一 fail closed。
- 素材路径只能是素材根目录内的规范相对路径，逐级拒绝符号链接、Windows reparse point、目录、
  空文件、越界路径和大于 2 GiB 的文件。
- Rust 与 Worker 的 ready、health、cancel 继续使用 HMAC 域隔离证明；密钥、真实路径、原始异常和
  localhost URL 不进入用户可见返回或普通日志。

## RED

Roadmap 进入 `🧪 RED` 后执行：

```bash
python3 scripts/run_im_03_acceptance.py
```

实际退出码为 2，明确报告验收脚本不存在，证明当时没有安全网关契约、真实 HTTP 失败矩阵或
冻结 Worker 与 Rust 启动器的纵向验收。

## GREEN

```bash
python3 scripts/run_im_03_acceptance.py
python3 scripts/check_embedded_browser_video_roadmap.py
uvx ruff check --ignore RUF001 --config backend/pyproject.toml \
  scripts/run_im_03_acceptance.py scripts/test_material_video_gateway.py \
  scripts/test_material_video_worker.py workers/material_montage/*.py
cargo fmt --manifest-path frontend/src-tauri/Cargo.toml -- --check
git diff --check
```

macOS arm64 验收实际重新冻结完整 onedir Worker，并由公开 Rust `LocalVideoOrchestrator` 启动该
可执行文件，完成随机端口握手、认证健康检查、认证取消、停止和清理；不是 Mock Worker。

Windows 11 x86_64 重新冻结 4,383 files / 749,943,876 bytes 的真实候选，冷启动探针
2.725 秒。生产 Rust 编排器实际启动
`automation-tool-material-video-worker.exe`，两条相关端到端用例完成 ready HMAC、随机
loopback、认证 health/cancel、模型秘密投影、停止和 Job Object 进程树清理。

首轮 Windows RED 还发现统一编排器后续新增 `renderBrowser: null` 后，素材成片 Worker 的
严格 bootstrap 字段集未同步，导致真实进程固定退出并被映射为 `ProcessUnavailable`。Python
契约现要求该字段必须存在且只能为 `null`；缺失、对象或任意非空值仍拒绝。

## 失败矩阵

- 相对、缺失、符号链接或 reparse point 素材根目录，以及可执行路径异常：启动前拒绝。
- bootstrap 字段缺失、多余、重复、协议/Worker 类型错误、令牌长度错误或超限：固定失败且不监听。
- 统一 bootstrap 的 `renderBrowser` 在 Python 素材成片 Worker 中只能为 `null`；既兼容
  Rust 当前协议，又拒绝把动效渲染浏览器权限误授给该 Worker。
- 无认证、重复认证头、错误来源、通配来源、未知路由、查询串、非法请求 ID：固定状态和错误体。
- 流式请求、缺失/重复/超限 Content-Length、错误 Content-Type、畸形或重复 JSON 字段：拒绝。
- 绝对路径、`..`、反斜杠、缺失文件、目录、符号链接、空文件、超限文件和根目录外目标：拒绝。
- ready、health 或 cancel 的事件、版本、端口、任务 ID、类型或 HMAC 证明不匹配：Rust 侧拒绝。

## 正常用户路径验收

不适用。IM-03 是 App 私有 Worker 的进程和网络安全底座，没有新增用户页面。验收使用正式冻结
候选和生产 Rust 编排器真实跨进程通信，不用 fixture 代替最终进程；从“视频制作”入口打开完整
页面和提交任务属于 IM-05、IM-07、IM-08。

## 真实边界

- macOS arm64 与 Windows x86_64 均已通过真实冻结进程链路；Windows 结果来自本机实际
  `.exe` 和 Job Object，不依赖历史上被账单阻断的托管 Job。
- 网关当前只提供安全基础路由，不声称上游 WebUI 已可用，也不把原始 localhost 暴露给 React。
- CORS 是浏览器边界的补充而不是认证；每个请求仍必须持有 Rust 进程内会话令牌。

## 清理

- 测试网关均绑定系统分配随机端口，并在 `finally` 中 shutdown、close、join；没有使用项目默认端口。
- 冻结环境、候选包和素材根目录均在系统临时目录，验收结束删除；Rust stop 终止 Worker 进程树。
- 两次 RED 与最终 GREEN 均由独立临时候选复现；修复后没有遗留 Worker 或监听端口。
- 不触碰另一个 session 的端口、Profile、进程或本地数据，也不写入上游 submodule。

## 文档变化

- 新增安全网关契约、本独立证据和真实验收脚本；只更新专项 Roadmap 的 IM-03 状态与汇总。
- Windows 补证新增统一 bootstrap 兼容回归，防止后续共享字段再次让严格 Worker 静默失配。
- 不修改旧 `docs/development-roadmap.md`，不把安全底座冒充已完成的用户视频制作功能。

## 2026-07-24 Windows 原生补证

```powershell
uv run --project backend --locked python scripts/run_im_03_acceptance.py
```

前两轮使用全新 Windows 候选稳定复现统一 bootstrap 字段漂移；第三轮在修复后完成相同的
4,383 文件候选构建和全部相关真实跨进程测试，3 项 Rust test target 均通过（其中 IM-05
专用 WebUI 用例按环境变量边界返回，IM-03 的普通网关与模型网关两项均真实启动候选）。
Python 网关 5 项失败矩阵、Worker 5 项测试及专项 roadmap 检查同时通过。

## 遗留项

IM-03 无遗留验收缺口。IM-04 继续接文案模型设置，IM-05 才从正式 App 入口验证完整 WebUI；
这些是既定下游任务，不计作 IM-03 未完成项。
