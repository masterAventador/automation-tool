# LE-06 剪辑 REST API

用户可操作：否
证据类型：分层实现

## 日期与状态

- 开工日期：2026-07-30
- 当前状态：T1～T5 已完成；T6 真服务契约与模块收口进行中
- 前置：LE-05 `🔍 待验收`，四表、四迁移、四仓储已经合入 `smart-edit`

## 执行粒度（用户于 2026-07-30 调整）

- 项目统一规则已写入 `CLAUDE.md`「测试执行粒度（§8 补充）」；本节只记录 LE-06
  如何消费该规则；
- T1～T5 每个子任务仍执行 TDD、相关单元/契约测试、改动面静态检查、Codex Review、
  台账回填、提交与推送；正常情况下不重复运行耗时约 8 分钟的 `tests/integration`
  整目录；
- LE-06 整个功能模块到 T6 收口时统一运行一次 `tests/integration` 整目录，并完成真实
  Uvicorn/PostgreSQL 契约验收；单个子任务确因跨模块、高风险基础层或完成定义需要全量时，
  执行前说明原因再追加；
- 子任务涉及的数据库 GREEN 在 T6 前明确记为“待模块收口复验”，不能把快速测试冒充真实
  PostgreSQL 证据；若中途出现必须立刻验证的高风险数据库故障，先向用户说明再追加运行。

## 首期边界

首期严格对齐 `frontend/src/features/video-editing/video-editing-gateway.ts` 的六个操作：

1. 项目列表、创建；
2. 时间轴读取、保存；
3. 作业列表、提交。

项目是 write-once，不做更新或删除。素材登记与查询作为 LE-18 的后端前置在本任务交付。
项目与作业缺少的分页仓储查询归本任务补，不把不存在的 update/delete 偷塞进“CRUD”。

## 开工前【先定】

### 1. HTTP 错误分层

- 请求体或域构造拒绝：422；
- 可公开且调用方有明确动作的持久化失败：404 / 409 / 503；
- 四个 `...DataRejected`：500。它表示仓储的服务端调用方给错类型/状态，或数据库出现
  仓储不认识的约束，不是 HTTP 客户端可以通过改请求稳定修好的答案；
- 四个 `Invalid*Model`：500。它们表示既有数据库行在水合时被域拒绝；
- `DataRejected`、`Invalid*Model` 与未知异常原样上抛给全局 unexpected handler，
  由它记录异常并产出固定 `internal` 信封；不把 500 包成不会记录的 `AppError`。

T1 的动态词汇守卫必须发现四个应用模块全部 4 / 5 / 5 / 7 个持久化失败子类。新增失败类
若没有明确公开映射或明确内部失败归属，测试必须变红。

### 2. 修订冲突的公开形状

`PublicError` 增加可选、严格校验的 `details` 判别联合。首个成员：

```text
kind = timeline_revision_conflict.v1
currentRevision = 大于等于 1 的整数
```

其它错误不带 `details`。版本不得塞进 message。冲突后的 `currentRevision` 是再次读取到的
最新修订，允许比发生冲突的那一版更新。

### 3. 一个项目只能有一个 timeline 身份

采用数据库可强制的一对一身份表，不使用“先查没有再随机插入”：

- 新表 `editing_project_timelines`：`project_id` 为主键并外键指向项目，
  `timeline_id` 唯一；
- 第一次保存时间轴时用单条数据库写入争夺项目的 timeline 身份；并发首存只能有一个身份
  成功，失败方读取已存在身份后重建请求或答冲突；
- `timelines (project_id, timeline_id)` 增加复合外键指向该身份表，任何绕过 API 的写入
  也不能给同一项目制造第二个 timeline 身份；
- Timeline 仍是不可变修订，不增加 update；`EditingJob.update(previous, changed)` 的 CAS
  只由 LE-12 Worker 状态写回消费。

迁移、仓储方法名与并发 RED 已在 T4 固定并落地；身份方案没有重选。

## 失败矩阵

| 边界 | 本任务覆盖 |
| --- | --- |
| 非法请求、未知字段、错误 UUID/枚举/区间 | 422 固定信封，不反射输入 |
| 重复项目/素材/修订/作业 | 409，既有行不被覆写 |
| 引用的项目或 timeline 修订不存在 | 409，提示先补引用，不假装目标资源 404 |
| 查询目标不存在 | 404 |
| 数据库不可用 | 503，`retryable=true` |
| 仓储调用错误、未知约束、坏存量行 | 500 固定信封，并走 unexpected 日志路径 |
| 同时保存第一版时间轴 | 数据库只允许一个 timeline 身份 |
| 同时间戳项目/作业跨页 | 复合总序游标，不重不漏 |
| Uvicorn 启动/端口/进程边界 | T6 用专属空闲端口真实起服务，进程退出后复核无残留 |
| PostgreSQL 真实依赖 | 整目录 integration + T6 真服务契约 |

## Task 进度

| Task | 内容 | 状态 |
| --- | --- | --- |
| T1 | 错误映射层 | 已完成 |
| T2 | 项目路由与分页仓储 | 已完成 |
| T3 | 素材路由 | 已完成 |
| T4 | Timeline 身份、修订与路由 | 已完成 |
| T5 | 作业路由与分页仓储 | 已完成 |
| T6 | 真服务契约、双轮审查与收口 | 进行中 |

## T2 项目路由与分页仓储

### 开工前定下的接口

- 路径固定为 `/api/v1/editing-projects`：`POST` 创建、`GET` 分页列表，
  `/api/v1/editing-projects/{project_id}` 只读详情；没有 update/delete；
- 创建体只承载 `title`、`output` 与 `captionStyle`。`projectId` 与 `createdAt` 由服务端
  生成；LE-01 前端旧 DTO 的 `sourceArtifactIds` 已被新领域模型淘汰，不把它带回 API；
- 公开快照逐字对应当前 `EditingProject`：`projectId/title/output/captionStyle/createdAt`，
  不虚构领域里不存在的 `updatedAt`；
- 列表默认 20 条、最多 100 条；仓储固定
  `created_at DESC, project_id DESC`，游标是同时携带两列的 canonical base64url JSON，
  非规范编码、重复键、未知字段、坏 UUID 或坏时间一律 422；
- App Session 认证沿用 `require_current_installation_access`。自审实际证明“只认证、
  不按 Installation 过滤”会让任一合法设备读取共享 Control Plane 上的全部项目，因此
  `editing_projects.installation_id` 从建表起就是非空外键，项目行自身承载唯一 owner；
  项目领域对象保持不带认证信息，仓储只保留 `save/get/list_page` 三个现行方法且全部
  必填 `InstallationId`。跨 Installation 的详情统一 404、列表不出现外部项目；不保留
  无 owner 入口、scoped 后缀别名、所有权旁表或兼容适配器。

### RED 目标

1. 真实 PostgreSQL 中四个项目使用同一 `created_at`，按两页读取时仍按 `project_id`
   总序排列且不重不漏；
2. OpenAPI 只暴露创建、列表、详情三个 App Session 保护操作；
3. 创建与查询返回严格公开快照，未知字段、非法画幅/字幕跨字段组合均为固定 422；
4. 不存在或非法 project id 为同一 404；重复创建为 409；数据库不可用为可重试 503；
5. 游标不透明且篡改、非 canonical 或形状漂移均为固定 422。

### RED、GREEN 与安全修正（2026-07-30）

首轮契约 RED 停在缺少 `EditingProjectService`；首轮真实 PostgreSQL RED 只缺分页方法，
结果为 `1 failed, 445 passed, 5 skipped`。完成路由、应用服务、分页仓储、组合根与 OpenAPI
后，契约、相关回归和整目录 integration 曾分别达到 GREEN，其中 integration 为
`446 passed, 5 skipped`。

随后在提交前自审发现 Installation 隔离缺口，先补两条 RED 再改生产代码：

1. 内存契约把两个 Installation 的项目放在同一仓储，旧服务因仍调用无范围分页方法失败；
2. 真实 PostgreSQL 断言所有权关系必须由数据库承载，整目录结果为
   `1 failed, 446 passed, 5 skipped`，唯一失败是
   `editing_project_installations` 尚不存在。

之后才新增 `0040` 迁移、SQLAlchemy schema、原子所有权写入及三条 scoped 查询。该方案
保留了 LE-05 的无 owner 仓储入口，并以旁表和第二套方法追加新能力；用户随后明确整个
项目首次发布前没有任何兼容负担，因此这只是已经删除的过渡实现，不再是现行结构。

无兼容收敛先新增结构 RED，要求三个公开方法都必须接收 `InstallationId`、三个
`*_for_installation` 别名不存在、项目表 owner 非空；结果为 `1 failed`，首先命中旧
`save(project)` 签名。GREEN 直接把 owner 外键和分页索引并入基线迁移 `0036`，删除
`0040` 与所有权旁表，仓储、应用服务、契约内存仓储及 Timeline/作业建链测试全部改用
唯一接口。用户已把项目测试粒度统一为“模块收口跑一次全量”，因此整目录 integration
仍延后到 T6；本次只运行改动所必需的项目真实 PostgreSQL 专项和两条下游建链用例。

修正过程中又用 RED 证明 Pydantic 会把字符串 `"1.25"` 悄悄转换为字幕行距并返回 201，
再给该浮点边界启用严格类型校验，使同一用例返回固定 422。

### 当前快速门禁

| 门禁 | 结果 |
| --- | --- |
| 项目 API、错误映射与项目仓储单测 | 44 passed |
| Review 追加的项目契约、仓储单元与真实 PostgreSQL 专项 | 43 passed |
| 项目仓储真实 PostgreSQL 专项 | 10 passed |
| Timeline / 作业真实 PostgreSQL 建链回归 | 2 passed |
| API / 应用 / bootstrap / 项目仓储覆盖率 | 262 statements、42 branches，100% |
| 改动面 `ruff format` / `ruff check` | 通过 |
| 改动面 mypy（生产、契约、单元、迁移） | 7 source files，Success |
| OpenAPI 生成漂移 | 后端 OpenAPI `--check` 通过；公开 HTTP 契约未变 |
| Alembic 图 | 单 head：`20260729_0039`；项目归属已直接并入基线迁移 `20260729_0036` |
| 用户文案门禁 | 精确命中既有 2 条 branding 基线，均在未改的 `WorkbenchShell.tsx` |
| 专项台账计数 | 通过 |

真实 PostgreSQL 所有权用例核对了两台 Installation 的写入隔离、跨设备 404、列表隔离、
项目行上的非空 owner、外键、复合分页索引与伪造 owner 结构拒绝；专项已通过。
`tests/integration` 整目录仍只在 T6 统一执行。

### 捕获力与 Codex Review

- 在独立 Python 进程里把游标的 `project_id` tiebreaker 固定为错误值，等时间项目第二页
  从预期两项变为空，目标测试 exit 1；
- 在独立 Python 进程里故意让内存仓储忽略 Installation owner，跨设备列表立刻混入外部
  项目，隔离测试 exit 1；
- 两次变异都只改进程内对象，进程结束即还原，工作树文件未改；
- 规格自审逐项确认只提供创建、列表、详情，没有 update/delete；公开快照没有旧
  `sourceArtifactIds` 或虚构的 `updatedAt`；分页使用复合总序；App Session 同时落实到
  数据库所有权范围；
- 质量自审确认请求值严格拒绝、游标 canonical、项目与 owner 原子提交、未知约束不伪装成
  重复、异常信息不反射连接与存量值；
- 实现提交：`5442ded`；
- 首轮执行 `codex review --commit 5442ded`，发现 1 条 P1：OpenAPI 权威快照已新增三个
  operation，但 `frontend/src/api/generated/control-plane.ts` 未同步，`pnpm check:api`
  确定性失败；
- 先保留上述生成漂移 RED，再运行既有唯一生成入口 `pnpm generate:api`，修复提交为
  `544e7a9`；`pnpm check:api`、后端 OpenAPI 快照检查和 TypeScript 类型检查均通过；
- 复审执行 `codex review --commit 544e7a9`，结论为未发现该提交引入的缺陷。Review
  额外运行全量前端契约时命中的既有失败均位于本提交未改文件，Reviewer 已明确排除为本
  提交发现；
- 本次无兼容收敛提交为 `a3c29dc`；`codex review --commit a3c29dc` 核对迁移图、
  非空 owner 外键与分页索引、异常映射、全部调用方和全仓残留引用，并复跑项目仓储文件
  及 Timeline/作业建链共 12 项真实 PostgreSQL 测试，全部通过，未发现缺陷。LE-06
  模块全量与真服务验收仍留在 T6。

## T3 素材路由

### 开工前定下的接口与所有权

- 路径固定为 `/api/v1/editing-materials`：`POST` 登记；
  `GET /{material_id}` 按身份查询；集合 `GET` 用必填 `contentDigest` 做导入判重查询；
  `PUT /{material_id}/description` 更新描述。没有列表、删除或其它素材字段更新；
- 登记体逐字段承载当前 `Material` 快照，并由调用方提供 `materialId`，因为同一个身份还要
  登记进执行器侧 `MaterialPathRegistry`。请求与响应都不出现本机路径；所有请求模型
  `extra=forbid`，所以 `path`、`filePath`、`sourcePath`、`localPath`、`fileUri` 等
  路径形状字段都得到固定 422；
- 描述更新是按 `source` 判别的严格联合：`user` 只接受 `description`；`ai` 还接受
  `tags` 与 `describedAt`。当前快照已经是 `USER` 时，服务必须显式抛
  `MaterialDescriptionProtected`，不能把 `Material.with_ai_description()` 返回
  `self` 的静默保护误报成成功；仓储的原子 UPDATE 谓词继续兜住读取后发生的并发用户写；
  更新后重新读取并返回实际存储快照，调用方不能从“没有异常”推断自己提交的 AI 文本一定
  仍是最终值；
- App Session 不能只认证不授权。`materials.installation_id` 从建表起就是非空外键，
  `(installation_id, content_digest)` 唯一；同一文件可由两台 Installation 各自登记，
  跨设备详情与摘要查询均为 404，也不会共享随后可变的用户描述；
- 素材仓储只保留 `save/get/find_by_digest/update_description` 四个现行方法，每个方法
  都必须显式接收 `InstallationId`。不保留无 owner 入口、`NULL` 命名空间、旧方法、
  scoped 后缀别名或兼容适配器。

### RED 目标

1. OpenAPI 只暴露登记、两种查询与描述更新四个 App Session 保护操作；
2. 登记、ID 查询与摘要查询返回严格的当前域快照，任何路径形状或未知字段都固定 422；
3. 两台 Installation 可登记相同摘要，但详情、摘要和描述更新都互相不可见；
4. 用户描述往返后，AI 再写答 `material_description_protected` 409，既有文本不变；
5. 非法 UUID/摘要/枚举/数值/描述联合答固定 422；重复登记 409、查询缺失 404、
   数据库不可用 503。

### RED、GREEN 与边界修正（2026-07-30）

三层 RED 都先命中缺失能力：

1. API 契约收集即因缺少 `InvalidMaterialQuery` / `MaterialService` 报 ImportError；
2. 仓储参数单测为 `1 failed, 35 passed`，唯一失败是
   `SqlAlchemyMaterialRepository` 没有 `save_for_installation`；
3. 真实 PostgreSQL 专项为 `1 failed`，同样停在缺少 scoped 保存方法。

首轮 GREEN 增加领域登记工厂、四方法应用服务、严格 API 模型与组合根。最初实现仍为了
LE-05 旧内部调用保留无 owner 方法和 `NULL` 命名空间；用户随后明确项目首次发布前在
代码、API、协议、配置、数据库和构建侧都没有兼容负担，因此该方案不再成立。

无兼容收敛先新增结构 RED，要求四个公开方法都必须接收 `InstallationId`、四个
`*_for_installation` 别名不存在、Schema owner 非空；结果为 `1 failed`，首先命中旧
`save(material)` 签名。GREEN 直接改写基线迁移 `0037` 和 SQLAlchemy Schema：
`installation_id NOT NULL`、只保留 `(installation_id, content_digest)` 唯一索引；
仓储与应用层统一到四个现行方法，全部调用方和测试同步修改，没有迁移、回填、fallback
或旧入口。真实 PostgreSQL 素材文件随后 `39 passed`。

描述服务在写回后重新读取：正常 AI 更新返回实际快照；当前或并发变成 USER 都答 409。
API 对所有请求模型禁额外字段，五种路径形状逐一得到固定 422。撤销面契约也显式把素材
登记和描述写入登记为 App Session 保护的业务写操作。

### 当前快速门禁

| 门禁 | 结果 |
| --- | --- |
| 素材 API、OpenAPI/撤销面、领域与仓储相关回归 | 当前 141 passed |
| 素材仓储真实 PostgreSQL 回归文件 | 39 passed |
| API / 应用 / bootstrap / 素材仓储分支覆盖率 | 当前 301 statements、52 branches，100%（141 passed） |
| 改动面 `ruff format` / `ruff check` | 7 files，通过 |
| 改动面 mypy（生产、契约、单元、迁移） | 6 source files，Success |
| OpenAPI 生成漂移 / TypeScript 类型检查 | 后端 OpenAPI `--check`、`pnpm check:api`、`pnpm typecheck` 通过 |
| Alembic 图 | 单 head：`20260729_0039`；素材归属已直接并入基线迁移 `20260729_0037` |
| 用户文案门禁 | 精确命中既有 2 条 branding 基线，均在未改的 `WorkbenchShell.tsx` |
| 专项台账计数 | 通过 |

直接把 integration 文件交给 mypy 会由既有 `tests/integration/conftest.py` 的两个脚本导入
和一个 `Any` 返回共 3 条基线挡住；上表的 10 文件清单不含该既有夹具，integration 改动
本身由真实 PostgreSQL pytest 覆盖。`tests/integration` 整目录与真 Uvicorn 仍按会话统一
规则只在 T6 收口复验。

### 捕获力与提交前双轮自审

- 在独立 Python 进程里把内存仓储的 scoped `get` 临时改成忽略 owner，跨设备详情从
  预期 404 变成 200，隔离用例在断言处 exit 1；变异只存在于该进程，工作树未改；
- **规格审查**：只有登记、ID/摘要查询与描述更新；没有列表/删除/任意素材更新；请求和
  OpenAPI 都无路径字段；用户描述保护同时覆盖当前快照和数据库并发窗口；
- **质量审查**：ID 与摘要查询都按 Installation 收窄；同摘要跨设备独立、同设备由数据库
  拒绝；未知完整性约束保持内部 `DataRejected`，不会误报公开重复；API 返回写后重读的
  存储快照；Material 初建走领域工厂，既有构造保护守卫保持 GREEN；
- 实现提交 `a5ecf1d` 的首轮 `codex review` 发现迁移降级在跨 Installation 同摘要时失败，
  且 `describedAt` 会接受数值和非 OpenAPI 字符串；按未发布规则删除过渡迁移、直接改写
  `0037` 并收紧输入，修复提交为 `ae7e346`；
- `codex review --commit ae7e346` 又发现时间字符串只检查 `T`，仍接受缺秒、无冒号时区
  和逗号小数，且台账仍写旧 `0041`。RED 为 `3 failed, 4 passed`，修复提交
  `85eeb9b` 增加完整 RFC 3339 形状并修正文档；
- `codex review --commit 85eeb9b` 最终只发现 Ruff 格式门禁失败；格式 RED 已复现，
  修复提交 `2f580f5` 的复审确认 AST 等价、格式/lint 与 32 项素材 API 契约均通过，
  未发现缺陷；
- 本次按用户扩展后的“所有新代码均无兼容负担”规则继续删除无 owner 仓储入口和数据库
  `NULL` 命名空间，收敛提交为 `d34c380`；最终
  `codex review --commit d34c380` 未发现缺陷，并独立复跑 70 项素材契约/仓储单测、
  39 项真实 PostgreSQL 仓储测试与改动面 mypy，全部通过。

## T4 Timeline 身份、修订与路由

### 开工前定下的接口、身份与冲突契约

- 路径固定为 `/api/v1/editing-projects/{project_id}/timeline`：`GET` 读取该项目最新
  修订，`PUT` 保存下一修订；没有按任意 timeline id 枚举、删除、覆盖或 update。
  保存体只承载 `durationMs/tracks`，不接受 `timelineId/projectId/revision/createdAt`；
  Timeline 身份、下一 revision 与创建时间都由服务端产生；
- `editing_project_timelines` 直接并入基线迁移 `0038`：`project_id` 为主键并外键指向
  项目，`timeline_id` 唯一，`(project_id, timeline_id)` 再作为 `timelines` 的复合外键
  目标。第一版保存用一条 owner 限定的数据库写入争夺身份，身份与首修订在同一事务提交；
  两个并发首存不能各自产生一条 lineage；
- Timeline 仓储只保留三个现行方法：
  `save(timeline, installation_id)`、`get(timeline_id, revision, installation_id)` 与
  `latest_revision(project_id, installation_id)`。三个都必填 `InstallationId`，读取通过
  项目 owner 收窄；不保留无 owner 签名、scoped 别名、过渡迁移或 fallback；
- 保存先读取当前最新修订，构造 `revision + 1` 后尝试 write-once INSERT。并发方撞到
  已存修订或首版身份争夺后，服务必须重新读取当前最新修订，并抛只携带正整数
  `current_revision` 的安全应用冲突；不能自动把陈旧草稿重放成下一版；
- `PublicError.details` 是可选且 `extra=forbid` 的判别联合，首个且当前唯一成员为
  `{kind:"timeline_revision_conflict.v1", currentRevision:int>=1}`。只有 Timeline
  修订冲突携带该字段；其它错误序列化时完全不出现 `details`，不得把版本塞进 message；
- 非法项目 id、轨道/片段/转场形状和未知字段固定 422。不存在、跨 Installation 或尚无
  Timeline 的读取统一 404；保存引用不存在或无权访问的项目沿用
  `timeline_project_missing` 409，不能产生孤儿身份或修订。

### RED 目标

1. `PublicError` 接受唯一严格冲突 details，拒绝未知 kind、额外字段、布尔/零 revision；
   普通错误响应没有 `details`；
2. 两个并发首存只能得到一个 timeline identity 与一个 revision 1；失败方的 409 details
   必须是提交后重新读取到的当前 revision；
3. 后续两个并发保存只能新增一个 `revision + 1`，冲突响应允许报告比撞车版本更高的当前值；
4. GET/PUT 都按 Installation 隔离，未知字段、客户端身份/版本字段及非法完整 Timeline
   结构均固定 422；
5. 真实 PostgreSQL 裸写不能给一个项目制造第二个 timeline id，也不能让 Timeline 行绕过
   身份表；重复修订不覆写既有行。

### RED、实现与真实边界（2026-07-30）

- 错误 details 首个 RED 在收集阶段因
  `ImportError: TimelineRevisionConflictDetails` 失败；HTTP / Service RED 为
  12 failed，首先命中缺失 Timeline 路由与服务；Schema / Repository RED 首先命中缺失
  `editing_project_timelines` 导出；
- 实现提交 `e504e35` 直接改写基线迁移 `0038`，加入一对一身份表与复合外键；仓储仅保留
  owner 必填的 `save/get/latest_revision`，API 只暴露 latest GET 与 next-revision PUT，
  同步生成 OpenAPI 与 TypeScript 类型，没有旧签名、别名、过渡迁移或 fallback；
- 并发首存、重复修订、跨 Installation 隔离、裸写身份约束、坏存量水合、严格请求体和
  冲突重读均有 RED 后转绿。首轮真实 PostgreSQL 整目录结果为
  `451 passed, 5 skipped`，用于发现并修正 asyncpg 嵌套 constraint name、既有坏 UUID
  夹具与 PostgreSQL 18 的 NOT NULL 约束表示差异。

### 首轮 Codex Review 与修复（2026-07-30）

`codex review --commit e504e35` 报告 4 个 P2：

1. PUT 把仓储水合抛出的 `InvalidTimelineModel` 错误降为 422；
2. 409 冲突 details 没有进入 OpenAPI；
3. `latest_revision` 没有 project-first 可索引路径；
4. 本文件仍错误写着 T4 未开始且没有证据。

修复 RED 精确得到 `4 failed, 109 passed`：依次命中缺少 409、坏存量返回 422、
snake_case 兼容输入被接受，以及 SQL 未经过 `editing_project_timelines`。GREEN 将请求
转换与服务/仓储异常边界拆开，完整 Timeline 构造拒绝转为请求 422、坏存量仍走全局 500；
409 通过 `ErrorEnvelope` 暴露严格 `PublicErrorDetails`；latest 先用项目身份主键取得
timeline id 再走既有主键；请求模型删除 `populate_by_name`，不保留 snake_case 兼容输入。

期间发现 `python -m automation_tool.control_plane.bootstrap.openapi` 因模块没有
`__main__` 而是假绿；改用项目正式入口
`uv run automation-tool-export-openapi --output ../contracts/openapi/control-plane.v1.json`
重新生成权威快照，再生成前端类型。

### 修复后门禁（2026-07-30）

| 门禁 | 结果 |
| --- | --- |
| T4 API / errors / application / repository statement + branch 覆盖 | 128 passed，354 statements / 54 branches，100% |
| Control Plane 全量单元与契约 | 2291 passed，1 skipped（既有 POSIX 文件模式条件） |
| `tests/integration` 整目录 | 451 passed，5 skipped，485.00s |
| 改动面 `ruff format --check` / `ruff check` | 通过 |
| 改动面 mypy | 5 source files，Success |
| 正式 OpenAPI 生成 / 前端 `generate:api` 与 `check:api` | 通过；409 生成 `ErrorEnvelope` 与严格 details 类型 |

本任务真实边界已经覆盖 PostgreSQL 迁移、并发与仓储；真实 Uvicorn 进程、端口回收和完整
HTTP 契约仍按计划在 T6 收口。

修复提交为 `29e0a97`（`fix(le-06): 修正时间轴审查问题`）。随后执行
`codex review --commit 29e0a97`；复审独立核对错误边界、OpenAPI 契约、项目身份查询，
并复跑 114 项定向测试、Ruff、mypy、正式 OpenAPI `--check` 与前端 `check:api`。最终结论：
**未发现该提交引入的可操作缺陷**。

## T1 错误映射层

### RED 目标

1. 动态发现四个持久化失败词汇，逐类断言 HTTP 状态、code、固定 message 与 retryable；
2. 四个 `DataRejected` 必须原样上抛，不得变成 422 或静默的 `AppError(500)`；
3. 四个坏存量行 `Invalid*Model` 通过真实 FastAPI 全局处理器得到固定 500 信封，响应不含
   异常文本；
4. 未登记异常原样上抛，新增异常不能被“按名字前缀猜测”吞掉。

RED、GREEN、门禁、真实边界与提交号在执行后回填。

### RED（2026-07-30）

命令：

```text
uv run pytest tests/unit/control_plane/test_editing_api_errors.py -q
```

结果：`2 failed, 4 passed`。两个失败都停在
`ModuleNotFoundError: automation_tool.control_plane.api.editing_errors`；动态发现集合
本身已经通过，证明当前词汇正好是 4 / 5 / 5 / 7。四个坏存量行的 HTTP 用例先绿，是因为
既有全局 unexpected handler 已能给固定 500 信封。

首轮 GREEN 又暴露一处**测试夹具缺陷**：直接上抛断言给四个 `Invalid*Model` 传了
`"private stored value"`，但这四个异常的构造器有意不收参数，结果是
`4 failed, 6 passed`，失败类型全是 `TypeError`。回看才发现前一轮 HTTP 四参也因同一写法
实际让 `TypeError` 走了 500，属于假绿。修正为无参构造，并用异常自身固定消息不出现在响应
中守泄漏；生产实现未改。

### GREEN 与门禁（2026-07-30）

| 命令 | 结果 |
| --- | --- |
| T1 文件 pytest | 10 passed |
| T1 模块分支覆盖 | 100%：20 statements / 2 branches / 0 partial |
| `pytest tests/unit/control_plane -q` | 1931 passed，1 skipped（既有 POSIX 文件模式条件） |
| `ruff format --check` / `ruff check`（两个 Python 文件） | 通过 |
| `mypy`（两个 Python 文件） | Success |
| `scripts/check_local_editing_roadmap_counts.py` | 通过 |

门禁首轮先抓到两项测试侧问题：两个文件尚未格式化；反射结果未显式收窄为异常类型且断言
读取了静态基类上不存在的 `.message`。格式化后，mypy 又指出集合成员上的 cast 已因
`issubclass` 收窄而多余；删掉后通过。三项都没有改生产行为。

### 捕获力与两轮审查

捕获力实测在独立 Python 进程内从 `_PUBLIC_FAILURES` 删除 `EditingJobStale` 后，直接调用
动态守卫，进程 exit 1，并从 `translate_editing_error` 原样抛出 `EditingJobStale`。变异只在
内存中发生，进程结束即还原，工作树文件未改。

- **规格审查**：4 / 5 / 5 / 7 全词汇逐类有明确公开或内部处置；404 / 409 / 503 与 LE-05
  交接逐项一致；四个 `DataRejected`、四个 `Invalid*Model`、未知异常都保留给全局 500；
- **质量审查**：用 exact type 而非名字前缀或继承猜状态；公开 message 只取无参异常的固定
  字符串；内部 500 不被 `AppError` 截走日志；新增词汇会先使动态集合断言变红。

### Codex Review 与提交（2026-07-30）

实现提交为 `b5e8b46`（`feat(le-06): 建立剪辑 API 错误映射`）。随后执行
`codex review --commit b5e8b46`；审查逐项核对 17 个公开映射、8 个内部 500 分支、全局
unexpected handler 与异常定义，并独立复跑 T1 文件测试、Control Plane 全量单元测试、
ruff、mypy 和台账计数。结论为：**未发现会破坏现有行为的可操作缺陷**。

T1 未接路由、数据库或用户路径，真实边界止于错误转换单点与 FastAPI 全局错误处理器。

## T5 作业路由与分页仓储

### 开工前定下的接口、提交语义与所有权

- 路径固定为 `/api/v1/editing-projects/{project_id}/jobs`：`POST` 提交该项目当前最新
  Timeline 修订，`GET` 按项目分页列出作业；`GET /api/v1/editing-jobs/{job_id}` 按
  作业身份查询。没有 PATCH、DELETE、取消或任意状态写回；双参 CAS
  `EditingJob.update(previous, changed)` 仍只归 LE-12；
- POST 请求体固定为空对象 `{}`，由严格模型拒绝任何客户端提供的 job/timeline 身份、
  revision、状态、时间或产物字段。`jobId`、当前 `timelineId/revision`、QUEUED 状态与
  两个时间戳全部由服务端产生；不存在可提交的当前修订时答
  `editing_job_timeline_revision_missing` 409；
- 公开快照逐字对应当前 `EditingJob`：
  `jobId/projectId/timelineId/timelineRevision/status/failureCode/outputArtifactId/
  createdAt/updatedAt`。不沿用 LE-01 旧前端 DTO 的 `editingJobId`、输入/输出产物数组、
  `paused` 或 `outcome_uncertain`；LE-17 直接更新唯一消费方；
- 列表默认 20 条、最多 100 条；仓储固定
  `updated_at DESC, job_id DESC`，游标是同时携带两列的 canonical base64url JSON，
  非规范编码、重复键、未知字段、坏 UUID 或坏时间固定 422；
- 作业所有权必须由数据库结构承载，不只在服务层先查项目。`editing_jobs` 直接增加非空
  `installation_id`，并用 `(project_id, installation_id)` 复合外键指向项目的唯一 owner；
  分页索引固定为 `(installation_id, project_id, updated_at, job_id)`。仓储
  `save/get/list_page_by_project` 都显式接收 `InstallationId`，跨 Installation 的详情与
  列表统一不可见；不保留无 owner 签名、scoped 别名、兼容适配器或过渡迁移；
- `save` 仍由复合 Timeline 外键与 QUEUED 部分唯一索引原子拒绝坏修订和重复排队。
  `RevisionAlreadyQueued` 公开为 409；重复 job id 同样 409 但保留独立 code。T5 不读取后
  覆盖、不 upsert，也不把冲突改成返回既有作业。

### RED 目标

1. OpenAPI 只暴露提交、项目分页与 ID 查询三个 App Session 保护操作，不出现状态写回；
2. 空对象提交、详情与列表返回严格当前域快照；额外提交字段、非法 ID/limit/cursor 固定
   422 或资源语义对应的 404，不反射输入；
3. 两台 Installation 的作业由数据库 owner 约束隔离，跨设备详情 404、列表不混入；
4. 同 `updated_at` 作业跨两页按 `job_id` 总序不重不漏，游标同时携带两列且不可篡改；
5. 同一修订第二个 QUEUED 作业答 409，既有作业不被覆写；无当前 Timeline 修订答 409；
6. 数据库不可用答可重试 503，坏存量 `InvalidEditingJobModel` 仍走固定、记录日志的 500。

### RED、GREEN 与结构修正（2026-07-30）

首轮契约 RED 在收集期即停在缺少 `EditingJobService`；首轮仓储结构 RED 为 7 项失败，
依次命中旧 `save/get` 签名、缺少非空 owner 列与缺少项目分页方法。GREEN 后只保留
`save/get/list_page_by_project` 三个 owner 必传入口，没有无 owner 重载、scoped 别名或
兼容适配器。

所有权结构直接并入尚未发布的基线迁移：`editing_projects` 增加
`(project_id, installation_id)` 唯一约束，`editing_jobs.installation_id` 非空，并以
`(project_id, installation_id)` 复合外键指向项目 owner；列表索引固定为
`(installation_id, project_id, updated_at, job_id)`。真实 PostgreSQL 用例同时证明：

1. 外部 Installation 按 ID 查询统一 404、按项目列表为空；
2. 伪造 owner 保存被数据库复合外键拒绝，且不会误报成 Timeline 修订缺失；
3. 三个相同 `updated_at` 的作业按 `job_id DESC` 分两页读取时不重不漏。

应用服务提交时只读取 owner 范围内的最新 Timeline 修订，服务端生成 job 身份、QUEUED
状态和时间；API 严格拒绝任何客户端身份、修订、状态或产物字段。公开 OpenAPI 与前端
生成类型已同步三个 operation；Rust `ControlPlaneOperation` 的封闭允许列表也同步
`listEditingJobs/submitEditingJob/getEditingJob`，并把全部剪辑 App Session operation
的 401 统一解释为 Installation access denied。

### 当前快速门禁

| 门禁 | 结果 |
| --- | --- |
| 作业 API、作业仓储单元与作业领域定向测试 | 189 passed |
| API / 应用 / bootstrap / 作业仓储覆盖率 | 323 statements、58 branches，100% |
| 作业与项目仓储真实 PostgreSQL 专项 | 59 passed |
| 改动面 Ruff / mypy | Ruff 通过；生产、契约、单元与迁移共 9 source files mypy 通过 |
| OpenAPI / 前端生成契约 | 后端 `--check`、`pnpm check:api`、`pnpm typecheck` 通过 |
| Rust operation 封闭允许列表 | 路径/方法/状态与完整 OpenAPI 两项定向测试通过 |

捕获力抽查临时把仓储的 `job_id DESC` 改为升序；真实 PostgreSQL 所有权/复合分页用例
立即失败，首项从 `...103` 变为 `...101`。还原后同一用例通过，工作树未保留变异。
按本任务的模块级测试粒度，`tests/integration` 整目录与真实 Uvicorn/PostgreSQL HTTP
契约仍统一留到 T6 收口执行；本节没有把专项 59 项冒充模块全量。

### Codex Review 首轮发现与修复（2026-07-30）

实现提交为 `aa13a49`（`feat(le-06): add editing job REST API`），已推送。随后执行
`codex review --commit aa13a49`；审查器独立复跑作业与项目仓储的 59 项真实 PostgreSQL
专项后，报告 1 个 P2：三个作业 operation 的运行时 422 都由全局处理器产出
`ErrorEnvelope`，但 OpenAPI 仍声明为 FastAPI 默认 `HTTPValidationError`，使生成的
TypeScript 类型与真实错误信封不一致。

修复先在作业 API 契约测试中逐一断言三个 operation 的 422 schema；测试按预期从
`HTTPValidationError != ErrorEnvelope` 变红。随后只在 FastAPI 路由源声明统一
`ErrorEnvelope`，重新生成 OpenAPI 与 TypeScript，不手改生成产物。修复后作业 API 文件
24 项通过，Ruff、mypy、后端 OpenAPI `--check`、`pnpm check:api` 与
`pnpm typecheck` 均通过。修复提交为 `2160fce`
（`fix(le-06): align job validation contract`），已推送。

`codex review --commit 2160fce` 复审逐项核对路由源、OpenAPI、生成 TypeScript 与运行时
错误处理，并独立复跑 29 项作业 API/OpenAPI 契约测试、Ruff、mypy、前端契约漂移检查与
TypeScript 类型检查；结论为：**未发现新的可操作缺陷**。T5 至此完成，模块级
`tests/integration` 与真实 Uvicorn/PostgreSQL HTTP 仍按约定只在 T6 运行一次。

## T6 真服务契约与模块收口

### RED 与测试边界

先只写真实 HTTP 用例而不启动 Uvicorn，定向测试在项目专属空闲端口得到
`ConnectionRefusedError`，证明它没有悄悄退回进程内 `TestClient`。补齐进程夹具后又依次
抓到两条测试基础设施问题：

1. `/api/v1/health` 的正式响应还包含 `version`，旧就绪判断漏掉该字段，实际 200 被误判为
   未就绪；改为逐字段核对完整健康契约；
2. 本机 `Popen.terminate()` 让子进程以 `-SIGTERM` 退出；改用 Uvicorn 正式捕获的
   `SIGINT`，并继续要求 10 秒内返回 0。SIGKILL 只保留为明确失败路径，不能冒充正常清理。

最终夹具复用仓库唯一的 `reserve_control_plane_port()`，只从 automation-tool 专属的
`18765–18864` 端口段取值，启动前二次检查空闲；环境先删除全部
`AUTOMATION_TOOL_*`，再只注入本轮随机 PostgreSQL URL，避免意外消费开发机配置。

### 真实纵向证据

`test_editing_api_http_vertical.py` 在单独 Python 进程中用正式 factory 启动 Uvicorn，并
通过无代理的真实 loopback HTTP 完成：

1. 两台 Installation 的设备凭据经公开 `/api/v1/device-sessions` 各自兑换 App Session；
2. 未认证项目列表为固定 401；带 `sourcePath` 的素材登记为固定 422，响应不反射私有路径；
3. 素材按 ID/摘要往返，项目创建/列表，Timeline 首修订保存/读取，作业提交/列表/详情与
   重复排队 409 全部走公开接口；
4. 外部 Installation 对素材、项目、Timeline、作业详情均只见 404，集合查询为空；
5. 服务退出后直接查询同一个真实 PostgreSQL：项目、素材、作业 owner 都等于发起
   Installation；项目只关联一个 timeline 身份；修订为 1；QUEUED 作业精确引用该
   timeline 与修订；两条 App Session 均已落库。

每个响应还逐次核对调用方 `x-request-id` 原样回传及 `cache-control: no-store`。无论正文
断言成功还是失败，`finally` 都会关闭 Uvicorn；退出后再次确认专属端口可绑定、带本轮
唯一命令行标记的 PID 集合为空。补齐启动失败与 Windows 清理回归后，定向最终结果为
`3 passed in 8.38s`。

捕获力抽查临时让外部 Installation 复用 owner 的 App Session，第一条跨租户素材详情断言
立即由预期 404 变为实际 200，测试 exit 1；恢复独立会话后同一真实纵向测试重新 GREEN。
这次变异证明验收依赖真实身份隔离，不是只检查“接口有响应”。

### 模块级门禁（2026-07-30）

| 门禁 | 结果 |
| --- | --- |
| 后端 integration 整目录（本模块唯一一次） | 453 passed，5 个显式真人/可见浏览器验收 skipped，7m42s |
| Control Plane 单元与契约全包 | 首轮 2329 passed / 1 skipped / 1 failed；补齐 `submitEditingJob` 撤销面精确集合后 2330 passed / 1 skipped |
| T6 真实 Uvicorn/PostgreSQL 定向 | 3 passed；专属端口、真实进程、真实 HTTP、真实落库、启动失败/退出无残留与 Windows 清理契约 |
| 改动面 Ruff / mypy | 两个 Python 文件 format/check 通过；定向文件常规 mypy 仅余 `conftest.py` 3 条既有基线 |
| OpenAPI / 前端 | 后端快照 `--check`、`pnpm check:api`、`pnpm typecheck` 全部通过 |
| Rust operation 登记 | App operation 与完整 OpenAPI 精确集合定向测试 1 passed |

常规 mypy 直接分析 integration 文件时仍会进入既有 `conftest.py`，命中已登记的两个脚本
导入和一个 `Any` 返回共 3 条基线；本文件自身没有新增类型错误。
Rust 定向测试只报告未触及 `src/lib.rs` 的既有 unused-assignment warning。

### Codex Review 与修复

实现提交 `2c41d35` 的首轮 `codex review` 报告 3 项：Windows 不支持向普通子进程发送
`SIGINT`、健康契约断言失败发生在 context manager 接管前会泄漏子进程、App Session
落库证据错误地统计共享数据库全部历史会话。先写两个进程替身回归，RED 分别命中缺少
Windows process group 和启动断言后未回收；GREEN 后 Windows 使用独立 process group
与 Uvicorn 捕获的 `CTRL_BREAK_EVENT`，所有启动失败有界 terminate/kill，落库断言只接受
本测试两台 Installation 各一条 App Session。修复提交为 `cf0752e`，已推送。

`codex review --commit cf0752e` 继续报告 2 项 P2：常规 mypy 在会话计数字典新增 2 条严格
类型错误，而 `--follow-imports=skip` 隐藏了它们；新增两条清理测试后本节仍登记成 1 条
定向测试。前者的 RED 为常规 mypy 从父提交仅 3 条 `conftest.py` 基线变成 5 条，随后改为
显式 `dict[UUID, int]` 并消费 SQLAlchemy typed tuple；后者已按实际 `3 passed` 同步修正。
本轮修复提交后继续执行 Codex Review，T6 在复审干净前保持进行中。

当前代码、真实依赖与分层证据已收齐；本节随实现提交后执行 `codex review`。正式 App
尚未有用户可操作路径，最终台账状态只能顶格标 `🔍 待验收`，升为 ✅ 依赖 LE-17。
