# C10-13 部署/回滚手册

- 状态：✅ 已完成
- 目标：为单实例 Customer Demo 提供部署、迁移、备份、隔离恢复、账号/设备吊销、应用回滚、紧急停服和恢复收尾的一条安全运维路径。
- RED：路线图先置为 `🧪 RED`；新增 Node 契约测试准确失败于 operations contract、正式 runbook 和验收 runner 不存在，随后才补实现。
- 机器契约：`contracts/operations/customer-demo-runbook-v1.json` 固定九步顺序、单 Control Plane/Ingress、明确云目标、禁止数据库 downgrade、恢复只进新隔离数据库、回滚只用显式旧 digest、紧停保留数据库，以及只读 Secret 文件/stdin 两种 Secret 输入。
- 正式手册：`docs/customer-demo-operations-runbook.md` 给出发布前检查、备份 receipt、串行迁移部署、HTTPS health/version、账号级应急吊销与用户级单设备吊销、隔离恢复、schema 兼容/不兼容回滚分支、环境级紧急停服和恢复收尾；明确禁止生产库原地恢复、浮动 tag、自动 rollback、第二副本、数据库 downgrade 和 `down --volumes`。
- 执行门禁：`scripts/run_c10_13_acceptance.py --print-checklist` 输出无 Secret 检查清单；`--full-rehearsal` 校验手册契约后串行执行 C10-03、C10-10、C10-11，失败与超时只返回固定错误。
- 演练结果：真实 PostgreSQL 最小权限迁移、只读备份和新库恢复通过；单 Control Plane/Ingress 部署后 HTTPS health/version 200，原容器重启一次、application network flap 两次均恢复，Executor/outbox/`Last-Event-ID` 5 项恢复通过；账号/单设备吊销 6 项矩阵通过，匿名业务写为 0，其他账号与同账号其他设备保持有效。
- 边界：没有命名云目标、云身份或变更单，因此本轮只运行隔离 rehearsal，不执行真实云、DNS、公开证书、客户账号或生产停服操作。
- 清理：C10-03/C10-10 演练的临时容器、网络、卷、证书、备份、状态目录和随机端口均由各 runner 精确回收；没有删除或修改其他项目资源。
