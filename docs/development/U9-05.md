# U9-05 登录账号自动绑定设备

- 状态：✅ 已完成
- 日期：2026-07-23
- 提交：本任务独立提交
- RED：`uv run --project backend pytest -q backend/tests/unit/control_plane/test_account_installation_bindings.py` 按预期在收集阶段失败：`ModuleNotFoundError: automation_tool.control_plane.application.account_installation_bindings`；已冻结账号 Session + 设备密钥证明、五分钟一次性 challenge、fresh `atdc1` 和固定失败类型契约。
- GREEN：新增 `20260723_0030` 可逆迁移、Installation nullable owner 与数据库不可变 owner trigger、账号绑定 challenge 表/并发 pending 唯一约束、原子首次归属/同账号凭据轮换、`device.bound` append-only 审计；账号停用后既有设备 Session 和新 Session exchange 同时失效，未归属本地 P9 Installation 保持兼容。
- API：只新增 `POST /api/v1/account-installations/binding-challenges` 与 `POST /api/v1/account-installations/bindings`，两者只接受账号 access bearer；请求体没有账号或 Installation ID，不存在配对码、设备轮询或审批接口。
- App：`login_product_account` 在保存账号 Session 和返回安全 React 快照前，使用生产 Ed25519 设备身份完成 challenge 签名并把 fresh/rotated `atdc1` 写入 Rust 私有设备凭据 vault；绑定或落盘失败时撤销刚创建的账号 Session，不向 WebView 返回任何 bearer。
- 真实边界：只允许登录账号自动绑定当前 Installation；不使用配对码、设备轮询、匿名设备申请或后台逐设备审批。
- 失败矩阵：已覆盖首次绑定、同账号轮换、跨账号拒绝、签名/载荷篡改、challenge 过期/重放、同账号同设备 pending challenge 并发单赢家、账号停用、Installation 吊销、owner 直接改绑、旧凭据/既有 Session 失效和秘密零暴露。
- 验证：`uv run --project backend pytest -q backend/tests` → `2293 passed, 5 skipped`；`uv run --project backend ruff check backend/src backend/tests`、`ruff format --check`、`mypy backend/src` 全绿；账号绑定定向 PostgreSQL/API/应用测试全绿。
- 验证：Frontend `167` 项 Node 契约、`252` 项 Vitest、ESLint、TypeScript、OpenAPI snapshot/生成 DTO、默认与 customer-demo 构建、production boundary 全绿。
- 验证：Rust `cargo test --all-targets --all-features`、全目标全特性 Clippy `-D warnings`、Rustfmt 全绿；隐藏真实 macOS Tauri/WKWebView 账号门禁回归 `1 passing`。
- 清理：Pytest 隔离 PostgreSQL 容器由 fixture 回收；绑定测试只清理自身 challenge/Session/凭据/Installation 并保留 append-only 审计；Tauri Driver、App 与 WDIO 临时目录均已退出/回收；未使用真实账号、外部浏览器或云端部署。
- 文档：本任务记录独立于短路线图。
- 遗留：认证运维账号入口、用户设备列表/吊销和完整真实 Tauri 登录至停用/吊销纵向矩阵按依赖留给 U9-06。
