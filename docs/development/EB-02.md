# EB-02 完成证据

> 状态：🚧 实现中
>
> 日期：2026-07-23
>
> 提交：本文件所在的 EB-02 任务提交

## RED

先运行 `python3 scripts/run_eb_02_acceptance.py`，因缺少共享 Chromium
验证契约而按预期失败；任务没有从文档结论倒推“共用可行”。

## GREEN

已实现版本锁定、能力页面、三方进程/Profile/控制租约探针、134 项目录单帧
渲染、12 套风格单帧渲染和双平台 GitHub Actions 门禁。macOS arm64 使用
Chromium 149.0.7827.55 的真实进程验证已经通过；Windows x86_64 证据仍待
远端任务收集，因此本任务尚未完成。

## 失败矩阵

- 错误 Chromium 版本、多个 Chromium 可执行文件或出现
  `chrome-headless-shell`：立即失败。
- Browser Use 的 `executable_path`、随机 loopback CDP、可见 Playwright、
  渲染进程、媒体或图形能力任一失败：立即失败。
- 134 项目录、12 套风格、独立 Profile 或独占控制租约数量不符：立即失败。

## 正常用户路径验收

不适用。EB-02 是正式功能实施前的双平台浏览器兼容门禁，不新增用户入口。

## 真实边界

只有 macOS arm64 与 Windows x86_64 的真实进程证据都通过，才能标记完成。

## 清理

每轮临时 Profile、静态站点、渲染项目和浏览器进程均在探针结束后关闭并删除；
`.local/eb-02` 仅保存被 Git 忽略的本机调试证据。

## 文档变化

新增共享浏览器验证契约、能力页面、Python/Node 锁文件、渲染探针、验收脚本和
双平台工作流；专项 Roadmap 当前状态改为“实现中”。

## 遗留项

等待 Windows x86_64 实机结果；收到双平台证据、核验摘要并完成最终验收后再
改为“✅ 已完成”。
