# EB-02 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-24
>
> 提交：本文件所在的 EB-02 任务提交

## RED

先运行 `python3 scripts/run_eb_02_acceptance.py`，因缺少共享 Chromium
验证契约而按预期失败；任务没有从文档结论倒推“共用可行”。

## GREEN

已实现版本锁定、能力页面、三方进程/Profile/控制租约探针、134 项目录单帧
渲染、12 套风格单帧渲染和双平台门禁。macOS arm64 与 Windows x86_64 均使用
Chromium 149.0.7827.55 / revision 1228 的真实进程完成验证。

2026-07-24 在 Windows 11 x86_64 实体机执行
`scripts/validate_shared_chromium.py --browser-root ... --platform-id windows-x86_64`
GREEN：headed Playwright、Browser Use `executable_path`、随机 loopback CDP 和
独立 HyperFrames 渲染共用同一份 `chrome.exe`；三套并发 Profile 互异，134 个目录项、
12 套风格、字体/图片/视频/音频/Lottie/Canvas/WebGL/WebGPU、透明画面和横竖屏均通过。
证据文件 SHA-256 为
`3bab78042c135143377fc9791ed2ed54cd4aa859a294fa99628e37761cdbb161`，浏览器可执行文件
SHA-256 为 `b798f9e53a98d29eb7f36f8c409f905d3184780a04d2bcb56989067194784bd1`，
134+12 结果摘要为 `91c6bbc162cfc0cb2ed315cecd359b7ffdd65c3fe3be6f574a1b2e4e8abab8cf`。

## 失败矩阵

- 错误 Chromium 版本、多个 Chromium 可执行文件或出现
  `chrome-headless-shell`：立即失败。
- Browser Use 的 `executable_path`、随机 loopback CDP、可见 Playwright、
  渲染进程、媒体或图形能力任一失败：立即失败。
- 134 项目录、12 套风格、独立 Profile 或独占控制租约数量不符：立即失败。

## 正常用户路径验收

不适用。EB-02 是正式功能实施前的双平台浏览器兼容门禁，不新增用户入口。

## 真实边界

macOS arm64 与 Windows x86_64 的真实进程证据均已通过；本任务不代表后续正式包
签名、公证或干净机用户入口验收。

## 清理

每轮临时 Profile、静态站点、渲染项目和浏览器进程均在探针结束后关闭并删除；
Windows 复验后没有残留本轮 `chrome.exe`。`.local/eb-02-windows` 仅保存被 Git
忽略的下载缓存与本机证据，未写正式资源目录。

## 文档变化

新增共享浏览器验证契约、能力页面、Python/Node 锁文件、渲染探针、验收脚本和
双平台门禁；Windows 证据摘要写回契约，专项 Roadmap 当前状态改为“已完成”。

## 遗留项

历史 GitHub Actions 运行 `29947376456` 在分配 runner 前同时拒绝 macOS 与 Windows
Job，两个 Job 都没有执行步骤。Check Run `89016080953`、`89016081071` 的官方
注释均为账号近期付款失败或 Actions 消费额度不足；这不是产品代码或验证脚本
失败，因此没有把该运行冒充平台证据。

2026-07-23 用户决定不依赖 GitHub Actions，Windows 证据改由实体机补齐；该遗留项已于
2026-07-24 闭合。Windows 首轮还真实暴露并修正了 `chrome.exe --version` 不退出、
Browser Use 构造时继承 SOCKS proxy 但未安装 `socksio`，以及构造失败前 Playwright
context 未进入 finally 清理的问题；当前以 PE `ProductVersion`、构造期代理环境隔离和
全阶段 finally 关闭完成 fail-closed 修正。EB-04 的 Windows 专属门禁现已解锁。

## 2026-07-23 探针参数修复

`chromium_args()` 补 `--use-mock-keychain`（macOS 钥匙串隔离，见 BU-02 同日缺陷
记录）；2026-07-24 Windows 补证已使用该参数并通过。
