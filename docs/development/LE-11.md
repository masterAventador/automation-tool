# LE-11 本地音频管线

用户可操作：否
证据类型：分层实现

## 日期与状态

- 开工日期：2026-08-01
- 当前状态：T1～T5 已完成；顶格 `🔍 待验收`
- 前置：LE-10 本地画面管线 T1～T7 已完成

## 开工前【先定】

「原声处理方式」采用 `TimelineClip` 上的强类型字段，不增加第六种轨道，也不收窄设计的
三态承诺。该字段只允许在 `AMBIENT` 原声 clip 上出现，三态为：

1. `AUTO_DUCK`：先应用 clip 的 `gain_db` 基准，再以旁白作为 sidechain 压低；默认值；
2. `FIXED_VOLUME`：只应用 `gain_db`，不经过 sidechain，也不作为 sidechain 源；
3. `MUTED`：保留 Timeline 中的用户选择和基准音量，但渲染时不排入任何混音分支。

`NARRATION` 只作为 sidechain 源并进入最终混音；`MUSIC` 继续经过旁白闪避；无有效音频的
视觉素材不生成 `AMBIENT` clip。把 mode 放在 clip 而不是轨道上，才能在同一原声轨逐片段
切换，且静音不需要删除 clip、丢掉用户设置。

首期所有音频先统一为 48kHz、stereo、`fltp`。每段严格按 Timeline 的 source window 取片、
按 start 延迟、按 duration 裁切，轨道补静音并裁到画面目标时长；最终成片恰好一个 H.264
视频流和一个 AAC 音频流，音轨时长与 LE-10 帧栅格成片时长一致到容器毫秒精度。

## Task 进度

| Task | 内容 | 状态 |
| --- | --- | --- |
| T1 | 领域/API/存储表达原声三态，默认自动闪避 | ✅ 已完成 |
| T2 | 无路径音频计划、投影与 sidechaincompress 编译 | ✅ 已完成 |
| T3 | `has_audio` 绑定门禁、无声素材不排 ambient、三态分流 | ✅ 已完成 |
| T4 | 48kHz 归一、受控执行及音轨时长/形状核验 | ✅ 已完成 |
| T5 | 三态真实听感/电平证据、双平台候选与 LE 收口 | ✅ 已完成 |

## T1 RED 目标

1. 新增闭集 `OriginalAudioMode`，只含 `auto_duck/fixed_volume/muted`；原始字符串不得冒充。
2. 每个 `AMBIENT` clip 必须显式带 mode；其他四种轨道必须为 `None`。`gain_db` 继续表示
   三态共同的基准值，`muted` 不删除 clip。
3. Timeline API 用 `originalAudioMode` 严格收发，PostgreSQL JSON 文档用
   `original_audio_mode` 往返；未知/错误类型 fail closed。
4. 本地 Timeline 生成器只给 `has_audio=true` 的原声生成 `AMBIENT + AUTO_DUCK`；无有效
   音频仍不生成原声轨，不把静音和“素材没有声音”混成一个事实。
5. 同步 OpenAPI/前端生成契约，领域、API、仓储与生成契约均有漂移守卫。

首次聚焦运行必须失败于 `OriginalAudioMode` 尚不存在；确认 RED 后才改生产模型。

## T1 RED、GREEN 与功能验收

领域测试首次收集精确失败于 `OriginalAudioMode` 不存在。领域 GREEN 后，API 纵向用例携带
`originalAudioMode=fixed_volume` 首次得到 422；API GREEN 后，仓储旧 fixture 首次因 JSONB
缺新键而拒绝；本地 Timeline 生成器也首次因 ambient 没有 mode 得到领域拒绝。OpenAPI
快照最后精确报告应用契约漂移。五层都由目标能力尚未实现导致，不是环境或既有失败。

领域新增闭集 `OriginalAudioMode`，只允许 `auto_duck/fixed_volume/muted`。每个 AMBIENT
clip 必须持有强类型 mode，其他轨必须为 `None`；三态都保留来源窗和 `gain_db`，因此 muted
不会删掉用户设置。初始 Timeline 生成器为原声 clip 显式选择 `AUTO_DUCK`，纯旁白计划仍
没有 ambient 轨。API 以 `originalAudioMode` 往返，仓储以 `original_audio_mode` 写入 JSONB；
OpenAPI 快照与生成 TypeScript 同步。

提交前结构审查发现一项升级风险：历史 JSONB 没有新键，若直接把 dataclass 字段集当唯一
reader 键集，所有旧 Timeline 都会被判坏。修复先用真实旧形状得到 `1 failed`，再只接受
“当前完整键集”或“恰好少这一个 LE-11 键的旧键集”；旧 AMBIENT 映射为产品原有的
`AUTO_DUCK`，其他轨映射 `None`，任何其他缺键/多键继续 fail closed。writer 永远写新形状。

专项与覆盖率合跑 `285 passed`，5 个改动生产模块合计 503 条语句、142 个分支全部 `100%`；
Control Plane 受影响大回归 `2037 passed, 1 skipped`。真实 PostgreSQL JSONB + HTTP 纵向
`38 passed`，其中 `fixed_volume` 真写入数据库并按枚举身份读回；OpenAPI 两条前端聚焦门禁
通过，Ruff、严格 mypy、`git diff --check`、路线图计数与 51 项证据深度门禁全绿。

前端全合约的诚实结果为 `270 passed, 2 failed, 5 skipped`：两项失败分别是生产组合根仍用
sessionStorage 视频剪辑网关、遗留 video-editing WDIO spec 没有执行器，均为路线图已明确
归 LE-17 的当前红项；T1 没有把它们误报为通过，也没有越过任务边界提前修复。

实现检查点 `b9eb3e2d` 推送后完成提交级结构化 Review，复审三态闭集、轨道专属约束、
muted 状态保留、API 422 边界、JSONB 当前/历史精确形状、领域完整重建和生成契约；没有新增
finding。T1 正式完成。

## T2 RED 目标

1. 新增版本化无路径 `local-editing.audio-render.v1`：项目/Timeline 身份、修订、总时长和
   NARRATION/AMBIENT/MUSIC clip；每段只含 MaterialId、毫秒窗、`gain_db` 与 ambient mode，
   不含路径、codec、has_audio、命令或诊断。
2. Control Plane 从完整重建的真实 Project + Timeline 投影音频计划；视觉/字幕不进入协议，
   三种音轨缺失均合法，raw enum、list、身份或嵌套篡改固定拒绝。
3. Executor 编译每段 `atrim + asetpts + volume + aresample/aformat + adelay`，同轨先混合；
   NARRATION 是唯一 sidechain，AUTO_DUCK ambient 和 MUSIC 经 `sidechaincompress`，FIXED
   ambient 绕过 sidechain，MUTED 不进入图。
4. 没有旁白时，待闪避分支直接保持基准音量，不伪造 sidechain；最终所有可听分支 `amix`，
   `apad + atrim` 精确到 Timeline 总长。filter graph 不含路径或用户文本。
5. T2 只交付协议、投影和确定性 compiler；素材 `has_audio`/本地绑定归 T3，受控运行、AAC
   与 ffprobe 归 T4。首次 RED 必须失败于音频协议尚不存在。

## T2 RED、GREEN 与功能验收

协议测试首次收集精确失败于 `AudioRenderPlan` 不存在；Control Plane 纵向测试随后失败于
`create_local_editing_audio_render_plan` 不存在；Executor 测试最后失败于
`audio_rendering` 模块不存在。三个 RED 都由目标能力缺失导致。补齐实现后，又用 513 段
单轨计划证实只设总上限会错误接受超长单轨，得到实际失败再加每轨 512 段的独立上限。

交付版本化、无路径的 `local-editing.audio-render.v1`。计划只承载 UUID 身份、修订、总时长、
三种音频轨、来源窗、时间窗、基准增益和 ambient 三态；构造器完整重验嵌套值，拒绝 raw
enum、list、跨轨重叠、非规范轨序和篡改后的对象。Control Plane 从完整重建的 Project 与
Timeline 投影，按 narration/ambient/music 规范排序，排除视觉和字幕，视觉-only 工程得到
合法空音频计划。

确定性 compiler 为每段生成 `atrim + asetpts + volume + aresample + aformat + adelay +
apad + atrim`，先合并轨内片段。旁白是唯一 sidechain；AUTO_DUCK ambient 与 MUSIC 经过
`sidechaincompress`，FIXED ambient 绕过，MUTED 完全不进入输入或滤镜图；没有旁白时不
伪造 sidechain。最终分支统一补齐并裁到 Timeline 时长。返回值只含 MaterialId、滤镜图和
输出 label，`repr`、异常和滤镜图均不泄露路径或用户文本。

专项合跑 `238 passed`；协议、投影和 compiler 合计 428 条语句、104 个分支全部 `100%`，
其中 compiler 单模块 79 条语句、24 个分支 `100%`。macOS 随包候选用真实三音源执行图，
得到 `48000Hz, stereo, 1.000000s, 192078 bytes`；Windows 在 `F:\automation-tool` 同步同一
提交后得到相同媒体形状与字节数。两端既有 FFmpeg 8.1.2 候选均通过扩展后的 27 项精确
滤镜能力合约。最终 Windows 三组单元专项再次得到 `22 passed`。

代码审查共关闭三项问题：第一轮发现新增的 9 个生产滤镜尚未进入随包能力合约，先以精确
集合门禁得到 RED，再同步 contract/checker/self-test；第二轮发现协议只限制总片段数、未
限制单轨，以上述 513 段实际 RED 修复；提交后复审发现 compiler 重建计划时会把被篡改成
list 的 `clips` 转回 tuple，等于替调用方洗白容器漂移，先新增实际 RED，再在
`2716f0b8` 改为把原容器交给协议边界重验。修复后的完整 diff 再审，无新增 finding。T2
实现检查点为 `71de0a7f`，审查修复检查点为 `2716f0b8`，均已推送。

## T3 RED 目标

1. 新增只存在于 Executor 本机的素材绑定，把 `MaterialId` 精确关联到绝对来源路径与
   LE-07 已探测的 `has_audio` 事实；绑定、异常和 `repr` 不得泄露路径。
2. 绑定集合必须与计划中的素材身份精确对账，重复、缺失、多余、raw 容器和篡改对象均
   fail closed；实际 FFmpeg 输入顺序必须与 T2 compiler 的 MaterialId 顺序严格一致。
3. `has_audio=false` 的 AMBIENT 不排输入、不进入滤镜图；AUTO_DUCK/FIXED_VOLUME/MUTED
   继续按三态分流，MUTED 即使文件不存在也不得被进程触碰。
4. NARRATION 或 MUSIC 被计划为可听但 `has_audio=false` 时固定拒绝，不能静默产出缺轨成片；
   错误只含稳定 code 与 MaterialId，不含路径。
5. T3 只负责本地事实与路径绑定及输入门禁；进程、AAC 参数、发布和 ffprobe 核验仍归 T4。
   首次 RED 必须证明当前没有绑定入口，或当前会错误把无声 ambient 排进输入。

## T3 RED、GREEN、代码审查与功能验收

T3 测试首次收集精确失败于 `AudioRenderBindingRejected` 等本地绑定入口不存在，确认当前只有
无路径 compiler、没有任何 `has_audio` 门禁。GREEN 新增 Executor-only
`AudioRenderSourceBinding`，只持 MaterialId、绝对路径和 LE-07 已探测的布尔事实；路径从不
进入计划、滤镜图、异常文本或对象 `repr`。绑定集合必须是 tuple，并与计划中去重后的全部
MaterialId 精确相等；重复、缺失、多余、非法 UUID/路径/布尔类型及篡改对象均固定拒绝。

绑定按计划顺序生成 `-i` argv，而不是相信绑定传入顺序。MUTED 始终被 compiler 排除；
`has_audio=false` 的 AMBIENT 由本地事实门禁额外排除，AUTO_DUCK 与 FIXED_VOLUME 的有声
素材继续进入各自分支。NARRATION 或 MUSIC 若无声则以 `source_has_no_audio + MaterialId`
拒绝，不静默降级成缺轨成片。不存在的 muted/无声 ambient 路径只作身份绑定，不执行 stat、
open 或 FFmpeg 输入。

macOS 真实验收把 narration 指向真实 44.1kHz WAV，把 silent ambient 和 muted 都指向根本
不存在的文件；公开绑定结果只有 1 个输入，真实 FFmpeg 成功得到 48kHz stereo、1000ms、
192078 bytes，证明两个排除素材没有被进程触碰。Windows 真机同步 `b8f5c0eb` 后，41 条
音频协议/投影/绑定专项全绿；定位并校验 `vf04-windows-20260724-r5` 随包候选后，同一两条
真实媒体验收均通过，receipt 同为 `1input,192078bytes`。第一次 Windows 真实用例失败仅因
误用了不存在的 debug 候选路径，修正到已验 r5 候选后复验通过，未把环境路径问题冒充产品
失败或代码修复。

专项最终为 macOS/Windows 各 `41 passed`；新增后 audio module 为 157 条语句、44 个分支
全部 `100%`。Ruff、严格 mypy、`git diff --check` 全绿。提交前结构审查逐项复核绑定精确
集合、输入顺序、重复 MaterialId、多种无声轨语义、三态分流、路径泄漏和篡改重验；实现
检查点 `b8f5c0eb` 推送后再次按完整提交 diff 复审，并在 Windows 真实执行验证进程边界，
无新增 finding。T3 正式完成。

## T4 RED 目标

1. 把 LE-10 画面命令与 T3 音频绑定装配为唯一受控 FFmpeg 命令，显式输出 H.264 + AAC；
   不经 shell、不查 PATH、不读环境变量，输入编号和 filter graph label 必须精确对账。
2. 有可听音频时固定 AAC、48kHz、stereo；音频计划为空或全部被本地事实排除时明确生成
   视频-only 成片，不伪造静音轨。进程输出、超时、取消和工作区错误映射保持有界且路径安全。
3. 复用受控执行、来源快照、暂存和原子发布边界；任一音频来源在执行前后发生身份/大小/
   mtime 漂移时拒绝发布，失败不得覆盖既有成片或留下临时产物。
4. ffprobe 必须断言恰好一个视频流；有声计划再断言恰好一个 AAC 音频流、48000Hz、stereo，
   且音轨/容器时长与 LE-10 帧栅格目标在容器毫秒容差内一致；多轨、缺轨、错 codec/采样率/
   声道/时长均 fail closed。
5. 首次 RED 至少分别失败于当前没有音视频联合命令入口和当前核验器不认识音频形状；T4
   只交付执行与机器可核验形状，三态听感/电平对比和 LE 双平台收口归 T5。

## T4 RED、GREEN、代码审查与功能验收

联合命令测试首次收集精确失败于 `audiovisual_rendering` 模块不存在；第二段执行测试精确
失败于 `execute_audiovisual_render` 不存在。两个 RED 分别钉住命令装配和受控执行缺口。
GREEN 先调用 LE-10 视觉 compiler，按真实视觉/字幕 `-i` 数量计算首个音频输入编号，再把
T3 的本地绑定输入和路径无关音频图合入唯一 `filter_complex`。有声计划显式双 map，并固定
`libx264 + aac/192k/48000Hz/stereo`；空计划或被本地事实全部排除的计划保留显式 `-an`，
不伪造静音轨。命令始终是 argv tuple，不经 shell，不发现工具。

执行层没有复制第二套生命周期，而是把 LE-10 原有受控进程抽成内部共享路径：视觉、字幕
和所有可听音频来源在运行前后均做身份/大小/mtime 快照对账；取消、超时、输出上限、工作区
失败、临时文件清理和硬链接原子发布继续走同一边界。被排除的 silent ambient/muted 不要求
stat，路径不存在也合法。ffprobe 现在要求恰好 1 个 H.264 视频流，并按命令决定再要求恰好
1 个 AAC 音频流；音频必须 48000Hz、2 声道，音轨和容器时长都与帧栅格目标相差不超过
1ms。错 codec、采样率、声道、时长、多轨、缺轨和来源漂移都拒绝且不发布。

macOS 随包候选真实联合成片为 `h264+aac,720x1280,30fps,30frames,1000ms,48000Hz,stereo,
28479bytes`；Windows r5 候选为同样媒体形状、`28487bytes`。两端还并跑原有复杂视觉、纯
音频图和排除不存在音频路径用例，共 `8 passed`；Windows 受影响专项 `210 passed`，macOS
同组也是 `210 passed`。新增 audiovisual composer 与扩展后的受控执行合计 479 条语句、
142 个分支全部 `100%`；Ruff、严格 mypy、`git diff --check` 全绿。

代码审查以实际 RED 关闭两项 finding：一是视觉 compiler 报随包工具不可用时，联合层最初
会折叠为普通非法请求，导致上层丢失可诊断的稳定 code；新增失败用例后贯穿保留
`tool_unavailable`。二是 receipt 用 set 成员关系校验三项音频形状，若边界收到 list 等不可
哈希篡改值会抛裸 `TypeError`；真实 RED 后改为不依赖哈希的精确 tuple 比较，恢复
`output_invalid`。实现检查点 `2c0f91e2` 推送后按完整提交 diff 与 Windows 真机进程边界
复审，无新增 finding。T4 正式完成。

## T5 RED 目标

1. 用真实旁白和真实原声生成 AUTO_DUCK、FIXED_VOLUME、MUTED 三份 H.264 + AAC 成片；来源、
   时间窗、基准增益和编码参数相同，只有 mode 不同，保证对比可归因。
2. 把成片音轨解码为 48kHz stereo PCM，在旁白出现前和出现期间测原声频率分量：自动闪避
   期间必须显著下降、固定音量必须保持基准、静音必须接近噪声底；同时证明旁白三份都存在。
3. 电平阈值、窗口、频率和判定写成可重复的自动验收，不以“进程成功”冒充听感；保留三份
   可播放真实媒体的 hash/大小/媒体形状 receipt，诊断和日志不得带用户路径。
4. macOS 与 Windows 已验随包候选运行同一验收，能力 contract、版本、许可和真实输出均过；
   平台允许编码字节/hash 不同，但三态电平关系和媒体形状必须一致。
5. T5 完成后运行 LE-11 受影响回归、完整 backend integration 与路线图/证据门禁，做最终
   code review；正式 App/安装包用户路径尚依赖 LE-17/19/22/23，因此 LE-11 顶格保持
   `🔍 待验收`，不虚标用户验收完成。

## T5 RED、GREEN、双平台听感证据与 LE 收口

三态真实验收使用同一 2 秒 300Hz 原声、同一 600ms 1000Hz 旁白、同一画面、时间窗、
`gain_db=-6dB` 和编码参数，只改变 ambient mode。首跑不是形式上的绿：AUTO_DUCK 在旁白
期间的原声频率幅度为 `0.028755`，FIXED_VOLUME 为 `0.044249`，只压到约 65%，未达到
“至少减半”的既定可听区分，验收真实 RED。没有放宽断言，而是把 sidechain threshold 从
`0.05` 调到 `0.02`；复跑自动闪避降为 `0.012876`，即固定音量的约 29%（约 -10.7dB），
旁白出现前仍与固定音量同为 `0.044259`。

最终 macOS 与 Windows 解码 PCM 的频段结果完全一致：

| mode | 原声 0.2～0.5s | 原声 0.9～1.1s | 结论 |
| --- | ---: | ---: | --- |
| AUTO_DUCK | 0.044259 | 0.012876 | 旁白出现时显著闪避 |
| FIXED_VOLUME | 0.044259 | 0.044249 | 保持基准音量 |
| MUTED | 0.000000 | 0.000000 | 原声不进入成片 |

三份成片的 1000Hz 旁白最小幅度均为 `0.088294`，最大/最小差小于 15%，证明三态没有误删
旁白。三份都由正式受控入口得到 H.264 + AAC、720×1280@30、60 帧、2000ms、48kHz stereo。
macOS receipt：AUTO `52030 bytes / 6a502a9a…d334dd3657`，FIXED `53367 bytes /
290c4550…5a5b8ae`，MUTED `22092 bytes / 4645e590…db81d4`；Windows receipt：AUTO
`52117 bytes / 96873b1c…a5aee32`，FIXED `53466 bytes / e7f0d24a…09c6be`，MUTED
`22042 bytes / cd121a04…9b88276`。平台编码字节可以不同，媒体形状和电平关系完全一致。

LE 结束的全受影响集合包含领域、API、真实 PostgreSQL、协议、投影、三态 compiler、本地
绑定、视觉/字幕联合命令、受控执行和三项真实媒体，结果 `502 passed`。按 LE 边界运行完整
`backend/tests/integration`：首次在沙箱因 loopback bind/浏览器进程统一被系统拒绝，转到
获准的非沙箱同命令后正式结果为 `460 passed, 17 skipped in 392.11s`；其中随包媒体用例因
全量命令未注入候选路径而按设计 skip，但均已在两平台单独真跑。路线图计数、51 项证据深度、
Ruff、严格 mypy、`git diff --check` 和各改动模块 100% 分支门禁均绿。

T5 Review 复核频率窗口、AAC 解码、模式唯一变量、阈值可归因、跨平台容差、receipt 隐私和
进程边界；发现验收中直接造音/解码的 subprocess 原先没有 timeout，工具异常时可能无限挂住。
提交实际修复 `5714912d` 后，所有这些子进程统一 30 秒上限，macOS/Windows 再验结果不变；
最终完整 diff 无新增 finding。三态实现/验收检查点为 `2dd24c39`，T5 正式完成。

LE-11 的五个 Task 已全部完成。正式 App 中的工作台入口、任务调度、安装包和用户可见验收
仍由 LE-17/LE-19/LE-22/LE-23 关闭，因此本 LE 按证据纪律顶格标为 `🔍 待验收`。
