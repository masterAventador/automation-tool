# LE-04 剪辑项目与任务状态机

用户可操作：否
证据类型：分层实现

## 日期

2026-07-29

## 提交列表

按时间顺序，共 18 条 = 起点 `a7a7aae`（计划提交，是区间基点本身）+ 区间 `a7a7aae..84ba7e9` 内属本线的 17 条。

**区间内共 19 个提交，另外 2 条属其他工作线、不在下表**：`dcfeb8e`（LE-07 线的门禁修正，本线 cherry-pick 过来）与 `cf5ae10`（LE-09 线的验收判据与字体现状更正）。三条线共享同一棵仓库的分支历史，所以区间里会混入他线提交——这是并行开线的常态，核对时按 `le-04` 前缀与内容归属判断，不能按区间条数。

```
a7a7aae docs(le-04): 实施计划 + 台账转实现中；§1 去掉已废弃线的提法
4bd2660 feat(le-04): 输出规格与字幕样式，宽高锁偶数、字体只收注册键
1cff869 fix(le-04): 标题长度上限挪到第一个真实使用者所在的 task
1c9092c test(le-04): 补齐字幕样式四个区间端点的接受用例
e53ef70 fix(le-04): domain 层 7 处正则结尾锚点改 \Z，守卫不再依赖调用动词
1e63211 feat(le-04): 剪辑项目聚合根，承载每次渲染继承的输出设置
d168b3b fix(le-04): 本模块的 _validate_text 去掉无调用者的 optional 参数
8510fb5 test(le-04): 补 title 的真值非字符串输入，钉住 isinstance 守卫
86cd12a docs(le-04): 并发规则改为按工作线计，登记三条线各自的 worktree
90db790 docs: CLAUDE.md 并发规则按工作线计，并明写「门禁不得宽于它所守护的规则」（与 86cd12a 是同一件事的两半）
96dde6f feat(le-04): 时间轴接上所属项目，LE-03 留的 owner 空补齐
d386b48 test(le-04): 钉住 LE-03 遗留的四条承重护栏与三处未闭合边界
032a17b feat(le-04): 剪辑作业状态机，六态闭图与非法转换拒绝
f11656d test(le-04): 用 match 属性钉住七处正则锚点，更正原来说错的机制
cbdff34 feat(le-04): 剪辑作业聚合根，状态与事实强耦合、转换走窄方法
971a695 test(le-04): 钉住四个跨类型 ID 护栏与领域异常基类
5cac9cb feat(le-04): 剪辑项目与作业转出口，台账收口
84ba7e9 docs: 台账加 §4.1，说明并行期间三份副本的计数只对本分支成立
```

## 各 task 的 RED/GREEN

- **Task 1（`OutputSpec`/`CaptionStyle` 值对象）**：RED — `pytest tests/unit/control_plane/domain/test_editing_project.py` 报 `ModuleNotFoundError: No module named '...editing_project'`；GREEN — 53 条用例通过，全量 domain 679 passed 无回归。
- **Task 2（`EditingProject` 聚合根）**：RED — 同文件报 `ImportError: cannot import name 'EditingProject'`；GREEN — 写完最小实现后覆盖率 98%（`_validate_text` 的 `optional=True` 分支无调用者），补 1 条直测私有函数的用例后 77 passed、100%/0 partial；全量 domain 708 passed。
- **Task 3（`Timeline` 接上 `project_id`）**：RED — `pytest tests/unit/control_plane/domain/test_timeline.py` 报 `TypeError: Timeline.__init__() got an unexpected keyword argument 'project_id'`（26 个失败）；GREEN — 补字段与校验后 111 passed，同时同步更新了一条对 `Timeline` 字段做逐字段全集比对的既有断言（被动跟随字段新增，不验证新行为本身）；全量 domain 713 passed。
- **Task 4（`EditingJob` 状态机）**：RED — `pytest tests/unit/control_plane/domain/test_editing_job.py` 报 `ModuleNotFoundError: No module named '...editing_job'`；GREEN — 22 条用例通过，变异自证发现 `can_transition` 一处零拒绝用例缺口并补测后 25 条全绿；全量 domain 744 passed。
- **Task 5（失败码与 `EditingJob` 聚合根）**：RED — 追加用例后报 `ImportError: cannot import name 'EditingJob'`；GREEN — 56 条通过，变异自证发现 `_moved_to` 内跳过时间戳格式校验会让裸 `TypeError` 逃逸（而非统一的 `InvalidEditingJobModel`），补测后 57 条全绿；全量 domain 782 passed。之后代码质量审查跑 60 个变异体，4 处跨类型 ID 护栏零拒绝用例缺口 + 1 处 `InvalidEditingJobModel` 缺 `ValueError` 基类断言，补 25 行测试后域内 787 passed。
- **Task 6（本任务，转出口与收口）**：RED — 在 `test_editing_job.py` 末尾加转出口守护测试，跑单测报 `AttributeError: module 'automation_tool.control_plane.domain' has no attribute 'EditingProject'`；GREEN — 把两个模块的公开名接入 `domain/__init__.py` 后该用例通过；全量回归见下。

## 四条门禁退出状态（本任务）

| 命令 | exit |
| --- | --- |
| `pytest -q`（全量） | 0（3686 passed，7 skipped，454.9s） |
| `automation-tool-export-openapi --output ../contracts/openapi/control-plane.v1.json --check` | 0（无 diff） |
| `automation-tool-export-executor-schema --output ../contracts/protocol/executor-v1.schema.json --check` | 0（无 diff） |
| `ruff format --check` / `ruff check` / `mypy`（domain 子集） | 均 0 |

全库 `ruff check .`：98 个既有错误，与改动前一致，未新增。
全库 `mypy`：17 个既有错误（跨 8 个文件），与改动前一致，未新增。

## 两个新模块的覆盖率数字（最终态）

- `editing_project.py`：Stmts 75 / Miss 0，Branch 24 / BrPart 0，Cover 100%，82 passed。
- `editing_job.py`：Stmts 107 / Miss 0，Branch 18 / BrPart 0，Cover 100%，63 passed。

复核命令（两模块合并 `TOTAL 182 / 0 / 42 / 0 / 100%`，145 passed）：

```bash
cd backend && .venv/bin/python -m pytest \
    tests/unit/control_plane/domain/test_editing_project.py \
    tests/unit/control_plane/domain/test_editing_job.py \
    --cov=automation_tool.control_plane.domain.editing_project \
    --cov=automation_tool.control_plane.domain.editing_job \
    --cov-report=term-missing --cov-fail-under=100
```

`editing_project.py` 的数字来回走过一趟，几个时点都记在这里，免得下一个人拿某一版数字对不上就以为文档在骗人。**最后一行的 hash 只能由紧随其后的一个提交填上——提交装不下自己的 hash**，这一点第一次写这张表时踩过：先写了一个尚不存在的 hash（差点又造一个核对不了的引用），改成占位符提交后取真值回填，而 `--amend` 又把 hash 换掉了，于是那一行指向了一个被 amend 弃掉的对象。所以自引用的行一律用后续提交回填，不要 amend。

| 时点 | Stmts | Branch | passed | 原因 |
| --- | --- | --- | --- | --- |
| Task 2 完成 | 75 | 24 | 79 | 初始实现 |
| `d168b3b` 之后 | 73 | 22 | 79 | 删掉 `_validate_text` 里无调用者的 `if value is None and optional: return`——正好 2 语句 2 分支 |
| `d1dcd8b` 之后 | 75 | 24 | 81 | 终审 I4 补的跨字段守卫「字号不得高过画幅」——同样是 2 语句 2 分支，外加 2 条用例 |
| `93ad496` 之后（现状） | 75 | 24 | 82 | 确认复审查出「越界一格」变异存活，补 1 条边界外用例；**生产代码未动**，故语句/分支不变 |

**此处曾把 73 归因为「第 1 条账的锚点更正——`\Z` 改回 `$`」，那是错的**：代码里的 `\Z` 一处都没改回 `$`（实测 7 处仍全是 `\Z`），第 1 条账那次 `\Z→$` 是 LE-09 子代理为证明「测试对锚点不敏感」跑的一次性变异实验，从未落进本分支；`f11656d` 只动了 6 个测试文件、零个源码文件，不可能改变语句数。这个错误出现在一段专门用来纠正此前错误的文字里，由 LE-04 终审实测查出；随后 `d1dcd8b` 改了模块却没同步这一行，把 73/22/79 留成了过期数字，由 T6 代码质量审查实测查出。**同一段落连着两次栽在「数字与它旁边的解释各自漂移」上，所以现在改成上面这张按提交排的表——每一行都能用 `git show` 单独核对。**

## 本计划「本计划有意不做的事」（整表照抄）

| 不做 | 理由 |
| --- | --- |
| `EditingJob` 不带 `kind` 字段 | 首期只有渲染一种作业。AI 起草作业（LE-16 产出 Timeline 草稿）落地时才知道它的真实形状，现在加一个只有一个取值的字段就是预测性抽象 |
| 字幕不含颜色字段 | 台账登记的基线是「字号/描边/行距/字体」四项。白字黑描边是短视频字幕的通行约定，首期没有让用户改颜色的用户故事 |
| 不做变速、不做画中画 | 沿用 LE-03 的首期锁定 |
| 模型调用类失败码不在本 task | LE-13/15/16 落地时按实际失败形态扩枚举，枚举是可扩展的 |
| `EditingProject` 不含素材清单 | 素材与项目的关联属仓储层（LE-05），领域对象不持有集合引用 |

## LE-03 终审登记的两处缺口，本任务认领了哪一处

LE-03 终审发现输出画幅/帧率/字幕样式基线三样是渲染必需项却无人认领。**本任务认领并落实了这一处**：`EditingProject` 现在承载 `OutputSpec`（画幅+帧率）与 `CaptionStyle`（字号/描边/行距/字体键）。

**「原声处理方式」三态开关不在本任务**，仍归 LE-11——那是音频管线的自动闪避/固定音量/静音三态，与本任务的输出规格、字幕样式基线是两件独立的事，本任务未涉及也未提前决定其形状。

## 字幕颜色有意不做

台账登记的基线是四项（字号/描边/行距/字体），白字黑描边是通行约定，首期没有让用户改颜色的用户故事。

## `EditingJob` 有意不带 `kind` 字段

首期只有渲染一种作业，AI 起草作业落地时才知道真实形状。

## 另外必须记进 LE-04.md 的账

（标题原写「四笔账」，加进第 5 条时就与内容脱节了——标题里的计数是同一个数字的第二处存放，正是台账规则禁止的形态，故去掉。）

1. **我在 T1 那轮关于正则锚点的说法是错的，已更正。** 当时把 domain 层 7 处 `^...$` 改成 `^...\Z` 并让 5 个模块各加一条「结尾换行拒绝用例」，声称那些用例钉住了锚点。**并没有**——LE-09 线的子代理实测顶回：把 6 个模块全部 7 处 `\Z` 改回 `$`，整个域套件 **713 passed 零失败**，因为 16 个调用点全用 `fullmatch`，而 `fullmatch` 下 `$` 与 `\Z` 完全等价。只有 (`$` + `match`) 这一种组合会放过结尾换行。正确的钉法是**用 `match` 调用测那条属性**（`assert PATTERN.match("x\n") is None`），已在 `f11656d` 补上并逐处变异验证。原来那 5 条注释里说错的机制一并改掉了。

2. **`CANCELLING` 的 ack 责任归 LE-12。** T4 的审查转交的问题：一个从未被 worker 取走的作业进了 `CANCELLING` 之后由谁 ack 到 `CANCELLED`。答案已写进 `EditingJob` docstring——调度器在派发时撞见竞态需自己调用 `confirm_cancelled`；调度器崩溃/失联的兜底是带取消超时的对账把作业判 `FAILED`（`WORKER_LOST`），不构成死锁因为 `CANCELLING → FAILED` 是合法边。**实现调度与对账逻辑本身属 LE-12，本任务只登记责任归属。**

3. **`is_terminal` 对裸字符串静默返回 `False`**（T4 审查的低置信度提醒）。`False` 的含义是「仍在运行」，是不安全的那个方向。缓解是聚合根在持久化边界处就把状态解析成枚举——本任务已落实，但 LE-05 建仓储时要沿用。

4. **一次提交归属事故与它的教训。** 我在提交锚点改动时用了 `git add <目录>` 而不是逐文件，把 T5 子代理当时未提交的 188 行测试卷进了那个提交。功能未受损，但历史归属错了，已用 `git reset --soft` + 逐文件重新暂存拆成 `f11656d` + `cbdff34`，`--force-with-lease` 推送；两条并行分支均未含被弃提交，故无影响。

   **拆分未丢失任何内容**，测量结果固化如下（拆分前那个提交是 `9696772`，**它已被 force-push 弃掉、不可从任何 ref 到达**，所以这里写结论而不是写一条命令——在新 clone 里 `git diff 9696772 ...` 只会得到 `unknown revision`）：

   | 对比 | 结果 |
   | --- | --- |
   | `9696772` → `cbdff34` | 2 文件，+164 / −1（`editing_job.py` +155/−1、`test_editing_job.py` +9），是子代理在拆分**之后**继续写入的增量；那个 `−1` 是 `from typing import Final` 拓宽成 `Final, Never, final`，不是内容丢失 |
   | `9696772^{tree}` | `9ad5d312…` |
   | `cbdff34^{tree}` | `28ce4df2…` |

   两处更正都记在这里：原写「最终树逐字节相同」按字面不成立（两个树哈希不同，因为子代理在我拆分之后还在写），由 LE-04 终审查出；改成「拆分未丢失任何内容」后，**那句话又拿一个 dangling 对象当证据**，与 C2「引用的内容没进 git、clone 复现不出来」是同一形状，由终审复审查出——所以现在改成把读数写死在文档里。**这些读数也只在这台机器上可复核，且只到 reflog 过期为止**（`gc.reflogExpireUnreachable` 默认 30 天），之后连本机都验不了，所以它们就是最终记录本身，不是通往证据的指针。**规矩要写准确**：不是「等子代理跑完再提交」（它什么时候落盘看不见），而是**只要有子代理可能在同一棵树里写文件，控制方一律逐文件 `git add`，绝不用目录或 `-A`**。

5. **「变异全杀」属实，但变异集少了一维——同一形态今天四次复发，第四次就在写下这条规则的这段话里。** 终审 I4 补的跨字段守卫 `font_px > output.height`，我当时报「三个变异全杀（含边界收紧与 canary）」，经复核**属实**；漏的是 `height → width` 这一维，它**存活，81 passed 零失败**。原因是拒绝用例用的是**正方形** 128×128——两边无区分度，`200 > 128` 和 `200 > 128` 一模一样，而守卫的全部主张是关于高度的。

   覆盖率给不出任何提示：这条 `if` 的两条臂都跑到，读满 100% / 0 partial，**而「比的是哪一维」根本不是 coverage 测量的东西**。

   同一天的另外两次都在 LE-09：加 `range(0x00, 0x20)` 后 `range(0x00, 0x1F)` 存活（U+001F 无端点用例）；加 `range(0x80, 0xA0)` 后 `range(0x80, 0xA1)` 存活，而它会拒掉 **U+00A0（NBSP）**——一个三个真实随包字体全都覆盖、从网页粘贴的文案里极常见的字符，97 条用例一条不红。三次的作者声明都属实，三次都由第三方复审用变异抓出。

   **第四次**：我把下面这条固定动作写进本条账、同时按它改了夹具，**却没跑它自己要求的那条「越界一格」变异**。确认复审跑了：`> output.height` → `> output.height + 1` **存活**。进一步实测，存活窗口是 **k ∈ [1, 71] 全部 81 passed 零失败，k=72 才被杀**——因为拒绝用例（`font_px=200`, `height=128`）离边界整整 72 px，接受用例（`font_px=200`, `height=200`）正好压在边界上只能杀收紧方向，**放松方向从头到尾没人守**。已补 `test_a_caption_one_pixel_taller_than_the_frame_is_refused`（`1080×198` 配 `font_px=199`，高必须偶数故取 198/199），k=1/30/71 现在全部被它杀掉，`height→width` 被三条用例杀。

   **固定动作（后续 task 一律照做，本条自己就是反例）**：一次改动只要引入了有界的东西——`range(a, b)`、区间常量、跨字段比较——就必须
   - 有 `a`/`a-1`/`b-1`/`b` 四个端点用例；
   - **实跑**「越界一格」变异（`+1` 与 `-1` 两个方向都要），不是写下来就算；
   - 跨维比较的夹具必须让两维**有区分度**，正方形画幅、等长区间这类无区分度夹具本身视为缺陷；
   - **拒绝侧的夹具要贴着边界，不要离得老远**——离边界 72 px 的拒绝用例只能证明「远处会拒」，证明不了边界在哪。

6. **在别人正跑变异的树上提交是有风险的。** 确认复审跑到一半时我在同一棵 worktree 上提交了 `3fc8e23`（台账更正，只改 1 行文档）。复审者核实了它与自己无关、未夹带任何变异体（`editing_project.py` 的 sha256 与 pristine 基线逐位相同），但明确提醒：**若那次提交撞上它的变异窗口且用了 `git add -A`，变异体就进历史了**——正是第 4 条账要防的形态，只是角色对调了一次。规矩补齐：**只要有第三方正在这棵树上跑变异或审查，控制方要么等它返回，要么逐文件 `git add` 且提交前 `git diff --cached` 逐行确认**。

范围主张必须限定清楚（「后端」而不是「全项目」）——LE-03 在这上面栽过一次。

## 真实边界

- 纯领域层任务，不涉及数据库、REST API、Rust 或前端，不适用四层测试门禁中的 UI Harness / Tauri 原生 / Tauri 桌面 E2E 三层。
- 不含任何外部副作用、不含供应商概念，`InvalidEditingProjectModel`/`InvalidEditingJobModel`/`InvalidEditingJobTransition` 均为纯值校验异常。
- `domain/__init__.py` 转出口只做重新导出，不引入新行为；接线后 20 个 domain 模块全部完成再导出（此前 18/20，本任务补齐 2 个）。

## 失败矩阵

- 非法状态转换：`EditingJobStateMachine` 六态转换表覆盖，所有非法目标态均拒绝（`InvalidEditingJobTransition`）。
- 结构不变式：宽高奇偶、字体键路径穿越、描边吞字、标题长度、时间戳格式与时区、状态-事实耦合（终态必须/不得携带对应事实）、时序不倒退，均有拒绝与接受两侧用例。
- 跨类型 ID 混淆：`job_id`/`project_id`/`timeline_id`/`output_artifact_id` 四处 isinstance 护栏均有「结构合法但资源类型错误」的用例（971a695）。
- 调度器崩溃/竞态导致的 `CANCELLING` 悬挂：责任归属已写入 docstring，具体实现留给 LE-12（见第 2 条账）。

## 清理

未留下临时脚本、调试代码或注释掉的代码；task 1-5 过程中使用的归因打桩脚本、变异测试临时脚本均在验证完成后删除，未进入任何提交。

## 文档证据

- 本文件。
- `docs/local-video-editing-roadmap.md`：LE-04 行转 `✅ 已完成`，LE-09/LE-10 行补来源说明，§4 计数与 §5 当前下一步同步更新。
- `docs/superpowers/plans/2026-07-29-le-04-editing-project-and-job.md`：实施计划，含前 4 条账的原文（第 5 条是终审复审之后新增的，不在计划里）。
