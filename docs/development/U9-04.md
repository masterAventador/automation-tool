# U9-04 Tauri 登录与账号状态 UI

- 状态：✅ 已完成
- 日期：2026-07-23
- 提交：本任务提交
- RED：先新增 React 账号门禁与状态 UI、Tauri invoke gateway、Rust 私有 Session vault 和 IPC 安全边界测试；首轮分别准确失败于缺少 `AccountSessionGate`、账号 gateway 与 `account_session_vault` 模块，证明未复用设备 Session 或把 bearer secret 暴露给 React。
- GREEN：账号聚焦 Vitest `18/18`，Frontend Node 契约 `166/166`、Vitest 全量 `252/252`、ESLint、严格 TypeScript、OpenAPI 漂移、生产边界和默认/customer-demo 两种 Vite 构建全绿；Rust `cargo test --all-targets --all-features` 全绿，新增 vault `4/4`，control-plane 单元 `29/29`，全目标全特性 Clippy `-D warnings`、Rustfmt 与 `control-plane-e2e` 编译全绿。`pnpm test:account-session-tauri` 构建 `desktop-test-driver` 隔离 customer-demo App，WDIO 隐藏真实 macOS Tauri/WKWebView `1/1` 通过。
- 真实边界：`customer-demo` 组合根把账号 Gate 放在 `StartupGate` 外层；隐藏真实 App 在空 vault 下只显示登录/恢复页，固定 Rust `restore_product_account_session` 返回 exact `{state:"unauthenticated",account:null}`，刷新窗口后仍不挂载工作台。access/refresh 只存在 `product-account-session-v1` Rust 私有 secret，React 只接收 canonical active 账号投影；默认 P9 Profile 继续无产品账号门禁。
- 失败矩阵：覆盖检查态、未登录、离线 fail-closed/重试、错误凭据统一脱敏、运维恢复票据、改密后强制重新登录、注销、重启 refresh、refresh/session 失效清除、vault 损坏清除、固定 401 分类、网络结果不确定不可重试、未知响应字段和 WebView bearer 零暴露。
- 清理：真实 Tauri 验收窗口和 driver 已退出；隔离 AppData `com.aventador.automationtool.u904acceptance` 已移入废纸篓；未启动外部浏览器、云端部署、真实产品账号或平台副作用，没有残留相关进程。
- 文档：更新根/Frontend README、前端架构、工程结构、项目规则、路线图短台账和质量证据路径；156 份历史任务记录已逐字迁移到 `docs/development/<任务ID>.md`，路线图由 2996 行缩至约 570 行。
- 遗留：账号 Installation 自动归属与双门禁由 U9-05 实现；真实 Control Plane 登录/停用/吊销纵向桌面验收由 U9-06 完成，不用 Mock 冒充。
