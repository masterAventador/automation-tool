# LE-19 智能剪辑入口

用户可操作：是
证据类型：正式 App 纵向路径

## 日期与状态

- 开工日期：2026-08-01
- 当前状态：T1～T6 完成；T7 Harness 已实现，真实外部模型双平台验收待授权
- 前置：LE-16 分层生成器已完成，LE-18 正式素材库已完成

## 开工前【先定】

正式 App 当前只具备“导入素材 → 手工编辑时间轴 → 提交渲染”。LE-13～LE-16 已分别交付
素材理解、人声分析、文案分句/TTS、语义匹配/片段选择和路径无关 Timeline 草稿，但没有一条
生产装配链把这些能力接到冻结 Worker、Control Plane、Tauri 和工作台。素材导入当前只登记
ffprobe 事实；不能靠测试预置 `aiDescription`、转写或镜头边界，冒充真实用户路径已经接通。

本 LE 拆为七项。每项严格 RED → GREEN → 逐文件/提交级代码审查 → 专项功能验收；完整
`backend/tests/integration` 只在 T7 收口或跨层变更确有必要时运行。

| Task | 内容 | 状态 |
| --- | --- | --- |
| T1 | 智能草稿生成核心：复用 LE-13～16 公开边界，补真实可解码区间、分析缺口、旁白注册计划、阶段进度/协作取消与固定失败闭集 | ✅ 完成 |
| T2 | 冻结 Worker 协议与进程：认证 start/progress/cancel/succeed，成功后 commit、失败/取消后 abort 补偿，密钥/路径/文案不出私有边界 | ✅ 完成 |
| T3 | Control Plane 分析写回与旁白登记：素材理解/人声结果的受约束 API、原子仓储、OpenAPI、真实 PostgreSQL 与并发保护 | ✅ 完成 |
| T4 | Rust Worker client 与 Tauri 编排：分页取素材、模型配置、生成状态机、补偿、保存 Timeline，以及“一键直出片”复用同一生成结果后提交 | ✅ 完成 |
| T5 | Tauri commands 与前端 gateway/DTO：严格快照、轮询、取消、错误闭集、生产 feature gate 与无路径边界 | ✅ 完成 |
| T6 | 工作台智能剪辑入口：一句话、生成草稿/一键直出片、真实分阶段进度、取消与可恢复失败；生成草稿直接落入现有时间轴编辑区 | ✅ 完成 |
| T7 | 正式 App 正常/失败纵向验收、macOS/Windows 同 SHA 复验、品牌术语契约、完整集成、最终 Review 与路线图收口 | 🔍 待验收 |

## T1 RED 目标

1. 生产生成器只接收路径无关的素材事实；实际源路径仅由 Worker 私有注册表解析，并在使用前后
   核对同一文件身份。视频必须先真实解码证明可用区间，不能把容器头 duration 当成逐帧完备。
2. 素材缺描述/镜头边界或缺人声分析时，生成器通过既有 LE-13/14 公开实现补齐；人工描述受
   保护，不得被 AI 覆盖。所有视觉素材在首次文案模型调用前完成输入和本机前置校验。
3. 无人声素材严格走“分句 → TTS 真时长 → 语义匹配 → 等长片段 → 唯一分配”；有人声视频
   按素材输入顺序形成原声段，不做 TTS、不参与文案素材候选。
4. TTS 文件只写入本机私有 generation workspace；调用方预分配的旁白 MaterialId 与内容摘要、
   时长等路径外事实一一绑定。成功返回 LE-16 `LocalEditingTimelineDraft` 和旁白登记计划，失败/
   取消不返回部分草稿。
5. 进度阶段与比例单调、封闭且可验；取消在模型调用之间、TTS 句间、素材解码/分析循环与最终
   发布前均可观察。取消、素材不足、素材过短、无相关素材、配置缺失、上游拒绝和本机失败使用
   固定产品错误，不携带 prompt、转写、MaterialId、路径、模型请求 ID 或 traceback。
6. 首次聚焦运行必须精确失败于 T1 生产编排模块/公开入口尚不存在；确认捕获力后才创建实现。

## T1 RED 证据

首次运行 `tests/unit/executor/test_smart_edit_generation.py` 在收集期精确失败于
`automation_tool.executor.smart_edit_generation` 不存在，结果为 `1 error`。既有 LE-13～16
领域与协议类型均已成功导入，失败来自目标生产编排入口尚未创建，不是夹具或环境错误。

## T1 GREEN 与功能验收证据

- 新增 `smart_edit_generation` 封闭装配边界：混合原声/旁白、全原声免文案免 TTS、固定三类
  产品失败、单调阶段进度、协作取消、路径无关分析写回与旁白登记计划均由构造器约束。
- 新增 `smart_edit_media`：随包 FFmpeg 逐帧跑到流末尾，读取 `out_time_us` 形成真实可解码
  区间；容器声明 4 秒但实际只解出 0.4 秒时只发布 `[0, 400)`，不再冒充整段可用。
- 新增 `LocalSmartEditGenerationPipeline`：所有素材先完成注册表解析、完整 probe、不可变事实核对
  和视频解码，再允许第一次模型调用；人工描述不被覆盖，无声素材不会构造 VAD/ASR 下漏斗。
- TTS 在句间观察取消；第二句开始前取消会回滚第一句已生成文件，且不会调用第二次云端 TTS。
- T1 及 LE-13～16 相关专项回归：`401 passed`；严格 mypy 覆盖 8 个相关源/测试文件为
  `Success: no issues found`；Ruff check/format 全绿。
- 正式功能验收 `tests/integration/test_smart_edit_pipeline_real_media.py` 使用 App 随包
  FFmpeg/FFprobe 生成并探测 320×180、17 fps、2 秒 H.264 文件，再经真实注册表、完整逐帧
  解码和生产 prepare 管线，结果 `1 passed`，可解码区间精确为 `[0, 2000)`。

## T1 实现内审发现与修复

1. 分析写回计划最初只绑定 MaterialId，没有强制等于实际参与匹配的准备后快照；已增加完全
   一致性校验和反例，杜绝写回另一份描述/转写。
2. 本机 probe 等路径异常最初可能留在 Python `__context__`；已改成离开异常处理块后生成固定
   拒绝，并用含私密中文路径的 `OSError` 验证 cause/context 均为空。
3. 无声视频最初会提前构造可听语音分析工厂；已改成传入真正惰性下漏斗，`has_audio=false`
   时构造次数严格为 0。
4. 解码成功最初直接把容器 duration 当可用终点；已改为 FFmpeg 实际 `out_time_us`，短解码
   反例从错误 `[0, 4000)` 收紧为 `[0, 400)`。
5. 旁白登记最初没有单文件字节上限，也没有强制登记音频就是草稿实际引用音频；现已锁到
   `MAX_VOICEOVER_BYTES`，并校验唯一 sequence、MaterialId 和草稿引用顺序。
6. 提交级逐文件 Review 发现第一份素材调用视觉模型时，后续待理解素材可能还没有完成抽帧；
   已改为所有素材先完成 probe、解码和受控抽帧，再允许第一次模型调用，并加入双素材顺序反例。
7. 补齐图片素材专项路径：图片不进入视频解码或语音分析，用 1 ms 仅作为模型结果校验尺度，
   写回前丢弃该合成时长和镜头边界，Material 仍保持 `duration=None`、空镜头边界。

## T1 Code Review 结论

对任务提交 `68f1bd2a` 及上述 Review 修复逐文件检查了正确性、隐私异常链、取消/回滚、
macOS/Windows 路径与测试缺口；两轮 finding 均已转为捕获性测试并复验，最终无剩余
actionable finding。尝试启动独立 `codex review --commit 68f1bd2a` 时，执行环境因仓库内容外发
未获授权而拒绝 app-server，未发送任何代码；随后使用本地同提交结构化 Code Review 完成审查，
没有以跳过审查的方式收口。

## T2 RED 目标

1. 冻结 Worker bootstrap 接受受约束脚本模型配置，并为智能剪辑提供认证的 start/cancel 请求与
   progress/succeeded/failed/cancelled 响应；未知字段、乱序、重复终态和跨进程请求一律拒绝。
2. 每次生成拥有独立私有 workspace 与 pipeline；成功结果在调用方确认 commit 前不登记旁白，
   失败、取消、断连或 commit 超时统一 abort，清理生成文件和暂存映射。
3. Worker stdout 只输出固定协议 JSON；stderr、异常和 repr 不得携带 API key、用户 prompt、
   转写、MaterialId、主机路径、模型 request id 或 traceback。
4. 首个 T2 专项测试必须精确失败于智能剪辑 Worker 消息/入口尚不存在，随后才写实现。

## T2 RED 证据

首次收集 `tests/unit/executor/test_smart_edit_worker_protocol.py` 精确失败于
`local_editing_worker` 尚无 `LocalSmartEditStartCommand`；补到进程边界时又精确失败于
`automation_tool.executor.smart_edit_worker_process` 不存在。既有本地剪辑 Worker、LE-13～16
生成器和素材注册表均可导入，失败来自本任务声明的智能剪辑消息与入口，不是测试环境或夹具。

## T2 GREEN 与功能验收证据

- Worker bootstrap 只接受锁定百炼地址、三种在册模型和受约束 API key；智能剪辑 start、cancel、
  progress、prepared、commit、abort 与固定终态复用同一 HMAC 通道，单操作槽拒绝跨任务、乱序、
  重复终态和未知字段。
- 每个 job 只读 App 预建的 0700 私有目录，生产 pipeline 直接装配 LE-13～16 公开边界；生成结果
  与旁白先留在 staging，prepared 只发布路径无关 SHA-256。commit 前重新核对结果文档与每个旁白
  的 containment、类型、字节数和摘要，再一次性移动并原子登记；abort、取消、断连和 120 秒
  commit 超时均清理 request、result、workspace 和暂存映射。
- `MaterialPathRegistry.register_many` 在所有标识与文件通过校验后只替换一次 durable document；
  校验、替换或回滚任一步失败都不会暴露部分批次。异常、repr、stdout/stderr 只保留固定闭集，
  API key、prompt、转写、MaterialId、主机路径、模型请求 ID 和 traceback 均不越过私有边界。
- backend 本地编辑/素材/智能生成相关专项最终为 `769 passed`；Worker 网关、冻结准备和 motion
  workspace 兼容性脚本为 `202 tests OK (skipped=1)`；另有 motion entry/旁白/生成管线兼容回归
  `107 passed`。17 个变更 Python 文件通过 Ruff format，生产/测试关键面 Ruff check 全绿，8 个
  新增或核心变更源/测试文件严格 mypy 为 `Success: no issues found`。
- 真实 macOS arm64 冻结候选出厂审计为 `2139 files / 373334596 bytes / 116 dependencies /`
  `Python 3.11.15 / startup 12.952s`（`du` 361 MiB）；候选自身 `--probe`、
  `--probe-smart-edit-runtime` 和 `--probe-dependency local-editing-runtime` 全部返回 ready，真实加载
  NumPy 2.4.4、ONNX Runtime 1.23.2、随包 Silero 模型及许可证，不以源码环境冒充冻结验收。

## T2 实现内审发现与修复

1. 首轮逐文件审查发现普通本地渲染的兜底失败分支在接智能线程时被误删；已恢复固定
   `render_failed` 终态并复验原有本地剪辑回归。
2. job root 与 generated-materials 的父链最初可能经符号链接逃逸；已对真实路径、非 symlink、
   私有权限和 containment 逐层复核，并加入 job/generated root 反例。
3. 旁白摘要读取异常最初可能被当作校验通过；已改为默认无效，并用不可读摘要注入验证固定
   `commit_failed` 且异常链为空。
4. 注册表失败且 staging 回移也失败时，durable 音频最初可能残留；现会删除该 job 的 durable
   目录，并由双故障测试断言没有未登记文件。
5. 源码测试已绿但冻结 spec 最初没有 NumPy、ONNX Runtime、Silero 模型、许可证和百炼目录；
   已全部写入锁定依赖、spec、cache key 和候选自身运行时探测。
6. 首次真实构建发现 NumPy 2.5.1 不支持 Worker 的 Python 3.11，随后实测锁到 2.4.4；新增
   ONNX 依赖后安装分发数由 114 变成 116，也由真实 inventory 驱动更新，而非放宽审计。
7. smart worker 只为 workspace 误导入了完整浏览器动效 agent，冻结候选因此报
   `local-editing-runtime(MotionAuthoringRejected)`；已抽出轻量共享 workspace、把包入口改成惰性
   公开导出，并加冷启动门禁断言 smart import 不加载重型 agent。
8. 第二轮 Review 发现 prepared 后的非法旧 `worker.cancel` 会在严格协议拒绝后降级成 WebUI
   `worker.cancelled` 假成功；捕获性测试先红，现只有协议空闲时才允许 legacy fallback。
9. 第二轮 Review 还发现 prepared 后若 `result.json` 被替换，commit 只核对旁白仍会成功；现于
   移动和登记前重新核对私有结果文件的普通文件类型、目录边界、大小和 prepared SHA-256。

## T2 Code Review 结论

对 T2 全部协议、并发状态、文件事务、冻结装配和测试逐文件做了两轮本地结构化 Code Review；
上述 finding 均先转为捕获性测试或真实构建失败，再修复、专项复验并重建冻结候选，最终无剩余
actionable finding。T1 时外部 `codex review` 已因仓库内容外发未获授权而被执行环境拒绝，本任务
不绕过该限制或重复外发，使用同等检查项的本地审查闭环，不以跳过 Review 收口。

## T3 RED 目标

1. Control Plane 只接受路径无关、schema 严格且与当前 MaterialId/content digest 绑定的分析写回；
   AI 结果不得覆盖人工描述，过期素材快照、未知字段、跨项目 ID 和不完整语音事实必须拒绝。
2. 新生成旁白以调用方预分配 MaterialId、摘要、真实时长和受约束媒体事实批量登记；分析更新、
   旁白登记与生成结果确认必须位于一个仓储事务，任一冲突不得留下部分写入。
3. 内存仓储与 PostgreSQL 仓储使用同一乐观并发语义；并发素材变更、重复请求和失败重试可验，
   OpenAPI/DTO 与领域闭集同步，API 响应、日志和异常不得出现 Worker 路径或私有文案。
4. 首个 T3 聚焦测试必须精确失败于分析写回/旁白批量登记的 Control Plane 命令或端口尚不存在，
   确认捕获力后才创建实现。

## T3 RED 证据

首次收集 `tests/unit/control_plane/test_smart_edit_material_writeback.py` 精确失败于无法从
`control_plane.application.materials` 导入 `SmartEditMaterialAnalysisWriteback`；既有 Material
领域、单项理解/人声写回与 PostgreSQL 仓储均正常导入，失败来自本任务声明的批量命令尚不存在。

## T3 GREEN 与功能验收证据

- 新增路径无关、摘要绑定且 repr 全脱敏的分析/旁白批量命令；最多 32 份分析和 32 条旁白，
  MaterialId、旁白摘要和批次内身份均唯一，旁白只能是有真实时长、完整 `[0,duration)` 语音段和
  转写的音频 Material。Worker 的 `relativePath`、bytesWritten、prompt 等私有字段不属于 API DTO，
  嵌套未知字段由 Pydantic `extra=forbid` 在进入应用层前拒绝。
- 正式 `POST /api/v1/editing-materials/smart-edit-writebacks` 受 App session Installation scope 保护，
  分析逐项绑定当前 MaterialId/contentDigest；用户描述未变化时只允许独立人声三元组写回，AI
  结果遇到用户接管或过期快照返回固定 409，跨 Installation 目标按 404 拒绝。
- PostgreSQL 仓储按 MaterialId 排序 `FOR UPDATE`，先完整 hydrate/校验分析与旁白冲突，再在同一
  transaction 更新理解/人声列并批量 INSERT 旁白；精确相同的 MaterialId/摘要/事实重试幂等，
  不同身份或摘要冲突整体回滚。数据库错误、摘要和连接身份不进入固定公共错误。
- committed OpenAPI 已新增严格 request/response schema 与唯一 operationId
  `applySmartEditMaterialWriteback`，snapshot drift 测试绿色；正式响应只返回已有
  `EditingMaterialResponse` 领域事实，不返回 Worker 路径或用户生成 prompt。
- T3 领域、仓储验证、API、OpenAPI 与智能 Worker 专项为 `227 passed`；真实 PostgreSQL 定向验收
  `5 passed`：原子成功/精确重试、用户描述接管、触发器拒绝 narration INSERT 后回滚先前 UPDATE、
  正式 HTTP API 纵向落库，以及错误凭据无身份泄漏。11 个变更 Python 文件 Ruff format/check
  全绿，8 个核心源/测试文件严格 mypy 为 `Success: no issues found`。按用户约定未在单个 LE Task
  后重复跑完整 `tests/integration`，完整集成留到 T7。

## T3 实现内审发现与修复

1. 首版批量仓储为建索引键先 `MaterialId.parse`、随后才 `_hydrate`；损坏的存储 UUID 会绕过既有
   hydration 分类并被误报为数据库不可用。现先完整 `_hydrate`，再用已验证领域 ID 建映射，结构
   边界测试与损坏行语义保持一致。
2. 全套 Material 结构门禁发现 T2 smart worker 所在模块导入 `Material` 后调用了同名但无关的
   `str.replace/os.replace`，触发“禁止 dataclasses.replace 绕过用户描述保护”的保守扫描。没有
   放宽门禁；时间戳改为显式 UTC 规范化，目录事务改用目标不存在时等价且竞态更保守的
   `os.rename`，结构测试恢复全绿。
3. 上述目录调用点变化后，Review 发现“注册表失败 + 回移失败”测试仍 mock `os.replace`，已经
   不再真的注入第二重故障。已将 mock 移到生产实际 `os.rename` 调用点，复验 durable 音频仍被
   删除，不以误绿测试收口。
4. API Review 补齐跨 Installation、嵌套 `relativePath`、过期 digest、用户描述接管和精确重试
   反例；错误闭集动态枚举同步加入 `MaterialSnapshotConflict`，新失败不会从邻近类型猜 HTTP 状态。
5. 真实 PostgreSQL Review 用临时触发器在分析 UPDATE 之后拒绝旁白 INSERT，数据库回读确认两者
   均未落库；正式 ASGI 路由再以同一 schema 纵向写入真实库，避免“仓储测试绿但生产 API 未接线”。

## T3 Code Review 结论

对应用命令、严格 DTO、公共错误、SQL 行锁/事务、幂等冲突、OpenAPI 和 T2 兼容改动完成两轮本地
结构化 Code Review。上述 finding 均已转为捕获性测试或真实数据库故障注入并复验，最终无剩余
actionable finding；继续遵守 T1 已记录的仓库内容外发限制，不重试外部 `codex review`，审查没有
被跳过。

## T4 RED 目标

1. Rust Worker client 从受 Installation scope 保护的分页素材库和已锁定模型配置构造私有 job
   request，认证 start/cancel/commit/abort，严格验证 progress 单调、prepared/success HMAC、
   result SHA-256 与路径无关 schema；未知、乱序、跨 job 或重复终态一律失败关闭。
2. Tauri 编排使用一个显式生成状态机串起 Worker prepare → T3 原子写回 → Worker commit → 保存
   Timeline；任一步失败、取消、窗口销毁或超时均走对应 abort/补偿，不留下 CP 旁白、Worker staging
   或半保存 Timeline。生成草稿与“一键直出片”必须复用同一个 generation result，不得调用模型两次。
3. 草稿按现有 project/timeline revision 乐观并发边界保存；一键直出片只在同一草稿保存成功后复用
   既有 render submit，分页遗漏、素材快照冲突和旁白登记冲突使用固定可恢复错误且不携带路径、
   prompt、转写、API key、请求 ID 或 traceback。
4. 首个 T4 聚焦 Rust 测试必须精确失败于 smart-edit Worker client/编排入口尚不存在，确认捕获力后
   才创建实现；macOS 先跑专项，Windows 编译/真机行为在涉及平台分支时同步复验，最终双平台留 T7。

## T4 RED 证据

首次运行 `frontend/src-tauri/tests/smart_edit_runtime.rs` 精确失败于无法解析
`automation_tool_desktop_lib::smart_edit_runtime`；既有 Tauri library、Control Plane client 和本地
剪辑 runtime 均可编译，失败来自本任务声明的生产生成编排模块尚不存在。

## T4 GREEN 与功能验收证据

- Rust Worker client 只把 prompt 和素材事实写入 App 私有 asset root 下的 job 目录；Unix 权限为
  0700/0600，父链逐级拒绝 symlink，Windows 复用既有 reparse-point 拒绝。stdin 只传路径无关 jobId
  与 HMAC；progress/prepared/failed/cancelled、commit/abort/succeeded 全部绑定协议、Worker 版本、job
  和 detail，`result.json` 再以普通文件、4 MiB 上限与 SHA-256 复核。
- 生产状态机分页读取完整素材库并只投视觉素材，按当前模型 ID 与 API key 精确复用/重启冻结
  Python Worker；一个 generation 串起 prepare → T3 写回 → Worker commit → revision-bound Timeline
  save。“生成草稿”和“一键直出片”共用同一 Worker result，后者只在保存后调用既有 submit/dispatch，
  不会第二次调用模型。
- Timeline save 新增可选 `expectedRevision`；服务端以当前 revision 检查并依靠 immutable revision
  唯一键关闭并发窗口，首次保存使用 0。Rust 对 transport/protocol 不确定结果回读并逐字段确认恰为
  下一 revision 后才继续，避免误删已被 Timeline 引用的旁白。
- 写回 409 不再盲删碰撞 ID；只有已确认写回，或 outcome uncertain 后回读的 ID、摘要、时长、完整
  语音区间和转写均精确匹配时才允许补偿。鉴权/摘要异常、commit/abort 响应损坏和超时会强杀进程树
  并验证删除私有 staging；取消与已入队 progress/prepared 竞态最终稳定为 abort/cancelled。
- macOS 专项最终为 Rust Control Plane `42 passed`、真实 Worker 子进程事务 `4 passed`、公开 runtime
  `2 passed`、既有本地剪辑 runtime `1 passed`；`cargo clippy --lib --tests -- -D warnings` 与
  `cargo fmt --check` 全绿。后端写回/Timeline/API/OpenAPI 专项 `76 passed`，真实 PostgreSQL
  narration-only 写回 `1 passed`；9 个 Python 变更文件 Ruff format/check 全绿，6 个核心源文件严格
  mypy 为 `Success: no issues found`。按约定未在单 Task 后重复完整 integration。
- Windows 真机 `winbox` 在 `F:\automation-tool` 快进到同一提交 `cc7e26ed` 后，完整 Rust lib/tests
  clippy 编译门禁全绿，Control Plane `42 passed`、smart-edit runtime `2 passed`；平台专属 reparse-point
  分支由 Windows 编译覆盖，最终正式 App 双平台纵向行为仍按计划留到 T7。

## T4 实现内审发现与修复

1. T3 原 schema 要求至少一条 analysis，但素材事实已新鲜时 Worker 可合法只生成 narration；已允许
   narration-only、仍拒绝空批次，并补单元、HTTP 契约与真实 PostgreSQL 纵向验收。
2. Rust 默认 camelCase 会把字段写成 `progressPerMille`，与冻结协议的 `progressPermille` 不同；已显式
   rename，并由真实子进程协议测试捕获。
3. 初版只在保存前 GET revision，检查与 PUT 之间仍有竞态；已把 `expectedRevision` 下沉到后端服务和
   immutable revision 冲突处理，首存/过期/下一 revision 契约均有反例。
4. prepared 解析异常与 commit/abort 终止响应异常最初会留下活动 Worker 和 staging；已增加只针对
   当前 job 的 emergency stop、进程树终止、受验证目录清理，篡改摘要用例断言目录确实消失。
5. 写回失败最初无条件按预分配 narration ID 删除，409 碰撞时可能误删原素材；现只删除回读后完整
   事实精确匹配的本次旁白，普通 409 不执行 CP 删除。
6. Timeline PUT outcome uncertain 最初被误报 revision conflict 并立刻补偿，若服务端实际已保存会造出
   引用缺失素材的 Timeline；现回读并逐字段确认 exact next revision，无法确认时保留引用资源并返回
   固定 operation unavailable。
7. 同 modelId 的 Worker 最初会在 API key 已轮换后继续复用旧密钥；现于私有 Rust 边界同时比较模型
   与 secret configuration，不向 status/debug 暴露密钥。
8. 取消后解析器最初拒绝取消前已入队的认证 progress/prepared，导致竞态被误判为鉴权失败；捕获性
   真进程测试先复现失败，现继续消费认证事件，并让 prepared 结果输给 abort，最终目录清空。
9. Review 同步收紧 smart-edit 响应上限、请求目录父链、未知字段/乱序/重复终态、Timeline 返回 revision
   与草稿逐字段一致性，以及分析/旁白 writeback 只构造一次，所有 finding 均已专项复验。

## T4 Code Review 结论

对 Worker DTO/HMAC/文件事务、Control Plane client、模型 Worker 复用、生成状态机、补偿顺序、并发
Timeline 保存和后端兼容改动完成多轮本地结构化 Code Review。上述 finding 均先转为捕获性测试或
明确失败，再修复并复验；最终无剩余 actionable finding。继续遵守 T1 已记录的仓库内容外发限制，
不重试被拒绝的外部 `codex review`，没有以跳过审查收口。

## T5 RED 目标

1. 新增严格 Tauri commands：start 返回 generation 快照，get 轮询，cancel 只接受 canonical UUID v4；
   request/snapshot/error 均是路径无关闭集，未知字段、未知 generation、重复取消和终态取消固定拒绝。
2. commands 只在正式 desktop 组合根管理的 `SmartEditRuntime` 上工作，并进入 invoke allowlist；生产
   feature gate、测试构建和 App setup state 必须一致，不能创建第二份 runtime 或 sessionStorage 替身。
3. 前端 gateway/DTO 用严格 Zod 解码完整 mode/status/stage/progress/timeline/renderJob/failureCode，轮询
   有界且可取消；Tauri invoke 名称、camelCase 字段和 Rust serialization 逐字契约，不接收主机路径、
   API key、prompt 回显、转写或任意后端 message。
4. T5 首个聚焦测试必须精确失败于 smart-edit commands/gateway/DTO 尚不存在，随后才创建实现；专项
   验收覆盖 start → poll → terminal、cancel、固定错误映射、production composition 与既有剪辑网关回归。

## T5 RED 证据

三个边界均先独立跑红：功能层 Vitest 精确失败于无法解析
`features/video-editing/smart-edit-gateway`；Tauri adapter Vitest 精确失败于无法解析
`platform/tauri/smart-edit-gateway`；生产命令结构测试精确报告三个 command 与两个生产 handler 均缺失。
既有视频剪辑 DTO、Tauri invoke mock 和 Rust `SmartEditRuntime` 均可加载，失败来自本任务目标尚未创建。

## T5 GREEN 与功能验收证据

- 新增 `start_smart_edit_generation`、`get_smart_edit_generation`、`cancel_smart_edit_generation` 三个同步
  Tauri command；只接收严格 request 或 canonical generationId，不接收路径、源文件、API key、转写或
  Worker result。cancel 在同一 runtime 锁内返回 `cancelling` 快照，消除 cancel 成功后再 GET 的终态竞态。
- 功能层 Zod DTO 锁定 mode、五态 status、八阶段 stage、12 个固定 failure code 和字段名
  `progressPermille`；同时约束 active/成功/失败/取消事实、项目身份、Timeline 与 render Job 的 ID/revision
  一致性。严格对象会拒绝 prompt 回显、privatePath、未知阶段和 `progressPerMille` 拼写漂移。
- `TauriSmartEditGateway` 在 invoke 前验证 prompt/UUID/mode，响应逐次绑定 generation/project/mode；轮询
  固定 250 ms、最多 14400 次（一小时），running/cancelling 继续、首个终态停止，AbortSignal 在首调前和
  等待中均可中断且不会再发 native 请求。
- Rust runtime state 在 App setup 只 manage 一次；三个命令仅进入正式 build 与 control-plane-e2e handler，
  明确不进入无真实 Control Plane 的 plain desktop-e2e。command error 使用统一
  `{code,message,retryable}`，前端同时核对固定 code 和固定 `retryable=false`，未知/扩展/私密 payload
  统一降级为不透明 `operation_unavailable`。
- 专项最终为 smart-edit/既有 editing gateway Vitest `18 passed`、生产命令/剪辑 runtime Node 契约
  `8 passed`、Rust runtime `2 passed` 与 command error `1 passed`；TypeScript 全量 typecheck、4 个变更
  TS 文件 ESLint、Cargo fmt 和 `clippy --lib --tests -D warnings` 全绿。`control-plane-e2e` 与
  `desktop-e2e` 两种 Rust feature 均实际 `cargo check` 通过；按约定未运行完整 integration。

## T5 实现内审发现与修复

1. 初版 command 若 cancel 后另做 snapshot，会允许后台终态在两步之间插入，前端收到的不是
   `cancelling`；已让 runtime 的 cancel 在同一 mutex 临界区返回克隆快照，并保持重复/终态取消固定拒绝。
2. 初版 gateway 只校验 native error code，却原样信任 `retryable`；同一
   `generation_not_found` 可被漂移成可重试。现 code 与 `retryable=false` 必须同时精确匹配，反例统一降级。
3. 成功 render 快照最初只靠各自 schema，未证明 Job 就是该 generation 保存的 Timeline；现强制
   projectId、timelineId、timelineRevision 三者一致，并用错误 revision 反例捕获。
4. 轮询取消最初只有调用前短路的目标，Review 补了等待定时器期间 abort 的捕获性测试；实现会清除
   timer、移除 listener，且取消后 native 调用次数不再增加。
5. 生产结构门禁同时钉住三命令次数、签名无路径字段、runtime 模块不持有 `tauri::command`、plain E2E
   排除和 setup 单实例，防止后续另造测试 runtime 或把正式边界接回浏览器替身。

## T5 Code Review 结论

对 Rust command/error/feature gate、取消原子性、Zod 状态不变式、Tauri invoke 参数/响应绑定、轮询预算
与中止资源清理完成两轮本地结构化 Code Review。上述 finding 均有反例和专项复验，最终无剩余
actionable finding；继续遵守既定仓库内容外发限制，不重试外部 `codex review`。

## T6 RED 目标

1. 在现有 `VideoEditingWorkbench` 内增加正式智能剪辑区：一句话输入、生成草稿、一键直出片、深度
   思考开关入口、真实阶段与千分比进度、取消按钮；不得新造独立编辑器或 sessionStorage 结果通道。
2. draft 成功必须把返回 Timeline 直接替换进现有时间轴编辑状态并保持可继续人工保存/提交；render
   成功显示同一 generation 的 queued Job。整个 UI 只通过注入的 `SmartEditGateway`，生产 main 必须
   实例化并逐层传递真实 `TauriSmartEditGateway`，test harness 使用显式替身。
3. running/cancelling 禁止重复生成和手工提交竞态；窗口卸载停止轮询并请求取消。12 个固定失败码映射为
   可理解中文与明确恢复动作，不渲染 native message、prompt、转写、路径或模型术语。
4. T6 首个 UI 测试必须精确失败于智能剪辑入口/prop 尚不存在；专项功能验收覆盖 draft 落编辑区、render
   不重复生成、阶段进度、取消、可恢复失败、卸载清理、键盘/label/禁用态及既有手工编辑回归。

## T6 RED 证据

首个组件测试先要求现有工作台出现注入式“智能剪辑”页签，运行后精确失败于找不到该页签；继续补齐
用户路径后，测试又依次失败于 `SmartEditGateway` 没有实时快照回调、深度思考开关缺少开启态文案，
以及取消终态仍显示“正在匹配画面”。生产组件、gateway 回调和文案均在对应红灯之后实现。

提交前 Code Review 还发现项目切换会把上一项目的失败提示与进度带入下一项目；新增双项目组件测试后
精确失败于第二个项目仍显示“部分素材当前不可用”和 64% 失败进度，随后才让项目切换清空旧生成快照。

完整前端契约首次复验另捕获两项既有断言漂移：冻结 Worker 安装分发已随 T2 的 NumPy/ONNX Runtime
从 macOS `114` 增至 `116`、Windows `117` 增至 `119`，以及声明时间轴却因动画运行时缺失而从未注册
的文档现由有界预热返回 `render_timeout`，旧测试仍要求 `render_static_frames`。两条测试均先实际失败，
再按已经生效且更严格的生产事实更新；未放宽 Worker 的拒绝边界。

## T6 GREEN 与功能验收证据

- `VideoEditingWorkbench` 只在注入正式 gateway 时显示智能剪辑页签；一句话最多 4000 字，可选择生成草稿
  或复用同一 generation 一键直出片。开始和活动期同时用 UI 禁用态与同步 ref 阻止双击、手工提交和项目
  切换，项目切换后不会显示另一项目的生成状态。
- gateway 每次轮询把严格快照回调给页面；八个阶段、千分比、running/cancelling/succeeded/failed/
  cancelled 均使用封闭中文状态。12 个业务失败码与 7 个 gateway 失败码只映射固定恢复动作，不显示
  native message、模型供应商、prompt、转写、本机路径或密钥。
- draft 成功把返回的 Timeline 原样装入现有时间轴状态，实际页面显示“当前修订：第 1 版”、唯一
  Material ID 和可用的“保存时间轴”；render 成功把同一 generation 的 queued Job 放入现有任务列表，
  实际页面显示“排队中”和“时间轴第 1 版”，没有第二套编辑器或 sessionStorage 结果通道。
- 取消先请求正式 gateway 并持续轮询到权威 cancelled；页面显示“已在匹配画面阶段取消 · 64%”。组件
  卸载会中止轮询，并对已经取得 generation ID 的原生任务请求取消；start 尚未返回时的迟到 generation
  也由捕获的 gateway 取消，不会遗留后台生成。
- 无头真实浏览器从测试 Harness 正常入口依次完成“创作 → 轻量剪辑 → 新建项目 → 智能剪辑”，实操
  覆盖深度思考开启、草稿进度/落入时间轴、直出片排队、取消终态与按钮禁用；浏览器控制台无错误，
  验收后关闭会话和 Vite 服务。该证据只证明正式 React 交互与显式 Harness adapter，不能替代 T7 的
  Tauri/Rust/Worker/Control Plane 纵向验收。
- 最终专项为 7 个 Vitest 文件 `107 passed`；完整前端契约 `287 tests / 286 passed / 1 skipped /`
  `0 failed`，跳过项是本机未暂存动画运行时资产，不涉及智能剪辑。TypeScript `tsc -b`、改动面 ESLint、
  用户品牌门禁和生产 `vite build` 全绿；生产构建只告警既有单块体积超过 500 kB。

## T6 实现内审发现与修复

1. 开始生成后原实现仍允许切换项目，可能让后台结果落到错误页面；现活动期拒绝项目切换并给出明确动作。
2. render 成功自动切到任务页后，失败恢复提示也曾被一起切走；现只有成功终态导航，失败保留在智能页。
3. Harness 的 AbortSignal 等待最初可能漏掉已取消信号或遗留 listener；现先检查 signal，并在定时完成时移除。
4. 进度条最初把所有终态当 active；现 success/exception/normal 与活动态分别映射。
5. generation 开始后若父层替换 gateway，卸载可能向新实例取消旧任务；现固定持有启动该任务的 gateway。
6. 开关打开后辅助文案仍写“默认关闭”，取消后阶段仍写“正在匹配”；均由浏览器视觉验收发现并修正为
   “已开启”和“已在匹配画面阶段取消”。
7. 项目切换会遗留上一项目的终态与失败提示；新增失败测试后在切换边界清空旧快照和消息。
8. 完整契约在沙盒中因 Worker 无权绑定随机 loopback 而假性挂起；终止本轮重复进程后改在已授权真实环境
   单实例复验，确认不是产品死锁。测试结束已核对本轮 Worker/Chromium 无残留。

## T6 Code Review 结论

对组件状态机、同步防重 ref、项目切换、卸载/取消竞态、严格错误文案、Timeline/Job 既有状态复用、
生产 prop 链、Harness 资源清理、品牌扫描与两项契约漂移完成逐文件本地结构化 Code Review。上述 finding
均已有实际红灯、真实浏览器观察或专项回归证据，修复后再次复审无剩余 actionable finding。执行环境
继续禁止把仓库内容发送给外部 `codex review` 服务，因此没有重复尝试外发；审查在本地完成且未跳过。

## T7 RED 目标

1. 从正式隐藏 Tauri App 的正常用户入口完成素材导入、智能草稿落时间轴和一键直出片，核对真实
   PostgreSQL、冻结 Worker、Timeline、Job 与最终 Artifact，而不是测试 Harness 或直接 Command。
2. 覆盖配置缺失、素材不可用、取消竞态和原生失败的固定页面终态，确认 App 退出后 Worker、轮询、端口、
   私有目录和测试数据库均按本次隔离标识清理。
3. macOS 与 Windows 在同一提交 SHA 上分别复验正式链路；Windows 使用现有 Docker Desktop 提供隔离
   PostgreSQL，不尝试安装或使用 Colima。
4. 运行品牌术语、专项模块、完整 backend integration 与 LE 结束门禁，完成最终 Code Review 后才把
   LE-19 和因它待验收的前置能力更新为真实状态。

## T7 正式 App Harness 检查点

状态：实现完成，真实外部模型纵向验收待明确授权

已实现一套独立隐藏 Tauri App 旅程：从正式素材导入按钮导入受控图片，经真实设置页保存模型配置，
先验证缺配置固定失败，再执行草稿与一键直出片，最终从 PostgreSQL 核对两版 Timeline、唯一成功 Job
和 Artifact，并用 ffprobe 检查 H.264 `720×1280@20`、AAC 与真实时长。runner 使用项目既有专用
runtime/PostgreSQL/App 隔离工具，退出时核对端口、Worker、App 数据与数据库资源清理；Windows 路径
复用 Docker Desktop，不引入 Colima。

本检查点只证明 harness 自身已可构建和审查，不能冒充真实纵向通过：结构门禁 `1 passed`，Python
Ruff/格式、前端 TypeScript build 和 `git diff --check` 均以退出码 0 完成。真实运行会把固定测试提示词
和程序生成的受控图片发送给外部模型；该外发此前未获批准，因此尚未执行，LE-19 继续待验收。

### T7 本地完整集成检查点

路线图收口时首次完整运行 `backend/tests/integration`，得到一个有效 RED：真实 H.264 素材
用例仍按 LE-24 之前的签名调用 `pipeline.prepare`，缺少必填的 `enable_thinking`。最小修复
显式传入默认关闭的 `False`；专项 `1 passed` 后搜索全部同类调用并逐项复审，没有第二个漏传
点，Ruff、格式和差异检查全绿。完整 integration 复跑最终为 `472 passed, 17 skipped`。

该证据关闭了 T7 的本地完整集成门禁，但不替代真实外部模型、正式 App 双平台同 SHA 与最终
Artifact 验收；LE-19 仍为待真实验收。

### T7 双平台外部执行预检

在没有发起模型请求的前提下，macOS 与 Windows 已完成同一套最终执行前预检：两端项目密钥
均通过生产 `read_model_key()` 格式校验；Windows 密钥放在 Git 忽略目录，ACL 只允许当前
用户、SYSTEM 和 Administrators，`Medium Mandatory Level`/`LIMITED` 用户实测可读。
LE-19/LE-22/LE-23 的隔离 App 数据、Windows 安装目录均不存在，双平台正式包报告仍在。

两端分别运行 runner、LE-24 计时和 LE-22/23 证据契约测试，均为
`17 passed, 17 subtests passed`；正式页面旅程结构测试均为 `2 passed`。预检没有发送固定
提示词、受控图片、人声音频或抽帧，也没有连接模型服务。至此没有剩余本地环境前置缺口；
T7 只待明确外发授权后执行真实正常/失败旅程。
