# IM-08 完成证据

> 状态：🔍 待验收
>
> 日期：2026-07-23
>
> 提交：本文件所在的 IM-08 独立验收门禁提交

## 交付范围

- 固定知识讲解、资讯摘要、榜单三类真实样片，不允许用同一视频重复命名凑数。
- 每条样片必须由正式 App 正常入口和真实外部服务生成，校验文件摘要、20～90 秒时长、视频流、音频流，并由真人复核旁白、烧录字幕和完整播放。
- 每项外部图片、视频、音乐、字体等素材都必须记录来源、授权或许可证及复核人；没有权利记录的样片拒绝。
- 固定断网、文案 API、素材 API、配音 API、磁盘满、用户取消、App 重启和 Worker 崩溃八项正常入口失败矩阵。
- macOS arm64 与 Windows x86_64 都必须提交正式签名包、摘要、干净安装、正常入口、退出清理和平台报告。
- 独立校验器拒绝绝对路径、越界路径、symlink、未知字段、摘要不符、缺音频、时长越界，以及任何 mock、fixture、fake 或 placeholder 证据。

## RED

Roadmap 进入 `🧪 RED` 后执行 `python3 scripts/run_im_08_acceptance.py`，退出码为 2，明确失败于脚本不存在。当时没有三类样片和双平台正式包的统一证据格式，也没有能够阻止 Mock、缺素材权利或任意文件冒充验收结果的门禁。

## GREEN

```bash
python3 scripts/test_im_08_evidence_verifier.py
python3 scripts/run_im_08_acceptance.py
python3 scripts/check_embedded_browser_video_roadmap.py
```

确定性门禁验证严格 schema、真实证据类型、相对路径 containment、链接拒绝、文件 SHA-256、ffprobe 音视频流和时长、真人复核、逐素材权利记录、八项失败场景及双平台正式包。`--formal-evidence <目录>` 是唯一正式证据入口；未传该参数只代表验收设施就绪，不代表本任务完成。

## 失败矩阵

| 场景 | 必须提交的真实结果 |
| --- | --- |
| 断网 | App 给出可理解失败，任务不伪成功，Worker/临时文件可清理 |
| 文案 API 失败 | 错误脱敏，任务失败且不进入素材/发布阶段 |
| 素材 API 失败 | 不使用未授权随机替代素材，允许用户重试或调整来源 |
| 配音 API 失败 | 不生成无声“成功”成片，失败原因和重试状态一致 |
| 磁盘满 | 原子导入不留下伪 Artifact 或部分文件 |
| 用户取消 | App 状态最终为已取消，生成和复制停止 |
| App 重启 | 已导入成片和检查点保留，运行中任务按既定恢复策略收口 |
| Worker 崩溃 | App 不显示成功，进程清理并给出可重试结果 |

## 正常用户路径验收

IM-07 已从正式 Tauri App 左侧“视频制作”入口打开真实冻结 Worker，并验证子窗口、权威任务/成片空状态和退出清理。IM-08 的三类真实视频与双平台正式包尚未执行：没有把确定性单元测试、临时文件或测试驱动页面冒充正常用户验收。

正式执行时必须在两种平台分别安装签名候选包，从左侧“视频制作”选择“智能素材成片”，在完整制作界面提交三类主题，等待 App“制作任务”显示最终状态，在“成片”中播放并人工复核，然后依次通过正常界面触发取消、重试、关闭和重启。所有样片、失败截图/报告和安装包都放入外部证据目录，再运行：

```bash
python3 scripts/run_im_08_acceptance.py --formal-evidence <证据目录> --ffprobe <锁定的ffprobe路径>
```

## 真实边界

当前没有可用的百炼、素材站和配音正式凭据，没有经过签名/公证的 macOS 候选包，也没有由 EB-16 装配内置 Chromium 并完成 Authenticode 的 Windows 正式签名包。当前 Windows 11 x86_64 实体机已经完成 IM-02～IM-05 的冻结 Worker、网关、真实 Streamlit 与正常 App/WebView2 技术链，不再缺 Windows 开发设备；但在正式候选不存在时，不能把开发工作站或普通未签名包冒充干净安装和正式包证据。三类视频的内容质量、旁白、字幕、素材权利和八项真实失败路径因此均不能诚实宣称通过，任务保持 `🔍 待验收`。

2026-07-25 Windows 状态复核：专项 Roadmap 的 EB-16 仍为 `⬜ 未开始`；当前用户和
本机证书库的 Code Signing certificate 均为 0；工作区中百炼、素材站和配音验收配置
均不存在。IM-08 的 Windows 部分不是“漏跑现成命令”，而是正式输入尚未产生：
`scripts/run_im_08_acceptance.py` 不带 `--formal-evidence` 只验证设施就绪，带参数则
必须读取真实样片、八项失败矩阵和双平台正式包报告，不能自行生成这些外部事实。

## 清理

当前只执行纯读取契约检查和临时目录拒绝测试，测试目录退出时自动删除；没有启动服务、占用固定端口、生成样片或写入用户 AppData。正式验收器只读取用户明确提供的证据目录，不删除或改写其中内容。

## 文档变化

新增 IM-08 独立证据、正式验收契约、真实证据校验器与就绪门禁；Roadmap 只更新 IM-08 状态和汇总计数。

## 2026-07-23 待验收缺口排查与本机复验

- 本机重跑 `python3 scripts/run_im_08_acceptance.py`（未带 `--formal-evidence`）：就绪门禁与
  3 项证据校验器测试通过，验收设施无回归。
- 凭据排查：本机与仓库均无可用百炼（DashScope `sk-`）API Key、素材站或配音服务凭据；
  `.local/secrets/aliyun-video-editing.json` 仅为 OSS/ICE 用途的 RAM AccessKey，格式与权限
  均不满足文案模型调用。
- 签名排查：本机钥匙串只有 `Apple Development` 开发证书，没有分发用
  `Developer ID Application` 证书，无法产出签名/公证 macOS 候选包。
- 缺口分类：**需用户提供密钥**（百炼、素材站、配音）＋ **需正式包**（macOS Developer ID
  证书与公证、EB-16 Windows 内置 Chromium 包、Windows Authenticode 与正式干净安装）。
  2026-07-25 已有 Windows 实体机，不再把“需 Windows”列为独立缺口；没有正式包和凭据时
  本机无可继续补验项。

## 遗留项

先完成 EB-16 并提供 Windows Authenticode 发布身份、macOS 签名/公证候选包，以及明确授权的百炼、素材站和配音凭据；再分别在 macOS 与 Windows 正式包的正常入口生成三类样片并收集八项失败矩阵。只有 `python scripts/run_im_08_acceptance.py --formal-evidence <证据目录> --ffprobe <锁定的 ffprobe>` 完全通过后，才能把 IM-08 改为 `✅ 已完成`；当前环境不存在可替代这些外部事实的离线命令。

## 2026-08-04 台账更正：原记的三项阻塞里两项已不成立

本文件「真实边界」一节写着「当前没有可用的**百炼、素材站和配音**正式凭据，没有经过
**签名/公证的 macOS 候选包**」。逐项核对后，其中三条里有两条已经过期，剩余缺口比原记
的小得多。

| 原记阻塞 | 实际 | 依据 |
| --- | --- | --- |
| 百炼正式凭据 | **已有** | `.local/secrets/bailian-model.json`，用户 2026-07-23 提供并验真 |
| 配音正式凭据 | **不需要单独凭据** | `contracts/video/bailian-model-catalog.v1.json` 的 `voiceover` 用 `qwen3-tts-instruct-flash-2026-01-26`、`api_mode: dashscope_native`，走的是**同一把百炼 key**；`speech_recognition`（`qwen3-asr-flash`）同理 |
| 签名/公证的 macOS 候选包 | **已有** | 2026-08-04 出的正式包：App 与 DMG 双双 `Accepted`、staple 生效、`spctl` `accepted / source=Notarized Developer ID` |
| 素材站凭据 | **仍缺**（唯一真缺口） | 素材来源是 Pexels 等站点，配置入口在内嵌制作界面的「素材 API」设置页，见 `IM-06.md` 交付范围第 3 条。需要用户注册一个（免费）素材站 API Key |
| Windows Authenticode 正式包 | **仍缺** | 归 EB-16 的 Windows 段 |

**为什么值得单独更正**：这份证据是判断「还缺什么」的依据。写着缺三样、实际只缺一样，
会让人以为这条线离交付很远，从而不去推进本可以推进的部分。CLAUDE.md §2.1 要求
「发现台账与现实不符时，当场查清现实并立即改正」——这里改的是证据文件里的现实描述，
任务状态仍是 `🔍 待验收`（素材站凭据与 Windows 包两项确实还缺）。
