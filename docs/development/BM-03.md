# BM-03 完成证据

> 状态：🔍 待验收
>
> 日期：2026-07-23
>
> 提交：本文件所在的 BM-03 独立实现提交

## 交付范围

- Node Worker（`workers/motion_composition/worker.mjs`）bootstrap 协议新增必填
  `renderBrowser` 通道：`null` 或 `{chromiumMajor, executablePath, launchTimeoutSeconds}`
  的精确键集合；可执行文件必须是绝对路径、常规文件、非符号链接且带执行位，
  Chromium major 限定 100–999，启动超时限定 1–60 秒，任何偏差固定退出码 65 且不回显
  路径、令牌或输入。
- 新增 HMAC 认证命令 `worker.render.verify`：Worker 只使用 bootstrap 里 Rust 已验证的
  单一 Chromium 路径，在 `automation-tool-renderjob-<jobId>-` 专属临时目录内以独立
  detached 无头进程启动（`--headless` + `--remote-debugging-pipe`，无监听调试端口），
  先经 `--version` 探测实际 major，再经 CDP 管道 `Browser.getVersion` 核对运行中产品
  版本，两处都必须与 Rust 显式声明的 major 一致，最后 `Browser.close` 干净退出。
- 成功发出 `worker.render.verified`（含 chromiumMajor 与 HMAC 证明）；失败只发固定
  reasonCode（`render_browser_unavailable` / `render_browser_unusable` /
  `chromium_major_mismatch` / `render_timeout` / `render_protocol_invalid`），不泄漏路径。
- 禁用一切 fallback：Worker 不读 `HYPERFRAMES_BROWSER_PATH`、`PRODUCER_HEADLESS_SHELL_PATH`、
  `PUPPETEER_CACHE_DIR`、`CHROME_PATH` 或 HOME 缓存目录；渲染浏览器子进程环境只含指向
  RenderJob 目录的 `HOME`/`TMPDIR`，上述发现类变量不会传播。
- Rust 侧（`frontend/src-tauri/src/local_video_orchestrator.rs`）新增
  `VideoWorkerRenderBrowserConfiguration`（复用 `validate_executable_path` 的绝对路径/
  祖先符号链接/执行位校验 + major/超时范围校验）、`VideoWorkerLaunch::with_render_browser`
  bootstrap 注入，以及 `LocalVideoOrchestrator::render_verify`（命令签发、verified/failed
  事件 HMAC 校验、major 与期望一致性校验、`RenderRejected` 错误码）。
- 新增 `scripts/test_motion_video_render_adapter.py`（Worker 层失败矩阵门禁）与
  `scripts/run_bm_03_acceptance.py`（EB-03 契约暂存真实 Chrome for Testing 后跑通
  Rust→Node→真实 Chromium 纵向链路）。

## RED

激活后先运行：

```bash
python3 scripts/test_motion_video_render_adapter.py
```

实际输出 `AssertionError: expected bootstrap rejection, got exit 0`——旧 bootstrap
形状（无 renderBrowser 键）当时仍被接受，渲染通道与 fallback 禁用边界不存在。Rust 侧
RED 由 `cargo test --test local_video_orchestrator` 给出 11 个编译错误（
`VideoWorkerRenderBrowserConfiguration`、`with_render_browser`、`render_verify`、
`RenderRejected` 均不存在）。首次真实验收还暴露出完整版 Chrome 149 下
`--headless --screenshot` 不退出（挂起 >2 分钟），据此把无头健康探测改为与上游
puppeteer 同源的 `--remote-debugging-pipe` 方案，并重走一轮 RED（旧实现返回
`render_capture_invalid`）→ GREEN。

## GREEN

```bash
python3 scripts/test_motion_video_render_adapter.py        # 10 项全 PASS
python3 scripts/test_motion_video_worker.py                # BM-02 回归通过
cargo test --manifest-path frontend/src-tauri/Cargo.toml --test local_video_orchestrator  # 14 passed
cargo test --manifest-path frontend/src-tauri/Cargo.toml --lib                            # 86 passed
python3 scripts/run_bm_03_acceptance.py --archive <EB-03 缓存归档>
uvx ruff check --ignore RUF001 --config backend/pyproject.toml scripts/test_motion_video_render_adapter.py scripts/run_bm_03_acceptance.py
cargo fmt --manifest-path frontend/src-tauri/Cargo.toml -- --check
python3 scripts/check_user_facing_branding.py
python3 scripts/check_embedded_browser_video_roadmap.py
```

验收脚本用 EB-03 builder 从摘要锁定归档现场暂存真实 Chrome for Testing 149.0.7827.55
（macOS arm64），随后 `real_worker_render_verify_launches_the_locked_chromium` 走通
正式链路：Rust `LocalVideoOrchestrator` → 真实 `worker.mjs` → 真实内置 Chromium 无头
启动（RenderJob 目录 + CDP 管道 getVersion=Chrome/149.0.7827.55 + Browser.close 干净
退出，全程 2.38s），`render_verify` 返回 149；随后同一真实二进制在声明 major=150 时
被 Worker 以 `chromium_major_mismatch` 拒绝且不进入无头启动。

## 失败矩阵

- 路径缺失 / 相对路径 / 非常规文件 / 无执行位 / 符号链接 / 多余字段 / 类型错误 /
  major、超时越界：bootstrap 固定退出码 65，错误不含路径与输入正文。
- bootstrap 后可执行文件被替换或删除：渲染时 `render_browser_unusable`，不启动。
- `renderBrowser: null` 且发现类环境变量全部指向可记录诱饵浏览器：只回
  `render_browser_unavailable`，诱饵从未被调用——无系统浏览器发现、无缓存 fallback、
  无上游下载。
- `--version` major 与声明不符：`chromium_major_mismatch`，且记录证明未发起无头启动；
  运行中产品版本漂移（getVersion 与 --version 不一致）同样拒绝。
- CDP 管道响应为垃圾数据 / 超长 / 非零退出：`render_protocol_invalid`。
- 无头进程挂起：launchTimeoutSeconds 后 SIGKILL 整个进程组（detached 进程组），
  实测挂起进程被杀死、事件为 `render_timeout`；RenderJob 目录在成功、失败、超时
  路径一律删除（finally）。
- 伪造 HMAC 的 render 命令被静默忽略，不触碰浏览器也不产生事件；Rust 侧伪造事件
  证明、reasonCode 非法字符、major 与期望不符均按 `AuthenticationRejected` 拒绝；
  Worker 返回 failed 事件映射为 `RenderRejected`。
- 未配置 renderBrowser 时 Rust `render_verify` 直接 `ConfigurationInvalid`，不发 IPC，
  Worker 保持健康。
- Worker 无监听调试端口（断言禁止 `--remote-debugging-port`），CDP 只经 stdio 管道。

## 正常用户路径验收

不适用（分层证据说明）：BM-03 是 Worker 渲染适配的内部进程边界任务，没有新增用户
页面；用户从正式 App 制作动效视频的入口在 BM-08/BM-16 交付后才存在，届时必须补真实
App 纵向验收。本任务验收不是 Mock：真实摘要锁定 Chromium 由 EB-03 builder 现场暂存，
经正式 Rust 编排器与真实 Worker 完整协议启动并核对版本。

## 真实边界

- macOS arm64 已用真实 Chrome for Testing 149.0.7827.55 完成纵向验收；Windows x86_64
  证据按 2026-07-23 用户决策拆为独立待补项（见 `docs/development/windows-evidence-checklist.md`），
  Worker 的 detached 进程组杀灭与路径校验在 Windows 上需另行验证。
- 验收链路的 Node 为开发机 Node（wrapper 脚本），隔离 Node 22 打包运行时链路已由
  BM-02 验收覆盖；BM-16 冻结完整生产包时两者合并验证。
- 实测发现完整版 Chrome 149 `--headless --screenshot` 不会自行退出（keychain/GCM 服务
  常驻），因此健康探测采用 CDP 管道；真实逐帧渲染由 BM-16 接入上游引擎（引擎配置
  `chromePath` + `expectedChromiumMajor` 显式传入，本任务的 major 声明即其单一来源）。
- 上游引擎的 `PRODUCER_HEADLESS_SHELL_PATH`/`HYPERFRAMES_BROWSER_PATH`/家目录缓存
  fallback 属于上游进程内逻辑；本任务从进程边界禁用（环境不传播 + 显式 chromePath），
  BM-16 打包时需复核上游进程内仍不可达这些 fallback。

## 清理

- 每个 RenderJob 临时目录（`automation-tool-renderjob-<jobId>-*`）在 finally 中删除，
  测试与验收结束后断言系统临时目录无该前缀残留。
- 无头浏览器为 Worker 的 detached 子进程组：正常路径 `Browser.close` 退出，超时路径
  SIGKILL 进程组，Worker 停止时既有 `ManagedProcessTree` 覆盖整树；验收后 `pgrep`
  断言无暂存路径存活进程。
- 暂存目录位于 `.local/embedded-browser-video-studio/ebvs-bm03-<pid>/`，验收结束整体
  删除；仓库不提交浏览器二进制；测试全部使用无头模式，不弹窗口。

## 文档变化

- 新增本证据文件与 `scripts/test_motion_video_render_adapter.py`、
  `scripts/run_bm_03_acceptance.py`；专项 Roadmap 仅更新 BM-03 状态行与计数；
  `docs/development/windows-evidence-checklist.md` 登记 BM-03 Windows 待补项；
  未修改旧 `docs/development-roadmap.md`。

## 遗留项

- Windows x86_64：Worker 进程组终止语义、路径/reparse 校验与真实验收（依赖 EB-04
  Windows 暂存），按清单在 Windows 机器补齐后方可闭合。
- 正式 App 用户入口纵向验收随 BM-08（页面）与 BM-16（生产包冻结）补齐。
- 上游引擎进程内 fallback 的不可达性复核归入 BM-16 打包验收。
