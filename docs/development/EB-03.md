# EB-03 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-23
>
> 提交：本文件所在的 EB-03 任务提交

## 交付范围

- 新增暂存契约 `contracts/browser/embedded-chromium-staging.v1.json`：锁定与 EB-01 兼容性
  契约一致的 Chrome for Testing `149.0.7827.55`（修订 1228），macOS arm64 目标锁定官方
  下载地址（Playwright 官方 CDN `cdn.playwright.dev/builds/cft/...`，重定向仅允许
  Google 官方 `storage.googleapis.com/chrome-for-testing-public/<版本>/mac-arm64/` 前缀）、
  归档 SHA-256、根目录名、可执行文件路径与禁止条目（headless shell/第二浏览器）。
  URL 模板实据来自锁定 Playwright 1.61.0 driver 的 cftUrl 实现，非猜测。
  `windows-x86_64` 目标按 2026-07-23 用户决策只登记 `buildable=false` 与待补原因，
  由 EB-04 在真实 Windows 环境暂存。
- 新增构建器 `scripts/build_embedded_chromium_staging.py`：只消费已下载、摘要锁定的
  归档；安全解压（拒绝路径穿越、绝对路径、逃逸/绝对 symlink、重复条目、超量条目、
  非预期根目录、禁止条目）；锁定可执行文件必须存在且可执行；生成确定性
  `staging-manifest.json`（逐文件 SHA-256/大小/可执行位、symlink 目标、总量、来源
  URL 与归档摘要，排序稳定）。运行时零下载。
- 新增确定性测试 `scripts/test_embedded_chromium_staging.py`（12 项，零网络，合成
  归档）与验收入口 `scripts/run_eb_03_acceptance.py`。

## RED

```bash
python3 scripts/test_embedded_chromium_staging.py
# ModuleNotFoundError: No module named 'build_embedded_chromium_staging'
```

实际运行并记录以上失败（12 用例全部因构建器模块不存在无法收集），证明当时不存在
暂存构建器、契约与清单能力。实现后 ruff 暴露 5 处（未用导入/未用变量/超长行），
修复后再进 GREEN，未放宽断言。

## GREEN

```bash
python3 scripts/test_embedded_chromium_staging.py            # 12 passed
uvx ruff check --ignore RUF001 --config backend/pyproject.toml \
  scripts/build_embedded_chromium_staging.py scripts/test_embedded_chromium_staging.py \
  scripts/run_eb_03_acceptance.py                            # All checks passed
python3 scripts/run_eb_03_acceptance.py                      # 全链路通过（见下）
python3 scripts/check_embedded_browser_video_roadmap.py
```

`run_eb_03_acceptance.py` 真实链路（macOS arm64 本机）：

1. 归档为构建期一次性下载（171MB，官方 CDN → Google CfT 桶），SHA-256
   `311211b5…786630` 与契约锁定值一致；
2. 同一归档独立暂存两次，两份 `staging-manifest.json` 字节级一致（可复现）；
3. 暂存树体量校验通过（完整 Chromium 规模）；
4. 用锁定 Playwright 1.61.0 以 `executable_path` 指向暂存二进制、无头 + 全新临时
   Profile + `offline=True` 真实启动，`context.browser.version` 精确等于
   `149.0.7827.55`，UA 确认 HeadlessChrome，探针结束关闭上下文，无进程残留；
5. 启动全程未触网、未执行 `playwright install`、未出现第二套浏览器。

## 失败矩阵

- 归档摘要不匹配、输出目录已存在、未知/不可构建目标（含 Windows 目标提前构建）：
  构建前拒绝；
- 路径穿越（`..`）、绝对路径条目、绝对 symlink、逃逸 symlink、重复条目：解压中拒绝，
  已建目录不含越界文件；
- 非预期第二根目录、`chrome-headless-shell` 等禁止条目、锁定可执行文件缺失或不可
  执行：暂存后校验拒绝；
- 契约损坏、字段缺失或形状漂移：加载即拒绝；
- 全部拒绝路径均为固定消息，不反射归档内容。

## 正常用户路径验收

不适用。EB-03 是构建期供应链任务，不新增用户入口；暂存产物进入正式安装包与用户
可见链路分别由 EB-05/EB-06 与后续任务验收。

## 真实边界

- 真实验收在 macOS arm64 本机完成（真实下载、真实解压、真实离线启动探针）；
- Windows x86_64 暂存属 EB-04，按用户决策等待真实 Windows 环境，本任务不冒充；
- 归档缓存位于 `.local/embedded-browser-video-studio/eb-03-cache/`（Git 忽略），
  重新下载时必须命中契约锁定摘要；
- macOS 签名/公证与安装包装配属 EB-16，此处只保证暂存树与清单可复现。

## 清理

- 两次暂存树与启动探针 Profile 均在临时目录，验收结束自动删除；
- 浏览器进程随上下文关闭退出，复查无残留进程；
- 仓库内不提交归档、暂存树或清单产物，只提交契约与脚本。

## 文档变化

新增本证据文件、暂存契约与三个脚本；专项 Roadmap 仅更新 EB-03 状态行与进度计数
（未开始 39→38、已完成 30→31），未修改旧 `docs/development-roadmap.md`。

## 遗留项

- EB-04：Windows x86_64 暂存与同等门禁，待用户提供 Windows 环境（契约已预登记
  目标与官方下载地址，`buildable=false` 防误用）；
- EB-05：把本任务的 staging-manifest 汇入单一发行物 Manifest（含 Browser Use 与
  渲染验证版本、许可证与 SBOM）；
- 归档下载步骤未做多镜像回退（单一官方 URL + 摘要锁定）；若官方 CDN 路径变化，
  必须先更新契约核对日期与来源实据再改代码。
