# EB-14 完成证据

> 状态：🔍 待验收（内置 Chromium 上的私信与恢复链路迁移及既有失败矩阵已通过；
> 真实抖音平台最终状态与 Windows 证据见遗留项）
>
> 日期：2026-07-25
>
> 提交：本文件所在的 EB-14 独立实现提交

## 任务

- 私信与恢复链路（私信页对象 + 私信动作目标校验/频控/单次发送/结果验收 +
  side-effect 恢复读取双终态不重发）原样迁移：语义零重写，只把启动来源换成摘要
  校验过的内置 Chromium；本期不接 Browser Use。

## RED

迁移型任务：私信页、私信动作、side-effect 恢复三个集成测试仅删系统 Chrome 常量
与 skip 守卫、注入 `staged_embedded_chromium` 夹具，目标校验/频控/单次发送/结果
验收/暂停-取消-紧停/崩溃-重启恢复断言一行未改。迁移后直接 4/4 GREEN，无伪 RED；
风险由既有矩阵回归与 diff（6 insertions / 23 deletions，全为启动来源替换）背书。

## GREEN

```text
uv run pytest tests/integration/test_douyin_direct_message_page_browser.py \
  tests/integration/test_douyin_direct_message_action_browser.py \
  tests/integration/test_douyin_side_effect_recovery_browser.py   4 passed（真实内置 Chromium）
uv run pytest tests/unit/executor                                 822 passed, 1 skipped
uvx ruff check --config pyproject.toml tests/integration/         All checks passed
python3 scripts/check_embedded_browser_video_roadmap.py           通过
```

## 失败矩阵

原样保留：目标校验拒绝、频控上界、单次发送幂等、结果验收、暂停/取消/紧停与
进行中动作竞争、崩溃与重启后恢复读取双终态且不自动重发；对应单测在 822 项
执行器单测中回归。

## 正常用户路径验收

正式入口（App→Rust→Executor 私信动作与恢复命令）由 P 系列既有桌面 E2E 承载；
本任务只改测试启动来源，无产品代码变更。真实抖音私信最终状态需受控账号：
`🔍 待真实账号`。

## 真实边界

- 内置 Chromium 与系统 Chrome 行为一致性由同套页面对象两代浏览器测试分别证明；
- Windows 同链路证据由并行 Windows 会话补齐。

## 清理

- 测试经 `runtime.running` 在成功/失败路径关闭浏览器与进程树；staged Chromium
  随 pytest 会话临时目录清理，无残留。

## 文档变化

- 激活专项 Roadmap 的 EB-14 并更新为 `🔍 待验收`、同步计数；
- `docs/development/windows-evidence-checklist.md` 新增 EB-14 待补节。

## 遗留项

- 真实抖音私信/恢复链路受控账号最终状态验收：`🔍 待真实账号`；
- Windows 平台同链路证据随并行 Windows 会话补齐；
- 上述闭合前保持 `🔍 待验收`。
