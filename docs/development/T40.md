# T40 登记并剔除权利未决的 UTM Kabel KT 字体

> 状态：✅ 默认拒绝与物理排除已完成；该字体权利本身仍是“无法确认”，不是已获授权
>
> 日期：2026-07-26
>
> 原提交：`1660d211ccd8309f83afcd04c2fc7d663897ad79`
>
> 证据补记：第三批事后补录，未与原代码/契约同提交

## 结论

字体字节只附有“Free for everyone”表述，没有可核实的商用再分发或嵌入授权。原提交没有猜测
许可，而是登记为 `NOASSERTION`、`undetermined`、`deny`，并从 material-video-worker
冻结清单中物理排除。

## RED

第二批记录保留了两次预期断言失败：

- 权利登记不存在：`'font-utm-kabel-kt' not found`；
- 登记后，物理排除仍未完成：`'fonts/UTM Kabel KT.ttf' not found`。

第二条消息来自门禁要求冻结包中不得再存在该路径；原始完整终端输出未入仓。

## GREEN

- `contracts/quality/asset-rights-policy.v1.json` 绑定精确字体摘要并设为默认拒绝；
- `contracts/quality/material-video-worker-package.v1.json` 不再冻结该字体；
- 第三方声明投影同步表达未分发；
- `scripts/test_asset_rights_policy.py` 覆盖精确字节、重复 ID 和篡改 allow 的拒绝；
- `scripts/test_material_video_worker.py` 与 `check_third_party_sources.py` 阻止字体回流。

## 失败矩阵

| 场景 | 结果 |
| --- | --- |
| 字体未登记 | 权利门禁拒绝 |
| 未决字体被改为 allow | 篡改测试拒绝 |
| 相同 ID 重复登记 | 唯一性门禁拒绝 |
| 同名不同字节替换 | 精确摘要不匹配，拒绝 |
| Worker 包重新带入字体 | 冻结包与候选审计拒绝 |

## 真实边界

这是供应链和包内容任务，不需要用户 UI 交互。它证明当前候选不能携带这份字体，但不构成对
字体权利的法律鉴定，也不声称未来永远不得使用；拿到可核实授权后必须走独立升级任务。

## 清理

没有修改只读 vendor；删除只发生在受控 Worker 候选装配面，没有触碰原始上游仓库。

## 文档与遗留

原任务摘要在第二批交接单中；权利事实以
`contracts/quality/asset-rights-policy.v1.json` 为机器可读来源。
