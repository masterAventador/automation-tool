# C10-08 云端部署

- 状态：✅ 已完成（provider-neutral 工件与隔离部署演练）
- 日期：2026-07-23
- 范围：交付并执行 provider-neutral 单实例部署流水线，串行验证备份、一次性 Alembic migration、单 Control Plane、HTTPS ingress、health/version 和不可变镜像身份；不自动扩容、不自动数据库 downgrade、不连接未指定的真实云账号
- 前置依赖：C10-03～C10-07 已完成
- RED：先新增 Compose/发布计划机器契约和部署执行契约，要求外部私网/Secret volume、migration 与应用启动分离、Control Plane 精确 1 replica/1 worker/无端口、入口唯一公网、串行锁、备份 receipt、digest/OCI 身份、health/version 失败停止；当前 C10-08 部署清单、执行器和隔离部署验收均不存在，测试应准确失败
- GREEN：
  - 新增 `deploy/customer-demo/compose.v1.json`，将一次性 migration、单 Control Plane 与唯一 HTTPS ingress 分开编排；应用容器固定单副本/单 worker、无宿主端口、只读根文件系统、drop all capabilities，网络和 Secret volume 均由外部预创建。
  - 新增 `deploy/customer-demo/release-plan.v1.json` 与 `README.md`，固定串行阶段、备份前置、不可变 digest、禁止自动扩缩容/故障副本和数据库 downgrade，并明确真实云验收边界。
  - 新增 `scripts/deploy_customer_demo.py`，实现白名单非 Secret 配置、备份 receipt/产物校验、OCI 版本与 revision 校验、独占部署锁、migration、单实例启动、容器隔离检查、HTTPS health/version 验证和失败时仅停止新服务。
  - 新增 `scripts/run_c10_08_acceptance.py`，在隔离 Docker 网络/volume/PostgreSQL 上构建真实镜像、生成短期测试 CA、注入文件 Secret、执行真实备份和部署，再验证 Alembic revision、单副本、无应用宿主端口及日志无 Secret。
- 验证：
  - `node --test frontend/tests/customer-demo-deployment-execution.test.mjs`：2/2 通过。
  - `backend/.venv/bin/ruff check scripts/deploy_customer_demo.py scripts/run_c10_08_acceptance.py`：通过。
  - `backend/.venv/bin/mypy scripts/deploy_customer_demo.py scripts/run_c10_08_acceptance.py`：通过。
  - `backend/.venv/bin/python scripts/run_c10_08_acceptance.py`：通过；迁移 revision `20260723_0030`，Control Plane/ingress 各 1 副本，HTTPS health/version 均为 200。
- 边界：未连接任何未指定的云账号，也不把本地隔离演练描述成真实公网部署；拿到明确 provider/项目/域名后可复用同一工件补真实云证据。
