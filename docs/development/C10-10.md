# C10-10 网络/重启恢复

- 状态：✅ 已完成（隔离部署恢复演练）
- 日期：2026-07-23
- 范围：在 C10-08 同一单实例 HTTPS 部署演练中执行 Control Plane 原容器重启和两次 application network 抖动，验证不创建第二副本、入口恢复；同时重放 H8-07 的生产 Executor 重连/持久 outbox 和 Last-Event-ID 事件续传契约；不连接真实云账号、不执行真实平台动作、不修改专项 Roadmap
- 前置依赖：C10-09、H8-07 已完成
- RED：先固定恢复计划、部署中恢复 probe hook 与 C10-10 验收入口；当前 C10-08 演练完成健康检查后立即清理，无法在同一部署生命周期注入重启/抖动，也没有把 Executor 重连与事件游标续传纳入 Customer Demo 恢复门禁，测试应准确失败
- GREEN：
  - 新增 `deploy/customer-demo/recovery-plan.v1.json`，固定 Control Plane 原容器重启 1 次、application network flap 2 次、Control Plane/ingress 最大各 1 副本、禁止自动扩缩容/数据库重启与 downgrade，并把 Executor 重连、持久 outbox 和 `Last-Event-ID` 续传列为同一恢复门禁。
  - C10-08 隔离部署验收新增只接收非 Secret 运行句柄的 `DeploymentRecoveryContext` / `recovery_probe`；probe 在服务清理前执行，普通 C10-08 路径保持不变。所有外部命令加入有限超时，避免 Docker 引擎异常时无限挂起。
  - 新增 `scripts/run_c10_10_acceptance.py`：同一已迁移 HTTPS 部署内重启原 Control Plane 容器，再两次断开 Control Plane 的 application 网络（DB 网络保持在线），以原 IPv4 和 `control-plane` alias 接回；每次等待 HTTPS 恢复，并断言容器身份、入口身份和 1+1 副本没有变化。
  - 部署恢复后精确重放 H8-07 生产 Executor 的 1012 重启重连、无 close frame 异常断网、心跳间隙重连/持久 outbox，以及服务端 SSE 失败重连和 PostgreSQL 事件游标续传 5 项用例。
- 失败发现与修正：
  - 首次将 published-port ingress 容器断网后，Docker Desktop 没有恢复宿主端口转发表；即使原 IP 接回也不可用。恢复 drill 改为断开 ingress→Control Plane 的 application 链路，入口和宿主端口全程不动，更贴合目标故障域。
  - 一次 Docker volume writer 在创建容器前卡住；中断后清理专属残留 volume，并给 C10-08 命令增加总超时、Secret volume writer 增加 60 秒超时，后续重跑通过。
- 验证：
  - C10-10 Node 契约 2/2，Ruff、Mypy 通过。
  - 真实 Docker 恢复演练通过：Alembic `20260723_0030`；Control Plane 原容器重启 1 次，application network flap 2 次；Control Plane/ingress 原容器均保留且各 1 副本；HTTPS health/version 恢复为 200。
  - 协议恢复 5/5 通过，事件恢复头固定 `Last-Event-ID`。
  - Frontend 全量 Node 193/193、Vitest 253/253、ESLint、TypeScript、build 与 OpenAPI drift 全绿；本任务未修改 Backend/Rust 产品代码，C10-09 同一工作树全量 Backend 2379/5 skipped 与 Rust all-targets/all-features 已通过。
- 边界：这是 C10-08 provider-neutral 工件的隔离容器恢复演练，不宣称真实云基础设施、DNS 或公网网络故障证据；没有执行真实平台动作，也没有自动创建替代副本或回滚数据库。
