# H8-14 工作台指标

- 状态：✅ 已完成
- 日期：2026-07-21
- 提交：本记录、Installation-scoped PostgreSQL 指标投影、FastAPI/OpenAPI、Tauri 固定 operation、正式工作台 UI 和隐藏 App 纵向验收属于单一 `feat: 完成权威工作台指标` 提交；完成后立即推送 `main`
- RED 与产品口径：先把唯一台账置为 `🧪 RED`；Backend 单元测试准确因 `workbench_metrics` 应用模块不存在而收集失败，Frontend Node 契约准确因 Rust `GetWorkbenchMetrics` operation 不存在而失败。既有“今日任务”只从最近 20 条客户端列表按本机日期估算，既非完整事实也没有已冻结的用户时区；H8-14 将 MVP 口径明确为当前 Installation 累计指标，并同步产品规划，避免把不完整样本冒充今日统计。后续若增加按日/区间指标，必须先建立账号时区和明确时间边界
- 服务端权威投影：新增 `workbench.metrics.v1` 只读快照和 `GET /api/v1/workbench/metrics`；路由复用 `app.control-plane` Installation 守卫，数据库适配器在一条 SQL 的两个聚合 CTE 中按 Installation 统计既有 `tasks`/`task_actions`。Task 成功包含 succeeded/partially_succeeded，当前接管只计 awaiting_human，结果不确定单列；Action 直接按持久 outcome 汇总。所有计数要求非负、JavaScript 安全整数且分类和不超过总数，固定 `no-store`；异常统一为脱敏可重试 503
- 桌面正式链：React 工作台改为独立 10 秒 TanStack Query 展示累计任务、成功任务、失败任务、当前需接管、任务结果待确认、成功动作、失败动作和动作结果待确认；1 秒运行状态轮询保持独立。TypeScript Zod、Rust serde/解析器与 OpenAPI DTO 都拒绝未知字段、错误版本、越界或不一致计数。WebView 只调用固定 `get_workbench_metrics`，Rust 自行从 App 私有凭据换取 `app.control-plane` Session 并访问固定路径，不暴露 URL、Header、Bearer、Installation ID 或通用 invoke
- 隐私、失败与只读矩阵：覆盖非法 Installation、错误依赖、数据库异常、服务缺失、未认证、跨 Installation 数据、空安装实例、部分成功、失败、接管、不确定、其它非统计状态、未知响应字段、错误版本、负数/布尔/浮点/超安全整数、分类和大于总数及 Abort Signal。响应和 UI 不含诊断文本、Cookie、评论/私信正文、页面内容、Executor SQLite 或本机路径；接口没有 POST、表、缓存、写入、上传或平台副作用
- 原调用方验收：`pnpm test:h8-14-tauri` 使用唯一 `com.aventador.automationtool.h814acceptance`、`visible=false` App、专属 `automation-tool-h814-<pid>` Compose project、随机 PostgreSQL/WebDriver 端口和真实 Uvicorn。App 从测试特性内只准备 Installation/Task，runner 在当前与另一 Installation 种入相同结构事实；正式页面再经 TypeScript gateway → Tauri IPC → Rust 固定 operation/App Session → HTTP → PostgreSQL 渲染 `8/2/1/1/1` Task 与 `5/1/1/1` Action 事实，证明跨 Installation 隔离且读取前后数据库计数不变，1/1 通过
- 门禁：Backend `1977 passed, 5 skipped in 218.23s`，13211 条语句/3028 个分支覆盖率 100%，341 个 Python 文件格式、Ruff、严格 Mypy 315 个源码文件、uv lock、OpenAPI 与 Executor Schema 全绿；Frontend 115 项 Node 契约、199 项 Vitest、5 项全局无头 Playwright、冻结安装、peer dependency、ESLint、严格 TypeScript、API 漂移、production boundary 与 Vite build 全绿；Rust 默认/`desktop-e2e`/`control-plane-e2e` 三套完整测试、Rustfmt、三套全目标 Clippy `-D warnings` 与 Actionlint 全绿。并行重负载时既有紧停 `<2s` 时序断言一次超时，释放 Backend 负载后重跑完整 `control-plane-e2e` 套件通过，未降低时限或测试门禁
- 资源与文档：隐藏 App 验收成功/失败都恢复 production Vite 资产，回收自身 App、WebDriver、Uvicorn、PostgreSQL 容器/网络/Volume、AppData 和全部端口；常规 Playwright 保持全局无头，没有访问真实平台、默认浏览器 Profile、系统钥匙串或其他项目资源。同步根/Backend README、产品规划、前后端架构、工程结构、OpenAPI/生成 DTO 和本唯一台账，没有新增重复规划文档
- 后续：进入 `H8-15`，逐项审计本台账第 4.1 节失败矩阵，给所有可自动化分支补正式测试或登记不适用/需真实平台的明确理由，不把已有测试数量直接当完整矩阵证据
