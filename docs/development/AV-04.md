# AV-04 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-23
>
> 提交：本文件所在的 AV-04 独立任务提交（`ci: 建立专项路线图与独立证据门禁`）

## 交付范围

- 建立 87 个精确任务 ID、六种状态、汇总计数、唯一激活任务和依赖引用检查。
- 每个已激活任务必须对应唯一 `docs/development/<任务ID>.md`，并具备状态、日期、提交、RED、GREEN、失败矩阵、正常用户路径、真实边界、清理、文档和遗留项。
- 限制专项 Roadmap 的行数/体积并禁止追加 RED/GREEN、提交或完成记录，防止状态台账不断膨胀。
- 禁止把 AV/EB/BU/VF/IM/BM/VE/PB/SA/CQ 任务行复制到旧 `docs/development-roadmap.md`。
- 将 Roadmap、第三方源码和用户品牌检查接入只读 GitHub Actions governance job。

## RED

命令：

```bash
python3 scripts/run_av_04_acceptance.py
```

实际结果：退出码 1，明确报错缺少 `scripts/check_embedded_browser_video_roadmap.py`。修改前专项状态和独立证据约定只有文档说明，没有可执行门禁。

## GREEN

命令：

```bash
python3 scripts/check_embedded_browser_video_roadmap.py --self-test
python3 scripts/check_embedded_browser_video_roadmap.py
python3 scripts/run_av_04_acceptance.py
python3 scripts/run_av_01_acceptance.py
python3 scripts/run_av_02_acceptance.py
python3 scripts/run_av_03_acceptance.py
uvx ruff check --ignore RUF001 --config backend/pyproject.toml scripts/check_embedded_browser_video_roadmap.py scripts/run_av_04_acceptance.py
git diff --check
```

实际结果：全部退出码 0。门禁核对 87 行任务和实际状态汇总，自测证明重复任务、汇总漂移、两个激活任务、Roadmap 内嵌完成记录、旧 Roadmap 双写和证据缺字段都会失败。

## 失败矩阵

- 任务缺失、重复、多出、状态词非法、依赖引用未知或自依赖：拒绝。
- 六种状态汇总缺失、重复、非整数、总数不为 87 或与任务行不一致：拒绝。
- 同时出现两个 RED/实现中任务：拒绝。
- 已激活任务缺少独立文件、状态不一致、日期/提交或任一必需章节缺失：拒绝。
- 单任务证据超过 64 KiB，或 Roadmap 超过 900 行/128 KiB：拒绝。
- Roadmap 出现完成记录、RED/GREEN 证据或提交详情：拒绝。
- 旧 Roadmap 出现专项任务表格行：拒绝。
- CI 未初始化精确 Submodule、源码/许可证漂移或正式 UI 出现禁止词：governance job 失败。

## 正常用户路径验收

不适用。AV-04 是开发台账和 CI 静态治理任务，没有新增用户入口。它只证明状态与证据结构可信，不能替代任何浏览器、视频、剪辑或发布功能从正式 App 正常用户路径完成验收。

## 真实边界

- 实际读取当前 87 行专项 Roadmap、AV-01～AV-04 独立证据和合并后最新旧 Roadmap；没有修改或复制旧 Roadmap 任务状态。
- GitHub Actions 使用完整历史和递归 Submodule checkout，再运行 Roadmap、源码供应链和 UI 品牌三个无写入检查；没有使用 Mock 结果替代正式 CI 配置。
- 自测只在系统临时目录复制证据并注入失败，退出时自动删除，不修改真实台账。

## 清理

本任务只执行静态文件检查和临时目录故障注入，没有启动 App、浏览器、Worker、端口、数据库或容器。自测临时目录由 `TemporaryDirectory` 自动清理。

## 文档变化

- `docs/development/README.md`
- `docs/development/AV-01.md`
- `docs/development/AV-02.md`
- `docs/development/AV-03.md`
- `docs/embedded-browser-video-studio-roadmap.md`
- `docs/project-structure.md`
- `.github/workflows/quality.yml`
- `scripts/check_embedded_browser_video_roadmap.py`
- `scripts/run_av_04_acceptance.py`

## 遗留项

当前门禁检查证据结构和静态一致性，不判断每条证据的事实真实性。每个后续任务仍须实际执行其 RED/GREEN、正式 App 正常用户路径和真实最终状态验收；CQ-05 再核对所有首期任务与双平台正式包证据。
