# IM-04 完成证据

> 状态：🔍 待验收
>
> 日期：2026-07-23
>
> 提交：本文件所在的 IM-04 独立实现提交

## 交付范围

- 复用 App 已有“文案模型服务”作为唯一设置和连接测试入口，不在内嵌 WebUI 再维护一套
  Provider、Base URL、模型或密钥。
- 将阿里百炼固定映射到上游可消费的 OpenAI-compatible 调用层，Base URL 固定为百炼官方地址，
  模型只允许 DeepSeek V4 Pro、GLM 5.2 和通义千问 3.7 Max 的锁定快照。
- Rust 从原生秘密存储生成不可公开的 Worker 投影，经私有 stdin bootstrap 传入；序列化缓冲、
  Rust 持有对象和设置请求按生命周期清零，公开状态只包含模型 ID。
- Worker 只在内存创建兼容客户端，不导入或修改上游配置，不把真实密钥放进 React/Streamlit
  页面状态、`config.toml`、公开状态、Debug、普通日志或原始错误。

## RED

Roadmap 进入 `🧪 RED` 后执行：

```bash
python3 scripts/run_im_04_acceptance.py
```

实际退出码为 2，明确报告验收脚本不存在。已有 VF-05 设置与连接测试，但当时没有从原生秘密
存储到冻结视频 Worker 的适配、协议、真实进程证据或上游配置零写入门禁。

## GREEN

```bash
python3 scripts/run_im_04_acceptance.py
python3 scripts/check_embedded_browser_video_roadmap.py
uvx ruff check --ignore RUF001 --config backend/pyproject.toml \
  scripts/run_im_04_acceptance.py scripts/test_material_video_model_adapter.py \
  workers/material_montage/*.py
cargo fmt --manifest-path frontend/src-tauri/Cargo.toml -- --check
git diff --check
```

macOS arm64 现场重新构建完整冻结 Worker，由 `ModelServiceSettings` 保存测试秘密、生成原生投影，
再由生产 `LocalVideoOrchestrator` 启动真实候选。ready 状态确认锁定模型已经安装，健康、停止和
临时目录清理成功；包目录没有产生上游 `config.toml`。

## 失败矩阵

- 来源 Provider、目标适配器、Base URL、用途、模型、密钥格式或字段集合漂移：启动前拒绝。
- 未配置文案服务、损坏秘密存储、用途错误或旧模型：只返回固定配置错误，不启动降级 Provider。
- Worker bootstrap 缺失、多余、重复或含任意地址/模型：固定拒绝，不监听端口。
- 未安装适配器、响应缺少文本或请求异常：只返回固定错误，不反射密钥、URL 或服务端正文。
- Debug、公开状态和序列化结果不得出现测试密钥、百炼 Base URL 或原始 localhost 地址。
- 候选目录出现 `config.toml`，或适配过程导入上游配置模块：验收失败。

## 正常用户路径验收

IM-04 没有新增用户控件，唯一正常入口继续是 VF-05 已正式验收的“设置与诊断 → 文案模型服务”：
用户选择模型、保存密钥并点击“测试连接”，页面只看到配置状态和固定结果。本任务没有建立第二个
WebUI 设置入口；真实冻结进程验收证明同一份原生配置可被 Worker 消费。IM-05 才从“视频制作”
正式入口打开完整内嵌页面。

## 真实边界

- 当前没有用户提供的有效百炼密钥，因此不伪造生成成功：VF-05 已从正式 App 对百炼生产端点
  实测无效密钥得到脱敏 401，本任务用合成密钥只验证秘密传递和客户端安装，不发外部模型请求。
- macOS arm64 真实冻结链路已通过；Windows 原生 workflow 受 GitHub Actions 账单状态影响，
  尚无成功证据，因此状态保持 `🔍 待验收`。
- 本任务提供脚本生成调用适配器；把它接到完整 WebUI 表单、任务进度与取消属于 IM-05。

## 清理

- 冻结候选、uv/PyInstaller 环境、素材目录和原生秘密存储均位于隔离临时目录，验收结束删除。
- Worker 使用随机 loopback，Rust stop 结束进程树；不使用默认端口，不清理其它 session 资源。
- 不提交测试密钥、冻结包、运行配置、日志、App 数据或生成素材。

## 文档变化

- 新增文案模型适配契约、本独立证据和验收入口；只更新专项 Roadmap IM-04 状态与汇总。
- 不修改旧 `docs/development-roadmap.md`，不把内存适配声明成完整 WebUI 或视频生成完成。

## 2026-07-23 待验收缺口排查与本机复验

- 在合并后 `main` 的全新 worktree 重跑 `python3 scripts/run_im_04_acceptance.py`：重建真实
  冻结候选，`ModelServiceSettings` 保存测试秘密并生成原生投影，生产编排器启动真实候选，
  锁定模型安装确认、健康、停止与清理全部通过；候选目录无上游 `config.toml`，验收无回归。
- 密钥排查（本机可得性）：Tauri 生产 App 私有目录（`com.aventador.automationtool`）不存在；
  `.local/secrets/` 仅有 OSS/ICE 用途的 RAM AccessKey（非 DashScope `sk-` 格式，且 RAM 权限
  只有 OSS+ICE，无百炼授权）；环境变量与用户级配置无 `DASHSCOPE_API_KEY`。结论：真实
  百炼生成调用**待用户提供**在百炼控制台创建的 `sk-` API Key，本机今天不可补，不伪造通过。
- 缺口分类：**需用户提供密钥**（百炼 `sk-` API Key 的真实生成调用）＋ **需 Windows
  （CI 账单阻断）**；本机 macOS 确定性证据已当日复验。

## 2026-07-23 真实百炼密钥验收与密钥格式缺陷修复

用户提供了百炼工作空间 API Key（`sk-ws-…`，已按用户要求登记在
`docs/credentials-bailian-model.md`，本机消费路径 `.local/secrets/bailian-model.json`）。
用真实密钥走验收时发现并修复一个真实缺陷：

- **缺陷**：真实工作空间 Key 含点号分段，而三处生产校验
  （`workers/material_montage/model_service_adapter.py` 的 `API_KEY_PATTERN`、
  `model_service_settings.rs::is_valid_api_key`、
  `local_video_orchestrator.rs::valid_model_api_key`）只允许字母数字与 `-`/`_`，
  会把用户的真实密钥在设置页与 Worker 引导两级全部拒绝。
- **RED**：先在 `scripts/test_material_video_model_adapter.py` 与
  `frontend/src-tauri/tests/model_service_settings.rs` 增加"点号分段合成 Key 必须被接受"
  的失败测试（Python 1 error / Rust `workspace_api_keys_with_dots_are_accepted_end_to_end`
  FAILED），并保留空格/斜杠/错误前缀仍被拒绝的反例。
- **GREEN**：三处校验的允许字符集补入 `.`，其余边界（`sk-` 前缀、长度上限、脱敏 repr、
  Zeroizing）不变。Python 5 用例、Rust `model_service_settings` 9 用例 +
  `local_video_orchestrator` 6 用例全部通过；ruff 与 `cargo fmt --check` 通过。
- **真实生成调用（原待凭据缺口，已补）**：

```bash
AUTOMATION_TOOL_REAL_CLOUD=1 AUTOMATION_TOOL_BAILIAN_CREDENTIALS=<主仓库>/.local/secrets/bailian-model.json \
  uv run --with openai python ../scripts/run_im_04_real_generation.py
# IM-04 real generation ok: deepseek-v4-pro -> 27 chars
# IM-04 real generation ok: glm-5.2 -> 25 chars
# IM-04 real generation ok: qwen3.7-max-2026-06-08 -> 26 chars
```

  新增门控脚本 `scripts/run_im_04_real_generation.py`（无凭据或未显式开启时跳过，
  不进任何常规门禁）：生产适配器模块按真实密钥完成 parse →
  客户端安装 → 对锁定生产地址的真实生成请求，三个锁定模型全部返回正常中文文案，
  输出与 repr 均无密钥、Base URL 或原始服务端错误。真实密钥在公共锁定地址
  `dashscope.aliyuncs.com/compatible-mode/v1` 实测可用，契约 Base URL 无需变更。
- 完整确定性验收 `python3 scripts/run_im_04_acceptance.py` 在修复后重跑通过
  （冻结候选 + 生产编排器 + 网关测试 + 上游零写入门禁）。

## 遗留项

真实百炼生成调用已于 2026-07-23 用用户密钥补齐（见上节）。剩余缺口只有 **Windows
原生候选（GitHub Actions 账单阻断）** 与完整 WebUI 正式入口（IM-05 承接）；两项通过后
才能改为完成。IM-05 继续接内嵌页面；确定性实现已完成，不因外部 CI 账单阻塞后续任务。
