# LE-16 语义匹配与片段选择

用户可操作：否
证据类型：分层实现

## 日期与状态

- 开工日期：2026-07-31
- 当前状态：T1、T2、T3、T4 已完成；T5 实现、功能验收与 LE 收口门禁已绿，待提交级
  Review；LE-16 整体保持 `🚧 实现中`
- 前置：LE-13、LE-15 均已交付；同时消费 LE-07 的截断素材事实

## 范围

本任务消费 LE-15 的脚本分句与真实旁白时长、LE-13 写回的素材描述/标签/镜头边界，
先得到完整的句子与素材语义分数矩阵，再按本地确定性规则选择可解码片段，并把有人声
素材作为自带旁白段落编排，最终产出可直接由领域构造器接受的 Timeline 草稿。

本任务不渲染、不混音、不建立 App 页面，也不把本机素材路径交给模型。正式 App 入口归
LE-17/LE-19，正式安装包纵向验收归 LE-22/LE-23。

## Task 进度

| Task | 内容 | 状态 |
| --- | --- | --- |
| T1 | 句子 × 素材描述语义匹配，转写并入依据 | ✅ 已完成 |
| T2 | 在已验证可解码的镜头区间内选择等长片段 | ✅ 已完成 |
| T3 | 有人声素材按自带旁白段落编排 | ✅ 已完成 |
| T4 | 四类产品边界行为 | ✅ 已完成 |
| T5 | 产出满足全部领域不变式的 Timeline 草稿 | 🚧 实现与验收已绿，待 Review |

## 开工前【先定】

### T1 模型协议、输入与排序

1. 复用 `contracts/video/bailian-model-catalog.v1.json` 的 `script` 文本用途、固定 HTTPS
   端点和既有密钥来源，不新增第二份模型目录。供应商无关的
   `SemanticMatchingAdapter` 只接收句子、候选证据和请求级 Options；百炼请求/响应形状
   只存在于百炼 Adapter。
2. 输入脚本必须是完整的 `ScriptSegmentationResult`；候选必须是 `Material` 中可提供
   画面的 VIDEO/IMAGE，且已有非空 `ai_description`。AUDIO、重复 MaterialId、空候选或
   未理解素材全部在付费调用前拒绝，绝不静默过滤。
3. 模型只看到本地生成的 `m0001..mNNNN` 临时候选键、句子 sequence/text、素材描述与
   标签；`has_speech=true` 时必须把完整 `speech_transcript` 一并放入匹配依据，
   `has_speech=false` 时不制造 transcript 字段。模型看不到 MaterialId、摘要、尺寸、
   响度、时长、路径、浏览器或发布平台信息。
4. 首期最多一次匹配 32 条素材。为完整消费 LE-15 合法的 128 句，句子按固定 16 条一批
   请求；每批都含全部候选，最后在本地合并为完整矩阵。请求 JSON 最多 2 MiB，超过即在
   首次付费调用前整体拒绝，不截断描述或转写；模型响应与结构化正文继续使用硬上限。
5. 模型必须为本批每个 `sentenceSequence × candidateKey` 恰好返回一个整数分数
   `0..100`；缺项、重复、未知键/句号、额外字段、布尔冒充整数、非 `stop`、拒答、超时
   和畸形 JSON 都转换成固定、无异常链的
   `SemanticMatchingRejected("semantic matching request rejected")`，且不自动重试。
6. 模型不返回赢家。每句结果由本地按“分数降序、素材输入顺序升序”排列；首期相关阈值
   定为 `60`，`score >= 60` 才合格。T1 保留完整分数矩阵；T4 再决定没有合格候选时的
   产品结果。
7. `enable_thinking` 是请求级布尔配置，默认精确为 `false`，每个批次都显式发送；
   LE-24 后续只负责把同一用户总开关传入，不改协议。配置和 Adapter 的 `repr` 不得含
   API Key。

### T2 片段边界与 LE-07 截断事实

1. 视频的 `Material.duration_ms` 只代表容器头声明，不证明该时刻有可解码帧。选片器在
   造草稿前必须消费执行器对当前已批准源文件给出的“实际可解码区间”证据；不得用
   `duration_ms`、文件大小或失败码推断帧完备，也不得把验证结果回写成伪造素材时长。
2. 镜头候选区间由 `shot_boundaries_ms` 与实际可解码区间求交；没有镜头边界时只使用
   已验证区间。视频的 `source_out_ms - source_in_ms` 必须精确等于旁白占位时长，
   继续服从 LE-03 的不变速约束；静态图片没有源时间轴，可占任意合法旁白时长。
3. 片段选择只在语义合格且足够长的候选中进行：先按 T1 排名，再选能完整容纳占位时长
   的最早镜头区间；同分、同素材、同镜头都以输入和时间顺序稳定决胜，不再发模型请求。

### T3 有人声素材

1. 文案句子只分配给 `has_speech=false` 的素材；有人声素材不会消耗句子、不会排 TTS
   narration，也不会与无人声素材竞争句子槽位。
2. 每条有人声视频按素材输入顺序独立占段。取片范围覆盖它的主体
   `speech_segments_ms`，时长由原声区间决定；排同源 ambient 原声与
   `speech_transcript` 字幕。首期不做说话人分离。
3. 全部素材均有人声是合法的纯原声成片：忽略未使用的文案/TTS 结果，不因“没有句子
   分配”报错。纯无人声则自然退化为纯 TTS 旁白成片。

### T4 四类产品行为

| 场景 | 【先定】行为 |
| --- | --- |
| 无人声素材数量少于待分配句子 | 不复用同一素材，不产部分草稿；返回 `INSUFFICIENT_MATERIALS` |
| 有候选但实际可解码区间都短于对应占位时长 | 尝试下一名合格素材；全失败后返回 `SOURCE_TOO_SHORT` |
| 某句全部分数低于 60 | 不以最高低分硬配；返回 `NO_RELEVANT_MATERIAL` |
| 全部素材均有人声 | 成功产出纯原声草稿，不调用 TTS、不返回错误 |

固定产品失败结果不得携带文案、转写、MaterialId、模型请求 ID 或本机路径；同一次生成
全有或全无，不留下半份 Timeline。

### T5 Timeline 草稿

1. Visual 轨从 0 开始连续相接并精确结束于 Timeline.duration；首期不自动加转场。
2. 无人声段的 visual、narration、caption 三者起点与时长完全一致；有人声段的 visual、
   ambient、caption 三者按原声区间一致，且没有 narration clip。
3. 所有视频 clip 都携带等长 source in/out；图片 clip 不携带 source window。生成结果
   必须直接通过 `TimelineClip`、`TimelineTrack`、`Timeline` 构造器，不复制或绕开领域
   校验。

### T5 进程边界、身份与稳定顺序

1. T4 的 Executor 成功值不直接导入 Control Plane。Executor 先投影为
   `protocol/local_editing.py` 中版本化、无路径的最终段落计划；Control Plane 只消费该
   协议并调用领域构造器，继续保持两个部署单元只通过协议协作。
2. TTS 文件进入 Timeline 前必须由后续正式接线调用方注册为 AUDIO Material，并把
   `sequence + 固定相对路径 + MaterialId` 绑定交给 Executor 投影边界；边界核对绑定后只
   把 MaterialId 放进无路径协议，不把相对路径送进 Control Plane。T5 不伪造 MaterialId，
   也不拿画面 MaterialId 冒充旁白音频。
3. 混合草稿的稳定顺序定义为：先按素材输入顺序排列全部自带原声段，再按脚本 sequence
   排列全部 TTS 段。T3 已分别保留这两种顺序且没有可证明的跨集合顺序，T5 不凭空猜测
   交错关系。
4. narration 与原声 ambient 默认 `gain_db=0.0`；两类段落在时间上不重叠，因此原声段
   没有 narration sidechain，不会被当作背景声压低。首期不生成 music 或 transition。
5. 最终协议把单条 caption 限制同步到领域的 2000 字、总时长限制同步到
   `100..600000 ms`。上游更长的合法转写或素材仍由领域边界固定拒绝，不放宽、复制或
   绕过 Timeline 不变式。

## T5 RED 目标

1. 版本化最终段落协议能表达原声视频、旁白视频和旁白图片，但不含路径字段；素材身份、
   source window、字幕、总时长、顺序和 32 条上限全部 fail closed；
2. Executor 投影要求每个 TTS 句子恰好一个已注册 AUDIO Material 绑定，核对 sequence 与
   固定相对路径后丢弃路径；缺失、额外、错序、身份碰撞或篡改绑定统一固定拒绝；
3. Control Plane 工厂只导入协议与领域，构造连续 visual、按段对齐的 narration/ambient/
   caption，并直接返回领域 `Timeline`；纯原声不建 narration，纯旁白不建 ambient；
4. VIDEO source window 等长保留，IMAGE 保持 windowless，所有 transition 为 `None`，
   visual 精确结束于 Timeline.duration；
5. 协议常量与领域最小/最大时长、caption 上限建立漂移守卫，Executor 新模块继续不导入
   Control Plane，Control Plane 新模块不导入 Executor。

T5 分三次 RED：先让最终协议测试失败于类型不存在，再让 Executor 投影测试失败于公开
边界不存在，最后让领域工厂测试失败于模块不存在。每次确认目标缺失后才写对应生产代码。

## T5 RED 证据

1. 最终协议首次聚焦运行在收集期失败于 `local_editing` 没有
   `LOCAL_EDITING_TIMELINE_DRAFT_VERSION`，结果 `1 error`；
2. 协议 GREEN 后，Executor 投影首次聚焦运行在收集期失败于
   `speech_paragraph_draft` 没有 `NarrationMaterialBinding`，结果 `1 error`；
3. 投影 GREEN 后，Control Plane 工厂首次聚焦运行在收集期失败于
   `automation_tool.control_plane.application.local_editing_timeline` 不存在，结果
   `1 error`。

三次失败都来自当轮目标公开能力尚未实现；前一层已单独转绿后才开始下一层生产代码。

## T5 GREEN、功能验收与捕获力

共享协议新增 `local-editing.timeline-draft.v1` 的最终段落与整份草稿。原声段要求 visual 与
audio 使用同一个 MaterialId 且必须有等长视频 source window；旁白段要求单独的 AUDIO
MaterialId，visual 可为等长视频 window 或 windowless 图片。整份草稿固定全局 sequence、
唯一 visual、唯一且不碰撞 visual 的旁白 audio、32 条素材合计上限、`100..600000 ms`
总时长和 2000 字 caption 上限，冻结值跨公开边界时逐字段重建。协议不含 path 字段。

Executor 新增本地 `NarrationMaterialBinding`：调用方必须先把 TTS 文件注册为 AUDIO
Material，再按 TTS sequence 与固定相对路径绑定身份。投影边界逐项核对后丢弃相对路径，
只把 UUIDv4 身份送入共享协议；混合结果稳定按“素材输入顺序的原声段 → 脚本 sequence 的
旁白段”排列。Executor 继续不导入 Control Plane。

Control Plane 新工厂只导入共享协议和现有领域模型。它按累计时长构造无转场的连续 visual；
原声段排同源 ambient，旁白段排注册后的 narration，全部段落排 caption，音频默认
`gain_db=0.0`。VIDEO 保留等长 in/out，IMAGE 保持 windowless。最终对象逐个调用
`TimelineClip`、`TimelineTrack`、`Timeline` 构造器，新模块不导入 Executor。

三个新聚焦文件分别 `11 passed`、`8 passed`、`7 passed`；与 T2/T3/T4 合跑覆盖率为
`139 passed`，四个相关生产模块共 519 条语句、152 个分支全部 `100%`。LE-16 T1～T5、
旁白链路与 Timeline 领域受影响单元回归 `417 passed`。在 LE 收口点按约定运行完整
`backend/tests/integration`：受限沙箱首次因禁止绑定 `127.0.0.1` 端口而产生环境级级联
失败，该结果不计入产品证据；完整权限原样重跑最终为 `458 passed, 8 skipped`，耗时
`394.09s`，skip 均为需要显式真人/真实云验收的既有标记。Ruff、严格 mypy、diff、路线图
计数与证据深度门禁全绿。

分层功能验收从 T4 的混合成功值开始，经旁白 Material 绑定投影到无路径协议，再从公开
Control Plane 工厂得到真实领域 Timeline。实际终态总长 `1200 ms`，轨道为
`visual/narration/ambient/caption`；visual 三段分别为 `0+300`、`300+400`、`700+500`，
source window 为 `100..400`、`200..600`、图片 `null..null`；narration 只在后两段，
ambient 只在首个原声段，三段 caption 起点和时长与 visual 完全一致，协议字段扫描
`protocolHasPathField=false`。这仍是 LE-19 正式 App 接线前的分层真实对象验收，不冒充
用户可操作终态。

捕获力验证在 `/private/tmp` 隔离包把累计起点错误增加 1ms，并先确认导入精确来自隔离
副本。混合工厂用例立即得到 `1 failed, 6 deselected`，且失败由真实 `TimelineTrack`
领域构造器拒绝 visual 间隙触发，证明测试与领域双重阻止非连续草稿。隔离副本与覆盖率
文件均已清理。实现检查点提交推送后执行 T5 与 LE-16 模块最终 Review，修完全部 finding
并复审通过后才收口。

## T3 RED 目标

1. 混合输入只把 `has_speech=false` 的素材交给无人声句子/TTS/T1/T2 编排边界；有人声
   素材不出现在任何句子候选中；
2. 多条有人声视频无论外部语义顺序如何，始终按素材输入顺序形成独立原声段；
3. 每个原声段的 source window 覆盖主体首个人声起点到最后一个人声终点，visual、
   ambient、caption 的段内起点和时长完全一致，且不存在 narration；
4. 原声段字幕精确使用完整 `speech_transcript`，不制造第二份转写或 TTS；
5. 全部素材有人声时成功返回纯原声中间草稿，无人声编排 Adapter 不得被调用；
6. 纯无人声输入不制造 ambient 原声段，原样保留既有句子、旁白与 T1/T2 候选输入；
7. 重复素材身份、非视频声明语音、语音段越界或乱序、转写缺失等非法协议值统一
   fail closed，固定错误不得包含文案、转写、MaterialId 或路径。

首次 RED 必须精确失败于目标编排模块尚不存在；确认后才创建生产实现。T3 不提前实现
T4 的四类产品错误，也不构造 T5 的领域 `Timeline`。单个 Task 只运行专项、受影响回归
和静态门禁；整目录 integration 留到 LE-16/T5 收口时按实际跨层影响决定。

## T3 RED 证据

首次运行 `test_speech_paragraph_draft.py` 在收集期精确失败于
`automation_tool.executor.speech_paragraph_draft` 不存在，结果为 `1 error`；协议扩展与
生产模块均尚未创建，因此失败来自目标行为缺失，不是夹具、导入路径或环境依赖错误。

## T3 GREEN、功能验收与捕获力

最小实现新增版本 `local-editing.speech-paragraph.v1` 的路径无关人声素材投影，以及
`executor/speech_paragraph_draft.py` 段落草稿编排边界。有人声视频直接按素材输入顺序
形成独立原声段，source window 从主体首个人声起点覆盖到末段终点，段内 visual、ambient、
caption 均从 0 开始且时长一致，字幕精确使用完整转写，`narration is None`。无人声规划
边界只接收 `has_speech=false` 的素材 UUID；其已有句子、TTS 相对路径和 T2 候选被原样
保留给 T4，Adapter 返回有人声身份会被固定无泄漏错误拒绝。全部素材有人声时不调用该
Adapter，直接得到纯原声中间草稿。新 Executor 模块不导入 Control Plane 实现。

首轮专项 `32 passed`；Review 修复后专项扩展为 `39 passed`，与 T2 协议测试合跑
`91 passed`，新增编排模块 102 条语句/20 个分支、共享协议模块 96 条语句/24 个分支均为
`100%`。T1/T2、脚本分句与 TTS 受影响回归为 `228 passed`。三份改动文件 Ruff
format/check、严格 mypy 与 `git diff --check`
全部通过；按用户指定粒度没有在 T3 重跑整目录 integration，留到 LE-16/T5 收口时按
跨层影响决定。

功能验收从公开 `build_speech_aware_paragraph_draft` 入口构造一条有人声视频与一条无人声
视频，得到协议版本 `local-editing.speech-paragraph.v1`；原声 source window 为
`100..1600 ms`，visual/ambient/caption 三者均为 `1500 ms`，字幕为 `真实原声字幕`，
`hasNarration=false`；Planner 与 narrated 候选都只出现无人声素材 UUID。这是分层功能
验收，正式 App 用户入口仍由 LE-19 装配，不在 T3 冒充用户可操作终态。

捕获力验证在 `/private/tmp` 隔离副本把无人声过滤条件反转，并先核对导入模块
`__file__` 精确指向隔离副本；混合输入守卫立即得到 `1 failed, 31 deselected`，失败点
为编排边界拒绝错误身份。隔离副本已删除，工作树生产文件未变异。

## T3 首轮提交级 Review finding 与修复

实现检查点 `5e07e18f` 推送后尝试运行 `codex review --commit 5e07e18f`，但当前执行环境
因“可能向未明确目标的外部服务发送私有提交源码与文档”拒绝启动。没有绕过该限制；改为
对该提交逐边界执行本地结构化 code review，并发现一项有效边界缺陷：Planner 返回的
`NarratedParagraphDraft` 虽然是冻结 dataclass，但若经错误反序列化或
`object.__setattr__` 绕过构造器篡改路径/候选字段，编排只检查 `isinstance` 就会把非法值
带入输出。

修复轮先把旁白相对路径篡改成 `C:/Users/private/secret.wav`，聚焦测试精确得到
`1 failed, 32 deselected`，证明旧边界没有二次校验。GREEN 在编排入口重建全部
`SpeechParagraphMaterial`，并对 Planner 返回的每个段落和每个
`FittingMaterialSegment` 逐字段重建，重新触发唯一公开构造校验；非法值统一收敛为固定、
无异常链且不含路径/转写/身份的拒绝。聚焦测试转为 `1 passed, 32 deselected`，扩展后的
专项、覆盖率、受影响回归和静态门禁结果见上一节。修复提交完成后继续本地提交级复审，
无 finding 前 T3 不升为完成。

修复提交 `aafa5a94` 推送后完成第二轮本地提交级 Review：逐项核对素材与 Planner 返回值
重建、异常链收敛、Windows/Unix 路径拒绝、有人声身份隔离、T2 候选时长与 UUIDv4
不变式，并复核提交 diff 只含目标修复、红测和证据；结合 `91 passed` 的 100% 语句/分支
覆盖、`228 passed` 受影响回归、Ruff 与严格 mypy 结果，未发现新的 finding。T3 因此正式
完成，下一步进入 T4 四类固定产品结果。

## T4 RED 目标

1. 无人声素材总数少于待分配句子时返回 `INSUFFICIENT_MATERIALS`，不返回部分草稿；
2. 某句存在语义合格候选，但所有实际可解码窗口都太短时返回 `SOURCE_TOO_SHORT`；若首名
   太短而下一名存在合格窗口，必须选择下一名成功，不得提前失败；
3. 某句全部语义分数低于 60 时返回 `NO_RELEVANT_MATERIAL`，不得把最高低分硬配；
4. 全部素材有人声时继续成功返回纯原声草稿，不调用无人声 Planner、不返回产品错误；
5. 无人声素材不得复用。逐句贪心会卡死但存在完整唯一匹配时，必须稳定回溯到完整方案；
   确实不存在唯一完整匹配时返回 `INSUFFICIENT_MATERIALS`；
6. 产品失败结果只含固定枚举码，不携带文案、转写、MaterialId、模型请求 ID 或路径；
   一次生成全有或全无。

T4 直接扩展 T3 的路径无关段落草稿协议与实现，不创建名字相近的新编排模块；不构造
领域 `Timeline`。首次 RED 必须失败于 T4 公开结果与解析入口尚不存在。

## T4 RED 证据

首次运行 `test_paragraph_draft_resolution.py` 在收集期精确失败于现有
`speech_paragraph_draft` 模块没有 `ParagraphDraftFailure` 与 T4 解析入口，结果为
`1 error`；T3 模块和测试环境可正常导入，因此 RED 来自目标产品结果尚未实现。

## T4 GREEN、功能验收与捕获力

T3 中间草稿现在显式保留本轮全部无人声素材身份，以及每句达到 60 分阈值的完整稳定排名；
T4 只从 T2 已证实可解码且等长的候选中做全局唯一分配。选择器按句子和候选顺序寻找
字典序最小的完整方案：当前首选会堵死后续句子时先跳到下一名，后续匹配也能通过增广路径
重排，既不复用素材，也不会把本来可完整生成的请求误报为素材不足。无人声素材总数先于
其他条件判定不足；其余失败按句子顺序稳定收敛为 `NO_RELEVANT_MATERIAL`、
`SOURCE_TOO_SHORT` 或无法形成唯一完整匹配的 `INSUFFICIENT_MATERIALS`。失败值只含固定
枚举码，不附带部分草稿或输入内容。全部有人声继续返回纯原声成功值。

首次 GREEN 后专项为 `49 passed`。边界自审随后发现，公开解析入口曾对被绕过冻结约束后
篡改成 `list` 的 `silent_material_ids` 静默执行 `tuple(...)`，没有像其他跨边界值一样严格
拒绝。先补回归并精确得到 `1 failed, 10 deselected`，再取消该容错转换；扩展后的 T3/T4
专项为 `55 passed`。与 T2 及共享协议合跑 `107 passed`，三个相关生产模块共 391 条语句、
126 个分支全部 `100%`；T1、T2、文案分句、旁白和 T3/T4 受影响回归为 `267 passed`。
三份改动文件 Ruff format/check、严格 mypy、`git diff --check`、路线图计数和验收证据深度
门禁全部通过；后两项分别报告 `local editing roadmap counts are consistent` 与
`acceptance evidence depth: 49 checks passed`。按会话约定没有在 T4 重跑整目录 integration，
留到 LE-16/T5 模块收口。

分层功能验收通过公开 `resolve_speech_aware_paragraph_draft` 一次覆盖四类产品结果与唯一
分配，实际输出为：素材不足 `INSUFFICIENT_MATERIALS`、全部合格候选过短
`SOURCE_TOO_SHORT`、全部低于阈值 `NO_RELEVANT_MATERIAL`；纯原声输入得到 1 个原声段、
0 个 TTS 段；贪心首选会堵死第二句的输入最终得到 `[true, true]`，即第一句稳定退到弹性
素材、第二句取得唯一必需素材。这仍是正式入口接线前的分层功能验收，不冒充 LE-19 的
用户界面终态。

捕获力验证在 `/private/tmp` 的隔离包副本中把“只有候选仍能给剩余句子形成完整匹配才可
选择”反转。先确认导入文件确实来自隔离副本，再运行回溯用例，精确得到
`1 failed, 15 deselected`：错误实现返回 `INSUFFICIENT_MATERIALS`，测试能阻止退化为逐句
贪心。隔离副本与覆盖率文件均已清理，工作树不保留变异。实现检查点提交并推送后执行
本 Task 提交级代码审查，无 finding 或修完全部 finding 后才把 T4 升为完成并进入 T5。

## T4 首轮提交级 Review finding 修复

实现检查点 `2fcd504e` 推送后执行提交级代码审查，并用固定种子的 5600 组候选图把公开
解析结果与暴力枚举的字典序最小唯一分配逐一对照，全部一致。审查发现一项边界 finding：
T4 的成功值构造器只确认嵌套值类型，没有逐字段重建；调用方若绕过冻结约束篡改 T2 候选
分数、原声时间或已选旁白路径，再包装成成功值时会漏过。相关输入/输出构造器还分别限制
原声与无人声数量，没有对一次请求最多 32 条素材执行合计限制。

修复轮先补 5 条回归，精确得到 `5 failed, 16 deselected`，分别证明上述三类篡改与两处
合计数量越界都会被旧实现接受。GREEN 抽取并复用 T2 候选逐字段重建，成功值同时重建原声
和已选旁白段，T3 中间值与 T4 成功值都按原声数加无人声/旁白数执行 32 条总上限。修复
复核又发现成功值会先迭代错误容器再判断它是否为 tuple；第 6 条 RED 精确得到
`1 failed, 21 deselected` 并证明错误 list 被消费一次，随后调整为先拒绝外层类型、再重建
内部值。最终相关覆盖合跑 `113 passed`，409 条语句、128 个分支全部 `100%`；受影响回归
`273 passed`，Ruff、严格 mypy、diff、路线图计数与证据深度门禁全绿。公开入口四类结果
与回溯验收再次得到相同终态，5600 组暴力枚举对照也再次通过。修复提交推送后继续第二轮
提交级 Review，无 finding 前 T4 仍不升为完成。

## T4 第二轮提交级 Review 与收口

修复提交 `68f5b958` 推送后完成第二轮提交级 Review。当前执行策略仍禁止外部 Review 进程
读取私有仓库源码，因此本轮继续采用本地结构化审查：逐项检查错误外层容器在迭代前拒绝、
T2 候选/原声/已选旁白逐字段重建、原声与无人声身份互斥、32 条合计上限、固定异常无
敏感链，以及修复 diff 是否只含目标 finding。复审同时核对 `113 passed` 的 100% 语句/
分支覆盖、`273 passed` 受影响回归、Ruff、严格 mypy、文档门禁和 5600 组暴力枚举对照，
未发现新的 finding。T4 因此正式完成，下一步进入 T5 领域 Timeline 草稿与 LE-16 模块
收口。

## T1 RED 目标

1. 百炼实现满足供应商无关协议，默认/开启 thinking 均精确进入请求，密钥不进 repr；
2. 请求只含临时候选键、句子、描述/标签和有声素材转写，不含领域身份与私有元数据；
3. 16 句固定分批仍合并出完整矩阵，每个句子 × 素材组合恰好一项；
4. 本地按分数和素材输入顺序稳定排序，`60` 分含边界、`59` 分不含；
5. AUDIO、重复素材、缺描述、超数量/请求预算在首次付费调用前拒绝；
6. 未知/缺失/重复矩阵项、越界/伪整数分数、超时/拒答/非 stop 只产生固定无泄漏异常。

首次运行聚焦测试必须因 `automation_tool.executor.semantic_matching` 不存在而在收集期
失败；确认 RED 后才允许创建生产模块。

## T1 RED 证据

首次运行 LE-16 T1 聚焦测试在收集期精确失败于
`automation_tool.executor.semantic_matching` 不存在，结果为 `1 error`；生产实现尚未
创建，因此该 RED 没有经过待测代码。

## T1 GREEN、边界与捕获力

最小实现新增供应商无关的请求/回复、完整分数矩阵和结果协议，以及复用既有 `script`
用途的百炼 OpenAI-compatible Adapter。最多 32 条视觉素材，LE-15 合法的最多 128 句
按 16 句固定分批；所有批次在首次付费调用前完成类型、身份、描述和 2 MiB 请求预算
预检。模型只见 `m0001..mNNNN` 临时键和明示语义证据，完整矩阵由本地闭包校验，排序、
并列决胜和 `score >= 60` 资格均由本地完成。

T1 专项最终为 `45 passed`，新生产模块 291 条语句、102 个分支均为 `100%`；与既有
脚本分句相关回归合跑为 `101 passed`。Ruff format/check、两份改动文件严格 mypy、
`git diff --check`、路线图计数和验收证据深度门禁全部通过，后两项分别报告
`local editing roadmap counts are consistent` 与 `acceptance evidence depth: 45 checks
passed`。按会话约定没有在本子任务运行整目录 integration，LE-16 模块全量留到 T5
收口。

捕获力验证临时把本地阈值从 60 改成 61，边界与稳定排序用例立即得到
`1 failed, 44 deselected`，精确证明 60 分本应合格却被判为不合格；还原后完整专项及
100% 语句/分支覆盖门禁恢复绿色，工作树不保留变异。

## T1 首轮 Codex Review 与修复

首轮 `codex review --commit ea772f9` 确认两项边界缺陷：

1. 上游 `Material` 合法允许描述和转写包含换行、制表符，LE-14 多批次 ASR 也会以换行
   拼接完整转写；T1 候选验证却会在付费调用前拒绝这些合法证据。
2. 百炼返回可解析对象但缺少 `choices`、`id`、`message`、`content` 或
   `finish_reason` 时，公开 Adapter 会泄漏原始 `KeyError`，没有收敛到固定拒绝。

修复轮先新增多行素材证据与缺失响应字段测试，精确得到 `2 failed`；随后把候选字符
边界与 `Material` 对齐，仅允许 `\n`、`\t`，并在 Adapter 内捕获缺失字段。两条聚焦
测试转为 `2 passed`，扩展后的 T1 专项为 `51 passed`，生产模块 293 条语句、102 个
分支继续为 `100%`；与脚本分句回归合跑为 `107 passed`。修复提交完成后必须再次运行
Codex Review，复审通过前 T1 不得升为完成。修复提交 `2c04b98` 已推送；第二轮
`codex review --commit 2c04b98` 确认两项修复均正确，专项测试、Ruff 和严格 mypy
通过，未发现回归，因此 T1 正式完成。

## T2 RED 目标与证据

T2 新增路径无关的实际可解码区间证据，必须绑定 `MaterialId + content_digest`；VIDEO
必须恰好各有一份证据，IMAGE 不制造源时间轴证据。选片先把镜头切点分出的区间与实际
可解码区间求交，再按 T1 的分数降序、素材输入顺序稳定排列，每条素材只保留最早的等长
可用片段。容器声明 `duration_ms` 不参与补尾：截断素材即使自报完整时长也不能越过已
验证帧；反向地，真实可解码区间超过偏短声明时也不被声明截断。

RED 覆盖 faststart 截断前缀、偏短容器声明、镜头与解码区间求交、无镜头边界、图片、
低分/过短过滤、稳定决胜，以及证据缺失、重复、错绑、矩阵漂移、AUDIO 和公开值非法
边界。首次运行在收集期精确失败于
`automation_tool.executor.segment_selection` 不存在，结果为 `1 error`；生产模块尚未
创建。

## T2 GREEN、边界与捕获力

最小实现新增路径无关、绑定 `MaterialId + content_digest` 的实际可解码区间协议，以及
按语义分数和素材输入顺序稳定排列的等长片段选择器。VIDEO 必须恰好各消费一份当前素材
字节对应的证据，IMAGE 不制造源时间轴；镜头区间与可解码区间求交后只取时间上最早的
完整窗口。`Material.duration_ms` 不参与选择，所以既不会给 faststart 截断素材补出
不存在的尾帧，也不会裁掉真实可解码、但超过偏短容器声明的帧。

实现检查点的 T2 专项为 `29 passed`，新生产模块 106 条语句、46 个分支均为 `100%`；
与 T1 语义匹配、脚本分句和旁白相关回归合跑为 `166 passed`。Ruff format/check 与
两份改动文件严格 mypy 均通过。按会话约定没有在本子任务运行整目录 integration，
LE-16 模块全量留到 T5 收口。

捕获力验证临时把镜头交集“长度恰好等于占位时长也可用”的 `>=` 改成 `>`，最早候选
因此从 2000ms 错移到 10000ms，聚焦测试精确得到 `1 failed, 28 deselected`；恢复后
相关回归重新全绿，工作树不保留变异。实现检查点提交并推送后执行本 Task 的
Codex Review，复审通过前 T2 不升为完成。

## T2 首轮 Codex Review finding 修复

实现检查点 `d9c507c` 推送后的 Codex Review 给出一项 P1、两项 P2，均有效：

1. 新选择器位于 Local Executor，却直接导入 Control Plane 的 `Material`、`MaterialId`
   和 Timeline 常量，违反两进程只通过版本化 `protocol/` 协作的边界，也会让冻结包的
   import 图带入服务端内部实现。
2. `VerifiedDecodableMaterial` 强制至少一个区间，无法表达容器头可读但零帧可解码的
   合法事实，因而不能自然尝试下一候选或在 T4 收敛为 `SOURCE_TOO_SHORT`。
3. 镜头与解码区间使用两层循环；合法上限 4096 个镜头边界 × 4097 个解码区间会产生约
   1680 万次求交。Review 的单素材探针实测约 `6.103s`，32 条素材会进一步放大阻塞。

修复轮先补三条回归，精确得到 `3 failed, 29 deselected`：零区间构造被固定拒绝；65 个
有序解码区间会把同一镜头序列重复遍历 65 次；源码仍含
`automation_tool.control_plane`。GREEN 新增版本
`local-editing.segment-selection.v1` 的路径无关素材与 T1 排名 DTO，共享限制和 UUIDv4
边界全部进入 `automation_tool.protocol.local_editing`；选择器只消费协议值，不再导入
Control Plane 或 T1 内部实现。零区间证据保留精确素材/摘要绑定并自然产生空候选；两个
严格有序区间序列改用双指针线性求交，找到最早完整窗口立即返回。

修复后，4096 个镜头边界与 4097 个解码区间的同形探针从约 `6.103s` 降至
`0.001986s`。自审又发现从强类型 `MaterialId` 改为协议 UUID 后，证据与结果值只检查
`isinstance(UUID)` 会放过 nil/非 v4 身份；先补两条公开值回归得到
`2 failed, 12 passed, 37 deselected`，再复用协议层唯一 canonical UUIDv4 判据收紧。
扩展后的 T2 专项为 `51 passed`；选择器与新协议模块共 170 条语句、
58 个分支均为 `100%`；与 T1 语义匹配、脚本分句和旁白相关回归合跑为
`188 passed`。Ruff format/check 和三份改动文件严格 mypy 均通过。修复提交推送后
执行第二轮 Codex Review，复审无 finding 前 T2 仍不升为完成。

## T2 第二轮 Codex Review finding 修复

第二轮 `codex review --commit beda123` 确认一项 P2：协议值只保证分数降序，合法的
同分候选元组仍可能与 `materials` 输入顺序相反；选择器直接迭代候选元组后，会丢掉 T2
固定的“同分按素材输入顺序”决胜。Review 探针以 `materials=(a,b)`、候选
`(b@80,a@80)` 精确得到错误结果 `(b,a)`。

修复轮先把同一合法协议值写成边界测试，精确得到 `1 failed, 51 deselected`；GREEN
在选择边界恢复 `(-score, material_index)` 排序，因此即使协议输入中的并列顺序漂移，
结果仍由本次素材输入顺序确定。扩展后的 T2 专项为 `52 passed`；选择器与协议模块共
172 条语句、58 个分支均为 `100%`；受影响回归合跑为 `189 passed`。Ruff
format/check、三份改动文件严格 mypy 与 `git diff --check` 全部通过。按模块测试粒度
规则没有运行整目录 integration；LE-16 模块全量仍留到 T5 收口。本轮修复提交并推送后
执行第三轮 Codex Review，第三轮无 finding 前 T2 不升为完成。

## T2 第三轮 Codex Review 与收口

同分决胜修复提交 `7ff632f` 推送后执行第三轮
`codex review --commit 7ff632f`。Review 独立复跑 T2 专项 52 条、Ruff 和严格 mypy，
并对最多 6 条素材的全部协议合法并列排列执行穷举探针；结果全部通过，确认不同分数仍按
分数降序、同分始终按素材输入顺序，未发现回归或其他 finding。T2 因此正式完成；下一步
为 T3 有人声素材按自带旁白段落编排，整目录 integration 和 LE-16 模块全量仍按约定留到
T5 收口。
