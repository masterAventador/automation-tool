# PC-13 — 中文字体内置：实测报告

> 状态：**已完成** ✅
>
> 字体已进包、生成机制已在真实内置 Chromium 上验过并产出成片。
> **渲染期注入已于 2026-07-29 收口**——接进了 `write_part_working_copy`，
> 并在真实 App 用户路径出的成片里核对到中文正常显示、无豆腐块（见第 20 节）。
> **Windows x86_64 复测已于 2026-07-31 闭合**，见第 20.1 节。
> 产品负责人 2026-07-27 定：取全字符集 7.42 MB，并把拉丁那 78 个一并修。
> 日期：2026-07-27
> 分支：`feature-audit`
> 证据目录：`.local/embedded-browser-video-studio/pc-13-evidence/`

用户可操作：否
证据类型：查证

---

## 0. 一句话结论

**「同名 `@font-face` + `unicode-range` 让汉字走中文字体」这条路在内置 Chromium 149 上成立，
不需要改 `vendor/hyperframes` 一个字节**——但成立的条件比预期严格一条：
**必须按零件实际请求的每一个 `(family, font-weight)` 组合各声明一条 CJK face**，
只声明 400、或声明 `font-weight: 100 900` 变量区间，**都不生效**，字还是掉回系统字体。

推荐形态：**一个 Noto Sans SC 可变字体 woff2 + 按零件扫描出的 (family, weight) 逐条声明**。
一个文件覆盖全部 7 个字重，实测两个字重的字形确实不同（不是假粗）。

体积（净增，安装包基线 `.local/release/release-package.json` 记录的 dmg 490,943,549 字节）：

| 覆盖档 | 单文件 woff2 | 占 dmg | 离线字体资产由 1.01 MB 变成 |
| --- | --- | --- | --- |
| GB2312（6,763 汉字） | 2,043,768 B（1.95 MB） | +0.42% | 2.96 MB（×2.9） |
| GBK（20,902 汉字） | 5,846,260 B（5.58 MB） | +1.19% | 6.59 MB（×6.4） |
| 全字符集（30,890 码点） | 7,782,440 B（7.42 MB） | +1.59% | 8.43 MB（×8.2） |

**建议取全字符集或 GBK，不建议 GB2312。** 理由在第 6 节：GB2312 缺的不是生僻字，是人名常用字。

---

## 1. 先纠正一件事：跨平台不一致比想的严重，而且不只是中文的问题

原始判断是「中文靠系统兜底 → 跨平台不一致」。全量扫过 134 个零件后，事实更糟：

```
134 个零件
├── 56 个   上游 <link> 了 Google Fonts 的 css2（离线目录里其中 54 个重写成了本地 CSS，
│           parallax-zoom / parallax-unzoom 的引用落在冻结文件清单之外）
├── 61 个   CSS 里写了具名字体，但整份文档从不加载任何 @font-face
└── 17 个   一个具名字体都没写，直接吃 body 默认
```

**也就是说 78 个零件（58%）今天连拉丁字母都在吃系统兜底**，不只是汉字。
被点名却从未装车的字体包括 `Archivo Black`（3 个零件）、`League Gothic`（2）、
`Oswald`、`Impact`、`Arial Black`、`Barlow Condensed`、`SF Pro Display`/`SF Pro Text`（12）、
`Menlo`/`Monaco`/`Consolas`/`Courier New`（各 24）、`Segoe UI`（13）、`Helvetica`（13）。

这一条在本次实测里有直接证据。对照组 A（今天的行为）渲染 `lt-bold-block`，
`#lt-tag` 的拉丁字符报的是 `SpaceMono-Bold` 且 `isCustomFont: false`——
**那是本机 `~/Library/Fonts` 里装着的 SpaceMono-Bold.ttf（Powerline 字体包带的），不是产品带的。**
换一台没装的机器，同一个零件的同一行字就是另一个字形。这正是 BM-16「同输入同版本出同结果」
要挡的那类漂移，而它今天已经在了。

**本任务只解决汉字那一半。** 拉丁那 78 个零件要不要一起补齐字体，是另一个决策，见第 8 节。

## 2. 现有取用机制（读契约得到，不是推断）

`contracts/video/offline-motion-dependencies.v1.json`：

- `artifacts` 里 `kind: "font"` 的 52 条，共 **1,031,980 字节**，从 `fonts.gstatic.com` 下载、锁 sha256；
- `fontFamilies` 那 18 条是**许可证台账**（`repositoryPath` 指 google/fonts 的 `ofl/` 目录 + 锁定 commit
  `00e726a9…` + 许可证文件摘要），不是文件清单；
- `stylesheets` 里每个 face 已经带 `unicodeRange`——**Google Fonts 本来就是按码点分区分发的，
  这个仓库已经在用这套机制**；
- `build_offline_motion_catalog.py:stylesheet_css()` 从 face 记录生成 `@font-face`，**它是通用的**。

第三点很重要：本报告推荐的机制不是新发明，是把已经在跑的分区机制多用一次。
`scripts/test_motion_cjk_font_probe.py::test_cjk_face_records_are_emitted_by_the_existing_generator`
把这一点钉住了——CJK face 记录喂给**未经修改的** `stylesheet_css()`，输出即为所需 CSS。

## 3. 实测：内置 Chromium 到底怎么选 face

环境：`.local/desktop-e2e/embedded-browser/macos-arm64/…/Google Chrome for Testing`，
`Chrome/149.0.7827.55`，用 `worker.mjs` 生产渲染会话的同一组 flag
（`--headless --remote-debugging-pipe --disable-lcd-text --disable-font-subpixel-positioning
--force-color-profile=srgb --proxy-server=127.0.0.1:9` …）。

被测零件：`lt-bold-block`，取自离线目录的**未经修改**副本，只把两个文本节点换成中文：
`Maya Chen` → `星云科技 Nebula`，`Host · Neuroscientist` → `主持人 · 神经科学家`。
视口 1920×1080，时间轴 seek 到 2.0 s。

测的是 `CSS.getPlatformFontsForNode`——浏览器逐元素报告**哪个物理字体画了几个字形**。
这个数字没真跑过写不出来。

零件自己的 CSS 请求：`#lt-name` = `"Archivo Black"` / 400，`#lt-tag` = `"Space Mono"` / 700。

| 变体 | 注入了什么 | `#lt-name`（400） | `#lt-tag`（700） |
| --- | --- | --- | --- |
| **A 对照** | 什么都不注入 | PingFang SC Regular ×11（含拉丁） | 系统 Space Mono Bold ×3 + PingFang SC Semibold ×8 |
| **B** | 拉丁 400/700 + CJK@**400** | PingFang ×7 + **Noto Sans SC ×4** ✅ | Space Mono Bold ×3 + **PingFang SC Semibold ×8** ❌ |
| **C** | B + CJK@**700** | 同 B ✅ | Space Mono Bold ×3 + **Noto Sans SC ×8** ✅ |
| **D** | 可变字体，声明 `font-weight: 100 900` | PingFang ×7 + Noto ×4 ✅ | **PingFang SC Semibold ×8** ❌ |
| **E** | 同一个 400 文件，分别声明成 400 和 700 | ✅ | **Noto ×8** ✅（但字重是 Regular，见下） |
| **F 推荐** | 一个可变 woff2，按零件的 (family,weight) 逐条声明 | ✅ `NotoSansSC-Thin_Regular` ×4 | ✅ `NotoSansSC-Thin_Bold` ×8 |

原始 JSON 在 `.local/embedded-browser-video-studio/pc-13-evidence/reports/variant-*.json`。

### 3.1 关键发现：字重分桶发生在 unicode-range 之前

B 和 D 是本次最有价值的两个失败。按 CSS Fonts 规范的字面读法，
应该先用 `unicode-range` 筛出覆盖该码点的 face，再在其中做字重匹配——
**Chromium 149 不是这么做的**：它先按 (weight, width, slope) 把同名家族的 face 分桶，
选中最匹配请求字重的那个桶，**再**在桶内解析 `unicode-range`。

于是在变体 B 里，家族 `Space Mono` 有三条 face：拉丁@400、拉丁@700、CJK@400。
元素请求 700 → 命中 {700} 桶 → 桶里只有拉丁 face → 汉字无覆盖 → **直接掉系统兜底，
根本不回头看 {400} 桶里那条能覆盖汉字的 CJK face**。
`document.fonts` 的状态是直接证据：变体 B 的 `Space Mono/400`（CJK 那条）始终是 `unloaded`，
浏览器压根没打算用它。

变体 D 说明 `font-weight: 100 900` 也救不了：变量区间 face 一样输给精确 `700` 的 face。
（`#lt-name` 那侧 D 是成功的——因为 `Archivo Black` 家族只有那一条 face，桶唯一。）

**结论：CJK face 必须按零件实际请求的字重逐条声明。** 这条如果漏掉，
就会出现「大部分零件中文正常、少数零件中文莫名其妙掉回系统字体」这种极难归因的偶发问题。

### 3.2 一个可变字体文件能不能供全部字重：能

变体 E 证明「同一个静态 400 文件声明成 700」技术上能命中，但渲染出来是 Regular 粗细——
浏览器相信了 face 的自述，不做合成加粗，于是拉丁是 Bold、汉字是 Regular，视觉打架。

正解是可变字体。单独做了一次对照（`render/weight-check.png`）：
同一个 `nssc-variable-full.woff2`，只声明 400 和 700 两条 face，渲染同一串汉字，
Chromium 分别报告 `NotoSansSC-Thin_Regular` 与 `NotoSansSC-Thin_Bold`
（`_Regular`/`_Bold` 后缀是 Chromium 给**派生变体实例**的命名，证明 wght 轴确实被设到了 400 和 700），
图上两行字的笔画粗细肉眼可辨。

> `postScriptName` 里的 `NotoSansSC-Thin` 是可变字体默认实例的名字（wght 轴默认值 100），
> 不代表渲染成了 Thin；后缀才是实际用到的实例。

## 4. 真实产物

用变体 F 的文档，按零件自己声明的 `data-duration: 4.8` 与 30 fps，
在内置 Chromium 里以 **1920×1080 原生尺寸**逐帧截了 144 帧，再用 ffmpeg 合成：

```
$ ffprobe -v error -show_entries stream=codec_name,width,height,r_frame_rate,nb_frames,pix_fmt \
          -show_entries format=duration,size .../pc13-lt-bold-block-zh.mp4
codec_name=h264
width=1920
height=1080
pix_fmt=yuv420p
r_frame_rate=30/1
nb_frames=144
duration=4.800000
size=40037
```

```
$ shasum -a 256 pc13-lt-bold-block-zh.mp4
6c1a18521d5981b5b0dd87674e5bba23d53e9bc6b65d7da800cb9c60bea55e85
```

144 帧共 3,291,788 字节，其中 **38 个互不相同的摘要**——即画面确实在动，
不是「渲了 144 张同样的图」（`reports/frames.json`）。
第 80 帧裁切图 `render/mp4-frame80.png`：黑块里「星云科技 NEBULA」，
黄标签里「主持人 · 神经科学家」，标签的中文明显比标题的中文粗——两者出自同一个字体文件的两个实例。

单帧静图另存：`render/variant-a.png`（对照，24,613 B，sha256 `8858fac0…`）与
`render/variant-f.png`（推荐，24,627 B，sha256 `daf7ad09…`）。
推荐变体重跑一次得到同一个 sha256 `daf7ad0928e75d0685cb55384302cae34dc441a80578161a7954f7bb4c9d2450`
——注入 CJK face 之后这一帧仍然是逐字节确定的，没有破坏 BM-16 要的那个性质。

**这条片子里拉丁的 `NEBULA` 仍然是 PingFang**，因为 `Archivo Black` 本来就没进过包——
第 1 节说的那个独立问题，本任务没有解决，也不该被这条产物掩盖过去。

## 5. 字体选型与许可

**Noto Sans SC**（即 Source Han Sans / 思源黑体）。

```
许可证：SIL Open Font License 1.1
声明行：Copyright 2014-2021 Adobe (http://www.adobe.com/), with Reserved Font Name 'Source'
OFL.txt：4,388 字节  sha256 1c05c68c34f9708415aada51f17e1b0092d2cea709bf4a94cd38114f9e73d7d9
```

与现有 18 个家族同为 OFL-1.1，可再分发，可嵌入产品，无署名弹窗要求（保留 OFL 文本即可）。

上游可以**锁在现有 18 个家族已经锁定的同一个 google/fonts commit**上，不需要引入新的信任源：

```
https://raw.githubusercontent.com/google/fonts/00e726a90e0b9698971c37b88c35ef958965448b/ofl/notosanssc/NotoSansSC%5Bwght%5D.ttf
17,772,300 字节  sha256 a3041811a78c361b1de50f953c805e0244951c21c5bd412f7232ef0d899af0da
```

实测该 pin 与 `main` 分支当前内容逐字节一致。

## 6. 体积：全部实测数字

### 6.1 为什么不能直接用 Google 的分发形态

Google Fonts 把 Noto Sans SC 切成 **101 个 unicode-range 子集**分发。在线场景下浏览器按需只取几个；
离线全量落盘则一个都不能少（用户可能打任何字）。实测总量：

| 形态 | 文件数 | 字节 |
| --- | --- | --- |
| wght 400 | 101 | 2,410,336（2.30 MB） |
| wght 500 | 101 | 2,441,496（2.33 MB） |
| wght 700 | 101 | 2,461,436（2.35 MB） |
| wght 900 | 101 | 2,429,544（2.32 MB） |
| 可变 100..900 | 101 | 4,514,816（4.31 MB） |
| （参考）Noto Serif SC 700 | 101 | 3,256,008（3.11 MB） |

这些数字看着比单文件小，但**覆盖也小**：101 个子集的码点并集只有 **15,605 个**，
而完整字体的 cmap 是 **30,890 个**。不是同一件商品。

**真正的否决理由是硬限制，不是体积**：`workers/motion_composition/worker.mjs:56`
`SANDBOX_ASSETS_MAXIMUM = 128`，第 345 行 `allowedAssets.length > SANDBOX_ASSETS_MAXIMUM` 直接拒绝。
**101 个子集一个字重就吃掉 128 个名额里的 101 个，两个字重（202）直接被门禁打回。**
合并成一个文件只占 1 个名额。

### 6.2 单文件 woff2（自己从锁定的上游 TTF 构建）

| 覆盖档 | 码点 | 字形 | 静态 400 | 静态 700 | **可变 100–900** |
| --- | --- | --- | --- | --- | --- |
| GB2312 + 基础标点 | 7,728 | 8,382 | 1,080,924 B | 1,108,140 B | **2,043,768 B（1.95 MB）** |
| Google Web SC 同覆盖 | 15,728 | 14,425 | 1,963,224 B | 2,014,276 B | **3,664,824 B（3.49 MB）** |
| GBK + 基础标点 | 22,056 | 22,741 | 3,134,624 B | 3,220,516 B | **5,846,260 B（5.58 MB）** |
| 全字符集 | 30,890 | 31,036 | 4,193,128 B | 4,314,792 B | **7,782,440 B（7.42 MB）** |

只要用可变字体，**字重数量不影响体积**——7 个字重和 1 个字重是同一个文件。
若走静态实例，每多一个字重就多付一份（400 + 700 ≈ 8.4 MB，比可变全字符集还贵）。
这是选可变字体的第二个理由（第一个是 3.2 节的真字重）。

### 6.3 净增

安装包基线：`.local/release/release-package.json` 记录 `disk_image_bytes = 490,943,549`（468 MB），
其中 `embedded-browser` 324 MB、`executor` 177 MB。woff2 已是 brotli 压缩，dmg 不会再压，
所以净增约等于文件本身：

| 档位 | 净增 | 占 dmg |
| --- | --- | --- |
| 可变 GB2312 | 1.95 MB | +0.42% |
| 可变 Google 同覆盖 | 3.49 MB | +0.75% |
| 可变 GBK | 5.58 MB | +1.19% |
| 可变全量 | 7.42 MB | +1.59% |

## 7. 裁字集的代价（这一节请产品负责人重点看）

裁到 GB2312 只要 1.95 MB，很诱人。代价是**生僻字变豆腐块**，而 GB2312 的「生僻」判定很宽：

从人名、网络用语里随手取 56 个字，GB2312 缺 **23** 个：

```
镕 璟 玥 頔 喆 沄 燚 靐 囧 溦 珺 婳 昫 骉 犇 皛 龘 兲 巭 嘦 嫑 覅 烎
```

`璟`、`玥`、`喆`、`珺`、`婳`、`犇`、`镕` 全是真实人名用字；`囧` 是网络常用字。
运营口播稿、嘉宾姓名、品牌名撞上其中任何一个，画面上就是一个方框，而且**没有任何环节会报错**——
渲染成功、门禁全绿、片子里一个豆腐块。

量化差距：URO 区汉字 GBK 覆盖 20,902 个，GB2312 只覆盖 6,763 个，**差 14,139 个**。

**建议：取全字符集（7.42 MB，+1.59%）。** 理由是这 5.5 MB 的差价买的是
「永远不会出现豆腐块」这个确定性，而本产品的输出是要发到公开平台的成片，
出一次豆腐块的品牌代价远超 5 MB 安装包。若产品负责人认为必须省，GBK（5.58 MB）是次优——
它覆盖 20,902 个 URO 汉字，上面 23 个缺字里除 `靐/骉/皛/龘/巭` 等极端造字外都在。
**不建议 GB2312。**

## 8. 落地方案与还没做的事

### 8.1 注入点：应该在 RenderJob 工作区副本，不是离线目录的 CSS

有两个可选注入点：

| 方案 | 覆盖 | 代价 |
| --- | --- | --- |
| 往 `stylesheets` 的 face 列表里加 CJK face | 只有那 56 个 `<link>` 了字体 CSS 的零件 | **零代码改动**，纯契约数据 |
| 渲染期往工作区副本注入生成的 `<style>` | 全部 134 个 | 需要在文案替换那一步顺手生成 |

推荐第二个：78 个零件根本没有 `<link>`，第一个方案够不着它们；
而且 PC-03 已经定了「替换发生在运行期写入 RenderJob 工作区的副本」，
CJK face 的生成天然属于同一步——扫零件 CSS 拿到 `(family, weight)` 对，逐条生成 `@font-face`。

生成量很小，实测 134 个零件的 `(family, weight)` 对：**中位数 2、均值 2.8、最多 18**
（`transitions-distortion` / `transitions-grid`），全目录并集 70 对，
出现过的字重只有 300/400/500/600/700/800/900 七个。每个零件几百字节 CSS。

`allowedAssets` 只需 +1（那一个 woff2）。`agent.py:lint_composition` 对 `url(...)` 的
`undeclared_asset` 检查不看扩展名，woff2 只要在工作区资产清单里就放行。

### 8.2 构建期怎么拿到那个文件

这里有一个必须正视的代价：**合并版 woff2 没有现成的下载地址**。
现有锁清单要求每个 artifact 有 `downloadUrl`（`test_offline_motion_catalog.py` 断言
`startswith("https://")`）。Google 只按 101 子集分发，jsDelivr 的 `@fontsource` 同理。
所以只能：

1. 把**上游可变 TTF**（17,772,300 B，第 5 节的 pin）作为 artifact 锁进清单；
2. 在 `build_offline_motion_catalog.py` 里加一步：用 fonttools + brotli 把它子集化并转 woff2；
3. 产物摘要由既有的 `generated.aggregateSha256` 重算比对覆盖，门禁仍然可重算。

代价是**新增构建期依赖 fonttools + brotli**，且构建机需要能跑它。
本次实测用的就是这条链路（fonttools 4.63.0），单档位耗时 10–35 秒。

### 8.3 契约还要动三处

`test_offline_motion_catalog.py` 对 face 有三条断言，加中文时都会撞上：

- `face["subset"] in lock["keptFontSubsets"]` → 要把 `chinese-simplified` 加进 `keptFontSubsets`；
- `face["artifactPath"] in artifact_paths` → woff2 要作为 artifact 入清单；
- `face["family"] in lock_families` → **这条是设计冲突**：`fontFamilies` 是「家族名 → 许可证」的映射，
  而我们要做的恰恰是「`Anton` 这个家族名下挂一份 Noto 的字节」。
  硬塞会让权利台账说谎（`Anton` 的 `licenseFileSha256` 指向 anton 的 OFL，字节却是 Noto 的）。
  **权利归属需要从「按家族」改成「按 artifact」**，否则 BM-12 那套权利审计会失真。
  这一处是本任务发现的、原计划里没有的工作量。

### 8.4 没做什么

- **没有改任何契约。** `offline-motion-dependencies.v1.json`、`motion-asset-overlay.v1.json`
  一字未动。覆盖档位是产品决策，等拍板；
- **没有碰 `vendor/hyperframes`。** `git status` 里 submodule 干净；
- **没有解决拉丁那一半。** 78 个零件的具名拉丁字体仍未装车，它们的英文今天仍随机器变化。
  要不要一起补，需要单独评估——粗算 `Archivo Black`、`League Gothic`、`Oswald`、
  `Barlow Condensed` 等几个 OFL 字体加起来在百 KB 量级，比中文便宜得多；
  `Menlo`/`SF Pro`/`Segoe UI`/`Helvetica` 是系统专有字体，不能再分发，只能换字或接受；
- **没有处理那 17 个不写 `font-family` 的零件。** 它们吃 `sans-serif` 默认，
  要让它们确定就得替它们指定字体，那等于改设计，需要单独决定；
- **没有覆盖 Windows。** 本次全部在 macOS 上测。字重分桶是 Blink 的字体匹配逻辑、与平台无关，
  但系统兜底字体（微软雅黑 vs 苹方）显然不同，落地任务需要在 Windows 上复测一遍变体 A 与 F。

## 9. RED / GREEN

新增 `scripts/motion_cjk_font_probe.py` 与 `scripts/test_motion_cjk_font_probe.py`（被
`scripts/run_script_tests.py` 自动发现，无需登记）。

RED（实现文件尚不存在）：

```
$ python3 scripts/test_motion_cjk_font_probe.py
AssertionError: scripts/motion_cjk_font_probe.py is missing
EXIT=1
```

第二轮 RED（`frame_seek_times` 尚不存在）：

```
AttributeError: module 'motion_cjk_font_probe' has no attribute 'frame_seek_times'
EXIT=1
```

GREEN：

```
$ python3 scripts/test_motion_cjk_font_probe.py
motion cjk font probe tests passed
executed checks: 5
EXIT=0

$ python3 scripts/run_script_tests.py
all 65 script tests passed (809 checks)
EXIT=0
```

（`executed checks:` 这一行是 `run_script_tests.py` 认的计数格式；第一版只打了
「passed: 5 checks」，被 runner 判成 `no positive executed-check count was reported`。）

五条断言分别钉住：具名家族提取（含 `var()`、通用关键字的排除）、
对真实零件 `lt-bold-block` 的提取结果、
**CJK face 记录能被未修改的 `build_offline_motion_catalog.stylesheet_css()` 直接生成**、
CJK 码点集不含拉丁与中点、逐帧 seek 时间不越过零件自身时长。

## 10. 失败矩阵

| 情形 | 期望 | 实测 |
| --- | --- | --- |
| 只声明 CJK@400，元素请求 700 | 汉字应走注入字体 | ❌ 掉系统兜底（变体 B）——**这是本任务最重要的发现** |
| 声明 `font-weight: 100 900`，元素请求 700 | 同上 | ❌ 掉系统兜底（变体 D） |
| 同一静态文件声明成两个字重 | 命中 | ✅ 命中但字重不对（变体 E） |
| 可变文件按精确字重逐条声明 | 命中且字重正确 | ✅（变体 F + `weight-check.png`） |
| CJK 的 `unicode-range` 误含拉丁 | 零件设计被中文字体覆盖 | 由 `test_cjk_range_excludes_latin…` 挡住 |
| 101 个子集文件进渲染资产清单 | 应被拒 | `worker.mjs:345` 拒绝（>128） |
| 运行时去 Google 取字体 | 应被拒 | `lint_composition` 的 `remote_reference` 拒绝；
渲染沙箱 `--proxy-server=127.0.0.1:9` 物理断网 |
| 零件文件被本任务改动 | 不允许 | `git status` 中 `vendor/hyperframes` 干净 |

## 11. 清理

- 探针每次运行结束在 `finally` 里 `killpg` 整个浏览器进程组并删除临时 profile；
  运行后 `pgrep -f 'Chrome for Testing'` 无残留；
- 中间截图与裁切图已删除，只保留证据目录里的 6 张图 + 1 条 mp4；
- 字体构建用的临时 venv、17.77 MB 上游 TTF、101×5 份子集 woff2 都在会话临时目录，未进仓库；
- 未启动任何本地服务、容器或数据库。

## 12. 文档

- `docs/product-completion-roadmap.md`：PC-13 行补上实测结论与待决项，状态保持 `🚧 实现中`
  ——**字体还没进包，标完成就是说谎**；
- 本文件是 PC-13 的唯一证据文件，不回写路线图。


---

# 第二轮：落地（2026-07-27 同日）

> 产品负责人下了两个决定：**取全字符集 7.42 MB**；**把拉丁那 78 个一并修**。
> 本节记录落地的实测、代码与仍未完成的部分。

## 13. 78 个零件的三类分类（实测）

按「上游写了名字但从没装车」重新全量扫描，这次把两种非 CSS 形态也算进来——
`vfx-liquid-background` 把字体栈写成 `&quot;Inter&quot;`，`morph-text` 把字体放在
`data-font` 属性里由脚本赋给 `style.fontFamily`。只读 `font-family:` 会漏掉这两个，
而且漏得无声无息。补上之后，**上一轮报告里说的「Figtree 从未被引用」是错的**——它在
`morph-text` 的 `data-font` 里，第一轮的扫描器看不见。

全量结果：**134 个零件、40 个家族、71 个 `(family, weight)` 组合**，扫描摘要
`a6d304ec68b58005964b57a9509535c630c4a8e6f3f57d1506b6a4b5a31bb90a`。

| 类 | 家族数 | 涉及零件 | `(family,weight)` 组合 | 体积净增 |
| --- | --- | --- | --- | --- |
| **1. OFL 且已在包内**，只是没声明 `@font-face` | 18 | 108 | 39 | **102,884 B**（见下，不是 0） |
| **2. OFL 但不在包内**，本任务新增 | 4 | 7 | 5 | **106,416 B（103.9 KB）** |
| **3. 不可再分发**，映射到可分发替代 | 15 | 48 | 24 | **0 B**（全部复用包内已有字体） |
| （另）交给系统的彩色 emoji | 3 | 1 | 3 | 0 B（见 15 节） |

### 13.1 第 1 类不是零成本——这是本轮的一处纠正

上一轮报告说第 1 类「补声明即可，零许可证成本」。许可证成本确实是零，**但字重不是**：
4 个已在包内的家族缺零件要的那个字重，因为当初只下载了零件 `<link>` 里请求的字重。

```
Figtree      包内 [900]              零件要 400   GAP
Gabarito     包内 [900]              零件要 400   GAP
Montserrat   包内 [300,700,800,900]  零件要 400   GAP
Poppins      包内 [700,800]          零件要 400   GAP
```

补这 4 个字重要 **102,884 字节**。漏掉它们的后果不是「字重不对」而是**整条 face 不生效**
（第 3.1 节测出的分桶行为），中文和拉丁一起掉回系统字体。

### 13.2 第 2 类：新增 4 个 OFL 家族

| 家族 | 字重 | 零件 | woff2（latin + latin-ext） |
| --- | --- | --- | --- |
| Archivo Black | 400 | `lt-bold-block` / `lt-kicker-name` / `lt-stack-bars` | 16,192 B |
| League Gothic | 400 | `lt-color-block` / `lt-side-rule` | 17,304 B |
| Oswald | 700 | `lt-accent-underline` | 24,712 B |
| Barlow Condensed | 700 / 800 | `lower-third-bild` | 48,208 B |

全部 OFL-1.1，锁在与现有 18 个家族**同一个** google/fonts commit `00e726a9…`，
许可证文件摘要逐个记进 `fontFamilies`。

### 13.3 第 3 类：15 个不可再分发字体的替代映射

**逐条记在 `contracts/video/motion-part-typography.v1.json` 里**（含 `reason` 与
`visualDifference` 两个必填字段，门禁强制），这里摘要：

| 原字体 | 权利人 | 替代 | 视觉差异 |
| --- | --- | --- | --- |
| Menlo / Monaco / SF Mono | Apple | **JetBrains Mono** | 字面更大、字腔更开、小写 l 带尾钩；比 SF Mono/Menlo 略宽，同字号每行字符数略少 |
| Consolas / Courier New | Microsoft / Monotype | **JetBrains Mono** | 同上；Courier New 是细笔画打字机体，替换后明显变粗变实 |
| Arial / Helvetica / Helvetica Neue / Segoe UI / SF Pro Text / SF Pro Display | Monotype / Linotype / Microsoft / Apple | **Inter** | Inter 的 x-height 更大、字腔更开放、末端更平直；Helvetica 系字腔更闭合。同字号下 Inter 视觉更大、行宽略宽 |
| Georgia | Microsoft | **Libre Baskerville** | 笔画对比更强、衬线更尖细；Georgia 更圆钝厚重 |
| Times New Roman | Monotype | **Libre Baskerville** | **本次视觉差异最大的一条**：Libre Baskerville 明显更宽、x-height 更大，Times 窄而紧，同一段文字会占更多横向空间 |
| Impact | Monotype | **Big Shoulders Display** | 更窄、更几何、字重可选；Impact 更厚重、字腔几乎闭合 |
| Arial Black | Monotype | **Montserrat** | Montserrat 是几何无衬线、更圆更宽；Arial Black 是新怪诞体、更方更紧，900 下 Montserrat 视觉略轻 |

**为什么不用度量兼容的替代（Arimo/Tinos/Cousine）。** 它们能保住换行与行宽，代价是再多 3 个家族。
放弃的理由有两条：一是这些零件**今天本来就在吃系统兜底**，度量从来没稳定过，谈不上「保住」；
二是换行溢出已经有专门的任务兜——PC-14 在替换后、渲染前实测每个槽的
`scrollWidth>clientWidth`，超了就硬失败回抛。**把度量风险交给一个会报错的门禁，
比交给一个多花 300 KB 的近似字体更靠谱。** 这一条如果 PC-14 最后没做，需要回来重新评估。

### 13.4 一个必须写下来的产品后果

`code-snippet-*` 这一族共 24 个零件是 VS Code / Apple Terminal 的**外观仿制**，
它们的官网预览用的就是 Menlo / SF Mono。换成 JetBrains Mono 之后，
**这些零件和上游官网预览长得不一样**，而且这是不可逆的——不是 bug，是许可证决定的。
将来有人问「为什么这个代码演示零件和官网预览不一样」，答案在这一节和那份契约里。

## 14. 落地的四件

### 14.1 按 `(family, weight)` 逐条声明，并让门禁查得出遗漏

新增 `scripts/motion_part_typography.py`（推导）+ `contracts/video/motion-part-typography.v1.json`（决策）
+ `scripts/check_motion_part_typography.py`（门禁）。

门禁**全部重算，不读回**：从只读 submodule 按冻结目录的文件清单重新扫 134 个零件，
比对扫描摘要；任何家族没有声明策略 → 红；任何声明了但没有零件用 → 红（防清单腐烂）；
**任何 `(family, weight)` 在包里找不到对应 woff2 → 红**。最后一条就是遗漏检测器。

它真的检测到了。加完策略表、还没往离线包里加字体时：

```
$ python3 scripts/check_motion_part_typography.py
the package cannot serve these (family, weight) requests, so those parts would silently
fall back to the host font: [('Archivo Black', 400), ('Arial Black', 400),
('Barlow Condensed', 700), ('Barlow Condensed', 800), ('Figtree', 400), ('Gabarito', 400),
('League Gothic', 400), ('Montserrat', 400), ('Oswald', 700), ('Poppins', 400)]
the Chinese face is not a locked artifact: offline-deps/fonts/woff2/noto-sans-sc/...
motion part typography check failed: 2 problem(s)
EXIT=1
```

10 条全中，包括 `('Arial Black', 400)` ——它自己不进包，但它替换到的 Montserrat 缺 400，
**替换链上的缺口也被查出来了**。补齐字体后：

```
$ python3 scripts/check_motion_part_typography.py
motion part typography is valid: 134 parts, 40 families
(22 packaged / 15 substituted / 3 host), 71 (family, weight) pairs
EXIT=0
```

### 14.2 权利归属从「按家族」改成「按 artifact」

`test_offline_motion_catalog.py` 原来断言 `set(lock_families) == ledger_families`，
隐含假设「face 声明用的名字 = 字体本身的名字」。本任务恰好要打破它：
`"Menlo"` 名下是 JetBrains Mono 的字节，每个拉丁家族名下还挂着一份 Noto Sans SC。
照旧规则，**Noto 的字节会继承 Menlo 的许可证行**——许可证台账最不该错的地方错了。

改法：每个 `kind:"font"` artifact 新增 `sourceFamily`（这堆字节到底是哪个字体），
许可证台账按 `sourceFamily` 键；断言改成**从 artifacts 重新推导**
`set(lock_families) == {每个 font artifact 的 sourceFamily} ∪ {builtFonts 的 sourceFamily}`，
孤儿行和缺失行都判红。face 的 `family` 从此明确只是 CSS 名字，允许与字体本身不同。

### 14.3 构建期新增 fonttools + brotli

`backend/pyproject.toml` 新增 `catalog-build` 分组，**精确 pin**：

```toml
catalog-build = ["brotli==1.2.0", "fonttools==4.63.0"]
default-groups = ["catalog-build", "dev", "executor"]
```

**故意不放进 `executor` 组**：`backend/automation-tool-executor.spec` 声明 `excludes=[]`，
进不进冻结包完全由 import 图决定（见 CLAUDE.md 9.2）。让字体工具链不在那个组里，
是**结构上**够不着，而不是靠一条按名字排除的规则——后者一次改名就失效。
`backend/tests/unit/executor/test_pyinstaller_bundle_contract.py` 断言了这一点，
并顺带钉住了版本 pin。

**为什么必须精确 pin：build 默认是不确定的。** 合并版 woff2 没有任何人分发
（Google 只给 101 个子集，撞 128 资产上限），只能从锁定的上游 TTF 本地构建，
于是它的 sha256 是对「这套工具链」的断言。实测：

```
第 1 次  0f17f8631f4cebfbd0c8f94f35a08c409ef183cbb9f0526e4daac8ffc772dfee  7,782,644 B
第 2 次  c6bb505ad23ff835d5b7ec992c0d53d3e13bc481ee884eae1f56e807a5594777  7,782,216 B
```

同一个输入、同一台机器、连长度都不一样。原因是 `TTFont` 默认 `recalcTimestamp=True`，
把 `head.modified` 写成构建时钟。关掉 `recalcTimestamp` 与 `recalcBBoxes` 之后，
连续三次输出同一个文件：

```
aef8c34277afad81ecd0227138a830263c0caea65b7aea66d1195395f097b55a  7,782,072 B  ×3
```

**若不发现这一条，锁进去的摘要会在任何人重建时变红，而且长得和「被篡改」一模一样。**

### 14.4 Windows：未复测

本轮全部在 macOS（arm64）上完成，**Windows 一次都没跑**。需要什么条件才能补：

- 一台 Windows 机器（或 CI 的 Windows runner）上有 `.local/desktop-e2e/embedded-browser/windows-*`
  的内置 Chromium 分发（EB-05/EB-06 的产物）；
- 跑 `scripts/build_offline_motion_catalog.py`（需要 `catalog-build` 分组）生成同一份离线目录，
  并核对 `generated.aggregateSha256` 与本机一致——**这一步本身就是 Windows 复测的第一个断言**，
  因为 woff2 是本地构建的，跨平台字节不一致会直接暴露；
- 跑本轮的两个验收（`lt-bold-block` 与 `code-snippet-apple-terminal-pro`），
  断言 `CSS.getPlatformFontsForNode` 里**没有任何 `isCustomFont: false` 的字体**。
  对照组预期是微软雅黑 / Segoe UI / Consolas，与 macOS 的苹方 / Space Mono 不同。

字重分桶那条结论属于 Blink 的字体匹配逻辑、与平台无关，但**系统兜底字体不同**，
所以对照组必须在 Windows 上重跑才有意义。

## 15. 彩色 emoji：唯一保留的跨平台差异

`caption-emoji-pop` 一个零件的 emoji 层写的是
`"Apple Color Emoji", "Segoe UI Emoji", "Noto Color Emoji"`。实测打包 Noto Color Emoji
需要 **2,058,000 字节（1.96 MB，10 个文件）**。

决定：**不打包，声明为 `host` 策略**，并在契约里逐条写清理由。判据与汉字不同——
macOS 与 Windows 都自带完整 emoji 字体，落到系统字体渲染出来是**正确的表情**而不是豆腐块；
汉字缺字是「显示错误」，emoji 换画风是「显示不同」。1.96 MB 只服务 1/134 个零件不划算。

代价写在契约的 `visualDifference` 里：Apple 立体风 vs Google/Microsoft 扁平风，
同一个零件在两台机器上表情长相不同。**这是本任务结束后已知的、唯一未消除的跨平台差异。**

`host` 是策略不是后门：必须逐个家族声明、必须写理由，门禁对空理由判红，没有通配符。

## 16. 体积总账

| 项 | 字节 | 说明 |
| --- | --- | --- |
| 中文（Noto Sans SC 可变全字符集） | 7,782,072 | 一个文件覆盖 300–900 全部 7 个字重 |
| 第 2 类新增 4 个 OFL 家族 | 106,416 | Archivo Black / League Gothic / Oswald / Barlow Condensed |
| 第 1 类补齐 4 个缺失字重 | 102,884 | Figtree / Gabarito / Montserrat / Poppins 的 400 |
| 第 3 类替代 | 0 | 全部复用包内已有字体 |
| emoji | 0 | 交给系统 |
| **合计净增** | **7,991,372（7.62 MB）** | 占 dmg 490,943,549 的 **+1.63%** |

离线字体资产由 1,031,980 B（52 个文件）变成 9,023,352 B（70 个下载 + 1 个构建），
离线目录文件数 384 → 411，聚合摘要 `128ea48c…` → `bfc663e7…`。

**一件必须报告的事：安装包里已经有中文字体了。**
`contracts/quality/asset-rights-policy.v1.json` 记着
`font-noto-sans-cjk-sc-regular`（16,437,364 B）与 `font-noto-sans-cjk-sc-bold`（17,002,248 B），
`bundledIn: material-video-worker`——「智能素材成片」那条链路当初为了替换不可再分发的
微软雅黑/华文黑体，已经装了 33.4 MB 的思源黑体 OTF。

**没有复用它，理由三条**：只有 Regular/Bold 两个字重，而零件要 7 个（照第 3.1 节的分桶结论，
其余 5 个字重会掉回系统字体）；它是 OTF 不是 woff2，且在另一个 bundle 的资源根下，
跨 bundle 取路径是构建路径分叉的开始；33.4 MB 换 7.42 MB 也不划算。
**但两条链路各带一份中文字体是真实的冗余（合计约 41 MB），值得单开一个整合任务。**

## 17. 真实产物（第二轮）

两条都用**生产解析路径**产出：`scan_catalog_parts()` → `resolve_faces()` → `part_font_css()`，
文件从离线目录按 RenderJob 的方式播种进工作区，再用内置 Chromium 渲染。

### 17.1 `lt-bold-block`（第 1+2 类：包内字体 + 新增字体 + 中文）

零件请求 `[('Archivo Black', 400), ('Space Mono', 700)]`，解析出 2 条 face，
工作区 4 个资产（`archivo-black-400.woff2` / `space-mono-700.woff2` / `chinese.woff2` / `gsap.min.js`）。

```
#lt-name  星云科技 Nebula     ArchivoBlack-Regular   ×7  custom
                              NotoSansSC-Thin_Regular ×4  custom
#lt-tag   主持人 · 神经科学家  SpaceMono-Bold         ×3  custom
                              NotoSansSC-Thin_Bold   ×8  custom
```

**没有一个字形来自系统字体**（第一轮对照组是 11 个字形全部 PingFang SC）。

```
$ ffprobe -v error ... pc13-lt-bold-block-packaged.mp4
codec_name=h264   width=1920   height=1080   pix_fmt=yuv420p
r_frame_rate=30/1   nb_frames=144   duration=4.800000   size=37863
$ shasum -a 256 pc13-lt-bold-block-packaged.mp4
77603a88bc387dcc85959f9b0e0ff23b02fc8440ab5e540bac883e4460e663cc
```

144 帧共 3,188,995 字节、38 个互不相同的摘要。第 80 帧裁切图 `round2/frame80.png`：
「NEBULA」终于是 Archivo Black 那个厚重的怪诞体，不再是 PingFang。

### 17.2 `code-snippet-apple-terminal-pro`（第 3 类：替代）

零件请求 6 个 `(family, weight)`——`Consolas/Courier New/Menlo/Monaco/SF Mono` 各 400
加 `SF Pro Text` 500——解析后**工作区只需要 4 个资产**：5 个等宽名字塌缩到同一个
`jetbrains-mono-400.woff2`，加 `inter-500.woff2`、`chinese.woff2`、`gsap.min.js`。

```
.output-line  "Last login: Mon Jun 2 09:14:22 on …"   JetBrainsMono-Regular ×41  custom
.prompt       "user@Mac myapp %"                       JetBrainsMono-Regular ×17  custom
```

零件写的是 `font-family: Menlo, Monaco, Consolas, "Courier New", monospace`，
一个字节都没改，画出来的是包内的 JetBrains Mono，**没有任何字形来自系统字体**。
截图 `round2/terminal-substituted.png`（101,460 B）。

### 17.3 全目录解析

134 个零件全部解析成功，**0 条 unmet**，共 368 条 face
（`round2/resolution-table.txt` 逐零件列出）。

## 18. RED / GREEN（第二轮）

```
$ python3 scripts/test_motion_part_typography.py
AssertionError: scripts/motion_part_typography.py is missing                    ← RED
$ python3 scripts/test_motion_part_typography.py
AttributeError: ... has no attribute 'part_typography'                          ← RED
$ python3 scripts/check_motion_part_typography.py
... 10 个 (family, weight) 服务不了 + 中文 face 不在锁清单                        ← RED（14.1 节全文）
$ python3 scripts/test_offline_motion_catalog.py
AssertionError: assert set(lock_families) == ledger_families                    ← RED（按家族的权利归属）

$ python3 scripts/test_motion_part_typography.py
motion part typography tests passed / executed checks: 8                        ← GREEN
$ python3 scripts/check_motion_part_typography.py
motion part typography is valid: 134 parts, 40 families
(22 packaged / 15 substituted / 3 host), 71 (family, weight) pairs              ← GREEN
$ python3 scripts/test_offline_motion_catalog.py
offline motion catalog tests passed / executed checks: 3                        ← GREEN
$ python3 scripts/check_offline_motion_catalog.py
offline motion catalog is valid: 134 items, 411 files, 0 remote URLs            ← GREEN
```

## 19. 还没做的（决定 PC-13 只能标 `🔍 待验收`）

1. **渲染期注入还没接进产品。** `part_font_css()` 已经能生成、已经用它渲出上面两条产物，
   但**执行器还没有在写 RenderJob 工作区副本时调用它**。那个写副本的地方正是 PC-03
   要建的文案替换点，PC-03 尚未开始。本轮刻意没有去改
   `motion_authoring/agent.py`（并行工作线在动它）。
   **所以：字体已经进包、机制已经在真实内置 Chromium 上验过，但用户从正式 App 走一遍
   还看不到效果。** 这是 `🔍 待验收` 而不是 `✅` 的原因，不要含糊过去；
2. **Windows 未复测**（14.4 节列了条件）；
3. **`caption-emoji-pop` 的 emoji 仍随系统变化**（15 节，已决策）；
4. **两条链路各带一份中文字体的 41 MB 冗余**（16 节）没有整合。

## 20. 清理（第二轮）

- 探针每次运行在 `finally` 里 `killpg` 浏览器进程组并删临时 profile；
  收工后 `pgrep -f 'Chrome for Testing'` 无残留；
- 临时 venv、17.77 MB 上游 TTF、101×5 份子集 woff2、各轮裁切图都在会话临时目录，未进仓库；
- `.local/offline-motion-deps/` 是 git 忽略的构建暂存，未提交；
- 未启动任何本地服务、容器或数据库。

## 20. 渲染期注入的收口（2026-07-29）

第 19 节记的两项「仍差」，第一项现已闭合。

`film_assembly.py` 给每个零件段调用 `write_part_working_copy(..., font_css=font_css_for(...))`，
`@font-face` 规则随工作区副本一起落进 RenderJob——**注入点就是产品渲染时走的那一条**，
不是测试专用分支。

真实终态：PC-10 于 2026-07-29 在正式 App 用户路径上出的成片
（h264 / 1920×1080 / yuv420p / 30fps / 960 帧 / 32.000 秒 / 968,120 字节），
第 6 镜是 `data-chart` 零件，抽帧后逐字核对：

```
ffmpeg -ss 20 -i .local/kept-evidence/2026-07-29-pc10-用上零件且不重复.mp4 \
       -frames:v 1 pc10-t20.png
```

画面上「增长趋势」「连续四周保持两位数增长」「第1周」「第2周」四处中文**全部正常
显示，无一个豆腐块**。这四处的上游原文是英文（`Monthly Revenue vs. Conversion Rate`
等，见 `PC-03.md` §14），所以它们出现在画面上这件事本身，同时证到了槽替换与字体
注入两条——**注入没生效的失败形态恰恰是「不报错、不失败，只是显示成豆腐块」**，
而这个读数把那种形态排除了。

模板镜头的中文同样正常（镜1「本周销售增长报告」、镜7「持续增长 未来可期」等）。

### 20.1 Windows x86_64 实机复测闭合（2026-07-31）

实机为 Windows 11 10.0.26200 / x64 / Intel i9-13900K，使用包内锁定的
Chromium 149.0.7827.55。先联网按锁补齐本机缓存，再以 `--offline` 完整重建：

```
offline catalog   134 items / 411 files
aggregateSha256   bfc663e7a1847b9652006d172c1de5ca5c0ecaddb539533c4d99fa341b893081
release catalog   134 items / 337 files
aggregateSha256   c88975a1fa19c4c4d16d5abe89acf7ece5b812517150079d22506fabb09bc9e4
Noto Sans SC      7,782,072 B
sha256            aef8c34277afad81ecd0227138a830263c0caea65b7aea66d1195395f097b55a
```

第一次 CDP 实测没有放行：Google Fonts 锁按 `latin-ext`、`latin` 排列，
`face_artifact()` 误取第一项并用宽 Latin range 声明，导致普通英文落到
Arial / Consolas。加回归测试并改为优先选择真正覆盖 U+0000 的 `latin`
子集后重跑，两份文档均为包内字体。2026-07-31 合并复审又发现仅选择 basic
`latin` 文件、却继续声明 Latin Extended 范围，会让 U+0100 等字符静默回退宿主字体。
第二条 RED 已把实现收紧为：`latin` 与 `latin-ext` 两个锁定文件各发一条
`@font-face`，每条只声明锁文件自己的 `unicodeRange`，不再用一条宽范围冒充两个子集：

- `lt-bold-block`：中文由 Noto Sans SC（4 + 8 glyphs）绘制，拉丁由
  Archivo Black 7 glyphs、Space Mono 3 glyphs 绘制；
- `code-snippet-apple-terminal-pro`：output / prompt / typed command 分别
  41 / 17 / 12 glyphs，全部由 JetBrains Mono 绘制；
- 两份 `CSS.getPlatformFontsForNode` 报告合计 `isCustomFont:false = 0`。

Windows 的 CDP pipe 不能使用 POSIX `pass_fds` / `preexec_fn`，探针新增
Playwright 原生 Windows transport，但仍调用同一个
`CSS.getPlatformFontsForNode` 判据。证据在
`.local/pc-13-windows/acceptance-summary.json`，截图与逐文档 JSON 同目录。
