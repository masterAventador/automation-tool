# C10-11 账号与设备吊销演示

- 状态：✅ 已完成
- 日期：2026-07-23
- 范围：把运维应急停用账号与用户主动吊销单台设备组成同一可执行演示矩阵，验证目标 Session/设备立即失效、同账号其他设备与其他账号保持有效，并机器审计 OpenAPI 不存在匿名业务写入口；不接触真实客户账号、不执行真实平台动作、不修改专项 Roadmap
- 前置依赖：C10-10 已完成
- RED：先固定 revocation 机器契约、匿名写面审计和统一验收入口，并增强应急停用测试对已签发/外部账号 Session 的即时认证断言；当前能力分散在 C10-06、U9 与设备 Session 测试中，没有同一 Customer Demo 证据闭环，测试应准确失败
- GREEN：
  - 新增 `contracts/security/customer-demo-revocation-v1.json`，分别固定账号应急停用和单设备吊销的失效/保留集合、认证入口、乐观 revision、审计要求、允许的认证 security schemes；匿名 mutation allowlist 只含建立认证的 `loginAccountSession`，匿名业务写数量固定为 0。
  - 增强真实 PostgreSQL 应急停用集成测试：停用前为目标与外部账号各签发真实 access Session；停用提交后目标 Session 立即 `AccountSessionRejected`，外部账号 Session 仍认证到原 user；既有原子全 Session/全所属设备吊销和审计断言保持不变。
  - 新增 OpenAPI 写面审计，枚举全部 POST/PUT/DELETE operation：除账号登录外每项必须精确声明一个受支持 security scheme 且无自定义 scope；App 业务写入口精确绑定 `AppSession`，账号、设备、注册/恢复入口分别绑定既有专用凭据。
  - 新增 `scripts/run_c10_11_acceptance.py`，统一执行账号全量吊销、单设备隔离吊销、父 credential/Installation 对既有设备 Session 的即时失效、无账号 Session 拒绝和匿名写面审计 6 项矩阵。
- 验证：
  - C10-11 Node 契约 2/2，Ruff、Mypy 通过。
  - 吊销矩阵 6/6 通过：`accountDisableImmediate=true`、`singleDeviceImmediate=true`、`siblingDevicePreserved=true`、`foreignSessionsPreserved=true`、`anonymousBusinessWrites=0`。
  - Frontend 回归 Node 195/195、Vitest 253/253 全绿。
  - 本任务未修改 Backend 产品代码；C10-09 同一工作树 Backend 全量 2379 passed / 5 skipped，C10-10 Frontend 全量 Node 193/193、Vitest 253/253 与所有静态/构建门禁均已通过。
- 边界：账号登录是认证建立入口，不被误计为匿名业务写；密码恢复、注册 bootstrap、设备 credential 与账号 refresh/access 都必须携带各自专用 bearer。验收只使用隔离测试账号与数据库，不接触真实客户账号或真实平台动作。
