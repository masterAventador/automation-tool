# BM-01 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-23
>
> 提交：本文件所在的 BM-01 独立实现提交

## 交付范围

- 品牌动效成片上游固定为 submodule `v0.7.68` / `71d84ff27f1c2b2828f4fdf9015c3da4157140ee`，验收拒绝 tag、commit 或脏工作树漂移。
- 上游当前随版本提供 13 个 frame preset；产品只登记公开展示的 12 套，`code-editorial` 保留为内部专用能力，不进入用户选择列表。
- 每套公开风格必须随上游存在非空 `FRAME.md`、`caption-skin.html` 和 `frame-showcase.html`，且不能是 symlink。
- 建立 12 个稳定英文内部 ID 到唯一中文展示名、中文适用说明的严格契约。UI 后续只能消费中文展示字段，不能直接展示上游名称。

## RED

Roadmap 进入 `🧪 RED` 后执行 `python3 scripts/run_bm_01_acceptance.py`，退出码为 2，明确失败于脚本不存在。当时虽然 submodule 已由 AV-02 固定，但没有“公开 12 套”清单、内部排除项或中文名称映射契约。

## GREEN

```bash
python3 scripts/run_bm_01_acceptance.py
python3 scripts/check_embedded_browser_video_roadmap.py
```

验收逐项核对 gitlink 当前 commit、精确 tag、上游干净工作树、13 个实际安装目录、12×3 个必要风格文件，以及 12 个不重复的中文名称和中文说明。契约只登记 12 套公开选择，内部 `code-editorial` 明确不可暴露。

## 失败矩阵

| 场景 | 结果 |
| --- | --- |
| submodule commit 或精确 tag 改变 | 拒绝 |
| 上游存在修改或未跟踪文件 | 拒绝 |
| 公开风格多于或少于 12 套 | 拒绝 |
| 内部专用风格进入公开清单 | 拒绝 |
| 英文 ID 重复、缺失或增加未知 ID | 拒绝 |
| 中文名为空、重复或直接等于英文 ID | 拒绝 |
| FRAME、字幕皮肤、展示页缺失/为空/链接 | 拒绝 |

## 正常用户路径验收

本任务只固定第三方源码和产品元数据契约，没有新增可操作 UI，因此不存在需要冒充的用户入口。BM-08 才会把这 12 套中文风格选择接到 App 正常路径；届时必须通过真实页面逐项选择和预览。本任务以只读 submodule、契约和确定性门禁作为适用的正式验收。

## 真实边界

- 当前只声明 12 套风格的身份、中文名称和随版本资源存在，不宣称已经用共用 Chromium 渲染通过；12 套渲染由 EB-02/BM-03/BM-16 跟踪。
- 这里的“整体风格”只决定颜色、字体、留白和画面外观，不把 134 个动效零件混称为风格。
- 上游项目名只存在于代码、submodule 和开发证据；后续用户界面使用“品牌动效成片”和本契约中文名称。

## 清理

验收只读取 submodule、契约和文件元数据，不启动服务、不占端口、不写上游工作树，也不下载外部资源。

## 文档变化

新增 BM-01 独立证据、12 套整体风格产品契约和可重复验收脚本；Roadmap 只更新 BM-01 状态与汇总计数。

## 遗留项

BM-02 继续实现隔离 Node 22 Worker；BM-03 使用项目唯一 Chromium 做渲染适配；BM-08 再把 12 套中文风格从正常 App 入口全部暴露给用户选择。
