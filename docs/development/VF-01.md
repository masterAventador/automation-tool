# VF-01 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-23
>
> 提交：本文件所在的 VF-01 任务提交

## RED

先运行聚焦测试，测试收集准确失败于
`automation_tool.control_plane.domain.video_creation` 不存在；没有复用旧 Roadmap
里尚未实现的 CT-01/CT-06 状态，也没有只靠文档宣称领域契约完成。

## GREEN

- 新增四类强类型 UUIDv4 ID，并复用现有 `ArtifactId`，没有建立第二套资源 ID。
- `ContentBrief` 固定一句话意图、语言、目标时长、画幅和输入 Artifact；
  `Storyboard` 固定连续分镜；`Timeline` 固定画面/音频/字幕轨、片段与通用转场；
  `RenderJob` 固定两种内部制作方式、状态、版本、输入/输出谱系；`Artifact` 固定
  角色、媒体类型、大小、SHA-256 和来源谱系，不包含路径。
- 9 类公开模型均为 frozen/slots，精确字段测试证明不存在 provider、model、vendor、
  api_key、base_url、voice_id 等制作方式专属字段或任意扩展字典。
- 25 项聚焦测试通过且新领域模块达到 100% 覆盖；Ruff、严格 Mypy 和独立
  VF-01 验收通过。

## 失败矩阵

- 空白/控制字符/超长文案、非法语言、时长和非 UTC 时间拒绝。
- 分镜空集、非连续序号、超长总时长拒绝。
- Timeline 空轨、重复轨/片段、同轨重叠、越界、字幕/媒体载荷错配拒绝。
- Artifact 角色与 MIME 不匹配、空/超大文件、非法摘要、重复或自引用谱系拒绝。
- RenderJob ID/枚举/版本错误、时间倒退、输入输出重叠，以及状态与输出/失败事实
  不一致全部拒绝。

## 正常用户路径验收

不适用。VF-01 只建立后续视频功能共用的后端领域契约，不新增用户入口。

## 真实边界

本任务不创建视频、不写文件、不启动 Worker，也不调用云服务或模型。

## 清理

测试只创建内存不可变对象，没有文件、端口、进程、数据库、容器或外部服务需要
清理。

## 文档变化

新增视频领域模块、公开领域导出、25 项单元测试、独立验收脚本，并同步后端架构、
工程结构和专项 Roadmap。

## 遗留项

VF-02 才接 Worker 生命周期，VF-03 才实现 RenderJob 私有目录和 Artifact 导入；
当前领域 `Artifact` 只描述稳定元数据，不替代 H8-09 的本机字节存储，也不实现
对象存储、持久化或用户页面。

后端全量实际执行为 2313 通过、5 跳过；同时发现并修正本分支 EB-01 精确锁版本
后遗留的 Playwright requirement 断言。全量门禁剩余失败均来自本轮同步的 `main`
提交 `01e5e51`：U9-05 新文件存在 Ruff format、10 个 Mypy 和覆盖率缺口；这些
文件不属于 VF-01，本任务未越权修改，VF-01 新模块自身为 100% 覆盖且严格类型
检查通过。
