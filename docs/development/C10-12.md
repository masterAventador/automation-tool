# C10-12 客户视角 Demo 验收

- 状态：✅ 已完成
- 目标：把安装→账号登录→设备自动绑定→工作台→扫码→预览→动作→结果→人工接管固化为有顺序、可执行、不会冒充正式设备验收的客户 Demo 门禁。
- RED：路线图先置为 `🧪 RED`；新增 Node 契约测试准确失败于 `customer-demo-acceptance-v1.json` 与 `run_c10_12_acceptance.py` 不存在，随后才补实现。
- 机器契约：`contracts/quality/customer-demo-acceptance-v1.json` 固定九步旅程、逐步源码证据、隔离全量与正式设备包两个 Profile，以及零外部平台写入、不得自动绕过挑战、不得使用真实客户凭据的安全边界。
- 执行入口：`scripts/run_c10_12_acceptance.py --full-isolated-app` 先校验证据锚点和扫码/发现接管/结果 PostgreSQL 聚焦矩阵，再串行运行 U9-06 账号与设备真实隐藏 Tauri App、H8-16F 工作台真实隐藏 Tauri App；`--device-package-handoff` 只输出 macOS/Windows 显式设备验收命令，不把当前自动化冒充正式安装包结论。
- 账号与设备证据：U9-06 的登录、重启、断网、Session 失效、重新登录、账号停用、恢复登录、设备吊销八个真实 Tauri 阶段全部通过，最终 PostgreSQL 与追加审计验证通过。
- 业务旅程证据：H8-16F 从正式 App/Control Plane/签名 Local Executor/系统浏览器调用面完成工作台、平台登录处理、发现两个目标、排除一个目标、最终确认、一次无写入 `browse` 动作及结构化成功/跳过结果；命令均精确 ACK，任务与 Attempt 收敛，外部平台写入为 0。
- 扫码与接管证据：正式系统 Chrome QR flow、发现终态矩阵和结构化结果矩阵 3/3 通过；验证码、风险页与页面漂移只能进入人工接管，不能自动处理或继续动作。
- 验证：C10-12 Node 契约 2/2、Ruff、Mypy、设备交接入口、完整隔离 runner 均通过；正式 macOS/Windows 安装包与授权测试账号验收继续由交接命令在对应设备显式执行。
- 清理：两套 App 使用独立 identifier、临时 AppData、独立 Compose project、随机端口与临时 PostgreSQL；运行结束已删除 AppData、容器、网络、卷、浏览器/Executor 后代并恢复生产 Vite assets。
