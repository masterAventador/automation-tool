# EB-11 完成证据

用户可操作：是

> 状态：🔍 待验收（**macOS 侧已于 2026-08-04 20:48 完全闭合**：当前源码树 `f20a014c`
> 的 Developer ID 签名 + 公证 + staple 包上，`run_eb_11_formal_app_acceptance.py`
> 首次跑完整条生命周期并写出 PASS 证据——真实扫码、主动复查、安全注销及旧 Profile 删除、
> 重扫、同一 Profile inode 重启复用、进程树清零全部成立。**仍待 Windows 正式 App 真实
> 扫码生命周期**，按用户要求留到其可配合时补验，故整条任务不提前改完成态）
>
> 日期：2026-08-04
>
> 提交：本文件所在的 EB-11 独立实现提交（代码、测试、验收、台账与证据同一提交）

## 任务

- 抖音扫码登录、登录状态探测、失效接管、注销与重启复用整链在摘要校验过的内置
  Chromium 与全新私有 Profile 上走正式执行链验收；
- 验证码、滑块与风控一律转人工接管，不实现绕过；
- 真实抖音扫码需受控账号，按项目规则以自建夹具页完成其余实现与失败矩阵，扫码
  相关验收项标 `🔍 待真实账号`。

## RED

- 2026-08-01 从 `/Applications/自动化运营工具.app` 的正式“账号与平台”页面确认真实
  账号显示“登录正常”，点击“我已处理，重新检查”后页面返回
  `process_unavailable`，可见状态的检查时间没有刷新。该安装制品是 Gatekeeper 接受且
  stapler 校验通过的 Notarized Developer ID App，CDHash 为
  `76f1970aeae49fb2f78ab4400e1a1bcdf4b5b6c0`，实测目录占用 1,104,984 KiB；
- 本轮先写 `frontend/tests/eb-11-formal-app-session-acceptance.test.mjs`，首次执行
  `1 passed, 1 failed, EXIT=1`：正式验收执行器尚未约束二次真实复查、确切签名/摘要、
  PID 定向 UI、完整自有进程树清理与防 symlink 证据写入。
- 后续 `codex review --uncommitted` 识别到 customer Demo 的真实 AppData 会按部署
  Profile 隔离在 `profiles/<profileId>/`，并指出 `/usr/bin/open` 已拉起 App、但 runner
  尚未返回 PID 时存在清理空窗。两项均先补 RED：runner 现从正式二进制反查精确签名
  Profile payload 后推导 Profile 根，只接受规范 UUIDv4 的抖音 Profile；每次启动通过
  一次性环境 nonce 认领自身实例，返回前失败也定向清理本次进程树。
- 第四轮 `codex review --uncommitted` 继续指出两个误放行窗口：由 CLI 传入预期摘要和
  build ID 会退化成待测制品自证，且人工扫码等待期间 App 路径可能被替换。对应测试先
  出现 4 组失败；发布构建现把 Git commit、包含未提交/未跟踪发布输入的完整源码树摘要、
  Executor build ID、部署 Profile 和目标架构写入 `Info.plist`，再由最外层 Developer ID
  签名封住。正式验收不再接收预期制品身份参数，只接受与当前 review 源码、包内 Executor
  和部署 Profile 全部一致的签名发布身份，并在最终证据落盘前再次执行签名、公证、完整
  App 树摘要与全部发布身份核验。
- 第五轮 review 在当前真实仓库执行发布身份计算，复现两个只读 Submodule 的 Gitlink
  被误判为目录，正式构建会在开始前失败。含真实临时 Submodule 的测试先 RED；源码身份
  现直接哈希索引 mode `160000` 与固定 commit，且拒绝未初始化、HEAD 漂移或工作树脏的
  Submodule。正式构建还会先执行既有第三方来源锁、许可证、权利清单与 SBOM 门禁；当前
  仓库的两条 Gitlink 已按索引 commit 纳入完整源码树摘要并实算成功。
- 第六轮 review 继续复现四个证据边界：人工等待期间可先替换 App、启动后再恢复原
  路径；未暂存删除会让源码摘要直接异常；原子写 `Info.plist` 会把 0644 降为 0600；
  写证据时 Ctrl-C 可能留下未宣告 PASS 的文件。四项测试均先单独 RED；runner 现把两次
  实际启动 PID 直接交给 macOS `codesign` 做动态验签，并在每段用户交互前后匹配首次
  核验的 CDHash、Team 与 Authority。源码摘要显式纳入已跟踪缺失项，发布身份写入保留
  plist 原权限，证据发布对包括 Ctrl-C 在内的中断回滚本次自有文件。
- 第七轮 review 复现安全注销只看页面而没有证明旧 UUID Profile 真被删除、证据父目录
  替换后报告路径与实际落点分离，以及相对 deployment Profile 首段软链绕过。三个测试
  先 RED；runner 现从 Chromium argv 绑定唯一规范 UUID Profile，要求旧目录、移除暂存
  目录、锁和 current marker 全部消失且没有进程继续使用，并只接受绝对、全路径无软链的
  deployment Profile；证据目录则以设备号/inode 持续绑定并在路径替换时回滚。
- 第八轮 review 复现包内 Executor/Chromium 可在相同路径被替换，以及未忽略的仓库内
  release work-dir 会让构建自己的输出改变签名源码摘要。两个测试先 RED；runner 现冻结
  两个嵌套运行时的 Identifier、Authority、Team ID 与 CDHash，并对实际 PID 动态验签；
  发布命令在产生任何输出前拒绝仓库根、`.git` 和未被 Git ignore 的仓库内工作目录。
- 第九轮 review 复现 LaunchServices 延迟拉起导致取消后残留进程、证据初检后父目录改指
  App/AppData，以及 JSON 已发布但 `PASS` 输出被 Ctrl-C 中断后留下假通过证据。三项测试
  先 RED；runner 现按 launch nonce 有界轮询并回收延迟进程树，取得目录句柄后用最终解析
  路径再次排除受保护目录，并把已发布 JSON 保留为 inode 绑定的可回滚事务，只有 `PASS`
  成功输出后才提交。
- 第十轮 review 复现浏览器只靠 `--user-data-dir` 字符串即可用软链或同路径替换跳出
  AppData、验收后更新台账会改变已构建制品绑定的源码摘要、证据提交失败前已打印
  `PASS`，以及取消清理的发现窗口短于正常启动超时。五项测试先 RED；runner 现以
  `O_NOFOLLOW` 逐层打开并绑定 0o700/当前用户的 Profile 根、平台目录和 UUID 目录，
  通过 `lsof` 证明 Chromium 进程树实际打开该目录且未打开系统 Chrome/Edge 默认
  Profile，并在审计后复核目录与进程身份。发布源码摘要只排除验收后台账，仍覆盖全部
  发布输入；证据在 inode/父目录复核成功后才打印并 flush `PASS`，打印中断仍回滚；
  nonce 清理覆盖完整 45 秒启动窗口并追加三次安静轮询。
- 第十一轮 review 复现安全注销可通过改名整个 `douyin/` 父目录隐藏旧 Profile、构建
  期间临时修改再恢复源码可绕过首尾摘要，以及 Chromium 只位于 App 子树而不是
  Executor 后代仍被接受。四项测试先 RED；runner 现保留原平台目录和 Profile 的打开
  句柄、设备号/inode，注销后同时从原父目录句柄和 macOS `F_GETPATH` 证明旧 inode 已无
  任何名称；同一进程快照还要求唯一 Chromium 是唯一已动态验签 Executor 的后代。
  发布入口会先把已 review 的主仓和两个 Gitlink 物化为隔离 checkout，从该 checkout
  重启完整构建；Submodule 的实际受跟踪字节也进入 v2 源码摘要，避免 Git LFS/index
  stat cache 隐藏漂移。当前真实仓库快照已实算一致并自动清理。
- 第十二轮 review 复现四个剩余缺口：隔离快照把 Gitlink 克隆成独立仓导致正式第三方
  来源门禁必然拒绝；`assume-unchanged`/`skip-worktree` 可隐藏第三方字节漂移；已暂存
  删除或重命名没有从 detached checkout 清除；扫码、扫码后复查与重启可分别使用不同
  UUID Profile 却仍写出复用成功。四项测试均先 RED；快照现拒绝全部 index 可见性屏蔽
  标志，以独立 `.git/modules/...` 元数据和工作树 `.git` 文件物化真实 Submodule，继承
  已验证 index stat cache、锁定 origin 与 `core.checkStat=minimal`，并镜像主仓 index、
  清除已删除路径。正式验收则跨二维码打开、扫码后健康复查和 App 重启健康复查比较同一
  Profile 路径、父目录设备号/inode 与 Profile 设备号/inode。当前 879MB 两个真实
  Submodule 的物化快照已再次执行 `check_third_party_sources.py` 并通过，且源码摘要与
  当前工作树一致。
- 第十三轮 review 复现两项发布边界：受跟踪软链接可指向仓库外可变文件，使实际构建
  内容变化但 `sourceTreeSha256` 不变；从 `frontend/` 正式入口传入相对 work/archive/
  部署与密钥路径后，快照子进程会改用仓库根重新解释参数。两项回归测试均先 RED；源码
  身份现只接受指向仓库内且由发布清单覆盖的相对软链接，绝对、越界、缺失与未纳入清单
  的目标全部拒绝，同时保留 Hyperframes 实际使用的仓库内相对目录链接。快照重启则绑定
  操作者原始 cwd，使所有相对参数保持同一解析基准。53 项发布/身份/正式验收 Python
  测试、Mypy、Ruff、前端单测/契约/ESLint/TypeScript/生产构建与六项质量门禁通过；当前
  真实主仓及两个 Submodule 再次完整物化、执行第三方来源门禁并得到一致源码摘要。
- 第十四轮 review 证明调用者可预置两个公开环境变量，把普通可写 checkout 和当时摘要
  伪装成已物化快照，从而在编译读取期间临时修改再恢复源码。伪造环境测试先 RED；父进程
  现通过启动前写满并关闭写端的一次性匿名管道传递快照路径和摘要，能力同时绑定直接父
  PID、FIFO 文件类型、固定协议与 4 KiB 上限，子进程单次读取后立即关闭并移除 FD 环境
  名。旧公开路径/摘要环境变量一律拒绝，父进程也会清除所有继承值；55 项 Python 门禁、
  Ruff 与 Mypy 通过，并包含合法能力单次消费和仅环境变量绕过拒绝两条回归。
- 第十五轮 review 在跨平台聚合门禁和验收后提交语义上找到 3 个 P1：Windows 3.12
  无法导入 `fcntl`、没有 `os.fchmod`，且台账完成提交会推进 `HEAD`，使源码字节未变的
  已验收 App 被旧逻辑误判失配。三条回归均先真实 RED；runner 现只在核对 macOS Profile
  inode 时动态加载 `fcntl`，Info.plist 原子临时文件改用跨平台 `os.chmod`，发布身份则要求
  源码摘要逐字一致并要求内嵌提交等于当前提交或是其真实 Git 祖先。这样最终台账提交可复核，
  旁支提交和任意旧源码仍会拒绝。58 项发布/身份/正式验收 Python 测试、Windows 缺失模块
  模拟、完整 Mypy、Ruff、2 项 Node 契约、11 项组件测试、ESLint、TypeScript、Vite 正式
  构建及证据/路线图/第三方来源门禁全部通过；真实宿主机 DMG 建立、挂载、内容核对和卸载
  用例也通过。
- 第十六轮 review 无新增发现；随后首次从该快照入口执行正式签名构建时，真实 Git
  身份复算暴露出测试未覆盖的差异：`.gitignore` 中带尾斜杠的三条目录规则不会匹配
  快照内同名的构建依赖软链接，导致 `.local`、`frontend/node_modules` 和
  `backend/.venv` 被误认成未 review 的发布源码并 fail-closed。真实临时 Git 仓库测试
  先复现 `release source symlink target must be relative and inventoried`；快照现仅在其
  一次性私有 `.git/info/exclude` 中追加三条精确锚定路径，仍在链接后复算完整源码身份。
  59 项发布/身份/正式验收 Python 测试和真实三依赖链接身份复算均已通过；失败构建未
  进入签名、公证或覆盖任何正式制品。
- 第十七轮 review 复现两条验收后立即失效路径：直接父进程仍可自行铸造匿名管道
  payload，让普通可写 checkout 冒充快照；证据若写到仓库内未忽略的新 JSON，则会在
  最终制品复核后改变源码摘要。两条测试先分别以 `TypeError`/`AttributeError` RED；
  子构建现除一次性能力外，还必须位于命令指定 work-dir 直属的 0700、当前用户拥有、
  `source-snapshot-*/repository` 独立 `.git` detached clone，普通 checkout 即使有父进程
  自报能力也拒绝。验收证据则在启动 App 前通过 Git ignore policy 拒绝任何会成为新发布
  源码清单项的仓库内路径，仓库外与 `.local/` 私有证据保持可用。
- 第十八轮 review 无新增发现；真实正式构建随后从物化快照完成 Developer ID 内外层签名、
  App 与 DMG 两次 Apple 公证和 staple、Gatekeeper 隔离下载评估、3093 项生产载荷审计与
  P9-05 全树审计。首次字体资产读取遇到一次上游 SSL record-layer failure，未进入最终
  封签或发布；同一锁定 URL 重试并校验摘要后进入机器级缓存，正式 App 与 539325351-byte
  DMG 全部门禁通过。
- 正式 App 验收首次受另一个仓库的 macOS `SecurityAgent` 密码窗口抢占而无法操作；该窗口
  由用户处理后，同一 `account navigation did not become ready` 仍稳定复现。真实 Tauri/
  WebKit AX 树证明“账号与平台”是支持 `AXPress` 的 `AXMenuItem`，但 AppleScript 直接遍历
  `entire contents` 得到空集合，且内联强转元素 `name` 不能可靠匹配。两项回归先得到
  `2 failed`；验收器现先物化 AX 元素列表，并把 `name` 绑定到变量后再比较。修后同一正式
  App 的真实侧栏点击成功，页面读到“抖音 / 登录正常”及既有检查时间；63 项发布、身份与
  正式验收 Python 测试和 Ruff 已通过，完整生命周期仍待本轮扫码验收后收口。
- 第十九轮 review 指出两项直接跨平台/清理缺陷，回归先分别得到异常和失败：Windows
  Python 3.12 不支持 `os.chmod(..., follow_symlinks=False)`，以及 SIGKILL 后新派生的
  helper 只被发现却没有收到终止信号。正式实现现对刚创建的临时文件使用跨平台 `chmod`，
  并在强制清理轮询中持续终止新发现的自有进程；60 项 Python、2 项 Node 契约、Ruff 与
  diff 门禁通过。review 同时证明同一用户下的恶意构建发起者可以自行仿造快照布局并在
  编译期间篡改后恢复；本地发布操作员与出包主机本来就是签名流程的信任边界，因此源码
  摘要只作为受信发布流程中的可复现性/误操作门禁，不再宣称能对抗同 UID 主动攻击者，
  也不继续横向扩展新的特权构建服务。
- 2026-08-03 正式构建先暴露 macOS 完整包体积基线仍使用旧样本：实际完整签名 App 为
  `1,301,102,222` bytes，旧基线会误拒绝。新增真实样本回归先 RED，基线改为该完整包后
  38/38 通过且下限仍窄于最小完整运行时。Apple 加速上传返回
  `abortedUpload/completedParts: []/deadlineExceeded` 后，新增“仅此错误自动用
  `--no-s3-acceleration` 重试一次”的回归先 RED，修复后发布装配 39/39；其他公证错误仍
  fail closed。
- 真实扫码验收又依次暴露 3 个只属于 macOS 采样器的竞态：Chrome
  `crashpad_handler` 会重挂到 PID 1；签名读取期间 Chromium 主 PID 可正常退出；`lsof`
  审计期间 Renderer 可退出并令命令返回 1。首轮 Codex Review 又复现两条更窄的退出窗口：
  进程可在 `codesign` 命令本身执行期间退出，重挂 helper 也可在 nonce 读取前退出。五条
  回归均先 RED；runner 现在只接纳携带本次随机 launch nonce 的重挂 helper，进程/PID 或
  open-file 样本发生真实变化时仅丢弃当轮并重试，稳定进程的外来身份、签名差异或
  `lsof` 失败仍立即拒绝。正式验收 runner 47/47、三组发布门禁合计 124/124 通过。

- 首轮 `uv run pytest tests/integration/test_douyin_login_embedded_browser.py`：
  `ModuleNotFoundError: No module named 'tests'`（共享 staging 夹具尚不存在，
  且 backend tests 非可导入包——夹具按 pytest 惯例落入
  `tests/integration/conftest.py`）；
- 二轮 4 errors：`StagingRejected: output directory already exists`
  （`tmp_path_factory.mktemp` 预创建目录，而生产 staging 管线要求输出目录不存在，
  改传 `mktemp(...)/"staging"` 子路径）；
- 三轮 2 failed：`SecurityError: Failed to read the 'localStorage' property`——
  `about:blank` 是 opaque origin 无持久化存储；重启复用/注销用例改为 route 到
  `https://www.douyin.com/...` 真实 origin 的自建状态页。

## GREEN

```text
uv run pytest tests/integration/test_douyin_login_embedded_browser.py   5 passed
uv run pytest tests/unit tests/contract                                 3119 passed, 1 skipped
uvx ruff check --config pyproject.toml tests/integration/               All checks passed
python3 scripts/check_embedded_browser_video_roadmap.py                 通过
python3 scripts/check_user_facing_branding.py                           通过
backend/.venv/bin/python -m unittest scripts.test_embedded_browser_package \
  scripts.test_release_assembly scripts.test_run_eb_11_formal_app_acceptance
                                                                         124 passed
backend/.venv/bin/python scripts/check_release_package_wiring.py         通过
```

五条整链（全部真实 staged 内置 Chromium + 全新 0o700 私有 Profile + 自建夹具页，
无系统浏览器、无网络、无 Mock 流程对象）：

1. QR 状态机：login_required → awaiting_scan（二维码可见）→ awaiting_confirmation
   → healthy → qr_expired；captcha/slider/risk 一律 HANDOFF_REQUIRED +
   RISK_CHALLENGE + circuit_open（人工接管，不绕过）；关闭后仅剩主窗口；
2. 会话探测：healthy/expired/missing/risk 四态与证据枚举逐一命中
   （`DouyinSessionDetector().check(window)`）；
3. 重启复用：同一 Profile 两次启动之间 localStorage 登录态证据持久化，权限保持
   0o700；
4. 注销：清除 cookie 与 localStorage 后重启不复用旧态；
5. 正式命令面：生产 `DouyinLoginCommandOperation.handle` 按生产 CLI 装配（真实
   `DouyinSessionHealthReporter` + 真实 `ExecutorLedger` + 真实
   `BrowserLaunchAuthority` + 真实 `DouyinQrLoginFlow`）驱动
   `douyin.login.open`→"awaiting_scan"、`douyin.login.recheck`→"healthy"、
   `douyin.logout.complete`→"logged_out"，命令带 50 位认证证明与绝对路径校验；
   outbound 队列收到 3 个健康信封且不含 Profile/浏览器路径，运行结束浏览器关闭。

共享夹具 `staged_embedded_chromium`（`tests/integration/conftest.py`）：经生产
staging 管线（manifest/版本/平台/逐文件摘要）从 EB-03 缓存解包，缓存缺失 skip，
绝不回退系统浏览器。

## 失败矩阵

- 二维码过期：QR_EXPIRED 显式暴露并保持 circuit_open；
- 验证码/滑块/风控：HANDOFF_REQUIRED 转人工，恢复 healthy 后 circuit 闭合；
- 登录失效：EXPIRED/MISSING 显式暴露，不静默重放；冲突页面事实 → UNKNOWN；
- 命令面：非法命令类型/缺路径/相对路径/控制字符路径在模型层拒绝（既有 3119 项
  单测含全量拒绝矩阵回归）；操作异常时 best-effort 关闭运行时并抛
  `PlatformCommandRejected`；
- 注销与运行中窗口竞争：logout 先关闭活动 flow/runtime 再 revoke 授权；
- Profile 边界：0o700 权限贯穿；staging 输出目录已存在即拒绝。

## 正常用户路径验收

历史分层证据已覆盖 App→Rust→Executor 的正式命令装配与内置 Chromium；2026-08-01
补做正式安装 App 的真实账号路径时，页面能读取既有“登录正常”投影，但用户主动复查
返回 `process_unavailable`，所以当前不能用历史投影声称会话可复用。完成条件是当前主线
重新构建的签名、公证 App 在同一正常页面首次复查成功，退出时 Executor/Chromium
进程树为零，重启后再次点击同一按钮并得到新的“最近检查”时间和“登录正常”。

2026-08-02 已闭合其中的当前故障：真实 App 的执行器因重连时反复重放过期 durable
outbox，被控制面以 4406 断开；修复版 production-mode 隔离 App 从同一“账号与平台”
入口点击“打开登录处理”，捕获到内置 Chromium 使用既有运营 Profile 启动，再点“我已
处理，重新检查”后页面时间刷新为 `2026/8/2 14:40:41`，本机和线上健康投影均到
revision 5。执行器持续跨越多个 heartbeat 周期，App 正常退出后自有进程树为零。该制品
为避免覆盖用户正式安装而使用 ad-hoc 重组，尚不能替代新的 Developer ID/公证包证据。
首轮 Codex Review 指出的 outbox 全历史恢复和外置租约幂等续删也已补 RED/GREEN，并在
macOS、Windows 原生 Profile 矩阵各 10/10 通过；这不改变上述正式发布包遗留边界。

2026-08-03 用户在已签名、完成全部内容审计但尚未取得 Apple ticket 的 production 候选
App 中完成真实扫码。产品先从 `profile_recovery_required` 经可见“安全注销/确认注销”清除
旧 Profile 与 Chromium，再从“打开登录处理”拉起包内签名 Executor 与 Chromium。扫码后
主动复查得到“登录正常”；正常退出前后扫码 Profile 始终为
`douyin/9c1e4f4f-1077-4f4a-a51c-da7c14edb07b`，device `16777233`、inode
`36371490`，重启复查时间从 `2026/8/3 02:04:10` 刷新为 `2026/8/3 02:08:20`，仍绑定
同一路径与 inode，退出后包内进程为零。该证据明确标记 `candidateOnly=true`、
`notarized=false`，只闭合 macOS 真实功能，不替代正式公证包证据。

同日当前源码重新从隔离 source snapshot 构建，App 内嵌 tree 摘要
`1b4270a7495b64af03f3ad668ed169d7cb71ec1de1f4e92b9eb096751aae8944`，Developer ID
深度验签、3,186 文件 / `1,301,102,220` bytes 内容门禁、E4-15 与 P9-05 全过。
普通与 `--no-s3-acceleration` 上传均在零 completed part 时超时，App 没有 staple；因此
仍不是正式通过。Shadowrocket VPN 未关闭，系统动态代理未改写。

## 真实边界

- 内置 Chromium 与系统 Chrome 在登录链行为一致性已由同套流程对象在两种浏览器的
  测试（B5 历史 + 本任务）分别证明；生产只接受内置路径（EB-10 已删除选择链路）；
- `about:blank` 无持久化存储是浏览器安全语义，登录态证据只在真实 origin 下持久化；
- 真实抖音页面选择器漂移风险由 `DOUYIN_QR_LOGIN_FLOW_VERSION` 版本化与
  待真实账号项覆盖；
- Windows 侧已于 2026-07-25 完成 staged Chromium 集成链路；它不是 Windows 正式
  App 正常用户路径证据。Windows 正式 App 的登录、注销与重启复用仍须在独立真机
  checkout 中补验。

## 清理

- 全部测试经 `runtime.running` 上下文在成功/失败路径关闭浏览器与进程树，逐条
  断言 `not runtime.is_running`；staged Chromium 位于 pytest 会话临时目录随
  测试清理；无残留 Profile/进程。

## 文档变化

- 2026-08-01 重新激活专项 Roadmap 的 EB-11 为 `🧪 RED`；2026-08-02 在正式 App
  页面导航和现有真实登录态读取闭合后，因用户次日再配合扫码改回 `🔍 待验收` 并同步计数；
- `docs/development/windows-evidence-checklist.md` 新增 EB-11 Windows 待补节；
- 本文件记录全部 RED/GREEN/矩阵/边界/清理证据。

## 遗留项

- Windows staged Chromium 链路已于 2026-07-25 补齐；Windows 正式 App 正常用户
  路径仍待在独立真机 checkout 验收，真实扫码按用户 2026-08-03 指示留到其可配合时；
- macOS 候选包真实账号生命周期已闭合。**2026-08-04：公证阻塞已解除**——EB-16 在
  `HEAD 40815b2b` 出的包完成 Developer ID 签名 + 公证 + staple，经
  `xcrun stapler validate`（`The validate action worked!`）与 `spctl -a -vvv -t exec`
  （`accepted` / `source=Notarized Developer ID`）独立复验，包已备在
  `.local/eb-11-notarized/自动化运营工具.app`。此前记录的 `deadlineExceeded` 系
  钥匙串 `at-tools-notary` profile 丢失所致，凭据本身一直有效；
- **剩余唯一一项：在该公证包上重跑一次真实扫码复验。** 需人工——
  `run_eb_11_formal_app_acceptance.py` 强制 `--interactive-device-acceptance` 且要求
  `sys.stdin.isatty()`，检查点 `douyin_scan_confirmed` 会提示在运营浏览器中扫码。
  2026-08-03 那次扫码的 Profile `9c1e4f4f-1077-4f4a-a51c-da7c14edb07b` 已不在盘上
  （正式 App 的 `embedded-browser-profiles/douyin/` 为空，时间戳停在 7/27），
  没有登录态可复用，所以必须重新扫。命令：

  ```bash
  backend/.venv/bin/python scripts/run_eb_11_formal_app_acceptance.py \
    --interactive-device-acceptance \
    --app "$PWD/.local/eb-11-notarized/自动化运营工具.app" \
    --deployment-profile <部署 Profile 路径> \
    --evidence <证据输出路径>
  ```


## 2026-08-04：正式公证包上的真实扫码（用户路径成立，验收脚本三处缺陷）

### 用了什么包

当日重出的正式发布包（前一个 `.local/eb-11-notarized/` 那份**用不了**：它由 EB-16 的函数
调用路径产出，`Info.plist` 里**没有** `AutomationToolReleaseIdentity`，用本脚本自己的
`read_signed_release_identity()` 实测直接
`EB-11 signed release identity is invalid`）。新包走 `build_release_package.py` CLI，
带 demo 部署 Profile，全程 27 分钟：

```text
commit b5884c53a4db | target macos-arm64 | profile demo-xuanbai
App 公证 Accepted / DMG 公证 Accepted / staple 生效
spctl: accepted, source=Notarized Developer ID
verify_release_artifact(): ✅ 发布身份校验通过
```

### 真实用户路径成立（可外部核对的终态）

用户从正式包的正常入口打开 App →「账号与平台」→「打开登录处理」→ 弹出运营浏览器 →
手机抖音扫码 + 手机确认 → 界面显示「登录正常」。磁盘终态：

```text
Profile  profiles/demo-xuanbai/embedded-browser-profiles/douyin/
         3f155810-eb7c-42d5-8f15-9ab345036f1e
         inode 310941051   创建 12:25:08   占盘 26 MB   权限 drwx------
Cookies  Default/Cookies  40,960 bytes  最后写入 12:39:22
         .douyin.com 37 条 / www.douyin.com 11 条 / login.douyin.com 1 条（共 50）
marker   current-douyin-profile-v1 → 3f155810-eb7c-42d5-8f15-9ab345036f1e
```

第三次运行验收脚本时 `open_account_page()` 的 `登录正常` 断言**通过**——即这个登录态
不只是界面文案，脚本读同一棵可访问性树也确认了。

### ❌ 验收脚本本身的三处缺陷（EB-11 未闭合的真正原因）

**这三处都不是环境问题。** 包是正式公证包、走的是正常入口、账号是真的，而
`run_eb_11_formal_app_acceptance.py` 仍然跑不完。

**1. 无法从「未登录」冷启动。** `open_account_page()` 死等 `HEALTHY_LABEL="登录正常"`，
拿不到就 `did not expose required UI state: 登录正常`。它验的是「已登录 → 注销 → 重扫 →
重启复用」这条生命周期，**起点必须已有登录态**。干净机上第一次扫码没有任何脚本路径可走
——而干净机正是真实用户的状态，也正是 EB-17 的验收对象。本次前两次失败都源于此。

**2. `press()` 分不清「点了」和「点了个禁用按钮」。** 它按可访问性名字找元素后
`perform action "AXPress"`，而 `PlatformSessions.tsx` 上三个按钮都带
`disabled={pending !== null}`。禁用状态下元素仍有 name，AXPress 照样返回成功，
脚本据此认为点击生效。这与本仓库 `~/.claude/CLAUDE.md`「命令必须能区分『没有』和
『没查』」是同一个根因，只是出在了驱动侧。

**3. 「主动复查」不产生可观察的 Chromium，而脚本硬要求它。**
`require_complete_runtime()` 要 `embedded_browser_observed`，
`require_safe_logout_cleanup()` 还要从 Chromium argv 的 `--user-data-dir` 反查旧 Profile
UUID。实测第三次运行 20 秒即失败于 `EB-11 did not observe the embedded Chromium`，
而同一时段 App 日志里**一个浏览器启动事件都没有**（只有 `startup.local.completed`、
`executor.auto_start.requested`、`executor.process.start.succeeded`）。用户亦确认：
「这个 app 我跑了好几次，从来不会主动弹那个浏览器」——只有手动点「打开登录处理」才会弹。

### 结论

- **产品能力**：正式包上的抖音扫码登录、登录态落盘与界面状态呈现，**已由真实账号验证**；
- **仍不成立**：注销后旧 Profile 是否真删干净、重启是否复用同一 inode、退出后进程树是否
  清零——这三项**只有脚本能验，而脚本跑不完**，因此 EB-11 保持 `🔍 待验收`；
- 修复上述三处缺陷需要改验收脚本（可能还要给 App 加一条「复查时确实启动浏览器」的可观察
  路径），属于独立任务，不在本次范围。**不得因为用户路径跑通就把 EB-11 标完成**——
  那正是本仓库反复出事的那种「验收深度不够」。

## 2026-08-04 下午：修掉验收脚本三处缺陷

三处均先写红再实现，红的措辞就是当天用户实跑时看到的报错。

### ① `press()` 现在区分「点了」和「点了个灰按钮」

`AXPress` 对禁用元素什么也不做，而禁用元素照样带 accessibility name，所以原实现按名字
匹配后把 AppleScript 的返回当作点击生效——两种情况输出完全相同。
`PlatformSessions.tsx` 上三个按钮都带 `disabled={pending !== null}`，即每次上一个动作
在途时都会进入该状态。

现在脚本在按下前先读 `enabled of elementReference`，禁用则报
`App control is present but disabled, so it was not pressed`。

RED：`test_press_refuses_a_control_that_is_only_visible_not_enabled`，实跑报
`'enabled of elementReference' not found in ...`。

### ② 冷启动：`open_account_page()` 改为观察后分流

原实现死等 `登录正常`，于是干净机（真实用户的状态、也是 EB-17 的验收对象）上第一次运行
必然超时于 `did not expose required UI state: 登录正常`，且在超时前不会弹出任何二维码。

现在返回 `(rendered, already_signed_in)`：没有登录态时主流程先走一次
`open_login_for_scan → confirm_scan_checkpoint → recheck`，把登录态建立起来——**这一步
本身就是 EB-11 要求的「首次扫码登录」，不是绕过**——随后照常验证复查、安全注销、重扫、
重启复用整条生命周期。

RED：`ColdStartTests` 两条，一条要求接受 `需要登录`，一条要求**仍然**能识别既有登录态
（否则「兼容冷启动」会退化成永远走冷启动分支）。

### ③ 运行时采样从慢速 UI 读取上摘下来

`observe_instance_runtime()` 原本与 `visible_ui_text()` 在同一个循环里交替执行，而后者
遍历整棵可访问性树，是循环里最慢的一步。登录态复查由执行器经 Playwright 拉起内置
Chromium 完成，进程很短命；当天实测它整个落在两次采样之间，于是
`did not observe the embedded Chromium` —— 而 App 侧一切正常，日志显示执行器进程正常启动。

**把循环采样加密解决不了**：循环阻塞在 AppleScript 里，不是在 sleep。新增
`continuous_runtime_observation()`，用独立线程按 `RUNTIME_SAMPLE_SECONDS = 0.05` 持续采样，
已接入 `open_login_for_scan` 与 `recheck_healthy_session` 两处观察窗口。

RED：`test_short_lived_chromium_between_two_ui_reads_is_still_observed`，实跑报
`module has no attribute 'continuous_runtime_observation'`。

### GREEN

```bash
backend/.venv/bin/python scripts/test_run_eb_11_formal_app_acceptance.py
# Ran 51 tests ... OK   （原 48 + 新增 3）
uvx ruff check --config backend/pyproject.toml \
  scripts/run_eb_11_formal_app_acceptance.py scripts/test_run_eb_11_formal_app_acceptance.py
# 仅剩 3 条历史存量（RUF100 / SIM105），与本次改动无关
```

### 仍未验证的部分

**这三处修复只经过单元测试，没有在真实 App 上跑过。** 修改本身改变了源码树，当前那个
已公证的包（`b5884c53`）的发布身份因此不再匹配——下一次真实扫码验收必须先重出包。
在真实链路复跑通过之前，EB-11 保持 `🔍 待验收`，不因单元测试全绿而改动状态。

### 出包与扫码必须连着做（2026-08-04 实测代价）

发布身份含**完整源码树摘要**，只排除 `docs/development/**` 与三份 roadmap。因此：

> **构建开始那一刻，树就冻结了。之后任何对被计入路径的提交，都会让这个包对 EB-11 失效。**

当天付出两次代价：

1. 上午出的包（`40845b2b`）被下午的改动作废；
2. 下午重出的包（`a505629b`）**在它自己还在跑公证的时候**就被作废了——我在等待期间提交了
   四次，其中两次动了 `scripts/` 与 `contracts/`（补驱动归属、修脚本聚合的两条红）。
   包跑完立即校验：`EB-11 App was not built from the current reviewed source tree`。

```text
包内 commit : a505629b0303      当前 HEAD   : d42b7b4cb726
包内树摘要 : b02a8c5ee5507f41   当前树摘要 : 4a6980cb9192889a
```

**操作规则**：出包与真实扫码之间不得提交任何计入摘要的改动。等待公证的那 15 分钟里，
只能改 `docs/development/**` 或三份 roadmap，或者干脆不改。要么就等真正准备扫码时再出包
——27 分钟里大部分是等 Apple，提前出并不省时间，反而会被后续工作作废。

**为什么这条容易犯**：等公证的十几分钟看起来是「空闲时间」，而手边永远有事可做；
而作废是**静默**的——包照常构建成功、公证成功、staple 成功、Gatekeeper 通过，
只有 EB-11 那一次身份校验才会说出来。

## 2026-08-04 20:48：macOS 整条生命周期在公证包上跑通（PASS）

上一节那三处脚本修复此前只有单元测试，这是它们**第一次在真实链路上跑**，也是
`run_eb_11_formal_app_acceptance.py` 第一次跑完并写出证据。

### 出包（本轮先被一道门禁拦下，拦对了）

第一次构建止于 staging：

```text
motion catalog release check failed: release manifest input pins drifted from the lock
```

判据是摘要而不是措辞——`contracts/quality/motion-asset-overlay.v1.json` 的实际摘要
`055c7190…` 与 `motion-catalog-release.v1.json` 里 pin 的值**一致**，漂的是本地那棵
8/1 15:20 构建的发布树（其 `manifest.json` 记着旧值 `cdea8048…`）。即仓库自洽，本地
产物过期。用 `backend/.venv/bin/python scripts/build_motion_catalog_release.py`
重建（134 items / 337 files / 150 处素材替换 / 68 项商标替换全部校验），仓库零改动，
`git status` 仍空，包的源码摘要不受影响。

**这道门禁挡住的是一批旧版素材替换被打进客户包**——而签名、公证、Gatekeeper
对此都不会有任何意见。

顺带一条本机事实：`python3` 是系统 3.9，跑这批脚本会报
`write_text() got an unexpected keyword argument 'newline'`（该参数 3.10 才有）。
一律用 `backend/.venv/bin/python`（3.12.13）。

### 用了什么包

```text
commit f20a014cc056  = 当日 HEAD        树摘要 509ab1366ad3…
profile demo-xuanbai  target macos-arm64  executorBuildId customer-demo-xuanbai
App 公证 Accepted + stapled（a14bcff6）  DMG 公证 Accepted + stapled（8a43f82a）
spctl 隔离评估 accepted / source=Notarized Developer ID
包体 1,302,085,541 bytes / 3,212 文件（内置浏览器 334 文件 338,111,343 bytes）
```

### 执行与结果

```bash
backend/.venv/bin/python scripts/run_eb_11_formal_app_acceptance.py \
  --interactive-device-acceptance \
  --app .../bundle/macos/自动化运营工具.app \
  --deployment-profile .local/customer-demo-release/deployment-xuanbai.json \
  --evidence .local/eb11-formal-20260804/evidence.json
# [EB-11] PASS: formal App real-session evidence written to …/evidence.json
```

本机已有 8/4 上午的登录态，故走 `already_signed_in` 分支，只需注销后重扫一次。
证据（`eb-11.formal-app-session.v1`）关键读数：

```text
realQrLoginConfirmed        true     safeLogoutObserved            true
restartSessionReused        true     ownedProcessTreeResidualCount 0
normalAppRecheckCount       3        privateBrowserStateRead       false
first/qr/restart 三个阶段 × (打包执行器 / 内置 Chromium / App 自有 Profile) 9 项全部 observed
可见状态「登录正常」，检查时间 20:47:04 → 20:47:20 → 20:48:39 → 20:48:52 逐段推进
```

### 可外部核对的终态（脚本之外，直接读磁盘）

```text
marker  embedded-browser-profiles/current-douyin-profile-v1 → a3b06c48-2a0c-4101-8e03-e43e92d0932d
        写于 20:47:14，重启后未再变——复用而非新建的独立佐证
Profile embedded-browser-profiles/douyin/a3b06c48-…
        inode 37220684  drwx------  创建 20:47:14  末次写 20:48:52  占盘 19 MB
Cookies Default/Cookies  45,056 bytes  20:48
目录下唯一 Profile：注销前那个已无任何名称（safe logout 的磁盘侧证据）
残留进程 0（`pgrep` 匹配 App / 执行器 / Chrome for Testing 全部为空）
```

Profile 创建于 20:47:14，落在注销之后、弹码之前——它就是本轮重扫新建的那个，
登录态落在其中，重启后仍是同一 inode。

**顶层 `profiles/demo-xuanbai/current-douyin-profile-v1`（7/26 21:33，指向早已不存在的
`739b9297…`）不是缺陷**：产品读的是 `embedded-browser-profiles/` 那份，
`browser_profiles.rs:20` 注释明写 EB-09 之前的旧根不迁移、不读取、不回退。

### 遗留

- **仅剩 Windows 正式 App 的真实扫码生命周期**，按用户要求留到其可配合时补验；
  在此之前 EB-11 保持 `🔍 待验收`，不因 macOS 闭合而改完成态
  （§8「macOS 和 Windows 的正式包、权限和内置 Chromium 链路分别验收」）。
