# CQ-02 完成证据

> 状态：🔍 待验收（确定性扫描已在 macOS + Windows 双平台闭合，缺真实 App 环境下的可访问性树证据）
>
> 日期：2026-07-26
>
> 提交：`68f0402` 运行时可访问性树扫描；本文件所在提交补齐 Rust/后端与产物名扫描
>
> 依赖：IM-08（🔍 待验收）、BM-16（🔍 待验收）、VE-08（✅）、AV-03（✅）

## 任务

扫描 React、WebView、标题、**可访问性树**、加载、错误、日志、任务、文件名和导出，
确认用户界面不泄漏上游技术名；法务页单独白名单。

## 起点事实

AV-03 已经交付 `scripts/check_user_facing_branding.py`：静态扫描
`frontend/index.html`、`frontend/src`、`frontend/src-tauri/tauri.conf.json`，
按 `contracts/quality/user-facing-terminology.v1.json` 检查禁用词、未解释术语与
概念区分文案，当时 50 个文件通过。

与 CQ-02 的差距，逐项核对过：

| CQ-02 点名的面 | 起点 | 本任务后 |
| --- | --- | --- |
| React 源码文案 | AV-03 已覆盖 | 不变 |
| WebView 标题 / `tauri.conf.json` | AV-03 已覆盖 | 增加 Rust 侧 `.title(...)` 窗口标题 |
| **可访问性树** | 未覆盖 | 运行时扫描（`frontend/e2e/upstream-name-leak.spec.ts`） |
| 加载 / 错误 / 通知文案 | 部分覆盖 | 运行时扫描补上运行时组合的部分 |
| **Rust 侧用户可见字符串** | 未覆盖 | 本任务覆盖 |
| **后端用户可见字符串** | 未覆盖 | 本任务覆盖 |
| 任务名 | 未覆盖 | 随中文文案一并扫描 |
| **产物文件名 / 导出名** | 未覆盖 | 本任务按文件名单独扫描 |
| 日志 | 未覆盖 | 只覆盖中文日志文案，见「真实边界」 |
| 法务页白名单 | 契约已声明路径 | 页面已于 2026-07-26 建成，白名单开始真实生效 |

## 两条扫描线的分界，实测得出

最初写的是"静态扫描永远补不上可访问性树"，但注入验证时**第一次注入的泄漏静态扫描也抓到了**
——它会扫模板字面量里的常量部分。换成一个值来自数据流、源码里根本不存在该字符串的可及名再试，
结果才是决定性的：

| 注入的泄漏形态 | 静态扫描 | 运行时可访问性树扫描 |
| --- | --- | --- |
| 模板里的常量（`via Browser Use`） | 抓到 | 抓到 |
| **值来自数据流**（`via ${approval.videoSummary}`，值在运行时才有） | **50 文件全过** | **当场抓住** |

## 原生源码为什么不能整文件扫

前端源码里出现上游项目名一律算违规，所以静态扫描读整个文件。Rust 与 Python 不同：
`HYPERFRAMES_FFMPEG_PATH` 是 Worker 的环境变量名，`skills/hyperframes-creative/frame-presets`
是素材根路径，`vendor/moneyprinterturbo` 是 Submodule 路径——CLAUDE.md 明确允许上游名出现在
代码、内部诊断和 SBOM 里。整文件扫会把这三处合法常量判成违规，唯一的"修法"是把词表放宽，
那等于把闸门拆了。

所以原生扫描换了判定口径：**只扫用户能读到的部分**，判据是机械的而不是"哪个模块算用户面"的主观判断——

1. **带中文的字符串字面量**＝运营文案或页面对象匹配的平台原文（Rust/Python 的标识符、
   路径和协议值都是 ASCII，这条把两者分开）；注释一律跳过，因为内部说明里写上游名是允许的；
2. **产物与导出文件名**＝落到用户磁盘上的文件。它不带中文，上一条看不见它；它也塞不进中文解释，
   所以只查上游名，不查未解释术语——对文件名要求"同句给出中文解释"是条不可能满足的规则。

## RED

第一轮（运行时可访问性树，提交 `68f0402`）：

```text
npx playwright test e2e/upstream-name-leak.spec.ts
  4 failed
  TypeError: Cannot read properties of undefined (reading 'snapshot')
```

`page.accessibility.snapshot()` 在当前 Playwright 已被移除，改用 `locator.ariaSnapshot()`
——它直接给出屏幕阅读器会走的那棵树的 YAML。

第二轮（Rust/后端与产物名，本提交）：

```text
python3 scripts/check_user_facing_branding.py --self-test
  NameError: name 'native_user_copy' is not defined
```

## GREEN

```text
npx playwright test e2e/upstream-name-leak.spec.ts        4 passed
python3 scripts/check_user_facing_branding.py --self-test passed
python3 scripts/check_user_facing_branding.py             passed (50 frontend, 247 native files)
scripts/check_embedded_browser_video_roadmap.py           passed
scripts/check_third_party_sources.py                      passed
```

## 扫描器有效性验证（不能省）

一个全绿的扫描器可能只是没在扫。两轮都做了注入验证，并且都在验证后恢复原状、复跑确认干净。

第二轮注入三处、一次跑出三类命中：

| 注入位置 | 注入内容 | 扫描结果 |
| --- | --- | --- |
| `material_video_studio.rs` 窗口标题 | `智能素材成片 by MoneyPrinterTurbo` | `forbidden term 'MoneyPrinterTurbo' in user copy` |
| `browser_use_safety.py` 动作确认提示 | `由 Executor 处理` | `unexplained industry term 'Executor'（use '本机执行器'）` |
| `motion_video_studio.rs` 成片文件名 | `hyperframes-broll-result.mp4` | 同时命中 `Hyperframes` 与 `broll` |

反向证据同样重要：仓库里本来就存在的 `HYPERFRAMES_FFMPEG_PATH`、
`skills/hyperframes-creative/frame-presets` 三处合法内部引用，扫描**没有**误报——
这正是"只扫用户可读部分"这条口径要证明的。

## 双平台证据（2026-07-26）

Windows 验收机 `F:\automation-tool`（HEAD `0ac7dff`）真机执行同两条门禁：

| 平台 | 静态扫描 | 运行时可访问性树扫描 |
| --- | --- | --- |
| macOS arm64 | passed (50 frontend, 247 native files) | 4 passed |
| Windows x86_64 | passed (50 frontend, 247 native files) | 4 passed |

**只对文件数不足以说明问题**——数量相同也可能是"漏扫一个、多扫一个"相互抵消。
所以另外导出了扫描器实际读取的**全部相对路径 + 每个文件内容的 SHA-256**，两边逐行对比：

```text
mac lines: 299   win lines: 299
### staticScan count=50
### nativeScan count=247
RESULT: IDENTICAL — same 50+247 files, same content hashes on both platforms
```

扫描器用 `relative_to(root).as_posix()` 归一化路径，`rglob` 在 NTFS 上没有漏项；
Windows 侧 `core.autocrlf=true` 但仓库 `.gitattributes` 强制 `eol=lf`，加上 Python
文本模式读取会归一换行，所以内容哈希两边完全相同，扫描语义不受换行影响。

Windows 侧首轮 4 条用例全失败，原因是该机 Playwright 浏览器缓存停在 chromium-1217，
而仓库锁定的 `@playwright/test@1.61.1` 需要 1228。补装后 4/4 通过——环境滞后，
不是产品缺陷，且没有放宽任何断言或跳过用例。

## 失败矩阵

| 场景 | 结果 | 覆盖 |
| --- | --- | --- |
| 任一页面的可及名含上游名 | 变红并指出是哪一页 | 运行时 |
| 可及名由运行时数据拼出 | 变红（静态扫描漏） | 运行时（已注入验证） |
| 发布临界点渲染后含上游名 | 变红 | 运行时 |
| 不明确结果的解释文案含上游名 | 变红 | 运行时 |
| 启动诊断（按错误码拼装）含上游名 | 变红 | 运行时 |
| 文档标题含上游名 | 变红 | 运行时 |
| 前端源码字面量含上游名或未解释术语 | 变红 | 静态（AV-03 既有） |
| Rust 窗口标题 / 风格名 / 错误文案含上游名 | 变红 | 静态（本任务，已注入验证） |
| 后端动作确认提示含未解释术语 | 变红 | 静态（本任务，已注入验证） |
| 成片或导出文件名含上游名 | 变红 | 静态（本任务，已注入验证） |
| Rust/Python 内部常量、路径、注释含上游名 | **不报**，这是允许的 | 静态（本任务，已反向验证） |
| 扫描根配置指向不存在的目录 | `configured scan root does not exist` 直接失败 | 静态 |
| 用户可见源码是符号链接 | 拒绝 | 静态 |

## 正常用户路径验收

运行时扫描本身走真实用户路径：在 UI Harness 里逐页点击左侧导航进入 8 个页面，再走完发布的
发起 → 确认 → 终态，每一步都对**渲染后的**可访问性树取快照。这一层证明的是 React 渲染结果。

Rust 与后端产生的字符串本任务用静态扫描覆盖，没有在真实 App 里核对——原因见「真实边界」。

## 真实边界

1. **运行时扫描只在 UI Harness 里跑**，用的是受控测试 Adapter。真实 App（debug 构建）会停在
   启动环境门禁"桌面运行环境需要处理"，工作台不挂载，八个页面走不到，所以真实 App 的可访问性树
   还没扫过。这与 PB-07 记录的是同一个环境门禁，需要预置好的启动环境才能打开。
2. **日志只覆盖中文文案**。带中文的日志文案会被扫，英文日志行不会。内部日志按 CLAUDE.md 本就
   允许出现上游名，用户可见的诊断导出用的是 `automation-tool-diagnostics-<id>.zip` 与
   `application/vnd.automation-tool.*` 这类产品中立命名，但"导出包内容逐条脱敏"没有独立断言。
3. **产物名扫描只认识契约里声明的扩展名**（`.mp4/.mov/.mp3/.wav/.srt/.vtt/.zip/.csv`）。
   新增一种导出格式而不更新契约，该格式的文件名不会被扫。
4. **法务页白名单已有对象**：`frontend/src/features/legal/third-party-software/` 已于
   2026-07-26 建成并接入左侧导航，白名单不再空转——品牌扫描文件数仍是 50，证明这一页确实
   走的是白名单豁免而不是放宽词表。详见 `FIX-third-party-software-notice-page.md`。
5. **词表是人工维护的**，新的上游依赖进来时要同步加词，没有机制强制这一点。

## 清理

扫描全部在无头浏览器里跑，由 Playwright 自身回收；静态扫描是纯读文件的进程内检查；
注入验证的三处改动已按备份恢复，`git status` 复核过只剩本任务的预期改动。未新增任何常驻服务。

## 文档

- `frontend/e2e/upstream-name-leak.spec.ts`（`68f0402` 新增）
- `scripts/check_user_facing_branding.py`（新增原生源码与产物名扫描）
- `contracts/quality/user-facing-terminology.v1.json`（新增 `nativeScan` 策略块）
- `docs/embedded-browser-video-studio-roadmap.md` CQ-02 行状态与进度概览计数
- 本文件

## 已知误报：中文 docstring 被当成用户文案

原生扫描的判据是"带中文的字符串字面量＝用户可见文案"。docstring 也是字符串字面量，
所以一个中文写的技术说明会被要求给出术语的中文解释——2026-07-26 写
`pyinstaller_support.remove_browser_installer_scripts` 时实测撞上：docstring 里提到
`Chromium`，扫描要求改成"浏览器组件"。

当时的处理是把该 docstring 改回英文（与本仓库绝大多数 docstring 一致），不是放宽扫描。
但判据本身有缺陷：**内部技术文档不是用户文案**，这个误报会长期存在，并且会形成一个
错误的激励——逼人不敢用中文写技术注释。注释（`#` 开头）不受影响，只有 docstring 会。

修法是让扫描器识别 docstring（Python 里是模块/类/函数体内的第一个字符串字面量）并跳过。
没有在 CQ-02 内修，因为它需要自己的 RED，而当时 EB-17 正处于活跃任务位。

## 遗留项

| 项 | 状态 | 归属 |
| --- | --- | --- |
| 真实 App（非 Harness）里的可访问性树扫描 | 未做，缺预置启动环境 | 随 EB-17 / CQ-04 的真实环境一起补 |
| 诊断导出包内容逐条脱敏断言 | 未做 | CQ-05 |
| 法务页本身 | ✅ 已闭合（2026-07-26），见 `FIX-third-party-software-notice-page.md` | — |
| 词表随新依赖同步的强制机制 | 未做 | CQ-05 |
| 原生扫描把**中文 docstring** 当用户文案（2026-07-26 实测撞上） | 未修，见下 | CQ-05 |
