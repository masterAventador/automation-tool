# C10-06 Demo 账号初始化与运维

- 状态：✅ 已完成
- 日期：2026-07-23
- 范围：复用 U9 认证运维 CLI 和 C10-05 文件 Secret，完成 Demo 账号创建、停用、恢复、密码重置及应急全产品 Session/全所属设备吊销；不增加运维 HTTP API、不新增角色系统、不接触真实客户账号
- 前置依赖：U9-06 账号/设备管理与 C10-05 运行时 Secret 已完成
- RED：先新增应急原子吊销 PostgreSQL 集成测试与容器运维契约，要求单一运维 capability、stdin Secret、expected revision、账号停用、credential version 前移、全 Session family/token、全所属 Installation/credential/device Session 同事务失效、逐设备 append-only 运维审计、外部账号不受影响和重复请求 fail closed；当前 CLI 没有 `emergency-revoke`，测试应准确失败
- GREEN：在既有 `automation-tool-account-operations` U9 运维入口新增 `emergency-revoke`，继续用单一 `atoc1` capability、固定 operations actor、stdin 私密输入、required request ID 与 optimistic expected revision；不新增 HTTP 管理面
- 原子吊销：同一 PostgreSQL 事务锁定 User 与其 active Installation，将账号设为 disabled、credential version/revision 各前移 1，吊销全部账号 Session family/token、所属 Installation、device credential 和 device Session；每台设备写一条 `device.revoked`，并写 `account.disabled`、`session.all_revoked`，统一 reason `operations_emergency_revoked`；外部账号和设备不受影响，空设备账号返回 0，未知、stale、重复和已停用请求 fail closed
- 最小权限：新增 `automation_tool_operations` 独立 PostgreSQL role，仅授予账号、Session、设备十张表的 `SELECT/INSERT/UPDATE`；无 superuser、建库/建角色、继承、复制、bypass RLS、DDL、DELETE、任务/RPA 数据或全库内建角色权限；连接上限 2。部署作业契约固定私网、单并发、UID/GID 65532、只读 rootfs、drop ALL、只读文件 Secret 和禁用自动重试
- 真实验收：`scripts/run_c10_06_acceptance.py` 构建当前生产镜像，在无宿主端口的临时私网 PostgreSQL 中应用迁移与最小权限 role，实际执行 create、重复 create 拒绝、disable、停用登录拒绝、restore、reset/recovery、新密码登录及 emergency-revoke；目标 2 台设备与全部账号/设备 Session 被吊销，foreign 设备保持 active，三类 operations 审计一致，并实测 operations role 可读账号但不能建表或读取 `tasks`
- Secret 与清理：数据库密码、运维 capability、初始/重置密码只经文件或 stdin；reset 的一次性 recovery token 是唯一允许的 Secret stdout，验收进程只在内存中立即消费且不落日志；镜像、container config 与日志无生成 Secret。真实验收自动清除任务专属容器、网络、named volumes 和镜像；未部署云资源、未创建真实客户账号
- 回归：C10-06 Node 契约 2/2；新增后端聚焦测试 19/19；前端契约 185/185；后端全量 2378 passed、5 skipped；`16062/16062` 语句与 `3714/3714` 分支 100%；Ruff、格式、Mypy 381 个源文件全绿
