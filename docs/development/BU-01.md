# BU-01 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-23
>
> 提交：本文件所在的 BU-01 任务提交

## RED

首次运行 `python3 scripts/run_bu_01_acceptance.py`，因
`contracts/browser-use/api-contract.v1.json` 尚不存在而按预期失败；验收没有读取
EB-02 的临时环境来掩盖生产契约缺口。

## GREEN

- 2026-07-23 重新核对 PyPI：最新稳定版仍是 2026-07-17 发布的 `0.13.6`；
  PyPI wheel SHA-256 已进入契约和 `uv.lock`。
- `tools/browser-use-contract` 使用 Python 3.12.13、精确依赖
  `browser-use==0.13.6` 和完整传递依赖锁，不污染现有 Control Plane/Playwright
  Executor 的 Pydantic、AnyIO、WebSockets 与冻结包。
- 契约固定 `Agent`、`BrowserSession`、`Tools`、`AgentHistoryList`、
  `AgentHistory`、`AgentOutput`、`ActionResult` 的公开导入、实际模块、关键参数和
  默认值；`Tools.action` 真实注册异步动作成功。
- 成功历史 fixture 经 `load_from_file → model_dump/save_to_file` 完整往返，
  `is_done/is_successful/final_result/urls/action_names/screenshots` 语义全部符合契约。
- 默认契约和错误版本、错误参数默认值、缺失 history fixture 三个拒绝样例均通过；
  独立质量门禁已加入 CI。

## 失败矩阵

- 包版本、Python minor、精确 requirement、锁文件 wheel 摘要任一漂移即拒绝。
- 公开导入移动、运行模块变化、关键参数消失或默认值变化即拒绝。
- history 字段/方法变化、fixture 不能读取或不能逐字段往返即拒绝。
- `Tools.action` 不能注册受控异步动作即拒绝。

## 正常用户路径验收

不适用。BU-01 只固定第三方 Python API 与序列化兼容边界，不新增用户入口。

## 真实边界

本任务不启动浏览器、不调用模型，也不宣称抖音发布可用。

## 清理

所有损坏契约和重序列化文件均位于自动删除的临时目录；本机 `.venv` 被
`.gitignore` 排除，不进入提交或安装包。

## 文档变化

新增 Browser Use API/历史契约、成功 fixture、独立 Python/依赖锁、检查器、
验收脚本和 CI Job；专项 Roadmap 将 BU-01 标记为完成。

## 遗留项

BU-02 才实现唯一 Chromium 的 `executable_path`/随机 CDP 双模式适配；BU-03
才裁剪 Tools，BU-05 才加入外部副作用安全门禁。本任务不提前宣称这些能力。
