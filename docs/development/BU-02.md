# BU-02 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-23
>
> 提交：本文件所在的 BU-02 任务提交

## 交付范围

- 新增双模式加固 harness `tools/browser-use-contract/browser_use_harness.py`
  （位于 BU-01 锁定的 browser-use==0.13.6 隔离环境工程内）：
  - 封闭两种启动形态：`IsolatedLaunchPlan`（已验证 `executable_path` + 全新空
    私有 Profile，路径不存在/非文件/无执行位/Profile 非空一律拒绝，杜绝发现与
    下载 fallback）与 `TakeoverLaunchPlan`（只接受
    `http://127.0.0.1:<port>` 精确形态的随机 loopback CDP；localhost 别名、
    0.0.0.0、外部地址、https/ws、带路径后缀全部拒绝）；
  - `create_session` 以固定关键字面构造 `BrowserSession`：`is_local=True`、
    `keep_alive=False`、`enable_default_extensions=False`、`captcha_solver=False`、
    `highlight_elements=False`；两种模式互斥注入，绝不混合；
  - `harness_environment`：启动环境强制 `BROWSER_USE_CLOUD_SYNC=false`、
    `ANONYMIZED_TELEMETRY=false` 并剥离 Cloud 凭据变量，Browser Use Cloud 不可达。
- 新增确定性测试 `scripts/test_browser_use_harness.py`（10 项，零网络零浏览器，
  会话工厂用注入的假 `browser_use` 模块断言加固关键字面）与验收入口
  `scripts/run_bu_02_acceptance.py`。

## RED

```bash
python3 scripts/test_browser_use_harness.py
# ModuleNotFoundError: No module named 'browser_use_harness'
```

实际运行并记录以上失败（10 用例全部无法收集），证明当时不存在双模式加固 harness。
实现后 ruff 暴露 1 处嵌套 if，修复后进 GREEN。验收脚本调试中依据真实运行修正两处
断言：CDP 目标标题存在惰性刷新（改为服务器端命中计数 + URL 精确匹配的更强证据）；
`--headless=new` 的 CDP 版本串前缀为 `Chrome/`（保留精确版本号断言）。

## GREEN

```bash
python scripts/test_browser_use_harness.py             # 10 passed
uvx ruff check --ignore RUF001 --config backend/pyproject.toml \
  tools/browser-use-contract/browser_use_harness.py \
  scripts/test_browser_use_harness.py scripts/run_bu_02_acceptance.py
python scripts/run_bu_02_acceptance.py                 # 原生目标双模式真实验收通过
uv run --project tools/browser-use-contract --locked python \
  scripts/check_browser_use_api_contract.py --self-test
pnpm --dir frontend test:contracts                     # 212/212
python scripts/check_embedded_browser_video_roadmap.py --self-test
```

`run_bu_02_acceptance.py` 真实链路（macOS arm64 本机，锁定 browser-use 0.13.6 环境）：

1. 用 EB-03 摘要锁定归档 + 生产暂存构建器现场暂存唯一 Chromium 149.0.7827.55；
2. 独立模式：harness 以 `executable_path` + 全新临时 Profile 无头启动，真实导航到
   本地随机端口 fixture 页面，URL 精确命中且服务器端计数确认真实请求；
3. 接管模式：同一暂存二进制以 `--remote-debugging-port=0` 独立进程启动，读取
   DevToolsActivePort 获得随机 loopback 端口，`/json/version` 实测版本精确等于
   149.0.7827.55，harness 经 `cdp_url` 接管并真实导航同一 fixture；
4. 两种模式各用独立 Profile；探针结束会话停止、进程树 terminate/kill、临时目录
   删除；全程无系统浏览器发现、无下载、无 Cloud 出站。

## 失败矩阵

- 可执行路径不存在、非文件、无执行位：计划构造即拒绝，不触碰 Browser Use；
- 已用过（非空）的 Profile 目录：拒绝，防止污染或复用运营数据；
- 非 loopback / localhost 别名 / 0.0.0.0 / 外部 IP / https / ws / 带路径的
  CDP 地址：拒绝；
- 未知计划类型：拒绝；
- Cloud 凭据出现在环境中：启动环境剥离；云同步与遥测强制关闭；
- `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY`/`NO_PROXY`（大小写任意）出现在宿主环境：
  启动环境全部剥离，本机 fixture/CDP 不受 SOCKS/HTTP 代理劫持，也不因可选代理依赖
  缺失而改变行为；
- CDP Chromium 提前退出、随机端口超时：探针显式失败并清理进程。

## 正常用户路径验收

不适用。BU-02 是 Browser Use 底座适配，不新增用户入口；抖音发布的用户路径由
PB-05～PB-08 验收。

## 真实边界

- macOS arm64 双模式真实验收已通过（真实暂存二进制、真实 CDP、真实导航）；
- 2026-07-24 Windows 11 x86_64 实体机会话使用 EB-04 锁定归档现场暂存 308 文件 /
  435,574,347 bytes；同一发行物先由生产 Rust `EmbeddedBrowserAuthority` 完成 Manifest
  与逐文件摘要验证（4.60 秒），再交给 Browser Use 0.13.6；
- Windows 独立模式以已验证 `chrome.exe` + 全新 Profile 导航随机 loopback fixture；
  接管模式由同一二进制使用 `--remote-debugging-port=0` 获得随机端口，CDP
  `/json/version` 精确返回 `Chrome/149.0.7827.55`，harness 接管后再次真实导航；
- Windows 首轮 RED 证明 POSIX execute-bit 判据不能验证 PE；现在 POSIX 继续要求执行
  位，Windows 固定要求 `.exe` 和 `MZ` 头，完整 PE/架构/摘要由前置 Rust Authority
  保证；
- Windows 第二轮 RED 证明宿主 SOCKS 代理会在浏览器启动前改变锁定环境；现在 Cloud、
  遥测、凭据及全部代理变量都由 `harness_environment` 封闭；
- 抖音接管模式的租约独占语义（暂停 Playwright 执行器、超时归还）属 BU-04。

## 清理

- 暂存树、两个探针 Profile、fixture 服务器全部临时创建并在验收结束删除/关闭；
- 浏览器进程随会话停止退出；Windows 外部 CDP 树使用本次创建的 PID 定向
  `taskkill /T` 兜底，无残留进程、Profile 或端口；
- 仓库不提交归档、暂存树或截图。

## 文档变化

Windows 补证把验收 runner 泛化到三个原生目标，新增 Windows 原生可执行判据、代理
隔离和进程树清理；更新本证据与 Windows 清单。专项 Roadmap 将 BU-02 从待验收改为
已完成，并同步计数（待验收 9→8、已完成 47→48）。

## 遗留项

BU-02 无遗留验收缺口。BU-03 已在本 harness 之上裁剪受限 Tools；BU-04 已建立
`BrowserSurfaceLease` 接管租约；BU-06 接入百炼模型，PB-05～PB-08 消费这些底座完成
抖音发布的正常用户路径。

## 2026-07-23 缺陷修复：钥匙串隔离

用户本机运行验收时，CDP 接管探针的裸启动 Chromium 触发 macOS「找不到用于存储
chromium 的钥匙串」系统弹窗——违反项目「不调用系统钥匙串」规则。修复：探针启动
参数与 EB-02 `chromium_args()` 统一补 `--use-mock-keychain`（Playwright 启动路径
本就默认注入该参数，故 EB-03/EB-05 探针不受影响）。修复后重跑
`run_bu_02_acceptance.py` 双模式验收通过且无系统弹窗。EB-07 接线时生产启动参数
必须包含同一隔离参数。
