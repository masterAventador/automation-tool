# P9-04 Windows Tauri 候选包

- 状态：🔍 待 EB-16 正式包与 Authenticode 验收（Windows 11 x86_64 普通候选实机已通过）
- 日期：2026-07-24
- Windows 状态复核：2026-07-25 当前用户与本机证书库的 `-CodeSigningCert` 数量均为 0；当前 P9-04 runner 只装配 `local-executor/package`，并且有意要求 installer/App/uninstaller 为 `NotSigned`，没有装配 `embedded-browser`。因此现有 GREEN 只能证明普通基础候选的生产构建、安装、权限、Executor 资源和卸载，不能证明现行内置 Chromium 首发包或正式签名
- 推进边界：Windows 普通候选已完成真实生产构建、安装、注册表、资源和卸载验收；本机没有 Authenticode 代码签名证书，因此只记录 `NotSigned` 普通包事实，不把它冒充正式 signer/chain/SmartScreen 证据
- RED：先把唯一台账置为 `🧪 RED` 并新增 P9-04 Node 契约，锁定最小 `currentUser` NSIS 覆盖、生产 Capability/CSP 继承、P9-02/Manifest/Resources 组合、无测试 Feature release、E4-15 审计、普通签名状态、非提权安装、HKCU/HKLM、卸载和清理边界；首跑准确失败于候选配置不存在
- 最小生产配置：`tauri.windows-candidate.conf.json` 只包含 `bundle.active=true`、`targets=["nsis"]` 和 `windows.nsis.installMode="currentUser"`。它不锁死 product/identifier/main binary，不覆盖可见生产窗口、`withGlobalTauri=false`、唯一 `main` Capability/空权限、CSP、updater passive 模式，也不写 `certificateThumbprint`/`signCommand` 或 updater artifact 开关
- Release 与 Resources：`pnpm --dir frontend test:p9-04-windows-package` 只允许非提权 Windows x86_64；从 P9-02 唯一 builder 生成隔离 Executor，以一次性随机 Ed25519 seed 签发/复验正式 Manifest，再映射到 `local-executor/package`。Windows 实机发现 Tauri 2.9.3 会丢弃绝对资源路径的盘符，现把同卷 `.local` 临时 Executor 严格转换为相对 `src-tauri` 的资源源路径；Tauri 只做无测试 Feature release NSIS，构建后二进制继续经过 E4-15 production assets/config/Cargo tree 审计
- 隔离安装矩阵：为避免触碰用户可能已有的正式安装，runner 只在临时生成配置中使用唯一 P9-04 product/identifier/main binary，预检 LocalAppData 与 HKCU/HKLM 均无同名事实后才声明所有权。PowerShell 参数改由 stdin JSON 传递，安装包路径含空格时仍严格读取 `Status/SignerCertificate/TimeStamperCertificate`；普通候选的 installer、built/installed App 和 uninstaller 都必须为 `NotSigned` 且无 signer/timestamp。NSIS 会把 release PE 内唯一 `BUNDLE_TYPE_VAR_UNK` 等长改为 `BUNDLE_TYPE_VAR_NSS` 后再装入，runner 按这唯一变换预计算整文件 SHA-256，拒绝除此之外的任何差异
- 注册表边界：安装根固定 LocalAppData，只允许唯一 HKCU 卸载记录并禁止 HKLM；显式在同一 `KEY_WOW64_64KEY/KEY_WOW64_32KEY` 视图打开父子键，精确核对 `DisplayName/DisplayVersion/InstallLocation/UninstallString`。Tauri v2.11.5 按模板给两个路径值写一层成对双引号，runner 只剥这一层，继续拒绝空值、非绝对路径、内嵌或不平衡引号
- Executor 与清理：安装后的 `local-executor/package` 再做 PE/依赖/路径/reparse 审计、Manifest 验签及源/安装逐文件大小与 SHA-256 比较；全过程不启动 App、浏览器、Control Plane、Docker 或真实账号，不写正式 product/AppData，不调用更新安装探针。成功或失败路径只对本轮已声明所有权的唯一隔离安装调用专属 `uninstall.exe /S`，要求安装根和 HKCU/HKLM 记录全部消失；临时 Executor、seed、生成配置、Cargo target、NSIS 与构建资产随临时目录删除
- 自动化门禁：2026-07-24 在 Windows 11 x86_64 非提权实体机会话执行 `pnpm --dir frontend test:p9-04-windows-package` GREEN；合并当日最新 `main` 后复验 E4-15 生产桌面审计通过，安装根共 369 个文件、177,616,688 bytes，内置 Executor 共 367 个文件、157,979,594 bytes，普通签名事实、NSIS 全文件 SHA、当前用户静默安装、HKCU-only/HKLM-zero、Manifest/资源清单和静默卸载均通过。Node 契约、Ruff format/check 与 `git diff --check` 同步全绿
- 清理证据：最终无缓存实机轮结束后，专属 LocalAppData 安装根不存在、`.local/automation-tool-p904-acceptance-*` 为 0、`automation-tool-p904-windows-acceptance` 进程为 0；中间诊断 Cargo target 也已删除。未触碰用户已有 Edge/WebView2、Node 或正式 Automation Tool 状态
- 待验收边界：正式发布仍需先由 EB-16 把 `windows-x86_64` 内置 Chromium 发行物装入只包含当前平台资源的 Windows 包，再提供 Authenticode 发布身份，在受控 Windows 设备复验 installer/App/uninstaller 的同 signer、证书链、RFC 3161 时间戳、SmartScreen、安装根内置 Chromium 清单与逐文件摘要、HKCU-only 安装和卸载边界；这些事实通过前保持 `🔍`，不能标记 `✅ 已完成`
- 无法在当前环境完成的原因：EB-16 尚未开始，现有普通候选没有 `embedded-browser`；两个证书库也没有 Code Signing certificate。`pnpm --dir frontend test:p9-04-windows-package` 会固定构建并接受 `NotSigned` 普通候选，不能把它重复运行后冒充正式签名验收
- 解锁条件：EB-16 生成只含 `windows-x86_64` Chromium 的正式候选，并由受控签名环境提供可验证的发布证书、私钥/签名服务和时间戳服务；随后必须用正式签名候选入口复验上述完整矩阵。满足这些条件前没有可在当前机器执行的等价命令
- 后续：P9-04 普通候选验收已被 P9-05 的内容审计复用；首发内置浏览器装配由 EB-16 承接，P9-04 保留最终 Windows 签名、安装与最小权限结论
