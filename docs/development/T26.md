# T26 从内置浏览器中剔除 Widevine

> 状态：✅ Widevine 剔除门禁已完成；剩余 Chrome for Testing 整体许可仍为明确的待法律确认边界
>
> 日期：2026-07-26
>
> 原提交：`d5e51114e96c0a99da5ded2dfcf101f1c96f14d4`
>
> 证据补记：第三批事后补录，未与原代码同提交

## 结论

任务没有把 Chrome for Testing 换成缺少 H.264/AAC 的 Chromium 开源构建，而是只剔除
许可证明确禁止未经单独协议再分发的 Widevine CDM。原始浏览器归档摘要保持不变，剔除发生在
受契约约束的 staging 阶段。

## RED

第二批记录的实际失败顺序是：

1. staging 契约缺少剔除声明：`'exclusions' not found`；
2. 许可门禁仍把目标记为 `pending`；
3. 发布预算仍是旧值：`343 != 324`。

这些是针对目标行为的断言失败，不是 ImportError 或文件缺失造成的顺带红。原终端 transcript
未入仓，故不补造运行时长和完整输出。

## GREEN

- macOS arm64 真实归档 staging：331 → 328 个文件，
  359,441,871 → 339,257,128 字节；
- 离线浏览器启动与篡改拒绝通过；
- Windows 契约的剔除列表为空，保持原行为；
- `test_embedded_chromium_staging.py`、`test_embedded_browser_distribution.py`、
  `test_embedded_browser_package.py` 与 `test_release_assembly.py` 均增加对应门禁；
- 第二批发布/分发相关回归记录为 staging 14/14、distribution 15/15、package 35/35、
  release assembly 38/38。

## 失败矩阵

| 场景 | 结果 |
| --- | --- |
| macOS staging 未剔除 Widevine | 分发清单和包门禁拒绝 |
| manifest 试图重新加入已排除路径 | `test_excluded_widevine_cannot_be_reintroduced_by_manifest` 拒绝 |
| 剔除前缀越出目标根 | 契约测试拒绝 |
| 许可状态仍为 pending | 包门禁拒绝 |
| Windows 不含该组件 | 空剔除列表，行为不变 |
| 浏览器归档被替换 | 原有归档 SHA-256 锁继续拒绝 |

## 真实边界

本任务真实处理了 macOS arm64 的锁定归档并启动浏览器，验证剔除不损失现有媒体能力。没有在
Windows 实体机上删除任何文件，因为对应 CfT 归档本来就没有 Widevine。这里也不把“剔除
Widevine”扩张成“Google Chrome for Testing 的所有再分发问题均已获得法律确认”。

## 清理

staging 和离线启动使用临时目录；未修改原始缓存归档，未下载新浏览器，未触碰用户浏览器
Profile。

## 文档与遗留

- 浏览器替换调研见 `docs/development/PLAN-chromium-replacement.md`；
- 原任务说明和摘要曾在第二批交接单中；
- 剩余 CfT 整体许可状态继续按契约中的真实结论处理，不能因 Widevine 已剔除而改写为已确认。
