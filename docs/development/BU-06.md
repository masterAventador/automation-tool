# BU-06 完成证据

> 状态：🔍 待验收
>
> 日期：2026-07-24
>
> 提交：本文件所在的 BU-06 任务提交

## 交付范围

- 新增锁定契约 `contracts/browser-use/bailian-model-catalog.v1.json`：封闭能力快照，
  锁定官方 OpenAI 兼容地址 `dashscope.aliyuncs.com/compatible-mode/v1`、地域
  cn-beijing、核对日期 2026-07-24，与三个模型的逐项能力
  （text/vision/function_calling/structured_output/context/dom_only）：
  - `qwen3.7-max-2026-06-08`：多模态（视觉默认模型）；
  - `glm-5.2`：文本 + Function Calling，DOM-only；
  - `deepseek-v4-pro`：纯文本，DOM-only。
- 新增网关 `tools/browser-use-contract/browser_use_bailian.py`（BU-01 锁定环境内）：
  - `load_bailian_model_catalog` 严格校验契约（deny-unknown 字段、能力自洽：
    text-only ⇔ dom_only_acceptance_required）；
  - `select_bailian_model`：视觉运行只准多模态模型，文本模型配视觉**阻止而非静默
    降级**；DOM-only 模型需调用方显式通过 DOM-only 真实页面验收标志；未登记 model
    id 拒绝（无通用菜单）；
  - `build_bailian_chat_model`：只从目录快照、只对锁定 base URL 构造真实 `ChatOpenAI`；
    api key 形状校验；
  - `redacted_model_descriptor`：见「真实边界」的上游缺陷缓解。
- 新增确定性测试 `scripts/test_browser_use_bailian.py`（11 项）与门控真实验收
  `scripts/run_bu_06_acceptance.py`。

## RED

```bash
uv run --project tools/browser-use-contract --locked python scripts/test_browser_use_bailian.py
# ModuleNotFoundError: No module named 'browser_use_bailian'
```

实际运行并记录以上失败（11 用例无法收集）。

## 真实边界发现的上游安全缺陷（外围适配缓解）

首轮 GREEN 时"api key 不进 repr"断言失败——**上游 `ChatOpenAI.__repr__` 明文打印
api_key**（browser-use 0.13.6 的已知行为，禁止修改上游源码）。按「上游问题用外围
适配」处置：新增 `redacted_model_descriptor`（只含 model_id/base_url/vision/
verified_at，永不含 key）作为日志/诊断中**唯一允许的模型表示**；生产代码约定绝不
repr/log 裸 `ChatOpenAI`。测试如实断言脱敏描述符无 key，并注释记录上游 repr 泄漏。

## GREEN

```bash
uv run --project tools/browser-use-contract --locked python scripts/test_browser_use_bailian.py  # 11 passed
uvx ruff check --ignore RUF001 tools/browser-use-contract/browser_use_bailian.py \
  scripts/test_browser_use_bailian.py scripts/run_bu_06_acceptance.py
python3 scripts/run_bu_06_acceptance.py   # 真实百炼调用（见下）
python3 scripts/check_embedded_browser_video_roadmap.py
python3 scripts/check_user_facing_branding.py
```

## 真实百炼验收（2026-07-24）

用户密钥（`.local/secrets/bailian-model.json`）走生产网关：
`build_bailian_chat_model` 构造真实 `ChatOpenAI`（锁定 base URL + 视觉默认模型）→
经它对百炼发起**一次真实 completion** 返回非空文本 → 装配进真实受限 `Agent`
（BU-03 tools + 域白名单 session），`agent.llm.model == qwen3.7-max-2026-06-08`。
能力核对（同日真实请求）：qwen3.7-max-2026-06-08 接受 image_url 并走视觉尺寸校验
（证明多模态激活）；deepseek-v4-pro 收到图片静默忽略只处理文本（证明纯文本，正是
需阻止其充当视觉模型的原因）。密钥不进任何输出。

## 失败矩阵

- 视觉运行选文本模型（glm-5.2/deepseek-v4-pro）→ 阻止，不降级为盲跑；
- DOM-only 模型未过 DOM-only 验收标志 → 拒绝；
- 未登记 model id → 拒绝（无通用菜单）；
- 非目录快照（裸字符串）构造 ChatOpenAI → 拒绝；
- 非法 api key 形状 → 拒绝；
- 契约字段缺失/多余/能力不自洽 → 加载即拒绝；
- 上游 repr 泄漏 key → 只用脱敏描述符，测试断言其无 key。

## 正常用户路径验收

部分：真实百炼调用与受限 Agent 装配已验证，但"用户从正式 App 选模型 → 抖音发布
真实视觉回合"的完整用户路径由 VF-05 设置页 + PB-05/PB-06 承接，故 `🔍 待验收`。

## 真实边界

- 视觉能力已用真实百炼请求核对（尺寸校验证明视觉管线激活）；带真实截图的完整
  视觉推理回合随 PB-05 真实发布页验收；
- 模型目录 alias 漂移：契约锁定解析后的 model id 与能力，不随 alias 自动换模型；
  "最新多模态 Max"若出新稳定版，需新任务重新核对后改锁；
- 上游 ChatOpenAI repr 泄漏 key 是上游缺陷，本任务以脱敏描述符 + "不 repr 裸对象"
  约定缓解，未改上游源码。

## 清理

- 真实验收单次 completion，无浏览器启动（session 仅装配不 start）；无残留；
  密钥仅在本机凭据文件，未进代码/日志/断言。

## 文档变化

新增本证据文件、契约、网关模块与两个脚本；专项 Roadmap 仅更新 BU-06 状态行与
进度计数（未开始 24→23、待验收 18→19）。

## 遗留项

- VF-05 设置页把百炼模型选择接入 `select_bailian_model` 能力校验（视觉/DOM-only
  提示）；
- PB-05/PB-06：抖音发布真实视觉回合（真实截图）与本网关串联；
- DeepSeek/GLM 的 DOM-only 真实页面验收（开放前置）随 PB-05 补。
