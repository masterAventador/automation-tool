# H8-16 规格复审

- 状态：✅ 已完成
- 日期：2026-07-21
- 提交：本记录、可执行 MVP 规格复审和 H8-16A～H8-16F 修复任务属于单一 `docs: 完成 MVP 规格复审` 提交；完成后立即推送 `main`
- RED：先把唯一台账置为 `🧪 RED`，新增 Node 契约准确失败于 `contracts/quality/mvp-spec-review.v1.json` 不存在；规格登记建立后持续逐项读取产品规划 4.5 节原文、路线图修复任务和源码证据锚点，不能用模块数量、覆盖率或旧隐藏 App 分层验收代替真实用户旅程
- 决策复核：RPA→内容→AI、Tauri-only、P9 无产品登录/U9 客户 Demo 前置、Control Plane 独立部署、Local Executor 永远本机、外部可见浏览器、独立 Profile、App 私有文件而非系统钥匙串、PostgreSQL/SQLite 权威边界以及例行测试全局无头共 10 项均与产品、前端、后端和当前实现一致，无需恢复旧 Web、AI 中台、默认浏览器 Cookie 或本机业务后端方案
- 验收结论：产品规划 14 条 MVP 验收中，打开即用、四类任务控制、人工接管、三进程恢复、不确定不重试和隐私共 6 条已有自动化证据；登录态跨真实 App 重启 1 条继续归 B5-15；双平台干净安装与正式包审计 2 条归 P9；环境自动诊断、任务触发登录/发现、真实目标预览入口、确认后动作执行和目标级最终证据 5 条存在正式组合缺口
- 关键断点：Rust 已有固定 `start_task_discovery`，但生产 `main.tsx`/`TaskRunDetails` 没有 discovery gateway 或按钮；目标确认只把 Task 改为 `queued`，应用组合根没有逐目标 ActionAuthorization 与 typed Outbox；正式 `ExecutorCommandProcessor` 对 `task.offer` 仍调用 `_success_batch`，没有接入 A7-10/A7-11/A7-12；`uq_execution_attempts_one_active_task` 当前索引 `task_id`，没有兑现 Installation 单活；Startup Gate 只组合 Control Plane 检查，Executor/受信浏览器/App 私有数据目录仍需分散查看
- 修复顺序：H8-16A 接桌面发现入口，H8-16B 修 Installation 单活并发，H8-16C 建服务端逐目标授权/投递，H8-16D 接正式 Executor 动作编排，H8-16E 合并启动环境诊断，H8-16F 校准可选过滤/用户黑名单、截图直览、结果导出和存储控制的当前/后置范围并跑通隐藏 App 整条用户旅程；H8-17 改为依赖 H8-16F，禁止绕过缺口直接宣称代码质量收口
- 范围：本任务只做只读审计、质量登记和台账拆解，没有修改运行时代码、数据库、OpenAPI、Executor wire、Tauri Command、页面行为或平台状态；没有启动 App、浏览器、服务、容器或访问真实账号
- 门禁：Frontend 118 项 Node 契约、ESLint 与严格 TypeScript 全绿；规格契约单独 1/1 通过，JSON 证据路径/锚点和六个修复任务逐项校验。未修改 Python/Rust/运行时资产，因此不伪跑无关纵向验收充数
- 后续：进入 `H8-16A`，把已有发现 Command 接到正式任务详情，并由唯一隐藏 App 从创建后的真实页面启动发现；真实抖音账号不可用时继续使用官方-origin 隔离页，不把它冒充 D6-16
