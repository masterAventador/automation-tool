# C10-07 App Demo Profile

- 状态：✅ 已完成
- 日期：2026-07-23
- 范围：把 F1-09 local/demo URL Schema 落到真实 Rust Transport 和构建信任边界；Demo Profile 的 baseUrl、允许域名和 profile ID 必须经 Ed25519 签名，账号 Session、设备身份/凭据及 App 私有业务数据与 local 隔离；不部署云端、不切换真实用户 Profile、不修改专项 Roadmap
- 前置依赖：F1-09、C10-04、C10-06 已完成
- RED：先新增 Node 架构契约与 Rust 行为测试，要求编译期签名 Profile、精确 HTTPS origin/allowlist、无运行时环境覆盖、local/demo 私有根不重叠和签名/载荷篡改 fail closed；当前 `deployment_profile` 原生模块、签名验收和 `ControlPlaneClient::for_deployment_profile` 均不存在，测试应准确失败
- GREEN：新增原生 `DeploymentProfile` 信任边界；无编译配置时只返回固定 local `http://127.0.0.1:8765`，Demo 必须提供 canonical JSON payload、Ed25519 signature 与独立 build trust key，build.rs 先验证再写入只读编译常量，运行时再次 `verify_strict`，不读取运行时环境、文件或 WebView 输入
- Profile 合约：签名内容固定 `customer-demo-profile.v1`、`profile=demo`、安全 `demo-*` profile ID、精确无凭据/端口/path/query/fragment 的 HTTPS root origin 与排序去重 allowlist；拒绝 HTTP、显式 443、相似/后缀域名、userinfo、路径、非 canonical JSON/base64url、弱 key、错误签名、未知字段及 payload/signature/key 部分缺失；错误不反射配置
- Transport：生产 Tauri setup 只通过 `ControlPlaneClient::for_deployment_profile` 建立客户端；local 继续固定 HTTP/WS loopback，Demo 从已验证 Profile 派生精确 HTTPS Control Plane 与同 host WSS Executor endpoint，继续禁用 redirect、proxy 与任意 URL 输入；React 不接收 baseUrl、allowlist、signature 或 trust key
- 数据隔离：local 保持现有 App Data 根以兼容未发布 MVP；Demo 使用 `<AppData>/profiles/<signed-profile-id>` 私有 `0700` 根，账号 Session、设备 identity/credential、浏览器 Profile/设置、Executor ledger、诊断与更新状态全部从该根初始化；拒绝 relative、文件或 symlink namespace，local/demo 固定文件名也不会重叠，不迁移或复用开发期数据
- 编译态验收：`scripts/run_c10_07_acceptance.py` 使用仅限测试的临时 Ed25519 seed 生成 `api.automation-tool.test` Profile，实际触发 build.rs 和 Rust test 编译，证明签名 Demo 被嵌入、篡改签名导致构建失败，随后清除配置重新构建并恢复 local；测试 material 不是生产 key，未创建真实客户 Profile 或发起网络请求
- 回归：C10-07 Node 契约 2/2；Profile Rust 5/5；真实编译态验收通过；Frontend Node 187/187、Vitest 253/253、ESLint、TypeScript、OpenAPI、默认/customer-demo 构建与生产边界全绿；Rust all-targets/all-features 209 passed、6 ignored，Clippy `-D warnings` 与 Rustfmt 全绿
- 清理与边界：测试临时数据根全部删除，Cargo/Vite 只留下 Git ignore 的常规构建缓存；没有修改云资源、DNS、TLS、真实账号、真实 App Data 或专项 Roadmap，C10-08 才执行云端部署
