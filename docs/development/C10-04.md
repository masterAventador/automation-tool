# C10-04 HTTPS/域名

- 状态：✅ 已完成
- 日期：2026-07-23
- 范围：提供域名构建期校验、TLS 1.2+、唯一 Control Plane 上游、请求体/连接/读取超时、HTTP 限流、安全响应头和 WebSocket/SSE 兼容反代；本任务不申请真实证书、不修改 DNS、不开放公网
- 前置依赖：C10-02 锁定 Control Plane 镜像已完成
- RED：先新增 HTTPS ingress 静态契约和真实 TLS→Ingress→Control Plane→PostgreSQL 验收，要求镜像/base digest 固定、域名非法时构建失败、证书只运行时只读挂载、HTTP 不提供业务明文入口、1 MiB 请求上限、固定超时/限流/安全头、未知 Host 拒绝、SSE/WebSocket 必需 header 透传；当前 ingress 资产和 C10-04 验收不存在，测试应准确失败
- GREEN：新增 digest 固定的双阶段 Ingress 镜像，构建期只接受 canonical `api.<domain>`；Nginx 以 `101:101`、只读 rootfs、零 Linux capability 运行，证书只从固定只读 Secret mount 读取，HTTP 仅作固定域名 308 跳转，HTTPS 固定反代私网 `control-plane:8000`
- 请求边界：仅 TLS 1.2/1.3，未知 Host 返回 421；body 上限 1 MiB，客户端 header/body 10 秒、上游连接 5 秒、发送/读取 60 秒；每来源 10 r/s、burst 20、并发 20，超限统一 429；SSE/WebSocket 保留 HTTP/1.1、Upgrade 并关闭响应 buffering
- 安全输出：固定 HSTS、nosniff、DENY frame、no-referrer、Permissions Policy 与 CSP；响应只暴露无版本 `nginx`，access log 不记录 URI/query/header/body，证书私钥、数据库 URL 和密码不进入镜像、环境变量或日志
- 真实验收：在无宿主机端口的隔离 Docker 网络中完成临时 CA/证书、PostgreSQL 迁移、真实 Control Plane 和 Ingress 纵向验证；结果为 HTTPS 200、HTTP 308、未知 Host 421、1 MiB 超限 413、TLS 1.1 拒绝，并发 64 次中 44 次返回 429；非法域名镜像构建失败
- 回归：`node --test frontend/tests/https-ingress-boundary.test.mjs` 3/3；`pnpm --dir frontend test:c10-04-https` 通过；`pnpm --dir frontend test:contracts` 180/180；`uv run --locked pytest` 2344 passed、5 skipped
- 清理：验收生成的临时 CA、私钥、env、探针、容器、网络、Secret volume 和两个任务镜像均按唯一名称清除；未申请真实证书、未修改 DNS、未开放公网、未执行部署
