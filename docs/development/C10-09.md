# C10-09 云端协议回归

- 状态：✅ 已完成
- 日期：2026-07-23
- 范围：让 local 与签名 Demo Profile 在同一 App 业务代码上回放同一 OpenAPI、Control Plane JSON fixtures 和 Executor fixtures，配置差异只能是编译期 Deployment Profile；不连接未指定的真实云环境、不修改专项 Roadmap
- 前置依赖：C10-08 已完成
- RED：先固定协议回归机器契约，要求 App REST allowlist 与 OpenAPI 全量一致、health/version fixture 同时由后端模型和 Rust parser 消费、Executor fixtures 继续三语言回放，并在 local/demo 两种编译配置运行完全相同的 App 协议测试；当前这些跨 Profile 约束与统一验收入口不存在，测试应准确失败
- GREEN：
  - 新增 `customer-demo-protocol-regression.v1` 机器契约，固定 local/demo 两种 Profile 共用的 OpenAPI、生成 DTO、原生 Transport、Control Plane fixtures、Executor Schema/fixtures；允许差异只有编译期 Deployment Profile，业务代码差异必须为空。
  - 新增共享 `health.json` / `version.json`；后端 Pydantic response model 与 Rust Control Plane parser 同时消费，并验证版本矩阵一致。
  - Rust App 的 34 个固定 REST operation 现在按 method/path/operationId 全量对照提交的 OpenAPI 快照，防止 App allowlist 少接口、多接口或路径漂移。
  - 新增 `scripts/run_c10_09_acceptance.py`：先回放后端 OpenAPI/JSON fixture、Python/TypeScript/Rust Executor fixture 与生成 DTO 漂移检查，再仅以三个签名 Profile 编译变量为差异，在 local/demo 下执行完全相同的 Rust App 协议测试；执行前后重新计算全部协议语料 SHA-256。
- 验证：
  - C10-09 Node 契约 2/2 通过。
  - Ruff 与 Mypy 通过，Rustfmt 无漂移。
  - C10-09 跨语言验收通过：共享协议回放 120 项；local/demo App 各回放 36 项且数量一致；`businessCodeDifferences=[]`，语料 digest `e3717edc947250dab0ca06665c5c9129a9231f0c26d6e1966b19d7099ef8f0c1`。
  - 全量回归：Backend 2379 passed / 5 skipped；Frontend Node 191/191、Vitest 253/253、ESLint、TypeScript、build 与 OpenAPI drift 全绿；Rust all-targets/all-features、Clippy `-D warnings` 与 Rustfmt 全绿。
- 边界：此任务验证同一 App 协议语料和编译 Profile 差异，不把隔离回放描述成真实公网云环境；C10-08 已独立覆盖同一部署清单的真实 HTTPS 容器链路。
