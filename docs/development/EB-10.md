# EB-10 完成证据

> 状态：✅ 已完成
>
> 日期：2026-07-24
>
> 提交：本文件所在的 EB-10 任务提交

## 交付范围

按"先搜索所有引用，再删除"的删除规范移除生产浏览器选择链路：

- **React**：删除 `BrowserSettings` 设置面板组件及其测试；App 设置区、工作台
  诊断区两处装配移除；`PlatformAdapter` 接口删除 `getBrowserSettings`/
  `selectBrowser` 与 `BrowserSettingsSnapshot`/`SupportedBrowserId` 类型；Tauri
  适配器删除对应方法、解析器与孤儿常量；相关测试的 mock 与用例同步清理。
- **Rust**：删除 `get_browser_settings`/`select_browser` 两个 Tauri Command、
  `BrowserSettingsCommandError`、`map_browser_settings_error`、三处
  invoke_handler 注册与 `BrowserSettingsService` 的受管 State 注册。生产运行时
  不再构造浏览器选择服务；`browser_settings`/`browser_discovery` 模块本体按
  台账映射表要求保留（实现与历史证据不删除），其集成测试继续守护，但**不存在
  任何可达的用户入口或生产消费者**（EB-07 起路径源已是内置发行物 Authority）。
- **桌面 E2E 同步**：三个平台会话类 spec 删除 `select_browser` 前置调用（内置
  链路无需选择）；startup-environment spec 的封锁文案期望从"尚未选择受信运营
  浏览器"更新为组件语义（浏览器组件缺失/损坏）。

## RED

```bash
pnpm --dir frontend exec vitest run src/app/App.test.tsx
# 1 failed：修复工具仍渲染"尚未选择运营浏览器"面板（缺席断言先行）
```

实际运行并记录以上失败后开始删除。删除过程中的两处真实破损（lib.rs 结构体注解
残留、e2e spec 空包裹）均由编译/构建门禁当场暴露并修复，未带病提交。

## GREEN

```bash
cargo build --locked && cargo test --locked      # crate 全量零失败
cargo fmt --manifest-path frontend/src-tauri/Cargo.toml -- --check
pnpm --dir frontend build                        # 含 e2e-tauri TS 编译
pnpm --dir frontend exec vitest run src/         # 48 文件 307 用例全过
pnpm --dir frontend typecheck && pnpm --dir frontend lint
uv run --project backend --locked python scripts/run_h8_16e_acceptance.py  # Windows Tauri 1/1
pnpm --dir frontend test:contracts               # 212/212
python scripts/check_embedded_browser_video_roadmap.py --self-test
python scripts/check_user_facing_branding.py
```

grep 复查：`get_browser_settings`/`select_browser`/`getBrowserSettings`/
`selectBrowser`/`BrowserSettingsSnapshot` 在 src、e2e、e2e-tauri 全部零生产引用。

## 失败矩阵

- 设置页与诊断页不再渲染任何浏览器选择控件（App 级缺席断言）；
- WebView 无法再调用已删除的两个 Command（invoke_handler 三个构型均未注册）；
- 生产启动不再构造浏览器选择服务（manage 移除）；
- 平台命令与启动健康完全走 Authority（EB-07/EB-08 既有失败矩阵持续生效）；
- 保留模块若被重新接入生产入口，本任务的缺席断言与 grep 清单将在评审中暴露。

## 正常用户路径验收

2026-07-24 在 Windows 11 x86_64 实体机会话从隐藏真实 Tauri App 的正式启动门禁
进入“打开本地修复工具”，验证页面只显示执行器诊断，不存在
`.browser-settings-card`，正文不含 Google Chrome、Microsoft Edge、保存浏览器选择或
当前选择；重新检查仍保持“浏览器组件缺失”，只提示重装官方客户端且无需安装其他
浏览器。正式 IPC 同时返回
`appData=ready / executor=ready / embeddedBrowser=component_missing`。

WebdriverIO 通过系统 WebView2/Edge 驱动操作 App WebView（1 passing，4.8 秒），它不是
运营浏览器选择或 fallback；App 没有启动、发现或选择系统 Chrome/Edge 作为运营浏览器。

## 真实边界

- Windows 隐藏真实 App、签名测试 Executor、正式启动聚合 IPC、设置/诊断正常入口和
  WebView DOM 缺席断言已通过；App、Executor、AppData 和两个隔离端口均由 runner
  清理；
- macOS 原实现阶段已通过完整 Rust/React 确定性回归；
- `browser_discovery`/`browser_settings` 模块保留为历史证据与测试资产，后续若
  正式退役再立独立清理任务；它们没有 Tauri Command、State、React Adapter 或生产
  消费者；
- EB-11/EB-17 的扫码、Session 与干净机纵向路径会继续验证内置 Chromium，但不是本
  删除任务的遗留验收条件。

## 清理

- 纯删除与测试调整，无浏览器启动、无临时资源；grep 复查无残留生产引用。

## 文档变化

Windows 补证复用正式启动环境 runner，更新本证据与 Windows 清单；专项 Roadmap 将
EB-10 从待验收改为已完成，并同步计数（待验收 10→9、已完成 46→47）。

## 遗留项

EB-10 无遗留验收缺口。EB-11 接入扫码/Session 时继续使用内置 Chromium，并复核
StartupGate 文案；EB-17 负责无系统浏览器干净机纵向验收。
