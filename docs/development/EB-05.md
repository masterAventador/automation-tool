# EB-05 完成证据

> 状态：🔍 待验收
>
> 日期：2026-07-23
>
> 提交：本文件所在的 EB-05 任务提交

## 交付范围

- 新增单一发行物 Manifest 构建器与校验器
  `scripts/build_embedded_browser_distribution.py`：
  - `build_distribution_manifest` 把一棵 EB-03 暂存树提升为
    `distribution-manifest.v1.json`：聚合首发安装包作为单一兼容单元交付的全部
    锁定版本（Playwright Python 1.61.0、Chromium 149.0.7827.55/修订 1228、
    Browser Use harness 0.13.6、渲染引擎 0.7.68），四份契约（暂存/兼容性/
    Browser Use API/共享验证）交叉一致性校验，任一版本在契约间不一致即拒绝；
  - Manifest 携带逐文件 SHA-256/大小/可执行位/symlink 目标清单、可执行文件、
    归档来源与摘要、许可证声明（Chrome for Testing：Chromium 源码 BSD-3-Clause，
    成品含 Google 许可组件，**再分发法务确认显式登记为 pending，EB-16 首发包
    前必须闭合**）与 CycloneDX 风格 SBOM 组件（含 purl 与归档哈希）；
  - `verify_distribution` 逐文件重算摘要复验：篡改、缺失、多余文件、额外浏览器
    二进制（headless shell 等）、平台错配、契约版本漂移、归档摘要与契约锁不符
    全部固定消息拒绝；`enforce_archive_lock=False` 仅用于合成树单元测试，默认
    模式有专门用例证明会拒绝合成归档。
- 新增确定性测试 `scripts/test_embedded_browser_distribution.py`（10 项，零网络）
  与验收入口 `scripts/run_eb_05_acceptance.py`。

## RED

```bash
python3 scripts/test_embedded_browser_distribution.py
# ModuleNotFoundError: No module named 'build_embedded_browser_distribution'
```

实际运行并记录以上失败（9 用例全部无法收集）。实现后首轮暴露真实设计问题：
合成树单测会命中契约归档锁 → 引入显式 `enforce_archive_lock` 参数并补
「默认模式必须拒绝合成归档」用例；期间一处用例被批量替换误加参数导致假失败，
按真实输出修正。ruff 1 处 import 排序自动修复。

## GREEN

```bash
python3 scripts/test_embedded_browser_distribution.py      # 10 passed
uvx ruff check --ignore RUF001 --config backend/pyproject.toml \
  scripts/build_embedded_browser_distribution.py \
  scripts/test_embedded_browser_distribution.py scripts/run_eb_05_acceptance.py
python3 scripts/run_eb_05_acceptance.py                    # 真实链路通过（见下）
python3 scripts/check_embedded_browser_video_roadmap.py
```

`run_eb_05_acceptance.py` 真实链路（macOS arm64 本机）：摘要锁定归档现场暂存 →
默认强锁模式生成发行物 Manifest → 331 个文件全量逐一重算 SHA-256 复验（约 359MB）
→ 故意篡改 Info.plist 后复验被拒绝 → 恢复后清理临时树。

## 失败矩阵

- 契约间版本漂移（兼容性/暂存/Browser Use/共享验证四方交叉）：构建与校验双拒；
- 归档摘要与契约锁不符：默认模式构建与校验双拒（合成树专用开关有独立用例）；
- 篡改文件（内容变化）、缺失文件、多余文件、额外浏览器二进制：逐文件复验拒绝；
- symlink 目标漂移、可执行文件缺失或无执行位：拒绝；
- 平台/目标错配、未知目标、Manifest 形状损坏：拒绝；
- 全部拒绝路径为固定消息，不反射树内容。

## 正常用户路径验收

不适用。EB-05 是构建期供应链任务，不新增用户入口；Manifest 的运行时消费方是
EB-06（Rust 解析与验证）。

## 真实边界

- macOS arm64 真实链路已通过（真实暂存树全量摘要复验 + 篡改拒绝）；
- **Windows x86_64 条目待补**：按 2026-07-23 用户决策等待 Windows 环境（EB-04
  暂存完成后，本 Manifest 直接以同一构建器纳入 windows 目标，无需改结构），
  故本任务保持 `🔍 待验收`；
- **Chrome for Testing 再分发法务确认待办**：Manifest 中
  `redistribution_review: pending` 为显式登记，EB-16 首发包门禁前必须闭合，
  不得静默转正；
- SBOM 当前覆盖浏览器发行物组件；两个 vendor submodule 的源码 SBOM 已由
  `third_party/source-submodules.cdx.json` 单独维护，不重复。

## 清理

- 单测与验收全部使用临时目录，结束自动删除；无网络、无浏览器进程、无残留。

## 文档变化

新增本证据文件与三个脚本；专项 Roadmap 仅更新 EB-05 状态行与进度计数
（未开始 36→35、待验收 10→11），未修改旧 `docs/development-roadmap.md`。

## 遗留项

- EB-04 完成后把 windows-x86_64 暂存纳入同一 Manifest 并复验（结构已就绪）；
- Chrome for Testing 再分发权的法务确认（EB-16 前置门禁，已在 Manifest 显式
  登记 pending）；
- EB-06 消费本 Manifest 做 Rust 侧发行物解析与运行时验证。
