# PC-03 — 第一批槽位表（锚定这一半）

> 状态：**实现中**
> 日期：2026-07-27
> 分支：`feature-audit`
> 提交：见本文件所属提交

用户可操作：否
证据类型：分层实现

---

## 0. 这一轮做完了什么、还差什么

**做完**：冻结 `contracts/video/motion-part-slots.v1.json`——18 个零件、48 个槽，
每个槽是「文本节点序号 + 原文」双锚，由 `check_motion_part_slots.py` 从**发布树**逐条重算比对。

**还差**（PC-03 保持 `🚧 实现中`）：每槽的角色标注、像素预算探针、以及运行期把文案写进
RenderJob 工作区副本的那一段。最后一项同时是 PC-13 的收口点（字体 CSS 也在那里注入）。

## 1. 一个会让所有槽都错位的坑：锚必须取自发布树

每个零件在这个项目里**有两份**：

```
vendor/hyperframes/…/spotify-card.html            上游原样，只读 submodule
   ↓ BM-12 离线化：删掉指向 Google 的 <link>，依赖改本地路径
   ↓ BM-13 overlay：品牌词替换
.local/motion-catalog-release/1.0.0/items/…        发布树，这份才进安装包
   ↓ 出片时复制进 RenderJob 工作区
工作区副本                                          替换发生在这里
```

替换发生在最下面那份，所以锚必须取自它。实测 `spotify-card`，**两个锚同时对不上**：

```
submodule   idx=24 'HyperFrames'   idx=26 'HeyGen'       idx=36 'Spotify'
发布树       idx=22 '动效画布'      idx=24 '人物创作平台'   idx=34 '音频平台'
```

文字不同是因为 overlay 换掉了品牌词；序号从 24 移到 22 是因为离线化删掉了前面的
Google Fonts `<link>`。**从 submodule 建表的话，70 个被 overlay 碰过的零件全部锚错——
而且不是报错，是把文案写进错误的节点，下游没有任何门禁看得见。**

代价：这道门禁需要发布树存在（`.local` 下的构建产物）。与
`check_motion_catalog_release.py` 同一类，那道门禁没有构建产物时同样直接红。
发布树是确定的——它的聚合摘要锁在 `motion-catalog-release.v1.json` 里。

## 2. 筛选：第一遍不算筛选

36 个零件 419 个可见节点。第一遍我只按名字排除了 5 个整屏界面仿制件，剩下 116 个全收——
**那不是 curated，正是我自己在 PC-02 里警告过的毛病。** 把 116 条逐条读完，找出四类不能替换的：

| 类 | 例 | 为什么 |
| --- | --- | --- |
| **许可署名** | `© OpenStreetMap contributors © CARTO`（2 个零件） | 让模型改掉它就是违反地图数据许可 |
| **自我宣传的演示页** | `vfx-magnetic`「Pixels bend toward your cursor」「No html2canvas」、`vfx-liquid-glass`「Ship videos 10x faster」 | 讲的是这个技术本身，和可用性分级已经排除的 30 个转场演示页同一类，第一遍漏判 |
| **界面家具** | `liquid-glass-context-menu` 的 Share / New Tab / ⌘T；`Follow`↔`Following`、`Subscribe`↔`Subscribed` | 后两者是按钮的两个状态，动效正是从前者变后者，替换会弄坏这一镜要展示的东西 |
| **槽还表达不了的结构** | `news-ticker` 把一句标题按强调切成三个节点（27/28/29），又把四条跑马灯文案各镜像一份（40..44 与 50..54） | 需要槽位分组，而分组还没设计。本轮整件排除 |

排除项**按零件名和按原文逐条写明理由**，不用节点数阈值——阈值是判断的代用品，不是判断。

结果 18 个零件 48 个槽，构成：

```
10 个下三分之一   姓名 + 身份
 3 个社交关注卡   昵称 + 账号 + 粉丝数
 1 个通知卡       应用名 + 标题 + 正文（原文本身就写着 Prompt to replace this title）
 1 个音乐卡       3 个品牌槽
 1 个收尾卡       标语 + 网址
 2 个地图件       城市 / 机场代码 / 状态词 / 标语
```

## 3. RED

```
$ python3 scripts/test_motion_part_slots.py
AssertionError: scripts/check_motion_part_slots.py is missing
```

## 4. GREEN

```
$ backend/.venv/bin/python scripts/check_motion_part_slots.py --write
motion part slots written: … ({'parts': 18, 'slots': 48})

$ backend/.venv/bin/python scripts/check_motion_part_slots.py
motion part slots are valid: 18 parts / 48 slots, every anchor re-derived from the release tree

$ backend/.venv/bin/python scripts/test_motion_part_slots.py
motion part slot tests passed: 18 parts / 48 slots
executed checks: 6
```

门禁需要 backend venv（`automation_tool/__init__.py` 会查已安装包的版本），
与 `scripts/test_motion_authoring_agent.py` 同一约束。

## 5. 失败矩阵

| 篡改 | 期望 | 实测 |
| --- | --- | --- |
| 某槽的 `original` 与发布树不符 | 拒绝 | 红 |
| 某槽的 `index` 指向另一个节点 | 拒绝 | 红 |
| `parentTag` 漂移 | 拒绝 | 红 |
| `counts` 与实际不符 | 拒绝 | 红 |
| 出现目录里没有的零件 | 拒绝 | 红 |
| 给 `deferred` 零件手工加槽位表 | 拒绝 | 红 |

另有两条在校验侧失败关闭但未单独造例：给布景零件加槽、给固定文本（署名、按钮状态）加槽。

## 6. 一处刻意的选择

**枚举器用的是随 Executor 打包的 `part_document.py`，不是门禁自己写一份。**
槽位表说的是「第 N 个文本节点」，这句话只有在**冻结时数节点的东西和渲染时数节点的东西
是同一份代码**时才成立。两份今天一致的实现是一次等待首次分歧的错位替换。

## 7. 清理

无临时文件、无后台进程。`--write` 只写一个新契约。

## 文档

- `docs/product-completion-roadmap.md`：PC-03 状态与剩余项；
- 本文件为 PC-03 的唯一证据文件，不回写路线图。
