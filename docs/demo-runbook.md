# 客户演示当天 Runbook

> 用途：演示当天拿着它照做。每一步写清**说什么、点什么、用户看到什么、要多久**，以及**这一步挂了怎么办**。
>
> 配套文件：`docs/demo-preflight-checklist.md`（演示前逐项检查）。本文件假设那份清单已经全绿——**没跑完清单就别照这份 runbook 开演。**
>
> 初版 2026-07-26；07-27 随清单一起重核（T70）；**07-27 清晨再次随清单重核（T113）**。
>
> 两轮之间又合入了七笔改产品行为的改动。**对本文件影响最大的是编排提速**：一句话生成比原来快了一多半，于是「等待期讲什么」这一幕的时间编排、以及「怎么这么慢」那段台词的依据，**都已经不成立**。
>
> 变了的地方用 `〔07-27 改〕`（T70 那轮）和 `〔07-27 二轮〕`（本轮）标出，并写清原来是什么。

---

## 0. 一页纸速查

| 项 | 值 |
| --- | --- |
| 演示形态 | **品牌动效成片**：一句话 → 一段 12 秒会动的 mp4 |
| 全程时长 | 约 **10–13 分钟**（含 1–3 分钟生成等待，**下限变宽了，见下一行**） |
| 唯一的长等待 | 一句话生成。**这个数刚刚变过**：编排那段快了一多半，估计**约 70 秒**就能出片，但**没在正式 App 上量过**。屏幕上仍然会写「通常 2 分钟左右，最长约 3 分钟」（界面里写死的，没跟着改） |
| 对客户怎么说 | 仍然**「大概三分钟」**。心里按 **4.5 分钟**准备。**说少了讲不完是尴尬，提前做完是惊喜——留余量的方向不要反过来** |
| ⚠️ 要有心理准备 | **它可能一分钟出头就好了**，比你准备的讲解短。幕 2 的内容按「随时可能被打断」组织，见那一节 |
| 演示机实测耗时 | ⬜ **_____ 分 _____ 秒**（彩排时填，见清单 B8）。**这一格现在比以前更重要**——连开发机的基线都是推算的 |
| 兜底素材 | 彩排时生成的那条成片，留在「视频制作 → 成片」页 |
| 出事找谁 | ⬜ **_____________**（填现场技术支持的联系方式） |

**三条铁律**

1. **不要临场改任何设置。** 演示机上的每一项配置都是前一天验过的，改了就没人知道还成不成立。
2. **不要点 §4 列出的任何禁区功能。**
3. **卡住超过 30 秒就走降级路径（§5），不要在客户面前 debug。**

〔07-27 改〕耗时那两行原来写的是「约 3 分 34 秒」，并把它当成典型值。**那是单次最坏样本**，而且当时跑的是 debug 构建的 `control-plane-e2e`，不是这条演示链路。七次连跑的真实分布是 85 / 124 / 178 秒（最小 / 中位 / 最大），所以全程时长跟着从 12–15 分钟收到 11–14 分钟。新增「对客户怎么说」一行，因为**讲出去的数字和内部基线不该是同一个**——内部要记中位数，对外要留余量。

〔07-27 二轮〕**又变了，这次是变快**。生成画面的方式改了：以前整套画面是模型一个字一个字写出来的，现在模型只填几个字段、画面由本机按模板画，模型要吐的字少了三分之二。代理层实测 28 次真调模型：**编排的模型耗时中位数从 87 秒降到 37 秒**，最坏从 117 降到 57，一次通过率还从 9/11 变成 10/10。
> **但两件事要分清**：① 端到端的「约 70 秒」是**算出来的**，没在正式 App 上量过；② **屏幕上那句「通常 2 分钟左右，最长约 3 分钟」是写死的，没跟着改**——它现在偏保守，但没说谎。
> 所以对客户的口径**不变**（仍按 3 分钟讲），改的是**演示者自己的预期**：新增「要有心理准备」一行，因为**被自己的产品提前做完打断，比等太久更容易乱**。

---

## 1. 开场前 10 分钟

照 `docs/demo-preflight-checklist.md` **C 段**跑那六条（会场网络 health、App 起来、抖音登录态、磁盘、兜底成片能播、勿扰模式）。约 6 分钟。

**任何一条不过，就不要按本 runbook 的原脚本开演**，直接翻到 §5 选一条降级路径。

另外做两件 runbook 专属的准备：

- **把窗口调到合适大小**，字号够大，投屏能看清；
- **想好第一句演示词的那句「一句话描述」**，别现场憋。建议提前写好两三句备选，见 §3.2。

---

## 2. 演示脚本

### 幕 0 · 打开与定位（约 1 分钟）

**动作**：打开 App，停在工作台。

**说什么**（大意，不必逐字）：

> 这是一个本地运行的运营自动化工具。它装在您自己的电脑上，浏览器操作、视频渲染都在本机完成，账号和素材不上传第三方。左边这几项是它现在能做的事——今天重点看「视频制作」这一块。

**用户看到**：左侧八项导航（工作台 / 新建任务 / 任务记录 / 视频制作 / 视频剪辑 / 作品发布 / 平台状态 / 设置与诊断）。

首页的「最近任务」每行叫「**07-26 21:33:53 的任务**」这种按时间的名字——可以顺口带一句「每次跑过什么都留着记录」。

**⚠️ 这一幕里不要点开「视频剪辑」**，理由见 §4。

**⚠️ 也不要展开首页下面那个「诊断信息」**。它折起来是有意的：里面是 `Revision`、`事件水位` 这类给排查用的字段，展开只会请客户问一个你不想在这里回答的问题。

**失败预案**：App 打不开或白屏 → 直接走 §5 降级路径 A。**不要在客户面前反复双击。**

〔07-27 改〕补两句。`T93`（`ec17ae0`）把首页的 `Revision` / 事件水位折进了默认收起的「诊断信息」，最近任务也从裸 UUID 改成了按创建时间的名字——**前者是新增的一个别点它，后者是新增的一个可以顺口讲的点**。原文两样都没有，因为当时首页还不长这样。

---

### 幕 1 · 提交一句话（约 1 分 30 秒）

**这一幕要尽早做完，因为幕 2 的等待要靠它开始计时。**

**点击路径**：

1. 左侧「**视频制作**」（默认就停在「**新建视频**」标签页）
2. 页面下半部分「选择制作方式」里，点那张卡片上的「**选择品牌动效成片**」按钮
   　→ 按钮上写的是完整的「选择品牌动效成片」，两张卡片各有一个，**别点成旁边那张**
3. 页面**自动把「一句话自动制作」卡片滚进视野**（不用自己找）
4. 在输入框里写一句描述（**上限 500 字**，写一句就够）
5. 点「**开始自动制作**」
6. **App 自己跳到「制作任务」标签页** —— 不要再去手动切

**说什么**：

> 我不需要写脚本、不需要挑素材、不需要自己剪。我只要用一句话说清楚想要什么，剩下的分镜、文案、画面都由模型生成，渲染在这台电脑本地跑完。

（点下按钮后）

> 它现在开始工作了，大概三分钟——模型要先把这句话拆成分镜、文案和整套画面，然后本机一帧一帧渲染出来再合成视频。您看这里有个计时器，它自己会说这次跑了多久、正常范围是多少。

**用户看到**：页面自动切到「制作任务」，上面一行走字的状态：

```text
正在自动编排这条视频 · 已用 0 分 8 秒 · 通常 2 分钟左右，最长约 3 分钟
```

**这行字是你的朋友，指给客户看。** 它同时回答了「还活着吗」和「这算正常吗」——不用你去解释。

超过 178 秒之后它会自己换成「已经超过实测最长的 3 分钟，可能是视频创作模型服务没有回应」。**那不是失败通知**，是它诚实地说「超出我见过的范围了」。真到了那一步，按 §5 降级路径 B 处理。

**关键信息（要主动说，别等客户问）**：

> 这个入口生成的是**固定 12 秒**的片子。需要别的长度，用下面那个「固定模板手工制作」，段数和每段时长都能自己调。

**为什么要主动说**：界面上已经明示了这一点，客户自己会看到。**由你先说出来，是产品有边界；被客户先发现，是产品有缺陷。**

**失败预案**：

| 现象 | 现场怎么办 |
| --- | --- |
| 提示「请先到"设置与诊断"配置视频创作模型服务。」 | **不要现场配密钥。** 直接走 §5 降级路径 B（放彩排成片） |
| 「本机渲染组件暂时不可用」 | 走 §5 降级路径 B |
| 「自动编排没能完成…视频创作模型服务可能连不上…」 | 模型服务侧的问题，**不是描述的问题**。走 §5 降级路径 B，**不要试图改一句话再跑一次** |
| 🆕 「自动编排没能完成……**这不是描述的问题，是我们这边出错了**，请重试」 | 我们程序自己的毛病。**重试一次就够**，仍然一样走 §5 降级路径 B。**不要改描述** |
| 🆕 「视频创作模型服务已经接上，但**连续 6 分**没有再返回内容……」 | 模型接上之后不说话了。走 §5 降级路径 B |
| 按钮点了没反应 | 等 5 秒，再点一次；仍无反应走 §5 降级路径 B |
| 描述被拒（提示要先描述） | 输入框是空的，补一句再点 |

> **🆕 一条现场就能用的判断规则**：看报错里有没有「**这不是描述的问题**」这句话。
>
> **有这句 → 别动那句描述**，问题不在它。以前有一批纯粹是程序自己接错线的失败，也照着同一个模板说「请换一句更具体的描述后重试」，演示者会当场去改描述，越改越错，而那句描述从头到尾根本没被读过。现在它们改口了。
>
> 唯一**真该**换描述的，是「这条成片的画面自始至终没有变化」那一条——见幕 3。

〔07-27 改〕四处：
> ① **点击路径错了两步**：按钮实际叫「选择品牌动效成片」（原来写「点选品牌动效成片」，听起来像点卡片本身）；提交后 App **自动跳页**，原来写的「可到制作任务查看进度」会让演示者多做一次没必要的操作。
> ② **原来引用的提示原文「已提交一句话自动制作，可到"制作任务"查看进度。」在代码里已经不存在了**。现在等待期显示的是那行带计时器和参照范围的字，改成指着它讲反而更好讲。
> ③ 「大概要三分半钟」改成「大概三分钟」，依据见 §0。
> ④ 新增「自动编排没能完成」这一行。这是 T90 改掉的那条缺陷：**模型服务连不上以前会被说成「你的描述做不出来」**——演示者照旧文案会当场去改那句话，越改越错，而问题根本不在那里。

---

### 幕 2 · 那一两分钟讲什么（**可能只有 1 分钟，按随时被打断准备**）

**这是整个演示的设计难点，也是最容易翻车的地方。**

**核心思路：留在「制作任务」页上讲，不要切走。**

这一点上一轮改过了。原来的编排是「提交完立刻走开去讲别的」，理由是让等待变成议程的一部分。现在改成不切页，两个原因：

1. **等待变短了。** 而且**这一轮又变得更短**：编排那段快了一多半，很可能**一分钟出头就好**。既然如此，走一趟「平台状态 → 设置 → 任务记录」再走回来根本来不及，走到一半被「好了」打断反而更乱；
2. **页面上现在有东西可指。** 那行带计时器和参照范围的字本身就是讲解材料——它替你回答了客户心里那句「是不是卡了」。

> ⚠️ **这一幕现在的主要风险不是「等太久」，是「话没讲完就好了」。**
>
> 下面那张表按优先级从上往下排，**讲到哪算哪**。成片一好就收，不要为了把话讲完而让客户干等着一个已经做好的视频。没讲到的内容留到幕 3、幕 4 顺带带过。

**切走本身已经不再有风险**（`a109178` + `7c5d5d6` 修掉了「切页丢任务、丢句子、丢失败」，并有 e2e 用例守着；渲染阶段失败也会在侧边栏亮红色「失败」角标）。**但不切页是零成本的，所以默认就不切。**

**推荐编排（站在「制作任务」页上讲，按重要性排序，讲到哪算哪）**：

| 顺序 | 讲什么 | 眼睛看哪里 |
| --- | --- | --- |
| 1（约 20 秒） | 指着那行走字的状态，说明它在干什么、正常要多久 | 「制作任务」标签页那行计时 |
| 2（约 40 秒） | **平台登录与人工接管**：这个工具用的是 App 自己的独立浏览器档案，不碰你日常用的 Chrome，也不读你的 Cookie；扫码登录一次就记住；遇到验证码、滑块、风控，它会停下来交给人，不会替你绕过 | **口头讲，不切页**。客户要求看再切到「平台状态」 |
| 3（约 30 秒） | **密钥都在本机**：模型密钥保存在这台电脑的受保护存储里，页面上永远不回显 | 口头讲 |
| 4（有多少讲多少） | **任务留痕**：每个任务什么时候跑的、做了什么、结果如何都有记录 | 口头讲，讲完看计时收尾 |

**第 1、2 项是必讲的**（前者解释屏幕上正在发生什么，后者是这个产品最硬的差异点）。**第 3、4 项讲不完就算了**，幕 3、幕 4 里都有地方能补回来。

**如果客户明确要求「让我看看那个平台状态页」**：切过去没问题，任务不会丢。看完切回「视频制作 → 制作任务」即可。**万一在你切走期间失败了**，左侧「视频制作」那一项会挂一个红色的「**失败**」角标（不是小蓝点），你不会错过。

**其他注意**：

- **不要在这段时间点「测试连接」**。它要发真实请求，多一次网络往返就是多一个失败点，而且此刻你的注意力在讲话上。
- **不要点「安全注销」**（§4）。
- 如果客户问「怎么这么慢」——**别道歉，给数字**：

  > 这一分多钟里，我们本机总共只花了十毫秒。其余全是模型在写——它要把这一句话拆成分镜和文案。之后本机把画面一帧一帧渲染出来再合成，大概三十秒。这台电脑没有连任何渲染农场，全部本地跑完。

  **⛔ 不要再说「模型还要把整套画面动效一起写出来，光这一项就占它输出的七成」。** 那是改版之前的事实——**现在模型根本不写画面了**，画面由本机按模板画，这也正是它变快的原因。照旧台词讲，是在客户面前说一件已经不成立的事。

**失败预案**：超过 **4 分半**仍未完成 → 别再等了，走 §5 降级路径 B，把话接成「我们先看一条之前生成的」。

〔07-27 改〕四处：
> ① **默认编排从「切走去讲别的」改成「留在原页讲」**，理由写在上面。原来那套的前提是三分半的等待，现在等待只有两分钟出头。
> ② 原来挂着的那个待确认前提（「切走会不会中断」，清单 D6）**已经有答案了：不会中断，已修且有用例**。所以那句「如果彩排发现切走会中断就不能用这套编排」删掉了——它问的那件事不再是未知数。
> ③ 「怎么这么慢」的解释口径换成有依据的版本。原来说「大部分是模型在写分镜和文案，渲染只占三十几秒」——方向对，但漏掉了**最占时间的那一项**（模型还要写整份动效画面，占它输出的 70%），也没有「本机只花十毫秒」这个最有说服力的数。
> ④ 超时阈值从 6 分钟收到 4 分半，跟 §0 的备用余量对齐。

〔07-27 二轮〕三处：
> ① **幕标题和时间预算从「两三分钟」改成「一两分钟，按随时被打断准备」**，讲解表也从固定时段改成按重要性排序、讲到哪算哪。这一幕现在的主要风险已经不是等太久，**是话没讲完视频就好了**。
> ② **上一轮刚补上的那句「占它输出的七成」，这一轮就作废了**——改版之后模型不再写画面。台词已换，并明确标了「不要再说」。**这正是为什么清单和 runbook 必须逐条重核而不是读一遍**：上一轮补进来的依据，隔一轮就可能因为产品变了而变成假话。
> ③ 「这两三分钟里」改成「这一分多钟里」，跟新的耗时对齐。

---

### 幕 3 · 成片播放（约 2 分钟）

**点击路径**：「视频制作」→「**成片**」标签页 → 找到刚才那条 → 点「**播放成片**」。

**说什么**：

> 这就是刚才那一句话生成的结果。整个过程里我没有写脚本、没有找素材、没有开剪辑软件。

**用户看到**：App 内嵌播放器播放一段 12 秒、画面在动的视频。

**可以顺带说的**：

> 生成出来的片子就在这里，可以直接进入发布流程，也可以导出后自己再加工。

**失败预案**：

| 现象 | 现场怎么办 |
| --- | --- |
| 「暂时无法读取这条成片」 | 点彩排那条成片播放（它前一天验过能播） |
| 画面自始至终不动，提示换一句描述 | 这是静图门禁拦下的。**照实说**：「它检测到画面没有变化，主动停下来了，不会给你一个假成品。」然后放彩排那条 |
| 播放器黑屏但有时长 | 换彩排那条 |

> 「画面没变化就拒绝交付」这件事，**如果发生了就大方讲出来**——一个会承认自己没做好的产品，比一个交付废片的产品更可信。

---

### 幕 4 · 发布链路（约 1 分 30 秒，**只讲不做**）

**点击路径**：左侧「**作品发布**」。

**说什么**：

> 成片可以从这里发布到平台。它的流程是：选好视频、填标题和简介，然后会**先弹出一个确认页，把要发到哪个账号、发什么内容完整列给你看**，你点确认它才真的发。这个确认步骤是不能跳过的。

**用户看到**：发布页、待发布视频卡片、标题与简介输入框、「发布到抖音」按钮。

**⛔ 到此为止。可以填标题和简介，可以点「发布到抖音」让确认卡片出现，但绝对不要点「确认发布」。** 详见 §4。

**如果客户说「那你发一个试试」**：

> 这台演示机连的是我们的测试账号，真发出去就是一条真实作品。我们不在演示里往真实平台投稿。回头在您自己的账号上，我们陪着走一次完整的。

**⚠️ 注意（写给准备演示的人，不是台词）**：产品**没有「草稿箱」这个选项，也没有任何「先存起来不发」的降级路径**。

这不是「暂时没做」，是**产品在契约层面就没有这个状态**。发布结果的取值集合由 `contracts/publishing/publish-workspace.v1.json` 定死，全部五种是：

```text
published            已发布
outcome_uncertain    结果待人工确认
not_published        未发布
handed_off           需要你在浏览器里处理
cancelled            已取消
```

**里面没有 draft。** 曾经有过「发布一律走草稿箱」的约束，但它**从未实现**。

所以：**点了「确认发布」就是真投稿，没有安全网、没有后悔按钮、没有「其实只是存了个草稿」这种可能性。** 演示前请确认自己完全理解这一句。见 §4。

〔07-27 改〕原文只说「产品没有草稿箱这个选项，代码里不存在」。这次补上了契约层的证据（五种 outcome 逐个列出，确认没有 draft），因为**这一条弄错的后果是在客户面前真发一条内容出去**——它值得有比「我记得没有」更硬的依据。

---

### 收尾（约 1 分钟）

**说什么**：

> 今天看到的是一条完整链路：一句话进去，一个能用的视频出来，接着可以走发布。这些都在您自己的电脑上跑，账号和素材不经过第三方。

**动作**：**停在成片页或工作台，不要注销、不要关 App。** 客户可能会问着问着又要看一眼。

---

## 3. 演示素材建议

### 3.1 那条「一句话」怎么写

品牌动效成片产出的是**文字动效 / 数据可视化**风格，不是带实拍素材的口播视频。**描述要往这个方向写，别让客户期待错。**

**好的方向**：产品亮点罗列、数据增长说明、活动预告、要点总结。
**不好的方向**：「拍一段员工在办公室工作的画面」——这条线做不出实拍素材。

### 3.2 备选描述（提前写好，别现场憋）

- 「用蓝色商务风做一段本周销售增长说明」
- 「用品牌动效介绍新品的三个亮点」
- ⬜ 第三句：**贴合客户行业的那一句** ← **强烈建议现场化一句**，效果比通用句子好得多；**但必须在彩排时先跑通它**，别把没试过的句子留到现场。

---

## 4. 不要演示什么

**下面每一项都不要出现在演示里。理由写清楚了，是为了让你判断客户临场提问时怎么回。**

| 功能 | 位置 | 为什么不演 | 客户问起怎么说 |
| --- | --- | --- | --- |
| **安全注销** | 平台状态 → 抖音卡片 | `T50` 已把原来的 5 秒过早超时对齐到完整 60 秒预算，代码与契约门禁已通过；但修改后尚未用真实抖音账号复验「点击注销 → 权威态 missing → 页面成功」。在补齐这条证据前仍不放进演示脚本，**不再沿用旧稿的故障结论**。⚠️ **但它现在是某个状态的唯一出路**，见本表下面那个框 | 「注销和账号管理我们单独看，今天先聚焦生成这条线」 |
| **确认发布到抖音** | 作品发布 → 确认发布 | **会真的投稿**。产品**在契约层就没有草稿状态**（五种 outcome 里没有 draft），点下去没有安全网 | 见 §2 幕 4 的台词 |
| **视频剪辑** | 左侧导航「视频剪辑」 | 两条：① 云端剪辑装配未完成，前端还走本机 `sessionStorage` 网关，页面会明说「云端剪辑功能尚未开通：时间轴修订会保留在本机，接入云端剪辑服务后才能提交剪辑任务」；② 这一页在 1280×800 下**比视窗高 7px**，一打开就多一条滚动条（`T100`，未修） | 「剪辑是下一阶段的能力，界面先占了位」 |
| **检查更新 / 自动更新** | 设置与诊断 | 包里更新是关闭状态（feed 地址是 `.invalid` 保留域名）。**现在它显示的是中性的「此版本未启用自动更新」，不再是红色失败**——所以它不难看，但**仍然不要点**：点了不会有任何有用的事发生 | 「演示包关掉了自动更新，避免演示中途弹更新」 |
| **智能素材成片** | 视频制作 → 制作方式 | 素材源 Key 未配置，上游在空列表时直接抛错；这条线一次都没走通过。**注意理由只剩这一条了**：它的完整制作页原来「整页点不动、深色输入框压在白底上」，那两个毛病已经修好，页面现在能点、也是产品配色——**但素材源那个硬阻塞没变** | 「今天演的是本机渲染这条线，不依赖外部素材站」 |
| **背景音乐** | 智能素材成片的完整制作页 | **已决定本期不做**（用户 07-27 拍板「先去掉就行」）。⚠️ **但已经装在演示机上的那个包里，那三个选项还在，而且没有任何说明**——去掉它的代码写好了却没进包（出包缓存的缺陷，已修根因）。**下一个包里才会真的消失。** 这一页本来就不演，所以影响有限。**不要主动提起** | 客户问才答：「音乐这一块本期先不做」 |
| **任何需要断网恢复的演示** | — | 没有降级路径 | — |
| **第三方软件声明 / 开源软件许可** | 设置与诊断页底部 | 法定必需页，但会列出上游项目名，与产品品牌边界冲突。另有 `T99`（折叠控件把英文 `collapsed` 读进可访问名，未修） | 不主动打开 |

> **⛔ 有一种状态会让「安全注销」从禁区变成唯一出路，必须知道**
>
> **如果 App 在运营浏览器正开着的时候被强制杀掉**（强制退出、断电、崩溃），磁盘上会留下一个「这个档案还在用」的占用标记，没有人来清它。此后「平台状态」页上的「打开登录处理」和「我已处理，重新检查」**每次点都失败，永远失败**，界面会明说要走「安全注销」——**那确实是唯一的出路，重试多少次都没用**。
>
> **在演示当天撞上它，处理顺序是**：
>
> 1. **别在客户面前处理。** 平台登录那一段本来就是口头讲的（幕 2 第 2 项），跳过展示，演示继续；
> 2. 演示结束后再走「安全注销」→ 重新扫码登录；
> 3. **预防比补救便宜得多**：前一天做完清单 B7 之后，**用正常方式退出 App**，别强制退出、别直接合盖断电，并确认运营浏览器窗口已经关掉。

〔07-27 改〕三处：
> ① **「检查更新」那一行的理由变了**。原来写「点了必然失败」，隐含它会显示红色错误——`T89`（`b6dce90`）新增了独立的 `disabled` 状态，把「这个构建没开更新」从 `failed` 里搬了出来，现在显示的是中性文案。**结论不变（仍然不点），但理由和客户看到的东西都变了**：如果演示者按旧文案以为那里会红，看到中性文案反而会以为哪里不对。
> ② 「视频剪辑」补上第二条理由（`T100` 的 7px 溢出），并写出页面上的完整原文。
> ③ 「安全注销」原来沿用的是修复前结论；C11 已按 T50 证据改正为「代码已修、待真实账号复验」，在复验前仍不演示。

〔07-27 二轮〕三处：
> ① **新增「安全注销从禁区变成唯一出路」那个框**。这是用户真撞上的状态，上一版完全没有它，而它同时踩了两条：一个必须做的动作，恰好是禁区里的功能。
> ② **「背景音乐」那一行原来写「当前版本不提供（界面已说明）」——对已经装在演示机上的那个包来说，这句话是错的**：控件还在，而且没有任何说明。去掉它的代码早就写好了，只是没进包。顺带把「在评估」改成「本期先不做」，因为这件事已经拍板了，不再是待决策。
> ③ 「智能素材成片」补一句：不演的理由现在**只剩素材源**那一条了，窗口本身的两个毛病（点不动、配色不对）已经修好。

### ⛔ 演示机上绝对不要跑的东西

**不要在演示机上运行本仓库任何 `scripts/run_*_acceptance.py`**，尤其 `scripts/run_u9_06_acceptance.py`——**它会删掉整个 App 私有数据目录**，包括抖音登录态和彩排成片。

**演示机只用来演示，不用来验证。** 哪怕出于「我验证一下环境」的好意也不行。

---

## 5. 降级路径

**判断标准：任何一步卡住超过 30 秒，就切降级，不要在客户面前 debug。**

### 路径 A — App 起不来 / 白屏 / 闪退

**先别修。** 按顺序试：

1. 完全退出 App，重新打开（一次，只一次）；
2. 仍不行 → **改用讲解 + 现成视频文件**：把彩排那条 mp4 从访达里直接播放，讲 §2 幕 1/3 的内容；
3. 同时安排技术支持在后台查（磁盘满？数据目录权限？），**但演示继续**。

**台词**：

> 这台机器上有点环境问题，我们先看生成出来的结果，具体环节我边放边讲。

### 路径 B — 生成失败或超时

**用彩排那条成片顶上。**

**点击路径**：「视频制作」→「成片」→ 找到彩排那条 → 「播放成片」。

**台词**：

> 这条是我们之前用同样的方式生成的，流程完全一样。

**不要说**「刚才那条失败了」。也不要重新提交一次——**失败过一次的东西没有理由第二次就好**，而你已经用掉了时间预算。（〔07-27 二轮〕原文的理由是「第二次同样要三分半」，那个数已经不对了；但**结论不变**，而且换成现在这个理由更硬——就算它只要一分钟，重试也是在赌。）

### 路径 C — 会场网络不通

**这是最难受的一种，因为一句话生成需要调模型，必须联网。**

1. 先切手机热点重试（**提前把热点准备好、连过一次**）；
2. 仍不通 → 全程改为**讲解 + 播放现成成片**，跳过幕 1、幕 2；
3. 平台状态、设置页、任务记录这些**本地页面仍可正常展示**，讲解材料够用。

**台词**：

> 现场网络到我们服务的链路不太稳，生成这一步要调模型必须联网。我先把结果和流程讲清楚，回头在您那边网络环境下我们完整跑一次。

### 路径 D — 抖音登录态掉了

**不要现场扫码**（掏手机、找入口、二维码过期，全在客户面前）。

幕 2 默认就不切页，所以这条基本不影响脚本：**平台登录那一段照讲不误，只是别切到「平台状态」页去展示**。如果客户主动要求看，就说「平台连接我们单独找时间过一遍」，把话头带回生成这条线。

**🆕 如果已经切过去了，而且点按钮报「上一次运营浏览器没有正常收尾。请先执行『安全注销』……」**——那就是 §4 那个框里说的永久失败状态。**当场不要处理**（唯一的出路是禁区里的那个功能），按上面这段跳过展示，演示结束后再收拾。

〔07-27 改〕原文写「跳过**幕 4** 的「平台状态」展示」——**幕号写错了**，平台状态在幕 2，幕 4 是发布链路。顺带按幕 2 改成不切页后的新编排重写。

〔07-27 二轮〕补上那个永久失败状态在现场怎么绕过去。

---

## 6. 演示后

- **不要注销**，不要清数据——留着下次演示或复盘用；
- 把这次的实测耗时、出过的岔子、客户问过的问题记下来，回填到本文件和清单；
- 如果演示中触发了任何**未在 §4 列出的**报错，记录**原始报错文案**（截图最好），这是最有价值的反馈。

---

## 7. 本文件里还不确定的地方

| # | 待确认 | 影响哪一段 | 谁去确认 |
| --- | --- | --- | --- |
| **R1″** | **新包尚未构建，而且现在连那个不合格的旧包都没了** | **全部**。已装的那个 App 界面落后 **62 笔**提交；20:52 那个 DMG 文件已从磁盘上消失。出包前先看清单 A9（解锁屏幕）和 A10（留时间 + 字体那步要重试） | 发版线（清单 D1′） |
| R2 | 演示机上的实际生成耗时 | §0 速查表、§2 幕 2 的时间编排 | 彩排（清单 B8）。**比以前更要紧**：连开发机的基线都是推算的 |
| R4 | 谁来操作键盘、懂不懂技术 | 决定 §5 降级路径要写多细、要不要现场配技术支持 | 项目负责人 |
| R5 | 客户行业定制的那句描述 | §3.2 | 演示负责人，**必须彩排跑通** |
| R7 | 会场网络情况 | §5 路径 C 要不要提前准备热点 | 演示当天，越早越好 |
| **R9** 🆕 | **正式包上按一次取消，看到的是不是「正在取消 → 已取消」** | 幕 2（万一手滑点了取消，要认得出这是正常的中间态） | 彩排时顺手做一次（清单 D14） |
| **R10** 🆕 | **正式包上抖音那两个按钮，在「已登录 + 重启过」之后点得通吗** | §2 幕 2 的平台登录讲解要不要切页展示、§5 路径 D | 新包出来后点一次（清单 D15） |

### 已结案

| 原编号 | 原来的问题 | 现在的答案 |
| --- | --- | --- |
| R1 | 带 JIT 修复的新包是否已就绪 | **已就绪**。包内两个 `node` 都能求值，签名公证四项全过（清单 A1、A4）。**但那个包另有两个问题**，所以换成了 R1′ |
| R1′ | 含界面修复与 DMG 修复的新包尚未构建 | **仍未构建，且情况更差了**——那个不合格的旧包本身也没了。换成 R1″ |
| R3 | 生成中切走页面再切回，任务是否继续 | **继续，且已修**（`a109178` + `7c5d5d6`），有 e2e 用例守着。**但幕 2 已改成默认不切页**——不是因为有风险，是因为不切是零成本的 |
| **R6** | 是否补背景音乐 | **已决策：本期不做**。用户 07-27 拍板「暂时先不做，后面再说，先去掉就行」。§4 那一行已按此改写，并写清「已装的包里它还在，下一个包里才会消失」 |
| **R8** | 抖音登录态跨 App 重启是否保住 | **已完成，保住了**。用户 07-27 在正式包上退出再打开，仍是「登录正常」。**但同一次操作挖出了那两个按钮的问题**，已另立 R10 |

> **R1″ 是阻塞级，但性质和原来的 R1 不一样。**
>
> 原来的 R1 是「品牌动效成片和抖音**能不能跑**」——那是产品死活的问题，现在已经确认是活的：签名包上真实出过片、真实扫码登录过。
>
> R1″ 是「**客户拿到的那个包是不是这个**」——代码全在主线，缺的只是再出一次包。风险从「功能可能不存在」降级成「发布动作还没做」，但**在那个包出来并跑完清单 A 段之前，本 runbook 仍然不可用**：已装的那个包客户装不进去，装进去了也是六十多笔提交之前的界面。
>
> **而且出包这件事本身现在有三个坑**，都在清单 A 段：屏幕锁着会让公证失败并报一句像是凭据丢了的话（A9）；三样构建期产物这次全要重做，其中一样是从源码现编的（A10）；中途要从境外取字体，国内网络下会瞬时失败、重试即过（A10）。**别把出包安排在演示当天早上。**

〔07-27 改〕R1 与 R3 结案移出，新增 R1′ 与 R8，并写清 R1′ 与原 R1 在性质上的差别——**「跑不通」和「还没发布」都是阻塞，但补救成本和该找谁差着一个数量级**，混在一起会让人判断错优先级。

〔07-27 二轮〕R6 与 R8 结案移出（一个已拍板、一个已做完）；R1′ 升级成 R1″（旧包也没了，落后从 40 笔变 62 笔）；新增 R9、R10；并把出包本身的三个坑写进 R1″ 的说明——**上一版把「再出一次包」当成一个纯粹的执行动作，而这一轮的实践证明它自己就会失败**。
